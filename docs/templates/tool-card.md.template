# {{tool-name}}

## Purpose
<!-- One or two sentences: what it does and the decision it lets a caller make. Not how. -->
{{What it does, and what a caller does with the answer.}}

## Invoke
<!-- A fenced command line. The script's own position is written {script} — never a literal path. Other placeholders are {like-this}. Optional arguments in [brackets]. -->
```
python3 {script} --in {file} [--out {file}] [--self-test]
```

## Inputs
<!-- Every long option the parser defines, in backticks, or the validator fails. Say Required or Optional plus the default. Nothing here that the parser does not define. Never document the flag argparse adds for you. -->
- `--in {file}` — {{what the input is}}, `-` for stdin. Required unless self-testing.
- `--out {file}` — report destination. Optional, default stdout.
- `--self-test` — run the built-in fixtures and exit. Optional.

## Outputs
<!-- Every file written, the stdout shape, and every exit code the script can return, each in backticks. An undocumented code fails the validator. Never write a backticked integer inside a comment. -->
- Files: `{out}` — {{what the report holds}}.
- stdout: `{{tool-name}}: records=N findings=F -> path`
- stderr: one line per finding.
- Exit: `0` {{clean}} · `1` {{at least one finding}} · `2` {{missing input, unreadable, or not the expected format}}.

## When NOT to use
<!-- Three bullets: the neighbouring job it is not, the input shape it will not take, and the judgement it deliberately refuses. -->
- {{The adjacent tool's job.}}
- {{An input shape it rejects.}}
- {{What it never judges, and why that is deliberate.}}

## Cost
<!-- Model calls (always zero) plus the real runtime driver. For a network tool, state the request count and the rate limit instead of milliseconds. -->
Zero model calls. {{Milliseconds; one pass over the input.}}
