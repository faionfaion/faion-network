<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="go-backend Decision Tree" version="1.0.0" est_tokens="500">
  <decision-tree>
    <root-question>Is this a Go HTTP service that can adopt the cmd/ + internal/ layout?</root-question>
    <branch when="service_type == 'http' AND layout_supported == true">
      <question>Can every exported I/O function take ctx context.Context as the first arg?</question>
      <branch when="context_propagation_possible == true">
        <question>Is the team willing to centralize error handling in one ErrorHandler middleware?</question>
        <branch when="one_error_handler_supported == true">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="one_error_handler_supported == false">
          <conclusion ref="r8"/>
        </branch>
      </branch>
      <branch when="context_propagation_possible == false">
        <conclusion ref="r5"/>
      </branch>
    </branch>
    <branch when="service_type != 'http' OR layout_supported == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="new Gin service, ctx everywhere accepted, ErrorHandler middleware accepted -> run-the-checklist"/>
  </decision-tree>
</text>
