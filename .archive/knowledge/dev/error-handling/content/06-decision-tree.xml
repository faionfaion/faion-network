<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="error-handling Decision Tree" version="1.0.0" est_tokens="500">
  <decision-tree>
    <root-question>Is the API JSON-over-HTTP REST (not gRPC, GraphQL, or streaming)?</root-question>
    <branch when="protocol == 'rest-json-http'">
      <question>Can the team install one framework-level exception handler?</question>
      <branch when="single_handler_supported == true">
        <question>Will the team commit to stable type URIs and the application/problem+json content-type?</question>
        <branch when="contract_commitment == true">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="contract_commitment == false">
          <conclusion ref="skip-this-methodology"/>
        </branch>
      </branch>
      <branch when="single_handler_supported == false">
        <conclusion ref="r8"/>
      </branch>
    </branch>
    <branch when="protocol != 'rest-json-http'">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="FastAPI service with global exception handler, stable URIs accepted -> run-the-checklist"/>
  </decision-tree>
</text>
