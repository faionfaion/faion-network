{
  "type": "object",
  "required": ["hook", "body"],
  "properties": {
    "hook": {"type": "string", "description": "Bold opening line (HTML)"},
    "body": {"type": "string", "description": "Full TG post text (HTML, under 900 chars)"}
  }
}
