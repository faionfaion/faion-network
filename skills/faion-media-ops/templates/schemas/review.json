{
  "type": "object",
  "required": ["approved", "feedback", "score"],
  "properties": {
    "approved": {"type": "boolean"},
    "feedback": {"type": "string", "description": "Specific feedback for revision if not approved"},
    "score": {"type": "integer", "minimum": 1, "maximum": 10}
  }
}
