<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="Agent Postmortem Template (Agent-as-Defendant) Core Rules" version="1.1.0" est_tokens="700" depth="essential">
  <summary>6 testable rules every application of agent-postmortem-template enforces. Each rule has a statement, a rationale anchored to industry practice, and a source citation.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every postmortem records the exact model identifier (provider plus version pin, e.g. anthropic/claude-sonnet-4-5-20250929) and deployment timestamp. 'Claude' or 'GPT-4' fails the template gate.</statement>
      <rationale source="Industry practice 2026">Half of agent regressions are silent model upgrades. Without a pinned version string the postmortem cannot distinguish a prompt bug from a provider-side model swap.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every postmortem includes a byte-exact prompt diff: system prompt at the time of incident vs system prompt at the last green eval run, with all variables substituted.</statement>
      <rationale source="Industry practice 2026">Prompt regressions are the #1 cause of agent incidents and are invisible without a rendered byte-diff.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Layer Attribution assigns root cause to exactly one of six layers (model, system prompt, tool schema, tool result, retrieved context, user input). If the team cannot pick one, escalate to a second reviewer; do not close.</statement>
      <rationale source="Industry practice 2026">'Combination of factors' forecloses corrective action; forcing a single primary layer routes the fix to the owner who can act.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Eval delta is recorded: score on the regression case before the fix, target score after. Closure requires the after-fix eval to be green.</statement>
      <rationale source="Industry practice 2026">Without eval delta the fix is theatre; eval-gated closure prevents same hallucination class re-shipping.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Replay artefacts (rendered prompt, tool-call payloads, tool results, retrieved chunks, response) are attached as files or trace links. If a layer's artefact cannot be replayed, the postmortem records a Trace Gap with an explicit instrumentation action item.</statement>
      <rationale source="Industry practice 2026">Replay is the difference between a postmortem and a story.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>When the incident is a pure infra outage (DB down, network split, quota exhaustion) and no agent decision sits on the causal path, the team MUST NOT open an agent-as-defendant postmortem. It MUST route the incident to the generic SRE postmortem template and record, in the incident ticket, the trace_id that was inspected plus the one-line finding 'no agent decision on the causal path'. If no trace_id could be inspected, that finding MUST NOT be asserted; the incident is routed as unknown and a Trace Gap action item is filed per r5.</statement>
      <rationale source="Google SRE Book, ch. 15 'Postmortem Culture: Learning from Failure'">Model pin, prompt diff and six-layer attribution are mandatory fields in this template; on a pure infra outage they cannot be filled, so the report either lies or stalls. Routing to the SRE template keeps the incident on a template whose fields can be filled, and recording the inspected trace_id makes the routing decision itself auditable instead of a guess that hides an agent-mediated cause under 'infra'.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
    <link path="content/06-decision-tree.xml" />
  </see-also>
</text>