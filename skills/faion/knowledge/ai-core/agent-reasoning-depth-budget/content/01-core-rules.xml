<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="Agent Reasoning Depth Budget Core Rules" version="1.1.0" est_tokens="600" depth="essential">
  <summary>6 testable rules every application of agent-reasoning-depth-budget enforces. Each rule has a statement, a rationale anchored to industry practice, and a source citation.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Start with the smallest budget the provider allows (or 1024 tokens); double until eval-score gain ≤2% per doubling; stop.</statement>
      <rationale source="Industry practice 2026">Plateau detection prevents paying for thinking that no longer moves the score; exponential sweep finds the knee in ≤6 runs.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Record budget × score × cost for at least 4 budgets; pick the smallest budget within 2% of the best score (Pareto pick).</statement>
      <rationale source="Industry practice 2026">Best-score pick over-spends; Pareto pick captures 95% of value at ~50% of cost.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Under-thinking detector: ≥30% of failures come from reasoning that stopped before a known sub-step. Over-thinking: judge-score plateau ≥1024 tokens before final answer.</statement>
      <rationale source="Industry practice 2026">Two detectors, two fixes: under → raise budget on those task types; over → compress or distill.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Inter-turn compression: at turn N&gt;2, prior thinking traces are summarized to ≤256 tokens before being re-included.</statement>
      <rationale source="Industry practice 2026">Without compression, multi-turn agents waste their context on stale reasoning at turn 5+.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Re-evaluate budget after every model version upgrade; old budgets may over-pay or under-think on the new model.</statement>
      <rationale source="Industry practice 2026">Reasoning efficiency changes between model versions; stale budgets degrade quality silently.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>When the agent emits pure output tokens only (no extended-thinking, reasoning-effort or thinking-token parameter is set on any request, and the provider usage record reports zero reasoning tokens), the team MUST NOT run the r1 budget sweep or the r2 Pareto pick. It MUST record in the cost ledger that the reasoning-token line item is zero for this agent, cite the usage field it checked, and bound spend through max output tokens and context size instead. The sweep is re-opened only when a reasoning parameter is enabled on the agent's requests.</statement>
      <rationale source="Anthropic extended thinking documentation; OpenAI reasoning models guide">The sweep varies one input, the thinking budget, and reads score against it; on an agent that has no such input the four runs return the same score and cost, and the resulting budget table is noise that looks like evidence. Every provider exposes the reasoning-token count in its usage record, so the skip is a checkable fact, not an opinion, and the ledger entry stops the sweep being re-proposed at the next cost review.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
    <link path="content/06-decision-tree.xml" />
  </see-also>
</text>