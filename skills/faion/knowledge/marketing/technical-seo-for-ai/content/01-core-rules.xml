<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Technical SEO for AI Core Rules" version="1.0.1" est_tokens="2550" depth="essential">
  <summary>17 testable rules every Technical SEO for AI application must enforce. These are HARD rules — user-supplied input cannot override them. Plus a skip-methodology rule for decision-tree leaves that exit the methodology. Structural requirements for AI-parseable content: heading hierarchy, entity clarity in opening paragraph, factual content with source attribution, Core Web Vitals targets, and a content freshness update strategy.</summary>
  <section title="Background">
    <p>AI crawler identification — the user agents the robots.txt and llms.txt rules below address:</p>
    <list>
      <item>GPTBot: OpenAI's crawler for ChatGPT training and browsing.</item>
      <item>Claude-Web: Anthropic's web-browsing crawler.</item>
      <item>PerplexityBot: Perplexity AI's content indexer.</item>
      <item>Googlebot: indexes for Google SGE (AI Overviews).</item>
    </list>
  </section>
  <section title="Rule set">
    <rule id="r1-llms-txt" testable="true" depth="essential">
      <statement>Every site ships an `llms.txt` at the root with crawler-allow / disallow rules and a content-license note. Missing file on a public-content site = REJECT.</statement>
      <rationale source="Crawler signal">Without llms.txt, AI crawlers default to opaque heuristics; signalling intent is the floor.</rationale>
    </rule>
    <rule id="r2-article-schema" testable="true" depth="essential">
      <statement>Every article page implements Article schema with `author`, `datePublished`, `dateModified`, `headline`, and `image`. Missing any field = REJECT.</statement>
      <rationale source="Structured-data baseline">Incomplete schema produces low-confidence citations.</rationale>
    </rule>
    <rule id="r3-author-credentials" testable="true" depth="essential">
      <statement>Author byline carries machine-readable credentials (sameAs to LinkedIn / GitHub / Google Scholar). Anonymous bylines = REJECT for authority pages.</statement>
      <rationale source="E-E-A-T technical layer">Author authority signals are how AI ranks trustworthiness.</rationale>
    </rule>
    <rule id="r4-clean-url-hierarchy" testable="true" depth="essential">
      <statement>URL structure is shallow (≤3 segments), human-readable, and matches the breadcrumb hierarchy. Cryptic URLs = REJECT.</statement>
      <rationale source="Crawler navigability">URL structure is a first-pass authority signal.</rationale>
    </rule>
    <rule id="r5-cwv-field-data" testable="true" depth="essential">
      <statement>Core Web Vitals measured on FIELD data (CrUX / PageSpeed Insights real-user) not LAB data. LCP &lt;2.5s, CLS &lt;0.1, INP &lt;200ms. Lab-only measurement = REJECT.</statement>
      <rationale source="Real-user reality">Lab numbers mislead; AI crawlers prefer fast-loading pages.</rationale>
    </rule>
    <rule id="r6-entity-consistency" testable="true" depth="essential">
      <statement>Named entities (products, authors, brand) use the same canonical form in schema, body, and breadcrumbs. Drift = REJECT.</statement>
      <rationale source="Entity reconciliation">AI crawlers reconcile across sources; drift lowers ranking.</rationale>
    </rule>
    <rule id="llms-txt-guides-ai-crawlers" testable="true" depth="essential">
      <statement>Create /llms.txt at the domain root to guide AI crawlers: list important pages, specify citation format, declare allowed and disallowed paths, and provide AI-specific guidance not covered by robots.txt.</statement>
      <rationale source="v1-source">robots.txt governs crawl access; llms.txt governs AI understanding and citation behavior. Without llms.txt, AI crawlers make assumptions about important pages and citation format that may not match your preferences.</rationale>
    </rule>
    <rule id="clean-descriptive-urls" testable="true" depth="essential">
      <statement>Use clean, descriptive URLs: hyphens not underscores, no session IDs, no hash routing, under 100 characters, consistent patterns across content types.</statement>
      <rationale source="v1-source">AI crawlers parse URL structure to infer content hierarchy and topic. Hash-based or session-parameterized URLs are not reliably indexed. Inconsistent patterns reduce AI confidence in entity-to-URL mapping.</rationale>
    </rule>
    <rule id="ai-crawler-robots-rules" testable="true" depth="essential">
      <statement>Configure separate robots.txt rules for AI crawlers to allow public content and block premium, admin, and user-generated content paths.</statement>
      <rationale source="v1-source">AI crawlers should access your authoritative content but not private or paywalled material. Blanket blocking all AI crawlers prevents visibility in AI search results; blanket allowing them risks exposure of private content.</rationale>
    </rule>
    <rule id="article-schema-with-author-credentials" testable="true" depth="essential">
      <statement>Every article must have Article schema with: author (Person type with name and credentials), datePublished, dateModified, and headline. Author schema must reference a credentials page or bio.</statement>
      <rationale source="v1-source">AI systems use schema markup to attribute content to specific authors and assess credibility. Unattributed content (no author schema) is treated as lower authority than content with explicit expert attribution. dateModified helps AI assess content freshness.</rationale>
    </rule>
    <rule id="faq-schema-on-qa-pages" testable="true" depth="essential">
      <statement>Add FAQSchema to pages with Q&amp;A sections. Each question-answer pair becomes a candidate for AI-generated response inclusion.</statement>
      <rationale source="v1-source">AI systems extract FAQ content as direct answers to user queries. FAQSchema makes the Q&amp;A structure machine-readable, increasing the probability that AI-generated responses cite your content for specific questions.</rationale>
    </rule>
    <rule id="consistent-entity-naming" testable="true" depth="essential">
      <statement>Use consistent entity naming throughout all pages: same company name, same product names, same author names across all schema and content. Never abbreviate inconsistently.</statement>
      <rationale source="v1-source">AI entity recognition depends on consistent naming across sources. Inconsistent entity names (full name vs. initials vs. common abbreviation) reduce confidence in entity resolution and may result in separate entities being created for what is the same business.</rationale>
    </rule>
    <rule id="single-h1-entity-in-first-paragraph" testable="true" depth="essential">
      <statement>Use a single H1 per page describing the main topic. First paragraph must define the main entity (who or what the page is about) explicitly. Use consistent entity naming in headings.</statement>
      <rationale source="v1-source">AI crawlers parse heading hierarchy to build a topic model of the page. An ambiguous or missing entity definition in the opening means the AI cannot reliably attribute the content to the correct entity in its knowledge base.</rationale>
    </rule>
    <rule id="statistics-with-source-and-date" testable="true" depth="essential">
      <statement>Present statistics and data in tables or lists with source attribution. Include publication date for all time-sensitive statistics.</statement>
      <rationale source="v1-source">AI systems extract factual claims for use in generated responses. Uncited statistics may be omitted or flagged as unverifiable. Dated statistics help AI assess whether the data is current enough to cite.</rationale>
    </rule>
    <rule id="core-web-vitals-thresholds" testable="true" depth="essential">
      <statement>Maintain Core Web Vitals: LCP &lt; 2.5s, INP &lt; 200ms, CLS &lt; 0.1. These thresholds affect both Google's crawl priority and AI Overview inclusion eligibility.</statement>
      <rationale source="v1-source">Slow pages are crawled less frequently and ranked lower in AI citation priority. Pages that shift layout during load produce unreliable content extraction. Performance is a prerequisite for AI visibility, not an optimization.</rationale>
    </rule>
    <rule id="content-freshness-quarterly" testable="true" depth="essential">
      <statement>Update time-sensitive content at least quarterly. Change dateModified in schema on every substantive update. Archive or redirect pages with statistics older than 24 months that cannot be updated.</statement>
      <rationale source="v1-source">AI systems weight content freshness when selecting sources for generated responses. Outdated statistics that AI cites create trust problems for both the AI system and your brand. Stale pages that cannot be updated should be removed from AI crawler access.</rationale>
    </rule>
    <rule id="author-credentials-visible-on-page" testable="true" depth="essential">
      <statement>Display author name, credentials, and a link to an author bio on every article page. Make credentials visible in the page text, not only in schema.</statement>
      <rationale source="v1-source">AI systems that cannot visibly verify author credentials from page content alone may treat the content as lower authority regardless of schema markup. Schema and visible page content must both carry authority signals.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>Skip this methodology when the decision tree resolves to a skip branch — preconditions are not met OR an adjacent methodology fits better.</statement>
      <rationale source="Decision-tree discipline">Skipping cleanly preserves the integrity of the rubric; misapplied methodologies produce false positives downstream.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
