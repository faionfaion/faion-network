<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Google Performance Max Decision Tree" version="1.2.0" est_tokens="550" depth="essential">
  <summary>Routing tree on observable signals. Every leaf <conclusion ref="..."/> points to a rule id from 01-core-rules.xml.</summary>
  <decision-tree>
    <root-question id="q-root">Is monthly_conversions ≥30 AND conversion API live AND brand negatives ready?</root-question>
    <branch when="any precondition false">
      <conclusion ref="pmax-30-conv-floor" verdict="skip-this-methodology; defer until preconditions met"/>
    </branch>
    <branch when="all_preconds_met">
      <question id="q-feed">Is there a product feed (ecom)?</question>
      <branch when="answer == yes">
        <conclusion ref="asset-groups-3-plus" verdict="run Shopping-blended PMax with product asset groups"/>
      </branch>
      <branch when="answer == no">
        <conclusion ref="asset-groups-3-plus" verdict="run lead-gen PMax with audience-themed asset groups"/>
      </branch>
    </branch>
    <branch when="customer_id contains hyphens OR (mcc == true AND login_customer_id header absent)">
      <conclusion ref="customer-id-unhyphenated" verdict="fix the client: 10-digit customer_id and login-customer-id header before any call"/>
    </branch>
    <branch when="any asset type below minimum count OR final_url scheme == http">
      <conclusion ref="asset-minimums-validated" verdict="complete the asset group; it cannot enter the auction otherwise"/>
    </branch>
    <branch when="target_cpa_or_roas set AND (days_running &lt; 14 OR conversions &lt; 30)">
      <conclusion ref="max-conv-14-days-30-conv" verdict="remove the target; run Maximize Conversions until 14 days and 30+ conversions"/>
    </branch>
    <branch when="daily_budget &lt; 3 * target_cpa">
      <conclusion ref="budget-3x-target-cpa" verdict="raise the budget; the campaign cannot exit learning"/>
    </branch>
  </decision-tree>
  <example trace="saas-pmax-q2">
    95 conv/mo, CAPI live, brand negatives ready, no product feed → audience-themed asset groups.
  </example>
</text>
