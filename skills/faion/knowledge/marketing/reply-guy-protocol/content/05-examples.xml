<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Reply Guy Protocol Worked Example" version="1.1.0" est_tokens="700" depth="essential">
  <summary>One end-to-end worked example of Reply Guy Protocol producing a valid playbook-step artefact, anonymised for portability.</summary>

  <section title="Worked example">

    <step n="1" name="trigger">
      <text>Engagement onboarded; the Reply Guy Protocol methodology was selected after Applies-If was satisfied (named owner Ruslan Faion, downstream consumer engagement charter, source-of-truth in docs/).</text>
    </step>
    <step n="2" name="apply-rules">
      <text>Walked the rules from 01-core-rules.xml. First-pass review surfaced one violation (missing evidence anchor on a non-trivial field); patched and re-ran.</text>
    </step>
    <step n="3" name="produce">
      <text>Filled templates/reply-protocol.md per the schema in content/02-output-contract.xml; emitted artefact_id with date; signed off by the engagement owner.</text>
    </step>
    <step n="4" name="validate">
      <text>Ran scripts/validate-reply-guy-protocol.py --file &lt;path&gt;; validator returned 0; the _smoke-test.md fixture also passed.</text>
    </step>

  </section>

  <example trace="happy-path">
    Applies-If held; inputs reachable; rules walked; artefact filled; validator returned 0; owner signed off; downstream consumer registered.
  </example>
</text>
