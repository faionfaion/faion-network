<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Google Ads Campaign Setup Decision Tree" version="1.2.0" est_tokens="800" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Always start at root-question; conclusions reference rule ids.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do the AGENTS.md applicability preconditions hold for this case?</root-question>
      <branch when="conversion_tracking_firing == false">
        <conclusion ref="conversion-tracking-first" verdict="wire conversion tracking; verify 24-hour firing before campaign create"/>
      </branch>
      <branch when="conversion_tracking_firing == true AND campaign_status == &apos;ENABLED&apos;">
        <conclusion ref="paused-status-default" verdict="create as PAUSED; flip to ENABLED only after checklist"/>
      </branch>
      <branch when="conversion_tracking_firing == true AND campaign_status == &apos;PAUSED&apos; AND display_partners == &apos;enabled&apos;">
        <conclusion ref="explicit-network-disable" verdict="disable Display + Partners explicitly via API"/>
      </branch>
      <branch when="conversion_tracking_firing == true AND campaign_status == &apos;PAUSED&apos; AND display_partners == &apos;disabled&apos; AND rsa_headlines &lt; 15">
        <conclusion ref="rsa-15-headlines-required" verdict="expand RSA to 15 headlines + 4 descriptions before launch"/>
      </branch>
      <branch when="conversion_tracking_firing == true AND campaign_status == &apos;PAUSED&apos; AND display_partners == &apos;disabled&apos; AND rsa_headlines &gt;= 15">
        <conclusion ref="pre-launch-checklist" verdict="run pre-launch checklist; flip ENABLED on pass"/>
      </branch>
      <branch when="campaign_type == &apos;undecided&apos; OR advertiser_history == &apos;new&apos;">
        <conclusion ref="default-to-search" verdict="create a Search campaign; revisit Display / PMax after 30+ conversions/month"/>
      </branch>
      <branch when="budget_amount_micros &lt; 1000000">
        <conclusion ref="budget-micros-check" verdict="unit error; recompute dollars * 1,000,000 before submission"/>
      </branch>
      <branch when="bid_strategy == &apos;TARGET_CPA&apos; AND conversions_accumulated &lt; 30">
        <conclusion ref="max-conversions-before-tcpa" verdict="switch to Maximize Conversions without target until 30+ conversions"/>
      </branch>
      <branch when="extension_api == &apos;Extension service&apos;">
        <conclusion ref="asset-resources-not-extension-service" verdict="recreate sitelinks, callouts, images as Asset resources"/>
      </branch>
  </decision-tree>

  <example trace="conversion_tracking_firing == true AND campaign_status == &apos;PAUSED&apos; AND display_partners == &apos;disabled&apos; AND rsa_headlines &gt;= 15 → conclusion ref=&quot;pre-launch-checklist&quot; verdict=&quot;run pre-launch checklist; flip ENABLED on pass&quot;">
    Reading: evaluate the named observable; follow the matching branch; apply the rule the conclusion references.
  </example>
</text>
