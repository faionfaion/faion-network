<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Ads Analytics Setup Core Rules" version="1.2.0" est_tokens="2800" depth="essential">
  <summary>20 testable rules every application of Ads Analytics Setup MUST enforce — spec-first events, Consent Mode v2, BigQuery link, UTM discipline, verification, GA4 property/event configuration, custom-dimension limits, PII hashing, gtag/GTM/Measurement Protocol install order. Rationale + source per rule. Rules referenced by id from `content/06-decision-tree.xml`.</summary>

  <section title="Rule set">
    <rule id="event-spec-before-code" testable="true" depth="essential">
      <statement>Event specification (name, params, trigger, owner) MUST be written before any tracking code. Code-first implementations are forbidden.</statement>
      <rationale source="Google Analytics event-spec docs">Code-first events accumulate inconsistency; spec-first events stay coherent over years.</rationale>
    </rule>
    <rule id="consent-mode-v2-mandatory" testable="true" depth="essential">
      <statement>Consent Mode v2 MUST be implemented from day one (not retrofitted). Without it, GDPR audit exposure is unbounded.</statement>
      <rationale source="Google Consent Mode v2 docs + GDPR practice">Retrofit Consent Mode breaks historical data + invites compliance fines; from-day-one keeps both clean.</rationale>
    </rule>
    <rule id="ga4-bq-link-day-one" testable="true" depth="essential">
      <statement>GA4 → BigQuery export MUST be linked on day one of property creation. Late-link loses raw events permanently.</statement>
      <rationale source="GA4 / BigQuery docs">GA4 stores raw events only after BQ link; pre-link events are aggregated-only forever.</rationale>
    </rule>
    <rule id="utm-naming-standard" testable="true" depth="essential">
      <statement>UTM naming MUST follow a documented convention (source/medium/campaign/term/content); free-form UTMs are rejected at tagging.</statement>
      <rationale source="GA4 / UTM convention practice">Free-form UTMs (case / typo / synonym variants) destroy attribution within 2 quarters.</rationale>
    </rule>
    <rule id="verification-before-go-live" testable="true" depth="essential">
      <statement>All events MUST be verified in DebugView before go-live. Un-verified events go to production silently broken.</statement>
      <rationale source="GA4 DebugView documentation">Silent broken events corrupt 30-60 days of data before discovery.</rationale>
    </rule>
    <p>GA4 property creation, tag installation via GTM or direct, enhanced measurement settings, and standard event schema for common business types.</p>
    <rule id="event-spec-versioned-in-repo" testable="true" depth="essential">
      <statement>Define the event tracking spec before writing any tracking code; treat it as a versioned document in the repo reviewed alongside the code it describes.</statement>
      <rationale source="GA4 event-spec practice">Analytics drift silently. Without a spec, events get added inconsistently, custom dimensions fill up, and the data becomes unreliable. A spec-first approach prevents this and makes the tracking auditable.</rationale>
    </rule>
    <rule id="bq-export-no-backfill" testable="true" depth="essential">
      <statement>Link GA4 to BigQuery from day one even if you do not use it immediately; the export cannot backfill historical data, so enabling it late means permanently losing that history.</statement>
      <rationale source="GA4 / BigQuery export docs">GA4 standard UI sampled above 10M events. BigQuery export is the only path to raw unsampled data. A delayed setup creates an irrecoverable data gap.</rationale>
    </rule>
    <rule id="explicit-conversion-marking" testable="true" depth="essential">
      <statement>Mark conversion events explicitly in GA4 Admin; do not rely on GA4's automatic conversion detection — it is unreliable across business types.</statement>
      <rationale source="GA4 key events docs">GA4 auto-marks some standard events as conversions, but the logic varies by property type and is not documented clearly. Explicit designation ensures the events the business cares about are actually tracked as conversions.</rationale>
    </rule>
    <rule id="purchase-transaction-id" testable="true" depth="essential">
      <statement>Include transaction_id in the purchase event for deduplication; missing it causes inflated revenue figures when users reload the confirmation page.</statement>
      <rationale source="GA4 ecommerce event docs">GA4 deduplicates purchase events by transaction_id. Without it, every page reload triggers a new purchase event, which is the most common source of inflated revenue reporting.</rationale>
    </rule>
    <rule id="custom-dimension-limits" testable="true" depth="essential">
      <statement>Stay under 50 custom event-scoped dimensions and 25 user-scoped dimensions per GA4 property; hitting the limit silently drops new dimensions without warning.</statement>
      <rationale source="GA4 property limits docs">GA4 has hard limits on custom dimensions. Once hit, new custom parameters are not collected — data simply disappears with no error message unless you check the property limits dashboard.</rationale>
    </rule>
    <rule id="user-id-feature-not-dimension" testable="true" depth="essential">
      <statement>Never use user_id as a custom dimension; use the dedicated User-ID feature in GA4 instead. High-cardinality dimensions (user IDs, session IDs) aggregate to "(other)" in reports.</statement>
      <rationale source="GA4 cardinality docs">Custom dimensions with high cardinality (millions of unique values) are aggregated to "(other)" in all reports. They consume a dimension slot and provide no reporting value.</rationale>
    </rule>
    <p>Conversion designation in GA4 and ad platforms, UTM parameter schema, naming conventions, and Consent Mode v2 requirements.</p>
    <rule id="consent-mode-v2-outside-eea" testable="true" depth="essential">
      <statement>Implement Consent Mode v2 from day one even outside the EEA; retrofitting it later requires a migration and risks 30-50% conversion loss in reports during the transition.</statement>
      <rationale source="Google Consent Mode v2 docs">Consent Mode v2 became mandatory in the EEA in March 2024. Sites outside EEA will need to add it when expanding. Installing it at launch eliminates a future migration and ensures modeled conversion data flows correctly.</rationale>
    </rule>
    <rule id="utm-constants-in-code" testable="true" depth="essential">
      <statement>Standardize UTM naming in code as exported constants; never rely on humans typing utm_source values manually — typos produce fragmented source/medium data that cannot be cleaned retroactively.</statement>
      <rationale source="UTM convention practice">A constant export (`UTM_SOURCES = {"google": "google", "meta": "meta"}`) prevents "Google", "google", "goog" all appearing as separate sources. Fragmented data corrupts channel attribution permanently.</rationale>
    </rule>
    <rule id="consent-denied-by-default" testable="true" depth="essential">
      <statement>Set analytics_storage and ad_storage to "denied" by default before the consent banner renders; only upgrade to "granted" after explicit user consent is recorded.</statement>
      <rationale source="Google Consent Mode v2 docs + GDPR practice">Collecting analytics data before consent is recorded violates GDPR in the EEA and is increasingly enforced. Denied-by-default is the only approach that survives a regulatory audit.</rationale>
    </rule>
    <rule id="hash-pii-sha256" testable="true" depth="essential">
      <statement>Hash all PII (email, phone) via SHA-256 on lowercase-trimmed values before sending to ad pixels; use server-side tagging to prevent raw PII from reaching client-side code.</statement>
      <rationale source="Google Ads / Meta enhanced conversions docs">Ad platforms require hashed PII for enhanced matching. Sending raw PII violates GDPR and ad platform terms. Skipping normalization (lowercase + trim) produces near-zero match rates.</rationale>
    </rule>
    <p>Agent-specific rules for analytics install, GTM, Measurement Protocol, and common LLM failure modes in analytics implementation.</p>
    <rule id="gtag-config-before-event" testable="true" depth="essential">
      <statement>Call gtag('config', ...) before gtag('event', ...) in all page load sequences; events queued before config runs are sent to the wrong property.</statement>
      <rationale source="gtag.js reference">gtag processes calls sequentially. An event fired before the config call has no property destination and silently drops. This commonly happens in SPA route-change handlers that fire before the config tag has loaded.</rationale>
    </rule>
    <rule id="cross-domain-linker-explicit" testable="true" depth="essential">
      <statement>For cross-domain tracking, configure the linker parameter explicitly; cross-domain sessions break silently after redesigns without obvious error messages.</statement>
      <rationale source="GA4 cross-domain measurement docs">Without linker configuration, users crossing domains create new sessions and appear as new users from "direct" traffic. The conversion gets attributed to the wrong channel.</rationale>
    </rule>
    <rule id="gtm-container-version-control" testable="true" depth="essential">
      <statement>Export and version-control the GTM container JSON; containers exported across accounts have secrets stripped and require manual re-linking of API keys.</statement>
      <rationale source="GTM container export docs">GTM container exports are the only backup for tracking configuration. Without version control, a misconfigured container deployment has no rollback path.</rationale>
    </rule>
    <rule id="mp-client-id-match" testable="true" depth="essential">
      <statement>Server-side Measurement Protocol events require the client_id matching the gtag-set first-party cookie; events without a matching client_id create orphaned sessions and inflate session counts.</statement>
      <rationale source="GA4 Measurement Protocol docs">GA4 stitches sessions using client_id. Server-side events with mismatched or missing client_id create new phantom sessions, inflating bounce rates and breaking funnel analysis.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>If applicability preconditions are unmet (see AGENTS.md Skip If), the methodology MUST be skipped and a one-line skip-reason logged.</statement>
      <rationale source="F-066 decision-tree convention">A named skip terminal keeps decision-tree leaves resolvable and prevents silent misuse on misfit inputs.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
