# purpose: cohort-implementation config template
# consumes: inputs named in AGENTS.md Prerequisites
# produces: artefact conforming to content/02-output-contract.xml
# depends-on: content/01-core-rules.xml + content/06-decision-tree.xml
# token-budget-impact: ~500 tokens when loaded
slug: cohort-implementation
owner: ""
settings:
  warehouse_engine: ""        # bigquery | snowflake | postgres | redshift
  retention_event: "login"    # must match event_type in cohort-retention-weekly.sql and schema.yml
  day_offsets: [1, 7, 14, 30, 60, 90]
  week_start: "monday"
  timezone: "UTC"
deviation_log_reference: ""
