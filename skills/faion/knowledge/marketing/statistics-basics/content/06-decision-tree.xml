<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Statistical Significance: Basics Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="do not run this methodology; document skip rationale"/>
    </branch>

    <branch when="preconditions == true">
      <question id="q-readiness">Are all readiness gates green?</question>
      <branch when="alpha_pre_committed == false">
        <conclusion ref="alpha-locked-pre-test" verdict="lock alpha in pre-test brief"/>
      </branch>
      <branch when="sample_size_from_power == false">
        <conclusion ref="sample-size-from-power-analysis" verdict="compute n via power analysis"/>
      </branch>
      <branch when="hypothesis_formal == false">
        <conclusion ref="hypothesis-stated-formally" verdict="write H0 + H1 explicitly"/>
      </branch>
      <branch when="type_2_acknowledged == false">
        <conclusion ref="type-1-type-2-tradeoff-explicit" verdict="state MDE + 1-power tradeoff"/>
      </branch>
      <branch when="peeking_policy_set == false">
        <conclusion ref="no-peeking-rule" verdict="set fixed-horizon OR declared sequential method"/>
      </branch>
      <branch when="all_checks_pass == true">
        <conclusion ref="no-peeking-rule" verdict="checklist green; proceed to test launch"/>
      </branch>
    </branch>
  </decision-tree>

  <example trace="happy-path">preconditions=true → readiness gates evaluated → all_checks_pass==true → conclusion ref=no-peeking-rule</example>
</text>
