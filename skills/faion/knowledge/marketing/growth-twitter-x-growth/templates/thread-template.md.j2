<!--
purpose: Markdown skeleton for a generic X thread (non-launch variant).
consumes: operator's thread topic + 5-7 sub-points
produces: a Markdown thread draft ready to schedule on X
depends-on: content/01-core-rules.xml (r3 weekly-thread rule)
token-budget-impact: ~300 tokens
-->

# Twitter/X Thread Template

## Tweet 1 — Hook
I went from {{ starting_point }} to {{ result }} in {{ timeframe }}.

Here's the exact playbook: {{ thread_emoji }}

## Tweet 2 — Promise
Most people {{ common_mistake }}.

I'll show you [what they'll learn] in the next [X] tweets.

## Tweets 3-8 — Value (one per tweet)
{{ number }}. [Tactic or Lesson]

[1-2 sentence explanation]

[Example or proof point if available]

## Tweet 9 — Summary
TL;DR:
1. {{ point_1 }}
2. {{ point_2 }}
3. {{ point_3 }}

## Tweet 10 — CTA
If you found this helpful:

1. Follow @{{ username }} for more {{ topic }}
2. RT tweet 1 to share with others
3. Drop a {{ emoji }} if this helped

[Optional: link to resource — put in first reply, not this tweet]
