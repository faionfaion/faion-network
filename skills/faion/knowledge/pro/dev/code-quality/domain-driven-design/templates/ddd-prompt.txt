You are modeling the <bounded context> context.
Ubiquitous language (authoritative, do not rename):
  <list terms from GLOSSARY.md here>

Produce:
- Aggregates (with root + invariants as method docstrings)
- Value objects (frozen dataclasses with validation in __post_init__)
- Domain events (frozen dataclasses, past tense)
- Repository interface (abstract, returns aggregates only, no ORM types)

Forbidden:
- ORM imports in domain layer (sqlalchemy, django.db, etc.)
- Public setters on entities
- References between aggregate roots via object pointer (use IDs only)
- Publishing events before transaction commit

For each invariant, cite the business rule it enforces.
For each value object, explain why it is immutable.

Review prompt:
Review this diff against DDD tactical patterns.
Reject if: anemic entity, aggregate root with public mutable state,
repository returning DTOs/rows, domain service with infrastructure deps,
event published before transaction commit. Output one verdict block.
