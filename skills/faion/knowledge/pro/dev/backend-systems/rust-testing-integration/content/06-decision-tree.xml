<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="rust-testing-integration Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml.</summary>

  <decision-tree>
      <root-question id="q-root">Does the SUT perform I/O against a real backend (DB, HTTP, queue)?</root-question>
      <branch when="sut_does_io == false">
        <conclusion ref="skip-this-methodology" verdict="skip — author a unit test with mockall"/>
      </branch>
      <branch when="sut_does_io == true">
        <question id="q-next">needs_real_database == true?</question>
        <branch when="needs_real_database == true">
          <question id="q-next">single_test == true?</question>
          <branch when="single_test == true">
            <conclusion ref="r2" verdict="transaction-rollback wrapper around a shared container"/>
          </branch>
          <branch when="single_test == false">
            <conclusion ref="r2" verdict="per-test schema isolation"/>
          </branch>
        </branch>
        <branch when="needs_real_database == false">
          <conclusion ref="r1" verdict="oneshot-only integration test against the router"/>
        </branch>
      </branch>
  </decision-tree>
</text>
