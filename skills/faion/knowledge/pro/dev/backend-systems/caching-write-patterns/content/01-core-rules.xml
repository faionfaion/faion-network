<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="caching-write-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for caching-write-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Cache-aside implementations MUST supply a typed key_builder function; MUST NOT fall back to hashing the repr() of args/kwargs — dict repr is non-deterministic across Python versions and mutable defaults produce key drift.</statement>
      <rationale source="v1-source">Args repr hashing silently produces different keys for logically identical calls (dict ordering, subclass repr). Typed key_builder enforces a stable, inspectable key contract and makes invalidation predictable.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>All cache-aside set operations MUST use setex (or SET EX) — never SET without a TTL. Invalidation MUST call delete on the exact key, not a wildcard KEYS/SCAN pattern in a hot path.</statement>
      <rationale source="v1-source">Keys without TTL become permanent stale data. KEYS * is O(N) and blocks Redis single-thread event loop; SCAN in a hot path still saturates I/O under traffic spikes.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Write-through implementations MUST write to the database first, then update the cache. If the cache update fails after a successful DB write, the implementation MUST delete (not skip) the stale cache key so subsequent reads trigger a fresh load from DB.</statement>
      <rationale source="v1-source">Writing cache before DB risks serving cached data that was never persisted. A failed cache update after a successful DB write leaves a stale cached value; deletion forces the next read to repopulate from the authoritative DB source.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Write-behind implementations MUST use a persistent flush queue (e.g., Redis list or a durable message queue) — NOT an in-process dict — so pending writes survive process restarts. The flush loop MUST implement bulk_write with batching and log failures without silently dropping the batch.</statement>
      <rationale source="v1-source">An in-process defaultdict(dict) for pending_writes loses all unflushed data on crash or OOM kill. A persistent queue survives restarts and makes the data-loss window explicit and bounded to the flush interval.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Every cached entity type MUST have a documented caching pattern selection in the codebase (comment, ADR, or schema table) stating: pattern name, TTL, invalidation trigger, and acceptable stale window.</statement>
      <rationale source="v1-source">Undocumented caching decisions become invisible assumptions that change silently during refactors. The selection must be a first-class artifact so reviewers can catch mismatches at PR time.</rationale>
    </rule>
  </section>
</text>
