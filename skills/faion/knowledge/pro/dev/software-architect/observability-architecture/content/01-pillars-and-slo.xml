<?xml version="1.0" encoding="UTF-8"?>
<text id="01-pillars-and-slo" title="Three Pillars and SLO-Based Observability">
  <summary>Metrics, logs, and traces as complementary pillars answering what/why/where; RED/USE/Golden Signals as the standard instrumentation frameworks; SLI/SLO/error-budget as the decision layer.</summary>

  <section title="Three Pillars">
    <rule>
      <statement>Instrument all three pillars (metrics, logs, traces) and correlate them with shared identifiers (trace_id, span_id) to enable root-cause analysis of unknown failure modes.</statement>
      <rationale>Metrics reveal that p99 latency spiked; logs show the error message; traces pinpoint which downstream service introduced the latency. Without all three correlated, root cause requires guesswork.</rationale>
    </rule>
    <list>
      <item>Metrics: aggregate numerical data over time (counter, gauge, histogram, summary). Answers "what happened and how often?"</item>
      <item>Logs: discrete events with timestamp and context. Answers "why did it happen?"</item>
      <item>Traces: end-to-end request paths across services. Answers "where in the call chain did it happen?"</item>
    </list>
  </section>

  <section title="Instrumentation Frameworks">
    <p>RED Method (for request-handling services): Rate (req/s), Errors (failed req/s), Duration (latency distribution). Apply to every service endpoint.</p>
    <p>USE Method (for resources): Utilisation (% busy), Saturation (queue depth), Errors (error count). Apply to CPU, memory, disk, network, connection pools.</p>
    <p>Golden Signals (Google SRE): Latency, Traffic, Errors, Saturation. Superset of RED; use at service dashboard level.</p>
    <rule>
      <statement>Base alerts on SLO burn rate, not raw metric thresholds.</statement>
      <rationale>A 5% error rate at 1 req/min consumes trivial error budget; a 0.5% error rate at 100k req/min exhausts the monthly budget in hours. Burn rate alerts catch both scenarios with a single rule.</rationale>
    </rule>
  </section>

  <section title="SLI and Error Budget">
    <rule>
      <statement>Define 2-3 SLIs per critical service; reject SLIs that cannot be measured from the user's perspective.</statement>
      <rationale>More than 3 SLIs per service creates alert fatigue and makes error budget policy ambiguous. Internal queue depth is not an SLI unless users directly observe it.</rationale>
    </rule>
    <p>Error budget table: 99% SLO = 7.3 h/month; 99.9% = 43 min/month; 99.95% = 21.6 min/month; 99.99% = 4.3 min/month.</p>
    <list>
      <item>Budget consumed 0-50%: normal development velocity</item>
      <item>Budget consumed 50-75%: prioritise reliability tickets this sprint</item>
      <item>Budget consumed 75-100%: reliability-focused sprint only</item>
      <item>Budget consumed 100%+: feature freeze until budget recovers</item>
    </list>
  </section>
</text>
