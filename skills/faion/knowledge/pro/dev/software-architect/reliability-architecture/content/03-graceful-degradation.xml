<?xml version="1.0" encoding="UTF-8"?>
<text id="03-graceful-degradation" title="Graceful Degradation">
  <summary>Classify features by criticality (P0-P3), define fallback behaviour at each tier, and implement load shedding to preserve core functionality when under stress.</summary>

  <section title="Feature Criticality Classification">
    <rule>
      <statement>Assign every feature a criticality tier (P0-P3) and define its degradation behaviour before a failure occurs, not during one.</statement>
      <rationale>Incident response under pressure produces inconsistent decisions. Pre-classified features and documented fallbacks allow automated or rapid manual degradation without debate.</rationale>
    </rule>
    <list>
      <item>P0 (Critical): Never degrade; allocate maximum resources. Example: checkout, authentication.</item>
      <item>P1 (Important): Degrade to cached or simplified response. Example: search, product pages.</item>
      <item>P2 (Standard): Disable or show generic content. Example: personalised recommendations, reviews.</item>
      <item>P3 (Optional): Silently disable. Example: analytics tracking, A/B test assignment.</item>
    </list>
  </section>

  <section title="Degradation Patterns">
    <list>
      <item>Static Fallback: return cached or default data when live service unavailable (cached product catalogue)</item>
      <item>Simplified Response: return basic subset of data, omit non-essential fields (basic user profile without preferences)</item>
      <item>Feature Toggle: disable feature at runtime via flag without deployment (turn off recommendation engine)</item>
      <item>Load Shedding: reject low-priority requests at admission to protect high-priority paths</item>
      <item>Read-Only Mode: disable writes but allow reads when write path is degraded</item>
    </list>
    <example type="good">
      <note>On recommendation service failure: P2 feature → return popular products from in-process cache rather than 500 error. User sees generic list; checkout path (P0) is unaffected.</note>
    </example>
    <antipattern>
      <description>Returning an error page for any downstream failure, including non-critical P2/P3 features.</description>
      <reason>Degrades the entire user experience for a failure in an optional feature. Partial page rendering with degraded components is always preferable for P2/P3.</reason>
    </antipattern>
  </section>

  <section title="Load Shedding Priority">
    <rule>
      <statement>Implement admission control at the service entry point that rejects requests by priority tier as load increases.</statement>
      <rationale>Without shedding, overload affects all tiers equally including P0. Prioritised shedding ensures P0 operations always have capacity.</rationale>
    </rule>
    <p>Load shedding ladder: Normal → accept all. High load → reject P3. Critical load → reject P2+P3. Overload → accept only P0 + health checks. Assign request priority via headers or request attributes at the gateway.</p>
  </section>
</text>
