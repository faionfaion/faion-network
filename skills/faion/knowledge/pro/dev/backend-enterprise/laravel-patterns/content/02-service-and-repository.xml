<?xml version="1.0" encoding="UTF-8"?>
<text id="02-service-and-repository" title="Service Layer, Repository Pattern, and DTO Flow">
  <summary>Service class wrapping DB::transaction, typed DTOs, and repository interface with implementation.</summary>

  <section title="Service with Transactions">
    <rule>
      <statement>Wrap multi-step writes in DB::transaction(function () { ... }). Do not rely on save() call ordering. Never perform HTTP calls, queue dispatches, or mailer sends inside a transaction — they run before the commit and cannot be rolled back.</statement>
      <rationale>Side effects inside transactions execute on a not-yet-committed state. If the transaction rolls back, the email is already sent or the HTTP call already made. Move side effects to after_commit callbacks or chained jobs.</rationale>
    </rule>
    <example type="good">
      <code lang="php"><![CDATA[
class UserService
{
    public function __construct(
        private UserRepository $repository,
        private NotificationService $notifications,
    ) {}

    public function create(CreateUserDTO $dto): User
    {
        $user = DB::transaction(function () use ($dto) {
            return $this->repository->create([
                'name'            => $dto->name,
                'email'           => $dto->email,
                'password'        => Hash::make($dto->password),
                'organization_id' => $dto->organizationId,
            ]);
        });

        // Side effects OUTSIDE transaction
        event(new UserCreated($user));
        $this->notifications->sendWelcome($user);

        return $user;
    }
}
      ]]></code>
    </example>
  </section>

  <section title="DTO Pattern">
    <rule>
      <statement>Pass typed DTOs between controllers and services. Never pass $request->validated() arrays into the service — arrays lose type information and are unchecked by the IDE or static analyser.</statement>
      <rationale>An array is untyped. A DTO is a named, typed value object. PHPStan can check property access on DTOs; it cannot check string keys in associative arrays.</rationale>
    </rule>
    <example type="good">
      <code lang="php"><![CDATA[
// Form Request converts validated input to a DTO
public function toDTO(): CreateUserDTO
{
    return new CreateUserDTO(
        name:           $this->validated('name'),
        email:          $this->validated('email'),
        password:       $this->validated('password'),
        organizationId: $this->user()->organization_id,
    );
}
      ]]></code>
    </example>
  </section>

  <section title="Repository Interface">
    <example type="good">
      <code lang="php"><![CDATA[
interface UserRepositoryInterface
{
    public function find(int $id): ?User;
    public function paginate(array $filters = [], int $perPage = 15): LengthAwarePaginator;
    public function create(array $data): User;
    public function update(User $user, array $data): bool;
    public function delete(User $user): bool;
}
      ]]></code>
    </example>
  </section>
</text>
