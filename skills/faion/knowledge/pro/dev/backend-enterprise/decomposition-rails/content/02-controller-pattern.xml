<?xml version="1.0" encoding="UTF-8"?>
<text id="02-controller-pattern" title="Thin Controller Pattern">
  <summary>Rails controller delegating to Service/Query Objects with Pundit authorization and explicit serializer wrapping.</summary>

  <rule>
    <statement>Controllers must stay under 80 LOC and delegate all domain logic to Services; authorize at the controller boundary — never inside a Service.</statement>
    <rationale>Services placing authorization inside assume the caller is always an HTTP request. Reusing a Service from a queue job or console command bypasses that assumption, creating privilege escalation.</rationale>
  </rule>

  <rule>
    <statement>Serializers own the response shape; never render json: model or use model.as_json — this leaks internal columns.</statement>
    <rationale>models.as_json exposes password_digest, remember_token, soft-delete timestamps. Explicit serializer attribute lists create a stable API contract.</rationale>
  </rule>

  <example type="good">
    <code lang="ruby"><![CDATA[
# app/controllers/api/v1/users_controller.rb (~70 lines)
module Api
  module V1
    class UsersController < ApplicationController
      before_action :authenticate_user!
      before_action :set_user, only: [:show, :update, :destroy]

      def index
        users = Users::SearchQuery.new.call(search_params)
        render json: UserSerializer.new(users).serializable_hash
      end

      def show
        authorize @user
        render json: UserSerializer.new(@user).serializable_hash
      end

      def create
        user = Users::CreateService.new(params: user_params).call
        render json: UserSerializer.new(user).serializable_hash, status: :created
      end

      def update
        authorize @user
        user = Users::UpdateService.new(user: @user, params: user_params).call
        render json: UserSerializer.new(user).serializable_hash
      end

      private

      def set_user
        @user = User.find(params[:id])
      end

      def user_params
        params.require(:user).permit(:name, :email, :avatar)
      end

      def search_params
        params.permit(:query, :status, :sort)
      end
    end
  end
end
]]></code>
  </example>
</text>
