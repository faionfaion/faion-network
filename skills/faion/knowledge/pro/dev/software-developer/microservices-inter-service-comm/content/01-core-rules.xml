<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="microservices-inter-service-comm Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for microservices-inter-service-comm. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every synchronous service client MUST set an explicit timeout (e.g. httpx timeout=10.0). Clients without a timeout wait indefinitely and exhaust connection pools.</statement>
      <rationale source="v1-source">Default "wait forever" behaviour means a slow dependency stalls all callers and exhausts the thread/connection pool under load.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every event class MUST carry schema_version, occurred_at, and aggregate_id. Events without these fields cannot be versioned, replayed, or debugged across service boundaries.</statement>
      <rationale source="v1-source">Schema version enables consumers to handle breaking changes gracefully. occurred_at enables ordering and deduplication. aggregate_id enables correlation across the event log.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Service addresses MUST be resolved via a registry (Consul, Kubernetes DNS, ECS Service Connect) or environment-injected config, never hardcoded strings like http://order-service:8000.</statement>
      <rationale source="v1-source">Hardcoded addresses prevent independent scaling, blue/green deployment, and service relocation. They also create a hidden dependency that fails silently when the target moves.</rationale>
    </rule>
  </section>
</text>
