<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="ddd-anti-corruption-layer Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for ddd-anti-corruption-layer. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The domain layer MUST define an interface for the external dependency (e.g., InventoryChecker, PaymentGateway) using only domain types and domain terminology. The ACL implementation in the infrastructure layer MUST implement this interface and be the ONLY place that imports the external client SDK or legacy API types.</statement>
      <rationale source="v1-source">Domain code importing external SDK types is a direct coupling: SDK upgrades, API changes, and provider swaps require domain code changes. The interface + ACL pattern confines that coupling to the infrastructure layer.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The ACL MUST translate external error types into domain exceptions. An ExternalApiError from the inventory service MUST become InventoryUnavailableError (a domain concept) before it crosses the ACL boundary. Raw external exceptions MUST NOT propagate into domain or application layer code.</statement>
      <rationale source="v1-source">External error types leak external semantics into domain error handling. Domain code should reason about domain outcomes (inventory unavailable) not external infrastructure outcomes (HTTP 503).</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>The ACL MUST define a fail-safe default for non-critical integrations (e.g., if the inventory API is unreachable, assume available rather than crashing the order flow). Document the failure mode explicitly in the ACL implementation.</statement>
      <rationale source="v1-source">External services are unreliable. Domain flows should degrade gracefully rather than propagate transient infrastructure errors as hard domain failures.</rationale>
    </rule>
  </section>
</text>
