<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="JUnit 5 + Spring Boot Test Core Rules" version="1.1.0" est_tokens="1100" depth="essential">
  <summary>Five testable rules for layered Spring Boot tests with JUnit 5, Mockito, Testcontainers.</summary>

  <section title="Rule set">

    <rule id="webmvctest-for-controllers" testable="true" depth="essential">
      <statement>Controller tests MUST be annotated `@WebMvcTest(YourController.class)` and inject `MockMvc`. Service collaborators MUST be `@MockBean`. `@SpringBootTest` is forbidden for controller-only tests.</statement>
      <rationale source="Spring Boot test slices docs">`@WebMvcTest` boots only the MVC layer — milliseconds vs seconds for `@SpringBootTest`.</rationale>
    </rule>

    <rule id="mockitoextension-for-services" testable="true" depth="essential">
      <statement>Service unit tests MUST use `@ExtendWith(MockitoExtension.class)` + `@Mock` + `@InjectMocks`. Manual constructor calls (`new OrderService(new MockRepo())`) are forbidden.</statement>
      <rationale source="Mockito docs">`MockitoExtension` enforces strict stubbing + auto-resets mocks between tests, catching divergence between setup and code.</rationale>
    </rule>

    <rule id="datajpatest-for-repo-slices" testable="true" depth="essential">
      <statement>Repository tests MUST use `@DataJpaTest` to load only the data layer. Custom `@Query` + Specification predicates + lazy-loading paths MUST be tested. Eager-load contracts MUST use `JOIN FETCH` or `@EntityGraph`.</statement>
      <rationale source="Spring Boot test slices docs">`@DataJpaTest` spins up only `EntityManager` + repositories — fast, focused, and rolls back per test.</rationale>
    </rule>

    <rule id="testcontainers-for-integration" testable="true" depth="essential">
      <statement>Integration tests MUST use `@SpringBootTest(webEnvironment = RANDOM_PORT) + @Testcontainers + @Container` static Postgres (or production DB engine). H2 in-memory MUST NOT be used when the code depends on relational constraints, JSONB, sequences, or vendor-specific SQL.</statement>
      <rationale source="Testcontainers docs + Hibernate dialect docs">H2 silently passes tests that production fails on (different SQL dialect, missing JSONB, no real FK enforcement).</rationale>
    </rule>

    <rule id="parameterizedtest-for-boundaries" testable="true" depth="essential">
      <statement>Boundary + validation tests MUST use `@ParameterizedTest` + `@ValueSource` / `@CsvSource` / `@MethodSource`. Duplicated `@Test` methods for parametric inputs are forbidden.</statement>
      <rationale source="JUnit Jupiter docs">`@ParameterizedTest` reports each input as a separate test result; duplicated `@Test` hides which case failed.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
