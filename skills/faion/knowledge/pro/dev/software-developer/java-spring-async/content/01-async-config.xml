<?xml version="1.0" encoding="UTF-8"?>
<text id="01-async-config" title="Spring Async: ThreadPoolTaskExecutor Configuration">
  <summary>Rules for creating named ThreadPoolTaskExecutor beans with proper shutdown, backpressure, and context propagation via TaskDecorator.</summary>

  <section title="Executor Configuration Rules">
    <rule>
      <statement>Define a named ThreadPoolTaskExecutor bean and always reference it by name: @Async("taskExecutor"). Never use bare @Async — it falls back to SimpleAsyncTaskExecutor (unbounded threads, no pooling).</statement>
      <rationale>SimpleAsyncTaskExecutor creates a new thread per call. Under load this exhausts OS thread limits silently. A named bounded pool provides predictable resource usage.</rationale>
    </rule>

    <rule>
      <statement>Set corePoolSize, maxPoolSize, queueCapacity, threadNamePrefix, and rejectionPolicy on every ThreadPoolTaskExecutor bean. Set setWaitForTasksToCompleteOnShutdown(true) and setAwaitTerminationSeconds to a bounded value.</statement>
      <rationale>Without shutdown wait, in-flight tasks (emails, webhook calls) are dropped on JVM shutdown during deploys. Bounded termination prevents graceful shutdown from hanging indefinitely.</rationale>
    </rule>

    <rule>
      <statement>Use CallerRunsPolicy as the default rejection policy for fire-and-forget executors. Use AbortPolicy only when the caller can handle RejectedExecutionException.</statement>
      <rationale>CallerRunsPolicy degrades gracefully under overload by running the task on the caller's thread, providing natural backpressure without dropping tasks.</rationale>
    </rule>

    <rule>
      <statement>Wrap every ThreadPoolTaskExecutor in a TaskDecorator that copies MDC context map, SecurityContextHolder, and OTel current span to the async thread before execution, restoring originals afterward.</statement>
      <rationale>MDC (trace IDs), SecurityContext (authenticated principal), and OTel span are ThreadLocal. Without propagation, async threads produce untraced logs and null principals, breaking observability and security.</rationale>
    </rule>

    <example type="good">
      <code lang="java"><![CDATA[
@Configuration
@EnableAsync
public class AsyncConfig {

    @Bean(name = "taskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(4);
        executor.setMaxPoolSize(8);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("Async-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setWaitForTasksToCompleteOnShutdown(true);
        executor.setAwaitTerminationSeconds(60);
        executor.setTaskDecorator(task -> {
            Map<String, String> mdc = MDC.getCopyOfContextMap();
            SecurityContext sc = SecurityContextHolder.getContext();
            return () -> {
                try {
                    MDC.setContextMap(mdc != null ? mdc : Map.of());
                    SecurityContextHolder.setContext(sc);
                    task.run();
                } finally {
                    MDC.clear();
                    SecurityContextHolder.clearContext();
                }
            };
        });
        executor.initialize();
        return executor;
    }
}
      ]]></code>
      <note>One @EnableAsync per application (in a @Configuration class). Multiple executor beans are allowed — name each descriptively (emailExecutor, reportExecutor).</note>
    </example>
  </section>

  <section title="Micrometer Metrics">
    <rule>
      <statement>Register Micrometer metrics on every ThreadPoolTaskExecutor: queued, active, completed, rejected counts. Alert on rejected > 0 for any non-CallerRuns pool.</statement>
      <rationale>Without metrics, pool exhaustion is invisible until the application degrades. rejected > 0 is an immediate operational signal.</rationale>
    </rule>
  </section>

  <reference path="templates/async-config.java">Complete AsyncConfig with two named executors and TaskDecorator</reference>
</text>
