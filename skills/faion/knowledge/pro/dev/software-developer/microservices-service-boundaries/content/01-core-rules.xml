<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="microservices-service-boundaries Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for microservices-service-boundaries. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Each service MUST have single responsibility — it does one thing well, owns one bounded context.</statement>
      <rationale source="v1-source">Single responsibility keeps the service small enough to rewrite, test independently, and deploy without fear.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Services MUST be loosely coupled — no shared database tables across service boundaries, no shared ORM models, no direct imports from another service's infrastructure layer.</statement>
      <rationale source="v1-source">Shared data creates implicit coupling that defeats the purpose of microservices. Schema changes cascade unpredictably.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Related functionality MUST be grouped with high cohesion — everything a service needs to fulfil its bounded context lives inside it.</statement>
      <rationale source="v1-source">Low cohesion forces choreography for every operation, multiplying latency and failure surface.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Each service MUST own its data — its own schema/database/collection, with no direct reads or writes from external services.</statement>
      <rationale source="v1-source">Decentralized data is the foundation of independent deployability. Shared data is a hidden monolith.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Services MUST be designed for failure — every outbound call assumes the dependency will fail; timeouts, retries, and fallbacks are mandatory, not optional.</statement>
      <rationale source="v1-source">In a distributed system, partial failure is the normal state. Services that do not handle it degrade the entire system.</rationale>
    </rule>
  </section>
</text>
