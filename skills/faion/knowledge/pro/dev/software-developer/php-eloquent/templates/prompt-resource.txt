Generate the <Resource> HTTP resource:
- migration create_<resources>_table with user_id FK (cascadeOnDelete), status enum, soft deletes; down() fully reversible
- App\Models\<Resource> with $fillable, $casts (status => <Resource>Status enum), HasMany <related>
- <Resource>Factory + <Related>Factory
- App\Http\Resources\<Resource>Resource (eager-loads <related>)
- <Resource>Controller index/show/store with FormRequest + Policy stub
- tests/Feature/<Resource>Test.php (Pest) covering 200/422/403

Bind no repository — go through model directly.
Use HasUlids not auto-increment.
Enable Model::preventLazyLoading(!app()->isProduction()) in AppServiceProvider if not already set.
