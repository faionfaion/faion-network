<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="STRIDE Threat Model Template Output Contract" version="1.0.0" est_tokens="900" depth="essential">
  <summary>JSON Schema (draft-07) for the artefact this methodology produces. Validator script enforces required keys, types, and per-field constraints.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/stride-threat-model-template.json",
  "title": "STRIDE Threat Model Template",
  "type": "object",
  "required": ["artefact_id", "owner", "title", "sections", "rationale", "inputs_used", "version", "last_reviewed"],
  "properties": {
    "artefact_id": { "type": "string", "minLength": 1 },
    "owner": { "type": "string", "minLength": 1, "not": { "enum": ["team", "we", "us", "TBD", ""] } },
    "title": { "type": "string", "minLength": 1 },
    "sections": { "type": "array", "minItems": 1 },
    "rationale": { "type": "string", "minLength": 1 },
    "inputs_used": { "type": "array", "minItems": 1 },
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "last_reviewed": { "type": "string", "format": "date" }
  },
  "additionalProperties": false
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "artefact_id": "stride-threat-model-template-2026Q2-001",
  "owner": "ruslan@faion.net",
  "title": "Example spec for stride-threat-model-template",
  "sections": [
    {
      "name": "Constraints",
      "body": "Quoted client mandate."
    },
    {
      "name": "Options",
      "body": "≥2 options within mandate."
    }
  ],
  "rationale": "Closes the gap surfaced by the parent skill — input artefact 'task-brief.md' explicitly names the constraint set; output ties decisions to rule r1.",
  "inputs_used": [
    "task-brief.md",
    "constitution.md"
  ],
  "version": "1.0.0",
  "last_reviewed": "2026-05-23"
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "artefact_id": "",
  "owner": "team",
  "version": "latest"
}
]]></code>
    <note>Missing required fields; owner = 'team' is forbidden; placeholder values 'TBD' / 'TODO' / 'FIXME' rejected. Validator returns explicit violation list.</note>
  </example>

  <section title="Forbidden patterns">
    <pattern>any field with value 'TBD' / 'TODO' / 'FIXME'</pattern>
    <pattern>'team' / 'we' / 'us' as owner</pattern>
    <pattern>free-form prose where a structured field is required</pattern>
    <pattern>empty arrays for required list-typed fields</pattern>
    <pattern>version that is not semver (e.g. 'latest', '0', 'rc')</pattern>
  </section>

  <reference path="scripts/validate-stride-threat-model-template.py">Validator script enforcing this schema</reference>
</text>
