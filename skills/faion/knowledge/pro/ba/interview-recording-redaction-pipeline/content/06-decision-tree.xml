<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Interview Recording Redaction Pipeline Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for interview-recording-redaction-pipeline.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="consent_record_required_signal == true">
      <conclusion ref="consent-record-required"/>
    </branch>
    <branch when="transcript_generated_locally_signal == true">
      <conclusion ref="transcript-generated-locally"/>
    </branch>
    <branch when="auto_redact_pii_categories_signal == true">
      <conclusion ref="auto-redact-pii-categories"/>
    </branch>
    <branch when="human_review_checkpoint_signal == true">
      <conclusion ref="human-review-checkpoint"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
