{
  "__faion_header__": {
    "purpose": "minimum viable filled use-case set fixture",
    "consumes": "n/a",
    "produces": "schema-valid set snapshot",
    "depends-on": "02-output-contract.xml",
    "token-budget-impact": "~250 tokens"
  },
  "set_id": "smoke-appointments",
  "version_tag": "v0.1.0",
  "use_cases": [
    {
      "id": "uc-book-smoke",
      "trace_id": "PROD-001",
      "primary_actor": "Authenticated Patient",
      "secondary_actors": ["SMS Gateway"],
      "goal": "Book one appointment slot",
      "preconditions": ["session.authenticated == true"],
      "postconditions": ["appointment.status == 'booked'"],
      "main_success_scenario": ["1. select", "2. reserve", "3. confirm"],
      "alternative_flows": [{"id": "alt-1", "trigger": "slot taken", "steps": ["offer alternates"]}],
      "exception_flows": [{"id": "exc-1", "trigger": "sms timeout", "steps": ["queue retry"]}]
    }
  ]
}
