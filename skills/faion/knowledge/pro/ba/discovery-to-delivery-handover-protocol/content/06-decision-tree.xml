<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Discovery to Delivery Handover Protocol Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for discovery-to-delivery-handover-protocol.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="artefact_bundle_required_signal == true">
      <conclusion ref="artefact-bundle-required"/>
    </branch>
    <branch when="walkthrough_not_email_signal == true">
      <conclusion ref="walkthrough-not-email"/>
    </branch>
    <branch when="signed_ack_required_signal == true">
      <conclusion ref="signed-ack-required"/>
    </branch>
    <branch when="hot_line_30_days_signal == true">
      <conclusion ref="hot-line-30-days"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
