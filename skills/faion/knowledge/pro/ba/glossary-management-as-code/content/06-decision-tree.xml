<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Glossary Management as Code Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for glossary-management-as-code.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="yaml_source_of_truth_signal == true">
      <conclusion ref="yaml-source-of-truth"/>
    </branch>
    <branch when="term_record_shape_signal == true">
      <conclusion ref="term-record-shape"/>
    </branch>
    <branch when="pr_required_for_change_signal == true">
      <conclusion ref="pr-required-for-change"/>
    </branch>
    <branch when="owner_mandatory_signal == true">
      <conclusion ref="owner-mandatory"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
