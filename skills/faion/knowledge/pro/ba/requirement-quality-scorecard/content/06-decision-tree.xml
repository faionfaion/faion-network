<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Requirement Quality Scorecard Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for requirement-quality-scorecard.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="six_axis_shape_signal == true">
      <conclusion ref="six-axis-shape"/>
    </branch>
    <branch when="atomicity_rule_signal == true">
      <conclusion ref="atomicity-rule"/>
    </branch>
    <branch when="testability_rule_signal == true">
      <conclusion ref="testability-rule"/>
    </branch>
    <branch when="traceability_rule_signal == true">
      <conclusion ref="traceability-rule"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
