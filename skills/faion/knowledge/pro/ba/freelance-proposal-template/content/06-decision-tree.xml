<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Freelance Proposal Template Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for freelance-proposal-template.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="scope_envelope_explicit_signal == true">
      <conclusion ref="scope-envelope-explicit"/>
    </branch>
    <branch when="assumptions_listed_signal == true">
      <conclusion ref="assumptions-listed"/>
    </branch>
    <branch when="milestones_cap_payment_signal == true">
      <conclusion ref="milestones-cap-payment"/>
    </branch>
    <branch when="change_request_rate_signal == true">
      <conclusion ref="change-request-rate"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
