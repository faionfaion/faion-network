<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="requirements-documentation">
  <metadata>
    <tier>pro</tier>
    <group>ba</group>
    <domain>ba-core</domain>
    <summary>Produce sign-off-ready requirements (BRD + URS + SRS/FRD) aligned to IEEE 830 or ISO 29148. Write FRs in EARS patterns. Validate via YAML schema. Source is Markdown; PDF via pandoc.</summary>
    <category>requirements</category>
    <difficulty>intermediate</difficulty>
    <created>2026-04-28</created>
    <tags>
      <tag>requirements</tag>
      <tag>documentation</tag>
      <tag>brd</tag>
      <tag>srs</tag>
      <tag>ears</tag>
    </tags>
  </metadata>

  <content>
    <title>Requirements Documentation</title>

    <summary>Produce sign-off-ready requirements sets (BRD + URS + SRS/FRD) aligned to a single locked standard (IEEE 830-1998 or ISO/IEC/IEEE 29148:2018). Write functional requirements in EARS patterns (Ubiquitous, Event-driven, State-driven, Optional, Unwanted-behaviour, Complex). Validate structure via a YAML conformance schema before any document is rendered to PDF. Source is Markdown with frontmatter; the PDF is a generated artifact via pandoc.</summary>

    <why>
      <p>Unstructured requirements documents with ambiguous phrasing ("should be fast", "easy to use"), no acceptance criteria, and no version control produce test gaps and failed audits. EARS patterns map cleanly to test scaffolds, a conformance schema catches missing sections before review, and pandoc-from-Markdown gives a diffable source that generates a deterministic PDF.</p>
    </why>

    <when-to-use>
      <list>
        <item>Producing a classical, sign-off-ready requirements set for regulated work (IEC 62304, ISO 26262, DO-178C, BCBS 239, SOC2 audits)</item>
        <item>A vendor or client contract names a specific standard (IEEE 830, ISO 29148, IREB CPRE) and the deliverable list is fixed</item>
        <item>Hand-off across organizations where the document is the contract artifact</item>
        <item>Re-baselining: a previously approved pack must be re-issued at v2.0 with a redline, changelog, and fresh sign-off</item>
        <item>Auditors will physically read a PDF expecting: cover page, version table, approval block, glossary, traceability matrix, requirement IDs</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Solo or agile project where spec.md + acceptance tests already cover the audit surface</item>
        <item>Team has no document custodian — without a named owner the formal apparatus rots within two sprints</item>
        <item>Discovery work where requirements are still volatile — formal docs imply a baseline; baselining in flux is theatre</item>
        <item>Internal tooling for one team — lightweight user stories are sufficient</item>
        <item>The reviewer pool will not actually sign — unsigned formal docs are worse than informal ones</item>
      </list>
    </when-not-to-use>

    <section name="requirements-hierarchy">
      <rule>
        <statement>Maintain BRD (BR- prefix), URS (UR- prefix), and SRS/FRD (FR-/NFR- prefix) as separate files with distinct ID namespaces. One requirement per file remains the source-of-truth. Never blend BRD and FRD content into a single document.</statement>
        <rationale>BRD = why (business); URS = what users need (operational); SRS/FRD = what the system shall do (functional + NFR). Teams that produce overlapping content lose traceability between levels.</rationale>
      </rule>

      <list>
        <item>Business Requirements (BR-) — why are we doing this? example: "Increase revenue by 20%"</item>
        <item>Stakeholder/User Requirements (UR-/SR-) — what do users need? example: "Sales reps need to create quotes faster"</item>
        <item>Functional Requirements (FR-) — what shall the system do? example: "The system shall calculate quote total"</item>
        <item>Non-Functional Requirements (NFR-) — how shall the system perform? example: "Pages shall load in less than 2 seconds"</item>
      </list>
    </section>

    <section name="standard-selection-rule">
      <rule>
        <statement>Pick one standard per project — IEEE 830-1998 or ISO/IEC/IEEE 29148:2018 — and freeze it in constitution.md before any requirement is written. Mixing standards produces section-name skew and audit ambiguity.</statement>
        <rationale>IEEE 830 is more prescriptive on document structure; ISO 29148 is more process-oriented and better for systems engineering. Mixing them silently creates incompatible section numbering.</rationale>
      </rule>
    </section>

    <section name="smart-requirement-attributes">
      <rule>
        <statement>Every requirement must be: Specific (only one interpretation), Measurable (testable against acceptance criteria), Achievable (technically feasible), Relevant (traces to a business need), and free of subjective terms (fast, user-friendly, easy, robust, intuitive, seamless).</statement>
        <rationale>Subjective terms cannot be tested; a requirement containing them is a deferred conflict with QA.</rationale>
      </rule>
    </section>

    <section name="ears-patterns-for-functional-requirements">
      <rule>
        <statement>Write all functional requirements in one of the six EARS patterns. Default to Ubiquitous only when no trigger or state condition applies; always ask whether the requirement is actually event-driven or state-driven first.</statement>
        <rationale>Agents and teams default every FR to Ubiquitous ("The system shall..."), producing unconditional requirements that under-specify state machines and generate ambiguous test conditions.</rationale>
      </rule>

      <list>
        <item>Ubiquitous: "The system shall {response}." — unconditional</item>
        <item>Event-driven: "When {trigger}, the system shall {response}." — maps to test: given-when-then</item>
        <item>State-driven: "While {state}, the system shall {response}." — maps to test: given-setup-when-then</item>
        <item>Optional: "Where {feature included}, the system shall {response}." — feature-flag gated</item>
        <item>Unwanted-behaviour: "If {trigger}, then the system shall {response}." — error/exception handling</item>
        <item>Complex: combine two patterns; one per sentence</item>
      </list>

      <antipattern>
        <description>Using "should", "may", or "might" in an SRS that mandates "shall".</description>
        <reason>RFC 2119 and IEEE 830 conventions differ; an agent trained on RFC documents writes MAY/SHOULD in a "shall"-only SRS. Lint with the conformance schema; reject modal-verb drift.</reason>
      </antipattern>
    </section>

    <section name="document-versioning-rule">
      <rule>
        <statement>Version the document, not just the requirements: patch for typos, minor for added requirements, major for scope or baseline changes requiring re-sign-off. Approval block must name role + individual + date + Git tag (e.g., approved-2026-04-15-srs-v1.0). Never edit an Approved document in place; any change must bump version and reset status to Draft.</statement>
        <rationale>Agents editing Approved docs in place silently destroy the audit chain; CI-checked status invariants prevent this.</rationale>
      </rule>
    </section>

    <section name="four-role-pipeline">
      <list>
        <item>ba-doc-scaffolder (haiku) — emits empty document outline given {doc_type, standard, scale}; includes cover, version table, approval block (stub only), glossary reference</item>
        <item>ba-doc-populator (sonnet) — fills sections from elicitation transcripts and shared glossary.md; applies SMART attributes and EARS patterns per requirement</item>
        <item>ba-doc-conformance (sonnet) — validates against YAML schema: mandatory sections present, all FRs have ID + source + priority + AC, no forbidden words, no non-shall modals</item>
        <item>ba-doc-renderer — pandoc from Markdown source to DOCX/PDF with deterministic cover page; binary artifact stored in Git LFS; never edited directly</item>
      </list>

      <rule>
        <statement>feature-executor must not move a feature out of todo/ until the conformance checker passes. The SDD spec.md becomes the FRD section; a separate BRD.md is rendered for sponsors.</statement>
        <rationale>This enforces the conformance gate without requiring a separate CI pipeline step for every team.</rationale>
      </rule>
    </section>

    <section name="scaffolder-prompt-pattern">
      <example type="good" title="BA standards scaffolder">
        <code lang="xml"><![CDATA[
<role>BA standards scaffolder. Emit empty document outline only.</role>
<inputs>
  <doc_type>BRD|URS|SRS|FRD|Glossary</doc_type>
  <standard>IEEE-830-1998|ISO-29148-2018|IREB-CPRE</standard>
  <scale>minimal|standard|regulated</scale>
</inputs>
<rules>
  Emit Markdown headings only; no example content.
  Include cover page, version history table, approval block (stub), glossary reference.
  For SRS/FRD: include Functional, Non-Functional (Performance, Security, Usability,
    Reliability, Compatibility, Maintainability, Portability), Interfaces, Constraints,
    Assumptions, Traceability Matrix (Appendix A).
  Mark optional sections as: <!-- optional: scale=minimal -->
</rules>
]]></code>
      </example>
    </section>

    <section name="ears-formatter-prompt-pattern">
      <example type="good" title="EARS formatter — one requirement per call">
        <code lang="xml"><![CDATA[
<task>Rewrite requirement {req_id} into the correct EARS pattern.</task>
<patterns>
  Ubiquitous:   "The {system} shall {response}."
  Event-driven: "When {trigger}, the {system} shall {response}."
  State-driven: "While {state}, the {system} shall {response}."
  Optional:     "Where {feature included}, the {system} shall {response}."
  Unwanted:     "If {trigger}, then the {system} shall {response}."
  Complex:      Combine above; one pattern per sentence.
</patterns>
<output>JSON: {req_id, original, ears_pattern, rewritten, rationale,
               fallback_if_unchanged}</output>
]]></code>
      </example>
    </section>

    <section name="ai-agent-gotchas">
      <antipattern>
        <description>Standard hallucination — agent produces "IEEE 830-2025" sections that do not exist.</description>
        <reason>Pin the standard in the prompt and reject any structural claim not present in the schema file; use the srs-conformance.yaml as ground truth.</reason>
      </antipattern>

      <antipattern>
        <description>EARS pattern collapse — agent defaults all FRs to Ubiquitous.</description>
        <reason>Force a per-requirement EARS classification call before phrasing; require the agent to name the pattern before writing the sentence.</reason>
      </antipattern>

      <antipattern>
        <description>Approval block fabricated — agent populates approval table with names and dates.</description>
        <reason>Approval table must be a stub the human fills; an agent-populated approval block is a forged audit record.</reason>
      </antipattern>

      <antipattern>
        <description>Section-completeness theatre — agent fills every section with "N/A".</description>
        <reason>Force "drop or fill" — emit nothing if no content applies; track section coverage as a metric, not section presence.</reason>
      </antipattern>

      <antipattern>
        <description>Glossary forks — agent redefines a term inline rather than reading the shared glossary.md.</description>
        <reason>Provide glossary.md in context for every populate call; forbid in-doc definitions that conflict with the glossary.</reason>
      </antipattern>
    </section>

    <section name="mandatory-human-checkpoints">
      <list>
        <item>Approval block — names, roles, dates are human-only; agent produces the stub</item>
        <item>Status transition from Draft to InReview to Approved — requires human action in the source file</item>
        <item>Any v2.0 re-baseline requiring sponsor re-sign-off</item>
      </list>
    </section>

    <section name="agentic-workflow-multiagent-pipeline">
      <p>The ba-core take treats requirements docs as a multi-document set with strict document identity ({doc_type, version, status, approval_date}) and a single source-of-truth tree from which BRD / URS / SRS / FRD are rendered. Drive it with a four-role pipeline: (1) a scaffolder instantiates the standards-correct outline (IEEE 830 §5 or ISO 29148 clause 9) for the chosen doc type; (2) a populator fills sections from elicitation transcripts and the shared glossary.md, applying SMART for atomic requirements and EARS for FR phrasing; (3) a conformance checker validates structure (mandatory sections present, version table populated, every requirement has an ID, source, priority, AC) against a YAML schema; (4) a renderer emits PDF/DOCX via pandoc with a deterministic cover page, version history, approval block, and traceability matrix. feature-executor should not move a feature out of todo/ until the conformance checker passes; the SDD spec.md becomes the FRD section, and a separate BRD.md is rendered for sponsors.</p>

      <p><term name="recommended-subagents">Recommended subagents:</term></p>
      <list>
        <item>faion-sdd-executor-agent — owns spec.md per feature; treats the FR section as authoritative and forbids editing the rendered FRD directly.</item>
        <item>feature-executor — wires scaffolder → populator → checker as a quality gate before estimation; refuses to advance a feature lacking a glossary entry for any non-trivial term used in its requirements.</item>
        <item>brainstorm — diverge to surface NFR categories (security, usability, observability, compliance, accessibility, locale, performance, capacity, portability, maintainability) the standards-template lists but the populator skipped; converge to keep only those that apply.</item>
        <item>password-scrubber-agent — pre-render pass over the source tree before pandoc emits a PDF that may leave the company perimeter.</item>
        <item>A custom ba-doc-scaffolder (haiku) — emits the empty document outline given {doc_type, standard, scale} and a list of mandatory sections.</item>
        <item>A custom ba-doc-conformance (sonnet) — runs the YAML-schema check, reports missing sections / IDs / priorities / sources / AC.</item>
        <item>A custom ba-ears-formatter (sonnet) — rewrites unconditional FRs into the correct EARS pattern (Ubiquitous / Event-driven / State-driven / Optional / Unwanted-behaviour / Complex).</item>
        <item>A custom ba-glossary-curator (sonnet) — extracts candidate terms from the corpus, deduplicates against glossary.md, proposes definitions, never edits an approved term in place.</item>
      </list>
    </section>

    <section name="limitations-and-gotchas">
      <p><term name="template-fundamentalism">Template fundamentalism.</term> BAs paste a 60-section IEEE 830 outline, then half is left blank or filled with "N/A". Use a scaled template that explicitly drops sections instead of stubbing them.</p>

      <p><term name="brd-neq-frd-confusion">BRD ≠ FRD ≠ URS confusion.</term> Teams treat these as synonyms and produce overlapping content. BRD = why (business); URS = what users need (operational); SRS / FRD = what the system shall do (functional + NFR). Agent prompts must enforce the boundary.</p>

      <p><term name="invest-misapplied">INVEST misapplied to non-stories.</term> INVEST is for user stories, not FRs. Applying "Estimable / Small" to an FR pushes BAs to fragment requirements until they become tasks. Keep INVEST for the story-card view; use SMART + IEEE 830 quality attributes for FR-level docs.</p>

      <p><term name="ears-adoption-fails">EARS adoption fails silently.</term> Teams claim EARS but write only Ubiquitous ("The system shall...") and never Event-driven (When ... the system shall...) or State-driven (While ... the system shall...) or Optional (Where ... the system shall...). Result is unconditional requirements that under-specify state machines.</p>

      <p><term name="approval-block-as-ritual">Approval block as ritual.</term> Names without dated, versioned signatures = no audit trail. Tie sign-off to a Git tag or a signed PDF; agents cannot forge dates.</p>

      <p><term name="glossary-drift">Glossary drift.</term> Same term defined three different ways across BRD/URS/SRS. Force a single glossary.md referenced by all three.</p>

      <p><term name="numbering-archaeology">Numbering archaeology.</term> REQ-FR-001 vs FR-1 vs 1.4.2.1 — three doc generations, no mapping. Lock a numbering convention in constitution.md before a single REQ is written.</p>

      <p><term name="pdf-only-delivery">PDF-only delivery.</term> A signed PDF is the contract output, but the source must be diff-able Markdown. Agents that edit PDFs directly destroy provenance.</p>
    </section>

    <section name="ai-agent-gotchas-requirements-doc">
      <antipattern>
        <description>Standard hallucination. Agent produces "IEEE 830-2025" sections that do not exist.</description>
        <reason>Pin the standard in the prompt and reject any structural claim not present in the schema file. Use the srs-conformance.yaml as ground truth.</reason>
      </antipattern>
      <antipattern>
        <description>EARS pattern collapse. Agent defaults all FRs to Ubiquitous.</description>
        <reason>Force a per-requirement EARS classification call before phrasing. Require the agent to name the pattern before writing the sentence.</reason>
      </antipattern>
      <antipattern>
        <description>Modal-verb drift. RFC 2119 vs IEEE 830 conventions differ; agent trained on RFCs writes "MAY", "SHOULD" in an SRS that mandates "shall" only.</description>
        <reason>Lint with the schema; reject may|should|might. Ban these words at linting time.</reason>
      </antipattern>
      <antipattern>
        <description>Glossary forks. Agent redefines an existing term inline rather than reading the glossary.</description>
        <reason>Provide glossary.md in context for every populate call and forbid in-doc definitions.</reason>
      </antipattern>
      <antipattern>
        <description>Approval-block fabrication. Agent emitting a populated approval table with names + dates is forging an audit record.</description>
        <reason>Approval table must be a stub the human fills. Approval block must remain empty for agent-driven workflows.</reason>
      </antipattern>
      <antipattern>
        <description>Section-completeness theatre. Agent fills every section with "N/A" rather than dropping it.</description>
        <reason>Force "drop or fill" — emit nothing if no content applies; track section-coverage as a metric.</reason>
      </antipattern>
      <antipattern>
        <description>NFR template-numbness. Models reproduce IEEE 830 §5.5 categories verbatim without adapting to the project.</description>
        <reason>Run a tailoring pass: prune categories that do not apply (e.g., no portability for an internal-only web app).</reason>
      </antipattern>
      <antipattern>
        <description>Re-baseline silence. Agent edits an Approved doc in place.</description>
        <reason>Status must be a CI-checked invariant: any change to an Approved file must bump version and reset status to Draft.</reason>
      </antipattern>
      <antipattern>
        <description>PDF render drift. Re-rendering yields a binary diff (font kerning, generated date).</description>
        <reason>Pin pandoc version, supply a deterministic timestamp (SOURCE_DATE_EPOCH), and diff via pdftotext.</reason>
      </antipattern>
      <antipattern>
        <description>Traceability matrix overload. A 5,000-row matrix is unreadable.</description>
        <reason>Auto-generate one matrix per top-level capability instead of one project-wide.</reason>
      </antipattern>
    </section>

    <section name="best-practices-requirements-doc">
      <list>
        <item>Pick one standard per project (IEEE 830 or ISO/IEC/IEEE 29148) and freeze it in constitution.md. Mixing produces section-name skew.</item>
        <item>Maintain BRD.md, URS.md, SRS.md/FRD.md, and glossary.md as separate files at separate ID prefixes (BR-, UR-, FR-, NFR-). One requirement per file remains the source-of-truth.</item>
        <item>Write FRs in EARS form. The five canonical patterns map cleanly to test scaffolds (when → trigger, while → setup state, where → feature flag).</item>
        <item>Keep INVEST for the story-card view rendered for sprints; do not retrofit Estimable/Small onto formal FRs — they break atomicity.</item>
        <item>Version the document, not just the requirements: patch for typos, minor for added requirements, major for scope/baseline change requiring re-sign-off.</item>
        <item>Approval block names a role + named individual + date + Git tag (approved-2026-04-15-srs-v1.0). Tags are signed, dates immutable.</item>
        <item>Render PDFs from Markdown with pandoc; never edit PDFs by hand. Track the rendered PDF via Git LFS so reviewers diff source, not output.</item>
        <item>Maintain a single Traceability Matrix (Appendix A of SRS) auto-generated from traces_to: frontmatter. Manual matrices rot within one sprint.</item>
        <item>For regulated work, attach a Glossary version reference to every doc — Glossary v0.7 — so a term redefinition forces an explicit re-baseline of dependent docs.</item>
        <item>When migrating from DOORS / Polarion, use ReqIF as the lossless intermediate. CSV exports drop attribute hierarchy.</item>
      </list>
    </section>

    <section name="cli-tools-requirements-doc">
      <list>
        <item>pandoc — Render Markdown source → DOCX/PDF with custom cover-page template and reference docx; https://pandoc.org</item>
        <item>pandoc-crossref — Numbering for figures, tables, and requirements across a multi-doc set; https://lierdakil.github.io/pandoc-crossref/</item>
        <item>vale — Prose linter; ban "should/may/might" in shall-only documents; https://vale.sh</item>
        <item>markdownlint-cli2 — Heading-level + link-validity lint per IEEE 830 outline; npm i -g markdownlint-cli2</item>
        <item>yq — Validate YAML frontmatter on every REQ-XXX file (id, type, priority, source, traces_to); https://github.com/mikefarah/yq</item>
        <item>git-lfs — Track signed PDFs alongside Markdown sources; git lfs install</item>
        <item>cosign — Sign released PDFs with a developer key for tamper evidence; https://github.com/sigstore/cosign</item>
        <item>pdftotext (poppler) — Diff a re-rendered PDF against the previous baseline before re-issue; apt install poppler-utils</item>
        <item>mkdocs-material + mkdocs-mermaid2-plugin — Browseable HTML mirror of the doc set; pip install mkdocs-material</item>
        <item>req-if-converter (OSS) — Convert ReqIF (DOORS export) → Markdown source-of-truth; https://github.com/ReqIF</item>
      </list>
    </section>

    <section name="services-requirements-doc">
      <list>
        <item>IBM ENGINEERING DOORS Next — SaaS / on-prem; OSLC + REST; Standard for regulated industries; ReqIF round-trip with Markdown</item>
        <item>Jama Connect — SaaS; REST + webhooks; Strong baselining and review cycles; agents can post / fetch via REST</item>
        <item>Polarion ALM — SaaS / on-prem; REST + WebDAV; Heavy enterprise; aligned to ASPICE / IEC 62304</item>
        <item>codeBeamer — SaaS / on-prem; REST; Niche but strong traceability for safety-critical</item>
        <item>Visure Requirements — SaaS; REST; Lightweight; covers IREB and IEEE templates out-of-box</item>
        <item>ReqView — Desktop / OS-agnostic; OpenSpecTrace + JSON; Cheapest for solo / consultancy use</item>
        <item>Modern Requirements4DevOps — SaaS; Azure DevOps API; Inside ADO; renders BRD / FRD / URS as Word from work items</item>
        <item>Aha! — SaaS; REST + GraphQL; Roadmap to requirements link; weaker on formal sign-off</item>
        <item>Notion + GitHub source — SaaS / OSS; REST + gh CLI; Cheap "good enough" for non-regulated work; agents own the GitHub side</item>
        <item>Sphinx-Needs (sphinx-needs) — OSS; Python; Pure Markdown / RST source; ideal for fully agentic, audit-friendly</item>
      </list>
    </section>

    <templates>
      <template path="templates/brd-template.md" purpose="Business Requirements Document with cover, version, approval, and BR table sections"/>
      <template path="templates/user-story-template.md" purpose="Agile user story with Gherkin acceptance criteria"/>
      <template path="templates/srs-conformance.yaml" purpose="IEEE 830-aligned conformance schema for CI validation"/>
      <template path="templates/srs_conform.py" purpose="Python validator that fails CI when mandatory sections or phrasing rules are violated"/>
    </templates>

    <see-also>
      <link href="https://standards.ieee.org/standard/830-1998.html"/>
      <link href="https://standards.ieee.org/standard/29148-2018.html"/>
      <link href="https://www.ireb.org/en/downloads"/>
      <link href="https://pandoc.org/"/>
    </see-also>
  </content>
</methodology>
