<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Definition Of Ready Checklist Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for definition-of-ready-checklist.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="eight_item_gate_signal == true">
      <conclusion ref="eight-item-gate"/>
    </branch>
    <branch when="client_side_dep_precheck_signal == true">
      <conclusion ref="client-side-dep-precheck"/>
    </branch>
    <branch when="compliance_named_contact_signal == true">
      <conclusion ref="compliance-named-contact"/>
    </branch>
    <branch when="timebox_five_minutes_signal == true">
      <conclusion ref="timebox-five-minutes"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
