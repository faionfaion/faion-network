<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Inbound Intake Qualification Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for inbound-intake-qualification.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="five_question_rubric_signal == true">
      <conclusion ref="five-question-rubric"/>
    </branch>
    <branch when="score_threshold_routing_signal == true">
      <conclusion ref="score-threshold-routing"/>
    </branch>
    <branch when="templated_decline_signal == true">
      <conclusion ref="templated-decline"/>
    </branch>
    <branch when="decider_identified_signal == true">
      <conclusion ref="decider-identified"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
