<?xml version="1.0" encoding="UTF-8"?>
<text id="02-workflow" title="Resource Management — Agentic Workflow and Subagents">
  <summary>Capacity-planner and skill-matcher pipeline, leveler prompt pattern, and AI-specific failure modes in resource planning automation.</summary>

  <section title="Agentic Capacity Pipeline">
    <rule>
      <statement>A capacity-planner subagent ingests calendars (PTO, holidays), current Jira/Linear assignments, and skill tags from HR; produces a weekly availability matrix capped at 75% effective capacity.</statement>
      <rationale>Manual capacity planning is always stale; automated ingestion from authoritative sources (calendar + HR + tracker) produces a plan that updates as reality changes.</rationale>
    </rule>
    <p>A leveler agent compares demand (committed work from WBS) vs. supply (availability matrix), surfaces overload weeks, and proposes options with cost impact. The PM selects; the agent updates the plan and notifies affected leads. Re-runs weekly on cron.</p>
  </section>

  <section title="Recommended Subagents">
    <list>
      <item>capacity-planner — pulls PTO, ceremonies, current assignments → effective capacity per person per week (capped at 32h / 80% of 40h standard week).</item>
      <item>skill-matcher — matches required skills from WBS to people with skill-level scoring (skill_match × 0.6 + availability × 0.3 + cost_fit × 0.1).</item>
      <item>leveler — detects overload weeks, proposes mitigations (delay, split, add, cut scope) with cost and schedule impact per option.</item>
      <item>utilization-monitor — weekly delta vs. plan; flags drift greater than 15%; surfaces trend, not snapshot.</item>
    </list>
    <reference path="templates/capacity.py">Effective capacity calculator from roster, PTO, and meeting load</reference>
  </section>

  <section title="Prompt Patterns">
    <example type="good">
      <note>Capacity-planner prompt — hard cap at 80%, explicit subtraction logic:</note>
      <code lang="text"><![CDATA[
You are a capacity-planner. Given calendar events {ical}, sprint commitments
{jira_issues}, recurring meetings {meetings}, return per-person weekly hours
available for project work. Cap at 32h/week (80% of 40). Subtract:
- PTO/holidays (full day = 8h)
- Recurring meetings (sum per week)
- Reserve 4h/week for interruption buffer
Output JSON {person, week, available_h, committed_h, free_h}.
      ]]></code>
    </example>
    <example type="good">
      <note>Skill-matcher prompt — scoring formula prevents gut-feel assignments:</note>
      <code lang="text"><![CDATA[
You are a skill-matcher. Given task {task} requiring {skills} at {level},
return ranked candidates from {team_roster.yaml}.
Score = skill_match * 0.6 + availability * 0.3 + cost_fit * 0.1.
Reject candidates with score < 0.5. Output JSON with score and rationale.
      ]]></code>
    </example>
  </section>

  <section title="AI-Agent Gotchas">
    <antipattern>
      <description>Calendar API pulls miss informal meetings, pair sessions, and debugging deep dives.</description>
      <reason>Structured calendar data only captures formally scheduled events; ask the person to confirm their actual available hours before using the raw calendar count for planning.</reason>
    </antipattern>
    <antipattern>
      <description>Allocation agent packs the calendar to 100% if uncapped.</description>
      <reason>Agents optimize for filling capacity; hard-cap at 80% effective in the prompt and the output schema to prevent burnout plans.</reason>
    </antipattern>
    <antipattern>
      <description>Agent auto-rebalances assignments mid-sprint.</description>
      <reason>Mid-sprint reassignments break WIP and disrupt flow. Schedule rebalancing only at sprint boundaries.</reason>
    </antipattern>
    <antipattern>
      <description>Agents read calendar subjects to assess meeting load.</description>
      <reason>Calendar subjects are often sensitive (layoff prep, performance review); redact subject lines before any LLM input, keep only duration and recurrence pattern.</reason>
    </antipattern>
    <list>
      <item>Skill self-assessments inflate; use last-shipped-feature as ground truth for skill level.</item>
      <item>Hourly-rate fields in HR systems mix loaded and unloaded cost; document which rate EVM uses.</item>
      <item>Pulling PTO from HR APIs often hits permissioning issues; have a fallback "ask the EM" loop.</item>
      <item>Mandatory human checkpoints: resource plan approval, overload escalation to sponsor, contractor engagement, any assignment change that affects the critical path.</item>
    </list>
  </section>
</text>
