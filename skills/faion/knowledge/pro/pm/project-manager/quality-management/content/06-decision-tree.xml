<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Quality Management Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-1">Gate phase?</root-question>
    <branch when="signal == pre_commit">
      <conclusion ref="twice-not-duplicated" verdict="fast checks only — lint, format; reject if violated"/>
    </branch>
    <branch when="signal == pre_merge">
      <conclusion ref="dod-as-code" verdict="full DoD check from dod.yaml; mutation on critical modules; merge only if all must pass"/>
    </branch>
    <root-question id="q-2">Code source?</root-question>
    <branch when="signal == agent">
      <conclusion ref="higher-bar-for-agent-code" verdict="raise the bar: 90%+ coverage, mutation testing, no silent suppressions"/>
    </branch>
    <branch when="signal == human">
      <conclusion ref="must-vs-should-split" verdict="standard split applies"/>
    </branch>
  </decision-tree>
</text>
