<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Stakeholder Register Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-1">Register operation?</root-question>
    <branch when="signal == identify_new">
      <conclusion ref="groups-as-groups" verdict="individual OR group — preserve collective authority"/>
    </branch>
    <branch when="signal == rate_power">
      <conclusion ref="evidence-not-gut-feel" verdict="anchor to org-chart + budget authority + RACI"/>
    </branch>
    <branch when="signal == set_attitude">
      <conclusion ref="verify-attitude-in-conversation" verdict="'unknown' until direct conversation; never infer from email tone"/>
    </branch>
    <branch when="signal == commit_register">
      <conclusion ref="pii-separated" verdict="scrub PII; pre-commit scan"/>
    </branch>
  </decision-tree>
</text>
