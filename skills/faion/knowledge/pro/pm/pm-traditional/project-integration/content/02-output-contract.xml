<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Project Integration Management Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema draft-07 for the `config` artefact this methodology produces, plus one valid and one invalid example, and a forbidden-pattern list. Validator at `scripts/validate-project-integration.py` enforces required keys.</summary>

  <section title="Schema">
    <schema format="json-schema-draft-07">
      <code lang="json"><![CDATA[
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/project-integration.json",
  "title": "Project Integration Management",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "config"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "const": "project-integration"
    },
    "config": {
      "type": "object",
      "minProperties": 1
    },
    "format": {
      "enum": [
        "yaml",
        "toml",
        "json",
        "markdown"
      ]
    }
  }
}
]]></code>
    </schema>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "slug": "project-integration",
  "config": {
    "key": "value"
  },
  "format": "yaml"
}
]]></code>
    <note>Minimal example that passes the validator.</note>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "slug": "project-integration"
}
]]></code>
    <note>Fails: required fields missing or values outside their allowed range.</note>
  </example>

  <section title="Forbidden patterns">
    <forbidden>
      <pattern id="f1">TBD, TODO, FIXME, or placeholder text anywhere in the artefact body — strings are rejected by the validator.</pattern>
      <pattern id="f2">Slug mismatch — `slug` field does not equal the directory slug.</pattern>
      <pattern id="f3">Missing required field per schema — validator returns exit code 1 with the violating path.</pattern>
      <pattern id="f4">Non-deterministic timestamps in `approved_at` or equivalent fields — must be ISO-8601 UTC.</pattern>
    </forbidden>
  </section>

  <reference path="scripts/validate-project-integration.py">Validator script (stdlib only; --help + --self-test).</reference>
</text>
