<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Multi Project Allocation Tracker Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree on observable signals (preconditions, evidence access, named consumer) onto a concrete verdict; every leaf references a rule from content/01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold AND no Skip-If disqualifier fires?</root-question>

    <branch when="applies_if_all_true == true AND skip_if_any_true == false">
      <question id="q-evidence">Is the source-of-truth for inputs accessible AND can every non-trivial field be anchored?</question>
      <branch when="source_accessible == true AND evidence_per_field == true">
        <question id="q-reviewer">Is the named human reviewer (operator / lead / founder) defined AND in the loop?</question>
        <branch when="named_reviewer_present == true">
          <conclusion ref="per-person-allocation-row" verdict="apply the methodology; produce the artefact and route to reviewer for approval"/>
        </branch>
        <branch when="named_reviewer_present == false">
          <conclusion ref="rebalance-proposal-actionable" verdict="halt; designate a named human reviewer before continuing"/>
        </branch>
      </branch>
      <branch when="source_accessible == false OR evidence_per_field == false">
        <conclusion ref="evidence-per-allocation" verdict="downgrade to draft; flag missing-evidence rows verify-before-commit"/>
      </branch>
    </branch>

    <branch when="applies_if_all_true == false OR skip_if_any_true == true">
      <conclusion ref="skip-this-methodology" verdict="skip; methodology overhead does not pay back"/>
    </branch>
  </decision-tree>

  <example trace="mpat-2026-W21 — 8 people; olena-k over-allocated 1.1; proposal: defer nova phase-2 to W23; decision owner Iryna V.">
    preconditions=true AND evidence_per_field=true AND named_reviewer=true → apply, produce the artefact, route to reviewer.
  </example>
</text>
