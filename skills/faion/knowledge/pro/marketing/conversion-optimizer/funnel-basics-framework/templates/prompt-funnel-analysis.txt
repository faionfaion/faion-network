# purpose: LLM prompt for analysing funnel drop-off and prioritising fixes
# consumes: see content/02-output-contract.xml inputs
# produces: artefact conforming to content/02-output-contract.xml
# depends-on: content/01-core-rules.xml
# token-budget-impact: ~400-1000 tokens when loaded as context

Analyze the following funnel data and identify the biggest drop-off points.

Funnel data:
[PASTE STEP-BY-STEP METRICS: step name, user count, conversion rate]

Provide:
1. Rank each step transition by absolute users lost (highest first)
2. For each of the top 3 transitions, list 2-3 probable root causes based on common patterns
3. For each root cause, suggest one diagnostic action (session recording, survey, technical check)
4. Recommend which transition to address first with a one-sentence justification

Format: structured table + brief recommendation paragraph.
