<?xml version="1.0" encoding="UTF-8"?>
<text id="03-agent-rules" title="LinkedIn Ads API Gotchas and Agent Rules">
  <summary>LinkedIn Marketing API-specific failure modes, token rotation requirements, versioning rules, and human-in-loop checkpoints.</summary>

  <section title="API Versioning">
    <rule>
      <statement>Always include the LinkedIn-Version header in every API call and refresh the pinned version within 6 months; outdated versions return 410 deprecation errors.</statement>
      <rationale>LinkedIn changes its API versioning monthly. Agents pinning a version over 6 months old will hit 410 responses without a clear migration path in the error message.</rationale>
    </rule>
    <rule>
      <statement>Refresh OAuth access tokens before they expire (60-day lifetime); implement a token refresh flow that runs proactively, not reactively after a 401.</statement>
      <rationale>LinkedIn access tokens expire in 60 days. Agents relying on 401 errors to trigger refresh fail silently at midnight when the token expires mid-run, losing that day's data.</rationale>
    </rule>
  </section>

  <section title="Cost Units">
    <rule>
      <statement>LinkedIn cost data is in costInLocalCurrency as a decimal float (e.g., 45.50 USD), NOT in micros like Google Ads. Do not divide by 1,000,000.</statement>
      <rationale>Agents porting Google Ads micros logic to LinkedIn will divide costs by 1,000,000 and report CPLs of $0.000065 instead of $65 — undetectable if alerts are set in micros units.</rationale>
    </rule>
  </section>

  <section title="Rate Limits and Quotas">
    <rule>
      <statement>Rate limits apply per app, not per account; multi-tenant agents must implement a per-app token bucket and back off on 429 responses.</statement>
      <rationale>LinkedIn enforces rate limits at the OAuth app level, not per ad account. An agent managing 10 accounts with the same app will hit the limit 10x faster than a single-account agent.</rationale>
    </rule>
    <antipattern>
      <description>Assuming Lead Gen Form field order is stable across form versions.</description>
      <reason>Custom question fields are auto-keyed (e.g., question_1ab23). Field order and keys change if the form is edited. Agents must cache the field-map per form version, never assume index positions.</reason>
    </antipattern>
  </section>

  <section title="Attribution Lag">
    <rule>
      <statement>Wait 24-48 hours for LinkedIn metrics to stabilize before evaluating campaign performance; do not retune based on same-day data.</statement>
      <rationale>LinkedIn reporting has a 24-48 hour lag before conversion numbers are stable. Early optimization based on incomplete data causes over-reaction to noise.</rationale>
    </rule>
    <rule>
      <statement>Insight Tag conversions backfill for up to 90 days; do not declare a campaign a loser before the lookback window closes.</statement>
      <rationale>View-through conversions and delayed form completions backfill over 90 days. Cutting a campaign at 30 days permanently loses attribution for late-converting leads.</rationale>
    </rule>
  </section>

  <section title="Human-in-Loop Checkpoints">
    <list>
      <item>Budget increase above 25% in one step: human approval required</item>
      <item>Final ad copy approval before launch: human required</item>
      <item>ABM target-account list sign-off: human required</item>
      <item>Changing campaign objective: human required</item>
    </list>
  </section>
</text>
