<?xml version="1.0" encoding="UTF-8"?>
<text id="02-workflows" title="Multi-Mode Workflows">
  <summary>Sequential execution rules, the full research package workflow, the idea validation workflow, and error-handling table for research agent runs.</summary>

  <section title="Sequential Execution Rule">
    <rule>
      <statement>Always run research modes sequentially, never in parallel. Each mode's output must exist before the next mode starts.</statement>
      <rationale>Parallel mode invocations hit shared WebSearch quota (HTTP 429s), produce partial outputs, and corrupt the .aidocs/product_docs/ state. Nothing in the code enforces this; the orchestrator must implement it explicitly with a semaphore or sequential loop.</rationale>
    </rule>
    <rule>
      <statement>Pass the file path of prior outputs to subsequent mode prompts, not the file contents.</statement>
      <rationale>The subagent re-reads the file on demand, saving 5-20k tokens per call while giving the subagent full access to the prior output.</rationale>
    </rule>
  </section>

  <section title="Context Passing Rule">
    <rule>
      <statement>Run pains mode before validate mode. Never run validate before a pain inventory exists.</statement>
      <rationale>Validation without a pain inventory becomes self-confirmation. The pains output grounds the validate prompts in real user language.</rationale>
    </rule>
  </section>

  <section title="Output Provenance">
    <rule>
      <statement>Every output file must start with a 1-line provenance header: comment with agent name, mode, and ISO date.</statement>
      <rationale>Lets faion-improver audit staleness and detect when a pricing or competitor report needs a refresh.</rationale>
    </rule>
    <example type="good">
      <code lang="markdown"><![CDATA[
<!-- generated: faion-research-agent mode=market on 2026-04-26 -->
]]></code>
    </example>
  </section>

  <section title="Full Research Package Sequence">
    <p>Run modes in this order for a full package: ideas, pains, niche, market, competitors, personas, validate, pricing, names. After names, chain to faion-domain-checker-agent. All outputs go to .aidocs/product_docs/.</p>
    <reference path="templates/research-run.sh">Bash dispatcher implementing this sequence with output-path tracking</reference>
  </section>

  <section title="Error Handling">
    <list>
      <item>No ideas resonate — try a different framework, ask about hobbies and daily frustrations</item>
      <item>No pain points found — broaden search to adjacent problems and communities</item>
      <item>High competition — look for underserved segment rather than abandoning the idea</item>
      <item>All .com taken — suggest .io, check premium domain acquirability</item>
      <item>Trademark conflict detected — remove name from candidates, note reason in output</item>
      <item>Data not found — write "Data not available" in output file, do not estimate</item>
    </list>
  </section>

  <section title="Output Location Map">
    <list>
      <item>.aidocs/product_docs/idea-validation.md — ideas mode</item>
      <item>.aidocs/product_docs/market-research.md — market mode</item>
      <item>.aidocs/product_docs/competitive-analysis.md — competitors mode</item>
      <item>.aidocs/product_docs/pain-points.md — pains mode</item>
      <item>.aidocs/product_docs/user-personas.md — personas mode</item>
      <item>.aidocs/product_docs/problem-validation.md — validate mode</item>
      <item>.aidocs/product_docs/niche-evaluation.md — niche mode</item>
      <item>.aidocs/product_docs/pricing-research.md — pricing mode</item>
      <item>.aidocs/product_docs/name-candidates.md — names mode</item>
      <item>.aidocs/product_docs/executive-summary.md — full research package synthesis</item>
    </list>
  </section>

  <see-also>
    <link path="content/01-modes.xml"/>
    <link path="content/03-gotchas.xml"/>
  </see-also>
</text>
