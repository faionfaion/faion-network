<?xml version="1.0" encoding="UTF-8"?>
<text id="01-modes" title="Research Agent Modes">
  <summary>Nine operational modes for faion-research-agent plus domain checker, with correct invocation pattern, required context, and output file for each.</summary>

  <section title="Agent Roster">
    <rule>
      <statement>Use faion-research-agent for all research modes. Use faion-domain-checker-agent only for domain and handle availability checks.</statement>
      <rationale>Each agent is tuned for a specific scope; mixing them duplicates context and degrades output quality.</rationale>
    </rule>
    <p>Model selection by task: haiku for data lookup (ideas, names, domain-check); sonnet for pattern analysis (pains, personas, competitors, validate); opus for strategic synthesis (market, niche, pricing).</p>
  </section>

  <section title="Mode Invocation Pattern">
    <rule>
      <statement>Pass the mode inside the prompt field, not in subagent_type. The subagent_type must match the literal name in the agent frontmatter file.</statement>
      <rationale>Claude Code's Task tool resolves subagent_type against the agent file's name field. "faion-research-agent (mode: market)" is pseudocode — it does not route to a real agent.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
Task(
    subagent_type="faion-research-agent",
    prompt="Research TAM/SAM/SOM for {product}. Geography: {geo}. Vertical: {vertical}. "
           "Output to .aidocs/product_docs/market-research.md. Cite every figure with "
           "[Source](URL). If a figure is unavailable, write 'Data not available'."
)
]]></code>
      <note>Mode is expressed in the prompt; subagent_type is the literal agent name.</note>
    </example>
    <example type="bad">
      <code lang="python"><![CDATA[
Task(
    subagent_type="faion-research-agent (mode: market)",
    prompt="Research market for {product}"
)
]]></code>
      <note>subagent_type string does not match any agent frontmatter name. Task will fail to route.</note>
    </example>
  </section>

  <section title="Mode Reference Table">
    <p>Nine modes with required context and output file path:</p>
    <list>
      <item>ideas — context: user skills, available time, motivation | output: idea-validation.md | model: haiku</item>
      <item>market — context: product description, geography, industry | output: market-research.md | model: opus</item>
      <item>competitors — context: product description, key features, target market | output: competitive-analysis.md | model: opus</item>
      <item>pains — context: idea or product description | output: pain-points.md | model: sonnet</item>
      <item>personas — context: product, target market, known segments | output: user-personas.md | model: sonnet</item>
      <item>validate — context: problem statement | output: problem-validation.md | model: sonnet</item>
      <item>niche — context: idea description | output: niche-evaluation.md | model: sonnet</item>
      <item>pricing — context: product, geography | output: pricing-research.md | model: opus</item>
      <item>names — context: product description, target audience, tone | output: name-candidates.md | model: haiku</item>
    </list>
    <rule>
      <statement>Always pass geography explicitly in the prompt for market, pricing, and competitors modes.</statement>
      <rationale>Without geography the agent returns US-default figures; EU/UA context shifts pricing by ~30% and competitor rankings substantially.</rationale>
    </rule>
  </section>

  <section title="Domain Checker">
    <p>faion-domain-checker-agent checks .com, .io, .co domains plus GitHub username and Twitter handle. Chain it after names mode: generate 20 names, let the checker filter to survivors, present only those with .com available or acquirable at reasonable cost.</p>
    <rule>
      <statement>Generate 20 names before invoking domain check. Do not generate 5 and check — zero survivors occur ~70% of the time with a small candidate pool.</statement>
      <rationale>Domain availability is a hard filter; a larger candidate pool absorbs collisions without restarting the ideation loop.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-workflows.xml"/>
    <link path="content/03-gotchas.xml"/>
    <link path="templates/research-run.sh"/>
  </see-also>
</text>
