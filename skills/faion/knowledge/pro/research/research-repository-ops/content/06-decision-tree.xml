<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Research Repository Operations Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable input signals onto a conclusion that references a rule id from 01-core-rules.xml.</summary>
  <decision-tree>
    <root-question id="ops-state">Is repository in active use?</root-question>
      <branch when="state == decommissioning">
        <conclusion ref="skip-this-methodology" verdict="no ongoing ops"/>
      </branch>
      <branch when="state == active">
        <question id="cadence">Which cadence is due?</question>
          <branch when="cadence == weekly">
            <conclusion ref="weekly-cadence" verdict="run sync + tag + dedup + health report"/>
          </branch>
          <branch when="cadence == monthly">
            <conclusion ref="retention-twentyfour-months" verdict="run retention pass"/>
          </branch>
          <branch when="cadence == quarterly">
            <conclusion ref="access-quarterly-review" verdict="run access review"/>
          </branch>
      </branch>
  </decision-tree>
  <example trace="state=active -&gt; cadence=weekly -&gt; sync 612 -&gt; tag 598 -&gt; dedup 14 -&gt; report.">Worked path from root-question to conclusion.</example>
</text>
