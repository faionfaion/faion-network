<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Infrastructure as Code Basics Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="tool_imperative == true">
      <conclusion ref="declarative-not-imperative" verdict="select a declarative tool"/>
    </branch>

    <branch when="state_is_local == true">
      <conclusion ref="remote-state" verdict="migrate state to remote backend"/>
    </branch>

    <branch when="secrets_in_tfvars == true">
      <conclusion ref="secrets-out-of-state" verdict="move secrets to manager + rotate"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="ci-applies" verdict="wire CI WIF applies + baseline modules"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; tool_imperative == true -&gt; state_is_local == true -> conclusion ref.</example>
</text>
