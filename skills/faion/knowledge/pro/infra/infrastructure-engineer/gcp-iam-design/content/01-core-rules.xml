<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="gcp-iam-design Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for gcp-iam-design. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Basic roles (roles/owner, roles/editor, roles/viewer) MUST NOT be granted in production projects. Use predefined roles or custom roles with only the required permissions.</statement>
      <rationale source="v1-source">Basic roles grant access to all GCP services, not just the ones a workload needs. A service account with roles/editor can write to any bucket, create VMs, and modify IAM — far beyond what most workloads require.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Service account keys (JSON key files) MUST NOT be used for GKE workloads. Use Workload Identity Federation to bind Kubernetes service accounts to GCP service accounts instead.</statement>
      <rationale source="v1-source">Key files are static credentials that can be leaked via container logs, environment variables, or git commits. Workload Identity uses short-lived tokens bound to the pod identity and requires no secret management.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Each workload MUST have a dedicated service account. Never share a service account across multiple applications or environments. Never use the default compute service account.</statement>
      <rationale source="v1-source">Shared service accounts make it impossible to audit which workload made which API call, and over-permission one workload to satisfy another. Per-workload service accounts enable least-privilege and auditable activity logs.</rationale>
    </rule>
  </section>
</text>
