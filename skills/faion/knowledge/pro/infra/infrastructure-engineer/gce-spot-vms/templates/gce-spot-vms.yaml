# purpose: Skeleton for the config artefact produced by the gce-spot-vms methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gce-spot-vms.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

scheduling: example-scheduling
provisioning_model: example-provisioning_model
instance_termination_action: example-instance_termination_action
checkpoint_target: example-checkpoint_target
