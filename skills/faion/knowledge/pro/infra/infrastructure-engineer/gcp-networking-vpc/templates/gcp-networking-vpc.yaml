# purpose: Skeleton for the config artefact produced by the gcp-networking-vpc methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-networking-vpc.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

vpc_name: example-vpc_name
mode: example-mode
subnets: []  # array
firewall_basis: example-firewall_basis
flow_logs: example-flow_logs
