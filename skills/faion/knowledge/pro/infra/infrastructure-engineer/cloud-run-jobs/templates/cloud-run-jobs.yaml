# purpose: Skeleton for the config artefact produced by the cloud-run-jobs methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-cloud-run-jobs.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

name: example-name
image: example-image
task_count: 0  # int|number|string
parallelism: example-parallelism
task_timeout: 0  # int|number|string
max_retries: 0  # int|number|string
idempotent: false  # boolean
