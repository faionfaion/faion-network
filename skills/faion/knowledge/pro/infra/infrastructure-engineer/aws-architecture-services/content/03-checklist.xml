<?xml version="1.0" encoding="UTF-8"?>
<text id="03-checklist" title="AWS Architecture Services Pre-Deployment Checklist">
  <summary>Verification rules for EKS, RDS Aurora, ALB, S3, CloudFront, EventBridge, and cost optimization before deploying to production.</summary>

  <section title="EKS Cluster">
    <rule>
      <statement>Deploy EKS cluster in private subnets with IRSA enabled, secrets encryption via KMS, and managed node groups using Graviton instances.</statement>
      <rationale>Private subnets prevent direct internet exposure. IRSA eliminates long-lived access keys on nodes. KMS encryption protects secrets in etcd.</rationale>
    </rule>
    <rule>
      <statement>Enable at least CoreDNS, kube-proxy, VPC CNI, and EBS CSI driver as managed add-ons.</statement>
      <rationale>Managed add-ons receive security patches automatically without manual intervention.</rationale>
    </rule>
  </section>

  <section title="RDS Aurora">
    <rule>
      <statement>Always deploy Aurora with Multi-AZ (minimum 2 instances), encryption at rest via KMS, IAM authentication enabled, and deletion protection on prod.</statement>
      <rationale>Single-instance Aurora has no automatic failover. Deletion protection prevents accidental data loss in production.</rationale>
    </rule>
    <rule>
      <statement>Set backup retention to 30 days in production; enable Performance Insights and Enhanced Monitoring.</statement>
      <rationale>30-day retention meets most compliance requirements. Performance Insights identifies slow queries proactively.</rationale>
    </rule>
  </section>

  <section title="ALB and CloudFront">
    <rule>
      <statement>Redirect HTTP port 80 to HTTPS 443 with SSL policy ELBSecurityPolicy-TLS13-1-2-2021-06; enable access logs to S3.</statement>
      <rationale>TLS 1.3 policy disables weak cipher suites. Access logs are required for security audits and debugging.</rationale>
    </rule>
    <rule>
      <statement>Use CloudFront Origin Access Control (OAC) for S3 origins, not legacy OAI; enable HTTP/2 and HTTP/3.</statement>
      <rationale>OAC uses SigV4 signing and supports SSE-KMS, which OAI does not.</rationale>
    </rule>
  </section>

  <section title="Security and Cost">
    <rule>
      <statement>Tag all resources with Project, Environment, Owner, and ManagedBy=terraform; enable CloudTrail and AWS Config.</statement>
      <rationale>Tags enable cost allocation and compliance reporting. CloudTrail provides the audit trail required for SOC2/PCI.</rationale>
    </rule>
    <rule>
      <statement>Use Spot instances for fault-tolerant batch workloads, Reserved capacity for steady-state, and Instance Scheduler to stop non-prod environments overnight.</statement>
      <rationale>Instance Scheduler can save up to 75% on non-prod costs. Spot pricing is 60-90% cheaper than On-Demand for interruptible workloads.</rationale>
    </rule>
    <antipattern>
      <description>Overly permissive IAM with wildcard (*) resource ARNs in production.</description>
      <reason>Wildcards grant unintended access to all resources, violating least privilege and failing security audits.</reason>
    </antipattern>
    <antipattern>
      <description>Single-AZ deployments for critical workloads.</description>
      <reason>An AZ outage takes the entire workload offline. Multi-AZ is required for any SLA above 99.9%.</reason>
    </antipattern>
  </section>
</text>
