# purpose: Skeleton for the config artefact produced by the gce-instance-templates methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gce-instance-templates.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

name: example-name
machine_type: example-machine_type
image: example-image
service_account: 0  # int|number|string
startup_script: example-startup_script
metadata: example-metadata
