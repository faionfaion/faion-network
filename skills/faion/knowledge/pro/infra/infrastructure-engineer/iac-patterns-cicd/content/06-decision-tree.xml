<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="IaC CI/CD Patterns Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="auth_static == true">
      <conclusion ref="wif-only-ci" verdict="migrate to WIF/OIDC; remove static creds"/>
    </branch>

    <branch when="apply_on_pr == true">
      <conclusion ref="apply-only-on-merge" verdict="restrict apply to protected branch"/>
    </branch>

    <branch when="drift_detect_missing == true">
      <conclusion ref="drift-detected-nightly" verdict="add scheduled drift-detect job"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="env-promotion" verdict="wire dev → staging → prod gate + commit"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; auth_static == true -&gt; apply_on_pr == true -> conclusion ref.</example>
</text>
