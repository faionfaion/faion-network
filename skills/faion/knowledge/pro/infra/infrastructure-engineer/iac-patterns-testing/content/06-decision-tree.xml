<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="IaC Testing Patterns Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="policy_gates_missing == true">
      <conclusion ref="policy-as-code" verdict="wire OPA/Sentinel/Checkov before merge"/>
    </branch>

    <branch when="integration_against_prod == true">
      <conclusion ref="integration-on-real-cloud" verdict="carve sandbox project before next test"/>
    </branch>

    <branch when="teardown_missing == true">
      <conclusion ref="destroy-mandatory" verdict="add destroy/cleanup defer"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="plan-diff-asserts" verdict="add attribute assertions + commit"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; policy_gates_missing == true -&gt; integration_against_prod == true -> conclusion ref.</example>
</text>
