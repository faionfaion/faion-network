<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="aws-vpc-three-tier">
  <metadata>
    <tier>pro</tier>
    <group>infra</group>
    <domain>infrastructure-engineer</domain>
    <summary>Build a three-tier VPC from scratch: public/private/database subnets across 3 AZs, per-AZ NAT gateways, VPC endpoints for S3/ECR/Secrets Manager, Transit Gateway, ALB, and Flow Logs.</summary>
    <tags>
      <tag>aws</tag>
      <tag>vpc</tag>
      <tag>networking</tag>
      <tag>nat-gateway</tag>
    </tags>
    <category>aws</category>
    <difficulty>intermediate</difficulty>
    <created>2026-04-27</created>
    <related>
      <ref slug="aws-vpc-design"/>
      <ref slug="aws-ec2-provisioning"/>
      <ref slug="aws-iam-practical-patterns"/>
    </related>
  </metadata>

  <content>
    <title>AWS VPC Three-Tier Architecture: Subnets, NAT, Endpoints, ALB, Transit Gateway</title>

    <summary>Build a production three-tier VPC: 9 subnets (3 AZs x public/private/database), per-AZ NAT gateways, free S3 gateway endpoint, interface endpoints for ECR and Secrets Manager, an internet-facing Application Load Balancer with HTTPS, VPC Flow Logs, and Transit Gateway attachment for multi-VPC connectivity. All CLI commands are reproducible and environment-parameterized.</summary>

    <why>
      <p>The three-tier subnet pattern (public → private → database) is the foundational AWS network topology. Public subnets hold only load balancers and NAT gateways. Application servers run in private subnets with outbound internet via NAT. Databases run in isolated database subnets with no internet route. This separation limits the blast radius of a compromise: even if an application server is pwned, the database subnet has no direct path to the internet.</p>
      <p>Per-AZ NAT gateways eliminate the AZ-crossing charge and remove a single point of failure. VPC endpoints for S3 and ECR eliminate NAT gateway bandwidth charges for internal AWS service traffic and remove the public internet from the path. Transit Gateway replaces VPC peering for multi-VPC environments — one attachment per VPC instead of O(n²) peering connections.</p>
    </why>

    <when-to-use>
      <list>
        <item>Setting up a new production VPC from scratch.</item>
        <item>Adding private and database subnet tiers to an existing single-tier VPC.</item>
        <item>Connecting multiple VPCs (prod, staging, shared-services) via Transit Gateway.</item>
        <item>Reducing NAT gateway costs by adding S3 and ECR VPC endpoints.</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Proof-of-concept or dev-only environments — a single public subnet with restrictive security groups is sufficient; NAT gateway costs $32/month minimum.</item>
        <item>Serverless-only workloads (Lambda + API Gateway + DynamoDB) with no VPC dependency — attaching Lambda to a VPC adds cold start latency and requires NAT for internet access.</item>
      </list>
    </when-not-to-use>

    <section name="vpc-and-subnets">
      <rule testable="true">
        <statement>Production VPCs MUST span at least 3 Availability Zones with 3 subnet tiers each (public, private, database) totaling 9 subnets. CIDR blocks MUST be planned to avoid overlaps with other VPCs to permit future Transit Gateway or VPC peering. Enable DNS hostnames and DNS support on every VPC.</statement>
        <rationale>3 AZs provides N+2 redundancy. Public/private/database isolation is required by PCI-DSS and SOC 2. Non-overlapping CIDRs are a hard requirement for Transit Gateway routing; retrofitting is expensive.</rationale>
      </rule>

      <example type="good" title="Create VPC, IGW, and 9 subnets (3 AZs x 3 tiers)">
        <code lang="bash"><![CDATA[
# Create VPC
aws ec2 create-vpc \
    --cidr-block 10.0.0.0/16 \
    --tag-specifications 'ResourceType=vpc,Tags=[{Key=Name,Value=production-vpc}]'

# Enable DNS
aws ec2 modify-vpc-attribute --vpc-id vpc-xxx --enable-dns-hostnames '{"Value": true}'
aws ec2 modify-vpc-attribute --vpc-id vpc-xxx --enable-dns-support '{"Value": true}'

# Create Internet Gateway
aws ec2 create-internet-gateway \
    --tag-specifications 'ResourceType=internet-gateway,Tags=[{Key=Name,Value=production-igw}]'
aws ec2 attach-internet-gateway --vpc-id vpc-xxx --internet-gateway-id igw-xxx

# Public subnets
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.1.0/24 --availability-zone us-east-1a \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=public-1a},{Key=Tier,Value=public}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.2.0/24 --availability-zone us-east-1b \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=public-1b},{Key=Tier,Value=public}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.3.0/24 --availability-zone us-east-1c \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=public-1c},{Key=Tier,Value=public}]'

# Private subnets (application)
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.11.0/24 --availability-zone us-east-1a \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=private-1a},{Key=Tier,Value=private}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.12.0/24 --availability-zone us-east-1b \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=private-1b},{Key=Tier,Value=private}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.13.0/24 --availability-zone us-east-1c \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=private-1c},{Key=Tier,Value=private}]'

# Database subnets
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.21.0/24 --availability-zone us-east-1a \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=database-1a},{Key=Tier,Value=database}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.22.0/24 --availability-zone us-east-1b \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=database-1b},{Key=Tier,Value=database}]'
aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.23.0/24 --availability-zone us-east-1c \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=database-1c},{Key=Tier,Value=database}]'
]]></code>
      </example>
    </section>

    <section name="nat-gateways">
      <rule testable="true">
        <statement>Production environments MUST deploy one NAT gateway per Availability Zone (one_nat_gateway_per_az=true). Dev/staging MAY use a single NAT gateway to reduce cost. NAT gateways MUST be deployed in public subnets only. Each private subnet's route table MUST point to the NAT gateway in the same AZ.</statement>
        <rationale>A single NAT gateway is a single point of failure and routes all AZ cross-traffic through one AZ (incurring data transfer charges). Per-AZ NAT gateways cost ~$32/month each but eliminate the cross-AZ charge and the SPOF.</rationale>
      </rule>

      <example type="good" title="Per-AZ NAT gateways for production">
        <code lang="bash"><![CDATA[
# Allocate Elastic IPs
aws ec2 allocate-address --domain vpc --tag-specifications 'ResourceType=elastic-ip,Tags=[{Key=Name,Value=nat-1a}]'
aws ec2 allocate-address --domain vpc --tag-specifications 'ResourceType=elastic-ip,Tags=[{Key=Name,Value=nat-1b}]'
aws ec2 allocate-address --domain vpc --tag-specifications 'ResourceType=elastic-ip,Tags=[{Key=Name,Value=nat-1c}]'

# Create NAT Gateways (one per AZ in public subnets)
aws ec2 create-nat-gateway \
    --subnet-id subnet-public-1a \
    --allocation-id eipalloc-1a \
    --tag-specifications 'ResourceType=natgateway,Tags=[{Key=Name,Value=nat-1a}]'
aws ec2 create-nat-gateway \
    --subnet-id subnet-public-1b \
    --allocation-id eipalloc-1b \
    --tag-specifications 'ResourceType=natgateway,Tags=[{Key=Name,Value=nat-1b}]'
aws ec2 create-nat-gateway \
    --subnet-id subnet-public-1c \
    --allocation-id eipalloc-1c \
    --tag-specifications 'ResourceType=natgateway,Tags=[{Key=Name,Value=nat-1c}]'
]]></code>
      </example>
    </section>

    <section name="vpc-endpoints">
      <rule testable="true">
        <statement>All VPCs MUST have a Gateway endpoint for S3 (free) attached to all private route tables. Interface endpoints for ECR (ecr.api + ecr.dkr) and Secrets Manager MUST be created in private subnets when containers or instances pull images or fetch secrets — this eliminates NAT gateway charges for those traffic flows and removes the public internet from the path.</statement>
        <rationale>S3 Gateway endpoint is free and eliminates S3 traffic from NAT gateway bandwidth billing. ECR interface endpoints save ~$0.09/GB vs NAT for image pulls. Secrets Manager via interface endpoint keeps credentials fetches entirely private.</rationale>
      </rule>

      <example type="good" title="S3 gateway endpoint and ECR/Secrets Manager interface endpoints">
        <code lang="bash"><![CDATA[
# Gateway endpoint for S3 (free)
aws ec2 create-vpc-endpoint \
    --vpc-id vpc-xxx \
    --service-name com.amazonaws.us-east-1.s3 \
    --route-table-ids rtb-private \
    --tag-specifications 'ResourceType=vpc-endpoint,Tags=[{Key=Name,Value=s3-endpoint}]'

# Interface endpoint for Secrets Manager
aws ec2 create-vpc-endpoint \
    --vpc-id vpc-xxx \
    --vpc-endpoint-type Interface \
    --service-name com.amazonaws.us-east-1.secretsmanager \
    --subnet-ids subnet-private-1a subnet-private-1b subnet-private-1c \
    --security-group-ids sg-endpoints \
    --private-dns-enabled \
    --tag-specifications 'ResourceType=vpc-endpoint,Tags=[{Key=Name,Value=secretsmanager-endpoint}]'

# Interface endpoints for ECR
aws ec2 create-vpc-endpoint \
    --vpc-id vpc-xxx \
    --vpc-endpoint-type Interface \
    --service-name com.amazonaws.us-east-1.ecr.api \
    --subnet-ids subnet-private-1a subnet-private-1b subnet-private-1c \
    --security-group-ids sg-endpoints \
    --private-dns-enabled
aws ec2 create-vpc-endpoint \
    --vpc-id vpc-xxx \
    --vpc-endpoint-type Interface \
    --service-name com.amazonaws.us-east-1.ecr.dkr \
    --subnet-ids subnet-private-1a subnet-private-1b subnet-private-1c \
    --security-group-ids sg-endpoints \
    --private-dns-enabled
]]></code>
      </example>
    </section>

    <section name="transit-gateway">
      <p>Transit Gateway replaces VPC peering for environments with 3+ VPCs. One TGW attachment per VPC; the TGW handles routing between all attached VPCs via route table propagation. TGW MUST be in the shared-services account for hub-and-spoke topologies.</p>

      <example type="good" title="Create Transit Gateway and attach production + shared-services VPCs">
        <code lang="bash"><![CDATA[
# Create Transit Gateway
aws ec2 create-transit-gateway \
    --description "Central transit gateway" \
    --options "AmazonSideAsn=64512,AutoAcceptSharedAttachments=enable,DefaultRouteTableAssociation=enable,DefaultRouteTablePropagation=enable,VpnEcmpSupport=enable,DnsSupport=enable" \
    --tag-specifications 'ResourceType=transit-gateway,Tags=[{Key=Name,Value=central-tgw}]'

# Attach VPCs
aws ec2 create-transit-gateway-vpc-attachment \
    --transit-gateway-id tgw-xxx \
    --vpc-id vpc-production \
    --subnet-ids subnet-tgw-1a subnet-tgw-1b subnet-tgw-1c \
    --tag-specifications 'ResourceType=transit-gateway-attachment,Tags=[{Key=Name,Value=production-attachment}]'

aws ec2 create-transit-gateway-vpc-attachment \
    --transit-gateway-id tgw-xxx \
    --vpc-id vpc-shared-services \
    --subnet-ids subnet-tgw-shared-1a subnet-tgw-shared-1b \
    --tag-specifications 'ResourceType=transit-gateway-attachment,Tags=[{Key=Name,Value=shared-services-attachment}]'
]]></code>
      </example>
    </section>

    <section name="alb-and-flow-logs">
      <rule testable="true">
        <statement>Application Load Balancers serving production traffic MUST: be internet-facing with HTTPS listener using TLS 1.2+ (ELBSecurityPolicy-TLS13-1-2-2021-06), have an HTTP listener that redirects to HTTPS, and use ACM-managed certificates. VPC Flow Logs MUST be enabled on all production VPCs, writing to CloudWatch Logs with 30-day retention minimum.</statement>
        <rationale>HTTP-only ALBs transmit credentials and session tokens in cleartext. TLS policy enforcement prevents TLS 1.0/1.1 downgrade attacks. Flow logs are mandatory for incident investigation and compliance evidence.</rationale>
      </rule>

      <example type="good" title="ALB with HTTPS and HTTP redirect">
        <code lang="bash"><![CDATA[
# Create ALB
aws elbv2 create-load-balancer \
    --name web-alb \
    --subnets subnet-public-1a subnet-public-1b subnet-public-1c \
    --security-groups sg-alb \
    --scheme internet-facing \
    --type application \
    --ip-address-type ipv4

# Create target group
aws elbv2 create-target-group \
    --name web-targets \
    --protocol HTTP \
    --port 8080 \
    --vpc-id vpc-xxx \
    --target-type instance \
    --health-check-path /health \
    --health-check-interval-seconds 30 \
    --healthy-threshold-count 2 \
    --unhealthy-threshold-count 3

# Create HTTPS listener
aws elbv2 create-listener \
    --load-balancer-arn arn:aws:elasticloadbalancing:region:account:loadbalancer/app/web-alb/xxx \
    --protocol HTTPS \
    --port 443 \
    --certificates CertificateArn=arn:aws:acm:region:account:certificate/xxx \
    --ssl-policy ELBSecurityPolicy-TLS13-1-2-2021-06 \
    --default-actions Type=forward,TargetGroupArn=arn:aws:elasticloadbalancing:region:account:targetgroup/web-targets/xxx

# HTTP to HTTPS redirect
aws elbv2 create-listener \
    --load-balancer-arn arn:aws:elasticloadbalancing:region:account:loadbalancer/app/web-alb/xxx \
    --protocol HTTP \
    --port 80 \
    --default-actions 'Type=redirect,RedirectConfig={Protocol=HTTPS,Port=443,StatusCode=HTTP_301}'

# VPC Flow Logs
aws logs create-log-group --log-group-name /vpc/flow-logs/production
aws ec2 create-flow-logs \
    --resource-type VPC \
    --resource-ids vpc-xxx \
    --traffic-type ALL \
    --log-destination-type cloud-watch-logs \
    --log-group-name /vpc/flow-logs/production \
    --deliver-logs-permission-arn arn:aws:iam::123456789012:role/flow-logs-role \
    --tag-specifications 'ResourceType=vpc-flow-log,Tags=[{Key=Name,Value=production-flow-logs}]'
]]></code>
      </example>

      <antipattern>
        <description>Single NAT gateway shared across all Availability Zones in production.</description>
        <reason>One NAT gateway is a single point of failure. An AZ outage kills outbound internet for instances in all other AZs that route through it. Also incurs cross-AZ data transfer charges.</reason>
      </antipattern>

      <antipattern>
        <description>Deploying application servers in public subnets to avoid NAT gateway cost.</description>
        <reason>Public subnet instances get public IPs and are directly reachable from the internet. Security groups reduce exposure but do not substitute for network-layer isolation. Use private subnets with NAT for production workloads.</reason>
      </antipattern>
    </section>

    <checklist>
      <check>VPC spans 3 AZs with public, private, and database subnets.</check>
      <check>DNS hostnames and DNS support enabled on VPC.</check>
      <check>Per-AZ NAT gateways in production (single NAT only in dev/staging).</check>
      <check>S3 Gateway endpoint on all private route tables.</check>
      <check>Interface endpoints for ECR and Secrets Manager in private subnets.</check>
      <check>VPC Flow Logs enabled writing to CloudWatch Logs.</check>
      <check>ALB HTTPS listener using TLS13-1-2-2021-06 policy.</check>
      <check>HTTP listener redirects to HTTPS (301).</check>
      <check>Non-overlapping CIDR blocks for future Transit Gateway peering.</check>
    </checklist>

    <see-also>
      <link href="https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-best-practices.html"/>
      <link href="https://docs.aws.amazon.com/vpc/latest/tgw/tgw-best-design-practices.html"/>
      <ref slug="aws-vpc-design"/>
      <ref slug="aws-ec2-provisioning"/>
    </see-also>
  </content>
</methodology>
