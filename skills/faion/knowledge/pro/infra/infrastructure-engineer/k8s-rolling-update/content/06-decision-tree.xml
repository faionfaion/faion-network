<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Kubernetes Rolling Update Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="strategy_recreate_in_prod == true">
      <conclusion ref="no-recreate-prod" verdict="switch to RollingUpdate"/>
    </branch>

    <branch when="pdb_missing == true">
      <conclusion ref="pdb-required" verdict="create PDB before next rollout"/>
    </branch>

    <branch when="progress_deadline_missing == true">
      <conclusion ref="progress-deadline" verdict="set progressDeadlineSeconds at most 600"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="max-unavailable-bounded" verdict="ship rolling update"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; strategy_recreate_in_prod == true -&gt; pdb_missing == true -> conclusion ref.</example>
</text>
