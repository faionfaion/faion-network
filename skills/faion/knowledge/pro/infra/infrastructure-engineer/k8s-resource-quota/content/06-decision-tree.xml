<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Kubernetes ResourceQuota Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="limitrange_missing == true">
      <conclusion ref="quota-and-limitrange-together" verdict="apply LimitRange first"/>
    </branch>

    <branch when="storage_caps_missing == true">
      <conclusion ref="compute-and-storage" verdict="add storage + PVC caps"/>
    </branch>

    <branch when="priority_scope_missing == true">
      <conclusion ref="scope-priority-class" verdict="add scopeSelector splits"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="object-count-cap" verdict="cap object counts + commit"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; limitrange_missing == true -&gt; storage_caps_missing == true -> conclusion ref.</example>
</text>
