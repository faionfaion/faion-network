<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Kubernetes Security Hardening Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="psa_not_restricted == true">
      <conclusion ref="psa-restricted" verdict="label namespace enforce restricted; fix workloads"/>
    </branch>

    <branch when="network_policy_missing == true">
      <conclusion ref="network-policy-default-deny" verdict="apply default-deny + explicit allow"/>
    </branch>

    <branch when="container_caps_not_dropped == true">
      <conclusion ref="drop-capabilities" verdict="drop ALL + add minimum set"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="signed-images-only" verdict="wire image policy + commit"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; psa_not_restricted == true -&gt; network_policy_missing == true -> conclusion ref.</example>
</text>
