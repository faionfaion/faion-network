# purpose: Skeleton for the config artefact produced by the gce-managed-instance-groups methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gce-managed-instance-groups.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

name: example-name
template: example-template
placement: example-placement
target_size: 0  # int|number|string
rolling_update: example-rolling_update
health_check: example-health_check
