# purpose: Skeleton for the config artefact produced by the gce-autoscaling methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gce-autoscaling.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

mig: example-mig
metric: example-metric
target: example-target
min_replicas: 0  # int|number|string
max_replicas: 0  # int|number|string
cool_down_seconds: 0  # int|number|string
