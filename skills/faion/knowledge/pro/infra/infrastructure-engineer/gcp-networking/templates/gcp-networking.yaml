# purpose: Skeleton for the config artefact produced by the gcp-networking methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-networking.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

vpc_topology: example-vpc_topology
lb_strategy: example-lb_strategy
egress: example-egress
private_services: example-private_services
firewall_strategy: example-firewall_strategy
