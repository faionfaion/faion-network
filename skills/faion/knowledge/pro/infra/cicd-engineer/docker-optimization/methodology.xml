<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="docker-optimization">
  <metadata>
    <tier>pro</tier>
    <group>infra</group>
    <domain>cicd-engineer</domain>
    <summary># Docker Optimization</summary>
    <difficulty>advanced</difficulty>
    <created>2026-04-26</created>
  </metadata>
  <content>
    <title>Docker Optimization</title>
    <summary># Docker Optimization **Image size reduction, build caching, multi-stage builds, and security hardening** ## Overview Docker image optimization focuses on four key areas: 1. **Image Size** - Smaller images = faster pulls, less storage, reduced attack surface 2. **Build Caching** - Faster builds through intelligent layer ordering 3. **Multi-Stage Builds** - Separate build and runtime environments 4. **Security** - Non-root users, scanning, secrets management ## Quick Reference | Optimization | Impact | Complexity | |--------------|--------|------------| | Slim/Alpine base | -70-80% size | Low | | Multi-stage builds | -50-90% size | Medium | | BuildKit cache mounts | -60% build time | Low | | .dockerignore | Variable | Low | | Non-root user | Security | Low | | Image scanning | Security | Low | ## Base Image Comparison (2025-2026) | Base Image | Size | Use Case | |------------|------|----------| | `scratch` | 0 MB | Static Go/Rust binaries | | `gcr.io/distroless/static` | &lt;2 MB | Maximum security, no shell | | `cgr.dev/chainguard/static` | &lt;2 MB | Wolfi-based, zero CVEs | | `alpine:3.20` | ~5 MB | Minimal with package manager | | `debian:12-slim` | ~40 MB | Balance of tools and size | | `ubuntu:24.04` | ~70 MB | General purpose | ## Modern Best Practices (2025-2026) ### 1. Enable BuildKit (Default in Docker 23+) ```bash export DOCKER_BUILDKIT=1 ``` ### 2. Use Cache Mounts ```dockerfile # syntax=docker/dockerfile:1.7 RUN --mount=type=cache,target=/root/.cache/pip \ pip install -r requirements.txt ``` ### 3. Pin Image Digests for Supply Chain Security ```dockerfile FROM python:3.12-slim@sha256:abc123... ``` ### 4. Scan Images with Docker Scout or Trivy ```bash docker scout cves myapp:latest trivy image myapp:latest ``` ### 5. Use Chainguard/Wolfi Images for Zero-CVE Base ```dockerfile FROM cgr.dev/chainguard/python:latest ``` ## Size Reduction Formula ``` Final Size = Base Image + Dependencies + Application Code - Excluded Files ``` **Target:** Production images &lt; 200 MB (ideally &lt; 100 MB) ## Layer Ordering Principle Order Dockerfile instructions from **least to most frequently changing**: ``` 1. Base image (rarely changes) 2. System packages (occasionally changes) 3. Dependencies (changes with requirements) 4. Application code (changes frequently) ``` ## Tools | Tool | Purpose | Command | |------|---------|---------| | **Dive** | Layer analysis | `dive myapp:latest` | | **Hadolint** | Dockerfile linting | `hadolint Dockerfile` | | **Docker Scout** | Vulnerability scanning | `docker scout cves myapp` | | **Trivy** | Security scanner | `trivy image myapp` | | **DockerSlim** | Image minification | `docker-slim build myapp` | ## Related Files | File | Purpose | |------|---------| | [checklist.md](checklist.md) | Pre-build and post-build verification | | [examples.md](examples.md) | Language-specific Dockerfile examples | | [templates.md](templates.md) | Copy-paste production templates | | [llm-prompts.md](llm-prompts.md) | AI-assisted optimization promp</summary>
    <section name="full-content">
      <p># Docker Optimization **Image size reduction, build caching, multi-stage builds, and security hardening** ## Overview Docker image optimization focuses on four key areas: 1. **Image Size** - Smaller images = faster pulls, less storage, reduced attack surface 2. **Build Caching** - Faster builds through intelligent layer ordering 3. **Multi-Stage Builds** - Separate build and runtime environments 4. **Security** - Non-root users, scanning, secrets management ## Quick Reference | Optimization | Impact | Complexity | |--------------|--------|------------| | Slim/Alpine base | -70-80% size | Low | | Multi-stage builds | -50-90% size | Medium | | BuildKit cache mounts | -60% build time | Low | | .dockerignore | Variable | Low | | Non-root user | Security | Low | | Image scanning | Security | Low | ## Base Image Comparison (2025-2026) | Base Image | Size | Use Case | |------------|------|----------| | `scratch` | 0 MB | Static Go/Rust binaries | | `gcr.io/distroless/static` | &lt;2 MB | Maximum security, no shell | | `cgr.dev/chainguard/static` | &lt;2 MB | Wolfi-based, zero CVEs | | `alpine:3.20` | ~5 MB | Minimal with package manager | | `debian:12-slim` | ~40 MB | Balance of tools and size | | `ubuntu:24.04` | ~70 MB | General purpose | ## Modern Best Practices (2025-2026) ### 1. Enable BuildKit (Default in Docker 23+) ```bash export DOCKER_BUILDKIT=1 ``` ### 2. Use Cache Mounts ```dockerfile # syntax=docker/dockerfile:1.7 RUN --mount=type=cache,target=/root/.cache/pip \ pip install -r requirements.txt ``` ### 3. Pin Image Digests for Supply Chain Security ```dockerfile FROM python:3.12-slim@sha256:abc123... ``` ### 4. Scan Images with Docker Scout or Trivy ```bash docker scout cves myapp:latest trivy image myapp:latest ``` ### 5. Use Chainguard/Wolfi Images for Zero-CVE Base ```dockerfile FROM cgr.dev/chainguard/python:latest ``` ## Size Reduction Formula ``` Final Size = Base Image + Dependencies + Application Code - Excluded Files ``` **Target:** Production images &lt; 200 MB (ideally &lt; 100 MB) ## Layer Ordering Principle Order Dockerfile instructions from **least to most frequently changing**: ``` 1. Base image (rarely changes) 2. System packages (occasionally changes) 3. Dependencies (changes with requirements) 4. Application code (changes frequently) ``` ## Tools | Tool | Purpose | Command | |------|---------|---------| | **Dive** | Layer analysis | `dive myapp:latest` | | **Hadolint** | Dockerfile linting | `hadolint Dockerfile` | | **Docker Scout** | Vulnerability scanning | `docker scout cves myapp` | | **Trivy** | Security scanner | `trivy image myapp` | | **DockerSlim** | Image minification | `docker-slim build myapp` | ## Related Files | File | Purpose | |------|---------| | [checklist.md](checklist.md) | Pre-build and post-build verification | | [examples.md](examples.md) | Language-specific Dockerfile examples | | [templates.md](templates.md) | Copy-paste production templates | | [llm-prompts.md](llm-prompts.md) | AI-assisted optimization prompts | ## Agent Selection | Task | Model | Rationale | |------|-------|----------| | Set up GitHub Actions workflow from template | haiku | Pattern application, simple configuration | | Design CI/CD pipeline architecture | opus | Complex system design with many variables | | Write terraform code for infrastructure | sonnet | Implementation with moderate complexity | | Debug failing pipeline step | sonnet | Debugging and problem-solving | | Implement AIOps anomaly detection | opus | Novel ML approach, complex decision | | Configure webhook and secret management | haiku | Mechanical setup using checklists | ## Sources - [Docker Build Best Practices](https://docs.docker.com/build/building/best-practices/) - [BuildKit Documentation](https://docs.docker.com/build/buildkit/) - [Docker Multi-stage Builds](https://docs.docker.com/build/building/multi-stage/) - [OWASP Docker Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Docker_Security_Cheat_Sheet.html) - [Chainguard Images](https://www.chainguard.dev/chainguard-images) - [Dive Tool](https://github.com/wagoodman/dive) - [Hadolint Linter](https://github.com/hadolint/hadolint) - [Docker Best Practices 2026](https://thinksys.com/devops/docker-best-practices/) --- *Docker Optimization | faion-cicd-engineer* # Agent Integration — Docker Optimization ## When to use - Image is bigger than 200 MB and pull/push latency or storage cost is real (CI minutes, edge deploys, K8s pod start time). - CVE / supply-chain pressure: need to reduce attack surface, switch to distroless / Chainguard, sign + attest. - Build times have crept past 5 minutes and BuildKit cache mounts / multi-stage refactor would actually help. - Multi-arch builds (linux/amd64 + linux/arm64) for ARM laptops + Graviton/Ampere production. - Compliance requirement to run as non-root, read-only filesystem, no shell. ## When NOT to use - Hobby projects, internal-only single-tenant apps where image size genuinely doesn't matter — premature optimization burns more dev time than it saves. - Already-distroless image being "optimized" further by switching base every quarter — diminishing returns. - Apps that need a shell + package manager at runtime (debugging-heavy services). Going scratch/distroless creates ops drag. - Build that's slow because TESTS are slow, not because the image is bloated. Profile before optimizing. ## Where it fails / limitations - **Multi-stage stripping that breaks runtime.** Agents copy only the binary but forget timezone data, CA certs, locale files. Container starts, then breaks on first TLS call. - **Distroless + dynamic linking.** `gcr.io/distroless/static` for a Go binary built with cgo links to glibc → segfault. Must use `distroless/base` or static-build the binary. - **Alpine + glibc apps.** Python / Node wheels compiled against glibc fail on Alpine's musl. Result: silent ABI errors. Use `python:3.12-slim` (Debian) instead. - **Layer order ignored.** Agents `COPY . .` before `RUN pip install` → every code change busts the dep cache. Order: deps file → install → app code. - **`apt-get update` without cleanup.** Layer keeps the apt cache; image grows by 50MB. Always `apt-get update &amp;&amp; apt-get install -y --no-install-recommends X &amp;&amp; rm -rf /var/lib/apt/lists/*` in one RUN. - **Cache mounts not enabled.** Without `# syntax=docker/dockerfile:1.7` and `--mount=type=cache,target=/root/.cache/...`, BuildKit cache mounts are ignored. - **`.dockerignore` missing or wrong.** `.git/`, `node_modules/`, build outputs, IDE configs land in build context → 500MB context for a 10MB project. Build is slow before it even starts. - **Root user as default.** Even when not malicious, K8s `runAsNonRoot: true` PSP rejects the pod. Always `USER 65532:65532`. - **`COPY --chown=` after layer cache invalidation.** Each chown rewrites the layer; large `node_modules` chowned in one COPY = 200MB layer. - **Hardcoded image tags (`python:3.12`).** Agents update Python; tag floats and breaks. Pin by digest: `python:3.12-slim@sha256:...`. - **Buildx multi-arch without QEMU emulation in CI.** Cross-arch build is 10x slower; agents enable it without realizing the cost. - **Health-check shell expansion.** `HEALTHCHECK CMD curl -f http://localhost:8080/h || exit 1` requires a shell — distroless has none. Use exec form + the binary itself. - **`CMD ["sh", "-c", "..."]` in distroless.** Crashes — no shell. Use exec form `CMD ["/app", "--flag", "value"]`. ## Agentic workflow Treat the Dockerfile as a layered cache program, not just a recipe. Have a planning agent classify the build (compiled binary / interpreted with deps / static assets / mixed) and pick a base accordingly (scratch / distroless / chainguard / slim) BEFORE writing the Dockerfile. A second agent writes the Dockerfile with explicit layer ordering, BuildKit cache mounts, multi-stage build, non-root user, and digest-pinned base. A reviewer agent runs `hadolint`, `dive`, and `trivy image` on the built artifact and emits a per-layer breakdown. For prod images, agents also generate SBOM (`syft`) and sign with `cosign`. The build pipeline must produce an attestation; downstream deploys verify it. ### Recommended subagents - `faion-sdd-executor-agent` — drives Dockerfile spec → impl → review; quality gate must include hadolint + trivy + dive layer summary. - `password-scrubber-agent` — Dockerfiles attract `ARG SECRET_KEY` / hardcoded npm tokens / `RUN curl -u ...` patterns. - A custom `dockerfile-layer-auditor` (Sonnet, read-only) — given a Dockerfile, emits per-layer estimated size + cache-hit-rate + flags ordering issues. - A custom `image-cve-gate` — runs trivy/grype on built image; blocks merge if any HIGH/CRITICAL with available fix. ### Prompt pattern ``` Optimize Dockerfile for &lt;app&gt;. Inputs: language + framework, runtime needs (TLS, timezone, fonts, etc.), target arch list, prod or dev image, max acceptable size MB, CVE budget (none/low/medium-or-below). Output: (1) base image choice + rationale (scratch/distroless/chainguard/slim), (2) full Dockerfile with `# syntax=docker/dockerfile:1.7`, multi-stage, BuildKit cache mounts, digest-pinned base, non-root USER, .dockerignore, (3) expected size delta vs. current, (4) hadolint clean output, (5) trivy summary expected. Forbid: ADD when COPY suffices, root user, `apt-get install` without cleanup, COPY of .git or node_modules, base by floating tag, `latest` tag, missing HEALTHCHECK in exec form, dev tools (curl, vim) in prod image. ``` ``` Build &amp; audit: `docker buildx build --platform linux/amd64,linux/arm64 --sbom=true --provenance=true -t test:audit .` ; then run `hadolint`, `dive --json test:audit`, `trivy image --severity HIGH,CRITICAL --exit-code 1 test:audit`. Emit JSON {size_mb, layer_count, hadolint_warnings[], cves: {critical, high, fixable}, dive_efficiency, layers_over_50mb[]}. Reject if size_mb &gt; BUDGET OR fixable critical/high CVEs &gt; 0. ``` ## CLI tools | Tool | Purpose | Install / docs | |------|---------|----------------| | `docker buildx` | Multi-arch + BuildKit-native build | https://docs.docker.com/buildx/ | | `hadolint` | Dockerfile linter — agent's first quality gate | https://github.com/hadolint/hadolint | | `dive` | TUI/JSON layer analysis: spot waste, find fat layers | https://github.com/wagoodman/dive | | `trivy image` / `grype` | CVE scan on built image | https://github.com/aquasecurity/trivy | | `docker scout` | Docker's built-in CVE + recommendations | https://docs.docker.com/scout/ | | `syft` | SBOM generator (SPDX, CycloneDX) | https://github.com/anchore/syft | | `cosign` | Sign + verify images, attest SBOM | https://docs.sigstore.dev/cosign | | `docker-slim` (slim toolkit) | Auto-minify image by tracing runtime usage | https://github.com/slimtoolkit/slim | | `crane` | Container registry / image manipulation | https://github.com/google/go-containerregistry/tree/main/cmd/crane | | `regctl` | Inspect / copy / mutate manifests in registry | https://github.com/regclient/regclient | | `buildah` | Daemonless image build (rootless friendly) | https://buildah.io | | `kaniko` | In-cluster image build, no Docker daemon | https://github.com/GoogleContainerTools/kaniko | ## Services &amp; apps | Service | Type (SaaS/OSS) | Agent-friendly? | Notes | |---------|-----------------|-----------------|-------| | Docker Hub | SaaS | Yes | Free public; rate-limited pulls; mirror via cache for CI. | | GitHub Container Registry (GHCR) | SaaS | Yes | Free for public, tight GHA integration, OIDC auth. | | AWS ECR | SaaS | Yes | Cross-account replication; ECR Public for OSS. | | GCP Artifact Registry | SaaS | Yes | Workload Identity Federation = no static keys. | | Azure Container Registry | SaaS | Yes | ACR Tasks for in-cloud builds. | | Harbor | OSS | Yes | Self-hosted with replication, vulnerability scan, signing. | | Chainguard Images | SaaS / OSS | Yes | Zero-CVE base images; `cgr.dev/chainguard/&lt;lang&gt;` . | | Distroless (Google) | OSS | Yes | `gcr.io/distroless/static`/`base`/`cc`/`python3` etc. | | Wolfi (Chainguard's distro) | OSS | Yes | Underlies Chainguard images; build your own. | | BuildKit (CI cluster) | OSS | Yes | Shared remote cache via `type=registry` for fast CI builds. | | Sigstore / Rekor | OSS | Yes | Public transparency log for signed images. | | Earthly / Dagger / Bazel rules_oci | OSS | Partial | Higher-level build DSLs; agents can use but harder to debug. | ## Templates &amp; scripts See `templates.md` and `examples.md` for language-specific templates. Reference Python multi-stage Dockerfile (≤45 lines) — agent's safe default: ```dockerfile # syntax=docker/dockerfile:1.7 ARG PY_VER=3.12-slim-bookworm ARG PY_DIGEST=sha256:replace_with_actual_digest FROM python:${PY_VER}@${PY_DIGEST} AS builder ENV PIP_NO_CACHE_DIR=1 PIP_DISABLE_PIP_VERSION_CHECK=1 WORKDIR /build RUN --mount=type=cache,target=/root/.cache/pip,sharing=locked \ --mount=type=bind,source=requirements.txt,target=requirements.txt \ pip install --prefix=/install -r requirements.txt FROM python:${PY_VER}@${PY_DIGEST} AS runtime ENV PYTHONDONTWRITEBYTECODE=1 \ PYTHONUNBUFFERED=1 \ PATH="/install/bin:$PATH" \ PYTHONPATH="/install/lib/python3.12/site-packages" RUN groupadd -g 65532 app &amp;&amp; useradd -u 65532 -g 65532 -s /sbin/nologin -d /app app \ &amp;&amp; mkdir -p /app &amp;&amp; chown -R 65532:65532 /app COPY --from=builder --chown=65532:65532 /install /install COPY --chown=65532:65532 ./src /app USER 65532:65532 WORKDIR /app EXPOSE 8000 HEALTHCHECK --interval=30s --timeout=3s --retries=3 \ CMD ["python", "-c", "import urllib.request,sys;sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health',timeout=2).status==200 else 1)"] ENTRYPOINT ["python", "-m", "myapp"] CMD ["serve"] ``` ```bash # .dockerignore must exist; minimal sane default: .git .github .venv .pytest_cache __pycache__ *.pyc node_modules dist build *.log .env* .vscode .idea docs README.md ``` ## Best practices - Multi-stage by default. One stage for build deps, one for runtime; only the runtime stage ships. - Pin base by digest (`python:3.12-slim@sha256:...`); Renovate updates the digest weekly. - Order layers least-to-most-volatile: ARG/ENV → system pkgs → lang runtime → deps file → deps install → app code. - BuildKit cache mounts for package managers (`pip`, `npm`, `apt`, `cargo`, `go mod`). - Always non-root: explicit `USER` with high UID/GID, `runAsNonRoot: true` in K8s. - HEALTHCHECK in exec form (no shell), short timeouts, retries. - `.dockerignore` aggressively — your build context should be &lt; 10MB unless legit. - Multi-arch via buildx with native runners or QEMU; cache per-arch. - SBOM + provenance attestation on every prod build; verify in deploy step with cosign. - Trivy/Grype gate in CI; fail merge on fixable HIGH/CRITICAL. - Read-only root filesystem in K8s with explicit `emptyDir` mounts for any writable paths. ## AI-agent gotchas - Agents copy `FROM ubuntu:latest` from old samples; produces 70MB+ baseline and floating tag. Force slim/distroless and digest pinning. - `RUN curl -fsSL https://... | bash` — supply-chain risk + non-reproducible. Agents do this to install tools; require pinned packages or verified checksums. - Distroless has no shell, no `bash`, no `sh`. Agent's `RUN echo "..." &gt; /etc/...` fails late; use ConfigMap/env at runtime. - `COPY --link` (BuildKit) is faster but breaks if subsequent layer modifies the same paths. Agents use it everywhere blindly. - `ENV PIP_INDEX_URL` to a private mirror with credentials → leaks via `docker history`. Use BuildKit secret mounts. - Multi-stage final stage `FROM scratch` requires explicit copy of CA certs (`COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/`); agents miss this and TLS calls fail. - Buildx + GHA cache: `cache-from: type=gha,scope=...` requires matching `cache-to` and a non-default scope per workflow; agents share scopes and cause cache thrash. - `--platform=linux/amd64,linux/arm64` in `FROM` is wrong — that's for build platform, not target. Use `--platform=$TARGETPLATFORM` in multi-arch builds. - Image size measurement confusion: `docker images` shows uncompressed; registry pull size is compressed. Agents optimize the wrong number. - Human-in-loop checkpoints: switching base image family (Alpine→Debian, Debian→Distroless) MUST run full integration tests; don't auto-merge. - Trivy false positives on dev dependencies: agents add `--ignore-unfixed` and skip fixable issues. Configure ignore via `.trivyignore` with explicit reason + expiry. - Removing `apt-get` packages doesn't shrink the image — they're still in the layer. Use `--no-install-recommends` and clean caches in the same RUN. ## References - Docker build best practices — https://docs.docker.com/build/building/best-practices/ - BuildKit features — https://docs.docker.com/build/buildkit/ - Multi-stage builds — https://docs.docker.com/build/building/multi-stage/ - Distroless — https://github.com/GoogleContainerTools/distroless - Chainguard Images — https://www.chainguard.dev/chainguard-images - Hadolint rules — https://github.com/hadolint/hadolint/wiki - Dive — https://github.com/wagoodman/dive - Trivy — https://aquasecurity.github.io/trivy/ - Sigstore / cosign — https://docs.sigstore.dev/ - OWASP Docker Cheat Sheet — https://cheatsheetseries.owasp.org/cheatsheets/Docker_Security_Cheat_Sheet.html - Slim Toolkit — https://github.com/slimtoolkit/slim - BuildKit cache backends — https://docs.docker.com/build/cache/backends/ # Docker Optimization Checklist ## Pre-Build Checklist ### Base Image Selection - [ ] Using official images from trusted registries - [ ] Using slim/alpine/distroless variants where possible - [ ] Base image pinned to specific version (not `latest`) - [ ] Consider Chainguard/Wolfi images for zero-CVE base - [ ] Image digest pinned for supply chain security (production) ### Dockerfile Structure - [ ] `# syntax=docker/dockerfile:1.7` directive at top (for BuildKit features) - [ ] Instructions ordered from least to most frequently changing - [ ] Dependencies copied before application code - [ ] RUN commands combined with `&amp;&amp;` where appropriate - [ ] Cache cleaning in same layer as package installation ### Build Context - [ ] `.dockerignore` file present and comprehensive - [ ] Excludes: `.git`, `node_modules`, `__pycache__`, `venv`, `.env` - [ ] Excludes: `tests/`, `docs/`, `*.md` (except README if needed) - [ ] Excludes: IDE files (`.vscode/`, `.idea/`) - [ ] Build context size verified (`docker build` shows context size) ### Multi-Stage Build - [ ] Separate build and runtime stages - [ ] Build tools not present in final image - [ ] Only necessary artifacts copied to final stage - [ ] Using `--target` for specific stage builds if needed ### Security - [ ] Non-root user configured (`USER nobody` or specific UID) - [ ] No secrets in Dockerfile or build args - [ ] Using BuildKit secrets for sensitive data during build - [ ] No unnecessary packages installed - [ ] `--no-install-recommends` used with apt-get --- ## Post-Build Checklist ### Image Size Verification - [ ] Image size &lt; 200 MB (ideally &lt; 100 MB for microservices) - [ ] `docker history` shows no unexpectedly large layers - [ ] Dive analysis shows no wasted space - [ ] No dev dependencies in final image ### Security Scanning - [ ] `docker scout cves` or `trivy image` run - [ ] No critical/high vulnerabilities (or documented exceptions) - [ ] Base image CVEs reviewed and acceptable - [ ] Hadolint passes with no errors ### Runtime Verification - [ ] Container runs as non-root user - [ ] Read-only filesystem works (if applicable) - [ ] Resource limits tested (memory, CPU) - [ ] Health check configured and working - [ ] Graceful shutdown works (SIGTERM handling) ### Build Performance - [ ] Build uses cache effectively (rebuild without changes is fast) - [ ] BuildKit cache mounts configured for package managers - [ ] Parallel stages used where dependencies allow - [ ] CI/CD caching configured (registry cache or local) --- ## Quick Commands ```bash # Check image size docker images myapp --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}" # View layer sizes docker history myapp:latest # Deep analysis with Dive dive myapp:latest # Lint Dockerfile hadolint Dockerfile # Security scan docker scout cves myapp:latest trivy image --severity HIGH,CRITICAL myapp:latest # Verify non-root user docker run --rm myapp:latest whoami # Check build context size docker build --no-cache -t test . 2&gt;&amp;1 | grep "Sending build context" ``` --- ## Size Targets by Application Type | Type | Target Size | Base Image | |------|-------------|------------| | Static site (nginx) | &lt; 30 MB | nginx:alpine | | Go microservice | &lt; 20 MB | scratch or distroless | | Rust service | &lt; 30 MB | scratch or distroless | | Node.js API | &lt; 150 MB | node:20-alpine | | Python API | &lt; 200 MB | python:3.12-slim | | Java service | &lt; 250 MB | eclipse-temurin:21-jre-alpine | | Full-stack app | &lt; 300 MB | Multi-stage | --- ## Red Flags Warning signs that optimization is needed: | Issue | Indicator | Solution | |-------|-----------|----------| | Large image | &gt; 500 MB | Multi-stage, slim base | | Slow builds | Cache invalidation | Reorder instructions | | Many layers | &gt; 15 layers | Combine RUN commands | | Root user | `whoami` returns root | Add USER instruction | | Vulnerabilities | Scout/Trivy warnings | Update base, remove packages | | Large context | &gt; 100 MB sent | Fix .dockerignore | --- *Docker Optimization Checklist | faion-cicd-engineer* # Docker Optimization Examples ## Python (FastAPI/Django) ### Before Optimization (~950 MB) ```dockerfile FROM python:3.12 WORKDIR /app COPY . . RUN pip install -r requirements.txt CMD ["python", "main.py"] ``` ### After Optimization (~180 MB, -81%) ```dockerfile # syntax=docker/dockerfile:1.7 FROM python:3.12-slim AS builder WORKDIR /app # Install build dependencies RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \ gcc libpq-dev \ &amp;&amp; rm -rf /var/lib/apt/lists/* # Install Python dependencies COPY requirements.txt . RUN --mount=type=cache,target=/root/.cache/pip \ pip install --user -r requirements.txt # Runtime stage FROM python:3.12-slim WORKDIR /app # Copy only Python packages COPY --from=builder /root/.local /root/.local ENV PATH=/root/.local/bin:$PATH # Copy application COPY . . # Security: non-root user RUN useradd -r -s /bin/false appuser USER appuser EXPOSE 8000 CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"] ``` --- ## Node.js (Next.js) ### Before Optimization (~1.2 GB) ```dockerfile FROM node:20 WORKDIR /app COPY . . RUN npm install RUN npm run build CMD ["npm", "start"] ``` ### After Optimization (~150 MB, -87%) ```dockerfile # syntax=docker/dockerfile:1.7 # Dependencies stage FROM node:20-alpine AS deps WORKDIR /app COPY package*.json ./ RUN --mount=type=cache,target=/root/.npm \ npm ci --only=production # Build stage FROM node:20-alpine AS builder WORKDIR /app COPY package*.json ./ RUN --mount=type=cache,target=/root/.npm \ npm ci COPY . . RUN npm run build # Runtime stage FROM node:20-alpine AS runner WORKDIR /app ENV NODE_ENV=production # Security: non-root user RUN addgroup --system --gid 1001 nodejs &amp;&amp; \ adduser --system --uid 1001 nextjs # Copy only necessary files COPY --from=builder /app/public ./public COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./ COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static USER nextjs EXPOSE 3000 ENV PORT=3000 CMD ["node", "server.js"] ``` --- ## Go (Microservice) ### Before Optimization (~800 MB) ```dockerfile FROM golang:1.22 WORKDIR /app COPY . . RUN go build -o main . CMD ["./main"] ``` ### After Optimization (~12 MB, -98%) ```dockerfile # syntax=docker/dockerfile:1.7 # Build stage FROM golang:1.22-alpine AS builder WORKDIR /app # Cache dependencies COPY go.mod go.sum ./ RUN --mount=type=cache,target=/go/pkg/mod \ go mod download # Build with optimizations COPY . . RUN --mount=type=cache,target=/root/.cache/go-build \ CGO_ENABLED=0 GOOS=linux go build -ldflags="-w -s" -o main . # Runtime stage - scratch (smallest possible) FROM scratch # Copy binary COPY --from=builder /app/main /main # Copy CA certificates for HTTPS COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/ # Security: non-root (numeric UID for scratch) USER 65534 EXPOSE 8080 ENTRYPOINT ["/main"] ``` ### Alternative: Distroless (~15 MB) ```dockerfile FROM gcr.io/distroless/static-debian12 COPY --from=builder /app/main /main USER nonroot:nonroot ENTRYPOINT ["/main"] ``` --- ## Rust (Microservice) ### Optimized (~20 MB) ```dockerfile # syntax=docker/dockerfile:1.7 # Build stage FROM rust:1.75-slim AS builder WORKDIR /app # Cache dependencies RUN cargo new --bin app WORKDIR /app/app COPY Cargo.toml Cargo.lock ./ RUN --mount=type=cache,target=/usr/local/cargo/registry \ cargo build --release &amp;&amp; rm -rf src # Build application COPY src ./src RUN --mount=type=cache,target=/usr/local/cargo/registry \ --mount=type=cache,target=/app/app/target \ cargo build --release &amp;&amp; cp target/release/app /app/binary # Runtime stage FROM gcr.io/distroless/cc-debian12 COPY --from=builder /app/binary /app USER nonroot:nonroot ENTRYPOINT ["/app"] ``` --- ## Java (Spring Boot) ### Before Optimization (~600 MB) ```dockerfile FROM openjdk:21 COPY target/*.jar app.jar ENTRYPOINT ["java", "-jar", "app.jar"] ``` ### After Optimization (~200 MB, -67%) ```dockerfile # syntax=docker/dockerfile:1.7 # Build stage FROM eclipse-temurin:21-jdk-alpine AS builder WORKDIR /app # Copy build files COPY mvnw pom.xml ./ COPY .mvn .mvn COPY src src # Build with layer extraction RUN --mount=type=cache,target=/root/.m2 \ ./mvnw package -DskipTests &amp;&amp; \ java -Djarmode=layertools -jar target/*.jar extract # Runtime stage FROM eclipse-temurin:21-jre-alpine WORKDIR /app # Copy layers (ordered by change frequency) COPY --from=builder /app/dependencies/ ./ COPY --from=builder /app/spring-boot-loader/ ./ COPY --from=builder /app/snapshot-dependencies/ ./ COPY --from=builder /app/application/ ./ # Security RUN addgroup --system javauser &amp;&amp; adduser --system --ingroup javauser javauser USER javauser EXPOSE 8080 ENTRYPOINT ["java", "org.springframework.boot.loader.launch.JarLauncher"] ``` --- ## Nginx (Static Site) ### Optimized (~25 MB) ```dockerfile # syntax=docker/dockerfile:1.7 # Build stage (if using build tools) FROM node:20-alpine AS builder WORKDIR /app COPY package*.json ./ RUN npm ci COPY . . RUN npm run build # Runtime stage FROM nginx:alpine # Copy built files COPY --from=builder /app/dist /usr/share/nginx/html # Custom nginx config (optional) COPY nginx.conf /etc/nginx/conf.d/default.conf # Security: non-root RUN chown -R nginx:nginx /usr/share/nginx/html &amp;&amp; \ chown -R nginx:nginx /var/cache/nginx &amp;&amp; \ touch /var/run/nginx.pid &amp;&amp; \ chown nginx:nginx /var/run/nginx.pid USER nginx EXPOSE 80 CMD ["nginx", "-g", "daemon off;"] ``` --- ## Multi-Language (Python + Node.js) ### Parallel Build (~250 MB) ```dockerfile # syntax=docker/dockerfile:1.7 # Python dependencies (parallel) FROM python:3.12-slim AS python-deps WORKDIR /app COPY requirements.txt . RUN --mount=type=cache,target=/root/.cache/pip \ pip install --user -r requirements.txt # Node dependencies (parallel) FROM node:20-alpine AS node-deps WORKDIR /app COPY package*.json ./ RUN --mount=type=cache,target=/root/.npm \ npm ci --only=production # Build frontend (depends on node-deps) FROM node:20-alpine AS frontend-builder WORKDIR /app COPY --from=node-deps /app/node_modules ./node_modules COPY . . RUN npm run build # Final runtime FROM python:3.12-slim WORKDIR /app # Python packages COPY --from=python-deps /root/.local /root/.local ENV PATH=/root/.local/bin:$PATH # Static files from frontend build COPY --from=frontend-builder /app/dist ./static # Application code COPY . . USER nobody EXPOSE 8000 CMD ["gunicorn", "main:app", "-b", "0.0.0.0:8000"] ``` --- ## .dockerignore (Universal) ```dockerignore # Git .git .gitignore .gitattributes # Dependencies (rebuilt in container) node_modules venv .venv __pycache__ *.pyc *.pyo vendor # Build artifacts dist build *.egg-info target *.jar *.war # IDE and editors .vscode .idea *.swp *.swo .DS_Store # Docker Dockerfile* docker-compose* .docker .dockerignore # Environment and secrets .env .env.* *.local .secrets credentials.json # Testing tests test coverage .coverage .pytest_cache .nyc_output *.test.js *.spec.js # Documentation docs *.md !README.md LICENSE # CI/CD .github .gitlab-ci.yml .travis.yml Jenkinsfile # Logs and temp *.log logs tmp temp *.tmp ``` --- *Docker Optimization Examples | faion-cicd-engineer* # Docker Optimization LLM Prompts AI-assisted prompts for Docker image optimization. --- ## Dockerfile Review ### Analyze Existing Dockerfile ``` Review this Dockerfile for optimization opportunities: ```dockerfile {DOCKERFILE_CONTENT} ``` Analyze for: 1. Image size reduction opportunities 2. Build caching efficiency 3. Security issues 4. Layer optimization 5. Multi-stage build potential Provide specific recommendations with code examples. ``` ### Optimize for Size ``` Optimize this Dockerfile to minimize image size: ```dockerfile {DOCKERFILE_CONTENT} ``` Requirements: - Target size: &lt; {TARGET_SIZE} MB - Must use multi-stage build - Remove all unnecessary dependencies - Use slim/alpine/distroless base - Maintain functionality Provide the optimized Dockerfile with size reduction estimate. ``` --- ## Security Hardening ### Security Audit ``` Perform a security audit of this Dockerfile: ```dockerfile {DOCKERFILE_CONTENT} ``` Check for: 1. Running as root user 2. Secrets in build args or ENV 3. Unnecessary packages 4. Missing security headers 5. Outdated base images 6. Missing image signing Provide remediation for each issue found. ``` ### Harden Dockerfile ``` Harden this Dockerfile following OWASP container security guidelines: ```dockerfile {DOCKERFILE_CONTENT} ``` Apply: 1. Non-root user with specific UID/GID 2. Read-only filesystem compatibility 3. No shell if not needed (distroless) 4. Minimal attack surface 5. Image signing setup Provide the hardened Dockerfile with explanations. ``` --- ## Build Performance ### Optimize Build Time ``` Optimize this Dockerfile for faster CI/CD builds: ```dockerfile {DOCKERFILE_CONTENT} ``` Focus on: 1. Layer ordering for cache efficiency 2. BuildKit cache mounts for package managers 3. Parallel build stages where possible 4. Dependency caching strategy Current build time: {CURRENT_TIME} Target build time: &lt; {TARGET_TIME} ``` ### CI/CD Integration ``` Create a CI/CD-optimized Dockerfile for: Application: {APP_TYPE} CI Platform: {GITHUB_ACTIONS|GITLAB_CI|JENKINS} Registry: {REGISTRY_URL} Requirements: 1. Remote cache support (--cache-from) 2. Multi-platform build support (amd64, arm64) 3. Build args for versioning 4. Layer extraction for caching Include: - Dockerfile - CI configuration snippet - Cache strategy explanation ``` --- ## Language-Specific ### Python Application ``` Create an optimized Dockerfile for this Python application: Dependencies: {requirements.txt or pyproject.toml} Framework: {FASTAPI|DJANGO|FLASK} Python version: {VERSION} Requirements: 1. Multi-stage build 2. pip cache mount 3. Non-root user 4. Health check 5. Target size &lt; 200 MB Include .dockerignore file. ``` ### Node.js Application ``` Create an optimized Dockerfile for this Node.js application: Package manager: {NPM|PNPM|YARN} Framework: {NEXT.JS|EXPRESS|NESTJS} Node version: {VERSION} Requirements: 1. Multi-stage build (deps, builder, runner) 2. npm cache mount 3. Production dependencies only in final stage 4. Non-root user 5. Target size &lt; 150 MB Include .dockerignore file. ``` ### Go Application ``` Create an optimized Dockerfile for this Go application: Go version: {VERSION} CGO required: {YES|NO} Static binary: {YES|NO} Requirements: 1. Multi-stage build 2. go mod cache mount 3. Scratch or distroless final stage 4. Build flags for size optimization (-ldflags="-w -s") 5. Target size &lt; 20 MB ``` --- ## Troubleshooting ### Diagnose Large Image ``` Help diagnose why this Docker image is {SIZE} MB: ```dockerfile {DOCKERFILE_CONTENT} ``` I ran `docker history {IMAGE}` and got: ``` {HISTORY_OUTPUT} ``` Identify: 1. Which layers are largest 2. What's causing the bloat 3. Step-by-step optimization plan 4. Expected size after optimization ``` ### Fix Cache Invalidation ``` My Docker build cache keeps invalidating unexpectedly. Dockerfile: ```dockerfile {DOCKERFILE_CONTENT} ``` Build output shows cache miss at: {BUILD_OUTPUT} Help me: 1. Identify why cache is invalidating 2. Restructure for better cache hits 3. Explain layer caching rules ``` --- ## Migration ### Convert to Multi-Stage ``` Convert this single-stage Dockerfile to multi-stage: ```dockerfile {DOCKERFILE_CONTENT} ``` Create stages for: 1. Dependencies installation 2. Build/compilation 3. Runtime (minimal) Explain what artifacts to copy between stages. ``` ### Migrate Base Image ``` Migrate this Dockerfile from {CURRENT_BASE} to {TARGET_BASE}: ```dockerfile {DOCKERFILE_CONTENT} ``` Consider: 1. Package manager differences (apt vs apk) 2. Missing utilities 3. Path differences 4. User/group handling Provide migration steps and gotchas. ``` --- ## Comparison ### Compare Approaches ``` Compare these two Dockerfile approaches for {USE_CASE}: Approach A: ```dockerfile {DOCKERFILE_A} ``` Approach B: ```dockerfile {DOCKERFILE_B} ``` Compare: 1. Image size 2. Build time 3. Security posture 4. Maintainability 5. CI/CD compatibility Recommend the better approach with justification. ``` --- ## Generation ### Generate from Scratch ``` Generate an optimized Dockerfile for: Application type: {TYPE} Language/Framework: {LANG/FRAMEWORK} Build tool: {TOOL} Production requirements: {REQUIREMENTS} Include: 1. Multi-stage build 2. BuildKit features (syntax directive, cache mounts) 3. Security best practices 4. Health check 5. OCI labels 6. Comprehensive .dockerignore Follow 2025-2026 best practices. ``` ### Generate docker-compose ``` Generate docker-compose.yml for local development: Services: - App: {APP_DOCKERFILE} - Database: {POSTGRES|MYSQL|MONGODB} - Cache: {REDIS|MEMCACHED} Include: 1. Health checks 2. Volume mounts for hot reload 3. Network isolation 4. Environment file support 5. Resource limits ``` --- *Docker Optimization LLM Prompts | faion-cicd-engineer* # Docker Optimization Templates Copy-paste production-ready templates. --- ## Python (pip) ```dockerfile # syntax=docker/dockerfile:1.7 FROM python:3.12-slim AS builder WORKDIR /app # Install build dependencies if needed # RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends gcc libpq-dev \ # &amp;&amp; rm -rf /var/lib/apt/lists/* COPY requirements.txt . RUN --mount=type=cache,target=/root/.cache/pip \ pip install --user -r requirements.txt FROM python:3.12-slim WORKDIR /app COPY --from=builder /root/.local /root/.local ENV PATH=/root/.local/bin:$PATH COPY . . RUN useradd -r -s /bin/false appuser USER appuser EXPOSE 8000 CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"] ``` --- ## Python (Poetry) ```dockerfile # syntax=docker/dockerfile:1.7 FROM python:3.12-slim AS builder ENV POETRY_HOME=/opt/poetry \ POETRY_VIRTUALENVS_IN_PROJECT=true \ POETRY_NO_INTERACTION=1 RUN pip install poetry WORKDIR /app COPY pyproject.toml poetry.lock ./ RUN --mount=type=cache,target=/root/.cache/pypoetry \ poetry install --only=main --no-root COPY . . RUN poetry install --only=main FROM python:3.12-slim WORKDIR /app COPY --from=builder /app/.venv /app/.venv ENV PATH="/app/.venv/bin:$PATH" COPY --from=builder /app . RUN useradd -r -s /bin/false appuser USER appuser EXPOSE 8000 CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"] ``` --- ## Node.js (npm) ```dockerfile # syntax=docker/dockerfile:1.7 FROM node:20-alpine AS deps WORKDIR /app COPY package*.json ./ RUN --mount=type=cache,target=/root/.npm \ npm ci --only=production FROM node:20-alpine AS builder WORKDIR /app COPY package*.json ./ RUN --mount=type=cache,target=/root/.npm \ npm ci COPY . . RUN npm run build FROM node:20-alpine WORKDIR /app ENV NODE_ENV=production RUN addgroup --system --gid 1001 nodejs &amp;&amp; \ adduser --system --uid 1001 appuser COPY --from=deps /app/node_modules ./node_modules COPY --from=builder /app/dist ./dist COPY package*.json ./ USER appuser EXPOSE 3000 CMD ["node", "dist/index.js"] ``` --- ## Node.js (pnpm) ```dockerfile # syntax=docker/dockerfile:1.7 FROM node:20-alpine AS base RUN corepack enable &amp;&amp; corepack prepare pnpm@latest --activate FROM base AS deps WORKDIR /app COPY package.json pnpm-lock.yaml ./ RUN --mount=type=cache,target=/root/.local/share/pnpm/store \ pnpm install --frozen-lockfile --prod FROM base AS builder WORKDIR /app COPY package.json pnpm-lock.yaml ./ RUN --mount=type=cache,target=/root/.local/share/pnpm/store \ pnpm install --frozen-lockfile COPY . . RUN pnpm build FROM node:20-alpine WORKDIR /app ENV NODE_ENV=production RUN adduser --system --uid 1001 appuser COPY --from=deps /app/node_modules ./node_modules COPY --from=builder /app/dist ./dist USER appuser EXPOSE 3000 CMD ["node", "dist/index.js"] ``` --- ## Go (Scratch) ```dockerfile # syntax=docker/dockerfile:1.7 FROM golang:1.22-alpine AS builder WORKDIR /app COPY go.mod go.sum ./ RUN --mount=type=cache,target=/go/pkg/mod \ go mod download COPY . . RUN --mount=type=cache,target=/root/.cache/go-build \ CGO_ENABLED=0 GOOS=linux go build -ldflags="-w -s" -o /app/main . FROM scratch COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/ COPY --from=builder /app/main /main USER 65534 EXPOSE 8080 ENTRYPOINT ["/main"] ``` --- ## Go (Distroless) ```dockerfile # syntax=docker/dockerfile:1.7 FROM golang:1.22-alpine AS builder WORKDIR /app COPY go.mod go.sum ./ RUN --mount=type=cache,target=/go/pkg/mod \ go mod download COPY . . RUN --mount=type=cache,target=/root/.cache/go-build \ CGO_ENABLED=0 GOOS=linux go build -ldflags="-w -s" -o main . FROM gcr.io/distroless/static-debian12 COPY --from=builder /app/main /main USER nonroot:nonroot EXPOSE 8080 ENTRYPOINT ["/main"] ``` --- ## Rust ```dockerfile # syntax=docker/dockerfile:1.7 FROM rust:1.75-slim AS builder WORKDIR /app # Cache dependencies RUN cargo new --bin app WORKDIR /app/app COPY Cargo.toml Cargo.lock ./ RUN --mount=type=cache,target=/usr/local/cargo/registry \ cargo build --release &amp;&amp; rm -rf src COPY src ./src RUN --mount=type=cache,target=/usr/local/cargo/registry \ --mount=type=cache,target=/app/app/target \ cargo build --release &amp;&amp; cp target/release/app /binary FROM gcr.io/distroless/cc-debian12 COPY --from=builder /binary /app USER nonroot:nonroot EXPOSE 8080 ENTRYPOINT ["/app"] ``` --- ## Java (Spring Boot) ```dockerfile # syntax=docker/dockerfile:1.7 FROM eclipse-temurin:21-jdk-alpine AS builder WORKDIR /app COPY mvnw pom.xml ./ COPY .mvn .mvn COPY src src RUN --mount=type=cache,target=/root/.m2 \ ./mvnw package -DskipTests &amp;&amp; \ java -Djarmode=layertools -jar target/*.jar extract FROM eclipse-temurin:21-jre-alpine WORKDIR /app COPY --from=builder /app/dependencies/ ./ COPY --from=builder /app/spring-boot-loader/ ./ COPY --from=builder /app/snapshot-dependencies/ ./ COPY --from=builder /app/application/ ./ RUN addgroup --system javauser &amp;&amp; \ adduser --system --ingroup javauser javauser USER javauser EXPOSE 8080 ENTRYPOINT ["java", "org.springframework.boot.loader.launch.JarLauncher"] ``` --- ## Java (Gradle) ```dockerfile # syntax=docker/dockerfile:1.7 FROM eclipse-temurin:21-jdk-alpine AS builder WORKDIR /app COPY gradlew build.gradle settings.gradle ./ COPY gradle gradle RUN --mount=type=cache,target=/root/.gradle \ ./gradlew dependencies --no-daemon COPY src src RUN --mount=type=cache,target=/root/.gradle \ ./gradlew build -x test --no-daemon &amp;&amp; \ java -Djarmode=layertools -jar build/libs/*.jar extract FROM eclipse-temurin:21-jre-alpine WORKDIR /app COPY --from=builder /app/dependencies/ ./ COPY --from=builder /app/spring-boot-loader/ ./ COPY --from=builder /app/snapshot-dependencies/ ./ COPY --from=builder /app/application/ ./ RUN adduser --system --uid 1001 javauser USER javauser EXPOSE 8080 ENTRYPOINT ["java", "org.springframework.boot.loader.launch.JarLauncher"] ``` --- ## Nginx (Static) ```dockerfile # syntax=docker/dockerfile:1.7 FROM node:20-alpine AS builder WORKDIR /app COPY package*.json ./ RUN npm ci COPY . . RUN npm run build FROM nginx:alpine COPY --from=builder /app/dist /usr/share/nginx/html # COPY nginx.conf /etc/nginx/conf.d/default.conf RUN chown -R nginx:nginx /usr/share/nginx/html &amp;&amp; \ chown -R nginx:nginx /var/cache/nginx &amp;&amp; \ touch /var/run/nginx.pid &amp;&amp; \ chown nginx:nginx /var/run/nginx.pid USER nginx EXPOSE 80 CMD ["nginx", "-g", "daemon off;"] ``` --- ## .dockerignore (Universal) ```dockerignore .git .gitignore node_modules venv .venv __pycache__ *.pyc dist build *.egg-info target .vscode .idea *.swp .DS_Store Dockerfile* docker-compose* .docker .dockerignore .env .env.* *.local tests test coverage .coverage .pytest_cache docs *.md !README.md .github .gitlab-ci.yml *.log tmp temp ``` --- ## Health Check Template ```dockerfile HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \ CMD curl -f http://localhost:8080/health || exit 1 ``` For images without curl: ```dockerfile HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \ CMD wget -q --spider http://localhost:8080/health || exit 1 ``` --- ## Labels Template (OCI) ```dockerfile LABEL org.opencontainers.image.title="My App" \ org.opencontainers.image.description="Application description" \ org.opencontainers.image.version="1.0.0" \ org.opencontainers.image.vendor="Company" \ org.opencontainers.image.source="https://github.com/org/repo" \ org.opencontainers.image.licenses="MIT" ``` --- *Docker Optimization Templates | faion-cicd-engineer*</p>
    </section>
  </content>
</methodology>