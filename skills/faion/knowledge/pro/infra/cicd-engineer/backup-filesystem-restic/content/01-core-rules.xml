<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="backup-filesystem-restic Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for backup-filesystem-restic. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Restic backup scripts MUST load RESTIC_PASSWORD from a secrets manager or password file (RESTIC_PASSWORD_FILE) — never hard-code or pass on the command line. Scripts MUST run `restic check --read-data-subset=5%` at least weekly to detect silent repository corruption before a restore is needed.</statement>
      <rationale source="v1-source">Restic encrypts the repository; a lost or wrong password renders all data unrecoverable. Passing passwords on the CLI exposes them in process listings and shell history. The check command detects pack file corruption that can accumulate silently over months.</rationale>
    </rule>
  </section>
</text>
