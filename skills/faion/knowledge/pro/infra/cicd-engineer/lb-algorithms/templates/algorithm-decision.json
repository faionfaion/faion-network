{
  "__faion_header__": {
    "purpose": "Algorithm-choice artefact matching content/02-output-contract.xml",
    "consumes": "traffic profile (p50/p99), fleet capacity, autoscaling/NAT flags",
    "produces": "algorithm choice + weight vector + rationale string",
    "depends-on": "content/01-core-rules.xml (measure-before-choose, leastconn-default-variable-cost, weighted-by-capacity, no-ip-hash-autoscale)",
    "token-budget-impact": "~150 tokens when loaded as context"
  },
  "algorithm": "weighted-least-connections",
  "fleet_capacity": "heterogeneous",
  "request_cost": "variable",
  "autoscaling": true,
  "nat_behind_lb": false,
  "measured": {"p50_ms": 80, "p99_ms": 240},
  "weights": [100, 100, 50],
  "rationale": "p99/p50 = 3x indicates variable request cost; ASG scales backends; fleet has two large + one small node. leastconn adapts to variance; weights restore proportional capacity."
}
