<?xml version="1.0" encoding="UTF-8"?>
<text id="04-examples" title="GitLab CI/CD: Pipeline Examples">
  <summary>Node.js DAG pipeline, Docker build with Trivy scan, monorepo with change-based filtering.</summary>

  <section title="Node.js DAG Pipeline">
    <example type="good">
      <note>Node.js pipeline with DAG: lint and test run in parallel after install, build runs after both pass.</note>
      <code lang="yaml"><![CDATA[
default:
  image: node:20-alpine

cache:
  key:
    files: [package-lock.json]
  paths: [node_modules/, .npm/]

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_MERGE_REQUEST_IID

stages: [install, test, build, deploy]

install:
  stage: install
  script: npm ci --cache .npm --prefer-offline
  artifacts:
    paths: [node_modules/]
    expire_in: 1 hour

lint:
  stage: test
  needs: [install]
  script: npm run lint
  interruptible: true

test:
  stage: test
  needs: [install]
  script: npm test -- --coverage
  interruptible: true
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage/cobertura-coverage.xml

build:
  stage: build
  needs: [test, lint]
  script: npm run build
  artifacts:
    paths: [dist/]
    expire_in: 1 week
]]></code>
    </example>
  </section>

  <section title="Docker Build with Trivy">
    <example type="good">
      <note>Docker build with BuildKit cache, Trivy vulnerability scan blocking on CRITICAL findings.</note>
      <code lang="yaml"><![CDATA[
variables:
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_TAG: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA
  IMAGE_LATEST: $CI_REGISTRY_IMAGE:latest

docker-build:
  stage: build
  image: docker:24
  services: [docker:24-dind]
  variables:
    DOCKER_BUILDKIT: 1
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build --cache-from $IMAGE_LATEST --build-arg BUILDKIT_INLINE_CACHE=1
        --tag $IMAGE_TAG --tag $IMAGE_LATEST .
    - docker push $IMAGE_TAG
    - docker push $IMAGE_LATEST

trivy-scan:
  stage: scan
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  needs: [docker-build]
  script:
    - trivy image --exit-code 0 --severity HIGH,CRITICAL $IMAGE_TAG
    - trivy image --exit-code 1 --severity CRITICAL $IMAGE_TAG
]]></code>
    </example>
  </section>

  <section title="Monorepo Change Detection">
    <example type="good">
      <note>Only run API service jobs when files in `services/api/` or `shared/` change.</note>
      <code lang="yaml"><![CDATA[
.service-template:
  rules:
    - changes:
        - $SERVICE_PATH/**/*
        - shared/**/*

api-test:
  extends: .service-template
  stage: test
  variables:
    SERVICE_PATH: services/api
  script: cd services/api && npm test

api-build:
  extends: .service-template
  stage: build
  variables:
    SERVICE_PATH: services/api
  needs: [api-test]
  script:
    - cd services/api
    - docker build -t $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA .
    - docker push $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA
]]></code>
    </example>
  </section>

  <reference path="templates/nodejs-pipeline.yml">Complete Node.js pipeline template</reference>
  <reference path="templates/docker-build.yml">Docker build + Trivy scan template</reference>
  <reference path="templates/review-app.yml">Review app deploy/stop lifecycle template</reference>
</text>
