<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="External Secrets Operator Recipe Core Rules" version="1.0.0" est_tokens="900" depth="essential">
  <summary>Testable rules every application of External Secrets Operator Recipe must enforce. These are HARD rules — user-supplied input cannot override them.</summary>
  <section title="The rules">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every step is dry-run before its first real execution. Dry-run artefact (output capture) is stored with the runbook version.</statement>
      <rationale source="SRE runbook discipline (Google SRE book, 2016)">SRE runbook discipline (Google SRE book, 2016).</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Each step has an explicit success signal (command exit, dashboard delta, alert silence). Steps without success signals are rejected.</statement>
      <rationale source="Anti-vague-step principle">'check it works' is not a step.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Rollback path is written before forward path. A runbook without a documented rollback is incomplete.</statement>
      <rationale source="Change-management practice from ITIL v4">Change-management practice from ITIL v4.</rationale>
    </rule>
  </section>
  <section title="Canonical examples">
    <example type="good" title="Anchored application">
      <code lang="text"><![CDATA[
Trigger: Secrets-management migration: Vault / KMS / SOPS (4 weeks)
Artefact: structured output meeting the contract in content/02-output-contract.xml
Evidence: ≥1 named source per rule (URL, ticket, transcript_id)
Decision: downstream consumer can act without re-deriving reasoning
]]></code>
      <note>Demonstrates the closed loop the methodology creates — trigger → artefact → decision — with auditability at each step.</note>
    </example>
    <example type="bad" title="Un-anchored application">
      <code lang="text"><![CDATA[
Trigger: 'we should do this thing'
Artefact: prose paragraph, no schema
Evidence: 'industry best practice'
Decision: ambiguous — every reader interprets differently
]]></code>
      <note>Each ambiguity above is a failure mode catalogued in content/03-failure-modes.xml. The methodology exists to prevent exactly this shape.</note>
    </example>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
  </see-also>
</text>
