Create a production-ready Helm chart for: [APPLICATION NAME]

Application details:
- Language/runtime: [Node.js / Python / Go / Java / other]
- Exposed port: [PORT]
- Health check endpoints: /health/live (liveness), /health/ready (readiness)
- Config method: [environment variables | ConfigMap | Secret]
- External dependencies: [database / cache / message queue, or none]

Required chart components:
1. Chart.yaml with appVersion matching the application version
2. values.yaml with all sections: image, replicaCount, autoscaling, podDisruptionBudget, podAntiAffinity, podSecurityContext, securityContext, resources, service
3. _helpers.tpl with name, fullname, labels, selectorLabels, image, configChecksum helpers
4. deployment.yaml using all helpers; include all three probes (liveness, readiness, startup)
5. service.yaml
6. hpa.yaml (conditional on autoscaling.enabled)
7. pdb.yaml (conditional on podDisruptionBudget.enabled)
8. values.schema.json with required: [image], type validation on numeric fields
9. values-dev.yaml (minimal: replicas=1, autoscaling=false, pdb=false)
10. values-prod.yaml (full HA: minReplicas=3, podAntiAffinity.type=hard)

Security requirements for values.yaml:
- podSecurityContext: runAsNonRoot=true, runAsUser=1000, seccompProfile.type=RuntimeDefault
- securityContext: allowPrivilegeEscalation=false, readOnlyRootFilesystem=true, capabilities.drop=[ALL]
- autoscaling.minReplicas >= 2 in production
- podDisruptionBudget.minAvailable = 2 in production

Output each file as a separate code block labeled with the filename.
