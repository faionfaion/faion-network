<?xml version="1.0" encoding="UTF-8"?>
<text id="02-checklist" title="GitOps Implementation Checklist">
  <summary>Ordered checklist from model selection and repository setup through security, application configuration, monitoring, and disaster recovery.</summary>

  <section title="Model and Tool Selection">
    <list>
      <item>Evaluate push vs pull vs hybrid model based on security requirements, multi-cluster needs, audit requirements</item>
      <item>Select GitOps tool: ArgoCD (UI-heavy, multi-cluster, RBAC/SSO) or FluxCD (lightweight, modular, multi-source)</item>
      <item>Evaluate progressive delivery needs: Argo Rollouts (with ArgoCD) or Flagger (with Flux)</item>
      <item>Document decision rationale</item>
    </list>
  </section>

  <section title="Repository Setup">
    <list>
      <item>Create GitOps repository separate from application source code</item>
      <item>Choose folder-based environment strategy (not branch-based)</item>
      <item>Set up folder structure: apps/, infrastructure/, clusters/, projects/</item>
      <item>Configure branch protection: require PR reviews, status checks, no direct pushes to main</item>
      <item>Set up CODEOWNERS for team-based approval requirements</item>
      <item>Enable signed commits</item>
      <item>Add pre-commit validation (OPA/Conftest for policy checks)</item>
    </list>
  </section>

  <section title="Agent Installation (Pull-Based)">
    <list>
      <item>Verify Kubernetes cluster running v1.24+, kubectl configured, Helm v3 installed</item>
      <item>Create dedicated namespace for GitOps agent</item>
      <item>Install with HA for production: multiple replicas, resource limits, PDB configured</item>
      <item>Configure GitOps repository authentication: SSH key (recommended) or GitHub App</item>
      <item>Verify sync works end-to-end with a test application</item>
    </list>
  </section>

  <section title="Security Configuration">
    <list>
      <item>Disable default admin account in production</item>
      <item>Configure SSO/OIDC integration</item>
      <item>Create projects per team with role-based policies</item>
      <item>Choose secrets solution: External Secrets Operator, Sealed Secrets, or SOPS</item>
      <item>Verify no secrets committed to Git repository</item>
      <item>Enable TLS for all connections between GitOps agent and Git/cluster</item>
      <item>Enable webhook signature verification</item>
    </list>
  </section>

  <section title="Monitoring and Disaster Recovery">
    <list>
      <item>Configure alerts: sync failures, health degradation, drift detected, resource quota</item>
      <item>Integrate with log aggregation and set up audit logging</item>
      <item>Define maintenance windows and deny windows for production</item>
      <item>Document backup procedure for GitOps tool configuration</item>
      <item>Test restore process quarterly</item>
      <item>Document rollback procedure and test it before first production deployment</item>
    </list>
  </section>
</text>
