<?xml version="1.0" encoding="UTF-8"?>
<text id="02-checklists" title="Terraform Checklists: Project Setup, Pre-Apply, Security, Production">
  <summary>Actionable checklists for new project setup, module development, pre-apply review, state management, security audit, and production deployment.</summary>

  <section title="New Project Setup">
    <list>
      <item>Create infrastructure/ with environments/{dev,staging,prod}/ and modules/ directories</item>
      <item>Add .gitignore for .terraform/, *.tfstate, *.tfstate.backup, .terraform.lock.hcl exclusion (wait — commit lock file)</item>
      <item>Create remote state bucket with versioning, encryption, and access-restricted IAM</item>
      <item>Configure S3 use_lockfile=true (Terraform 1.10+) or equivalent per cloud</item>
      <item>Define required_providers with version constraints and required_version for Terraform</item>
      <item>Configure provider authentication via environment variables or IAM roles, never hardcoded</item>
      <item>Set default_tags on provider for automatic resource tagging</item>
    </list>
  </section>

  <section title="Module Development">
    <list>
      <item>main.tf: primary resource definitions</item>
      <item>variables.tf: all inputs with description, type, default, validation blocks</item>
      <item>outputs.tf: all managed resource outputs with descriptions; mark sensitive outputs sensitive=true</item>
      <item>versions.tf: provider version constraints (no provider config — root module only)</item>
      <item>tests/*.tftest.hcl: unit tests covering happy path and error conditions</item>
      <item>No hardcoded environment-specific values in modules</item>
    </list>
  </section>

  <section title="Pre-Apply Review">
    <list>
      <item>Review terraform plan output completely before any apply</item>
      <item>Verify no unexpected destroys or recreates (-/+)</item>
      <item>Verify correct environment is targeted</item>
      <item>Confirm state backend accessible and no concurrent operations in progress</item>
      <item>Validate credentials are valid and not expired</item>
      <item>Prepare rollback procedure before applying to production</item>
    </list>
  </section>

  <section title="Security Audit">
    <list>
      <item>No secrets in .tf files or committed .tfvars files</item>
      <item>State encryption enabled and verified</item>
      <item>Provider credentials via environment variables or IAM roles only</item>
      <item>Security groups follow least privilege (no 0.0.0.0/0 unless explicitly required)</item>
      <item>Encryption enabled on storage resources and databases</item>
      <item>Deletion protection enabled on critical resources (RDS, production buckets)</item>
      <item>IAM roles use least privilege; cross-account access documented</item>
      <item>Resources tagged per tagging policy; data residency requirements met</item>
    </list>
  </section>

  <section title="Troubleshooting Quick Reference">
    <list>
      <item>State lock error: check if another process is running; view lock in backend console</item>
      <item>Provider auth failure: check credential expiry; verify environment variables</item>
      <item>Resource already exists: import with terraform import or rename resource</item>
      <item>Dependency cycle: review depends_on usage; refactor resource definitions</item>
      <item>Version mismatch: check .terraform.lock.hcl; run terraform init -upgrade</item>
      <item>Debug: TF_LOG=DEBUG terraform plan 2&gt;&amp;1 | tee debug.log</item>
    </list>
  </section>
</text>
