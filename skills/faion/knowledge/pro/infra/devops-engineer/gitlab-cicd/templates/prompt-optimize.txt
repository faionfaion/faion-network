Optimize this GitLab CI/CD pipeline for faster execution:

{{PASTE EXISTING .gitlab-ci.yml}}

Focus on:
1. Identify jobs that can run in parallel using needs:
2. Improve caching strategy (key, paths, policy)
3. Reduce artifact sizes (only pass what downstream jobs need)
4. Add interruptible: true to all non-deploy jobs
5. Suggest rules: improvements to skip unnecessary jobs
6. Identify any only/except usage to replace with rules:

Return the optimized pipeline with inline comments explaining each change.
