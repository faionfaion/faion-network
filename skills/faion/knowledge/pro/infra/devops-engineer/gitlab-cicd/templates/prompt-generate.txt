Create a GitLab CI/CD pipeline for a {{LANGUAGE/FRAMEWORK}} project with:

Project details:
- Language: {{Node.js/Python/Go/Java}}
- Framework: {{Next.js/Django/FastAPI/Spring}}
- Testing: {{Jest/Pytest/Go test/JUnit}}
- Container: {{Docker}}
- Deploy target: {{Kubernetes/AWS/GCP/VPS}}

Requirements:
1. Build stage with dependency caching
2. Unit and integration tests with coverage reports
3. Security scanning (SAST, dependency scanning, container scanning)
4. Staging deployment on default branch (automated)
5. Production deployment on tags (manual)
6. Review apps for merge requests with auto_stop_in: 1 week

Use GitLab CI/CD best practices:
- Use rules: instead of only/except
- Use needs: for DAG parallelization
- Cache dependencies with lockfile-hash keys, artifact build outputs
- Include security scanning templates
- Set expire_in on all artifacts
- Use workflow: rules: to prevent duplicate pipelines
