<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Platform Governance: Policy-as-Code + FinOps Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on tenancy + tagging.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="draft-policies">
      <input>Backlog</input>
      <action>Author OPA bundle covering 4 baseline policies.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="wire-cost-dashboards">
      <input>Billing path</input>
      <action>Build per-team dashboard via labels.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="embed-in-golden-paths">
      <input>Templates</input>
      <action>Apply policy gates in CI + admission webhook.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="publish-validate">
      <input>Bundle</input>
      <action>Sign + publish bundle; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
