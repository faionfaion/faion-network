<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Load Balancer Health Checks Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on LB presence + deploy cadence.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="design-endpoints">
      <input>Dependency list</input>
      <action>Define `/healthz` (liveness) and `/ready` (readiness) with bodies.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="tune-parameters">
      <input>SLO + traffic</input>
      <action>Set interval/timeout/fall/rise per anti-flap rule.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="implement-handler">
      <input>Language</input>
      <action>Drop in the language-specific skeleton; bound timeout.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="validate-commit">
      <input>Outputs</input>
      <action>Run validator; smoke-test in staging; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
