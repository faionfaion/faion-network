<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="devops-elk-logstash-pipeline Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for devops-elk-logstash-pipeline. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>pipeline.workers MUST be set to the number of available CPU cores minus one (reserved for the input thread). pipeline.batch.size MUST be at least 500 for Elasticsearch outputs receiving more than 10,000 events/second.</statement>
      <rationale source="v1-source">Under-utilizing workers leaves CPU idle while the output thread waits for bulk ACKs. Batch size below 125 with high event rates causes excessive HTTP round-trips to Elasticsearch, increasing indexing latency and reducing throughput.</rationale>
    </rule>
  </section>
</text>
