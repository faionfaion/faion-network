<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Docker Security Hardening Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on compliance + scope.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="set-user-caps">
      <input>Baseline</input>
      <action>Add USER + cap-drop ALL + add minimum.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="read-only-fs">
      <input>Filesystem</input>
      <action>Enable read-only root + tmpfs.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="secrets-wiring">
      <input>Secrets inventory</input>
      <action>Move secrets to Vault / IRSA / Docker Secrets.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="scan-sign-validate">
      <input>Pipeline</input>
      <action>Wire Trivy + cosign; admission verify; run validator.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
