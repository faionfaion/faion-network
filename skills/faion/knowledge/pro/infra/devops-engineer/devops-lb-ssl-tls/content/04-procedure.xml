<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="SSL/TLS Termination at the Load Balancer Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on encryption ownership + compliance.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="pick-strategy">
      <input>Compliance + arch</input>
      <action>Termination default; re-encrypt for end-to-end; passthrough for PCI.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="set-versions-ciphers">
      <input>Strategy</input>
      <action>Pin TLS 1.2 minimum + AEAD cipher allowlist.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="configure-hsts-ocsp">
      <input>Domains</input>
      <action>Add HSTS + OCSP stapling.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="automate-rotation-validate">
      <input>Cert source</input>
      <action>Wire cert-manager/ACM; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
