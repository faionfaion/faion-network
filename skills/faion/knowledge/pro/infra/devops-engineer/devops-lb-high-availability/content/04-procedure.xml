<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Load Balancer High Availability and Cloud Patterns Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on SLO + DR commitment.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="derive-rto-rpo">
      <input>SLO + scenarios</input>
      <action>Translate SLO into RTO/RPO targets per failure mode.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="select-topology">
      <input>RTO + infra type</input>
      <action>Active-active / cloud-managed / DNS failover decision.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="select-cloud-lb-or-vrrp">
      <input>Topology + workload layer</input>
      <action>Pick ALB/NLB/GLB OR keepalived/VRRP pair.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="schedule-gameday-validate">
      <input>All</input>
      <action>Schedule first game-day; run validator; commit ADR.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
