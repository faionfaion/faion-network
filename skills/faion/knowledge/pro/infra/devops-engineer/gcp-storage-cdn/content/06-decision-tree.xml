<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="GCS and Cloud CDN Configuration Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml; mandatory under F-066.</summary>

    <decision-tree>
      <root-question id="q-preconditions">Are all `Applies If` preconditions satisfied for this input?</root-question>

      <branch when="preconditions == true">
        <question id="q-scope">Is the input scope explicitly named (environment / module / workload)?</question>
        <branch when="scope_explicit == true">
          <conclusion ref="lifecycle-rules-mandatory" verdict="apply the methodology end-to-end starting at procedure step 1"/>
        </branch>
        <branch when="scope_explicit == false">
          <conclusion ref="versioning-for-mutable" verdict="reject input — require explicit scope before applying the methodology"/>
        </branch>
      </branch>

      <branch when="preconditions == false">
        <conclusion ref="skip-this-methodology" verdict="return early; this methodology does not apply"/>
      </branch>
    </decision-tree>

    <example trace="happy-path">
      preconditions == true → scope_explicit == true → apply the methodology end-to-end (rule lifecycle-rules-mandatory).
    </example>
</text>
