<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="kubernetes-deployment Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for kubernetes-deployment. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Configure maxUnavailable and maxSurge explicitly; never rely on defaults for production services.</statement>
      <rationale source="v1-source">Default values (25% each) may cause too-aggressive rollouts for services with tight SLAs. Conservative settings (maxUnavailable: 1, maxSurge: 2) ensure only one pod is unavailable at a time while allowing faster rollout.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Always configure an AnalysisTemplate with concrete success thresholds before enabling automated canary promotion.</statement>
      <rationale source="v1-source">Canary without analysis is just a slow rolling update. The value comes from metric-gated promotion: if error rate exceeds threshold during any step, the rollout automatically aborts and reverts to stable.</rationale>
    </rule>
  </section>
</text>
