<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="devops-elk-architecture Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for devops-elk-architecture. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Production clusters with more than 5 nodes MUST assign dedicated node roles. Master nodes MUST NOT hold data. Heap for master nodes MUST be set to 4 GB; data node heap MUST be 50% of RAM with a hard maximum of 32 GB.</statement>
      <rationale source="v1-source">Mixed master-data nodes allow a data-heavy GC pause to delay cluster state updates, causing false node failures and shard reallocation storms. Heap above 32 GB disables the JVM's compressed ordinary object pointers (CompressedOops), doubling pointer size and wasting memory.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Set number_of_shards based on (expected_index_size_gb / 30). Never set number_of_shards above 5 for indices smaller than 100 GB. Always set number_of_replicas to at least 1 in production.</statement>
      <rationale source="v1-source">Over-sharding (many small shards) causes high per-shard overhead: each shard is a Lucene index with file handles, heap usage, and merge threads. Under-sharding (one massive shard) prevents horizontal search scaling and makes shard recovery on node failure extremely slow.</rationale>
    </rule>
  </section>
</text>
