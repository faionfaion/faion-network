<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Load Balancer Types and Algorithm Selection Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on HA need + ADR requirement.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="classify-traffic">
      <input>Traffic profile</input>
      <action>Tag traffic HTTP-vs-TCP, stateless-vs-sticky, length-variance &gt;10×?</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="select-layer">
      <input>Step 2</input>
      <action>L7 if HTTP; L4 if non-HTTP.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="select-algorithm">
      <input>Step 3 + capacity table</input>
      <action>Apply algorithm decision tree.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="write-adr-validate">
      <input>Steps 2-4</input>
      <action>Write ADR with rationale; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
