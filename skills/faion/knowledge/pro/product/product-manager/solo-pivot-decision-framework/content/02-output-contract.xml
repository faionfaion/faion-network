<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Solo Pivot Decision — Output Contract" version="1.0.0" est_tokens="700" depth="essential">
  <summary>Pins the shape of a `PivotDecision`. Must satisfy this contract before returning to operator.</summary>

  <section title="Required fields">
    <list>
      <item>`pivot_type` — enum {segment, feature, business-model, tech, channel, restart}</item>
      <item>`hypothesis` — object {`change`, `metric`, `baseline`, `target`, `deadline_days`, `kill_criterion`}</item>
      <item>`sunk_cost` — object {`money_usd`, `months`, `surviving_learnings` array ≥ 1}</item>
      <item>`runway_check` — object {`required_weeks`, `available_weeks`, `pass: bool`}</item>
      <item>`recommended_action` — enum {pivot, optimise, sunset, restart}</item>
      <item>`evidence_signals` — array of triggering metrics with baseline values</item>
      <item>`single_axis_check` — object proving only ONE pivot dimension changes</item>
    </list>
  </section>

  <section title="Forbidden patterns">
    <antipattern>
      <description>`pivot_type` populated with multi-axis combinations (e.g. ["segment","feature"]).</description>
      <reason>Violates r1. Multi-axis pivots get relabelled by r5 to "restart" or rejected.</reason>
    </antipattern>
    <antipattern>
      <description>`hypothesis.kill_criterion` field empty or "we'll see".</description>
      <reason>Violates r2. A pivot without a kill criterion never dies; founder bleeds out.</reason>
    </antipattern>
    <antipattern>
      <description>`sunk_cost.surviving_learnings` array empty.</description>
      <reason>Violates r3. If no learning survives, this is a restart per r5.</reason>
    </antipattern>
    <antipattern>
      <description>`recommended_action: pivot` AND `runway_check.pass: false`.</description>
      <reason>Violates r4. Insufficient runway forces sunset regardless of pivot quality.</reason>
    </antipattern>
    <antipattern>
      <description>`evidence_signals` contains aspirations ("I think users would want X") not measured metrics.</description>
      <reason>Pivots are triggered by data, not founder feelings.</reason>
    </antipattern>
  </section>

  <section title="Allowed transformations">
    <list>
      <item>Round `sunk_cost.money_usd` to nearest $500 for sharing.</item>
      <item>Express MRR baselines as YoY delta % if absolute is sensitive.</item>
      <item>Aggregate cohort metrics by month if signal is weekly-noisy.</item>
    </list>
  </section>

  <reference path="templates/pivot-decision.json">JSON Schema the output validates against.</reference>

  <section title="Self-check checklist">
    <list>
      <item>Exactly one axis changes (single-axis-check passes).</item>
      <item>Hypothesis has metric + baseline + target + deadline + kill-criterion.</item>
      <item>Sunk-cost statement enumerates ≥ 1 surviving learning.</item>
      <item>Runway gate passes for action `pivot` / `restart`.</item>
      <item>Recommended action consistent with metric evidence.</item>
    </list>
  </section>
</text>
