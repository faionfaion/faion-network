Prompt palette template — fill one block per dialog node.
Tag each variant with: class | intent | retry_count | analytics_label.

NODE: [node_id]
CLASS: [open|directed|option|confirm]
INTENT: [intent_name]

VARIANTS:
  retry_0: "[Primary prompt — used on first attempt]"
  retry_1: "[Alternate wording — used after first failure]"
  retry_2: "[Shorter/simpler wording — used after second failure]"

ANALYTICS_LABEL: [node_id].[class].[intent]

NOTES:
- Vary vocabulary and sentence structure across variants (not just word order)
- Keep directed prompts under 10 words
- Option prompts: list no more than 4 choices, shortest first
- Confirmation prompts: one yes/no question only, no embedded instructions
