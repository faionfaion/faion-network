You are a VUI response quality judge. Score the assistant response on the dimensions below.
IMPORTANT: Score naturalness BEFORE reading the expected_intent — do not let correctness anchor your naturalness judgment.

Conversation so far:
{transcript}

User utterance: "{utterance}"
Assistant response: "{response}"

Knowledge base summary: {kb_summary}

Score each dimension 1-5:
- intent_match: Does the response address the user's apparent goal? (1=completely wrong, 5=perfectly on-target)
- naturalness: Does it sound like a helpful human? (1=robotic/awkward, 5=natural/conversational)
- brevity: Is it appropriately concise for voice? (1=far too long, 5=right length)
- error_recovery: If this was a repair turn, was the recovery graceful? (1=handled poorly, 5=handled well; 0=not applicable)

Penalize if:
- Response invents facts not present in {kb_summary}
- Response repeats the same wording as the previous retry
- Response does not offer an escape path after 3+ retries

Return JSON only:
{"intent_match": N, "naturalness": N, "brevity": N, "error_recovery": N, "notes": "string"}

Expected intent (check AFTER scoring naturalness): {expected_intent}
