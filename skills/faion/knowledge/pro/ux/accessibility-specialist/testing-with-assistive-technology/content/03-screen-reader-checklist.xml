<?xml version="1.0" encoding="UTF-8"?>
<text id="03-screen-reader-checklist" title="Screen Reader Testing Checklist">
  <summary>Structured checklist covering content, structure, forms, navigation, dynamic content, and custom components for screen reader verification.</summary>

  <section title="Content">
    <rule>
      <statement>Every image must have a meaningful alt attribute; decorative images must have alt="" so they are skipped.</statement>
      <rationale>WCAG 1.1.1 Non-text Content (Level A). A missing or generic alt (e.g., "image.jpg") conveys nothing to SR users.</rationale>
    </rule>
    <list>
      <item>All images have alt text.</item>
      <item>Alt text is meaningful, not generic ("photo", "icon").</item>
      <item>Decorative images have alt="" and no ARIA label.</item>
      <item>Complex images (charts, diagrams) have long descriptions via figcaption or aria-describedby.</item>
      <item>Inline SVG icons have aria-label or title + role="img".</item>
    </list>
  </section>

  <section title="Structure">
    <rule>
      <statement>Headings must form a logical hierarchy (H1 → H2 → H3) with no skipped levels; landmarks must mark major regions.</statement>
      <rationale>SR users navigate by heading (H key in NVDA/JAWS) and by landmark (D key). Missing or skipped headings force linear reading of entire pages.</rationale>
    </rule>
    <list>
      <item>One H1 per page.</item>
      <item>Headings hierarchical — no skipped levels (e.g., H1 → H3).</item>
      <item>Landmarks present: main, nav, header, footer.</item>
      <item>Lists marked up as ul/ol/dl, not styled divs.</item>
      <item>Data tables have th elements with scope; caption provided.</item>
      <item>Reading order matches visual order.</item>
    </list>
  </section>

  <section title="Forms">
    <rule>
      <statement>Every form input must have a programmatically associated label; error messages must be announced via ARIA live region or focus management.</statement>
      <rationale>WCAG 1.3.1, 3.3.1, 3.3.2. "Edit, blank" is the most common screen reader announcement — it means no label is associated.</rationale>
    </rule>
    <list>
      <item>All inputs have associated &lt;label for="..."&gt; or aria-label / aria-labelledby.</item>
      <item>Error messages linked to inputs via aria-describedby.</item>
      <item>Required fields marked with aria-required="true" or required attribute.</item>
      <item>Helper/hint text associated with inputs via aria-describedby.</item>
      <item>Field groups wrapped in fieldset + legend.</item>
      <item>Form validation errors announced — via live region or focus shift to error summary.</item>
    </list>
  </section>

  <section title="Dynamic Content">
    <rule>
      <statement>All asynchronous state changes (loading, success, error, count updates) must be announced via an appropriate ARIA live region.</statement>
      <rationale>SR users receive no visual feedback from DOM mutations unless the accessibility tree signals a change.</rationale>
    </rule>
    <list>
      <item>aria-live="polite" for non-critical updates (search results count, auto-save status).</item>
      <item>aria-live="assertive" for critical errors only — interrupts current SR reading.</item>
      <item>Loading spinners have aria-live or aria-busy="true" on the container.</item>
      <item>Modal dialogs trap focus on open; focus returns to trigger on close.</item>
      <item>Modals have role="dialog", aria-modal="true", and aria-labelledby pointing to title.</item>
    </list>
  </section>

  <section title="Navigation">
    <list>
      <item>Skip link to main content is first focusable element; visible on focus.</item>
      <item>All links have descriptive text — no bare "click here" or "read more".</item>
      <item>Current page indicated with aria-current="page" on nav link.</item>
      <item>Link purpose is clear from link text alone or link text + programmatic context.</item>
    </list>
  </section>
</text>
