# AT Test Case Template (Gherkin)
# One test case per WCAG Success Criterion per AT platform.
# Mark each case as "automatable" (Guidepup) or "human-only".

Feature: [Component or page name] AT accessibility
  WCAG SC: [e.g., 1.3.1 Info and Relationships / 4.1.2 Name, Role, Value]
  AT Platforms: NVDA+Firefox, JAWS+Chrome, VoiceOver+Safari, TalkBack+Chrome

  Scenario: [Descriptive scenario name]
    # Mode: automatable | human-only
    Given [page or component state — e.g., "the signup form is displayed"]
    When  [user action — e.g., "I press Tab to move focus to the email input"]
    Then  [expected announcement — e.g., "the screen reader announces 'Email address, required, edit text'"]

  Scenario: Form error announced on invalid submit
    # Mode: automatable (Guidepup/NVDA)
    Given the signup form is displayed
    When  I type "not-an-email" into the email field and press Enter
    Then  the screen reader announces that the email field has an error within 1500ms

  Scenario: Modal dialog focus managed correctly
    # Mode: human-only
    Given the page is loaded
    When  I activate the "Open dialog" button
    Then  focus moves to the dialog and its accessible name is announced
    And   Tab cycles within the dialog only
    When  I press Escape
    Then  the dialog closes and focus returns to the "Open dialog" trigger
