<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="semantic-tokens-and-modes Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for semantic-tokens-and-modes. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use three tiers exactly — primitive, semantic, component — and ensure component tokens reference semantic tokens, never primitives.</statement>
      <rationale source="v1-source">Component-to-primitive coupling means every primitive rename requires touching component definitions; the semantic layer is the decoupling boundary.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every alias must have a value in every declared mode; missing values cause silent fallbacks that ship wrong colors.</statement>
      <rationale source="v1-source">A mode with a missing alias resolves to undefined behavior; different platforms handle undefined differently, making the regression unpredictable and hard to reproduce.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Reserve one mode per axis as the explicit default; document inheritance rules so consumers of unset modes fall back deterministically.</statement>
      <rationale source="v1-source">Implicit inheritance from an undocumented default causes surprise regressions when the default mode is renamed or reordered.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Bound mode combinations by design decision, not by combinatorial exhaustion; mode explosion (4 color × 3 density × 5 brands = 60 configs) kills maintainability.</statement>
      <rationale source="v1-source">Each supported configuration requires validation; unchecked mode growth creates an exponential testing burden that makes additions prohibitively expensive.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Co-locate contrast partner pairs in the token spec (e.g., `text.on-surface-primary` declares its surface partner); validation requires both halves.</statement>
      <rationale source="v1-source">WCAG contrast must be verified as a pair, not per token; without co-location the validator cannot determine which text color applies to which surface color in each mode.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Generate runtime CSS custom properties at build time, not at app boot; boot-time JS theming causes flicker and hurts Core Web Vitals.</statement>
      <rationale source="v1-source">CSS custom properties set at boot add a render-blocking JS execution step before first paint; build-time generation eliminates this entirely.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use agents for mechanical token operations (alias generation, mode-matrix expansion, contrast checks, format conversion); keep humans on naming and semantic decisions.</statement>
      <rationale source="v1-source">Agents are reliable at deterministic transformations but poor at naming intent; auto-promoted primitive-to-semantic names are the one place where human meaning is irreplaceable.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Treat a token rename as a breaking API change; use codemods or alias deprecation periods, never silent removals.</statement>
      <rationale source="v1-source">Consumers reference tokens as strings in code, config, and MDX; a silent removal breaks builds at runtime without compile-time warning.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Run the mode coverage check in CI on every PR that touches token source files; block on any missing (alias, mode) pair.</statement>
      <rationale source="v1-source">A missing value detected in CI costs a fix; a missing value that ships costs a themed-product regression report, a hotfix, and user trust.</rationale>
    </rule>
  </section>
</text>
