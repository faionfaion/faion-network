<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Design Tokens Fundamentals Worked Example" version="1.1.0" est_tokens="700" depth="essential">
  <summary>End-to-end example applying Design Tokens Fundamentals to a concrete case.</summary>

  <section title="Button primary background token">
    <example>
      <code lang="markdown"><![CDATA[
```json
{
  "primitive": { "color": { "blue-500": { "$value": "#1c5fdb", "$type": "color" } } },
  "semantic": { "color": { "text": { "primary": { "$value": "{primitive.color.blue-500}", "$type": "color", "contrast_ratio_on_white": 6.8, "pass": true } } } },
  "component": { "button": { "primary": { "background": { "$value": "{semantic.color.text.primary}", "$type": "color" } } } }
}
```

Dark theme overrides only semantic.color.text.primary → primitive.color.blue-200; component layer untouched.
]]></code>
    </example>
    <note>Three-tier flow; contrast ratio baked; dark theme via semantic override only.</note>
  </section>
</text>
