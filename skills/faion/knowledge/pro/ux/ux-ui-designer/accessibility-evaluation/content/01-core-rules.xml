<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="accessibility-evaluation Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for accessibility-evaluation. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Run automated scanning (axe-core, Pa11y, Lighthouse) as stage 1, but never claim compliance based on automated results alone — tools catch 30-40% of issues.</statement>
      <rationale source="v1-source">Deque and WebAIM data (2024) confirm automated tools miss the majority of issues: ARIA misuse, focus order in modals, live-region behavior, cognitive accessibility, and text-over-image contrast are invisible to headless scanners.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Pin the axe-core version per repo; rule additions in minor versions create phantom regressions that derail review cycles.</statement>
      <rationale source="v1-source">axe-core releases new rules in minor versions. An "axe-clean" baseline at v4.8 may produce violations at v4.9 for the same unmodified code, causing false regression alerts.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Test all of: Tab navigation reaching every interactive element, visible focus at all times, logical tab order matching visual flow, skip links, no keyboard traps, and documented shortcuts not conflicting with AT shortcuts.</statement>
      <rationale source="v1-source">WCAG 2.2 SC 2.1.1, 2.4.3, 2.4.7, and 2.4.11 cover these criteria. Each requires separate verification; automated tools cover none of them reliably.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>For SPA frameworks, audit after route transitions and after async state changes; focus management at route change is the #1 SPA accessibility failure.</statement>
      <rationale source="v1-source">Single-page apps frequently drop focus to the document body after navigation, forcing screen reader users to start re-reading from the top of the page.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use established AT pairings for screen reader testing: NVDA + Firefox, JAWS + Chrome, VoiceOver + Safari on Mac, VoiceOver + Safari on iOS, TalkBack + Chrome on Android.</statement>
      <rationale source="v1-source">AT behavior differs by browser pairing. A finding present in NVDA+Firefox may not reproduce in JAWS+Chrome — and vice versa. Pairings are the industry-standard evidence baseline for VPAT/ACR documentation.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Never accept a screen reader finding from an agent that does not include an audio recording or verbatim speech transcript as evidence; fabricated AT transcripts are undetectable and create false compliance records.</statement>
      <rationale source="v1-source">LLMs cannot reproduce NVDA/JAWS/VoiceOver speech buffers. Agent-invented transcripts appear plausible but are not evidence of conformance.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Classify every finding into Critical (blocks access completely, fix immediately), High (major barrier with difficult workaround, this sprint), Medium (creates difficulty, next sprint), or Low (enhancement, backlog).</statement>
      <rationale source="v1-source">Consistent priority classification lets stakeholders make resource decisions and produces comparable metrics across audit cycles. Unclassified findings stall remediation planning.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Treat each WCAG criterion as a separate test case with a stable ID so re-runs produce diffs, not full rewrites.</statement>
      <rationale source="v1-source">Without stable IDs, every re-run generates a new report that cannot be compared to the previous one. Diffs are the only way to track remediation progress and detect regressions.</rationale>
    </rule>
  </section>
</text>
