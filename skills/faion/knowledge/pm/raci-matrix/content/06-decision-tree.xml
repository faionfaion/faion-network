<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="RACI Matrix Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-1">Violation type?</root-question>
    <branch when="signal == no_a">
      <conclusion ref="exactly-one-accountable" verdict="escalate to sponsor to pick an Accountable"/>
    </branch>
    <branch when="signal == multiple_a">
      <conclusion ref="exactly-one-accountable" verdict="force single choice; surface conflict to sponsor"/>
    </branch>
    <branch when="signal == no_r">
      <conclusion ref="at-least-one-responsible" verdict="assign R or remove task from scope"/>
    </branch>
    <branch when="signal == c_gt_4">
      <conclusion ref="consulted-cap-4" verdict="reduce to essential C; convert remainder to I"/>
    </branch>
    <branch when="signal == all_i_in_row">
      <conclusion ref="role-titles-not-names" verdict="role not needed for this task — remove from matrix"/>
    </branch>
  </decision-tree>
</text>
