# purpose: YAML template for KPI definitions + thresholds + sources.
# consumes: see content/02-output-contract.xml inputs for reporting-basics
# produces: report
# depends-on: content/01-core-rules.xml + content/02-output-contract.xml
# token-budget-impact: ~200-1000 tokens when loaded as context

# kpi-thresholds.yaml
# Skeleton for Reporting Basics. Fill per artefact.

artefact_id: <slug-YYYY-MM-DD>
template_version: "1.1.0"
fields: {}
evidence: []
status: draft
