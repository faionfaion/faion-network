<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Product Lifecycle Management Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>Step-by-step procedure for applying product lifecycle management end-to-end.</summary>

  <section title="Procedure">

    <step n="1" name="collect-signals">
      <input>Product analytics dashboard</input>
      <action>Extract yoy_growth, monthly_churn, NPS, market_share for the product.</action>
      <output>Signals object.</output>
      <decision-gate>If any signal missing, escalate to data team.</decision-gate>
    </step>
    <step n="2" name="classify-stage">
      <input>Signals + stage matrix</input>
      <action>Run stage-suggest.sh to propose a stage based on signal thresholds.</action>
      <output>Proposed stage tag.</output>
      <decision-gate>If signal pattern is mixed, escalate to portfolio review.</decision-gate>
    </step>
    <step n="3" name="match-strategy">
      <input>Stage tag + stage-strategy-guide.md</input>
      <action>Label the tag low/medium/high by how far the four signals agree, author the investment_strategy paragraph matching the stage, and name the stage-specific playbook it routes to.</action>
      <output>Investment strategy text + stage_confidence + routed_playbook.</output>
      <decision-gate>If the signals disagree, the label cannot be high; escalate a mixed reading to portfolio review rather than rounding it up.</decision-gate>
    </step>
    <step n="4" name="write-transition-memo">
      <input>Last-quarter tag + this-quarter tag</input>
      <action>Write transition_memo if stage changed; else write 'no change' memo with signals.</action>
      <output>Transition memo.</output>
    </step>
    <step n="5" name="sunset-if-decline">
      <input>Stage tag</input>
      <action>If stage == decline, kick off sunset plan (templates/sunset-plan.md) within 30 days.</action>
      <output>Sunset plan linked.</output>
      <decision-gate>If stage==decline and no sunset plan after 30d, escalate.</decision-gate>
    </step>

  </section>
</text>
