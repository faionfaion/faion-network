<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Feedback Management Decision Tree" version="1.1.1" est_tokens="550" depth="essential">
  <summary>Routing tree on observable signals to apply / skip / route-elsewhere. Each leaf references a rule id from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-volume">Inbound feedback volume &gt; 20 items/day across &gt;= 2 channels?</root-question>
    <branch when="high_volume == false">
      <conclusion ref="skip-this-methodology" verdict="read items directly; pipeline overhead exceeds value"/>
    </branch>
    <branch when="high_volume == true">
      <question id="q-act">PM committed to dispose top themes monthly?</question>
      <branch when="will_act == false">
        <conclusion ref="skip-this-methodology" verdict="sentiment-only loops amplify noise without action; commit first"/>
      </branch>
      <branch when="will_act == true">
        <question id="q-compliance">Regulated content (HIPAA / GDPR DSAR) requiring DPA review?</question>
        <branch when="compliance_blocked == true">
          <conclusion ref="skip-this-methodology" verdict="run DPA + redaction first; do not feed raw to remote LLM"/>
        </branch>
        <branch when="compliance_blocked == false AND outbound_auto_send == true">
          <conclusion ref="draft-and-queue-before-send" verdict="switch close-loop agent to draft-and-queue; human approves before send"/>
        </branch>
        <branch when="compliance_blocked == false AND dedup_method == 'full-text'">
          <conclusion ref="embedding-dedup" verdict="switch dedup to embedding similarity (cosine >= 0.85)"/>
        </branch>
        <branch when="compliance_blocked == false">
          <conclusion ref="single-canonical-store" verdict="build canonical store + fixed taxonomy + monthly review"/>
        </branch>
      </branch>
    </branch>
  </decision-tree>

  <example trace="acme-saas-2026">
    high_volume=true -&gt; will_act=true -&gt; compliance_blocked=false -&gt; apply pipeline (rule single-canonical-store).
  </example>
</text>
