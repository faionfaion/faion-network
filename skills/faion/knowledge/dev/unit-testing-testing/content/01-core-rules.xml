<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="Unit Testing (FIRST + AAA) Core Rules" version="1.1.0" est_tokens="600" depth="essential">
  <summary>7 testable rules every application of unit-testing enforces. Each rule has a statement, a rationale anchored to industry practice, and a source citation.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every unit test satisfies FIRST: Fast (&lt;10ms), Isolated (no shared state), Repeatable (same result every run), Self-validating (pass/fail in code), Timely (written with or before the code).</statement>
      <rationale source="Beck, Test Driven Development">Slow or order-dependent tests get disabled; non-self-validating tests rot.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Each test has visible Arrange-Act-Assert sections separated by blank lines or comments.</statement>
      <rationale source="Beck, xUnit Test Patterns">Reviewers locate the failing phase from the diff alone.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Test names follow `method_scenario_expected` OR `should_when` OR `given_when_then`. Pick one per codebase and never mix.</statement>
      <rationale source="Osherove, The Art of Unit Testing">Mixed conventions force readers to mentally translate; one convention compresses.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Assert on observable behavior, never on private state via reflection or `_internal` access.</statement>
      <rationale source="Meszaros, xUnit Test Patterns">Implementation-coupled tests break on refactor; behavior-coupled tests survive.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Target ≥80% line coverage AND ≥70% branch coverage on new code. Mutation coverage is an audit signal, not a CI gate.</statement>
      <rationale source="Industry consensus / Stryker docs">Line-only coverage misses untested branches; mutation is too slow for every PR.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>One logical assertion per test. Use bundled-fact helpers (e.g., `assertUserMatches`) only when the bundle is the unit of behavior.</statement>
      <rationale source="Beck, xUnit Test Patterns">Multi-assertion tests stop at the first failure and hide downstream issues.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>When the test would touch a database, a network socket, the filesystem outside a temp dir, or wall-clock sleep - anything that cannot meet the Fast (&lt;10ms) and Isolated bounds of r1 - it MUST NOT be filed as a unit test, MUST NOT be counted toward the &gt;=80% line / &gt;=70% branch targets of r5, and MUST NOT enter the `avg_runtime_ms` of the 02-output-contract record. It MUST be moved to the integration or E2E layer using the marker or build tag defined by the language-runner methodology (`testing-pytest`, `testing-go`, `testing-javascript`), or the pure logic MUST be extracted behind an injected collaborator and that logic unit-tested instead.</statement>
      <rationale source="Martin Fowler - UnitTest (bliki); Beck, Test Driven Development">A test that opens a connection is the one that flakes, takes 30 seconds and gets the whole suite disabled - the exact failure the Applies-If 'why does this unit suite take 30 seconds' names. Counting it as a unit test also inflates coverage with lines that only pass when the database is up.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
    <link path="content/06-decision-tree.xml" />
  </see-also>
</text>