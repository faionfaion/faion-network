<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="files-reference Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Does the task language/area map to exactly one named section of this catalog?</root-question>
    <branch when="section_matches == 1">
      <question>Does the picker find at least one slug whose verbatim name fits the task verb+noun?</question>
      <branch when="candidate_count >= 1">
        <question>Does Glob resolve at least one candidate path?</question>
        <branch when="resolved_count >= 1">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="resolved_count == 0">
          <conclusion ref="r4"/>
        </branch>
      </branch>
      <branch when="candidate_count == 0">
        <conclusion ref="skip-this-methodology"/>
      </branch>
    </branch>
    <branch when="section_matches == 0 OR section_matches > 1">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="task='Write pytest fixture' -> section='python' -> slug='python' -> resolved=true -> run-the-checklist"/>
  </decision-tree>
</text>
