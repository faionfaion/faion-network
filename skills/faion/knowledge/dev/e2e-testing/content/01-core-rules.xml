<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="e2e-testing Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Nine testable rules covering scope, locators, waits, isolation, POM, and mocking for Playwright E2E.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement source="playwright.dev/best-practices">E2E tests cover complete user journeys only — not individual component behavior or API logic.</statement>
      <rationale>E2E tests are 10-20x slower than unit tests. Testing logic already covered at cheaper layers wastes CI budget and creates maintenance cost without adding signal.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement source="testing-pyramid-folklore">Limit the critical-path E2E suite to less than 50 tests. Gate non-critical tests to a nightly run.</statement>
      <rationale>Suites beyond 50 tests routinely exceed 30 minutes in CI, causing teams to skip or disable them. A tight budget forces selection of only the most valuable journeys.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement source="playwright.dev/locators">Use only getByTestId(), getByRole(), or getByLabel() locators. Never use CSS selectors, IDs, or XPath.</statement>
      <rationale>CSS selectors break on any style refactor. data-testid attributes are stable and signal intent to developers that the element is tested. getByRole aligns tests with accessibility expectations.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement source="playwright.dev/actionability">Never use page.waitForTimeout() or hard-coded sleeps. Use Playwright's built-in auto-wait via expect(locator).toBeVisible() or explicit network/navigation waits.</statement>
      <rationale>Hard-coded waits are the primary source of flakiness in E2E suites. Playwright auto-waits for actionability; explicit assertions wait for the right state.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement source="playwright.dev/test-isolation">Each test must seed its own data via API in beforeEach and clean up in afterEach. Never rely on data left by another test.</statement>
      <rationale>Test-order dependencies cause cascading failures that are difficult to diagnose. API-based seeding with timestamped unique data prevents unique_email violations.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement source="playwright.dev/auth">Reuse storageState for authenticated sessions — log in once in global setup; use saved cookies for the rest of the suite.</statement>
      <rationale>Logging in via UI per test adds 2-5 seconds each. Saved auth state reduces suite time by 30-50% on auth-heavy applications.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement source="POM-canon-Fowler">Create one Page Object class per route. Name methods after user intent, not DOM events: login() not clickSubmitButton().</statement>
      <rationale>Intent-based names make test specs read like user stories. DOM-event names couple the test to the implementation and break on any UI restructure.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement source="v1-source">Create and clean up test data via API endpoints in beforeEach/afterEach, never through the UI.</statement>
      <rationale>UI-based seeding tests something other than the actual test scenario and is 5-10x slower. API seeding is deterministic and isolated.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement source="playwright.dev/network">Mock all third-party API calls at the network boundary using page.route(). Never depend on live SaaS in CI.</statement>
      <rationale>Real third-party calls add 1-2% flake rate from network variability. Stripe, Auth0, and similar services have sandbox modes but even those add latency and can have outages.</rationale>
    </rule>
    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement source="self">Run all rules above against the candidate spec file before marking the suite ready.</statement>
      <rationale>Aggregate conclusion target for the decision tree's positive branch.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement source="self">If preconditions fail, do not apply this methodology — defer to unit/integration testing or skip the test entirely.</statement>
      <rationale>Aggregate conclusion target for the decision tree's negative branch.</rationale>
    </rule>
  </section>
</text>
