{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/dev/claude-md-creation/skeleton.vars.schema.json",
  "title": "skeleton template variables",
  "description": "Values for templates/skeleton.md.j2 and templates/skeleton.html.j2. `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "layout": {
      "type": "string",
      "title": "Layout",
      "description": "Is the repository a single package or a monorepo with several apps?",
      "enum": ["single", "monorepo"]
    },
    "repo_name": {
      "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/repo_name"
    },
    "template": {
      "type": "string",
      "title": "Template",
      "description": "Which CLAUDE.md skeleton was the file built from: minimal, standard or monorepo?",
      "enum": ["minimal", "standard", "monorepo"]
    },
    "token_count": {
      "type": "integer",
      "title": "Token count",
      "description": "How many tokens does the committed CLAUDE.md measure?",
      "minimum": 1
    }
  },
  "required": [
    "layout",
    "repo_name",
    "template",
    "token_count"
  ],
  "x-faion-dictionary-refs": 1
}
