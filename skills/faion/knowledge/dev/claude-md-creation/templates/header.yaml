# purpose: Frontmatter schema for the claude-md-creation record.
# consumes: Header block of templates/skeleton.md.
# produces: Validator-friendly frontmatter for the record file.
# depends-on: scripts/validate-claude-md-creation.py.
# token-budget-impact: zero — pure schema.

version: "1.0.0"
repo: "acme/intake-api"
layout: "single"
template: "standard"
token_count: 480
