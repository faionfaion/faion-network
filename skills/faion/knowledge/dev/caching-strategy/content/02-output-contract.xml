<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Caching Strategy Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema (draft-07) for the caching-strategy artefact. Validator enforces the listed required fields + cross-field checks.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/caching-strategy.json",
  "type": "object",
  "required": [
    "strategy_id",
    "pattern",
    "ttl_seconds_base",
    "ttl_jitter_pct",
    "invalidation",
    "single_flight",
    "hit_rate_target"
  ],
  "properties": {
    "strategy_id": {
      "type": "string",
      "pattern": "^CACHE-[A-Z0-9-]{2,40}$"
    },
    "pattern": {
      "type": "string",
      "enum": [
        "cache-aside",
        "read-through",
        "write-through",
        "write-behind"
      ]
    },
    "ttl_seconds_base": {
      "type": "integer",
      "minimum": 1,
      "maximum": 86400
    },
    "ttl_jitter_pct": {
      "type": "integer",
      "minimum": 5,
      "maximum": 50
    },
    "invalidation": {
      "type": "object",
      "required": [
        "mode"
      ],
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "ttl-only",
            "write-through",
            "pubsub-event"
          ]
        }
      }
    },
    "single_flight": {
      "type": "boolean"
    },
    "hit_rate_target": {
      "type": "number",
      "minimum": 0.5,
      "maximum": 1.0
    },
    "store": {
      "type": "string",
      "enum": [
        "redis",
        "valkey",
        "memcached",
        "in-memory",
        "cdn"
      ]
    }
  }
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "strategy_id": "CACHE-PRICING-GET",
  "pattern": "cache-aside",
  "ttl_seconds_base": 300,
  "ttl_jitter_pct": 20,
  "invalidation": {
    "mode": "pubsub-event",
    "channel": "pricing-changes"
  },
  "single_flight": true,
  "hit_rate_target": 0.85,
  "store": "redis"
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "strategy_id": "x",
  "pattern": "cache-aside",
  "ttl_seconds_base": 86400,
  "ttl_jitter_pct": 0,
  "invalidation": {
    "mode": "hope"
  },
  "single_flight": false,
  "hit_rate_target": 0.4
}
]]></code>
    <note>strategy_id pattern fails; ttl_jitter_pct=0 (no jitter); invalidation.mode not in enum; single_flight=false; hit_rate_target below floor.</note>
  </example>

  <section title="Forbidden patterns">
    <list>
      <item>ttl_jitter_pct &lt; 5.</item>
      <item>invalidation.mode not in {ttl-only, write-through, pubsub-event}.</item>
      <item>single_flight=false on a multi-replica deployment.</item>
      <item>hit_rate_target &lt; 0.5.</item>
    </list>
  </section>

  <reference path="scripts/validate-caching-strategy.py">Validator script</reference>
</text>
