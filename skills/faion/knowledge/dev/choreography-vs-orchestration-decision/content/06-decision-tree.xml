<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="choreography-vs-orchestration-decision Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml.</summary>

  <decision-tree>
      <root-question id="q-root">Is workflow ownership naturally distributed across multiple teams that already operate event streams?</root-question>
      <branch when="ownership_already_distributed == false">
        <conclusion ref="r5-ownership-cost" verdict="pattern=orchestration via a saga coordinator"/>
      </branch>
      <branch when="ownership_already_distributed == true">
        <question id="q-next">workflow_steps &lt;= 5?</question>
        <branch when="workflow_steps &lt;= 5">
          <conclusion ref="r5-ownership-cost" verdict="pattern=choreography (event-driven, no coordinator)"/>
        </branch>
        <branch when="workflow_steps &gt; 5">
          <conclusion ref="r5-ownership-cost" verdict="pattern=orchestration; debug cost of choreography dominates"/>
        </branch>
      </branch>
  </decision-tree>
</text>
