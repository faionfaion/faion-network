# purpose: Frontmatter schema for the llm-friendly-architecture audit record.
# consumes: Header block of templates/skeleton.md.
# produces: Validator-friendly frontmatter for the record file.
# depends-on: scripts/validate-llm-friendly-architecture.py.
# token-budget-impact: zero — pure schema.

version: "1.0.0"
owner: "role:<handle>"
commit_sha: "<7-40 hex>"
line_limit: 250
