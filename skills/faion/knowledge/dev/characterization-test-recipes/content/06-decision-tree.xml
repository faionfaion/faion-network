<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Characterization Test Recipes Decision Tree" version="1.0.1" est_tokens="500" depth="essential">
  <summary>Routes a refactor candidate by preconditions → coverage → signoff. Branches name observable manifest features; leaves reference rules.</summary>
  <decision-tree>
    <root-question id="q-applies">Are prerequisites satisfied: legacy module callable, input samples reachable, snapshot lib present?</root-question>
    <branch when="prereqs_met == false">
      <conclusion ref="skip-this-methodology" verdict="abort"/>
    </branch>
    <branch when="prereqs_met == true AND deleting_code == true">
      <conclusion ref="skip-this-methodology" verdict="skip-deleting-code"/>
    </branch>
    <branch when="prereqs_met == true AND deleting_code == false AND bug_on_purpose == true">
      <conclusion ref="skip-this-methodology" verdict="block-bug-on-purpose"/>
    </branch>
    <branch when="prereqs_met == true AND deleting_code == false AND bug_on_purpose == false AND branch_coverage_pct &lt; 70">
      <conclusion ref="r4-sweep-termination" verdict="block-insufficient-coverage"/>
    </branch>
    <branch when="prereqs_met == true AND deleting_code == false AND bug_on_purpose == false AND branch_coverage_pct &gt;= 70 AND signoff_complete == false">
      <conclusion ref="r2-snapshot-reviewed" verdict="block-no-signoff"/>
    </branch>
    <branch when="prereqs_met == true AND deleting_code == false AND bug_on_purpose == false AND branch_coverage_pct &gt;= 70 AND signoff_complete == true">
      <conclusion ref="run-the-checklist" verdict="wrap-refactor"/>
    </branch>
  </decision-tree>
  <example trace="happy-path">prereqs_met=true → deleting_code=false → bug_on_purpose=false → branch_coverage=87 ≥ 70 → signoff complete → verdict=wrap-refactor (ref run-the-checklist).</example>
</text>
