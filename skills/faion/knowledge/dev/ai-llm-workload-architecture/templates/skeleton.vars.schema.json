{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/dev/ai-llm-workload-architecture/skeleton.vars.schema.json",
  "title": "skeleton template variables",
  "description": "Values for templates/skeleton.md.j2 and templates/skeleton.html.j2. `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "baseline_run_url": {
      "type": "string",
      "title": "Baseline run URL",
      "description": "Which CI run is the baseline every threshold is relative to?",
      "format": "uri"
    },
    "feature_name": {
      "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/feature_name"
    },
    "owner_handle": {
      "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/owner_handle"
    },
    "production_model_id": {
      "type": "string",
      "title": "Production model id",
      "description": "Which dated or versioned model id does production call (never an alias such as latest)?",
      "pattern": "(\\d{4}-\\d{2}-\\d{2}|\\d{8}|@\\d+|:\\d+|-v\\d+(\\.\\d+)*$)"
    }
  },
  "required": [
    "baseline_run_url",
    "feature_name",
    "owner_handle",
    "production_model_id"
  ],
  "x-faion-dictionary-refs": 2
}
