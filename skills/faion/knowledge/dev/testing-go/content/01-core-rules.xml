<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="Testing in Go Core Rules" version="1.1.0" est_tokens="700" depth="essential">
  <summary>8 testable rules every application of testing-go enforces. Each rule has a statement, a rationale anchored to industry practice, and a source citation.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use table-driven tests with named struct fields for any function with multiple input/output pairs.</statement>
      <rationale source="Go Wiki TableDrivenTests">Anonymous positional structs hide which field is which; named fields keep cases self-documenting as the table grows.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Wrap each row in `t.Run(tc.name, func(t *testing.T){...})` so a failing case names itself in output.</statement>
      <rationale source="pkg.go.dev/testing">Without `t.Run` the failure log says `TestFoo failed at row 5`; with `t.Run` it says `TestFoo/zero_price`.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Use `require.NoError` for must-have preconditions, `assert.Equal` for non-fatal field checks.</statement>
      <rationale source="stretchr/testify README">`require` stops the test before a nil deref; `assert` lets multiple field mismatches surface in one run.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Mock collaborators via small interfaces (≤3 methods) defined in the consumer package.</statement>
      <rationale source="Effective Go - interfaces">Large interfaces are hard to mock and signal the consumer is asking for too much. Defining at the consumer keeps the dependency direction correct.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use `httptest.NewRecorder` for handler unit tests, `httptest.NewServer` for client tests.</statement>
      <rationale source="pkg.go.dev/net/http/httptest">Recorder skips network; Server gives a real `http.Client` codepath. Mixing them masks bugs at the transport layer.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Prefer `b.Loop()` (Go 1.24+) over `for i := 0; i &lt; b.N; i++` in new benchmarks.</statement>
      <rationale source="Go 1.24 release notes">`b.Loop()` keeps the timer correct across setup/teardown and avoids `b.ResetTimer` boilerplate.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Separate integration tests with a `//go:build integration` build tag.</statement>
      <rationale source="Go Wiki BuildTags">Without a tag, slow DB/network tests run in the fast feedback loop and developers disable the lot.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>When the test target is Python, JavaScript/TypeScript or Rust code - decided by the source file's extension and the absence of a `go.mod` in the module root, never by the task wording - the agent MUST NOT emit a `*_test.go` file, a `t.Run` table, an `httptest` harness or a `b.Loop()` benchmark for it; it MUST route Python to `testing-pytest` and JavaScript/TypeScript to `testing-javascript`, and for Rust MUST report that no Related methodology exists rather than adapting these rules. The `test_files` field of the 02-output-contract record MUST stay empty for such a target.</statement>
      <rationale source="Go Modules Reference - go.mod; Go Wiki TableDrivenTests">Every rule here is a `testing`-package idiom: `t.Run`, `require`, `httptest`, `//go:build` tags have no equivalent semantics in pytest or Jest, so a transplanted pattern reads as Go-shaped noise in the other language and gets deleted on the next review. Deciding on `go.mod` and extension makes the skip deterministic instead of a judgement about what the task 'is about'.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
    <link path="content/06-decision-tree.xml" />
  </see-also>
</text>