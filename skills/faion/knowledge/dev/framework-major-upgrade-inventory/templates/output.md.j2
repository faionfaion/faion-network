<!-- purpose: inventory report skeleton for one major step — same sections and field names as content/02-output-contract.xml -->
<!-- consumes: deployed framework versions per environment, vendor release notes and upgrade guide, CI deprecation run, dependency manifest, package release notes (AGENTS.md Prerequisites) -->
<!-- produces: Framework Major Upgrade Inventory report validating against scripts/validate-framework-major-upgrade-inventory.py -->
<!-- depends-on: content/01-core-rules.xml, content/02-output-contract.xml -->
<!-- token-budget-impact: ~800 tokens when filled -->

# Framework Major Upgrade Inventory — {{ framework_name }} {{ current_version }} to {{ target_version }} ({{ repo_name }})

## Framework and step

- `current_version` / `target_version`: {{ current_version }} / {{ target_version }} — `major_gap`: 1 (one inventory per major step; `step_index` / `steps_total` for multi-major paths)
- `upgrade_guide_url`:

## Deprecation run (current version, warnings as errors)

- `command` (e.g. `python -W error::DeprecationWarning -m pytest`, `node --throw-deprecation`, `config.active_support.deprecation = :raise`):
- `ran_on_version`: — `log_url`: — `distinct_warnings`: — every warning has an entry below: yes

## Release notes covered (every minor between current and target)

| Version | URL |
|---------|-----|

## Breaking-change map

| id | Title | Vendor citation (URL, version) | Call sites (count, search kind, pattern, link) | Status (affected / not_affected) | Classification (codemod: tool, command, dry-run diff lines / manual) | Risk: runs in, blast radius, detectability, characterisation test |
|----|-------|-------------------------------|------------------------------------------------|----------------------------------|------------------------------------------------------------------|------------------------------------------------------------------|

## Unverified (suspected, no vendor citation)

| Suspected change | Search tried |
|------------------|--------------|

## Dependent packages

| Package | Current | Earliest compatible | Released? | Release notes URL | Blocker (plan: fork / replace / remove / wait) |
|---------|---------|---------------------|-----------|-------------------|-------------------------------------------------|

## Runtime prerequisites

| Kind | Target minimum (source URL) | Environment: current, meets minimum | Blocker (upgrade plan) |
|------|-----------------------------|-------------------------------------|------------------------|

## Risk matrix (blockers first, then by blast radius and detectability)

1.

## Review

- `status`: draft | ready_for_review | approved (reviewer, approved_on)
- Upgrade branch opened by agent: no — the named reviewer opens and merges

## Validation

Run `python scripts/validate-framework-major-upgrade-inventory.py --file <inventory.json>`. Exit 0 = valid, exit 1 = violations on stderr.
