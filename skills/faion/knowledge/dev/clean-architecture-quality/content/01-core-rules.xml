<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="clean-architecture Core Rules" version="1.1.1" est_tokens="1050" depth="essential">
  <summary>7 testable rules grounding the clean-architecture methodology. Each rule pairs a binary check with a sourced rationale. Dependency rule, four-layer definitions, forbidden import directions, and project folder layout.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Source code dependencies must point only inward: Frameworks → Interface Adapters → Use Cases → Entities. Nothing in an inner circle may know anything about an outer circle.</statement>
      <rationale source="v1-source">Inward-only dependencies mean business rules (Entities, Use Cases) are stable and framework-independent. You can swap the database or web framework without touching business logic.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Enforce the dependency rule with architecture tests (import-linter, ArchUnit). The rule only holds if it is machine-checked — human review alone is insufficient.</statement>
      <rationale source="v1-source">Developers and LLMs routinely import ORM models into entities or inject HTTP clients into use cases. Without automated enforcement these violations accumulate silently.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Entities (domain/): Enterprise business rules. No framework imports. Python dataclasses or plain classes. Framework-independent, database-independent.</statement>
      <rationale source="v1-source">If an entity imports SQLAlchemy, the business rules cannot be tested without a database, and migrating the ORM requires touching domain code.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use Cases (application/): Application-specific business rules. Orchestrate entities via repository interfaces. Take Input DTOs, return Output DTOs. No HTTP, no ORM.</statement>
      <rationale source="v1-source">Use cases define what the application does, not how it does it. Keeping HTTP and ORM out keeps them testable and delivery-mechanism agnostic.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Interface Adapters (presentation/): Controllers, presenters, gateways. Convert between external formats (HTTP request/response) and use case Input/Output DTOs. No business logic here.</statement>
      <rationale source="v1-source">Business logic in controllers cannot be reused across delivery mechanisms (REST API, CLI, event handler). Controllers should be thin glue.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Frameworks and Drivers (infrastructure/): ORM implementations, external APIs, message brokers. Implement the interfaces defined in inner layers. Never imported from inner layers.</statement>
      <rationale source="v1-source">Outer layers know inner layer interfaces; inner layers never import outer layers. This is the inversion of control that makes the dependency rule work.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Skip when the input does not satisfy the Applies-If preconditions or another methodology owns this output shape.</statement>
      <rationale source="Skip-rule guard">Prevents duplicate work and wrong-tool application.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/06-decision-tree.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="templates/import-linter.ini">import-linter contracts enforcing the dependency rule</link>
    <link path="templates/repository-interface.py">Abstract repository interface template</link>
    <link path="templates/use-case.py">Use case skeleton with Input/Output and dependency injection</link>
  </see-also>
</text>
