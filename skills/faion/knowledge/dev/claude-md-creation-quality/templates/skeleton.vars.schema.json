{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/dev/claude-md-creation-quality/skeleton.vars.schema.json",
  "title": "skeleton template variables",
  "description": "Values for templates/skeleton.md.j2 and templates/skeleton.html.j2. `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "layout": {
      "type": "string",
      "title": "Layout",
      "description": "Does the repository hold one deployable package or several with their own manifests?",
      "enum": ["single-package", "multi-package"]
    },
    "line_budget": {
      "type": "integer",
      "title": "Line budget",
      "description": "What numeric line budget does the first comment of the root CLAUDE.md declare (100 is the workable default)?",
      "minimum": 10,
      "maximum": 300
    },
    "line_count": {
      "type": "integer",
      "title": "Line count",
      "description": "What does wc -l report for the committed root CLAUDE.md?",
      "minimum": 1
    },
    "repo_name": {
      "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/repo_name"
    },
    "reviewer_handle": {
      "type": "string",
      "title": "Reviewer handle",
      "description": "Which role:handle is the named reviewer who approved the CLAUDE.md PR?",
      "pattern": "^[a-z-]+:[a-z0-9._-]+$",
      "x-faion-sensitive": true,
      "x-faion-placeholder": "<REVIEWER_HANDLE>"
    }
  },
  "required": [
    "layout",
    "line_budget",
    "line_count",
    "repo_name",
    "reviewer_handle"
  ],
  "x-faion-dictionary-refs": 1
}
