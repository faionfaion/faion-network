# purpose: Frontmatter schema for the claude-md-creation-quality record.
# consumes: Header block of templates/skeleton.md.
# produces: Validator-friendly frontmatter for the record file.
# depends-on: scripts/validate-claude-md-creation-quality.py.
# token-budget-impact: zero — pure schema.

version: "1.0.0"
repo: "acme/platform"
layout: "multi-package"
line_budget: 100
line_count: 84
reviewer: "tl:mreyes"
