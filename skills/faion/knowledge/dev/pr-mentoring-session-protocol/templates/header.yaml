# purpose: Frontmatter schema for the pr-mentoring-session-protocol record.
# consumes: Header block of templates/skeleton.md.
# produces: Validator-friendly frontmatter for the record file.
# depends-on: scripts/validate-pr-mentoring-session-protocol.py.
# token-budget-impact: zero — pure schema.

version: "1.0.0"
pr: "https://<host>/<org>/<repo>/pull/<n>"
junior: "role:<handle>"
senior: "role:<handle>"
date: "2026-09-09"
