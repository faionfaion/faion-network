Read reports/semgrep.json and reports/trufflehog.json from this PR.

For each finding:
1. Decide reachability:
   - Function called from a user-facing path → critical priority
   - Test code only → suppress with rationale
   - Dead code or unreachable import → suppress with rationale

2. If reachable + High/Critical severity:
   - Open a fix proposal with a regression test that fails before the fix and passes after
   - Cite the specific OWASP category (e.g. A01 Broken Access Control)

3. If suppressed:
   - Add to .semgrepignore with a one-line reason and a review-by date (3 months from today)

Output: PR description draft + unified diff(s).
Do NOT push or merge. Do NOT auto-close findings without a rationale.
