<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Evolutionary Architecture Fitness Functions — Worked Example" version="2.0.0" est_tokens="1450" depth="recommended">
  <summary>One complete fitness-function suite spec for a billing service (structural, complexity and performance functions) with a note on each non-obvious value, and one rejected spec showing what the validator and the reviewer see.</summary>

  <section title="Scenario">
    <p>billing-service, a Kotlin/Spring service with an `api` / `domain` / `persistence` layering that has eroded twice. The architect (arch:alice) wants three guards in CI: layer direction, complexity in `billing.core`, and p95 latency of checkout. Complexity already has 37 violators, so it cannot start as an absolute limit.</p>
  </section>

  <section title="Resulting spec">
    <example type="good" title="ff-billing-service">
      <code lang="json"><![CDATA[
{
  "suite_id": "ff-billing-service",
  "system": "billing-service",
  "owner": "arch:alice",
  "dependency_graph": {
    "layers": ["api", "domain", "persistence"],
    "allowed_edges": [{"from": "api", "to": "domain"}, {"from": "domain", "to": "persistence"}]
  },
  "results_store": {
    "location": "prometheus://ci-metrics/fitness_function_value",
    "keyed_by": ["function_id", "commit"],
    "retention_days": 180
  },
  "review": {"cadence": "quarterly", "last_reviewed_at": "2026-09-01", "next_review_at": "2026-12-01"},
  "functions": [
    {
      "id": "layer-direction",
      "characteristic": "dependency direction between api, domain and persistence",
      "metric": "count of imports violating the allowed edges or forming a cycle",
      "unit": "violations",
      "kind": "structural",
      "mode": "automated",
      "tool": "ArchUnit",
      "command": "./gradlew test --tests com.acme.billing.arch.LayerRulesTest",
      "threshold": {"operator": "==", "value": 0},
      "baseline": {"value": 0, "measured_at": "2026-09-01"},
      "threshold_mode": "absolute",
      "consequence": "blocking",
      "trigger": "triggered",
      "scope": "atomic",
      "owner": "arch:alice",
      "consecutive_red_reviews": 0
    },
    {
      "id": "core-cyclomatic-complexity",
      "characteristic": "cyclomatic complexity of billing.core functions",
      "metric": "count of functions with cyclomatic complexity above 10",
      "unit": "functions",
      "kind": "complexity",
      "mode": "automated",
      "tool": "lizard",
      "command": "lizard -C 10 -w src/billing/core",
      "threshold": {"operator": "==", "value": 0},
      "baseline": {"value": 37, "measured_at": "2026-09-01"},
      "threshold_mode": "ratchet",
      "absolute_from": "2027-01-01",
      "consequence": "blocking",
      "trigger": "triggered",
      "scope": "atomic",
      "owner": "swe:bob",
      "consecutive_red_reviews": 0
    },
    {
      "id": "checkout-p95-latency",
      "characteristic": "p95 latency of POST /checkout at 200 rps",
      "metric": "http_req_duration p(95)",
      "unit": "ms",
      "kind": "performance",
      "mode": "automated",
      "tool": "k6",
      "command": "k6 run --vus 50 --duration 10m perf/checkout.js",
      "threshold": {"operator": "<", "value": 400},
      "baseline": {"value": 310, "measured_at": "2026-09-01"},
      "threshold_mode": "absolute",
      "consequence": "informational",
      "trigger": "continual",
      "scope": "holistic",
      "owner": "sre:carol",
      "consecutive_red_reviews": 0
    }
  ]
}
]]></code>
      <note>`dependency_graph.allowed_edges` is in the spec, not only in `LayerRulesTest`, so a reviewer can see the intended direction without reading Kotlin (r-ff-structural-dependency-function). `core-cyclomatic-complexity` has baseline 37 against threshold 0, so it ships as `ratchet` with `absolute_from: 2027-01-01`; an absolute limit would be red on the first run (r-ff-threshold-with-baseline-ratchet). The k6 run takes ten minutes, so it is `holistic` / `continual` / `informational` and lives in the nightly job, not the pull-request path (r-ff-blocking-vs-informational). `results_store.keyed_by` is function id plus commit and `retention_days: 180` covers two quarterly reviews (r-ff-results-trended). No function is `manual`, so the one-fifth cap is trivially met.</note>
    </example>
  </section>

  <section title="Rejected spec">
    <example type="bad" title="a manual quality score in the pull-request path">
      <code lang="json"><![CDATA[
{
  "id": "maintainability",
  "characteristic": "maintainability stays high",
  "metric": "quality score",
  "unit": "points",
  "kind": "complexity",
  "mode": "manual",
  "tool": "architecture sync",
  "threshold": {"operator": ">", "value": 8},
  "baseline": {"value": 7, "measured_at": "2026-09-01"},
  "threshold_mode": "absolute",
  "consequence": "blocking",
  "trigger": "triggered",
  "scope": "holistic",
  "owner": "arch:alice",
  "consecutive_red_reviews": 3
}
]]></code>
      <note>What the validator prints: `characteristic` matches the -ility pattern (r-ff-one-attribute-one-metric); `mode: manual` without `review_cadence` (r-ff-executable-in-ci); one manual entry in a one-entry suite is 100 percent, over the one-fifth cap; `scope: holistic` with `trigger: triggered` (r-ff-blocking-vs-informational); `consecutive_red_reviews: 3` with no `action` (r-ff-owner-and-review-cadence); and the suite has no `kind: structural` entry (r-ff-structural-dependency-function). What the reviewer sees without the validator: baseline 7 against an absolute threshold of 8 means the check is red today and will be marked allow-failure by Friday (fm-threshold-fails-today).</note>
    </example>
  </section>
</text>
