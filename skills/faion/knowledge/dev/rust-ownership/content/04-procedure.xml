<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Rust Ownership Procedure" version="1.2.0" est_tokens="1000" depth="essential">
  <summary>5-step audit applied per function before writing the body, followed by three crate-level steps that keep the result from decaying.</summary>

  <section title="Procedure">

    <step n="1" name="list-callers">
      <input>function path</input>
      <action>Run `grep -rn 'fn_name(' src/` or `cargo check` to enumerate call sites. Note for each whether caller has owned or borrowed value.</action>
      <output>call-site list</output>
    </step>

    <step n="2" name="answer-3-questions">
      <input>function purpose + call sites</input>
      <action>Answer: (1) keeps_value — does the fn store the input, return it, or drop it? (2) modifies_value — does it write through the input? (3) shares_across_threads — does any path spawn a thread / async task with the input?</action>
      <output>{keeps_value, modifies_value, shares_across_threads}</output>
    </step>

    <step n="3" name="pick-signature">
      <input>answers</input>
      <action>Apply decision tree in 06-decision-tree.xml. Write the chosen signature. Add a 3-line audit comment above the fn using `templates/ownership-audit-comment.tmpl.rs`.</action>
      <output>signature + audit comment</output>
    </step>

    <step n="4" name="cargo-check">
      <input>signature + draft body</input>
      <action>Run `cargo check`. If borrow checker complains: re-audit; do NOT add `.clone()` or `Rc&lt;RefCell&gt;` reflexively. Common fix: split call site so two `&amp;mut` references do not overlap.</action>
      <output>compiling code</output>
      <decision-gate>If body genuinely cannot compile without `.clone()` AND the value is small (Copy or small struct): accept the clone with a `// clone justified: small + cold path` comment.</decision-gate>
    </step>

    <step n="5" name="record">
      <input>final signature + answers</input>
      <action>Emit decision-record JSON per 02-output-contract.xml schema. Append to crate-level `ownership-decisions.json`.</action>
      <output>decision-record entry</output>
    </step>

    <step n="6" name="run-clone-audit">
      <input>Crate source</input>
      <action>Run `templates/audit-clones.sh` over the whole crate and categorise every surviving `.clone()` as required, removable, or a signature bug. Rewrite the removable ones to `&amp;T` / `&amp;mut T`; annotate the required ones with the reason.</action>
      <output>Clone audit report + clone-count baseline</output>
    </step>

    <step n="7" name="replace-rc-refcell-across-threads">
      <input>Every `spawn` / `tokio::spawn` / rayon closure site</input>
      <action>Grep the values captured at each spawn boundary. Any `Rc&lt;T&gt;` or `Rc&lt;RefCell&lt;T&gt;&gt;` becomes `Arc&lt;Mutex&lt;T&gt;&gt;` or `Arc&lt;RwLock&lt;T&gt;&gt;` — pick RwLock only when reads genuinely dominate.</action>
      <output>Thread-safe shared state</output>
      <decision-gate>Never reach for `unsafe impl Send` to make the bound go away. Rule arc-mutex-across-threads.</decision-gate>
    </step>

    <step n="8" name="install-ci-gate">
      <input>CI runner + the clone baseline</input>
      <action>Run `audit-clones.sh` on every PR and fail or warn when the clone count regresses without a stated reason. Emit the crate-level audit block alongside the per-function decision records.</action>
      <output>Live gate + validated artefact</output>
    </step>

  </section>
</text>
