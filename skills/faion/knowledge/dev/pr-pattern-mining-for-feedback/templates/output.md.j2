<!-- purpose: PR pattern-mining feedback report skeleton — same sections and field names as content/02-output-contract.xml -->
<!-- consumes: merged PRs and review comments fetched by a stated query, the language's formatter and linter catalogue, CI config, PR template, previous report (AGENTS.md Prerequisites) -->
<!-- produces: PR Pattern Mining For Feedback report validating against scripts/validate-pr-pattern-mining-for-feedback.py -->
<!-- depends-on: content/01-core-rules.xml, content/02-output-contract.xml -->
<!-- token-budget-impact: ~800 tokens when filled -->

# PR Pattern Mining — {{ team_name }}, {{ period_label }}

## Scope

- `kind`: team (no per-author counts) | mentee (handle, mentee is the primary reader, feeds performance evaluation: yes / no)

## Corpus

- Repositories: {{ repo_name }}, — window: start / end — merged PRs: — review comments: — `sufficient_sample` (>= 20 PRs and >= 100 comments; otherwise observations only, no rules):
- `query` (exact, re-runnable):
- Previous report URL (same scope and query), if any:

## Automation gap (formatter / linter-coverable comments, excluded from every count)

- Comments excluded: — tool: — config change (rule ids / config keys):

## Top patterns (at most 5, ranked by count x cost; `ranking_basis`: review_round_trips | label_severity)

| id | Behaviour (code does X; it should do Y) | Before | After | Distinct PRs (>= 3) | Distinct reviewers (>= 2, or 1 across >= 5 PRs) | Comment URLs (>= 3) | Count | Cost | Score | Baseline | Re-measure date | Delta vs previous |
|----|-----------------------------------------|--------|-------|---------------------|--------------------------------------------------|---------------------|-------|------|-------|----------|-----------------|-------------------|

## Appendix patterns (same shape, below the top five)

## Single occurrence (below the recurrence threshold; no rule)

| Behaviour | Comment URLs |
|-----------|--------------|

## Suggested rules (one enforcement channel each, in preference order)

| Pattern | Rule | Channel (linter_or_formatter_rule: id / ci_check_or_test: id / pr_template_checkbox / written_guideline) | Why not the earlier channels | Status (new / active / ineffective / withdrawn) | Cycles without drop |
|---------|------|------------------------------------------------------------------------------------------------------------|------------------------------|-------------------------------------------------|---------------------|

## Notes (observations with no channel yet)

## Review

- `status`: draft | ready_for_owner_check | checked_and_shared — owner: — checked cited URLs and scope on: — shared by agent: no

## Validation

Run `python scripts/validate-pr-pattern-mining-for-feedback.py --file <report.json>`. Exit 0 = valid, exit 1 = violations on stderr.
