<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="ruby-rails">
  <metadata>
    <tier>pro</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>Rails backend patterns: Service Objects with transactions, RSpec specs, Sidekiq jobs with retry policies, and error handling.</summary>
    <category>backend</category>
    <difficulty>intermediate</difficulty>
    <created>2026-04-28</created>
    <tags>
      <tag>ruby</tag>
      <tag>rails</tag>
      <tag>rspec</tag>
      <tag>sidekiq</tag>
      <tag>backend</tag>
    </tags>
    <languages>
      <language>ruby</language>
    </languages>
    <frameworks>
      <framework>rails</framework>
    </frameworks>
  </metadata>

  <content>
    <title>Ruby on Rails Backend Development</title>

    <summary>Rails backend patterns for production-grade applications. Service Objects encapsulate business logic (user creation, order processing) with ActiveRecord::Base.transaction for data consistency and ServiceResult for success/failure semantics. RSpec model specs validate constraints, association specs check belongs_to/has_many, scope specs isolate filtering. Service specs exercise the happy path and error cases. Sidekiq background jobs with retry policies (exponential backoff) for async work (email, exports, notifications). Controllers stay thin, delegating to services. Pagination via kaminari.</summary>

    <why>
      <p>Service Objects isolate business logic from HTTP concerns. A Users::CreateService encapsulates "how to create a user" — validations, transactions, audit logging, async notification — separate from "how to respond to POST /users". This separation makes the logic testable without mocking the controller, reusable across API/web/CLI, and maintainable as features grow.</p>
      <p>ServiceResult (success/failure) provides explicit contract: the caller knows whether the action succeeded and what data/errors resulted. ActiveRecord::Base.transaction ensures data consistency — if any step fails, all changes roll back. Sidekiq jobs with retry policies decouple critical async work from the request lifecycle.</p>
      <p>RSpec specs at the model level test validations and scopes without HTTP overhead. Service specs test the business rule in isolation. Request specs (formerly integration tests) test the full controller-to-model flow, hitting the router and auth layers.</p>
    </why>

    <when-to-use>
      <list>
        <item>Any Rails/Ruby backend shipping to production with a database.</item>
        <item>Building multi-user SaaS with proper data isolation, audits, and async notifications.</item>
        <item>Complex domain logic (order processing, payment reconciliation, report generation).</item>
        <item>API backends with strict JSON contracts, validation errors, and fault handling.</item>
        <item>Background job pipelines for email, data export, or data transformation.</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Simple static sites or content management — Rails is overkill.</item>
        <item>Real-time systems requiring sub-100ms latency — consider Go or Rust.</item>
        <item>Teams unfamiliar with Rails conventions — the convention overhead requires buy-in.</item>
      </list>
    </when-not-to-use>

    <section name="service-objects">
      <p>Service Objects encapsulate business logic. Initialize with params and dependencies (current_user, logger). Call the service via .call or similar. Return a ServiceResult with success/failure semantics and data/errors. Use ActiveRecord::Base.transaction to ensure atomicity.</p>

      <rule testable="true">
        <statement>Every endpoint that touches the database MUST delegate to a Service Object (Users::CreateService, Orders::ProcessService). The service MUST wrap database operations in ActiveRecord::Base.transaction and return a ServiceResult (success/failure with data or errors).</statement>
        <rationale>Services isolate business logic from HTTP, making it testable without a request and reusable across API/web/CLI. Transactions ensure data consistency. ServiceResult provides an explicit contract so callers don't have to catch exceptions.</rationale>
      </rule>

      <rule testable="true">
        <statement>A Service MUST raise ActiveRecord::RecordInvalid on validation failures (save!) and catch it to return a ServiceResult.failure. It MUST NOT swallow exceptions; catch specific errors (RecordInvalid, PaymentGatewayError) and re-raise unexpected errors so they bubble to error tracking (Sentry, etc.).</statement>
        <rationale>save! raises when validations fail. Catching RecordInvalid and converting to ServiceResult.failure is idiomatic Rails. Re-raising unexpected errors ensures they bubble to monitoring so on-call engineers know a service is broken.</rationale>
      </rule>

      <example type="good" title="Service Object with transaction and result">
        <code lang="ruby"><![CDATA[
# app/services/users/create_service.rb
module Users
  class CreateService
    def initialize(params:, current_user: nil)
      @params = params
      @current_user = current_user
    end

    def call
      user = User.new(user_params)

      ActiveRecord::Base.transaction do
        user.save!
        send_welcome_email(user)
        create_audit_log(user)
      end

      ServiceResult.success(user)
    rescue ActiveRecord::RecordInvalid =&gt; e
      ServiceResult.failure(e.record.errors.full_messages)
    end

    private

    def user_params
      @params.slice(:name, :email, :password)
    end

    def send_welcome_email(user)
      UserMailer.welcome(user).deliver_later
    end

    def create_audit_log(user)
      AuditLog.create!(
        action: 'user.created',
        resource: user,
        actor: @current_user
      )
    end
  end
end

# app/services/service_result.rb
class ServiceResult
  attr_reader :data, :errors

  def initialize(success:, data: nil, errors: [])
    @success = success
    @data = data
    @errors = errors
  end

  def success?
    @success
  end

  def failure?
    !@success
  end

  def self.success(data = nil)
    new(success: true, data: data)
  end

  def self.failure(errors)
    new(success: false, errors: Array(errors))
  end
end
        ]]></code>
        <note>Service encapsulates user creation, email dispatch, and audit logging within a transaction.</note>
      </example>

      <example type="good" title="Controller delegating to service">
        <code lang="ruby"><![CDATA[
# app/controllers/api/v1/users_controller.rb
module Api
  module V1
    class UsersController &lt; ApplicationController
      def create
        result = Users::CreateService.new(
          params: user_params,
          current_user: current_user
        ).call

        if result.success?
          render json: UserSerializer.new(result.data), status: :created
        else
          render json: { errors: result.errors }, status: :unprocessable_entity
        end
      end

      private

      def user_params
        params.require(:user).permit(:name, :email, :password)
      end
    end
  end
end
        ]]></code>
        <note>Controller is thin: extract params, call service, render result.</note>
      </example>
    </section>

    <section name="rspec-testing">
      <p>Use RSpec for specs. Model specs validate presence/uniqueness/length and association structure. Service specs exercise the business logic (happy path, error cases). Request specs test the full endpoint (auth, routing, JSON shape). Factory Bot creates test fixtures.</p>

      <rule testable="true">
        <statement>Every model MUST have a model spec using shoulda-matchers (validate_presence_of, validate_uniqueness_of, have_one, have_many) to document and test constraints. Scope specs MUST isolate the WHERE clause: create(:user, active: true) and create(:user, active: false), then assert User.active returns only the active one.</statement>
        <rationale>Shoulda matchers read as documentation. Scope specs catch regressions when someone refactors the WHERE logic. Model specs are fast (no HTTP, no full Rails setup) and isolated.</rationale>
      </rule>

      <rule testable="true">
        <statement>Service specs MUST cover the happy path (valid params → success result, data persisted, side effects dispatched) and error cases (invalid params → failure result with errors, no data persisted).</statement>
        <rationale>Service specs verify the contract: given input, the service returns success/failure. Error cases are as important as happy paths — they document what the service does when things go wrong.</rationale>
      </rule>

      <example type="good" title="Model spec with validations and associations">
        <code lang="ruby"><![CDATA[
# spec/models/user_spec.rb
require 'rails_helper'

RSpec.describe User, type: :model do
  describe 'validations' do
    it { is_expected.to validate_presence_of(:email) }
    it { is_expected.to validate_presence_of(:name) }
    it { is_expected.to validate_uniqueness_of(:email).case_insensitive }
    it { is_expected.to validate_length_of(:name).is_at_least(2).is_at_most(100) }
  end

  describe 'associations' do
    it { is_expected.to have_one(:profile).dependent(:destroy) }
    it { is_expected.to have_many(:orders).dependent(:nullify) }
  end

  describe 'scopes' do
    describe '.active' do
      let!(:active_user) { create(:user, active: true) }
      let!(:inactive_user) { create(:user, active: false) }

      it 'returns only active users' do
        expect(User.active).to eq([active_user])
      end
    end
  end

  describe '#full_name' do
    let(:user) { build(:user, first_name: 'John', last_name: 'Doe') }

    it 'returns combined first and last name' do
      expect(user.full_name).to eq('John Doe')
    end
  end
end
        ]]></code>
        <note>Model spec documents validations, associations, and scopes.</note>
      </example>

      <example type="good" title="Service spec with happy path and error cases">
        <code lang="ruby"><![CDATA[
# spec/services/users/create_service_spec.rb
require 'rails_helper'

RSpec.describe Users::CreateService do
  describe '#call' do
    subject(:service) { described_class.new(params: params) }

    context 'with valid params' do
      let(:params) { { name: 'John', email: 'john@example.com', password: 'secret123' } }

      it 'creates a user' do
        expect { service.call }.to change(User, :count).by(1)
      end

      it 'returns success result' do
        result = service.call
        expect(result).to be_success
        expect(result.data).to be_a(User)
      end

      it 'sends welcome email' do
        expect { service.call }
          .to have_enqueued_mail(UserMailer, :welcome)
      end
    end

    context 'with invalid params' do
      let(:params) { { name: '', email: 'invalid' } }

      it 'does not create a user' do
        expect { service.call }.not_to change(User, :count)
      end

      it 'returns failure result with errors' do
        result = service.call
        expect(result).to be_failure
        expect(result.errors).to include(/Email/)
      end
    end
  end
end
        ]]></code>
        <note>Service spec covers happy path and validation failure.</note>
      </example>
    </section>

    <section name="sidekiq-background-jobs">
      <p>Use Sidekiq for async work. Define a Job class with sidekiq_options (queue, retry count, dead letter queue). Use sidekiq_retry_in to define custom backoff (exponential, linear per exception type). Catch specific exceptions and re-raise, letting the job framework handle retry.</p>

      <rule testable="true">
        <statement>Every async operation (email, export, data sync) MUST be enqueued as a Sidekiq Job (include Sidekiq::Job, sidekiq_options queue: ..., retry: ...). Jobs MUST catch specific exceptions (PaymentGatewayError) with custom retry logic and re-raise unexpected errors so monitoring systems catch them.</statement>
        <rationale>Sidekiq decouples async work from the request lifecycle. If email dispatch fails, the job retries; the user's account creation is not rolled back. Retry policies ensure transient failures (network blips) are retried; permanent failures (invalid email) fail fast.</rationale>
      </rule>

      <rule testable="true">
        <statement>Job perform methods MUST handle missing records gracefully (ActiveRecord::RecordNotFound). Jobs MUST log via Sidekiq.logger, not puts, so logs appear in the job queue monitoring system.</statement>
        <rationale>A job may be enqueued for an object that is later deleted. Logging via Sidekiq.logger ensures the message appears in job monitoring dashboards (Sidekiq Pro, ContractuallyObliged, etc.).</rationale>
      </rule>

      <example type="good" title="Sidekiq job with retry policies">
        <code lang="ruby"><![CDATA[
# app/jobs/process_order_job.rb
class ProcessOrderJob
  include Sidekiq::Job

  sidekiq_options queue: :default, retry: 3, dead: true

  sidekiq_retry_in do |count, exception|
    case exception
    when PaymentGatewayError
      (count + 1) * 60 # Linear backoff for payment issues
    else
      (count ** 4) + 15 # Exponential backoff
    end
  end

  def perform(order_id)
    order = Order.find(order_id)

    return if order.processed?

    OrderProcessor.new(order).process!

    NotifyCustomerJob.perform_async(order_id)
  rescue ActiveRecord::RecordNotFound
    # Order deleted, nothing to do
    Sidekiq.logger.warn "Order #{order_id} not found"
  rescue PaymentGatewayError =&gt; e
    # Will retry with custom backoff
    raise
  rescue StandardError =&gt; e
    ErrorTracker.capture(e, order_id: order_id)
    raise
  end
end

# app/jobs/notify_customer_job.rb
class NotifyCustomerJob
  include Sidekiq::Job

  sidekiq_options queue: :notifications, retry: 5

  def perform(order_id)
    order = Order.find(order_id)
    OrderMailer.confirmation(order).deliver_now
  end
end
        ]]></code>
        <note>ProcessOrderJob uses custom sidekiq_retry_in logic for different exception types. NotifyCustomerJob is simpler.</note>
      </example>

      <example type="good" title="Batch processing in Sidekiq">
        <code lang="ruby"><![CDATA[
# app/jobs/batch_export_job.rb
class BatchExportJob
  include Sidekiq::Job

  sidekiq_options queue: :exports, retry: 1

  def perform(export_id)
    export = Export.find(export_id)
    export.update!(status: :processing)

    records = export.query_records

    CSV.open(export.file_path, 'wb') do |csv|
      csv &lt;&lt; export.headers
      records.find_each(batch_size: 1000) do |record|
        csv &lt;&lt; export.row_for(record)
      end
    end

    export.update!(status: :completed, completed_at: Time.current)
    ExportMailer.ready(export).deliver_later
  rescue StandardError =&gt; e
    export.update!(status: :failed, error_message: e.message)
    raise
  end
end
        ]]></code>
        <note>Batch export uses find_each(batch_size: 1000) to avoid loading all records into memory.</note>
      </example>
    </section>


    <section name="when-not-to-use-detailed">
      <p>Hard real-time / low-latency systems (sub-10ms p99). MRI Ruby + Rails middleware is not the right footprint; pick Go/Rust.</p>
      <p>CPU-bound workloads (ML inference, video transcoding). Use Rails as the API surface, push compute to a service in another language.</p>
      <p>Strict single-binary distribution. Rails wants Bundler + a Ruby runtime — not friendly to "drop one binary on the box."</p>
      <p>Polyglot orgs without Ruby expertise. Rails magic (autoloading, callbacks, method_missing, scopes) is opaque to non-Ruby agents and becomes a maintenance liability.</p>
      <p>Greenfield where the team explicitly committed to typed Python/Go/TS — Rails is not a "we'll add types later" stack (Sorbet/RBS exists but is opt-in, not idiomatic for most gems).</p>
    </section>

    <section name="where-it-fails-limitations">
      <p>Callback hell. before_save, after_create_commit, around_destroy chains create implicit ordering that breaks when reused via accepts_nested_attributes_for. The README's before_validation :normalize_email is fine; production accumulates 8 callbacks and diagnoses become forensic.</p>
      <p>Fat models, then fat services. Skinny-controller advice pushes logic to model, model bloats, "service objects" extract — but service-object conventions are not standardised. Teams diverge (Interactor, Trailblazer, dry-transaction, hand-rolled call like the README).</p>
      <p>N+1 queries. ActiveRecord lazy loading is a footgun; bullet gem catches local cases, prod still surprises. The README's with_associations chain is correct; agents skip it.</p>
      <p>Strong parameters drift. params.require(:user).permit(...) lists drift from the model schema as columns are added; tests don't catch silent ignored fields.</p>
      <p>Sidekiq retry semantics. At-least-once delivery; without idempotency, retries double-charge. README's return if order.processed? is the right pattern; agents forget it.</p>
      <p>Autoloading (Zeitwerk) errors. Constant-name → file-name mismatch produces NameErrors only at runtime. Test suites must boot the full app to surface them.</p>
      <p>Asset pipeline churn. Sprockets / Webpacker / esbuild / importmap / Propshaft — every Rails upgrade asks you to migrate. Agents copy snippets across pipelines.</p>
      <p>Scope leakage into tests. A scope on User bleeds into FactoryBot defaults; tests pass locally, fail under random ordering. Use default_scope only when you are sure.</p>
      <p>Database transactional state in tests. RSpec + transactional fixtures + Sidekiq-inline = surprising commit ordering for after_create_commit callbacks.</p>
    </section>

    <section name="cli-tools">
      <p>Essential CLI tools: rails, bundle, rake, rspec, rubocop, brakeman, bullet, bundler-audit, sidekiq, kamal, solargraph/ruby-lsp, dotenv-rails, factory_bot_rails, rspec-bisect, rails db:migrate:status.</p>
    </section>

    <section name="services">
      <p>External services: Sidekiq + Redis, Solid Queue/Cache, Heroku/Render/Fly.io, Kamal, Postgres, Skylight/Scout/Datadog, Honeybadger/Sentry/Rollbar, AppSignal, Faktory, Mailtrap/Postmark/SES, GoodJob, ImageMagick/libvips.</p>
    </section>

    <section name="best-practices">
      <p>Service objects: one shape. Query objects extract scopes. Cap callbacks: ≤3. Strong params + serializers. Idempotent jobs. Backoff strategy per error. Transactions: no HTTP. Strict loading in tests. Pin versions. Credentials: encrypted vs ENV. Migrations: forward-only.</p>
    </section>

    <section name="agent-gotchas">
      <p>Old Rails syntax. Wrong paths. Strong-params drift. Blind retries. update_all skipping. N+1 in serializers. Hallucinated traits. Migration mismatch. permit!. Human review for data migrations. Idempotency testing.</p>
    </section>

    <section name="agentic-workflow">
      <p>Five-stage pipeline: Design (inventory) → Code-gen (files) → Jobs (Sidekiq) → Tests (specs) → Review (lint). Pin Rails version. Subagents: faion-sdd-executor-agent, password-scrubber, rails-review, /faion.</p>
    </section>


    <section name="greenfield-use-cases">
      <p>Greenfield SaaS or CRUD-heavy domains where Rails-the-omakase (ActiveRecord, ActionController, ActionMailer, ActiveJob, Hotwire) maps cleanly to the product. Solo / small-team builds where convention-over-configuration is the productivity edge — you want rails generate scaffold shaped code, then refine. Modernising an existing Rails 5/6 app to Rails 7+ with Hotwire/Turbo, Solid Queue, Solid Cache, Kamal deploys. Backends with substantial async/background work where Sidekiq + Redis is operationally cheap. Teams already running RSpec + FactoryBot + Capybara who want service-object + query-object discipline layered on top of Rails defaults.</p>
    </section>

    <section name="activerecord-patterns">
      <p>Efficient database access with proper query optimization. Query Objects for chainable filters with proper scopes using lambdas. Eager loading via includes to prevent N+1. Callback structure and association semantics (has_one, has_many, through). Query Objects pull chained scopes out of the controller to improve testability and reusability. Scopes defined with lambdas (not block form) ensure they compose properly. Eager loading with includes(...) or eager_load(...) must be applied before iteration. Associations: has_one is singular and implies one-to-one, has_many is plural and implies collection loading, has_many :through connects two models via a join table, belongs_to is the inverse of has_one/has_many.</p>
    </section>

    <section name="query-objects-pattern">
      <p>Query Objects encapsulate SELECT logic. Initialize with a base relation (defaults to Model.all). Chain methods that return self for composability. Final .results returns the relation. Controllers call UsersQuery.new.active.with_role('admin').search(term).results. This pattern keeps controllers thin and SELECT logic testable in isolation. Pagination via kaminari: query.paginate(page: params[:page]).results.</p>
    </section>

    <section name="rspec-test-strategy">
      <p>RSpec for specs across three levels. Model specs validate presence/uniqueness/length and association structure using shoulda-matchers. Scope specs isolate the WHERE clause: create fixture with condition true, create fixture with condition false, assert scope returns only the true case. Service specs exercise business logic: happy path (valid params → success, side effects dispatched), error cases (invalid params → failure, no persistence). Request specs test full endpoint: auth, routing, JSON shape, status codes. Use Factory Bot for test data creation. Never use database_cleaner with transactional fixtures and Sidekiq-inline together — the commit ordering becomes surprising.</p>
    </section>


    <see-also>
      <link href="https://guides.rubyonrails.org/"/>
      <link href="https://sidekiq.org/"/>
      <link href="https://rspec.info/"/>
      <link href="https://github.com/thoughtbot/factory_bot"/>
    </see-also>
  </content>
</methodology>
