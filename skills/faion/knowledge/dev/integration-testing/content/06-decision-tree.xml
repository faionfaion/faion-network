<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="integration-testing Decision Tree" version="1.0.0" est_tokens="500">
  <decision-tree>
    <root-question>Does the service depend on a real external service (DB, queue, cache) that Testcontainers supports?</root-question>
    <branch when="external_dep_present == true">
      <question>Can CI run Docker (or has a CI-native services block for the dep)?</question>
      <branch when="ci_can_run_docker == true">
        <question>Does the framework support DI override (or middleware-based session swap)?</question>
        <branch when="di_override_possible == true">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="di_override_possible == false">
          <conclusion ref="r8"/>
        </branch>
      </branch>
      <branch when="ci_can_run_docker == false">
        <conclusion ref="skip-this-methodology"/>
      </branch>
    </branch>
    <branch when="external_dep_present == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="FastAPI + Postgres, GHA with Docker, get_db override -> run-the-checklist"/>
  </decision-tree>
</text>
