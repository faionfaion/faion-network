<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="A/B Testing Failure Modes" version="1.1.0" est_tokens="700" depth="essential">
  <summary>6 antipatterns commonly observed in shipped A/B runtimes and in the experiment designs behind them. Each pairs symptom + root-cause + concrete fix.</summary>

  <section title="Antipatterns">

    <antipattern id="random-choice-bucket" severity="high">
      <symptom>Same user sees variant A on web and variant B on mobile in the same session; conversion attribution scrambled.</symptom>
      <root-cause source="Microsoft ExP guidance + Internal — A/B audits 2024">Bucket function used random.choice / Math.random instead of a hash of user_id + experiment_id.</root-cause>
      <fix>Replace assignment with 64-bit hash of (user_id || experiment_id || salt) mod 100. Pin salt per experiment. See rule deterministic-hash.</fix>
    </antipattern>

    <antipattern id="no-srm-check" severity="high">
      <symptom>Experiment "wins" with p=0.02; observed split was 47/53 instead of 50/50; ship causes a regression.</symptom>
      <root-cause source="Kohavi et al. — Trustworthy Online Controlled Experiments">SRM not checked; bucket leak or filter on one arm silently biases the result.</root-cause>
      <fix>Compute chi-square p-value of observed split vs expected; if p &lt; 0.001 mark decision=invalid-srm and refuse to ship. See rule srm-gate.</fix>
    </antipattern>

    <antipattern id="peeking" severity="high">
      <symptom>Team kills an experiment at 30% of sample size after a "significant" result; the lift was noise.</symptom>
      <root-cause source="Optional stopping bias literature">No sequential-test correction; team treats fixed-horizon p-value as live-monitor.</root-cause>
      <fix>Lock decision until planned sample size reached, or use sequential-test (mSPRT) with adjusted alpha. See rule wilson-ci-not-percent.</fix>
    </antipattern>

    <antipattern id="post-assignment-changes" severity="medium">
      <symptom>Mid-experiment, targeting rule changes; old buckets now exposed to a different variant; data invalid.</symptom>
      <root-cause source="Internal — flag-change audit">Targeting changes during the experiment without re-assignment isolation.</root-cause>
      <fix>Freeze the assignment salt + targeting rule at experiment start; any change ends the experiment and starts a new one. See rule sticky-across-surfaces.</fix>
    </antipattern>

    <antipattern id="multi-variable-change" severity="high">
      <symptom>The variant changes layout, copy and pricing at once. It wins, and nobody can say which of the three did it — or whether one of them lost.</symptom>
      <root-cause source="Practitioner experience">Several teams shipped into the same variant because it was the one already running.</root-cause>
      <fix>Split into orthogonal experiments, or use a factorial design that can attribute the effect. See rule preregistered-design — the plan is what stops the variant accumulating passengers.</fix>
    </antipattern>

    <antipattern id="ignored-guardrail" severity="high">
      <symptom>The variant ships on a positive primary metric and quietly reduces revenue per session; the regression surfaces weeks later in a monthly review.</symptom>
      <root-cause source="Practitioner experience">Guardrails were tracked on the dashboard but never gated the decision.</root-cause>
      <fix>Make the guardrail check a hard gate in the analysis script: any guardrail regression blocks the ship recommendation mechanically, before a human weighs it. See rule guardrails-gate-the-ship.</fix>
    </antipattern>

  </section>
</text>
