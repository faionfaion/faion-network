<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="best-practices-2026 Core Rules" version="1.1.0" est_tokens="1200" depth="essential">
  <summary>Eight testable rules covering AI-assisted coding, TypeScript 5 strict, React 19/Next 15, Python 3.12/3.13, and AI-testing discipline as of mid-2026.</summary>

  <section title="AI-assisted coding">
    <rule id="r1-match-tool-to-task" testable="true" depth="essential">
      <statement>Match the AI coding tool to the task. Copilot — daily inline completion in the IDE. Cursor — exploratory multi-file edits with chat. Claude Code — documentation, large refactors, test-suite generation, agentic loops. Using one tool for all tasks degrades output.</statement>
      <rationale source="DevProductivity 2025 surveys; internal benchmarks">Each tool has a different context window, interaction model, and latency profile. The cost of mismatched tooling is wasted prompts and worse code.</rationale>
    </rule>
    <rule id="r2-four-part-prompt" testable="true" depth="essential">
      <statement>Structure every coding prompt with four parts: Context (stack, constraints, target file), Task (specific action), Requirements (must-haves, edge cases), Output (expected format, file locations). Unstructured prompts are forbidden in tracked sessions.</statement>
      <rationale source="Anthropic prompt engineering guide">Unstructured prompts produce generic code that ignores project conventions. The four-part structure anchors the model to specifics.</rationale>
    </rule>
    <rule id="r3-no-auto-accept-security" testable="true" depth="essential">
      <statement>Never auto-accept AI-generated code for auth, data access, payments, or business-critical logic. Manual line-by-line review required. Tools may auto-apply diffs only in `tests/`, `docs/`, and stylistic refactors.</statement>
      <rationale source="GitClear AI Code Quality Report 2025">AI can increase defect rates significantly without review on security-sensitive surfaces.</rationale>
    </rule>
  </section>

  <section title="TypeScript 5 strict">
    <rule id="r4-ts-strict-flags" testable="true" depth="essential">
      <statement>Enable `strict: true` PLUS `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`, and `moduleResolution: "bundler"` in every new TypeScript project.</statement>
      <rationale source="TypeScript handbook; @tsconfig/strictest">These flags together catch the most common runtime errors at compile time. The plain `strict` preset misses array-index-OOB and excess-property edge cases.</rationale>
      <reference path="templates/tsconfig-strict.json">Working tsconfig with the flags above.</reference>
    </rule>
  </section>

  <section title="React 19 / Next 15">
    <rule id="r5-react-19-patterns" testable="true" depth="essential">
      <statement>On React 19+/Next 15+: prefer Server Components for data-fetching surfaces; use `use(promise)` only inside Suspense boundaries; use `'use client'` directives sparingly (per file). Do not introduce `getServerSideProps`-era idioms in new code.</statement>
      <rationale source="React 19 docs; Next.js 15 release notes">RSC reduces client bundle size and shifts data-fetching closer to data sources. The deprecated SSR data hooks still work but are no longer the recommended path on App Router.</rationale>
    </rule>
  </section>

  <section title="Python 3.12 / 3.13">
    <rule id="r6-python-baseline" testable="true" depth="essential">
      <statement>Pin Python floor at 3.12 (3.13 next-version target). Use PEP 695 type parameter syntax for new generics. Use `tomllib` stdlib for TOML reads. Use `pathlib.Path` over `os.path`. `pyproject.toml` is the single config file — no `setup.cfg`, no `.flake8`.</statement>
      <rationale source="https://peps.python.org/pep-0695/; PEP 680">3.12+ is the supported floor across major Python ecosystems (Django 5.x, FastAPI). 3.13 brings the experimental free-threaded build but is not yet default-on.</rationale>
    </rule>
  </section>

  <section title="AI testing">
    <rule id="r7-ai-testing-discipline" testable="true" depth="essential">
      <statement>AI-generated tests are drafts, not oracles. Every AI-drafted test MUST be reviewed against the production code it claims to verify. Property-based testing (Hypothesis, fast-check) and mutation testing (mutmut, Stryker) complement but do not replace AI-drafted unit tests.</statement>
      <rationale source="2025 study: AI-generated tests passed at compile-time but missed mutation 38% of the time">AI tests routinely encode the implementation, not the contract. Mutation testing is the cheapest reality check.</rationale>
    </rule>
  </section>

  <section title="Snapshot discipline">
    <rule id="r8-snapshot-and-drift" testable="true" depth="essential">
      <statement>Extract the relevant rules into the project's `constitution.md`; cite `best-practices-2026` once with the snapshot version. Re-run the drift scanner quarterly. Rules that no longer match current-stable practice are deprecated, not edited in place.</statement>
      <rationale source="ADR practice; documentation-as-code">This file is a snapshot and will age. The constitution is the versioned, project-specific contract agents cite without risking stale references.</rationale>
      <reference path="templates/bp2026-drift.sh">Drift scanner.</reference>
    </rule>
  </section>
</text>
