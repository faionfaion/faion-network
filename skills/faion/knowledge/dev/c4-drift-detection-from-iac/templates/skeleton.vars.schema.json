{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/dev/c4-drift-detection-from-iac/skeleton.vars.schema.json",
  "title": "skeleton template variables",
  "description": "Values for templates/skeleton.md.j2 and templates/skeleton.html.j2. `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "comparison_level": {
      "type": "string",
      "title": "Comparison level",
      "description": "Which C4 level does the report compare: container (default) or system-context?",
      "enum": ["container", "system-context"]
    },
    "model_commit": {
      "type": "string",
      "title": "Model commit",
      "description": "At which commit was the machine-readable C4 model read?",
      "pattern": "^[0-9a-f]{7,40}$"
    },
    "run_url": {
      "type": "string",
      "title": "Run URL",
      "description": "Where is the comparison run that produced this report?",
      "format": "uri"
    },
    "state_identity": {
      "type": "string",
      "title": "State identity",
      "description": "Which applied state was read: Terraform serial and lineage, Pulumi export version, or CloudFormation stack id and last-updated time?"
    },
    "system_name": {
      "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/system_name"
    }
  },
  "required": [
    "comparison_level",
    "model_commit",
    "run_url",
    "state_identity",
    "system_name"
  ],
  "x-faion-dictionary-refs": 1
}
