# purpose: Threat Model as Code config skeleton
# consumes: Prerequisites bundle (see AGENTS.md)
# produces: artefact conforming to content/02-output-contract.xml (config)
# depends-on: content/01-core-rules.xml + content/02-output-contract.xml
# token-budget-impact: ~200-1000 tokens when loaded as context

artefact_id: threat-model-as-code-2026Q2-001
owner: <named-human-or-role>
version: 1.0.0
last_reviewed: 2026-05-23

config:
  # methodology-specific keys here
  <key>: <value>

rationale: |-
  ≥2 sentences; cite at least one input by name.

inputs_used:
  - path: docs/task-brief.md
    sha: <git-sha-or-version>
