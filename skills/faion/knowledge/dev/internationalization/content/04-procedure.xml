<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Internationalization Procedure" version="1.1.0" est_tokens="800" depth="essential">
  <summary>7-step procedure to apply internationalization end-to-end, from catalogue scaffold to translator handoff.</summary>
  <section title="Procedure">
    <step n="1" name="initialise-catalogues">
      <input>Locale list + i18n library</input>
      <action>Create locales/&lt;code&gt;/messages.json (or .po). Pin library version. Wire to build pipeline.</action>
      <output>Catalogue scaffold</output>
    </step>
    <step n="2" name="externalise-strings">
      <input>Existing codebase + lint</input>
      <action>Replace hardcoded strings with t(key) calls. Add lint rule that blocks future regressions. Sweep with codemod where possible.</action>
      <output>All strings in catalogue</output>
      <decision-gate>If a string genuinely cannot be translated (debug-only log), exempt explicitly and document.</decision-gate>
    </step>
    <step n="3" name="author-icu-patterns">
      <input>Catalogue + plural-needing strings</input>
      <action>Convert plurals + gender-aware strings to ICU MessageFormat. Cover each locale's plural rules.</action>
      <output>ICU patterns in catalogue</output>
    </step>
    <step n="4" name="rtl-audit">
      <input>Codebase CSS</input>
      <action>Walk CSS; replace physical (left/right/margin-left) with logical (inline-start/end). Audit icons with direction.</action>
      <output>RTL-ready styles</output>
    </step>
    <step n="5" name="wire-pseudo-loc-ci">
      <input>Catalogue + pseudo-loc tool</input>
      <action>Add CI step that runs the pseudo-loc transformer + renders pages. Fail on truncation or missing-key events.</action>
      <output>Pseudo-loc gate</output>
    </step>
    <step n="6" name="gate-key-drift">
      <input>Locale catalogues + CI config</input>
      <action>Add the key-drift check to CI: compare every locale catalogue against the base and fail on any missing key. Declare the fallback chain (tag → language root → base) in the same commit.</action>
      <output>Key-drift gate + documented fallback chain</output>
      <decision-gate>If a key is deliberately untranslated for one locale, list it in an explicit allowlist — never leave it to fall back silently.</decision-gate>
    </step>
    <step n="7" name="translator-handoff">
      <input>Base catalogue + glossary + screenshots for context</input>
      <action>Export to the translation platform (Crowdin / Phrase / Lokalise) and commit the round-trip path so re-imports are a pipeline step, not a manual paste.</action>
      <output>Translation pipeline</output>
    </step>
  </section>
</text>
