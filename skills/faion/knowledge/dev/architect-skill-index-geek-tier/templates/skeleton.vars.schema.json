{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/dev/architect-skill-index-geek-tier/skeleton.vars.schema.json",
  "title": "skeleton template variables",
  "description": "Values for templates/skeleton.md.j2 and templates/skeleton.html.j2. `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "entry_count": {
      "type": "integer",
      "title": "Entry count",
      "description": "How many distinct target slugs does the index contain?",
      "minimum": 1
    },
    "index_title": {
      "type": "string",
      "title": "Index title",
      "description": "What is the index called in the geek tier?"
    },
    "next_due": {
      "type": "string",
      "title": "Next relink due",
      "description": "On what date (YYYY-MM-DD), one review cadence after the last run, is the next relink due?",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "relink_run_url": {
      "type": "string",
      "title": "Relink run URL",
      "description": "Where is the run that re-resolved every slug and re-read every est_tokens?",
      "format": "uri"
    }
  },
  "required": [
    "entry_count",
    "index_title",
    "next_due",
    "relink_run_url"
  ],
  "x-faion-dictionary-refs": 0
}
