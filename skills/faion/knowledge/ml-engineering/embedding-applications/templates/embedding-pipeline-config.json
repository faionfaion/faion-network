{
  "__faion_header__": {
    "purpose": "embedding-pipeline-config artefact",
    "consumes": "model name+version + domain bench + DB metric",
    "produces": "config validated against 02-output-contract.xml",
    "depends_on": "content/02-output-contract.xml schema",
    "token_budget_impact": "0 LLM tokens; declarative"
  },
  "model_name": "text-embedding-3-large",
  "model_version": "2026-04",
  "provider": "openai",
  "batch_size": 256,
  "normalize": true,
  "metric": "cosine",
  "domain_bench_path": "git://<repo>/eval/embed-bench.jsonl",
  "recall10_threshold": 0.7,
  "vector_metadata_fields": ["model_name", "model_version", "created_at"]
}
