<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Embedding Applications Decision Tree" version="1.1.0" est_tokens="500">
  <summary>Routes domain class + bench availability + corpus class to candidate model set.</summary>

  <decision-tree>
    <root-question id="q-labels-available">Are ≥50 labeled domain query/doc pairs available?</root-question>
    <branch when="labeled_pairs &lt; 50">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="labeled_pairs &gt;= 50">
      <question id="q-domain">Domain class?</question>
      <branch when="domain == code_or_multilingual">
        <conclusion ref="r5-provider-params"/>
      </branch>
      <branch when="domain == legal_biomedical">
        <conclusion ref="r1-mteb-plus-domain"/>
      </branch>
      <branch when="domain == general">
        <conclusion ref="run-the-checklist"/>
      </branch>
    </branch>
  </decision-tree>

  <example trace="support-bot-corpus-q2-2026">
    labeled_pairs=120 → domain=general → run-the-checklist (shortlist + bench top-3 general embedders).
  </example>
</text>
