# purpose: OTel collector pipeline (receive → process → export).
# consumes: see AGENTS.md ## Prerequisites
# produces: config
# depends-on: content/02-output-contract.xml schema for llm-observability-stack
# token-budget-impact: ≤500 tokens to fill

# Stub — see methodology AGENTS.md ## Templates table.
