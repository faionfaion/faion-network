<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Chunking Strategies — Decision Tree" version="1.1.0" est_tokens="400">
  <decision-tree>
    <root-question id="q-root">How many distinct document types in the corpus?</root-question>
    <branch when="doc_types == 1 and structure_present == true">
      <conclusion ref="r2-structure-aware-when-possible">Use a structure-aware chunker matched to that type.</conclusion>
    </branch>
    <branch when="doc_types == 1 and structure_present == false">
      <conclusion ref="r5-chunk-size-by-model">Use recursive-character chunker sized for the embedding model.</conclusion>
    </branch>
    <branch when="doc_types &gt; 1">
      <conclusion ref="r4-per-type-dispatcher">Build a dispatcher picking strategy per type.</conclusion>
    </branch>
    <example trace="doc_types == 1 and structure_present == true">
      <p>Use a structure-aware chunker matched to that type.</p>
    </example>
  </decision-tree>
</text>
