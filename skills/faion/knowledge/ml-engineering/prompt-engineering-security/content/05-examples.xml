<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Prompt Engineering — Security and Injection Defense — Worked Example" version="1.0.0" est_tokens="600" depth="medium">
  <summary>One end-to-end worked example showing the artefact shape this methodology produces.</summary>

  <section title="Worked example">
    <example type="good">
      <code lang="python"><![CDATA[
def create_safe_prompt(system: str, user_input: str) -> list:
    """Create prompt with clear boundaries."""
    sanitized = sanitize_user_input(user_input)

    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"""<user_input>
{sanitized}
</user_input>

Respond to the user input above."""}
    ]
]]></code>
      <note>System prompt is separate; user input is wrapped and post-fixed with a clear instruction anchor.</note>
    </example>
  </section>
</text>
