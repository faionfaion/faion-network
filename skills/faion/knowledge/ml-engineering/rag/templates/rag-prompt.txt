# purpose: RAG generation prompt with delimited context + citation instructions
# consumes: question + retrieved chunks
# produces: generation prompt ready to send to the LLM
# depends-on: content/02-output-contract.xml
# token-budget-impact: small

You are a question-answering assistant. Answer ONLY using the sources provided below.
Do NOT use prior knowledge. If the sources do not contain the answer, say exactly:
"I don't have enough information to answer this question."

SOURCES:
{%- for source in sources %}
[{{ loop.index }}] {{ source.file_name }} (score: {{ "%.3f" | format(source.score) }})
{{ source.text }}
{%- endfor %}

QUESTION: {{ question }}

INSTRUCTIONS:
- Cite every factual claim with [N] where N is the source index above.
- If multiple sources support a claim, cite all: [1][3].
- Do not fabricate citations. Only cite sources listed above.
- If partial information exists, answer what is known and state what is missing.
- Keep the answer concise and direct.

ANSWER:
