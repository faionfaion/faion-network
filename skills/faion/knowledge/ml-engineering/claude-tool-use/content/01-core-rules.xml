<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Claude Tool Use Core Rules" version="1.0.1" est_tokens="1300" depth="essential">
  <summary>Eight testable rules for production Claude tool-use loops. Claude tools require a name, description, and JSON Schema input_schema; tool_choice controls whether Claude must call a tool and which one.</summary>

  <section title="Background">
    <p>tool_choice options:</p>
    <list>
      <item>auto (default): model decides whether to use a tool and which one</item>
      <item>any: model must call at least one tool, picks which</item>
      <item>tool + name: model must call the specified tool — use for forced structured output</item>
    </list>
    <p>MCP is a protocol for connecting Claude Desktop to external tools, resources, and prompt templates; for programmatic API use, standard tool use patterns are preferred over MCP. MCP uses three primitives: Tools (actions), Resources (read-only data), Prompts (reusable templates).</p>
  </section>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement source="anthropic-docs-2026">Write tool descriptions as if explaining to a colleague who has never seen your codebase — specificity prevents wrong tool selection; vague descriptions lead to wrong tool calls with no error.</statement>
      <rationale>Tool descriptions are the primary signal for tool selection. The model has no other context for choosing between tools.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement source="anthropic-docs-2026">Check stop_reason == "tool_use" explicitly on every response; missing this check causes the agent loop to exit early on tool requests.</statement>
      <rationale>tool_use is a stop, not an error. The API returns stop_reason="tool_use" whenever Claude wants to call a tool; the caller must handle it.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement source="agent-loop-folklore">Set a max-iterations guard (10–20 turns); tool loops without a limit run indefinitely when a tool returns a consistently unhelpful result.</statement>
      <rationale>Infinite loops burn quota and can be triggered by a single broken tool implementation that always returns the same response.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement source="anthropic-docs-2026">Append response.content (the full list including tool_use blocks) as the assistant message, not just the text. Omitting tool_use blocks breaks conversation history and causes API errors on the next turn.</statement>
      <rationale>The API requires complete message history including tool_use and tool_result blocks to reconstruct the conversation state.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement source="anthropic-docs-2026">Execute all tool_use blocks from a single response concurrently — do not serialize what Claude dispatched in parallel; return all results in one user message.</statement>
      <rationale>Claude requests parallel tools to reduce round-trips; serializing them defeats the optimization and increases latency.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement source="anthropic-docs-2026">Return tool errors as content (not is_error=True) unless the error should terminate the loop — Claude can often recover from tool errors when they're returned as data.</statement>
      <rationale>is_error=True signals an unrecoverable error; most tool failures (timeouts, not-found) are recoverable if Claude sees the error message as data.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement source="forced-tool-pattern">To get guaranteed typed output, define a single extraction tool and force it with tool_choice={"type": "tool", "name": "..."}; access the result from tool_use.input.</statement>
      <rationale>Prompt engineering ("return only JSON") fails occasionally even with temperature=0. The forced tool call routes through the tool use pathway which is more reliable for structured data.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement source="mcp-spec">MCP is primarily designed for Claude Desktop (stdio transport); programmatic API usage should use standard tool use, not MCP.</statement>
      <rationale>MCP servers communicate via stdio by default and require subprocess lifecycle management; standard tool use is stateless and easier to operationalize in servers.</rationale>
    </rule>
    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement source="self">Preconditions hold → implement the loop per content/04-procedure.xml.</statement>
      <rationale>Aggregate positive-branch conclusion target.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement source="self">Non-Claude model or no tool calls needed → skip; use vendor-specific or pure-text completion.</statement>
      <rationale>Aggregate negative-branch conclusion target.</rationale>
    </rule>
  </section>
</text>
