<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Ollama Agent Integration — Worked Example" version="1.0.0" est_tokens="600" depth="medium">
  <summary>One end-to-end worked example showing the artefact shape this methodology produces.</summary>

  <section title="Worked example">
    <example type="good">
      <code lang="python"><![CDATA[

from openai import OpenAI

# Drop-in replacement: just change base_url
client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama"  # Required but not validated by Ollama
)

def local_chat(prompt: str, model: str = "llama3.1:8b") -> str:
    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": prompt}],
        max_tokens=1024,
    )
    return response.choices[0].message.content

# Streaming via OpenAI-compatible endpoint
stream = client.chat.completions.create(
    model="llama3.1:8b",
    messages=[{"role": "user", "content": "Tell me a joke"}],
    stream=True
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)

]]></code>
      <note>The /v1/ endpoint has slightly different error format than the native Ollama API — test error handling paths with both if using mixed clients.</note>
    </example>
  </section>
</text>
