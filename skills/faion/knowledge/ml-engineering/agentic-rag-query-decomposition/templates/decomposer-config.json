{
  "__faion_header__": {
    "purpose": "decomposer-config artefact",
    "consumes": "planner availability + parallel retriever + confidence threshold",
    "produces": "config validated against 02-output-contract.xml",
    "depends_on": "content/02-output-contract.xml schema",
    "token_budget_impact": "0 LLM tokens; declarative"
  },
  "min_sub_queries": 2,
  "max_sub_queries": 4,
  "planner_model": "sonnet",
  "parallel": true,
  "confidence_threshold": 0.6,
  "coverage_required": true
}
