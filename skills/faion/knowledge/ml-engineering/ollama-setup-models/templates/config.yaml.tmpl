# purpose: yaml config skeleton for ollama-setup-models
# consumes: task brief + env
# produces: yaml config matching the contract
# depends-on: content/02-output-contract.xml
# token-budget-impact: small

# ollama-setup-models — config skeleton
# Replace placeholders, then validate with scripts/validate-ollama-setup-models.py
env: dev
keys:
  timeout: 30
  retries: 3
forbidden_seen: []
