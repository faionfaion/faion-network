<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Tool Use Basics — Decision Tree" version="1.1.0" est_tokens="400">
  <decision-tree>
    <root-question id="q-root">Does the task require calling external functions or APIs from the LLM?</root-question>
    <branch when="needs_external_calls == true and iterations_estimate &lt;= 10">
      <conclusion ref="r1-typed-tool">Use tool-use pattern with the typed registry.</conclusion>
    </branch>
    <branch when="needs_external_calls == true and iterations_estimate &gt; 10">
      <conclusion ref="r2-bounded-iterations">Reconsider — this is closer to an agent than tool use. Route to ai-agent-patterns or agents-react-pattern.</conclusion>
    </branch>
    <branch when="needs_external_calls == false">
      <conclusion ref="r1-typed-tool">Skip — structured-output-basics is sufficient.</conclusion>
    </branch>
    <example trace="needs_external_calls == true and iterations_estimate &lt;= 10">
      <p>Use tool-use pattern with the typed registry.</p>
    </example>
  </decision-tree>
</text>
