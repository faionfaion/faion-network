<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Guardrails Testing Decision Tree" version="1.1.0" est_tokens="400" depth="essential">
  <summary>Pick suite scope by trigger.</summary>

  <decision-tree>
    <root-question id="q-trigger">What triggered the test run?</root-question>
    <branch when="trigger == pre_deploy">
      <conclusion ref="three-suite-split" verdict="run all three suites; verdict gates merge"/>
    </branch>
    <branch when="trigger == model_upgrade">
      <conclusion ref="three-suite-split" verdict="run all three + refresh perf baseline only after green"/>
    </branch>
    <branch when="trigger == monthly_redteam">
      <conclusion ref="payload-library-versioned" verdict="run security suite + bump to v_next.jsonl; accuracy + perf optional"/>
    </branch>
    <branch when="trigger == fp_incident">
      <conclusion ref="fp-budget" verdict="run accuracy suite only with reported input added; lower budget temporarily"/>
    </branch>
  </decision-tree>

  <example trace="model upgrade → full three-suite run + new baseline">
    <code lang="text"><![CDATA[
trigger=model_upgrade → all three suites → if green, refresh bench.json baseline
]]></code>
  </example>
</text>
