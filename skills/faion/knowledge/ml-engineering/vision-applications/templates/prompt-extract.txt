<!-- purpose: Structured field extraction prompt for document task. -->
<!-- consumes: field_list placeholder. -->
<!-- produces: VLM JSON with all listed fields (null where absent). -->
<!-- depends-on: response_format json_object + Pydantic validation downstream. -->
<!-- token-budget-impact: ~80 prompt tokens. -->
Analyze this image and extract the following fields as JSON.
Required fields: {field_list}
For missing or illegible fields, use null.
Do not include fields not in the list.
Return only valid JSON, no explanation.
