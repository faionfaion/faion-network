{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/ml-engineering/llm-observability/observability-spec.vars.schema.json",
  "title": "observability-spec template variables",
  "description": "Values for templates/observability-spec.md.j2 and templates/observability-spec.html.j2. Generated by tpl-jinja from observability-spec.md; `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "classes_redacted": {
      "type": "string",
      "title": "Classes redacted",
      "description": "The classes redacted used under 'PII redaction (rule r4)'.",
      "enum": [
        "email",
        "phone",
        "national ID",
        "other"
      ]
    },
    "drop_alert": {
      "type": "integer",
      "title": "Drop alert",
      "description": "The drop alert used under 'Continuous eval (rule r2)'."
    },
    "eval_accuracy": {
      "type": "integer",
      "title": "Eval accuracy",
      "description": "The eval accuracy used under 'Alerts'."
    },
    "owner_channel": {
      "type": "string",
      "title": "Owner channel",
      "description": "Eval accuracy. From section 'Alerts'."
    },
    "pii_redaction": {
      "type": "string",
      "title": "Pii redaction",
      "description": "The pii redaction used under 'PII redaction (rule r4)'."
    },
    "region": {
      "type": "string",
      "title": "Region",
      "description": "Trace storage region. From section 'Vendor (rule r6)'."
    },
    "sampling_policy": {
      "type": "integer",
      "title": "Sampling policy",
      "description": "The sampling policy used under 'Traces (rules r1, r5)'."
    },
    "scoring": {
      "type": "string",
      "title": "Scoring",
      "description": "The scoring used under 'Continuous eval (rule r2)'.",
      "enum": [
        "exact match",
        "LLM-judge rubric"
      ]
    },
    "spec_name": {
      "type": "string",
      "title": "Spec name",
      "description": "LLM Observability Spec."
    },
    "trace_coverage_pct": {
      "type": "string",
      "title": "Trace coverage pct",
      "description": "The trace coverage pct used under 'Traces (rules r1, r5)'."
    }
  },
  "required": [
    "classes_redacted",
    "drop_alert",
    "eval_accuracy",
    "owner_channel",
    "pii_redaction",
    "region",
    "sampling_policy",
    "scoring",
    "spec_name",
    "trace_coverage_pct"
  ],
  "x-faion-dictionary-refs": 0
}
