<?xml version="1.0" encoding="UTF-8"?>
<text id="02-xr-accessibility" title="XR Accessibility with AI">
  <summary>XAUR coverage by disability type, three-tier input fallback hierarchy, and privacy architecture requirements for spatial computing.</summary>

  <section title="AI Accessibility by Disability Type">
    <list>
      <item>Vision: AI describes spatial environment for blind users; object detection with audio feedback; obstacle detection; text recognition and reading aloud; scene summarization</item>
      <item>Motor: gaze-based selection (eye tracking); voice commands for all interactions; AI gesture simplification (reduce precision needed); dwell-time activation; head-tracking alternatives</item>
      <item>Cognitive: AI simplifies complex spatial interfaces; step-by-step guidance with visual arrows; reduce information density; context-aware help; predictive text for spatial keyboards</item>
      <item>Hearing: visual captions for spatial audio; haptic feedback for sound cues; visual indicators for directional sound; AI lip-reading for avatars</item>
    </list>
  </section>

  <section title="Three-Tier Input Fallback">
    <rule>
      <statement>Design all spatial interactions with a three-tier fallback hierarchy — gaze → voice → controller/gesture. Never assume a single input modality.</statement>
      <rationale>Motor, vision, or hearing impairments may prevent use of any single modality. Three-tier fallback ensures the interaction is accessible even when two of the three modalities are unavailable.</rationale>
    </rule>
    <reference path="templates/interaction-fallback-spec.md">Template for documenting spatial interaction fallback hierarchy with privacy fields</reference>
  </section>

  <section title="XAUR Gap Analysis">
    <rule>
      <statement>Run W3C XAUR gap analysis early in design, not after implementation — spatial accessibility retrofits are expensive.</statement>
      <rationale>Spatial interfaces are harder to retrofit for accessibility than web interfaces. Core input methods and scene understanding architecture decisions made early determine what accessibility is achievable.</rationale>
    </rule>
    <rule>
      <statement>XAUR is not machine-checkable — treat agent gap analysis as a structured opinion, not a compliance verdict. Human XR accessibility expert must validate on actual hardware.</statement>
      <rationale>No automated tool covers XR accessibility criteria as of 2026. Agent analysis is input for human review, not a substitute.</rationale>
    </rule>
  </section>

  <section title="Privacy Architecture">
    <list>
      <item>Local processing where possible — biometric and spatial data should not leave device for real-time features</item>
      <item>Clear privacy controls with explicit opt-in for AI features</item>
      <item>Data minimization — collect only what is needed for the specific interaction</item>
      <item>Transparent AI decision-making — users must be able to understand and override AI behavior</item>
      <item>GDPR/CCPA disclosure required for eye tracking, face tracking, hand tracking, and scene scanning</item>
    </list>
  </section>

  <reference path="templates/prompt-xaur-gap-analysis.txt">Sonnet prompt for structured XAUR gap analysis of XR design specs</reference>
</text>
