<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agent-integration" title="Agent Integration for LLM Conversational AI">
  <summary>Subagent roles, prompt patterns, service catalog, and gotchas for building agent-driven conversational AI pipelines.</summary>

  <section title="Subagent Roles">
    <rule>
      <statement>Use a Sonnet subagent for conversation turn generation and a Haiku subagent for TTS, logging, and guardrail validation.</statement>
      <rationale>Sonnet handles intent understanding and ambiguity resolution; Haiku handles mechanical integration tasks (TTS API calls, log formatting, tool call validation) at lower cost and latency.</rationale>
    </rule>
  </section>

  <section title="Services">
    <list>
      <item>OpenAI Whisper (cloud) — high-accuracy ASR REST API (90%+ on standard accents)</item>
      <item>Deepgram — low-latency streaming ASR; best for real-time voice</item>
      <item>AssemblyAI — real-time streaming ASR with speaker diarization</item>
      <item>Amazon Polly — neural TTS with SSML support and voice persona selection</item>
      <item>ElevenLabs — ultra-realistic TTS with voice cloning and streaming</item>
      <item>OpenAI Realtime API — end-to-end voice (ASR + LLM + TTS) over WebSocket</item>
      <item>Vapi — managed voice agent infrastructure with phone call support</item>
      <item>Pipecat — OSS voice agent framework (ASR → LLM → TTS pipeline)</item>
      <item>LiveKit Agents — real-time voice agent SDK</item>
    </list>
  </section>

  <section title="Prompt Patterns">
    <reference path="templates/persona-system-prompt.txt">Voice agent persona with scope boundary and escalation rules</reference>
    <reference path="templates/guardrail-validator-prompt.txt">Guardrail agent prompt for tool call validation</reference>
    <reference path="templates/pipeline.py">Minimal ASR → LLM → TTS pipeline (Python)</reference>
  </section>

  <section title="Gotchas">
    <antipattern>
      <description>Storing presigned ASR URLs instead of downloading immediately.</description>
      <reason>Presigned URLs from ASR services expire. Download the audio or transcript immediately after generation.</reason>
    </antipattern>
    <antipattern>
      <description>Long system prompts for persona definition.</description>
      <reason>Keep persona prompt under 200 tokens; use retrieval for factual knowledge. Long system prompts increase latency and cost without proportional quality gain.</reason>
    </antipattern>
    <antipattern>
      <description>Relying on automated metrics alone for quality monitoring.</description>
      <reason>Review conversation logs weekly for hallucinated facts, persona drift, and off-topic escalation. Automated metrics miss nuanced failures. Alert if escalation rate exceeds 10%.</reason>
    </antipattern>
  </section>
</text>
