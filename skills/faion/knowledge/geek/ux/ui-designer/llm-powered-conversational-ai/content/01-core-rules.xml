<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="llm-powered-conversational-ai Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for llm-powered-conversational-ai. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Route user speech through ASR → LLM → TTS in sequence; never skip or merge stages.</statement>
      <rationale source="v1-source">Each stage has distinct failure modes. ASR errors propagate to LLM as noisy input; LLM output must be buffered to sentence boundaries before TTS to prevent unnatural speech fragments.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Buffer LLM streaming output to sentence boundaries before submitting to TTS.</statement>
      <rationale source="v1-source">Partial-sentence TTS sounds unnatural and breaks comprehension. Sentence-boundary buffering adds minimal latency while preserving audio quality.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Keep conversation history to 5–10 turns; summarize older context rather than dropping it.</statement>
      <rationale source="v1-source">Unbounded history grows context window cost and latency without proportional benefit. Summarization preserves relevant prior context at fixed cost.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Define the persona scope boundary explicitly in the system prompt; require the LLM to escalate when outside it.</statement>
      <rationale source="v1-source">Without an explicit boundary, LLMs engage with off-topic queries confidently. Escalation triggers (user says "agent", "human", "frustrated") are more reliable than sentiment scoring.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Always confirm with the user before executing real-world actions (booking, purchasing, deleting).</statement>
      <rationale source="v1-source">One LLM turn of confirmation is cheap; a bad action is not. Never trust LLM-generated parameter values directly — validate against schema before execution.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Run a guardrail validator agent on every tool call before execution.</statement>
      <rationale source="v1-source">The guardrail agent must see the original ASR transcript, not just the LLM-generated call, because the LLM may hallucinate parameters not present in the user's speech.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use a Sonnet subagent for conversation turn generation and a Haiku subagent for TTS, logging, and guardrail validation.</statement>
      <rationale source="v1-source">Sonnet handles intent understanding and ambiguity resolution; Haiku handles mechanical integration tasks (TTS API calls, log formatting, tool call validation) at lower cost and latency.</rationale>
    </rule>
  </section>
</text>
