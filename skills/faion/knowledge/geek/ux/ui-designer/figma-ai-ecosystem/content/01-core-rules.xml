<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="figma-ai-ecosystem Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for figma-ai-ecosystem. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>None of Figma's AI features (Make, Draw, Sites publishing, Image Tools) can be triggered by agents via any API — all require a human in a Figma UI session.</statement>
      <rationale source="v1-source">There is no API or programmatic interface to these features. Any workflow that claims to "use Figma Make via an agent" requires a human at the keyboard. Agents that assume otherwise will silently fail.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use Figma Webhooks instead of polling the REST API to detect file changes.</statement>
      <rationale source="v1-source">Figma REST API rate limit is 150 requests/minute per token. Polling a complex file for changes quickly exhausts limits. Webhooks are event-driven and do not count against the rate limit for detection.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Agents prepare structured Figma Make prompts as versioned documents and log them for human execution.</statement>
      <rationale source="v1-source">Figma Make has no versioning; re-generating with the same prompt produces different results. Logging prepared prompts makes iteration traceable and the prompt library improvable over time.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>After a Figma Sites publish, always run a Lighthouse + axe audit — Figma does not validate accessibility or performance on publish.</statement>
      <rationale source="v1-source">Figma Sites published output is not accessibility-validated. Published pages may fail WCAG contrast, missing ARIA labels, and performance requirements that a designer cannot detect in the Figma canvas.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Separate Figma AI output frames from production design frames using a naming convention (e.g., "[AI Draft] Feature Name").</statement>
      <rationale source="v1-source">Figma Make output is non-deterministic and prototype-grade. Without a clear naming separator, AI draft frames may be accidentally included in developer handoff or treated as canonical designs.</rationale>
    </rule>
  </section>
</text>
