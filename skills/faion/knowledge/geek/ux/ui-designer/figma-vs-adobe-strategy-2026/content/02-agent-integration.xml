<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agent-integration" title="Agent Integration for Toolchain Audit">
  <summary>Agentic workflow for toolchain audits, service catalog, and gotchas for platform comparison automation.</summary>

  <section title="Agentic Workflow">
    <rule>
      <statement>Agents produce the toolchain analysis; humans make the final platform decision.</statement>
      <rationale>Tool selection involves expertise, team culture, and migration cost that cannot be fully captured in a scoring rubric. Agent output is a structured input to the decision, not the decision itself.</rationale>
    </rule>
    <rule>
      <statement>Provide team size, subscription tier, and task list as explicit inputs — agents cannot discover these from code.</statement>
      <rationale>Agents cannot access Figma billing or Adobe license information. Without explicit inputs, the agent will hallucinate or guess plan capabilities incorrectly.</rationale>
    </rule>
  </section>

  <section title="Services">
    <list>
      <item>Figma REST API (SaaS) — full read, limited write; best agent automation surface for design operations</item>
      <item>Adobe Firefly Services (enterprise SaaS) — asset generation REST API; requires enterprise plan</item>
      <item>Adobe Creative Cloud API (SaaS) — UXP automation inside CC apps; not fully headless</item>
      <item>Zeroheight (SaaS) — design system docs that integrate with both Figma and Adobe</item>
      <item>figma-export (community CLI) — export assets/specs via CLI for migration audits</item>
      <item>jq — parse and compare JSON exports from both platforms</item>
    </list>
  </section>

  <section title="Gotchas">
    <antipattern>
      <description>Referencing AI feature names rather than capabilities in automation scripts.</description>
      <reason>Both platforms have changed their AI feature names multiple times since 2024. Prompt patterns should reference capabilities (e.g., "text-to-vector generation") not product names (e.g., "Firefly Draw") that may be renamed.</reason>
    </antipattern>
    <antipattern>
      <description>Assuming Adobe Creative Cloud API is headless for automation pipelines.</description>
      <reason>Adobe CC automation via UXP requires a human session inside a CC app. Purely headless pipelines must use Firefly Services (enterprise only). Standard CC plans have no headless automation surface.</reason>
    </antipattern>
  </section>

  <reference path="templates/scoring.py">Weighted scoring script with product-team and agency presets</reference>
  <reference path="templates/toolchain-audit-prompt.txt">Claude prompt for scoring a team workflow against Figma and Adobe</reference>
</text>
