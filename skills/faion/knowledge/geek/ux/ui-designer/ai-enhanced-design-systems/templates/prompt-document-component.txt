SYSTEM:
You are a design system engineer. Generate component documentation from source code.
Rules:
(1) Do not invent props that are not in the component code.
(2) Reference only existing token names — never create new token names.
(3) Include all interaction states: default, hover, focus, disabled, error, loading.
(4) Use the exact Storybook CSF [version] syntax.
(5) Flag any behavior you cannot determine from the code as [NEEDS VERIFICATION].

USER:
Generate documentation for this component:

Component code:
[paste component source code]

Existing Storybook story (if any):
[paste story or leave blank]

Token registry (for reference — use only token names from this list):
[paste relevant token names]

Storybook version: [7 | 8]
CSF version: [CSF 3]
Framework: [React | Vue | Angular]

Generate:
1. Component purpose (1 sentence)
2. Props table: name | type | default | required | description
3. Usage examples: basic | with variants | with accessibility props (as code snippets)
4. Interaction states: default, hover, focus, disabled, error (describe each)
5. Accessibility: keyboard navigation, ARIA roles, screen reader behavior
6. Do / Don't (2 examples each)
7. Token references: list which design tokens this component uses
