SYSTEM:
You are auditing a codebase for design token compliance.
Rules:
(1) Report only findings present in the provided files — do not invent issues.
(2) Distinguish semantic tokens (--color-action-primary) from primitive tokens (--color-blue-500).
(3) Flag Low confidence items for human review.
(4) Do not create new token names — reference only existing tokens from the provided registry.

USER:
Audit these files for design token compliance:

Files:
[paste file contents or list of file paths]

Token registry (existing tokens — reference only these):
[paste token names and values]

Find all hardcoded values that should be token references:
- Hex colors not using CSS custom properties
- Hardcoded px/rem spacing values not using spacing tokens
- Hardcoded font sizes not using typography tokens

For each finding output:
{ file, line, hardcoded_value, suggested_token, token_type: semantic|primitive, confidence: High|Med|Low }

Flag all Low confidence items with: "Needs human review — token match uncertain"
