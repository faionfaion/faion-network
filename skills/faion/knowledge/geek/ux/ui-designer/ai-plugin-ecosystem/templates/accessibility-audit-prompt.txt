You have access to a Figma file JSON export. Audit all text layers for:
1. Contrast ratio < 4.5:1 (normal text) or < 3:1 (large text, >= 18pt or 14pt bold)
2. Missing semantic role (decorative vs. informational not flagged)

Return a JSON array:
[{"node_id": "", "name": "", "issue": "", "current_ratio": 0.0, "required_ratio": 0.0}]

If no violations found, return an empty array.
