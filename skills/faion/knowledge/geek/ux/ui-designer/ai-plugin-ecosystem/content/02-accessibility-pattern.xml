<?xml version="1.0" encoding="UTF-8"?>
<text id="02-accessibility-pattern" title="Accessibility Audit Pattern via Figma REST">
  <summary>WCAG contrast ratio check using Figma REST API data, bypassing the Stark plugin for agent-driven audits.</summary>

  <section title="Dual-Layer Audit Rule">
    <rule>
      <statement>Run both a plugin (Stark, during design) and an automated axe-core scan (post-handoff on rendered HTML) — neither alone is sufficient.</statement>
      <rationale>Figma plugin audits catch design-phase issues; axe-core catches implementation-phase regressions. Accessibility plugins flag issues but cannot auto-fix them; a human must apply remediations.</rationale>
    </rule>
  </section>

  <section title="Agent Contrast Check Pattern">
    <rule>
      <statement>Extract color fills from Figma REST API and compute WCAG contrast ratios in code, without requiring the Stark plugin.</statement>
      <rationale>Agents cannot trigger plugin runs. Computing contrast ratios from the REST API node data achieves the same result deterministically and without a human Figma session.</rationale>
    </rule>
    <example type="good">
      <note>Fetch file styles via REST, extract text/background fill pairs, compute relative luminance, flag pairs below WCAG thresholds.</note>
      <code lang="bash"><![CDATA[curl -H "X-Figma-Token: $FIGMA_TOKEN" \
  "https://api.figma.com/v1/files/$FILE_KEY/styles"]]></code>
    </example>
    <reference path="templates/contrast-check.py">WCAG contrast ratio computation from Figma REST API data</reference>
    <reference path="templates/accessibility-audit-prompt.txt">Claude prompt for auditing Figma file JSON for contrast violations</reference>
  </section>

  <section title="Gotchas">
    <antipattern>
      <description>Using a read-only Figma token for write operations.</description>
      <reason>A read-only token silently fails write operations. Token scopes (read vs. write) must be set correctly per operation type.</reason>
    </antipattern>
    <antipattern>
      <description>Logging plugin version alongside design outputs.</description>
      <reason>AI plugin outputs change with model updates. Without version logging, it is impossible to reproduce or audit which model state produced a given design artifact.</reason>
    </antipattern>
  </section>
</text>
