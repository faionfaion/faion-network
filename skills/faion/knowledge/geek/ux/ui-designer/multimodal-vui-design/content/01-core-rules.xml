<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="multimodal-vui-design Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for multimodal-vui-design. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Always provide a three-tier fallback hierarchy — primary (voice) → secondary (touch) → tertiary (keyboard if available).</statement>
      <rationale source="v1-source">Users who cannot speak need touch alternatives; users who cannot touch need keyboard. Designing primary-only leaves large user segments unable to complete interactions.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Design the fallback hierarchy before the primary interaction — if touch is broken, voice is broken too for a large user segment.</statement>
      <rationale source="v1-source">Multimodal state sync failures often affect both voice and visual layers simultaneously. The fallback path must work independently of the primary path.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Set visual element timeouts to match displayed content complexity: list (10s), detail card (20s), multi-step form (60s). Minimum 10s for accessibility.</statement>
      <rationale source="v1-source">Agent-suggested timeouts are often too short for elderly users or users with cognitive disabilities. Accessibility-minimum values must be specified explicitly in the prompt.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Spoken summaries must be shorter than visual content — users listening cannot process paragraph-length speech.</statement>
      <rationale source="v1-source">VUI users listening to confirmation speech cannot rewind. Visual content can be re-read; spoken content cannot. Keep summaries to 1–2 sentences maximum.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use a Haiku subagent for interaction pattern generation and APL/response template scaffolding, and a Sonnet subagent for state-sync consistency review and accessibility fallback audit.</statement>
      <rationale source="v1-source">Haiku handles template generation efficiently; Sonnet provides deeper review of edge cases and platform constraints. Human VUI designers validate on actual hardware before production deployment.</rationale>
    </rule>
  </section>
</text>
