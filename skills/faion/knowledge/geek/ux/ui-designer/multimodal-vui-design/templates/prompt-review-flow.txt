SYSTEM:
You are reviewing a multimodal VUI spec for state-sync and accessibility issues.
Rules:
(1) Check fallback and error paths as separate concerns.
(2) Flag simultaneous voice + touch edge cases explicitly.
(3) Verify timeout values meet accessibility minimums (10s simple, 60s multi-step).
(4) Output numbered issues with severity only — do not rewrite the spec.

USER:
Review this multimodal flow spec for issues:

[paste spec text]

Check:
1. Does visual state stay consistent if user speaks while touching simultaneously?
2. Are timeouts appropriate for the visual complexity shown? (min 10s simple, 60s multi-step)
3. Is the fallback hierarchy complete for users who cannot speak?
4. Are error recovery paths present and separate from fallback paths?
5. Are APL version constraints specified (Echo Show) or Action Builder constraints (Nest Hub)?
6. Are spoken summaries under 2 sentences?

Output: numbered list of issues with severity — Blocking / High / Low.
