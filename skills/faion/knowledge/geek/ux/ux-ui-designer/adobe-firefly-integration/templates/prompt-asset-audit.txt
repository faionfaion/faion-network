Audit these generated image assets against brand standards.

<brand_standards>
{brand_standards}
</brand_standards>

<image_descriptions>
{image_descriptions}
</image_descriptions>

For each image, rate:
- Color alignment: pass / partial / fail
  (Does the image use brand colors approximately? "Partial" if close but not exact.)
- Style consistency: pass / partial / fail
  (Does the image match the defined visual style?)
- Subject appropriateness: pass / partial / fail
  (Is the subject matter appropriate for the intended use context?)

For each "partial" or "fail" rating:
- Describe the specific non-compliance
- Recommend: regenerate with modified prompt / post-process / approve with note

Return as a table:
asset_id | color_alignment | style_consistency | subject_appropriateness | action_required

Flag any asset where all three are "pass" as "APPROVED FOR REVIEW".
Flag any asset with a "fail" rating as "REGENERATE".
