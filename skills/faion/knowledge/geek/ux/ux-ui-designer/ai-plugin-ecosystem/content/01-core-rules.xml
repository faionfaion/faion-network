<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="ai-plugin-ecosystem Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for ai-plugin-ecosystem. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Agents cannot invoke Figma plugins headlessly. The Figma plugin sandbox is browser-JS only — no server-accessible API exists.</statement>
      <rationale source="v1-source">Figma's plugin architecture runs code in a browser-sandboxed iframe. There is no REST API or CLI for triggering plugins externally. Agents that propose API-driven plugin automation are hallucinating.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Agent role is pre-processing (generate data files, configuration scripts) and post-processing (analyze Figma JSON exports). Humans run plugins in Figma.</statement>
      <rationale source="v1-source">This boundary is firm and determined by the Figma plugin sandbox architecture. Designing workflows that respect this boundary avoids wasted engineering effort.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Set brand guidelines (color variables, typography styles) in Figma before running any generative plugin.</statement>
      <rationale source="v1-source">AI plugin outputs anchor to existing styles when present; without them, generative outputs default to generic aesthetics that require rework.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Document which content in a file is AI-generated with a Figma annotation.</statement>
      <rationale source="v1-source">AI-generated content must be distinguishable from human-authored content for future audits, brand reviews, and accessibility checks.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Run Stark contrast audit on every screen before handoff; do not rely on visual inspection alone.</statement>
      <rationale source="v1-source">Contrast issues are invisible to designers; automated checking catches them at scale.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Version-pin plugin usage: note plugin version in design system changelog when a plugin modifies components at scale.</statement>
      <rationale source="v1-source">Plugin behavior and output vary across versions; tracking versions prevents accidental regressions when plugins auto-update.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Validate AI-generated icons (Magician) against brand voice before accepting — they skew toward generic/clip-art.</statement>
      <rationale source="v1-source">AI icon generation produces aesthetically safe but on-brand-ambiguous results; human review is required for brand consistency.</rationale>
    </rule>
  
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Skip this methodology when the Applies-If preconditions are not met OR any Skip-If disqualifier holds.</statement>
      <rationale source="F-066 v3 B5.6 — mandatory skip leaf">Decision-tree leaves must reference a rule id. The skip leaf needs a real rule so non-applicable inputs route cleanly out instead of producing false-positive verdicts.</rationale>
    </rule>
  </section>
</text>
