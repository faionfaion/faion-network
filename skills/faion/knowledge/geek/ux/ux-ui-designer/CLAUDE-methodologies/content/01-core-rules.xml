<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="CLAUDE-methodologies Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for CLAUDE-methodologies. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Agents can scaffold, template, and synthesize UX deliverables. Agents cannot recruit, moderate, or interpret participant behavior for user interviews, usability testing, card sorting, tree testing, diary studies, focus groups, or A/B testing.</statement>
      <rationale source="v1-source">These methods depend on observed human behavior in context. An agent generating synthetic "participant responses" produces stereotypes and invalid research artifacts.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>When an agent generates a persona without real user data as input, it must label the artifact explicitly: "SYNTHETIC PERSONA — not grounded in user research. Treat as a hypothesis only."</statement>
      <rationale source="v1-source">Synthetic personas look identical to research-grounded personas but encode the designer's assumptions, not actual user behavior. Mislabeling leads to design decisions based on fiction.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Journey maps generated by an agent without real user research data must be labeled: "HYPOTHESIS MAP — assumed, not validated." Every emotional state and pain point is an assumption until validated with real users.</statement>
      <rationale source="v1-source">Journey maps without the label appear authoritative. Teams downstream act on them as validated research, encoding assumptions into product decisions.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>When running a heuristic evaluation, an agent must have access to the actual UI (screenshot, DOM export, or prototype URL) — not just a description. Evaluations from descriptions are evaluations of descriptions, not of the interface.</statement>
      <rationale source="v1-source">LLM heuristic evaluations without seeing the actual UI produce plausible-sounding but shallow findings. The agent must state this limitation explicitly if UI access is unavailable.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>For each heuristic, the agent must output: rating (pass / partial / fail), specific issue citation if partial or fail, severity (0 cosmetic / 1 minor / 2 major / 3 catastrophic), and a proposed fix. Skip heuristics that cannot be evaluated — do not speculate.</statement>
      <rationale source="v1-source">Incomplete heuristic evaluations with missing severity ratings are not actionable. Developers need prioritized fixes, not a list of "this might be an issue."</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Match method to question type: use surveys for "how many?", user interviews for "why?", usability testing for "can users do X?", and heuristic evaluation for "does this violate principles?" Do not use an interview to answer a quantitative question or a survey to understand motivation.</statement>
      <rationale source="v1-source">Method-question mismatch produces invalid results. Surveys cannot reveal why; interviews cannot produce statistically significant counts.</rationale>
    </rule>
  
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Skip this methodology when the Applies-If preconditions are not met OR any Skip-If disqualifier holds.</statement>
      <rationale source="F-066 v3 B5.6 — mandatory skip leaf">Decision-tree leaves must reference a rule id. The skip leaf needs a real rule so non-applicable inputs route cleanly out instead of producing false-positive verdicts.</rationale>
    </rule>
  </section>
</text>
