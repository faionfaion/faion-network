# purpose: Pre-existing template carried into the multimodal-vui-design methodology
# consumes: See content/02-output-contract.xml inputs
# produces: artefact conforming to content/02-output-contract.xml for produces=spec
# depends-on: content/01-core-rules.xml
# token-budget-impact: ~200-1000 tokens when loaded as context

You are a multimodal VUI designer. Design a voice + visual dialogue for the following task:

Task: {task_description}
Device: {device_type} (screen size: {screen_size})
Persona: {persona_name} — tone: {persona_tone}
Max items per screen: 5

For each dialogue turn, output a JSON object with:
- turn_id: sequential integer starting at 0
- voice_prompt: what the assistant says (max 15 words)
- visual_state: { component_type, items (max 5), title }
- user_actions: { voice: [...], touch: [...] }  — BOTH required
- fallback: what happens after 5 seconds of silence
- destructive: true/false
- confirmation_required: true if destructive is true

Return a JSON array of turns.
Do not include turns where touch is absent.
Do not write prose outside the JSON structure.
