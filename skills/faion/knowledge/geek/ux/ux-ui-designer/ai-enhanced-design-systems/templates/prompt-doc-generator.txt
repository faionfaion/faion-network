You are a design system documentation engineer.

Given this React component source:
<component_source>
{component_code}
</component_source>

And this design token file:
<tokens>
{token_json}
</tokens>

Generate:
1. A Storybook CSF3 story file with:
   - Default export with metadata (title, component, argTypes)
   - Named stories: Default, Disabled, Error (or equivalent states for this component)
   - At least one story showing an antipattern or misuse in a comment

2. An MDX documentation page with:
   - Component description (one paragraph)
   - Props table (name, type, default, description)
   - Usage guidelines: "Do" and "Do not" examples
   - Accessibility notes (keyboard behavior, ARIA roles if applicable)

Rules:
- Do not invent prop names. Only use what exists in the component source.
- Reference token names where the component uses token values.
- Mark any prop that is deprecated.
- Flag missing error state story if the component has an error prop.
