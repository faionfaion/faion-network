Audit this component library for token usage violations.

<components_list>
{component_files}
</components_list>

<token_file>
{tokens}
</token_file>

For each file, identify:
- Hardcoded color values (#hex, rgb(), rgba(), hsl()) that should use a token reference
- Hardcoded spacing values (padding, margin, gap in px) that should use a spacing token
- Hardcoded font-size values that should use a typography token

Return a JSON array:
[
  {
    "file": "path/to/file",
    "line": 42,
    "hardcoded_value": "#1a73e8",
    "suggested_token": "color.primary.600",
    "type": "color"
  }
]

Rules:
- If no matching token exists, suggest the closest token and flag it as "approximate"
- Ignore values inside comments
- Ignore test files (*.test.*, *.spec.*)
- Cap output at 50 violations; summarize total count
