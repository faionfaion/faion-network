<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agentic-workflow" title="Agentic Workflow for Figma AI Ecosystem">
  <summary>What agents can and cannot do with Figma AI tools, prompt patterns, services, and agent gotchas.</summary>

  <section title="Agent Scope">
    <p>Agents cannot invoke Figma AI tools directly — Make, Draw, and image tools are browser-only with no public API. Agent roles: (1) generate optimized Figma Make prompts, (2) produce structured content briefs for human paste into Make, (3) analyze Figma REST API file exports to identify frames as candidates for AI image tools, (4) generate Figma Sites publishing checklists and pre-validate content.</p>
    <rule>
      <statement>Do not build workflows that assume Figma Make, Draw, or Sites have agent-callable APIs. They do not exist as of 2026.</statement>
      <rationale>Agents persistently hallucinate Figma Make API endpoints; this is the single most common error in Figma AI workflow design.</rationale>
    </rule>
    <rule>
      <statement>Audit Figma Variables before running Figma Make. Color/type tokens must be clearly named — Make uses variable names as context.</statement>
      <rationale>Poorly named variables (e.g., "style-2" instead of "color/brand/primary") produce generic AI output that ignores the design system.</rationale>
    </rule>
  </section>

  <section title="Recommended Subagents">
    <list>
      <item>haiku — generating Figma Make prompts, publishing checklists, image enhancement candidate lists from Figma JSON</item>
      <item>sonnet — full screen brief generation, design token audit from Figma JSON, Figma Sites content review</item>
    </list>
  </section>

  <section title="Prompt and Template References">
    <reference path="templates/find-image-nodes.py">Figma REST API script to find raster-fill nodes suitable for AI image tools</reference>
    <reference path="templates/prompt-make.txt">Prompt for generating optimized Figma Make input prompts</reference>
    <reference path="templates/prompt-sites-review.txt">Prompt for reviewing Figma Sites page specs for SEO and static limitations</reference>
  </section>

  <section title="Agent Gotchas">
    <antipattern>
      <description>Agent proposes polling Figma Webhooks for Make or Draw completion events.</description>
      <reason>Figma Webhooks only fire on file events, not on AI feature completion; polling for Make/Draw results via webhooks is architecturally impossible.</reason>
    </antipattern>
    <antipattern>
      <description>Agent generates Figma Make prompts longer than 150 words.</description>
      <reason>The in-app AI uses a limited context window; prompts over 150 words produce degraded output. Keep prompts concise and specific.</reason>
    </antipattern>
    <antipattern>
      <description>Agent underestimates Figma Sites SEO limitations for SEO-sensitive use cases.</description>
      <reason>Figma Sites has no server-side rendering; any page where search visibility matters is not suitable for Figma Sites publishing.</reason>
    </antipattern>
  </section>
</text>
