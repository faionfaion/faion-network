<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="figma-ai-ecosystem Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for figma-ai-ecosystem. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use Figma Make for 10-minute prototype generation; always rebuild final UI in proper Figma components.</statement>
      <rationale source="v1-source">Make output is for exploration and stakeholder alignment, not as a design deliverable or production starting point.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Keep Figma Make prompts under 150 words. The in-app AI context window is limited; longer prompts produce degraded output.</statement>
      <rationale source="v1-source">LLM-generated Figma Make prompts often exceed the effective context window; brevity and specificity improve output quality.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Keep Figma Draw sketches in a separate draft page. Do not mix generative vector sketches with component library frames.</statement>
      <rationale source="v1-source">Mixing generative and production content in the same Figma pages creates ambiguity during handoff and audits.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Publish Figma Sites only on a custom subdomain with a clear "prototype, not production" header.</statement>
      <rationale source="v1-source">Stakeholders who receive a Figma Sites URL without context may treat it as the production site; explicit labeling prevents scope confusion.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Generate 3 variations when using AI image Expand for hero banners; single-run outputs are often asymmetric.</statement>
      <rationale source="v1-source">AI expansion is probabilistic; single-run outputs frequently have visible seams or asymmetric fill. Selection from multiple variants is the reliable pattern.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Verify plan gating before designing workflows that depend on Figma AI features. All AI features require Figma Professional/Organization plans.</statement>
      <rationale source="v1-source">Free/Starter plan users cannot access Make, Draw, Sites AI features, or AI image tools; workflows designed without plan verification will fail for a subset of teams.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Do not build workflows that assume Figma Make, Draw, or Sites have agent-callable APIs. They do not exist as of 2026.</statement>
      <rationale source="v1-source">Agents persistently hallucinate Figma Make API endpoints; this is the single most common error in Figma AI workflow design.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Audit Figma Variables before running Figma Make. Color/type tokens must be clearly named — Make uses variable names as context.</statement>
      <rationale source="v1-source">Poorly named variables (e.g., "style-2" instead of "color/brand/primary") produce generic AI output that ignores the design system.</rationale>
    </rule>
  </section>
</text>
