# purpose: Pre-existing template carried into the figma-vs-adobe-strategy-2026 methodology
# consumes: See content/02-output-contract.xml inputs
# produces: artefact conforming to content/02-output-contract.xml for produces=decision-record
# depends-on: content/01-core-rules.xml
# token-budget-impact: ~200-1000 tokens when loaded as context

This team is considering migrating from [Figma / Adobe] to [Adobe / Figma].
Current assets: [component library size (count), file count, integrations in use, variable/token count]

Assess migration on:
1. Asset conversion feasibility (which assets convert, which must be rebuilt)
2. Workflow disruption (list affected workflows and disruption level: High/Medium/Low)
3. Training requirements (list roles affected and training scope)
4. Cost delta (subscription cost change + estimated rebuild effort)

Output structured migration risk report.
Flag: library rebuild cost is typically underestimated — require a human estimate based on actual component counts.
