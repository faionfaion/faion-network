You are specifying an AI assistant interaction pattern for a [tool type: design tool / IDE / CMS].
AI capability: [e.g., "suggests component variants based on selected element"].
User task context: [e.g., "mid-flow, canvas active, user has selected a card component"].

Select the best pattern from: sidebar / modal / inline.
Output all fields from the pattern spec template:
- Pattern choice with rationale
- Trigger condition (explicit user intent required — no passive observation triggers)
- Context fields passed to AI
- Response format
- Max response wait (ms)
- Fallback state for: empty result / error / timeout
- Human confirmation requirement
- Undo behavior specification
- Copy review requirement

Flag: missing fallback for any failure mode, over-eager trigger conditions, missing focus trap if modal, missing keyboard dismiss if inline.
