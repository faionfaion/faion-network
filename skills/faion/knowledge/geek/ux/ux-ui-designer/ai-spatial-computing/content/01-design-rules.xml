<?xml version="1.0" encoding="UTF-8"?>
<text id="01-design-rules" title="Spatial UI Design Rules">
  <summary>Concrete rules for gaze targets, depth layers, world anchoring, and graceful degradation in spatial UX.</summary>

  <section title="Gaze Targets">
    <rule>
      <statement>Define minimum gaze target size as 2 degrees of visual angle (approximately 35px at 1m on a typical HMD).</statement>
      <rationale>Targets smaller than 2 degrees cause selection errors and eye strain. This is the platform-consistent minimum across visionOS, OpenXR, and Meta Quest guidelines.</rationale>
    </rule>
  </section>

  <section title="Depth Layers">
    <rule>
      <statement>Assign UI to one of three depth zones: near (0.5m), mid (1–2m), far (4m+). Never mix reading distances within a single panel.</statement>
      <rationale>Vergence-accommodation conflict causes discomfort when text at different focal distances appears in the same panel. Each zone has distinct legibility requirements and interaction models.</rationale>
    </rule>
    <example type="good">
      <note>Correct depth zone assignments by content type:</note>
      <code lang="text"><![CDATA[
Near (0.5m)  → Action panels, input fields, notifications
Mid (1–2m)   → Main content panels, data displays
Far (4m+)    → Ambient information, environmental context
      ]]></code>
    </example>
  </section>

  <section title="World Anchoring">
    <rule>
      <statement>Anchor persistent UI to the world (not the user's head) by default.</statement>
      <rationale>Head-locked UI causes nausea in most users within minutes. World-anchored UI remains stable while the user moves, which is the expected behavior in spatial environments.</rationale>
    </rule>
    <antipattern>
      <description>Proposing head-locked UI panels for persistent content display.</description>
      <reason>Head-locked UI follows every head movement and induces nausea. Agents tend to propose head-locked layouts because they are simpler to specify — always flag and reject.</reason>
    </antipattern>
  </section>

  <section title="Graceful Degradation">
    <rule>
      <statement>Design an explicit fallback for every AI-driven spatial adaptation. AI will fail; the fallback must produce acceptable UX without AI.</statement>
      <rationale>AI scene understanding misclassifies rooms under poor lighting or cluttered environments. Voice integration fails in noisy public spaces. Gesture prediction degrades with non-standard postures. A spatial UI without fallbacks becomes unusable under these conditions.</rationale>
    </rule>
    <rule>
      <statement>Separate spatial layout from AI logic: layout specs must work without AI; AI adds enhancement only.</statement>
      <rationale>AI-dependent layout creates a single point of failure. Base layout must be functional; AI state is an enhancement layer.</rationale>
    </rule>
    <rule>
      <statement>Include explicit confidence thresholds in all scene classification rules. AI outputs are probabilistic, not deterministic.</statement>
      <rationale>Agents tend to produce hard scene-type mappings without confidence gates; low-confidence classifications applied without thresholds produce erratic UI behavior.</rationale>
    </rule>
  </section>
</text>
