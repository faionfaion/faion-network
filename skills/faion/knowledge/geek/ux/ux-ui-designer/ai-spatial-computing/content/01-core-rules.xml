<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="ai-spatial-computing Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for ai-spatial-computing. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Define minimum gaze target size as 2 degrees of visual angle (approximately 35px at 1m on a typical HMD).</statement>
      <rationale source="v1-source">Targets smaller than 2 degrees cause selection errors and eye strain. This is the platform-consistent minimum across visionOS, OpenXR, and Meta Quest guidelines.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Assign UI to one of three depth zones: near (0.5m), mid (1–2m), far (4m+). Never mix reading distances within a single panel.</statement>
      <rationale source="v1-source">Vergence-accommodation conflict causes discomfort when text at different focal distances appears in the same panel. Each zone has distinct legibility requirements and interaction models.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Anchor persistent UI to the world (not the user's head) by default.</statement>
      <rationale source="v1-source">Head-locked UI causes nausea in most users within minutes. World-anchored UI remains stable while the user moves, which is the expected behavior in spatial environments.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Design an explicit fallback for every AI-driven spatial adaptation. AI will fail; the fallback must produce acceptable UX without AI.</statement>
      <rationale source="v1-source">AI scene understanding misclassifies rooms under poor lighting or cluttered environments. Voice integration fails in noisy public spaces. Gesture prediction degrades with non-standard postures. A spatial UI without fallbacks becomes unusable under these conditions.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Separate spatial layout from AI logic: layout specs must work without AI; AI adds enhancement only.</statement>
      <rationale source="v1-source">AI-dependent layout creates a single point of failure. Base layout must be functional; AI state is an enhancement layer.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Include explicit confidence thresholds in all scene classification rules. AI outputs are probabilistic, not deterministic.</statement>
      <rationale source="v1-source">Agents tend to produce hard scene-type mappings without confidence gates; low-confidence classifications applied without thresholds produce erratic UI behavior.</rationale>
    </rule>
  </section>
</text>
