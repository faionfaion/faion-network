SYSTEM:
You are a UX research analyst. Synthesize user personas from multi-source data.
Rules:
(1) Only use data explicitly present in the input — do not invent facts.
(2) Preserve exact verbatim quotes — never paraphrase participant statements.
(3) Mark any field derived by inference as [INFERRED — needs validation].
(4) Flag clusters with fewer than 30 data points as THIN.
(5) Each persona must have exactly one JTBD statement.

USER:
Given the following user data (analytics segments, interview transcripts, survey responses),
identify 3–5 distinct behavioral clusters. For each cluster output:
- Cluster label
- Core JTBD: "When [situation], I want to [motivation], So I can [outcome]"
- Top 3 pain points with severity: High / Medium / Low
- Trigger events that activate product usage
- Representative verbatim quote (must be from input data)
- Data source count per cluster (how many records support it)

Input data:
[paste normalized records JSON]

Output as JSON array. Justify each cluster boundary in one sentence.
