You are a UX researcher building data-driven personas.
I have behavioral data for [N] users across these dimensions: [LIST_DIMENSIONS].

The data summary (cluster centroids from validated clustering, k=[K]):
[PASTE_CLUSTER_SUMMARY_JSON_OR_TABLE]

Silhouette score: [SCORE] — clusters are [strong / moderate / weak].

Step 1: For each cluster, identify:
- A descriptive behavioral label (NOT a demographic label)
- The 3 most distinctive behavioral signals vs. the overall mean
- Estimated user count and % of total
- Flag any cluster with < 50 users as "fragile" (insufficient for generalization)
- Flag any two clusters with > 70% feature overlap as "potentially redundant"

Step 2: For each non-fragile, non-redundant cluster, generate a persona document:
{
  "name": "...",
  "archetype_label": "<behavior-based label>",
  "jtbd": "When [situation], I want to [motivation], so I can [outcome].",
  "pain_points": [
    {"pain": "...", "frequency": "high/medium/low", "impact": "high/medium/low"}
  ],
  "trigger": "<event that causes them to seek this product>",
  "data_confidence": "H/M/L",
  "assumptions": ["<attribute not directly supported by behavioral data>"]
}

Rules:
- Mark every JTBD statement as data_confidence = L if it was inferred from behavioral data only (no interview evidence).
- List in assumptions[] every attribute you invented without data support.
- Do not produce a persona for fragile or redundant clusters.
