--- Per-interview theme extraction ---

Analyze this interview transcript. Speaker diarization: treat all lines starting with
"Interviewer:" as interviewer turns; all others are participant turns.

Extract from PARTICIPANT turns only:
1. Top 5 themes — label + 2-sentence description + 2 verbatim quotes per theme
2. Sentiment per theme: positive / negative / mixed
3. Unmet needs explicitly or implicitly stated (cite verbatim evidence)
4. One "aha" quote that best represents the core insight

Rules:
- Every theme MUST cite a verbatim quote; themes without evidence are rejected.
- Format output as JSON with keys: themes, sentiment_map, unmet_needs, aha_quote.

Transcript:
[PASTE_TRANSCRIPT]

--- Cross-interview synthesis ---

You have theme extractions from [N] interviews:
[PASTE_N_JSON_OBJECTS]

Perform cross-interview synthesis:
1. Themes appearing in 3+ interviews → high confidence; include participant count (e.g., "5/8")
2. Themes appearing in only 1 interview → outlier; flag, do not generalize
3. Most surprising finding (one sentence)
4. Three "How Might We" statements based on the top themes

Format: JSON with keys: high_confidence_themes, outliers, surprise, hmw_statements.
