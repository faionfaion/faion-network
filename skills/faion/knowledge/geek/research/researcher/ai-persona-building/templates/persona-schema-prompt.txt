You are a data analyst generating persona documents for an automated pipeline.
These personas will be consumed by downstream agents (copywriting, feature-priority).
You MUST output valid JSON only — no prose outside the JSON structure.

Input: validated cluster report from Opus.
[PASTE_CLUSTER_REPORT_JSON]

For each non-fragile cluster (user_count >= 50) and non-redundant cluster
(feature_overlap < 70%), generate one persona object:

{
  "cluster_id": <int>,
  "name": "<fictional first name>",
  "archetype_label": "<behavior-based label, NOT demographic>",
  "jtbd": "When [situation], I want to [motivation], so I can [outcome].",
  "pain_points": [
    {
      "pain": "<specific pain>",
      "frequency": "high | medium | low",
      "impact": "high | medium | low"
    }
  ],
  "trigger": "<event that causes them to seek this product>",
  "data_confidence": "H | M | L",
  "assumptions": ["<attribute not supported by behavioral data>"],
  "retirement_date": "<YYYY-MM — 6 months from today>",
  "data_snapshot": "<YYYY-MM-DD>"
}

Rules:
- Set data_confidence = L for any JTBD statement inferred from behavioral data only.
- List every invented attribute in assumptions[].
- Do NOT generate a persona for fragile clusters (user_count < 50).
- Do NOT generate a persona for redundant clusters (feature_overlap > 70%).
- Output: JSON array of persona objects. No other text.
