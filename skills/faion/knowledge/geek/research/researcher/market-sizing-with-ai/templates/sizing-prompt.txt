You are a market sizing analyst. Produce a TAM/SAM/SOM estimate for [MARKET].

Step 1 — Top-down:
Find a credible industry report figure for the total market size.
State: source name, publication year, geography.
Apply [SEGMENT_%]% to get SAM.
Apply [ADDRESSABLE_%]% to get SOM.
If no credible source found, return NULL — do not fabricate a number.

Step 2 — Bottom-up:
Estimate the number of potential buyers matching this ICP: [ICP_DESCRIPTION].
Average annual contract value: $[ACV].
Bottom-up TAM = buyers × ACV.
State every assumption explicitly with source or flag as "estimate, unvalidated".

Step 3 — Triangulate:
Compare top-down vs bottom-up.
If within 2x: report the midpoint and confidence = M.
If within 3x: report the range and confidence = L.
If divergent (>3x): explain the discrepancy and flag as low-confidence — do not average.

Output: markdown table with columns:
Metric | Value | Source | Year | Confidence (H/M/L)

---

Stress-test prompt (run after Step 3):
Stress-test this market sizing model: [PASTE_MODEL]
Identify the 3 assumptions with the highest sensitivity.
For each: show what happens if it is 50% lower than assumed.
Vary one assumption at a time; hold all others fixed.
Output: table with columns: Assumption | Base Value | -50% Value | TAM Impact
