<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agent-workflow" title="Synthetic Users Agent Workflow">
  <summary>Profile generation and response simulation pipeline with bias detection, gap analysis, and mandatory human checkpoints.</summary>

  <section title="Pipeline">
    <rule>
      <statement>Generate at least 5-8 profiles to detect uniformity bias; fewer profiles amplify the LLM's default persona and reduce signal diversity.</statement>
      <rationale>With fewer than 5 profiles, the model's prior dominates and all responses converge on the same stereotypical answers, which appear as "strong signal" but are actually LLM defaults.</rationale>
    </rule>

    <rule>
      <statement>Use adversarial prompting ("as a skeptical user, why would you NOT buy this?") to counteract the positive-response bias of synthetic profiles.</statement>
      <rationale>Standard prompts elicit cooperative, positive synthetic responses. Adversarial framing forces the model to generate the friction signals that matter for product decisions.</rationale>
    </rule>

    <rule>
      <statement>Flag topics where all profiles agree as low signal; flag topics with genuine divergence as high signal worth real-user follow-up.</statement>
      <rationale>Uniform synthetic agreement means the LLM defaulted to a consensus answer; divergence indicates a real dimension of variation in the ICP worth investigating.</rationale>
    </rule>

    <rule>
      <statement>Do not chain synthetic-user output directly into downstream agents (copy generation, pricing models) without human review.</statement>
      <rationale>Hallucinated preferences propagate through the pipeline and corrupt downstream outputs in ways that are hard to trace back to the synthetic source.</rationale>
    </rule>
  </section>

  <section title="Gotchas">
    <rule>
      <statement>When running simulated interviews at scale (50+ profiles), use small batches with explicit diversity constraints; diversity decreases as batch size grows.</statement>
      <rationale>At scale, the LLM regresses to a mean persona and response diversity collapses. Smaller batches with forced variation constraints preserve signal quality.</rationale>
    </rule>

    <rule>
      <statement>Inject forced variation in the profile construction prompt to prevent all profiles from sharing the same frustrations.</statement>
      <rationale>Without forced variation, profiles converge on common UX tropes ("too complex," "not enough integrations") that reflect LLM training data, not the actual ICP.</rationale>
    </rule>
  </section>

  <reference path="templates/synthetic-interview.py">Python: profile generation and simulated interview via Anthropic SDK</reference>
  <reference path="templates/profile-prompt.txt">Profile generation prompt with anti-uniformity constraints</reference>
</text>
