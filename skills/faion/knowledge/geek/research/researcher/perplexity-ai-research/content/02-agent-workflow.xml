<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agent-workflow" title="Perplexity Agent Workflow">
  <summary>Atomic sub-query decomposition pipeline: Sonnet decomposes, Haiku executes, Sonnet synthesizes with conflict resolution and confidence ratings.</summary>

  <section title="Pipeline">
    <rule>
      <statement>Decompose a research question into 3-5 atomic sub-queries before executing any Pro Search; each sub-query retrieves one fact with one expected source type.</statement>
      <rationale>Single-query compound research dilutes retrieval across all sub-questions. Atomic decomposition maximizes depth on each individual fact and makes citation validation tractable.</rationale>
    </rule>

    <rule>
      <statement>Always export citations and store them alongside the synthesized finding; the source is as important as the fact for downstream validation.</statement>
      <rationale>Without stored citations, findings cannot be audited, attributed, or updated when the source changes.</rationale>
    </rule>

    <rule>
      <statement>Assign confidence levels per synthesized claim: H = multiple independent sources agree; M = single credible source or partial agreement; L = single source or paywalled.</statement>
      <rationale>Confidence ratings force honest calibration and surface which claims need additional validation before external presentation.</rationale>
    </rule>

    <rule>
      <statement>Human review required for any Perplexity-sourced figure that will be presented externally; hallucination risk is lower than pure LLM but not zero.</statement>
      <rationale>Perplexity synthesizes from real sources but can misattribute, merge temporally distinct facts, or cite a source that supports only part of the synthesized claim.</rationale>
    </rule>
  </section>

  <section title="Gotchas">
    <rule>
      <statement>Flag paywalled citations as unverified; the agent cannot confirm whether the full article supports the claim.</statement>
      <rationale>A paywalled citation in the result list gives the appearance of source support without actual access to the source content.</rationale>
    </rule>

    <rule>
      <statement>Add exponential backoff and retry logic to any batch Perplexity API pipeline; rate limits on the Pro API are strict at lower tiers.</statement>
      <rationale>Batch research pipelines without retry logic fail silently on rate-limit errors and return incomplete results that appear complete.</rationale>
    </rule>
  </section>

  <reference path="templates/perplexity-research.py">Batch Pro Search script with citation extraction</reference>
  <reference path="templates/query-decompose-prompt.txt">Prompt for decomposing research questions into atomic Perplexity sub-queries</reference>
</text>
