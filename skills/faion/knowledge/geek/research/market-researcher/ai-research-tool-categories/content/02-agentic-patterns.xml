<?xml version="1.0" encoding="UTF-8"?>
<text id="02-agentic-patterns" title="Tool Selection Agentic Patterns">
  <summary>Prompt templates, CLI/SaaS table, and agent gotchas for tool selection and comparison workflows.</summary>

  <section title="Selection and Comparison Pattern">
    <rule>
      <statement>A Haiku agent handles classification: given a task description, map it to a phase and return the recommended tool set with rationale. A Sonnet agent handles comparison: evaluate 2-3 candidate tools against agent-friendliness, cost, and coverage.</statement>
      <rationale>Classification is a cheap mechanical task (Haiku); comparison requires structured reasoning across multiple criteria (Sonnet). Using Sonnet for classification wastes cost without quality benefit.</rationale>
    </rule>

    <example type="good">
      <code lang="xml"><![CDATA[
<tool_selection>
  <task>Competitive landscape sweep for AI note-taking tools, B2B segment, 2026</task>
  <phase>competitor-intel</phase>
  <constraints>
    <budget>mid</budget>
    <api_required>true</api_required>
  </constraints>
  <output>Ranked tool list with: tool name, phase fit, API available, cost tier, notes</output>
</tool_selection>
      ]]></code>
      <note>Constrains budget and API requirement explicitly. Prevents the agent from recommending enterprise-only tools when mid-tier is needed.</note>
    </example>

    <example type="good">
      <code lang="xml"><![CDATA[
<tool_comparison>
  <candidates>["Crayon", "Contify", "Klue"]</candidates>
  <criteria>["real-time updates", "API access", "NLP query support", "pricing"]</criteria>
  <output_format>markdown table</output_format>
</tool_comparison>
      ]]></code>
      <note>Structured comparison against explicit criteria. Agent cannot introduce criteria not in the list.</note>
    </example>

    <reference path="templates/tool-recommender.py">Haiku-based phase mapper returning recommended tool set with rationale</reference>
    <reference path="templates/tool-selection-prompt.txt">XML prompt for tool selection and comparison tasks</reference>
  </section>

  <section title="CLI and SaaS Reference">
    <list>
      <item>pytrends — Google Trends exploration phase (pip install pytrends)</item>
      <item>serpapi — Structured search results, exploration + competitor (pip install google-search-results)</item>
      <item>anthropic SDK — Classification and comparison reasoning (pip install anthropic)</item>
      <item>requests — Call Perplexity API, Contify webhooks (no install needed)</item>
    </list>

    <list>
      <item>Perplexity — REST API, exploration + synthesis</item>
      <item>Crayon — partial webhooks, competitor intel; requires manual competitor setup</item>
      <item>Klue — partial, competitor intel; better NLP than Crayon</item>
      <item>Contify — partial REST API, competitor + market news monitoring</item>
      <item>Looppanel — export only, interview analysis; no live query API</item>
      <item>Qualtrics — REST API, survey collection and analysis</item>
      <item>Brandwatch — partial REST, social sentiment; complex OAuth</item>
      <item>Synthetic Users — REST API, synthetic research for early validation</item>
      <item>Viewpoints.ai — REST API, simulated user panels</item>
      <item>Google Trends — free, pytrends, B2C proxy only</item>
    </list>
  </section>

  <section title="Agent Gotchas">
    <antipattern>
      <description>Agents recommend tools outside the approved list if not given a constrained tool registry.</description>
      <reason>Always pass the approved tools list explicitly in the prompt; agents have broad knowledge of tools and will default to popular ones regardless of approval status or API availability.</reason>
    </antipattern>

    <antipattern>
      <description>Interview-analysis tools require human-uploaded content; agents cannot conduct or ingest interviews without a preprocessing step.</description>
      <reason>Looppanel and Dovetail are transcript processors, not interview conductors. Agents must be told the transcript already exists before these tools are selected.</reason>
    </antipattern>

    <antipattern>
      <description>Synthetic research tools (Synthetic Users, Viewpoints.ai) produce biased outputs if the simulated persona pool is too narrow.</description>
      <reason>Agents must be given explicit demographic constraints before calling synthetic research tools; unconstrained calls produce results representing only the default persona.</reason>
    </antipattern>
  </section>
</text>
