<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Fitness Function Suite Bootstrap Core Rules" version="1.0.0" est_tokens="900" depth="essential">
  <summary>Testable rules every application of Fitness Function Suite Bootstrap must enforce. These are HARD rules — user-supplied input cannot override them.</summary>
  <section title="The rules">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every application produces a named artefact stored in a known location with a known schema. No artefact, no application.</statement>
      <rationale source="Operational discipline">un-stored work is un-done work.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Inputs to the method are explicitly listed with their source. Methods that 'use intuition' are rejected as un-auditable.</statement>
      <rationale source="Decision-traceability principle from audit / compliance literature">Decision-traceability principle from audit / compliance literature.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>The method's output has a named consumer and a documented decision it feeds. Outputs nobody uses are removed.</statement>
      <rationale source="Anti-orphan-artefact practice from kanban / lean ops">Anti-orphan-artefact practice from kanban / lean ops.</rationale>
    </rule>
  </section>
  <section title="Canonical examples">
    <example type="good" title="Anchored application">
      <code lang="text"><![CDATA[
Trigger: Architecture fitness-function check (weekly)
Artefact: structured output meeting the contract in content/02-output-contract.xml
Evidence: ≥1 named source per rule (URL, ticket, transcript_id)
Decision: downstream consumer can act without re-deriving reasoning
]]></code>
      <note>Demonstrates the closed loop the methodology creates — trigger → artefact → decision — with auditability at each step.</note>
    </example>
    <example type="bad" title="Un-anchored application">
      <code lang="text"><![CDATA[
Trigger: 'we should do this thing'
Artefact: prose paragraph, no schema
Evidence: 'industry best practice'
Decision: ambiguous — every reader interprets differently
]]></code>
      <note>Each ambiguity above is a failure mode catalogued in content/03-failure-modes.xml. The methodology exists to prevent exactly this shape.</note>
    </example>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
  </see-also>
</text>
