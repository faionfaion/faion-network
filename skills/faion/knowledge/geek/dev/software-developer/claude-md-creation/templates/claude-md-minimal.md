<!--
purpose: Minimal CLAUDE.md skeleton for a single-language repo.
consumes: Repo facts: commands, conventions.
produces: A project CLAUDE.md committed at the repo root.
depends-on: Fill the bracketed placeholders by hand.
token-budget-impact: ~250 tokens when fully filled.
-->
# [Project Name]

## Commands

```bash
npm run dev    # Development server
npm test       # Tests
npm run build  # Production build
npm run lint   # Lint + format
```

## Structure

- `src/` - Source code
- `tests/` - Test files
- `public/` - Static assets

## Conventions

- TypeScript strict mode
- ESLint + Prettier
- Conventional commits: `type: description`
