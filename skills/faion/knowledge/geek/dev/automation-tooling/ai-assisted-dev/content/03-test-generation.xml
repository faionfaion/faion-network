<?xml version="1.0" encoding="UTF-8"?>
<text id="03-test-generation" title="AI Test Generation Patterns">
  <summary>AI generates test stubs efficiently but requires human review of assertions for correctness — AI-generated tests can pass trivially without verifying actual behavior. Self-healing test tools reduce maintenance overhead but introduce non-determinism.</summary>

  <section title="Generation Workflow">
    <rule>
      <statement>Use AI to generate the first draft of tests; require human review of assertion logic before accepting. A test that calls assert result is not None proves nothing.</statement>
      <rationale>An agent generating tests and running them can certify its own incorrect output as passing. Green CI from AI-generated tests is not the same as meaningful test coverage. Human must spot-check a random subset for logical correctness.</rationale>
    </rule>
    <list>
      <item>Requirements: identify happy path, edge cases, error handling, integration boundaries.</item>
      <item>AI generates: test stubs with parametrize, fixtures, AAA structure.</item>
      <item>Human reviews: assertion logic is meaningful, mocks test behavior not implementation.</item>
      <item>Refinement: add missing scenarios identified during review.</item>
      <item>Execution: tests must pass in CI before commit.</item>
    </list>
  </section>

  <section title="Coverage Gap Analysis">
    <rule>
      <statement>After running coverage reports, prompt the AI with uncovered branches and complex conditions to generate targeted gap-filling tests.</statement>
      <rationale>AI generates tests from visible code structure, often missing branches that are only visible in coverage reports. Feeding the coverage gap back into the prompt produces higher-value tests than a generic "generate more tests" prompt.</rationale>
    </rule>
    <example type="good">
      <note>Coverage-driven gap fill prompt.</note>
      <code lang="text"><![CDATA[
Context: pytest, coverage report shows UserService.create_user() missing branch at line 47 (duplicate email path) and line 62 (missing required field).
Task: Generate two parametrized pytest tests covering these exact branches.
Output: pytest code only, no prose.
      ]]></code>
    </example>
  </section>

  <section title="Self-Healing Tests">
    <rule>
      <statement>Self-healing test tools (Virtuoso, mabl) that adapt to UI changes automatically require periodic human review to verify tests have not silently changed meaning.</statement>
      <rationale>Self-healing reduces maintenance overhead but introduces non-determinism: a test that previously verified button X behavior may silently adapt to verify button Y after a UI change. The intent of the test is lost.</rationale>
    </rule>
  </section>

  <reference path="templates/gen-tests.sh">Bash script calling Claude Code in --print mode to generate pytest stubs</reference>
</text>
