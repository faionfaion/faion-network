<!--
purpose: Work-breakdown-structure prompt template; output is a parsed WBS used by the decision record's integration fit score.
consumes: methodology inputs listed in AGENTS.md `## Prerequisites`
produces: prompt XML the methodology's task router sends to the model
depends-on: AGENTS.md `## Task Routing` for model selection per sub-task
token-budget-impact: <500 tokens prompt; output validated against content/02-output-contract.xml
-->
<task>Create a work breakdown structure for this project brief.</task>
<brief>{{project_brief}}</brief>
<team_size>{{n}}</team_size>
<output_format>
  JSON array of issues:
  [{ "title": "", "type": "epic|story|task|bug", "description": "",
     "acceptance_criteria": [], "dependencies": [], "labels": [] }]
</output_format>
<constraints>
  - Maximum 3 levels of nesting (epic -> story -> task)
  - Each task must have at least one acceptance criterion
  - Flag any dependencies between epics
  - Do not create more than 5 epics per brief
  - Do not call any PM tool API — return the JSON payload only
</constraints>
