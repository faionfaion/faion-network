<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="AI-Powered PM Tools (PM-Agile) — Output Contract" version="1.1.0" est_tokens="800" depth="essential">
  <summary>Pins the shape of the decision_record produced by ai-powered-pm-tools. Subagent must satisfy the schema below before returning.</summary>
  <section title="Required fields">
    <list>
      <item>incumbent (string)</item>
      <item>ai_tier_delta_pct (number)</item>
      <item>integration_fit_score (integer 0-4)</item>
      <item>roi_gate_threshold (number (cost-per-hour-saved))</item>
      <item>pilot_result (string)</item>
      <item>buyer (string (role:person))</item>
      <item>decision (string)</item>
    </list>
  </section>
  <section title="JSON Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "faion://ai-powered-pm-tools/output.schema.json",
  "type": "object",
  "required": ['incumbent', 'ai_tier_delta_pct', 'integration_fit_score', 'roi_gate_threshold', 'pilot_result', 'buyer', 'decision'],
  "properties": {
    "incumbent": {"type": "string"},
    "ai_tier_delta_pct": {"type": "number"},
    "integration_fit_score": {"type": "integer"},
    "roi_gate_threshold": {"type": "number"},
    "pilot_result": {"type": "string"},
    "buyer": {"type": "string"},
    "decision": {"type": "string"}
  }
}
]]></code>
  </section>
  <section title="Forbidden patterns">
    <antipattern>
      <description>required fields silently omitted; selection / record returned with empty body</description>
      <reason>Records without the required fields fail audit and renewal review.</reason>
    </antipattern>
    <antipattern>
      <description>numeric thresholds (price delta, ROI gate, integration score) replaced with prose ("looks great", "should be fine")</description>
      <reason>Prose thresholds cannot be compared across cycles; they decay to opinion.</reason>
    </antipattern>
    <antipattern>
      <description>buyer / reviewer field set to a team or channel rather than a named person</description>
      <reason>Anonymous decisions rot; nobody is accountable when the decision is later questioned.</reason>
    </antipattern>
  </section>
  <section title="Allowed transformations">
    <list>
      <item>Round numeric values to 2 significant figures for the executive summary; preserve precision in the JSON body.</item>
      <item>Translate buyer name to anonymised form for external audit copies; keep the full name in the internal record.</item>
    </list>
  </section>
  <section title="Valid example">
    <example type="good">
      <code lang="json"><![CDATA[
{ "incumbent": "example", "ai_tier_delta_pct": 1, "integration_fit_score": 1, "roi_gate_threshold": 1, "pilot_result": "example", "buyer": "example", "decision": "example" }
]]></code>
    </example>
  </section>
  <section title="Invalid example">
    <example type="bad">
      <code lang="json"><![CDATA[
{}
]]></code>
      <note>Empty object — every required field missing. Reject.</note>
    </example>
  </section>
  <section title="Self-check checklist">
    <list>
      <item>every required field is populated with a non-placeholder value</item>
      <item>numeric thresholds are numbers, not prose</item>
      <item>buyer / reviewer is a role:person, never a team</item>
      <item>artefact validates against the JSON Schema above</item>
    </list>
  </section>
</text>
