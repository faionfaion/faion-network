<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="MegaLinter as the Polyglot Quality Umbrella in CI Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">languages_count?</root-question>

    <branch when="languages_count &lt; 3">
      <conclusion ref="skip-this-methodology" verdict="skip — single-language"/>
    </branch>
    <branch when="languages_count &gt;= 3">
      <question id="q-2">ci_can_pull_docker?</question>
        <branch when="docker_pull == false">
          <conclusion ref="skip-this-methodology" verdict="skip — air-gapped"/>
        </branch>
        <branch when="docker_pull == true">
          <conclusion ref="single-ci-job" verdict="adopt MegaLinter"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="languages=5 → docker_pull=true → single-ci-job → MegaLinter all flavour + SARIF">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
