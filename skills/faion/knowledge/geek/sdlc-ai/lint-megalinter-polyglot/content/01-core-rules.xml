<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="lint-megalinter-polyglot Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for lint-megalinter-polyglot. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Polyglot repositories MUST configure MegaLinter as a CI job (oxsecurity/megalinter@v8 or pinned SHA) that runs on pull_request and push and uploads SARIF via github/codeql-action/upload-sarif. The same repository MUST NOT register MegaLinter inside .pre-commit-config.yaml, lefthook.yml, or .husky/pre-commit; pre-commit hooks MUST stay on the per-language native binaries (ruff, biome, hadolint, terraform fmt) so the local commit completes in under one second.</statement>
      <rationale source="v1-source">MegaLinter ships as a multi-gigabyte container and takes tens of seconds to start; running it on every commit makes humans use --no-verify and trains agents to do the same. Native tools at the hook give the same coverage on the staged diff in milliseconds, while the CI MegaLinter run ratchets up the gate with rules that need cross-file context (markdownlint, link-check, vale, gitleaks history scan).</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Repositories MUST select a MegaLinter flavor that matches the languages actually present in git ls-files. The selection MUST follow: pure Python → python; pure JS/TS → javascript; IaC-heavy (Terraform, K8s, Dockerfile) → terraform; mixed JS+Py+docs → cupcake; security-only quarterly scan → security. Repositories MUST NOT use the unflavored default image unless three or more flavor families apply.</statement>
      <rationale source="v1-source">The flavor changes pull time from minutes to seconds and runtime memory from gigabytes to megabytes. CI quotas, especially on GitHub free runners and on Hetzner self-hosted runners, are dominated by image pull; the flavor is the single biggest knob for keeping MegaLinter sustainable as a default gate.</rationale>
    </rule>
  </section>
</text>
