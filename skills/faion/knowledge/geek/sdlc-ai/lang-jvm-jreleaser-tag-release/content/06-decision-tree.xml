<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="JVM One-Tag Release via JReleaser Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">publishes_externally?</root-question>

    <branch when="publishes == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>
    <branch when="publishes == true">
      <question id="q-2">conventional_commits_adoptable?</question>
        <branch when="cc_adoptable == false">
          <conclusion ref="skip-this-methodology" verdict="skip until commits standardised"/>
        </branch>
        <branch when="cc_adoptable == true">
          <conclusion ref="single-tag-trigger" verdict="wire JReleaser + tag workflow"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="publishes=true → cc_adoptable=true → single-tag-trigger → jreleaser.yml + tag workflow">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
