<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="task-agent-drafts-spec-before-coding Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for task-agent-drafts-spec-before-coding. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>On ticket assignment the agent MUST post exactly one comment containing the four sections in this order: Current State, Desired State, Proposed Plan (INVEST-decomposed steps), Open Questions. The comment MUST mention the issue creator. The agent MUST NOT touch the source tree until the approval gate fires; any pre-approval write to a tracked file fails the lint and the cycle aborts.</statement>
      <rationale source="v1-source">Linear's own internal use of its agent and GitHub Copilot Workspace's spec→plan→diff→PR contract both treat the first-comment spec as the unit of human review; without it, reviewers must reconstruct intent from the diff, which is the exact cost the gate eliminates.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The agent MUST treat the gate as cleared only when an approval event originates from an identity NOT matching ^(claude|copilot|devin|cursor|rovo|duo|bot)-.*$ and is one of: Linear :thumbsup: reaction on the agent's spec comment, Jira issue comment matching ^/agent\s+approve\b, or GitHub issue labelled agent:approved by a user with write access. Self-approval, agent-to-agent approval, and approval before the spec comment is posted MUST be rejected.</statement>
      <rationale source="v1-source">Atlassian's "agents respect your permissions, project configurations, workflows, and audit trails" and GitHub's branch-protection-style separation of approver from author both encode the same invariant: an automated actor cannot satisfy a gate that exists to bound automation.</rationale>
    </rule>
  </section>
</text>
