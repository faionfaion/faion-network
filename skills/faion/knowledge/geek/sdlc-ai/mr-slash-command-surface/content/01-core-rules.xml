<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="mr-slash-command-surface Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for mr-slash-command-surface. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The bot configuration MUST set `auto_describe = true` and `auto_review = true` on PR open and on push events. `auto_improve` MUST be `false` — `/improve` MUST be opt-in by explicit comment. The `issue_comment` event MUST be wired so `/commands` posted as PR comments dispatch the matching bot action.</statement>
      <rationale source="v1-source">Auto-running describe and review covers the always-useful 80%; auto-running improve produces 5-15 suggestion patches per PR and trains reviewers to ignore the bot. Qodo Merge's own usage guide and CodeRabbit production studies converged on this default.</rationale>
    </rule>
  </section>
</text>
