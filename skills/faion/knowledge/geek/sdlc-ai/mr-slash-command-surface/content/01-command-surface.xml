<?xml version="1.0" encoding="UTF-8"?>
<text id="01-command-surface" title="Slash-Command Vocabulary, Auto-Trigger Policy, Idempotent Output Block">
  <summary>Standardize the slash-command vocabulary across review bots, auto-run only the cheap-and-useful commands, and isolate bot output behind HTML comments so re-runs don't clobber human edits.</summary>

  <section title="Standard command vocabulary">
    <p>Every PR-review bot worth installing exposes the same four commands. Pinning the names makes the steering surface portable across vendors.</p>

    <list>
      <item>`/describe` — regenerate PR title and walkthrough from the diff.</item>
      <item>`/review` — inline issues plus a structured summary (security, perf, tests, complexity).</item>
      <item>`/improve` — post code-suggestion patches reviewers can apply one-click.</item>
      <item>`/ask &lt;question&gt;` — free-form question against the diff plus repo context.</item>
    </list>

    <rule testable="true">
      <statement>The bot configuration MUST set `auto_describe = true` and `auto_review = true` on PR open and on push events. `auto_improve` MUST be `false` — `/improve` MUST be opt-in by explicit comment. The `issue_comment` event MUST be wired so `/commands` posted as PR comments dispatch the matching bot action.</statement>
      <rationale>Auto-running describe and review covers the always-useful 80%; auto-running improve produces 5-15 suggestion patches per PR and trains reviewers to ignore the bot. Qodo Merge's own usage guide and CodeRabbit production studies converged on this default.</rationale>
    </rule>

    <example type="good">
      <code lang="yaml"><![CDATA[
on:
  pull_request: { types: [opened, reopened, ready_for_review, synchronize] }
  issue_comment: { types: [created] }
jobs:
  pr_agent:
    runs-on: ubuntu-latest
    steps:
      - uses: qodo-ai/pr-agent@main
        env:
          GITHUB_ACTION_CONFIG.AUTO_DESCRIBE: "true"
          GITHUB_ACTION_CONFIG.AUTO_REVIEW: "true"
          GITHUB_ACTION_CONFIG.AUTO_IMPROVE: "false"
      ]]></code>
      <note>describe + review are always-on; improve waits for explicit `/improve`.</note>
    </example>

    <example type="bad">
      <code lang="yaml"><![CDATA[
on: { pull_request: [opened] }     # no issue_comment
jobs:
  pr_agent:
    steps:
      - env: { GITHUB_ACTION_CONFIG.AUTO_IMPROVE: "true" }   # noisy
      ]]></code>
      <note>No `issue_comment` event — slash commands silently fail. Auto-improve floods the PR with suggestion patches.</note>
    </example>

    <antipattern>
      <description>Letting the bot rewrite the entire PR body on every `/describe` re-run.</description>
      <reason>Human-edited reviewer notes are erased; reviewers stop adding context because it disappears.</reason>
    </antipattern>

    <reference path="templates/qodo-merge.yml">Working Qodo Merge workflow with auto-describe/auto-review.</reference>
    <reference path="templates/pr-description-block.md">HTML-comment-delimited block that survives bot re-runs.</reference>
    <reference href="https://qodo-merge-docs.qodo.ai/tools/review/">Qodo Merge `/review` and `/describe` documentation.</reference>
    <reference href="https://docs.qodo.ai/qodo-documentation/qodo-merge/pr-agent/usage-guide/automations_and_usage">Qodo Merge automations and usage guide.</reference>
    <reference href="https://github.com/qodo-ai/pr-agent">qodo-ai/pr-agent — open-source PR-Agent.</reference>
    <reference href="https://docs.coderabbit.ai">CodeRabbit `@coderabbitai` chat and slash commands.</reference>
    <reference href="https://docs.sourcery.ai/Code-Review/Overview/">Sourcery code-review surface.</reference>
  </section>
</text>
