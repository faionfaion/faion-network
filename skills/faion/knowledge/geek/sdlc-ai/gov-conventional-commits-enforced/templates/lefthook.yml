# purpose: lefthook commit-msg hook wiring commitlint
# consumes: commit message
# produces: pass/fail
# depends-on: commitlint.config.cjs
# token-budget-impact: ~100 tokens
commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}
