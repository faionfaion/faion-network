<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="sec-codeql-autofix-on-pr Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for sec-codeql-autofix-on-pr. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every protected branch MUST list the CodeQL check name (for example `CodeQL / Analyze (javascript-typescript)`) as a required status check, and the CodeQL workflow MUST run on `pull_request` for every language matrix entry. A PR introducing one or more new CodeQL alerts MUST fail the check until the alert is dismissed-with-reason or fixed in the same PR.</statement>
      <rationale source="v1-source">If the check is not required, a PR can be merged with red alerts and the gate is theatre. GitHub's own Advanced Security guidance ties the merge decision to the SARIF diff between base and head — that diff has to be the source of truth for the bot, the human reviewer, and the agent.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>An agent or human MUST treat each Copilot Autofix suggestion as a draft patch: read the alert's data-flow explanation, run the test suite locally with the patch applied, and only then apply via "Commit suggestion" or rewrite. Autofix patches MUST NOT be auto-applied by a bot, and MUST NOT be merged without at least one non-author approval.</statement>
      <rationale source="v1-source">Autofix achieves more than two-thirds correctness on common alert types but the remaining third either misses a constraint (locale, framework version, custom wrapper) or shifts the bug rather than fixing it; the reviewable-suggestion contract keeps the human or agent in the loop while still cutting median time-to-fix by roughly five times.</rationale>
    </rule>
  </section>
</text>
