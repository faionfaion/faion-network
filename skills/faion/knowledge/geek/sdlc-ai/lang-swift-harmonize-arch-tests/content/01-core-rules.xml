<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="lang-swift-harmonize-arch-tests Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for lang-swift-harmonize-arch-tests. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every Swift module with internal layering MUST ship a Tests/Architecture/ target containing at least one Harmonize test per layer boundary. CI MUST run swift test --filter Architecture (or include the target in the default scheme) on every PR; failures block merge. Agents are forbidden from suppressing a Harmonize test by adding the offending file to a Harmonize excludedPaths list — fix the import instead.</statement>
      <rationale source="v1-source">Architecture in a README is invisible to an LLM iterating on a fix; an architecture test fails the agent's tool-use loop with a specific symbol and file, which is the only signal an autonomous agent reliably reacts to.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every Swift project's CI MUST run, in order: swift-format lint --strict --recursive Sources Tests, swiftlint --strict, swift test (which executes Harmonize + unit + integration tests via the ST-0021 bridge). All four steps MUST be green for merge. Agents are forbidden from passing --quiet or removing --strict to silence findings.</statement>
      <rationale source="v1-source">A single command shape (swift test) for both unit and architecture tests means the agent's loop has one signal to interpret; mixing xcodebuild and swift test introduces format differences that LLMs misparse.</rationale>
    </rule>
  </section>
</text>
