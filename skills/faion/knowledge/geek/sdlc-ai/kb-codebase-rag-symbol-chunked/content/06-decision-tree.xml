<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Codebase RAG with Symbol-Boundary Chunking Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">repo_size_KLOC?</root-question>

    <branch when="repo_size_KLOC &lt; 5">
      <conclusion ref="skip-this-methodology" verdict="use Read directly"/>
    </branch>
    <branch when="repo_size_KLOC &gt;= 5">
      <question id="q-2">ast_parser_available_for_dominant_lang?</question>
        <branch when="ast_parser == false">
          <conclusion ref="fallback-line-chunk" verdict="use line-window fallback"/>
        </branch>
        <branch when="ast_parser == true">
          <conclusion ref="chunk-per-symbol" verdict="AST chunker"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="repo=120 KLOC → ast_parser=true (tree-sitter Python+TS) → chunk-per-symbol → padding 25% → qdrant">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
