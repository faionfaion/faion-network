<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="test-property-based-llm-invariants Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for test-property-based-llm-invariants. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The agent MUST classify each proposed property under exactly one of: `round-trip` (`f(g(x)) == x`), `idempotence` (`f(f(x)) == f(x)`), `oracle-equivalence` (`f(x) == oracle(x)`), `metamorphic` (`f(t(x))` relates to `f(x)` by some `R`), or `invariant-on-output` (`P(f(x))` holds for all valid `x`). Any property the agent cannot label MUST be rejected and not committed.</statement>
      <rationale source="v1-source">A closed rubric (a) makes review tractable, (b) forces the agent to think about the function's algebraic structure rather than its current implementation, (c) matches the categories Hypothesis/fast-check/proptest already shrink against well.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every committed property MUST carry a one-line comment of the form `# grounded: &lt;quoted source fragment&gt;` immediately above the `@given` (or `fc.assert` / `proptest!`) decorator. Properties with no quotable source MUST be deleted, not weakened. Code review MUST reject any property whose grounding comment does not appear in the source under test.</statement>
      <rationale source="v1-source">The grounding comment makes review O(seconds) per property and turns the "specification gap" into a hard error: if the agent cannot quote a source line, the spec is silent on the invariant and the property is the agent's hallucination, not the function's contract.</rationale>
    </rule>
  </section>
</text>
