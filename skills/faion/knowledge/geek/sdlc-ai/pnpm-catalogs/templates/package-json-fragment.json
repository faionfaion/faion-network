{
  "__faion_header__": {
    "purpose": "Per-package package.json fragment referencing catalog: and workspace:*",
    "consumes": "default catalog + named catalogs from pnpm-workspace.yaml",
    "produces": "config (package.json) matching content/02-output-contract.xml schema",
    "depends-on": "content/01-core-rules.xml (shared-dep-catalog-only, internal-workspace-link)",
    "token-budget-impact": "low — ~150 tokens when loaded as context"
  },
  "name": "@org/web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "react": "catalog:",
    "react-dom": "catalog:",
    "@org/core": "workspace:*",
    "@org/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:"
  }
}
