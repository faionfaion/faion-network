<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="mr-codemod-refactor-agent Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for mr-codemod-refactor-agent. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>For any cross-cutting refactor touching ≥20 call-sites, the agent MUST first run an AST codemod (jscodeshift, ts-morph, libcst, semgrep, codemod.com / Hypermod) and stage its output as a separate commit. The LLM tail-pass MUST run only after the codemod commit and MUST be limited to files the codemod could not safely edit (template literals, Markdown, comments, fixtures). The PR MUST contain ONE logical refactor; multiple unrelated renames MUST be split into separate PRs.</statement>
      <rationale source="v1-source">codemod.com, Martin Fowler's "Codemods: API Refactoring Tooling", and Vercel's AI SDK 5→6 migration explicitly converged on this split because the LLM-only path produced inconsistent renames at scale. The "one logical group" rule keeps the diff reviewable and the codemod re-runnable.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Cross-cutting refactor PRs MUST be produced by a three-role pipeline: (a) Architect agent emits a written migration plan listing files to touch and out-of-scope items; (b) Migration agent writes a re-runnable codemod and runs it; (c) Validator agent re-runs the codemod on the produced branch and asserts `git diff --quiet`. The Validator MUST run inside CI, not only locally.</statement>
      <rationale source="v1-source">The idempotence assertion (`re-run produces no diff`) catches every failure mode: a missed call-site, a wrong AST matcher, a hand-edit that broke determinism. Augment Code's multi-file refactor study and OpenAI's sandboxed migration cookbook both adopt this pipeline.</rationale>
    </rule>
  </section>
</text>
