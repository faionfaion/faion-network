<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Versioned Agent-Memory Files (decisions / patterns / mistakes) Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">project_duration?</root-question>

    <branch when="duration == one_off">
      <conclusion ref="skip-this-methodology" verdict="no need"/>
    </branch>
    <branch when="duration == ongoing">
      <question id="q-2">team_accepts_committed_ai_artefacts?</question>
        <branch when="team_accepts == false">
          <conclusion ref="skip-this-methodology" verdict="skip"/>
        </branch>
        <branch when="team_accepts == true">
          <conclusion ref="four-file-bundle" verdict="scaffold bundle"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="duration=ongoing → team_accepts=true → four-file-bundle → scaffold + wire agent load">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
