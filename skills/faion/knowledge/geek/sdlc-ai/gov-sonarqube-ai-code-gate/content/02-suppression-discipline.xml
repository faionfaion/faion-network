<?xml version="1.0" encoding="UTF-8"?>
<text id="02-suppression-discipline" title="Suppression Discipline Around the AI Gate">
  <summary>Allow inline suppressions only with a justification comment and a sunset date; never override the gate at the project or PR level.</summary>

  <section title="Narrow, justified, time-boxed suppressions only">
    <p>Sonar findings can be suppressed with `// NOSONAR` / `# NOSONAR` or by marking the issue "Won't fix" / "False positive" in the UI. AI agents will reach for the cheapest path to green; without discipline they suppress real findings instead of fixing them.</p>

    <rule testable="true">
      <statement>Inline `NOSONAR` / `# NOSONAR` suppressions MUST appear on the same line as the offending statement and MUST carry a trailing `reason=&lt;short text&gt; sunset=YYYY-MM-DD` comment. UI-level "Won't fix" / "False positive" transitions MUST be performed by a non-agent identity. Project-level `sonar.issue.ignore.*` rules require a CODEOWNER review and a linked tracker issue with a closure date. Disabling the AI gate, lowering its thresholds, or marking PRs "exempt" is forbidden.</statement>
      <rationale>Without a structured suppression contract the agent silently learns that suppression is the cheapest fix; with one, every dismissal becomes a reviewable, expiring artifact.</rationale>
    </rule>

    <example type="good">
      <code lang="python"><![CDATA[
# Acceptable: narrow, justified, dated.
secret = os.environ["LEGACY_TOKEN"]  # NOSONAR reason=migrating-to-vault sunset=2026-09-01
      ]]></code>
      <note>Reviewer can grep for `sunset=` and re-open expired suppressions automatically.</note>
    </example>

    <example type="bad">
      <code lang="yaml"><![CDATA[
# sonar-project.properties
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=python:S2068
sonar.issue.ignore.multicriteria.e1.resourceKey=**/*.py
      ]]></code>
      <note>Project-wide rule disable hides the entire class of findings without a CODEOWNER review or a sunset.</note>
    </example>

    <antipattern>
      <description>Adding `// NOSONAR` with no reason text so the agent can ship a clean PR before a deadline.</description>
      <reason>Untraceable suppressions accumulate; the AI defect class returns to the codebase and the gate becomes cosmetic.</reason>
    </antipattern>

    <reference href="https://docs.sonarsource.com/sonarqube-server/latest/user-guide/issues/#issue-resolution">SonarQube issue resolution and suppression docs.</reference>
    <reference href="https://blog.codacy.com/sonarqube-alternatives">Tooling comparison (Codacy / Code Climate fallbacks).</reference>
  </section>
</text>
