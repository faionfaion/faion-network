<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="test-consumer-contract-from-spec Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for test-consumer-contract-from-spec. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Consumer Pact files MUST be produced by a generator (PactFlow MCP, Pact AI skill, or equivalent) that reads OpenAPI / AsyncAPI / recorded traffic. The generator command MUST be committed (Makefile target, npm script, or pact:generate skill). Hand-edits to pacts/*.json MUST fail CI via a checksum or "regenerate to verify" step.</statement>
      <rationale source="v1-source">PactFlow reports up to 60% creation-time reduction and deterministic output when the spec is the input. Hand edits silently mask provider regressions and break can-i-deploy.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The provider repository's release pipeline MUST run pact-broker can-i-deploy --pacticipant &lt;provider&gt; --version $GIT_SHA --to-environment &lt;env&gt; as a required step. Deployment MUST be blocked when any consumer contract for that environment fails verification. The check MUST run before tag/push to the production registry, not after.</statement>
      <rationale source="v1-source">Without can-i-deploy, broker verification becomes informational only. The Pact ecosystem's central guarantee — "no provider release breaks a deployed consumer" — depends on this single gate.</rationale>
    </rule>
  </section>
</text>
