<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="uv-lockfile-floor Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for uv-lockfile-floor. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Commit uv.lock to the repo. Run installs with `uv sync --frozen` in CI and `uv sync` locally; mutate dependencies only via `uv add` / `uv remove` / `uv lock --upgrade`.</statement>
      <rationale source="v1-source">Reproducible installs require both the manifest (pyproject.toml) and the resolved transitive set (uv.lock). Committing the lock is the only way two agents on two machines compute the same env.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Never invoke `pytest`, `python`, `mypy`, `ruff`, or any other project tool bare. Always wrap in `uv run &lt;cmd&gt;`; for global tooling, use `uv tool install` / `uv tool run`.</statement>
      <rationale source="v1-source">uv re-verifies the lockfile against pyproject.toml on every `uv run`. A bare `pytest` may pick up a stale system-Python interpreter and report a false green.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>In CI, install with `uv sync --frozen --no-dev` (or `--all-extras` per matrix), never `uv sync --upgrade`.</statement>
      <rationale source="v1-source">`--frozen` fails the build if pyproject.toml and uv.lock disagree. Without it, CI quietly relocks and you lose the auditability of the committed lockfile.</rationale>
    </rule>
  </section>
</text>
