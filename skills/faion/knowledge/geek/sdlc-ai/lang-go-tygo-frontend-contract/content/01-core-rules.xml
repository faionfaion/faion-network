<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="lang-go-tygo-frontend-contract Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for lang-go-tygo-frontend-contract. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>API DTOs MUST live in Go and be generated to TypeScript via tygo generate -config tygo.yaml. The generated .ts file MUST be committed. CI MUST run tygo generate &amp;&amp; git diff --exit-code &lt;output-path&gt; and fail the PR on any diff. Frontend code MUST import only from the generated file; hand-written duplicate interfaces are forbidden.</statement>
      <rationale source="v1-source">Mechanical generation eliminates transcription error; the diff gate eliminates "forgot to regenerate" drift. Both failure modes are then deterministic CI signals an agent can self-correct on.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The tygo.yaml type_mappings block MUST cover at minimum: time.Time → "string", uuid.UUID → "string", json.RawMessage → "unknown", plus any project-internal newtypes. CI fails if tygo generate produces any in the output (grep -nE ': any[;,]' &lt;out&gt; &amp;&amp; exit 1).</statement>
      <rationale source="v1-source">Untyped any defeats the contract; making any a CI failure forces the developer (or agent) to add the mapping rather than punt to runtime.</rationale>
    </rule>
  </section>
</text>
