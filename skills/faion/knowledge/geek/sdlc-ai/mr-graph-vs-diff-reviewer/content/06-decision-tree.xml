<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Graph-Indexed vs Diff-Only AI Reviewers Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">wants_ai_review?</root-question>

    <branch when="wants_ai == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>
    <branch when="wants_ai == true">
      <question id="q-2">repo_size_KLOC?</question>
        <branch when="size_KLOC &lt; 30">
          <conclusion ref="graph-when-large-repo" verdict="lean diff-only"/>
        </branch>
        <branch when="size_KLOC &gt;= 100">
          <conclusion ref="graph-when-large-repo" verdict="lean graph-indexed"/>
        </branch>
        <branch when="30 &lt;= size_KLOC &lt; 100">
          <conclusion ref="evaluate-both-architectures" verdict="evaluate both"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="wants_ai=true → size=150 KLOC → graph-when-large-repo → evaluate Greptile + CodeRabbit → ADR picks Greptile">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
