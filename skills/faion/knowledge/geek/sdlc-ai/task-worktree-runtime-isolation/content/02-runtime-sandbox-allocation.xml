<?xml version="1.0" encoding="UTF-8"?>
<text id="02-runtime-sandbox-allocation" title="Per-worktree port, DB, cache, and secret allocation">
  <summary>Worktree isolation does not isolate runtime; the harness must allocate non-overlapping ports, database names, cache namespaces, and secret profiles per worktree.</summary>

  <section title="Runtime sandbox per worktree">
    <p>Two agents in two worktrees both running `npm run dev` will both ask for port 3000. Two agents both running `pytest` against `DATABASE_URL=postgres:///app_test` will both truncate the same tables. Filesystem isolation does not extend to OS-level resources; the harness has to allocate them explicitly.</p>

    <rule testable="true">
      <statement>For every dispatched worktree the harness MUST emit a per-worktree env file containing PORT, DATABASE_URL (with a worktree-unique database name), REDIS_URL (with a unique key prefix or DB index), and SECRETS_PROFILE (a 1Password / vault profile scoped to this worktree). Ports MUST come from a tracked range (e.g. 4000-4999) with a free-port allocator; DB names MUST encode the worktree slug; cache prefixes MUST include the slug. The agent MUST source this env file before any local service starts.</statement>
      <rationale>This is the difference between "filesystem isolation" and "runtime isolation". Without per-worktree allocation, parallel test suites step on each other's data, parallel dev servers fail to bind, and parallel CI jobs hold the same external rate limits. The penligent.ai 2026 report names this as the dominant production failure mode for worktree-based parallel agents.</rationale>
    </rule>

    <example type="good">
      <code lang="bash"><![CDATA[
# .wt-env emitted by spawn function
PORT=4023
DATABASE_URL=postgres://dev:dev@localhost:5432/app_wt_billing
REDIS_URL=redis://localhost:6379/3        # DB index 3 reserved for this slug
SECRETS_PROFILE=op://dev/wt-billing
# agent boots: source .wt-env && pnpm dev
      ]]></code>
      <note>Each worktree has a deterministic, conflict-free runtime profile.</note>
    </example>

    <example type="bad">
      <code lang="bash"><![CDATA[
# All worktrees inherit the parent shell env
export DATABASE_URL=postgres://localhost/app_test
export PORT=3000
# Three concurrent agents, three concurrent test runs:
# wt-a TRUNCATE users; wt-b INSERT; wt-c SELECT — random failures.
      ]]></code>
      <note>Shared resources, hidden cross-talk, intermittent test failures the agent cannot reproduce.</note>
    </example>

    <antipattern>
      <description>Allocating ports randomly per run instead of deterministically per slug, so a re-attached worktree gets a different port and the running dev server is orphaned.</description>
      <reason>Determinism per slug is what makes the worktree resumable across crashes and restarts; randomness leaks dangling processes that bind ports the next agent expects.</reason>
    </antipattern>

    <reference path="templates/wt-env.example">Per-worktree env template (PORT, DATABASE_URL, REDIS_URL, SECRETS_PROFILE).</reference>
    <reference href="https://www.penligent.ai/hackinglabs/git-worktrees-need-runtime-isolation-for-parallel-ai-agent-development/">Penligent: git worktrees need runtime isolation.</reference>
    <reference href="https://docs.bswen.com/blog/2026-03-18-ai-agent-worktree-isolation/">bswen 2026-03 worktree isolation field notes.</reference>
  </section>
</text>
