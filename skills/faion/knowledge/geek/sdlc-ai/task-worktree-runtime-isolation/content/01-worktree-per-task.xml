<?xml version="1.0" encoding="UTF-8"?>
<text id="01-worktree-per-task" title="The 1:1:1:1 mapping for parallel agents">
  <summary>Each agent run binds to exactly one task, one branch, one worktree, and one file-scope manifest; conflicts move from edit-time to merge-time.</summary>

  <section title="One-to-one binding">
    <p>Concurrent agents writing into the same working tree hit two failure modes: (a) git index-lock contention when two agents stage simultaneously, and (b) silent overwrite when one agent's diff lands on top of another's because they both edited the same file. Worktrees solve (a) outright (each worktree has its own index) and let (b) surface as a normal merge conflict — visible, not silent.</p>

    <rule testable="true">
      <statement>The harness MUST create one git worktree per dispatched agent, on a unique branch named after the task slug. Each worktree MUST carry a file-scope manifest (e.g. `WT-SCOPE`) listing the path globs the agent is allowed to edit; any Edit/Write outside the manifest aborts the agent. The orchestrator MUST NOT dispatch two agents into the same worktree, and MUST NOT reuse a worktree across tasks.</statement>
      <rationale>Without the manifest, the agent silently broadens scope and the merge conflict surfaces as "agent A and agent B both edited unrelated files in the same worktree" — equivalent to the no-worktree case. Without the unique branch, `git worktree add` refuses (a branch can be checked out by only one worktree at a time), so the contract is enforced by git itself.</rationale>
    </rule>

    <example type="good">
      <code lang="bash"><![CDATA[
# orchestrator dispatches feat-billing
git worktree add ../wt-feat-billing feat-billing
echo "apps/billing/**" > ../wt-feat-billing/WT-SCOPE
echo "tests/billing/**" >> ../wt-feat-billing/WT-SCOPE
claude --cwd ../wt-feat-billing --scope-file WT-SCOPE \
  --task "implement billing v2"
      ]]></code>
      <note>Agent boots in its own worktree, on its own branch, with its scope file declared.</note>
    </example>

    <example type="bad">
      <code lang="bash"><![CDATA[
# Two agents in the same checkout, no manifest
( cd repo && claude --task "billing v2" ) &
( cd repo && claude --task "auth refactor" ) &
wait
# Index lock errors, silent overwrites in shared files (settings.py).
      ]]></code>
      <note>No worktree, no scope manifest — concurrent edits collide.</note>
    </example>

    <antipattern>
      <description>Reusing the same worktree across sequential tasks "to save the venv install" without resetting branch / scope.</description>
      <reason>Branch state from task N leaks into task N+1; the next merge contains accidental commits from the prior task and the scope file is stale.</reason>
    </antipattern>

    <reference path="templates/wt-spawn.sh">Reusable spawn function that wires worktree + branch + scope.</reference>
    <reference href="https://www.augmentcode.com/guides/git-worktrees-parallel-ai-agent-execution">Augment Code: git worktrees for parallel AI agent execution.</reference>
    <reference href="https://devcenter.upsun.com/posts/git-worktrees-for-parallel-ai-coding-agents/">Upsun: worktrees for parallel AI coding agents.</reference>
  </section>
</text>
