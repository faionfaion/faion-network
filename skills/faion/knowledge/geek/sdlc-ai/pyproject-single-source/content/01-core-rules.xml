<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="pyproject-single-source Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for pyproject-single-source. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The repo contains zero of: `setup.py` (unless required for native build steps), `setup.cfg`, `.flake8`, `pytest.ini`, `tox.ini`, `mypy.ini`, `requirements.txt` (for first-party deps). All replaced by sections inside `pyproject.toml`.</statement>
      <rationale source="v1-source">PEP 621 provides the standard metadata table; PEP 518 provides the build-system table. Every modern Python tool reads `pyproject.toml`. Multiple config files create silent conflicts an agent will miss when editing one and forgetting another.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>When a repo uses pytest + ruff + coverage + a type checker, `pyproject.toml` MUST contain `[tool.pytest.ini_options]`, `[tool.ruff]`, `[tool.coverage.run]`, and exactly one of `[tool.ty]` / `[tool.mypy]` / `[tool.pyright]`. Missing sections mean the tool is reading either defaults or a legacy file.</statement>
      <rationale source="v1-source">If a tool runs without its TOML section present, it is either using global defaults (silent risk) or an undeclared config file (drift). Both are caught by checking the table is present.</rationale>
    </rule>
  </section>
</text>
