<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="lint-shellcheck-hadolint-iac-floor Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for lint-shellcheck-hadolint-iac-floor. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The pre-commit config of every repo containing the matching files MUST include: `shellcheck` on every `*.sh` and bash-shebanged file (-S style minimum), `hadolint` on every `Dockerfile*` (default ruleset), `yamllint` on every `.yaml`/`.yml` (with at minimum the `relaxed` profile), `actionlint` on every `.github/workflows/*.yml`, and `tflint --recursive` plus `terraform validate` on every Terraform module. CI MUST run the whole-tree variant of each tool on PR open. Findings of severity `error` or higher MUST block both the local commit and the merge check.</statement>
      <rationale source="v1-source">This mapping is the intersection of the OpenSSF Scorecard "infrastructure-as-code" metric, GitHub's own actionlint deployment guide, and the Docker best-practices catalog hadolint encodes. Skipping a tier (e.g., "we'll add shellcheck later") leaves a known class of bug — Shellshock-style quoting, rootful Docker images, action-injection workflows — uncaught at the cheapest gate.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>An AI agent that triggers a `shellcheck`, `hadolint`, `yamllint`, `actionlint`, or `tflint` finding MUST modify the offending file to satisfy the rule. The agent MUST NOT add `# shellcheck disable=...`, `# hadolint ignore=...`, `# yamllint disable-line`, `# actionlint shellcheck`-disable, or `tflint:disable` UNLESS: (a) the suppression cites the specific rule code, (b) a one-line justification is included in the same diff, and (c) a tracking issue (URL or ticket ID) is named in the comment for sunset. Blanket file-level disables are forbidden.</statement>
      <rationale source="v1-source">Disable comments accumulate forever and turn the linter advisory. The Anthropic Claude Code "constitution-driven" pattern and faion-net's own feedback_granular_commits.md memory both treat suppression with sunset tracking as the contract — an issue anchors the future cleanup. Without that anchor, the comment is technical debt with no cost owner.</rationale>
    </rule>
  </section>
</text>
