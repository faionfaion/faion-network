<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Infrastructure-as-Code Lint Floor (shellcheck, hadolint, yamllint, actionlint, tflint) Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">has_iac_artefacts?</root-question>

    <branch when="has_iac == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>
    <branch when="has_iac == true">
      <question id="q-2">which_linter_per_format?</question>
        <branch when="format == &apos;shell&apos;">
          <conclusion ref="per-format-linter" verdict="add shellcheck"/>
        </branch>
        <branch when="format == &apos;docker&apos;">
          <conclusion ref="per-format-linter" verdict="add hadolint"/>
        </branch>
        <branch when="format == &apos;gh-actions&apos;">
          <conclusion ref="per-format-linter" verdict="add actionlint"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="has_iac=true (bash + Dockerfile + actions) → per-format-linter → shellcheck + hadolint + actionlint">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
