# Pre-Commit Hooks as the Non-Negotiable Merge Floor

## Summary

Every repository with code MUST install a pre-commit framework that runs format, lint, secret-scan and type-check hooks before a commit is created — never afterward. The framework is `pre-commit` (Python, broadest plug-in ecosystem) for polyglot repositories, `lefthook` (Go, parallel) for monorepos with throughput pressure, or `husky` (Node) for pure JavaScript stacks. AI agents NEVER bypass with `--no-verify`; on any hook failure they read the output, fix the root cause, and re-stage. The list of installed hooks is documented in the repository `AGENTS.md` so the agent sees the contract before it writes the first line.

## Why

Pre-commit hooks are the cheapest deterministic gate available: they run on the developer's machine in sub-second time on a diff, turning "did you remember to lint?" into a structural impossibility instead of a discipline question. Lefthook is roughly ten times faster than husky for the same hook set because it is a single Go binary running hooks in parallel, and `pre-commit` (pre-commit.com) ships hundreds of pre-built hooks (ruff, black, gitleaks, detect-secrets, eslint, prettier, terraform fmt, shellcheck) with isolated environments per hook so adding a check is a one-line YAML edit. Skipping the hook with `--no-verify` is the dominant agent failure mode, because it breaks the entire downstream chain (CI, reviewers, autogenerated changelogs).

## When To Use

- Every repository that contains code, including throwaway prototypes that may grow.
- Polyglot repositories where multiple language linters need consistent invocation (`pre-commit` or `lefthook`).
- Monorepos with parallel hook execution requirements (`lefthook`).
- Teams using AI coding agents — the hook list is the agent's compile-time contract.

## When NOT To Use

- Single-file gist repositories with no merge workflow — overhead exceeds the benefit.
- Read-only or archived repositories — no commits arrive that the hook could gate.
- Documentation-only repositories with no executable artifacts — a markdownlint hook still helps but the framework choice is overkill if there is one file.

## Content

| File | What's inside |
|------|---------------|
| `content/01-hook-floor.xml` | The "every repo installs a pre-commit framework, hooks block format/lint/secret/type failures, agents never `--no-verify`" rule and the rationale tied to AI agent failure modes. |
| `content/02-framework-choice.xml` | Decision rule for `pre-commit` vs `lefthook` vs `husky` based on stack, throughput and parallelism needs. |

## Templates

| File | Purpose |
|------|---------|
| `templates/.pre-commit-config.yaml` | Minimal polyglot pre-commit config with ruff, gitleaks, prettier and a typecheck stage. |
| `templates/lefthook.yml` | Equivalent lefthook config showing parallel execution and stage selection. |
