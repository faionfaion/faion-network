<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Pre-Commit Linters MUST Run on Staged Files Only Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">whole_tree_lint_seconds?</root-question>

    <branch when="whole_tree_s &lt; 1">
      <conclusion ref="skip-this-methodology" verdict="skip — overhead negligible"/>
    </branch>
    <branch when="whole_tree_s &gt;= 1">
      <question id="q-2">linter_accepts_paths?</question>
        <branch when="accepts == false">
          <conclusion ref="skip-this-methodology" verdict="skip — replace linter first"/>
        </branch>
        <branch when="accepts == true">
          <conclusion ref="staged-files-only" verdict="switch to staged filter"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="whole_tree_s=12 → linter_accepts=true → staged-files-only → lefthook glob filter + nightly whole-tree CI">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
