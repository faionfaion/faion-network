<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Internal RFC Template Output Contract" version="1.0.0" est_tokens="700" depth="essential">
  <summary>Pins the shape of a valid Internal RFC Template output. Subagent must satisfy this before returning to the caller.</summary>

  <output-contract>
    <required>
      <field name="rfc_id" type="string" required="true">RFC-NNNN sequential</field>
      <field name="status" type="string" required="true">Draft|Discussion|Accepted|Rejected|Superseded</field>
      <field name="sections" type="object" required="true">{motivation, proposal, alternatives, open_questions}</field>
      <field name="comment_window" type="object" required="true">{opened_at, closes_at, decision_date}</field>
      <field name="superseded_by" type="string" required="true">RFC-NNNN if applicable</field>
      <field name="decider" type="string" required="true">named decider or committee</field>
      <field name="owner" type="string" required="true">RFC author</field>
    </required>

    <forbidden>
      <pattern id="f1">sections.length &gt; 7</pattern>
      <pattern id="f2">status missing OR not in the 5 canonical values</pattern>
      <pattern id="f3">comment_window &lt; 7 days</pattern>
      <pattern id="f4">rejected RFC deleted instead of archived</pattern>
    </forbidden>

    <allowed-transformations>
      <transformation>Trim narrative fields to ≤500 chars preserving named entities + numeric claims.</transformation>
      <transformation>Promote a 'no decision yet' state to status='pending' rather than fabricating a decision.</transformation>
    </allowed-transformations>

    <schema-reference path="templates/internal-rfc-template.json"/>
  </output-contract>

  <section title="Self-check checklist">
    <list>
      <item>rfc_id populated with a typed value</item>
      <item>status populated with a typed value</item>
      <item>sections populated with a typed value</item>
      <item>comment_window populated with a typed value</item>
      <item>superseded_by populated with a typed value</item>
      <item>decider populated with a typed value</item>
      <item>owner populated with a typed value</item>
      <item>No forbidden pattern matches the output</item>
      <item>Transformations applied where applicable</item>
    </list>
  </section>
</text>