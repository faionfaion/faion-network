Draft a spec.md and implementation-plan.md for this feature.

<task>Generate spec.md and implementation-plan.md</task>
<feature>{{feature_name}}</feature>
<intent>{{one_paragraph_description_of_what_it_should_do}}</intent>
<constraints>
  <technical>{{stack, existing APIs, DB schema constraints}}</technical>
  <business>{{scope limits, regulatory requirements}}</business>
  <out_of_scope>{{explicit exclusions — required}}</out_of_scope>
</constraints>

<output_format>
  spec.md:
  - FR list (FR-1..N) with descriptions
  - Edge cases with expected behavior
  - Given-When-Then acceptance criteria for each FR
  - Dependencies (internal systems, external APIs)
  - Explicit out-of-scope section
  - Assumptions list

  implementation-plan.md:
  - Phase breakdown (2-4 phases)
  - Task list with FR coverage
  - Token estimates (NOT time estimates)
  - Dependency order between tasks
  - Quality gates per phase

  NOTE: Use token estimates only. No time estimates.
  NOTE: Do not create implementation tasks until spec is human-approved.
  NOTE: List all assumptions made due to missing information.
</output_format>
