Generate a specification for the following feature.

<context>
  <product>{{product_description}}</product>
  <users>{{user_personas}}</users>
  <constraints>{{technical_and_business_constraints}}</constraints>
  <intent>{{feature_intent}}</intent>
  <out_of_scope>{{explicit_exclusions}}</out_of_scope>
  <existing_patterns>{{related_specs_or_code_patterns}}</existing_patterns>
</context>

<requirements>
  1. List functional requirements in FR-X format (FR-1, FR-2, ...)
  2. Identify and document edge cases with expected behavior
  3. Write acceptance criteria in Given-When-Then format for each FR
  4. List dependencies (internal systems, external APIs, data sources)
  5. List explicitly what is OUT OF SCOPE
  6. Flag all assumptions made due to missing information
  7. Do NOT proceed to implementation planning
</requirements>

<format>Use the standard spec.md template: metadata, overview, user stories, functional requirements, NFRs, edge cases, dependencies, risks, assumptions.</format>
