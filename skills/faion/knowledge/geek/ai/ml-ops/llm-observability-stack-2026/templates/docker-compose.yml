# purpose: Self-host Langfuse + Phoenix compose
# consumes: domain knowledge inputs (see Prerequisites)
# produces: filled-in config matching 02-output-contract schema
# depends-on: 01-core-rules.xml constraints
# token-budget-impact: ~150 tokens loaded as context

# docker-compose for llm-observability-stack-2026 self-host skeleton
version: '3.8'
services:
  app:
    image: example/llm-observability-stack-2026:latest
    environment:
      - LOG_LEVEL=info
    ports:
      - '8080:8080'
