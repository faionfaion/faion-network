<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="agents-framework-selection Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for agents-framework-selection. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>New production agent systems MUST default to LangGraph unless the primary use case is explicitly role-based multi-agent (CrewAI) or conversational debate/validation (AutoGen). LangGraph's state machine model, persistence layer, and SARIF-compatible tracing are the most mature for production debugging as of 2026.</statement>
      <rationale source="v1-source">LangGraph provides explicit state schemas (TypedDict), checkpointing for crash recovery, conditional edge routing, and LangSmith tracing out of the box. These properties are hard to retrofit onto simpler frameworks as production requirements grow.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every production agent deployment MUST integrate at least one observability platform (LangSmith or Langfuse) from day one. Logging every tool call and LLM call with arguments and results is not optional — it is the only mechanism for debugging non-deterministic agent failures after the fact.</statement>
      <rationale source="v1-source">Agent failures are non-deterministic and often occur only in production with real data. Without traces, debugging requires reproducing the exact input sequence, which is often impossible. Traces provide the full tool-call-result-state chain needed to diagnose failures post-hoc.</rationale>
    </rule>
  </section>
</text>
