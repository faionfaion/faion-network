<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="gemini-api Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for gemini-api. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Default to Gemini 3 Flash for new production apps; escalate to Gemini 3 Pro only when benchmark quality matters.</statement>
      <rationale source="v1-source">Gemini 3 Flash is fast, supports dynamic thinking, and costs the same as 2.0 Flash ($0.10/$0.40 per 1M tokens). Pro adds latency and cost with marginal quality gains on most tasks.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>For Gemini 3: use simpler prompts and temperature 1.0 (default) — do not set thinking_budget manually unless profiling shows it is needed.</statement>
      <rationale source="v1-source">Gemini 3 reasoning is adaptive. Over-specified prompts and low temperature fight the model's built-in planning. Let thinking_level=high handle complex tasks; only override when cost/latency is a hard constraint.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Use context caching for any prompt prefix &gt;32K tokens that is reused across requests — it cuts input cost by 75%.</statement>
      <rationale source="v1-source">Context caching stores a fixed prefix (system prompt + large document) on Google's infrastructure. Subsequent calls only pay for the incremental tokens. At 1M-token contexts, this transforms cost economics.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Always use start_chat() for multi-turn conversations — never manually append history to generate_content().</statement>
      <rationale source="v1-source">start_chat() manages history state automatically including thought signatures (Gemini 3 requirement). Manual history management misses thought tokens and breaks multi-turn coherence.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Pass Python functions directly as tools — Gemini SDK auto-generates the schema from type hints and docstrings.</statement>
      <rationale source="v1-source">Auto-schema generation eliminates manual JSON Schema maintenance. Docstring Args: sections become parameter descriptions. This is Gemini's native pattern and is more reliable than manual FunctionDeclaration objects.</rationale>
    </rule>
  </section>
</text>
