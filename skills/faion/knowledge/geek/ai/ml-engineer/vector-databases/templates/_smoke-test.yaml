# purpose: minimum-viable vector-db.yaml fixture
# consumes: nothing
# produces: spec for --self-test
# depends-on: vector-db.schema.yaml
# token-budget-impact: 0
kind: qdrant
connection:
  host: qdrant.internal
  port: 6333
collection:
  name: support-kb
  dim: 1024
  metric: cosine
  embedding_model: {name: voyage-3-large, version: "2026-04"}
multi_tenant:
  enabled: true
  filter_field: tenant_id
hybrid_search:
  enabled: true
  fusion: rrf
