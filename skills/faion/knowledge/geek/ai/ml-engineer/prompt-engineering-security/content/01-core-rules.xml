<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="prompt-engineering-security Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for prompt-engineering-security. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>User-supplied text MUST be wrapped in explicit delimiters in every LLM call. For Claude, use XML tags (user_input). For GPT-4, use markdown fenced sections or labeled headers. The system prompt MUST explicitly state that instructions within user_input tags do not override system instructions.</statement>
      <rationale source="v1-source">Without delimiters, the model cannot distinguish instructions from data. XML tags signal a semantic boundary that well-trained models respect, reducing (not eliminating) injection success rate.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Input sanitization MUST: (1) truncate inputs exceeding the maximum allowed length, (2) filter known injection pattern strings, (3) log warnings for filtered inputs. Sanitization MUST NOT be the sole defense — it MUST be combined with delimiter isolation.</statement>
      <rationale source="v1-source">Pattern-based filters catch known injection strings but are bypassable via encoding or paraphrasing. They are valuable as a signal (log warnings) and as friction, not as a complete barrier.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>System prompts for user-facing applications MUST include: (1) explicit scope definition, (2) instruction to ignore override attempts from user input, (3) a default fallback response for out-of-scope or suspicious requests. System prompts MUST NOT be revealed to users even if asked.</statement>
      <rationale source="v1-source">Explicit constraints in the system prompt reduce the model's tendency to comply with injection attempts. Fallback responses prevent the model from freezing or hallucinating when confused by adversarial input.</rationale>
    </rule>
  </section>
</text>
