<?xml version="1.0" encoding="UTF-8"?>
<text id="02-pipeline" title="Image Generation Pipeline and API Gotchas">
  <summary>Three-stage agentic pipeline (prompt-engineer → image-generator → quality-checker), API-specific gotchas, and production best practices.</summary>

  <section title="Agentic Pipeline">
    <rule>
      <statement>Use a three-stage pipeline: prompt-engineer subagent transforms brief → image-generator selects model and calls API → quality-checker VLM verifies output matches requirements.</statement>
      <rationale>Separating concerns prevents the generator from making prompt quality decisions and the prompt engineer from handling API errors. Each stage has a clear, testable responsibility.</rationale>
    </rule>
    <p>Quality-checker uses a vision model (Claude or GPT-4o) to verify subject present, style correct, no obvious artifacts. On failure, loop retries with revised prompt up to 3 times before escalating.</p>
    <reference path="templates/prompt-engineer.txt">Prompt to transform a content brief into a structured generation prompt</reference>
    <reference path="templates/generate-with-retry.py">DALL-E 3 batch generation with content-policy retry and URL expiry handling</reference>
  </section>

  <section title="API Gotchas">
    <antipattern>
      <description>Storing OpenAI image URLs without immediately downloading the image.</description>
      <reason>Image URLs from the OpenAI API expire after 1 hour. An agent that logs the URL for later retrieval will receive a 404 when the pipeline processes it.</reason>
    </antipattern>
    <antipattern>
      <description>Asserting the generated image matches the input prompt without checking `revised_prompt`.</description>
      <reason>DALL-E 3 silently modifies prompts for safety. The `revised_prompt` field shows what was actually generated; quality checks must be based on this, not the original prompt.</reason>
    </antipattern>
    <antipattern>
      <description>Submitting batch generation jobs without rate limit throttling.</description>
      <reason>DALL-E 3 is rate-limited to 5 images/minute on Tier 1. Agent batch jobs without throttling exhaust the rate limit in the first batch and stall all subsequent requests.</reason>
    </antipattern>
    <rule>
      <statement>Pre-filter prompts through a content-policy checker before hitting the API; rejected requests still consume latency and may count against rate limits.</statement>
      <rationale>Content policy rejections are unpredictable on borderline prompts. Pre-filtering with a lightweight classifier reduces API 400 errors and improves pipeline throughput.</rationale>
    </rule>
  </section>

  <section title="Best Practices">
    <list>
      <item>Log `revised_prompt` from DALL-E 3 alongside the output URL — essential for debugging drift between requested and generated images</item>
      <item>Use negative prompts in Stable Diffusion (`blurry, deformed, ugly, watermark`) to filter common artifacts; DALL-E API has no negative prompt support</item>
      <item>For brand style consistency, use Flux Kontext with a reference image rather than prompt keywords alone</item>
      <item>For regulated industries (HIPAA/SOC2), use AWS Bedrock + Stability AI SD 3.5 Enterprise</item>
      <item>Human approval checkpoint: for paid advertising channels, require human review before publishing — automated quality checks miss brand guideline violations</item>
    </list>
  </section>
</text>
