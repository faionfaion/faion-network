<?xml version="1.0" encoding="UTF-8"?>
<text id="02-rules" title="Chunking Rules, Metrics, and Failure Modes">
  <summary>Concrete rules for chunk size, overlap, metadata, evaluation, and the most common failure modes in RAG ingestion pipelines.</summary>

  <section title="Size and Overlap Rules">
    <rule>
      <statement>Default chunk size: 512 tokens. Rule of thumb: chunk_size = model_context / (top_k * 2). For 32K context with top_k=5: 512 tokens/chunk.</statement>
      <rationale>Chunk size must leave room for the question and generated answer in the context window after top_k chunks are concatenated.</rationale>
    </rule>

    <rule>
      <statement>Default overlap: 15% (77 tokens for 512-token chunks). Increase to 25% only for dense technical content. Set to 0 for naturally complete units (functions, sections).</statement>
      <rationale>15% overlap is empirically optimal for most corpora. Overlap &gt;25% causes duplicate content in top-k results, cluttering the LLM context with the same text multiple times.</rationale>
    </rule>
  </section>

  <section title="Metadata Rules">
    <rule>
      <statement>Attach metadata to every chunk: source filename, page number, section header, chunk index, total_chunks, token_count. Never omit source path.</statement>
      <rationale>Without source metadata, citations in RAG responses are wrong. Metadata enables filtering (e.g., retrieve only from a specific document) and is essential for debugging retrieval failures.</rationale>
    </rule>

    <example type="good">
      <code lang="python"><![CDATA[
chunk_metadata = {
    "chunk_id": "doc_001_chunk_003",
    "source": "document_name.pdf",
    "page": 5,
    "section": "Introduction",
    "header_path": "Chapter 1 > Overview > Background",
    "chunk_index": 3,
    "total_chunks": 25,
    "token_count": 512,
}
      ]]></code>
    </example>
  </section>

  <section title="Evaluation Targets">
    <rule>
      <statement>Evaluate chunking on a held-out query set (50+ representative queries) before committing to a strategy. Target: Precision@5 &gt; 0.7, Recall@K &gt; 0.8 for critical applications.</statement>
      <rationale>Chunking assumptions about your corpus are almost always wrong without measurement. Defaults that work for articles may fail for PDFs or code.</rationale>
    </rule>

    <list>
      <item>Precision@K: relevant chunks in top-K / K — target &gt; 0.7</item>
      <item>MRR: average of 1/rank of first relevant result — target &gt; 0.6</item>
      <item>NDCG@10: ranking quality with relevance scores — target &gt; 0.7</item>
      <item>Precision@5 &lt; 0.5: strategy too coarse; many empty chunks: parsing issue</item>
    </list>
  </section>

  <section title="Failure Modes">
    <antipattern>
      <description>Metadata loss during chunking: chunker drops source file path or page number.</description>
      <reason>Citations in the final RAG response will be wrong. Always verify metadata propagation through the full ingestion pipeline before indexing.</reason>
    </antipattern>

    <antipattern>
      <description>Overlap causes duplicate retrieval: top-k=20 returns 5 overlapping windows of the same paragraph.</description>
      <reason>LLM receives 5x the same content; responses are padded with repetition rather than diverse evidence. Deduplicate by source range before passing to reranker.</reason>
    </antipattern>

    <antipattern>
      <description>Semantic chunking embedding model mismatch: using text-embedding-3-small for chunking but text-embedding-3-large for retrieval.</description>
      <reason>Similarity thresholds calibrated in one vector space are meaningless in another. Use the same model for both ingestion and retrieval.</reason>
    </antipattern>

    <antipattern>
      <description>Rebuilding without re-chunking: changing chunk strategy but leaving old chunks in the same index.</description>
      <reason>Old and new chunks in the same index cause inconsistent retrieval behavior. Always rebuild the full index after changing chunking strategy.</reason>
    </antipattern>
  </section>

  <reference path="templates/chunker-dispatcher.py">Document-type-aware chunker dispatcher</reference>
  <reference path="templates/chunking-config.yaml">YAML configuration template for all strategies</reference>
</text>
