    <?xml version="1.0" encoding="UTF-8"?>
    <text id="06-decision-tree" title="Agents Safety Guardrails — Decision Tree" version="1.1.0" est_tokens="400">
      <decision-tree>
        <root-question id="q-root">Does the agent have at least one tool with irreversible side-effects?</root-question>
        <branch when="has_irreversible_tools == true and human_loop_configured == true">
  <conclusion ref="r4-human-loop-irreversible">Confirm trip conditions + recovery paths for all five categories.</conclusion>
</branch>
<branch when="has_irreversible_tools == true and human_loop_configured == false">
  <conclusion ref="r4-human-loop-irreversible">Block production; wire human-in-loop gate first.</conclusion>
</branch>
<branch when="has_irreversible_tools == false">
  <conclusion ref="r5-cost-cap">Skip irreversible category; still configure cost + execution caps.</conclusion>
</branch>
        <example trace="has_irreversible_tools == true and human_loop_configured == true">
          <p>Confirm trip conditions + recovery paths for all five categories.</p>
        </example>
      </decision-tree>
    </text>
