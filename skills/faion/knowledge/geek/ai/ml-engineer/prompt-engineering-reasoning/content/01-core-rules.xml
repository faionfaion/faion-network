<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Prompt Engineering — Reasoning Patterns — Core Rules" version="1.0.0" est_tokens="900" depth="essential">
  <summary>Five testable rules that gate any application of the prompt-engineering-reasoning methodology. Sources: OpenAI o1 / o3 reasoning model docs; Anthropic extended-thinking documentation; Google Gemini thinking models docs.</summary>

  <section title="Rule set">
    <rule id="r1-when-using-cot" testable="true" depth="essential">
      <statement>When using CoT, MUST separate reasoning from the final answer using explicit XML tags or labeled sections. The final answer section MUST contain only the answer, not restated reasoning. The task MUST benefit from sequential reasoning (math, logic, multi-step) before applying CoT.</statement>
      <rationale source="OpenAI o1 / o3 reasoning model docs">Mixing reasoning and answer in one block causes the model to commit early. Separation allows the model to complete all reasoning before generating the final answer, reducing premature commitment errors.</rationale>
    </rule>

    <rule id="r2-self-consistency-only" testable="true" depth="essential">
      <statement>Self-consistency MUST only be used when output reliability is more important than latency and cost. MUST use at least 3 independent reasoning paths. MUST NOT be used in real-time or latency-sensitive pipelines.</statement>
      <rationale source="Anthropic extended-thinking documentation">Self-consistency trades compute for reliability. Three samples provide a majority vote; fewer than three cannot establish a clear consensus.</rationale>
    </rule>

    <rule id="r3-critique-revise-include" testable="true" depth="essential">
      <statement>Critique-and-revise MUST include three labeled sections: initial response, critique (strengths + weaknesses + missing elements), and final response. The final response MUST address the critique, not merely restate the initial response.</statement>
      <rationale source="Google Gemini thinking models docs">Without explicit critique structure, models skip self-correction and repeat the initial response. The three-section format forces genuine improvement.</rationale>
    </rule>

    <rule id="r4-validate-produced-playbook" testable="true" depth="essential">
      <statement>Validate that the produced `playbook-step` matches the schema in `content/02-output-contract.xml` before handing off (rule r4).</statement>
      <rationale source="Wei et al. — Chain-of-Thought (arXiv:2201.11903)">Mechanical validation prevents downstream consumers from receiving malformed artefacts; the validator script enforces this at PR time.</rationale>
    </rule>

    <rule id="r5-validate-produced-playbook" testable="true" depth="essential">
      <statement>Validate that the produced `playbook-step` matches the schema in `content/02-output-contract.xml` before handing off (rule r5).</statement>
      <rationale source="OpenAI o1 / o3 reasoning model docs">Mechanical validation prevents downstream consumers from receiving malformed artefacts; the validator script enforces this at PR time.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
