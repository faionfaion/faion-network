<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="rag-evaluation Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for rag-evaluation. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Require stable document IDs before computing retrieval metrics; if your vector DB returns raw chunks, compute IDs as hash(content).</statement>
      <rationale source="v1-source">Precision@K and Recall@K require matching retrieved doc IDs against relevance labels. Without IDs the calculation is impossible.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Do not conflate RAGAS Faithfulness (grounded in retrieved context) with factual accuracy (correct against world knowledge) — a faithful answer can still be factually wrong if the retrieved context is wrong.</statement>
      <rationale source="v1-source">These measure different failure modes that require different fixes: bad retrieval vs hallucinating generation.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Separate retrieval evaluation from generation evaluation in every run; a combined score masks root cause and misdirects fixes.</statement>
      <rationale source="v1-source">Retrieval and generation have independent failure modes. A high faithfulness score with low hit rate means the model hallucinates consistently from bad retrieval — not a generation problem.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Target these baselines before production: Faithfulness &gt; 0.85, Answer Relevance &gt; 0.90, Hit Rate &gt; 0.90.</statement>
      <rationale source="v1-source">These thresholds represent consistent observed quality in production RAG systems across diverse domains.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Generate synthetic ground truth (question-answer pairs) with a different model than the one being evaluated.</statement>
      <rationale source="v1-source">Using the same model to generate and evaluate creates self-fulfilling evaluations — the evaluated model scores artificially high on its own synthetic questions.</rationale>
    </rule>
  </section>
</text>
