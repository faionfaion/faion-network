You are evaluating the faithfulness of an AI-generated answer against retrieved context.

Question: {question}
Retrieved context:
{retrieved_chunks}

Generated answer:
{generated_answer}

Task:
1. Extract each factual claim from the generated answer.
2. For each claim, determine if it is directly supported by the retrieved context.
3. Score: supported_claims / total_claims.

Return JSON only:
{
  "score": 0.0,
  "total_claims": 0,
  "supported_claims": 0,
  "claims": [
    {"claim": "...", "supported": true, "evidence": "quote from context or null"}
  ]
}

Rules:
- Score 1.0 = fully grounded, 0.0 = fully hallucinated
- A claim is "supported" only if the context explicitly states or clearly implies it
- Do NOT use world knowledge to verify claims — only the provided context counts
