# purpose: minimum-viable tts-config fixture
# consumes: nothing
# produces: spec for --self-test
# depends-on: tts-config.schema.yaml
# token-budget-impact: 0
provider: elevenlabs
model: eleven_turbo_v2
voice: {id: rachel, clone_consent: true}
cache: {enabled: true, ttl_days: 30}
fallback: {provider: openai, model: tts-1}
