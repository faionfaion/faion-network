<?xml version="1.0" encoding="UTF-8"?>
<text id="02-patterns" title="Reasoning-First Patterns: Think-Before-Act and Agentic Workflows">
  <summary>Think-before-act patterns (ReAct, Reflexion, Tree-of-Thought, Critique+Revise) and how to integrate reasoning models into agent pipelines.</summary>

  <section title="Core Patterns">
    <rule>
      <statement>Use ReAct (Reason + Act) for tool-using agents that need to explain their steps. Use Reflexion when the agent needs to self-evaluate and improve its output across multiple iterations. Use Tree-of-Thought only for problems with multiple valid solution paths.</statement>
      <rationale>ReAct makes agent reasoning transparent and auditable. Reflexion is cost-effective for quality-critical outputs: generate → critique → improve loop is cheaper than a single expensive reasoning model call. Tree-of-Thought explores branches and is only justified when the search space has genuinely different solution paths.</rationale>
    </rule>
    <list>
      <item>ReAct: interleaved reasoning and tool use; makes steps auditable</item>
      <item>Reflexion: generate → self-critique → improve; good for code and writing</item>
      <item>Tree-of-Thought: explore multiple paths, prune and select; complex problem solving</item>
      <item>Planning Loop: plan → execute → verify → adjust; multi-step task execution</item>
      <item>Critique and Revise: generate → expert critique → revision; quality-critical outputs</item>
    </list>
  </section>

  <section title="Routing by Task Complexity">
    <rule>
      <statement>Use a cheap model (haiku/gpt-4o-mini) to classify task complexity before dispatching to a reasoning model. Gate reasoning model invocation behind a classifier that evaluates whether the task requires multi-step verification.</statement>
      <rationale>Sending all tasks to reasoning models wastes 10-50x per call. A fast classifier adds 10-30ms and costs fractions of a cent. Most production traffic is simple tasks that don't benefit from extended reasoning.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
import anthropic
client = anthropic.Anthropic()

def route_task(task: str) -> str:
    """Classify complexity and return recommended model."""
    classifier_response = client.messages.create(
        model="claude-haiku-4-5",
        max_tokens=20,
        messages=[{"role": "user", "content":
            f"Rate task complexity: simple/complex. Task: {task[:200]}"}]
    )
    if "complex" in classifier_response.content[0].text.lower():
        return "claude-opus-4-5"   # reasoning model
    return "claude-haiku-4-5"      # fast model
      ]]></code>
    </example>
  </section>

  <section title="Extended Thinking Integration (Claude)">
    <rule>
      <statement>Set max_tokens high enough to accommodate thinking tokens plus the answer. If max_tokens is too low, thinking is truncated and the response may be incomplete. Budget: thinking_budget + expected_output_tokens + 1000 buffer.</statement>
      <rationale>Extended Thinking uses tokens from max_tokens budget. A common mistake is setting max_tokens=4096 with budget_tokens=8192 — the model tries to think 8K tokens but max_tokens caps the total, truncating both thinking and answer.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
import anthropic
client = anthropic.Anthropic()

budget = 8000   # thinking tokens
response = client.messages.create(
    model="claude-opus-4-5",
    max_tokens=budget + 4000,  # thinking + answer buffer
    thinking={"type": "enabled", "budget_tokens": budget},
    messages=[{"role": "user", "content": task}]
)
# content[0] is ThinkingBlock, content[1] is TextBlock
answer = response.content[1].text
      ]]></code>
    </example>
  </section>

  <antipattern>
    <description>Streaming DeepSeek R1 think tags directly to users.</description>
    <reason>DeepSeek R1 think tags contain the model's internal monologue including potentially confidential input echoing and intermediate reasoning that should not be visible to end users. Always strip &lt;think&gt; blocks before returning output.</reason>
  </antipattern>

  <reference path="templates/extended-thinking.py">Claude Extended Thinking invocation with budget selector</reference>
  <reference path="templates/prompt-reasoning.txt">System prompt pattern for reasoning agents</reference>
</text>
