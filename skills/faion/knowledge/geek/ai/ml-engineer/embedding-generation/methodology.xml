<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="embedding-generation">
  <metadata>
    <tier>geek</tier>
    <group>ai</group>
    <domain>ml-engineer</domain>
    <summary>Embeddings are dense vector representations of text that capture semantic meaning. They enable similarity search, clustering, classification, and form the foundation of RAG systems. This methodology</summary>
    <difficulty>intermediate</difficulty>
    <created>2026-04-29</created>
    <tags>
      <tag>embedding</tag>
      <tag>generation</tag>
    </tags>
  </metadata>
  <content>
    <title>Embedding Generation</title>
    <summary>Embeddings are dense vector representations of text that capture semantic meaning. They enable similarity search, clustering, classification, and form the foundation of RAG systems. This methodology</summary>
    <why>
      <p>Embeddings are dense vector representations of text that capture semantic meaning. They enable similarity search, clustering, classification, and form the foundation of RAG systems. This methodology covers embedding models, generation strategies, batch processing, caching, and optimization techniques.</p>
    </why>
    <when-to-use>
      <list>
        <item>Semantic search implementations</item>
        <item>Document similarity matching</item>
        <item>RAG pipeline construction</item>
        <item>Text clustering and classification</item>
        <item>Recommendation systems</item>
        <item>Anomaly detection in text</item>
      </list>
    </when-to-use>
    <section name="Pre-Implementation (checklist)">
      <p>### Requirements Analysis
- [ ] Define use case (search, RAG, classification, clustering)
- [ ] Estimate document volume and query rate
- [ ] Determine latency requirements (real-time vs batch)
- [ ] Identify privacy/compliance constraints (cloud vs self-hosted)
- [ ] Calculate budget constraints</p>
      <p>### Model Selection
- [ ] Evaluate models on your domain data
- [ ] Benchmark Recall@100 with your queries
- [ ] Measure p95 latency requirements
- [ ] Check context window fits your documents
- [ ] Verify multilingual support if needed</p>
    </section>
    <section name="Implementation (checklist)">
      <p>### API Integration
- [ ] Set up API credentials securely (env vars, secrets manager)
- [ ] Implement retry logic with exponential backoff
- [ ] Handle rate limiting gracefully
- [ ] Add request timeout handling
- [ ] Log API errors for monitoring</p>
      <p>### Batch Processing
- [ ] Implement batching (100-1000 items per batch)
- [ ] Sort documents by length before batching
- [ ] Add progress tracking for large batches
- [ ] Implement parallel processing for multiple batches
- [ ] Handle partial batch failures</p>
      <p>### Caching Layer
- [ ] Implement exact match cache
- [ ] Add semantic cache for similar queries
- [ ] Set appropriate TTL for dynamic content
- [ ] Monitor cache hit rates
- [ ] Implement cache invalidation strategy</p>
      <p>### Text Preprocessing
- [ ] Normalize whitespace (newlines, tabs)
- [ ] Handle empty/null text gracefully
- [ ] Truncate to model's context limit
- [ ] Apply consistent cleaning for index and query
- [ ] Consider chunking strategy for long documents</p>
      <p>### Vector Storage
- [ ] Pre-normalize embeddings before storage
- [ ] Choose appropriate index type (HNSW, IVF, etc.)
- [ ] Set up metadata storage for filtering
- [ ] Implement hot/cold storage tiers
- [ ] Plan reindexing strategy</p>
    </section>
    <section name="Production Hardening (checklist)">
      <p>### Performance Optimization
- [ ] Enable mixed-precision inference (FP16) if available
- [ ] Use GPU for local models
- [ ] Implement two-stage retrieval (cheap recall + reranker)
- [ ] Consider dimension reduction for storage efficiency
- [ ] Quantize vectors (INT8) if needed</p>
      <p>### Monitoring
- [ ] Track embedding generation latency
- [ ] Monitor API costs and token usage
- [ ] Alert on error rates
- [ ] Track cache hit/miss ratios
- [ ] Monitor vector DB query performance</p>
      <p>### Cost Optimization
- [ ] Batch API calls to reduce overhead
- [ ] Cache aggressively (target 60-80% hit rate)
- [ ] Use cheaper models for initial recall
- [ ] Schedule batch jobs during off-peak hours
- [ ] Consider self-hosted for high volume</p>
      <p>### Reliability
- [ ] Implement fallback to secondary model/provider
- [ ] Add circuit breaker for API failures
- [ ] Store embeddings persistently (avoid recomputation)
- [ ] Version embedding models (track which model generated each embedding)
- [ ] Plan migration path for model upgrades</p>
    </section>
    <section name="Quality Assurance (checklist)">
      <p>### Testing
- [ ] Unit tests for embedding functions
- [ ] Integration tests with actual API
- [ ] Benchmark tests for throughput
- [ ] Similarity sanity checks (known pairs)
- [ ] Edge case handling (empty, long, special chars)</p>
      <p>### Validation
- [ ] Verify embedding dimensions match expected
- [ ] Check for zero vectors (indicates errors)
- [ ] Validate similarity scores are reasonable
- [ ] Test retrieval quality with ground truth
- [ ] A/B test model changes</p>
    </section>
    <section name="Maintenance (checklist)">
      <p>### Ongoing Operations
- [ ] Schedule regular re-benchmarking (quarterly)
- [ ] Monitor for model deprecation announcements
- [ ] Track embedding model updates
- [ ] Review and optimize costs monthly
- [ ] Update documentation with learnings</p>
      <p>### Scaling Considerations
- [ ] Plan horizontal scaling for high traffic
- [ ] Consider multi-region deployment
- [ ] Implement dynamic GPU allocation
- [ ] Use spot instances for batch processing
- [ ] Optimize for renewable energy regions</p>
    </section>
    <section name="OpenAI Embeddings (examples)">
      <p>### Basic Embedding</p>
      <p>client = OpenAI()</p>
      <p>def get_embedding(text: str, model: str = &quot;text-embedding-3-small&quot;) -&gt; list:
    &quot;&quot;&quot;Generate embedding using OpenAI.&quot;&quot;&quot;
    text = text.replace(&quot;\n&quot;, &quot; &quot;)
    response = client.embeddings.create(
        input=text,
        model=model
    )
    return response.data[0].embedding
```</p>
      <p>### Batch Embedding</p>
      <p>for i in range(0, len(texts), batch_size):
        batch = texts[i:i + batch_size]
        response = client.embeddings.create(
            input=batch,
            model=model
        )
        embeddings = [item.embedding for item in response.data]
        all_embeddings.extend(embeddings)</p>
      <p>return all_embeddings
```</p>
      <p>### Dimension Reduction (text-embedding-3 models only)</p>
    </section>
    <example type="good">
      <code lang="python">def get_reduced_embedding(
    text: str,
    dimensions: int = 256,
    model: str = &quot;text-embedding-3-small&quot;
) -&gt; list:
    &quot;&quot;&quot;Get lower-dimensional embedding for efficiency.&quot;&quot;&quot;
    response = client.embeddings.create(
        input=text,
        model=model,
        dimensions=dimensions  # Native dimension reduction
    )
    return response.data[0].embedding</code>
    </example>
    <section name="Cohere Embeddings (examples)">
      <p>### Basic Usage with Input Types</p>
      <p>co = cohere.Client(api_key=&quot;your-api-key&quot;)</p>
      <p>def get_cohere_embeddings(
    texts: list[str],
    input_type: str = &quot;search_document&quot;,
    model: str = &quot;embed-english-v3.0&quot;
) -&gt; list:
    &quot;&quot;&quot;
    Generate Cohere embeddings with task-specific input types.</p>
      <p>input_type options:
    - search_document: For documents to be searched
    - search_query: For search queries
    - classification: For classification tasks
    - clustering: For clustering tasks
    &quot;&quot;&quot;
    response = co.embed(
        texts=texts,
        model=model,
        input_type=input_type
    )
    return response.embeddings</p>
      <p># Usage for RAG
doc_embeddings = get_cohere_embeddings(
    documents,
    input_type=&quot;search_document&quot;
)</p>
      <p>query_embedding = get_cohere_embeddings(
    [query],
    input_type=&quot;search_query&quot;
)[0]
```</p>
      <p>### Multilingual Embedding</p>
    </section>
    <example type="good">
      <code lang="python">def get_multilingual_embeddings(
    texts: list[str],
    input_type: str = &quot;search_document&quot;
) -&gt; list:
    &quot;&quot;&quot;Cohere multilingual embeddings (100+ languages).&quot;&quot;&quot;
    response = co.embed(
        texts=texts,
        model=&quot;embed-multilingual-v3.0&quot;,
        input_type=input_type
    )
    return response.embeddings</code>
    </example>
    <section name="Voyage AI Embeddings (examples)">
      <p>### Basic Usage</p>
      <p>vo = voyageai.Client(api_key=&quot;your-api-key&quot;)</p>
      <p>def get_voyage_embeddings(
    texts: list[str],
    model: str = &quot;voyage-3&quot;,
    input_type: str = &quot;document&quot;
) -&gt; list:
    &quot;&quot;&quot;
    Generate Voyage AI embeddings.</p>
      <p>input_type: &quot;document&quot; or &quot;query&quot;
    &quot;&quot;&quot;
    result = vo.embed(
        texts,
        model=model,
        input_type=input_type
    )
    return result.embeddings
```</p>
      <p>### Domain-Specific Models</p>
      <p># Legal documents
legal_embeddings = vo.embed(
    legal_texts,
    model=&quot;voyage-law-2&quot;,
    input_type=&quot;document&quot;
)
```</p>
    </section>
    <section name="Local Embeddings with Sentence Transformers (examples)">
      <p>### Basic Usage</p>
      <p># Load model (downloads on first use)
model = SentenceTransformer('all-MiniLM-L6-v2')</p>
      <p>def get_local_embedding(text: str) -&gt; np.ndarray:
    &quot;&quot;&quot;Generate embedding locally.&quot;&quot;&quot;
    return model.encode(text)</p>
      <p>def get_local_embeddings_batch(
    texts: list[str],
    batch_size: int = 32,
    show_progress: bool = True
) -&gt; np.ndarray:
    &quot;&quot;&quot;Batch embedding generation with GPU optimization.&quot;&quot;&quot;
    return model.encode(
        texts,
        batch_size=batch_size,
        show_progress_bar=show_progress,
        convert_to_numpy=True,
        normalize_embeddings=True  # Pre-normalize for cosine similarity
    )
```</p>
      <p>### BGE Models (Higher Quality)</p>
      <p>def get_bge_embedding(
    text: str,
    is_query: bool = False
) -&gt; np.ndarray:
    &quot;&quot;&quot;BGE embeddings with instruction prefix for queries.&quot;&quot;&quot;
    if is_query:
        # BGE models work better with instruction prefix for queries
        text = &quot;Represent this sentence for retrieval: &quot; + text
    return bge_model.encode(text, normalize_embeddings=True)
```</p>
      <p>### BGE-M3 (Best Self-Hosted)</p>
      <p>model = BGEM3FlagModel('BAAI/bge-m3', use_fp16=True)</p>
      <p>def get_bge_m3_embeddings(texts: list[str]) -&gt; dict:
    &quot;&quot;&quot;BGE-M3 returns dense, sparse, and colbert vectors.&quot;&quot;&quot;
    return model.encode(
        texts,
        batch_size=12,
        max_length=8192,
        return_dense=True,
        return_sparse=True,
        return_colbert_vecs=True
    )
```</p>
    </section>
    <section name="Ollama Embeddings (Local) (examples)">
      <p>### Basic Usage</p>
      <p>def get_ollama_embedding(
    text: str,
    model: str = &quot;nomic-embed-text&quot;
) -&gt; list:
    &quot;&quot;&quot;Generate embedding using Ollama.&quot;&quot;&quot;
    response = ollama.embeddings(
        model=model,
        prompt=text
    )
    return response[&quot;embedding&quot;]</p>
      <p>def get_ollama_embeddings_batch(
    texts: list[str],
    model: str = &quot;nomic-embed-text&quot;
) -&gt; list:
    &quot;&quot;&quot;Batch embeddings with Ollama (sequential).&quot;&quot;&quot;
    return [
        ollama.embeddings(model=model, prompt=text)[&quot;embedding&quot;]
        for text in texts
    ]
```</p>
    </section>
    <section name="Similarity Calculations (examples)">
      <p>### Cosine Similarity</p>
      <p>def cosine_similarity(a: np.ndarray, b: np.ndarray) -&gt; float:
    &quot;&quot;&quot;Calculate cosine similarity between two vectors.&quot;&quot;&quot;
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))</p>
      <p>def cosine_similarity_batch(
    query: np.ndarray,
    documents: np.ndarray
) -&gt; np.ndarray:
    &quot;&quot;&quot;Calculate cosine similarity between query and all documents.&quot;&quot;&quot;
    # Normalize
    query_norm = query / np.linalg.norm(query)
    doc_norms = documents / np.linalg.norm(documents, axis=1, keepdims=True)
    # Dot product of normalized vectors = cosine similarity
    return np.dot(doc_norms, query_norm)
```</p>
      <p>### Find Most Similar</p>
      <p>### Normalized Vectors (Faster)</p>
      <p>def normalize_batch(embeddings: np.ndarray) -&gt; np.ndarray:
    &quot;&quot;&quot;Normalize batch of embeddings.&quot;&quot;&quot;
    norms = np.linalg.norm(embeddings, axis=1, keepdims=True)
    norms[norms == 0] = 1  # Avoid division by zero
    return embeddings / norms</p>
      <p># For pre-normalized vectors, use dot product (faster)
def similarity_normalized(a: np.ndarray, b: np.ndarray) -&gt; float:
    &quot;&quot;&quot;Dot product for normalized vectors equals cosine similarity.&quot;&quot;&quot;
    return np.dot(a, b)
```</p>
    </section>
    <example type="good">
      <code lang="python">def find_most_similar(
    query_embedding: np.ndarray,
    document_embeddings: np.ndarray,
    top_k: int = 5
) -&gt; List[Tuple[int, float]]:
    &quot;&quot;&quot;Find most similar documents to query.&quot;&quot;&quot;
    similarities = cosine_similarity_batch(query_embedding, document_embeddings)
    top_indices = np.argsort(similarities)[::-1][:top_k]
    return [(idx, similarities[idx]) for idx in top_indices]</code>
    </example>
    <section name="Length-Based Sorting for Batch Optimization (examples)">
      <p>Achieves ~40% less computational waste.
    &quot;&quot;&quot;
    # Create index-text pairs and sort by length
    indexed_texts = [(i, text) for i, text in enumerate(texts)]
    sorted_texts = sorted(indexed_texts, key=lambda x: len(x[1]))</p>
      <p># Embed in batches
    embeddings = [None] * len(texts)
    for i in range(0, len(sorted_texts), batch_size):
        batch = sorted_texts[i:i + batch_size]
        batch_texts = [t[1] for t in batch]
        batch_embeddings = embed_func(batch_texts)</p>
      <p># Restore original order
        for (orig_idx, _), emb in zip(batch, batch_embeddings):
            embeddings[orig_idx] = emb</p>
      <p>return embeddings
```</p>
    </section>
    <section name="Async Embedding with Rate Limiting (examples)">
      <p>async_client = AsyncOpenAI()</p>
      <p>async def get_embedding_async(
    text: str,
    model: str = &quot;text-embedding-3-small&quot;,
    semaphore: asyncio.Semaphore = None
) -&gt; list:
    &quot;&quot;&quot;Async embedding with rate limiting.&quot;&quot;&quot;
    async with semaphore or asyncio.Semaphore(10):
        text = text.replace(&quot;\n&quot;, &quot; &quot;)
        response = await async_client.embeddings.create(
            input=text,
            model=model
        )
        return response.data[0].embedding</p>
      <p>async def get_embeddings_concurrent(
    texts: list[str],
    max_concurrent: int = 10
) -&gt; list:
    &quot;&quot;&quot;Generate embeddings concurrently with rate limiting.&quot;&quot;&quot;
    semaphore = asyncio.Semaphore(max_concurrent)
    tasks = [
        get_embedding_async(text, semaphore=semaphore)
        for text in texts
    ]
    return await asyncio.gather(*tasks)
```</p>
    </section>
    <section name="Production Embedding Service (templates)">
      <p># =============================================================================
# Configuration
# =============================================================================</p>
      <p>@dataclass
class EmbeddingConfig:
    &quot;&quot;&quot;Configuration for embedding service.&quot;&quot;&quot;
    provider: str = &quot;openai&quot;  # openai, cohere, voyage, local, ollama
    model: str = &quot;text-embedding-3-small&quot;
    dimensions: Optional[int] = None  # For dimension reduction
    batch_size: int = 100
    cache_enabled: bool = True
    cache_dir: str = &quot;.embedding_cache&quot;
    normalize: bool = True
    max_retries: int = 3
    timeout: int = 30</p>
      <p># =============================================================================
# Caching Layer
# =============================================================================</p>
      <p>class EmbeddingCache:
    &quot;&quot;&quot;Two-layer cache: exact match + persistent storage.&quot;&quot;&quot;</p>
      <p>def __init__(self, cache_dir: str = &quot;.embedding_cache&quot;):
        self.cache_dir = Path(cache_dir)
        self.cache_dir.mkdir(exist_ok=True)
        self.memory_cache: dict = {}  # Hot cache</p>
      <p>def _get_key(self, text: str, model: str) -&gt; str:
        &quot;&quot;&quot;Generate cache key from text and model.&quot;&quot;&quot;
        content = f&quot;{model}:{text}&quot;
        return hashlib.md5(content.encode()).hexdigest()</p>
      <p>def get(self, text: str, model: str) -&gt; Optional[list]:
        &quot;&quot;&quot;Retrieve cached embedding (memory first, then disk).&quot;&quot;&quot;
        key = self._get_key(text, model)</p>
      <p># Check memory cache first
        if key in self.memory_cache:
            return self.memory_cache[key]</p>
      <p># Check disk cache
        cache_file = self.cache_dir / f&quot;{key}.json&quot;
        if cache_file.exists():
            with open(cache_file, &quot;r&quot;) as f:
                embedding = json.load(f)
                self.memory_cache[key] = embedding  # Promote to memory
                return embedding</p>
      <p>return None</p>
      <p>def set(self, text: str, model: str, embedding: list):
        &quot;&quot;&quot;Cache embedding to memory and disk.&quot;&quot;&quot;
        key = self._get_key(text, model)</p>
      <p># Memory cache
        self.memory_cache[key] = embedding</p>
      <p># Disk cache
        cache_file = self.cache_dir / f&quot;{key}.json&quot;
        with open(cache_file, &quot;w&quot;) as f:
            json.dump(embedding, f)</p>
      <p>def get_batch(
        self,
        texts: list[str],
        model: str
    ) -&gt; tuple[list, list[int], list[str]]:
        &quot;&quot;&quot;
        Get cached embeddings for batch.</p>
      <p>Returns:
            - cached_embeddings: list of cached embeddings (or None)
            - uncached_indices: indices of texts not in cache
            - uncached_texts: texts not in cache
        &quot;&quot;&quot;
        cached = []
        uncached_indices = []
        uncached_texts = []</p>
      <p>for i, text in enumerate(texts):
            embedding = self.get(text, model)
            cached.append(embedding)
            if embedding is None:
                uncached_indices.append(i)
                uncached_texts.append(text)</p>
      <p>return cached, uncached_indices, uncached_texts</p>
      <p>def clear_memory(self):
        &quot;&quot;&quot;Clear in-memory cache.&quot;&quot;&quot;
        self.memory_cache.clear()</p>
      <p># =============================================================================
# Provider Implementations
# =============================================================================</p>
      <p>class EmbeddingProvider(ABC):
    &quot;&quot;&quot;Abstract base class for embedding providers.&quot;&quot;&quot;</p>
      <p>@abstractmethod
    def embed(self, text: str) -&gt; np.ndarray:
        pass</p>
      <p>@abstractmethod
    def embed_batch(self, texts: list[str]) -&gt; np.ndarray:
        pass</p>
      <p>class OpenAIProvider(EmbeddingProvider):
    &quot;&quot;&quot;OpenAI embedding provider.&quot;&quot;&quot;</p>
      <p>def __init__(self, config: EmbeddingConfig):
        from openai import OpenAI
        self.client = OpenAI()
        self.config = config</p>
      <p>def embed(self, text: str) -&gt; np.ndarray:
        text = text.replace(&quot;\n&quot;, &quot; &quot;)
        kwargs = {&quot;input&quot;: text, &quot;model&quot;: self.config.model}
        if self.config.dimensions:
            kwargs[&quot;dimensions&quot;] = self.config.dimensions
        response = self.client.embeddings.create(**kwargs)
        return np.array(response.data[0].embedding)</p>
      <p>def embed_batch(self, texts: list[str]) -&gt; np.ndarray:
        texts = [t.replace(&quot;\n&quot;, &quot; &quot;) for t in texts]
        all_embeddings = []</p>
      <p>for i in range(0, len(texts), self.config.batch_size):
            batch = texts[i:i + self.config.batch_size]
            kwargs = {&quot;input&quot;: batch, &quot;model&quot;: self.config.model}
            if self.config.dimensions:
                kwargs[&quot;dimensions&quot;] = self.config.dimensions
            response = self.client.embeddings.create(**kwargs)
            embeddings = [item.embedding for item in response.data]
            all_embeddings.extend(embeddings)</p>
      <p>return np.array(all_embeddings)</p>
      <p>class CohereProvider(EmbeddingProvider):
    &quot;&quot;&quot;Cohere embedding provider.&quot;&quot;&quot;</p>
      <p>def __init__(self, config: EmbeddingConfig):
        import cohere
        self.client = cohere.Client()
        self.config = config</p>
      <p>def embed(self, text: str, input_type: str = &quot;search_document&quot;) -&gt; np.ndarray:
        response = self.client.embed(
            texts=[text],
            model=self.config.model,
            input_type=input_type
        )
        return np.array(response.embeddings[0])</p>
      <p>def embed_batch(
        self,
        texts: list[str],
        input_type: str = &quot;search_document&quot;
    ) -&gt; np.ndarray:
        all_embeddings = []</p>
      <p>for i in range(0, len(texts), self.config.batch_size):
            batch = texts[i:i + self.config.batch_size]
            response = self.client.embed(
                texts=batch,
                model=self.config.model,
                input_type=input_type
            )
            all_embeddings.extend(response.embeddings)</p>
      <p>return np.array(all_embeddings)</p>
      <p>class LocalProvider(EmbeddingProvider):
    &quot;&quot;&quot;Local embedding provider using Sentence Transformers.&quot;&quot;&quot;</p>
      <p>def __init__(self, config: EmbeddingConfig):
        from sentence_transformers import SentenceTransformer
        self.model = SentenceTransformer(config.model)
        self.config = config</p>
      <p>def embed(self, text: str) -&gt; np.ndarray:
        return self.model.encode(
            text,
            normalize_embeddings=self.config.normalize
        )</p>
      <p>def embed_batch(self, texts: list[str]) -&gt; np.ndarray:
        return self.model.encode(
            texts,
            batch_size=self.config.batch_size,
            normalize_embeddings=self.config.normalize,
            show_progress_bar=len(texts) &gt; 100
        )</p>
      <p>class OllamaProvider(EmbeddingProvider):
    &quot;&quot;&quot;Ollama local embedding provider.&quot;&quot;&quot;</p>
      <p>def __init__(self, config: EmbeddingConfig):
        import ollama
        self.client = ollama
        self.config = config</p>
      <p>def embed(self, text: str) -&gt; np.ndarray:
        response = self.client.embeddings(
            model=self.config.model,
            prompt=text
        )
        return np.array(response[&quot;embedding&quot;])</p>
      <p>def embed_batch(self, texts: list[str]) -&gt; np.ndarray:
        embeddings = []
        for text in texts:
            response = self.client.embeddings(
                model=self.config.model,
                prompt=text
            )
            embeddings.append(response[&quot;embedding&quot;])
        return np.array(embeddings)</p>
      <p># =============================================================================
# Main Service
# =============================================================================</p>
      <p>class EmbeddingService:
    &quot;&quot;&quot;Production-ready embedding service with caching and batching.&quot;&quot;&quot;</p>
      <p>PROVIDERS = {
        &quot;openai&quot;: OpenAIProvider,
        &quot;cohere&quot;: CohereProvider,
        &quot;local&quot;: LocalProvider,
        &quot;ollama&quot;: OllamaProvider,
    }</p>
      <p>def __init__(self, config: Optional[EmbeddingConfig] = None):
        self.config = config or EmbeddingConfig()
        self.logger = logging.getLogger(__name__)
        self.cache = EmbeddingCache(self.config.cache_dir) if self.config.cache_enabled else None
        self._init_provider()</p>
      <p>def _init_provider(self):
        &quot;&quot;&quot;Initialize embedding provider.&quot;&quot;&quot;
        provider_class = self.PROVIDERS.get(self.config.provider)
        if not provider_class:
            raise ValueError(f&quot;Unknown provider: {self.config.provider}&quot;)
        self.provider = provider_class(self.config)</p>
      <p>def embed(self, text: str) -&gt; np.ndarray:
        &quot;&quot;&quot;Generate single embedding with caching.&quot;&quot;&quot;
        if self.cache:
            cached = self.cache.get(text, self.config.model)
            if cached:
                return np.array(cached)</p>
      <p>embedding = self.provider.embed(text)</p>
      <p>if self.config.normalize:
            embedding = self._normalize(embedding)</p>
      <p>if self.cache:
            self.cache.set(text, self.config.model, embedding.tolist())</p>
      <p>return embedding</p>
      <p>def embed_batch(self, texts: list[str]) -&gt; np.ndarray:
        &quot;&quot;&quot;Generate embeddings for batch with caching.&quot;&quot;&quot;
        if not texts:
            return np.array([])</p>
      <p>if self.cache:
            cached, uncached_indices, uncached_texts = self.cache.get_batch(
                texts, self.config.model
            )</p>
      <p>if not uncached_texts:
                # All cached
                return np.array(cached)</p>
      <p># Generate embeddings for uncached
            new_embeddings = self.provider.embed_batch(uncached_texts)</p>
      <p>if self.config.normalize:
                new_embeddings = self._normalize_batch(new_embeddings)</p>
      <p># Cache new embeddings
            for text, emb in zip(uncached_texts, new_embeddings):
                self.cache.set(text, self.config.model, emb.tolist())</p>
      <p># Merge cached and new
            result = []
            new_idx = 0
            for i, emb in enumerate(cached):
                if emb is None:
                    result.append(new_embeddings[new_idx])
                    new_idx += 1
                else:
                    result.append(np.array(emb))</p>
      <p>return np.array(result)
        else:
            embeddings = self.provider.embed_batch(texts)
            if self.config.normalize:
                embeddings = self._normalize_batch(embeddings)
            return embeddings</p>
      <p>def similarity(self, text1: str, text2: str) -&gt; float:
        &quot;&quot;&quot;Calculate similarity between two texts.&quot;&quot;&quot;
        emb1 = self.embed(text1)
        emb2 = self.embed(text2)
        return float(np.dot(emb1, emb2))  # Assumes normalized</p>
      <p>def find_similar(
        self,
        query: str,
        documents: list[str],
        top_k: int = 5
    ) -&gt; list[tuple[int, float, str]]:
        &quot;&quot;&quot;Find most similar documents to query.&quot;&quot;&quot;
        query_emb = self.embed(query)
        doc_embs = self.embed_batch(documents)</p>
      <p># Dot product (assumes normalized)
        similarities = np.dot(doc_embs, query_emb)
        top_indices = np.argsort(similarities)[::-1][:top_k]</p>
      <p>return [
            (idx, float(similarities[idx]), documents[idx])
            for idx in top_indices
        ]</p>
      <p>def _normalize(self, embedding: np.ndarray) -&gt; np.ndarray:
        &quot;&quot;&quot;Normalize single embedding.&quot;&quot;&quot;
        norm = np.linalg.norm(embedding)
        return embedding / norm if norm &gt; 0 else embedding</p>
      <p>def _normalize_batch(self, embeddings: np.ndarray) -&gt; np.ndarray:
        &quot;&quot;&quot;Normalize batch of embeddings.&quot;&quot;&quot;
        norms = np.linalg.norm(embeddings, axis=1, keepdims=True)
        norms[norms == 0] = 1
        return embeddings / norms</p>
      <p># =============================================================================
# Usage Example
# =============================================================================</p>
      <p>if __name__ == &quot;__main__&quot;:
    # OpenAI (cloud)
    service = EmbeddingService(EmbeddingConfig(
        provider=&quot;openai&quot;,
        model=&quot;text-embedding-3-small&quot;,
        dimensions=512,  # Reduce dimensions for efficiency
        cache_enabled=True
    ))</p>
      <p># Local (privacy)
    # service = EmbeddingService(EmbeddingConfig(
    #     provider=&quot;local&quot;,
    #     model=&quot;all-MiniLM-L6-v2&quot;,
    #     cache_enabled=True
    # ))</p>
      <p># Single embedding
    embedding = service.embed(&quot;Hello, world!&quot;)
    print(f&quot;Embedding shape: {embedding.shape}&quot;)</p>
      <p># Batch embedding
    texts = [&quot;First document&quot;, &quot;Second document&quot;, &quot;Third document&quot;]
    embeddings = service.embed_batch(texts)
    print(f&quot;Batch shape: {embeddings.shape}&quot;)</p>
      <p># Find similar
    results = service.find_similar(
        &quot;programming language&quot;,
        [&quot;Python is great&quot;, &quot;JavaScript is popular&quot;, &quot;I like pizza&quot;],
        top_k=2
    )
    for idx, score, text in results:
        print(f&quot;Score: {score:.3f} - {text}&quot;)
```</p>
    </section>
    <section name="Semantic Cache Template (templates)">
      <p>class SemanticCache:
    &quot;&quot;&quot;
    Two-layer cache: exact match + semantic similarity.</p>
      <p>Achieves 60-80% hit rate in production.
    &quot;&quot;&quot;</p>
      <p>def __init__(
        self,
        embedding_service: EmbeddingService,
        similarity_threshold: float = 0.95,
        max_cache_size: int = 10000
    ):
        self.embedding_service = embedding_service
        self.similarity_threshold = similarity_threshold
        self.max_cache_size = max_cache_size</p>
      <p>self.exact_cache: dict[str, any] = {}
        self.semantic_cache: list[Tuple[np.ndarray, str, any]] = []</p>
      <p>def get(self, query: str) -&gt; Optional[any]:
        &quot;&quot;&quot;Get cached result (exact match first, then semantic).&quot;&quot;&quot;
        # Exact match (fastest)
        if query in self.exact_cache:
            return self.exact_cache[query]</p>
      <p># Semantic match
        if not self.semantic_cache:
            return None</p>
      <p>query_embedding = self.embedding_service.embed(query)</p>
      <p>for cached_embedding, cached_query, cached_result in self.semantic_cache:
            similarity = np.dot(query_embedding, cached_embedding)
            if similarity &gt;= self.similarity_threshold:
                # Also add to exact cache for future
                self.exact_cache[query] = cached_result
                return cached_result</p>
      <p>return None</p>
      <p>def set(self, query: str, result: any):
        &quot;&quot;&quot;Cache result with both exact and semantic keys.&quot;&quot;&quot;
        # Exact cache
        self.exact_cache[query] = result</p>
      <p># Semantic cache
        embedding = self.embedding_service.embed(query)
        self.semantic_cache.append((embedding, query, result))</p>
      <p># Evict if too large (simple FIFO)
        if len(self.semantic_cache) &gt; self.max_cache_size:
            self.semantic_cache = self.semantic_cache[-self.max_cache_size:]</p>
      <p>def get_or_compute(
        self,
        query: str,
        compute_func: Callable[[str], any]
    ) -&gt; any:
        &quot;&quot;&quot;Get from cache or compute and cache.&quot;&quot;&quot;
        cached = self.get(query)
        if cached is not None:
            return cached</p>
      <p>result = compute_func(query)
        self.set(query, result)
        return result
```</p>
    </section>
    <section name="Batch Processor with Progress (templates)">
      <p>class BatchEmbeddingProcessor:
    &quot;&quot;&quot;Process large document sets with progress tracking.&quot;&quot;&quot;</p>
      <p>def __init__(
        self,
        embedding_service: EmbeddingService,
        batch_size: int = 100,
        max_workers: int = 4
    ):
        self.service = embedding_service
        self.batch_size = batch_size
        self.max_workers = max_workers</p>
      <p>def process(
        self,
        texts: list[str],
        show_progress: bool = True
    ) -&gt; np.ndarray:
        &quot;&quot;&quot;Process texts with progress bar.&quot;&quot;&quot;
        all_embeddings = []
        batches = [
            texts[i:i + self.batch_size]
            for i in range(0, len(texts), self.batch_size)
        ]</p>
      <p>iterator = tqdm(batches) if show_progress else batches
        for batch in iterator:
            embeddings = self.service.embed_batch(batch)
            all_embeddings.append(embeddings)</p>
      <p>return np.vstack(all_embeddings)</p>
      <p>def process_sorted(
        self,
        texts: list[str],
        show_progress: bool = True
    ) -&gt; np.ndarray:
        &quot;&quot;&quot;Process with length-based sorting (40% less compute).&quot;&quot;&quot;
        # Sort by length
        indexed = [(i, t) for i, t in enumerate(texts)]
        sorted_indexed = sorted(indexed, key=lambda x: len(x[1]))</p>
      <p>sorted_texts = [t for _, t in sorted_indexed]
        embeddings = self.process(sorted_texts, show_progress)</p>
      <p># Restore original order
        result = np.zeros_like(embeddings)
        for new_idx, (orig_idx, _) in enumerate(sorted_indexed):
            result[orig_idx] = embeddings[new_idx]</p>
      <p>return result
```</p>
    </section>
    <section name="FastAPI Embedding Service (templates)">
      <p>app = FastAPI(title=&quot;Embedding Service&quot;)</p>
      <p># Initialize service
service = EmbeddingService(EmbeddingConfig(
    provider=&quot;openai&quot;,
    model=&quot;text-embedding-3-small&quot;,
    cache_enabled=True
))</p>
      <p>class EmbedRequest(BaseModel):
    text: str
    model: Optional[str] = None</p>
      <p>class EmbedBatchRequest(BaseModel):
    texts: List[str]
    model: Optional[str] = None</p>
      <p>class SimilarityRequest(BaseModel):
    text1: str
    text2: str</p>
      <p>class EmbedResponse(BaseModel):
    embedding: List[float]
    dimensions: int
    model: str</p>
      <p>class SimilarityResponse(BaseModel):
    similarity: float</p>
      <p>@app.post(&quot;/embed&quot;, response_model=EmbedResponse)
async def embed(request: EmbedRequest):
    &quot;&quot;&quot;Generate embedding for single text.&quot;&quot;&quot;
    try:
        embedding = service.embed(request.text)
        return EmbedResponse(
            embedding=embedding.tolist(),
            dimensions=len(embedding),
            model=service.config.model
        )
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))</p>
      <p>@app.post(&quot;/embed/batch&quot;)
async def embed_batch(request: EmbedBatchRequest):
    &quot;&quot;&quot;Generate embeddings for multiple texts.&quot;&quot;&quot;
    try:
        embeddings = service.embed_batch(request.texts)
        return {
            &quot;embeddings&quot;: embeddings.tolist(),
            &quot;dimensions&quot;: embeddings.shape[1],
            &quot;count&quot;: len(request.texts),
            &quot;model&quot;: service.config.model
        }
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))</p>
      <p>@app.post(&quot;/similarity&quot;, response_model=SimilarityResponse)
async def similarity(request: SimilarityRequest):
    &quot;&quot;&quot;Calculate similarity between two texts.&quot;&quot;&quot;
    try:
        score = service.similarity(request.text1, request.text2)
        return SimilarityResponse(similarity=score)
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))</p>
      <p>@app.get(&quot;/health&quot;)
async def health():
    &quot;&quot;&quot;Health check.&quot;&quot;&quot;
    return {&quot;status&quot;: &quot;healthy&quot;, &quot;model&quot;: service.config.model}
```</p>
    </section>
    <section name="Model Selection Prompt (llm-prompts)">
      <p>Given the following requirements:
- Use case: {use_case}
- Document volume: {volume}
- Latency requirement: {latency}
- Budget: {budget}
- Privacy constraints: {privacy}
- Languages: {languages}</p>
      <p>Recommend the best embedding model considering:</p>
      <p>1. **Provider options:**
   - OpenAI text-embedding-3-large (3072d, $0.13/1M, MTEB 64.6)
   - OpenAI text-embedding-3-small (1536d, $0.02/1M, MTEB 62.3)
   - Cohere embed-v4 (1024d, $0.10/1M, MTEB 65.2, multilingual)
   - Voyage voyage-3-large (1536d, $0.12/1M, MTEB 63.8)
   - Voyage voyage-3.5-lite (1024d, $0.02/1M, MTEB 66.1)
   - BGE-M3 (1024d, free, self-hosted, MTEB 63.0)
   - nomic-embed-text (768d, free, local/Ollama)</p>
      <p>2. **Evaluation criteria:**
   - Quality (MTEB score, domain fit)
   - Cost (per 1M tokens)
   - Latency (API vs local)
   - Context window (max tokens)
   - Dimension (storage/speed tradeoff)</p>
      <p>Provide recommendation with reasoning.
```</p>
    </section>
    <section name="Chunking Strategy Prompt (llm-prompts)">
      <p>Document characteristics:
- Type: {doc_type} (e.g., legal contracts, technical docs, articles)
- Average length: {avg_length}
- Structure: {structure} (e.g., headers, paragraphs, tables)
- Query patterns: {query_patterns}</p>
      <p>Embedding model:
- Context window: {context_window} tokens
- Optimal chunk size: typically 256-512 tokens for retrieval</p>
      <p>Design chunking strategy considering:</p>
      <p>1. **Chunk size:** Balance context vs specificity
2. **Overlap:** Prevent information loss at boundaries
3. **Boundaries:** Respect semantic units (paragraphs, sections)
4. **Metadata:** What to preserve for filtering</p>
      <p>Output format:
- Recommended chunk size (tokens)
- Overlap size (tokens)
- Boundary rules
- Metadata to extract
- Code example
```</p>
    </section>
    <section name="Cache Strategy Prompt (llm-prompts)">
      <p>System characteristics:
- Query volume: {qps} queries per second
- Unique query rate: {unique_rate}% new queries
- Document corpus size: {corpus_size}
- Latency SLA: {latency_sla}
- Budget: {budget}</p>
      <p>Current metrics:
- Average embedding latency: {embedding_latency}ms
- Cache hit rate: {current_hit_rate}%</p>
      <p>Design caching strategy considering:</p>
      <p>1. **Cache layers:**
   - Exact match (hash-based)
   - Semantic cache (similarity threshold)
   - Hot/cold storage tiers</p>
      <p>2. **Infrastructure:**
   - Redis (sub-ms, in-memory)
   - Vector DB (ms, persistent)
   - Disk cache (10s ms, cheap)</p>
      <p>3. **Optimization:**
   - Target hit rate: 60-80%
   - TTL strategy for dynamic content
   - Cache warming for common queries</p>
      <p>Provide:
- Architecture diagram (text)
- Configuration recommendations
- Expected metrics improvement
```</p>
    </section>
    <section name="Batch Processing Optimization Prompt (llm-prompts)">
      <p>Current setup:
- Model: {model}
- Batch size: {batch_size}
- Processing time: {current_time}
- Cost: {current_cost}</p>
      <p>Hardware:
- GPU: {gpu_type} ({vram}GB VRAM)
- CPU: {cpu_cores} cores
- Memory: {ram}GB RAM</p>
      <p>Optimize for:
- [ ] Throughput (docs/sec)
- [ ] Cost reduction
- [ ] Latency
- [ ] Memory efficiency</p>
      <p>Provide optimization recommendations:</p>
      <p>1. **Batch size tuning** (based on VRAM)
2. **Length-based sorting** (reduce padding)
3. **Token-count batching** (variable length)
4. **Parallel processing** (multi-GPU/region)
5. **Mixed precision** (FP16)
6. **Quantization** (INT8)</p>
      <p>Include code examples for key optimizations.
```</p>
    </section>
    <section name="Quality Evaluation Prompt (llm-prompts)">
      <p>Test setup:
- Query set: {num_queries} queries
- Document corpus: {num_docs} documents
- Ground truth: {ground_truth_source}</p>
      <p>Models to compare:
{model_list}</p>
      <p>Evaluation metrics:
1. **Recall@K:** Relevant docs in top K
2. **MRR:** Mean reciprocal rank
3. **NDCG:** Normalized discounted cumulative gain
4. **Latency:** p50, p95, p99
5. **Cost:** Per 1000 queries</p>
      <p>Generate evaluation code and report template:</p>
      <p>Include:
- Statistical significance testing
- A/B testing framework
- Monitoring dashboard metrics
```</p>
    </section>
    <example type="good">
      <code lang="python"># Evaluation framework
def evaluate_model(model_name, queries, corpus, ground_truth):
    # Your evaluation code here
    pass</code>
    </example>
    <section name="Dimension Reduction Prompt (llm-prompts)">
      <p>Current setup:
- Model: {model}
- Current dimensions: {current_dims}
- Storage: {storage_size}
- Query latency: {latency}</p>
      <p>Constraints:
- Maximum acceptable recall loss: {max_recall_loss}%
- Target storage reduction: {storage_target}%
- Latency requirement: {latency_req}ms</p>
      <p>Options to evaluate:</p>
      <p>1. **Native reduction** (text-embedding-3 only)
   - API parameter: dimensions=256/512/1024</p>
      <p>2. **PCA** (post-processing)
   - Requires fitting on corpus
   - Adds preprocessing step</p>
      <p>3. **Quantization** (INT8)
   - 4x memory reduction
   - Minimal quality loss</p>
      <p>4. **Matryoshka embeddings**
   - Multiple granularities in one vector
   - Flexible truncation</p>
      <p>Recommend approach with tradeoff analysis:
- Quality vs storage vs latency chart
- Implementation code
- Benchmark methodology
```</p>
    </section>
    <section name="Troubleshooting Prompt (llm-prompts)">
      <p>Symptoms:
{symptoms}</p>
      <p>System details:
- Model: {model}
- Preprocessing: {preprocessing}
- Index type: {index_type}
- Similarity metric: {metric}</p>
      <p>Common issues checklist:</p>
      <p>1. **Model mismatch**
   - Query model != index model
   - Solution: Verify model consistency</p>
      <p>2. **Preprocessing inconsistency**
   - Different cleaning for index vs query
   - Solution: Standardize preprocessing</p>
      <p>3. **Wrong similarity metric**
   - Euclidean with unnormalized vectors
   - Solution: Use cosine or normalize first</p>
      <p>4. **Token truncation**
   - Long docs silently truncated
   - Solution: Chunking strategy</p>
      <p>5. **Empty/zero vectors**
   - Empty text produces zero vector
   - Solution: Filter empty inputs</p>
      <p>6. **Fine-tuning drift**
   - Model updated, embeddings stale
   - Solution: Reindex after model changes</p>
      <p>Diagnostic steps:
1. Log preprocessing for sample query
2. Compare query/doc embedding dimensions
3. Check vector norms (should be ~1 if normalized)
4. Test with known similar pairs
5. Verify index configuration</p>
      <p>Provide diagnostic code and resolution steps.
```</p>
    </section>
    <section name="Migration Planning Prompt (llm-prompts)">
      <p>Current state:
- Model: {current_model}
- Index size: {index_size} vectors
- Query volume: {qps} QPS
- Downtime tolerance: {downtime}</p>
      <p>Target:
- Model: {target_model}
- Reason: {migration_reason}</p>
      <p>Migration strategy options:</p>
      <p>1. **Blue-green deployment**
   - Build new index in parallel
   - Switch traffic atomically
   - Requires 2x storage temporarily</p>
      <p>2. **Gradual rollout**
   - Dual-write to both indexes
   - Route % traffic to new index
   - Compare metrics before full switch</p>
      <p>3. **Offline reindex**
   - Scheduled maintenance window
   - Faster but requires downtime</p>
      <p>4. **Streaming migration**
   - Reindex on read (lazy)
   - No downtime, slower queries initially</p>
      <p>Recommend strategy with:
- Step-by-step plan
- Rollback procedure
- Monitoring checklist
- Risk assessment
```</p>
    </section>
    <section name="Cost Optimization Prompt (llm-prompts)">
      <p>Current spending:
- Model: {model}
- Monthly cost: ${current_cost}
- Usage pattern: {usage_pattern}</p>
      <p>Optimization strategies:</p>
      <p>1. **Model downgrade**
   - text-embedding-3-large → text-embedding-3-small
   - Savings: ~85%
   - Quality tradeoff: Check Recall@K impact</p>
      <p>2. **Dimension reduction**
   - 3072d → 512d
   - Savings: Storage + compute
   - Use native API parameter</p>
      <p>3. **Caching**
   - Target 60-80% hit rate
   - Savings: Proportional to hit rate</p>
      <p>4. **Batching**
   - Reduce API call overhead
   - Optimal: 100-1000 items/batch</p>
      <p>5. **Two-stage retrieval**
   - Cheap embeddings for recall
   - Expensive reranker for precision</p>
      <p>6. **Self-hosted fallback**
   - BGE-M3 for high volume
   - API for low-latency/quality</p>
      <p>Calculate ROI for each strategy:
| Strategy | Savings | Implementation Cost | Payback |
|----------|---------|---------------------|---------|
| ... | ... | ... | ... |</p>
      <p>Recommend prioritized implementation plan.
```</p>
    </section>
    <section name="When to use (agent-integration)">
      <list>
        <item>Agent needs semantic search over a document corpus (RAG, knowledge base retrieval)</item>
        <item>Deduplication of items before processing: compare embeddings to skip near-duplicates</item>
        <item>Routing agent queries to the right specialized sub-agent based on semantic similarity to example inputs</item>
        <item>Clustering unlabeled agent outputs to identify patterns or failure modes</item>
        <item>Building a memory layer: encode past agent interactions, retrieve relevant context for new tasks</item>
      </list>
    </section>
    <section name="When NOT to use (agent-integration)">
      <list>
        <item>Keyword-exact matching (product SKUs, error codes, IDs) — BM25 or SQL LIKE is faster and cheaper</item>
        <item>Real-time response generation — embedding is a pre-processing step, not inline generation</item>
        <item>When corpus changes every second — index rebuild lag makes embeddings stale before they're useful</item>
        <item>Very short texts (&lt;5 words) — embeddings for short strings are unreliable; use exact match instead</item>
        <item>When all queries are highly specific technical terms (code identifiers, domain acronyms) — domain-specific models needed</item>
      </list>
    </section>
    <section name="Where it fails / limitations (agent-integration)">
      <list>
        <item>Model mismatch: querying with a different model than was used for indexing produces garbage similarity scores — always document model name + version in the index metadata</item>
        <item>Empty string embeddings produce zero vectors — must filter or the results are meaningless and pollute rankings</item>
        <item>Token limit truncation: texts exceeding the model's context (8191 tokens for OpenAI 3-large) are silently truncated — long documents need chunking before embedding</item>
        <item>Dimension mismatch: mixing embeddings from different models or different dimension settings causes silent errors in vector DBs that don't enforce schema</item>
        <item>Cosine similarity ceiling: at very high similarity (&gt;0.98), small numerical differences no longer reflect meaningful semantic difference — don't over-optimize threshold</item>
        <item>Reindexing after fine-tuning or model change: all existing embeddings are invalid; complete reindex is required</item>
      </list>
    </section>
    <section name="Agentic workflow (agent-integration)">
      <p>Agents use embedding generation as a retrieval gate: before making an LLM call, an agent embeds the user query and retrieves the top-K relevant chunks from a vector store. The retrieved context is injected into the LLM prompt. For memory systems, agent outputs are embedded and stored after each interaction; on the next invocation, the agent retrieves the most relevant prior interactions. Batch embed at ingestion time; generate single-query embeddings at inference time. Use a two-stage pipeline: cheap embedding model for recall (top-100), reranker for precision (top-10).</p>
      <p>### Recommended subagents
- RAG retrieval agents (rag-engineer skill) use embedding generation as their core primitive
- Memory agents that persist and retrieve past agent interactions</p>
      <p>### Prompt pattern
Query embedding + retrieval (Python):
```python
from openai import OpenAI
import numpy as np</p>
      <p>client = OpenAI()</p>
      <p>def embed(text: str, model: str = &quot;text-embedding-3-small&quot;) -&gt; list[float]:
    &quot;&quot;&quot;Generate normalized embedding for a single text.&quot;&quot;&quot;
    text = text.replace(&quot;\n&quot;, &quot; &quot;).strip()
    if not text:
        raise ValueError(&quot;Cannot embed empty text&quot;)
    resp = client.embeddings.create(input=[text], model=model)
    vec = np.array(resp.data[0].embedding)
    return (vec / np.linalg.norm(vec)).tolist()  # normalize for cosine similarity</p>
      <p>def batch_embed(texts: list[str], model: str = &quot;text-embedding-3-small&quot;, batch_size: int = 100) -&gt; list[list[float]]:
    &quot;&quot;&quot;Batch embed with chunking to stay within API limits.&quot;&quot;&quot;
    results = []
    for i in range(0, len(texts), batch_size):
        batch = [t.replace(&quot;\n&quot;, &quot; &quot;).strip() for t in texts[i:i+batch_size]]
        resp = client.embeddings.create(input=batch, model=model)
        for item in sorted(resp.data, key=lambda x: x.index):
            vec = np.array(item.embedding)
            results.append((vec / np.linalg.norm(vec)).tolist())
    return results
```</p>
    </section>
    <section name="CLI tools (agent-integration)">
      <p>| Tool | Purpose | Install / docs |
|------|---------|----------------|
| `openai` | text-embedding-3-small/large via API | `pip install openai` / platform.openai.com/docs/guides/embeddings |
| `cohere` | embed-v4 (multilingual, multimodal) | `pip install cohere` / docs.cohere.com/reference/embed |
| `sentence-transformers` | Local embedding generation (BGE-M3, nomic) | `pip install sentence-transformers` / sbert.net |
| `ollama` | nomic-embed-text, mxbai-embed-large locally | `ollama pull nomic-embed-text` / ollama.com |
| `voyageai` | voyage-3-large, voyage-3.5-lite (RAG-optimized) | `pip install voyageai` / docs.voyageai.com |</p>
    </section>
    <section name="Services &amp; apps (agent-integration)">
      <p>| Service | Type (SaaS/OSS) | Agent-friendly? | Notes |
|---------|-----------------|-----------------|-------|
| OpenAI Embeddings API | SaaS | Yes | text-embedding-3-large: best general quality; supports dimension reduction |
| Cohere Embed API | SaaS | Yes | embed-v4: multilingual (100+ languages), task-type input hint |
| Voyage AI | SaaS | Yes | Best for domain-specific RAG (legal, medical, code); trained on tricky negatives |
| Qdrant | OSS | Yes | Vector DB; self-hosted; ideal for production agentic retrieval |
| Chroma | OSS | Yes | Dev/test vector store; in-memory or persistent |
| pgvector | OSS | Yes | PostgreSQL extension; good when you already use Postgres |</p>
    </section>
    <section name="Templates &amp; scripts (agent-integration)">
      <p>See `templates.md` for: production embedding service with Redis cache, batch job with sorted-by-length optimization, Qdrant upsert pipeline.</p>
      <p>Inline: embedding cache with content-hash key (~30 lines):</p>
      <p>redis = Redis(host=&quot;localhost&quot;, port=6379, db=0)
EMBED_MODEL = &quot;text-embedding-3-small&quot;</p>
      <p>def cached_embed(text: str, client) -&gt; list[float]:
    &quot;&quot;&quot;Embed with exact-match Redis cache. Key = hash(model + text).&quot;&quot;&quot;
    key = f&quot;emb:{EMBED_MODEL}:{hashlib.sha256(f'{EMBED_MODEL}:{text}'.encode()).hexdigest()}&quot;
    cached = redis.get(key)
    if cached:
        return json.loads(cached)
    resp = client.embeddings.create(input=[text], model=EMBED_MODEL)
    vec = resp.data[0].embedding
    redis.setex(key, 86400, json.dumps(vec))  # 24h TTL
    return vec
```</p>
    </section>
    <section name="Best practices (agent-integration)">
      <list>
        <item>Always normalize embeddings before storing — enables dot product as a faster proxy for cosine similarity</item>
        <item>Sort batch inputs by token length before API calls — reduces padding waste, improves throughput ~40%</item>
        <item>Store embedding model name and version in index metadata — makes future migration auditable</item>
        <item>Use Cohere's `input_type` parameter (`search_document` vs `search_query`) — it materially improves retrieval precision</item>
        <item>Implement two-stage retrieval: embed-based recall (top-100) → reranker precision (top-10) — reduces cost while maintaining quality</item>
        <item>Cache embeddings for queries: user questions repeat more than you expect; Redis cache cut API calls 30-60% in production</item>
        <item>For multilingual corpora, use Cohere embed-v4 or BGE-M3 — OpenAI 3-large performance degrades outside English</item>
      </list>
    </section>
    <section name="AI-agent gotchas (agent-integration)">
      <list>
        <item>Human-in-loop checkpoint: when retrieval quality (relevance scores) drops below threshold, the agent should ask the user to clarify rather than hallucinating from low-quality context</item>
        <item>Never reuse an existing embedding index after changing the embedding model — the resulting similarity scores are meaningless; complete reindex required</item>
        <item>Embedding API rate limits apply per minute of input tokens, not per request — large batch jobs hit limits unexpectedly; implement backoff</item>
        <item>Zero-vector trap: filtering empty texts before embedding is mandatory — a zero vector matches everything at similarity ~0 and poisons rankings</item>
        <item>Dimension reduction (e.g., OpenAI 3-large from 3072 → 1024 dimensions) reduces storage and latency but slightly reduces recall; test on your actual query distribution before applying globally</item>
      </list>
    </section>
    <section name="References (agent-integration)">
      <list>
        <item>https://platform.openai.com/docs/guides/embeddings</item>
        <item>https://docs.cohere.com/reference/embed</item>
        <item>https://docs.voyageai.com/</item>
        <item>https://www.sbert.net/</item>
        <item>https://huggingface.co/spaces/mteb/leaderboard</item>
        <item>https://blog.skypilot.co/large-scale-embedding/</item>
        <item>https://www.mongodb.com/company/blog/engineering/token-count-based-batching-faster-cheaper-embedding-inference-for-queries</item>
      </list>
    </section>
  </content>
</methodology>
