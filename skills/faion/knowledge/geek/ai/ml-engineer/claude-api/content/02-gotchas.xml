<?xml version="1.0" encoding="UTF-8"?>
<text>
  <section name="tool-loop-gotchas">
    <antipattern name="infinite-tool-loop">
      <description>An agent without a MAX_TURNS guard calls tools indefinitely. Claude never reaches end_turn when a tool always returns an error, an ambiguous result, or a prompt that drives Claude to call more tools.</description>
      <reason>Unbounded loops burn tokens at tool_use call rates plus tool result input rates. At Sonnet pricing a 1,000-turn runaway costs ~$5 before hitting rate limits.</reason>
      <rule>Always enforce MAX_TURNS = 10 as the default. Log the turn count on every loop iteration. Raise RuntimeError with the loop count when the cap is hit so callers can inspect the conversation array.</rule>
    </antipattern>

    <antipattern name="tool-schema-drift">
      <description>A tool's input_schema is updated without updating the agent loop handler. Claude generates inputs matching the new schema; the handler expects the old field names and silently ignores or crashes on unknown fields.</description>
      <reason>Schema validation is not automatic — the API passes Claude's tool inputs directly without checking them against the registered schema on the server side.</reason>
      <rule>Validate tool inputs against the tool's input_schema (e.g., with jsonschema.validate) before passing them to the handler. Add a test for each tool that asserts the handler accepts the current schema's required fields.</rule>
    </antipattern>

    <antipattern name="side-effects-without-checkpoint">
      <description>An agentic loop executes irreversible side effects (database mutations, email sends, API POST calls) without a human confirmation step. Claude cannot undo executed side effects.</description>
      <reason>Claude may misinterpret ambiguous instructions or act on stale context, triggering writes the user did not intend.</reason>
      <rule>Inject a human-in-loop checkpoint (interrupt_before, confirmation prompt, or dry-run flag) before any tool call with write effects. For automated pipelines, restrict write tools to a sandboxed staging environment until human approval.</rule>
    </antipattern>
  </section>

  <section name="caching-gotchas">
    <antipattern name="cache-ttl-expiry">
      <description>Prompt Caching TTL is 5 minutes. Long gaps between requests invalidate the cache. Applications with fewer than ~12 requests per hour may never benefit from caching.</description>
      <reason>The cache TTL is fixed and not configurable. Low-traffic services pay cache write overhead on every cold request.</reason>
      <rule>Profile cache hit rate via usage.cache_read_input_tokens before committing to a caching strategy. If hit rate is below 50%, the caching overhead may not justify the savings.</rule>
    </antipattern>

    <antipattern name="cache-prefix-mismatch">
      <description>A cached block is prepended with a dynamic prefix (timestamp, request ID, user name) that changes between requests, invalidating the cache on every call.</description>
      <reason>Cache matching is prefix-based. Any content before the cached block that changes between requests breaks the prefix.</reason>
      <rule>Place all dynamic content (user message, retrieved context) after the cached block. Static content — system instructions, tool schemas, long documents — must appear before dynamic content in the message array.</rule>
    </antipattern>
  </section>

  <section name="thinking-gotchas">
    <antipattern name="silent-thinking-billing">
      <description>Streaming handler only listens for text_delta events. thinking blocks arrive before text blocks as thinking_delta events. The handler discards thinking tokens while still paying for them.</description>
      <reason>Extended Thinking tokens are billed at the full model input rate. Ignoring them in the handler does not skip their billing.</reason>
      <rule>Always iterate response.content (or handle thinking_delta in streaming) and extract both block types. Log thinking token count from usage.thinking_tokens to track cost.</rule>
    </antipattern>

    <antipattern name="thinking-on-short-output">
      <description>Extended Thinking is enabled for a task that requires a short answer (under 200 tokens). The minimum 1,024-token budget makes every call expensive regardless of response length.</description>
      <reason>Budget tokens are always billed even if the model does not use the full budget.</reason>
      <rule>Reserve Extended Thinking for tasks where accuracy improvement is measurable: math proofs, multi-step code debugging, research synthesis, contract analysis. Do not apply it to classification, summarization, or FAQ tasks.</rule>
    </antipattern>
  </section>

  <section name="operational-gotchas">
    <antipattern name="hardcoded-api-key">
      <description>ANTHROPIC_API_KEY is hardcoded in source code or committed to version control. Key exposure leads to unauthorized usage and billing.</description>
      <reason>API keys in code are frequently leaked via git history, container images, or CI logs.</reason>
      <rule>Load keys exclusively from environment variables or a secrets manager. Rotate keys on a schedule. Add ANTHROPIC_API_KEY to .gitignore scanning (e.g., truffleHog, detect-secrets).</rule>
    </antipattern>

    <antipattern name="no-retry-on-rate-limit">
      <description>Agent makes parallel requests without exponential backoff. HTTP 429 responses crash the loop instead of being retried.</description>
      <reason>Default tier rate limits (requests/minute, tokens/minute) are hit quickly in parallel agentic loops.</reason>
      <rule>Implement exponential backoff with jitter: wait = min(2**attempt + random.uniform(0, 1), 60). Use the SDK's built-in retry support or a decorator. Maximum 5 retries before propagating the error.</rule>
    </antipattern>

    <note>Batch API has a 24-hour processing window. If a batch is submitted and polling stops (process crash, restart), retrieve the batch ID from storage and resume polling. Do not resubmit the batch — it results in duplicate processing and double billing.</note>
  </section>
</text>
