<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="AI Video Generation Provider Selection Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Root question + 5 branches that route to rule ids in 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-root">Does the task require lip-sync OR native audio OR 4K?</root-question>
    <branch when="requires lip_sync == true">
      <conclusion ref="runway-when" verdict="apply rule runway-when"/>
    </branch>
    <branch when="requires native_audio == true OR requires resolution &gt;= 4K">
      <conclusion ref="veo-when" verdict="apply rule veo-when"/>
    </branch>
    <branch when="requires duration_s &gt; 10 AND chatgpt_pro_available == true">
      <conclusion ref="sora-when" verdict="apply rule sora-when"/>
    </branch>
    <branch when="requires custom_finetune == true OR self_host == true">
      <conclusion ref="replicate-when" verdict="apply rule replicate-when"/>
    </branch>
    <branch when="none of the above (default text-to-video, ≤10s)">
      <conclusion ref="luma-when" verdict="apply rule luma-when"/>
    </branch>
  </decision-tree>
</text>
