<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="llm-observability-stack Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for llm-observability-stack. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Choose Langfuse for self-hosted production; Helicone for cost-first optimization; LangSmith only if already on LangChain.</statement>
      <rationale source="v1-source">Langfuse is MIT-licensed, actively maintained, and covers tracing + evals in one tool. Helicone adds a proxy layer that tracks costs without code changes. LangSmith's value is its deep LangGraph integration; outside that stack it adds a vendor dependency for little gain.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Instrument via OpenTelemetry and route to any backend — never use vendor SDKs as the primary tracing layer.</statement>
      <rationale source="v1-source">OTEL gives vendor portability. Switching from Langfuse to Datadog requires only a config change when using OTEL exporters; it requires a full re-instrument when using vendor-specific SDKs.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Attach user_id, session_id, and feature_flag to every LLM span at call time — not post-hoc.</statement>
      <rationale source="v1-source">Post-hoc enrichment requires a join on a secondary store. Inline attribution enables per-user cost breakdown, session replay, and A/B comparison directly in the tracing UI.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Log both input and output token counts per call; never derive cost from character count alone.</statement>
      <rationale source="v1-source">Character count diverges from token count for non-ASCII content, code, and structured JSON. Token counts are available directly from provider responses and are the only reliable cost signal.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Alert on these baselines: error rate &gt;5%, P99 latency &gt;10s, TTFT P95 &gt;2s, hallucination rate &gt;10%, daily spend &gt;80% of budget.</statement>
      <rationale source="v1-source">These thresholds represent practical production baselines. Lower thresholds create alert fatigue; higher thresholds miss meaningful degradation. Tune to your SLA after establishing a 2-week baseline.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Create a parent trace per agent invocation and nest all tool calls as child spans — never emit flat independent spans.</statement>
      <rationale source="v1-source">Flat spans make it impossible to reconstruct reasoning chains or correlate tool results with the LLM decision that triggered them. Hierarchical spans enable full session replay.</rationale>
    </rule>
  </section>
</text>
