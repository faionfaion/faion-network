<?xml version="1.0" encoding="UTF-8"?>
<text id="01-frameworks" title="Multi-Agent Frameworks and Architectures">
  <summary>Framework comparison, architecture patterns, and selection decision tree for multi-agent LLM systems.</summary>

  <section title="Framework Comparison">
    <list>
      <item>LangGraph: fastest execution (2.2x faster than CrewAI), explicit state control, graph-based. Use for complex workflows with conditional branching. Steep learning curve.</item>
      <item>CrewAI: role-based teams, minimal setup, production-ready. Use for structured workflows with clear agent roles.</item>
      <item>AutoGen (Microsoft): conversational multi-agent, group chat. Higher latency. Use for research/flexible collaboration.</item>
      <item>OpenAI Swarm: lightweight handoffs, prototyping only. Limited features, not for production.</item>
    </list>

    <rule>
      <statement>Select framework by decision tree: need explicit state + complex branching → LangGraph; want role-based teams with minimal setup → CrewAI; need conversational collaboration → AutoGen; prototype only → Swarm. Default: LangGraph.</statement>
      <rationale>LangGraph's explicit state machine model is the most debuggable in production. Other frameworks trade flexibility for ease of setup but limit customization for complex workflows.</rationale>
    </rule>
  </section>

  <section title="Architecture Patterns">
    <rule>
      <statement>Hierarchical (Manager → Workers): use for complex projects with clear task decomposition. Collaborative/P2P: use for creative work and iterative refinement. Sequential pipeline: use for linear workflows and content processing.</statement>
      <rationale>Architecture pattern determines coordination overhead, parallelism, and failure isolation. Hierarchical provides the best failure containment — a worker failure doesn't cascade to other workers.</rationale>
    </rule>

    <rule>
      <statement>Use separate model tiers: orchestrator → Claude Opus; workers → Sonnet; formatters/classifiers → Haiku. Never use Opus for worker tasks.</statement>
      <rationale>Orchestrators make routing decisions requiring broad knowledge; workers execute specific, well-defined tasks. Using Opus for formatting wastes $15/M tokens on a task Haiku handles at $0.80/M.</rationale>
    </rule>
  </section>

  <section title="Communication Patterns">
    <list>
      <item>Direct message: point-to-point delegation between two agents</item>
      <item>Shared state file: all agents read/write a structured state document</item>
      <item>Queue: pub/sub for loose coupling; Redis/Valkey for production</item>
      <item>Group chat: multi-party conversation for collaborative reasoning (AutoGen pattern)</item>
    </list>

    <rule>
      <statement>For sequential pipelines, chain outputs as explicit inputs to the next stage. Never rely on shared memory or conversation history between agents — always serialize structured state.</statement>
      <rationale>Shared memory introduces race conditions and context window fragmentation. Serialized structured state is explicit, auditable, and restartable from any checkpoint.</rationale>
    </rule>
  </section>
</text>
