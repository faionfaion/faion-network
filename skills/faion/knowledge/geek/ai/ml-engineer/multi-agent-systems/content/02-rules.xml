<?xml version="1.0" encoding="UTF-8"?>
<text id="02-rules" title="Multi-Agent System Rules and Failure Modes">
  <summary>State management rules, orchestrator constraints, production guardrails, and multi-agent failure modes.</summary>

  <section title="State Management Rules">
    <rule>
      <statement>Use immutable state updates: agents append to state, never overwrite previous stages. Use PostgresSaver for LangGraph in production — never InMemorySaver (lost on restart).</statement>
      <rationale>Overwriting state prevents replay and debugging. In-memory state is lost on any crash or restart, which will happen in production.</rationale>
    </rule>

    <rule>
      <statement>Validate the output schema of each agent before passing to the next stage. Re-validate on receipt even if the sending agent validated on emit — serialization can corrupt data.</statement>
      <rationale>Schema drift between agents is the primary source of silent multi-agent failures. Each agent boundary is a contract; validate both sides.</rationale>
    </rule>

    <rule>
      <statement>Set `max_tokens` on each agent AND a total spend cap for the pipeline. Implement a global timeout watchdog that terminates the pipeline after N minutes.</statement>
      <rationale>Without per-agent token caps, a single stuck agent can exhaust the entire pipeline budget. A global watchdog prevents infinite loops that evade individual caps.</rationale>
    </rule>
  </section>

  <section title="Orchestrator Constraints">
    <rule>
      <statement>Limit orchestrators to &lt;=4 workers. Above 4, split into sub-orchestrators. Orchestrators with more workers lose coherence after 5+ exchanges.</statement>
      <rationale>LLM orchestrators experience prompt drift — original goals become diluted after many exchanges. Sub-orchestrators create scoped contexts that stay focused.</rationale>
    </rule>

    <rule>
      <statement>Keep orchestrators stateless: orchestrators route and synthesize only. Workers maintain their own state. Never have the orchestrator do the work itself.</statement>
      <rationale>Stateful orchestrators become bottlenecks and create shared-state race conditions when multiple workers report results simultaneously.</rationale>
    </rule>
  </section>

  <section title="Failure Modes">
    <antipattern>
      <description>Tool abuse loops: agents calling tools in cycles when stuck, running up costs without making progress.</description>
      <reason>Agents without iteration limits can call the same failing tool indefinitely. Implement circuit breakers and per-agent maximum iteration counts.</reason>
    </antipattern>

    <antipattern>
      <description>Context window fragmentation: sub-agents with small contexts miss critical info passed by the orchestrator as a summary.</description>
      <reason>Summaries lose precision. Worker agents must receive full task context, not a summary — summaries cause hallucinated assumptions.</reason>
    </antipattern>

    <antipattern>
      <description>Persona collapse in peer-to-peer: agents with similar prompts converge on agreement rather than productive disagreement.</description>
      <reason>For adversarial (generator/critic) patterns, prompts must explicitly differentiate roles and instruct the critic to actively challenge. Similar prompts produce echo chambers.</reason>
    </antipattern>

    <antipattern>
      <description>Destructive tool call without human-in-loop: agent executes file delete, API POST, or DB write without human confirmation.</description>
      <reason>Autonomous agents make mistakes. Irreversible operations require a confirmation gate. This is not optional for production systems.</reason>
    </antipattern>
  </section>

  <reference path="templates/sequential-pipeline.py">Minimal 3-agent sequential pipeline</reference>
  <reference path="templates/orchestrator-prompt.txt">Stateless orchestrator prompt pattern</reference>
</text>
