<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Multi-Agent Systems — Core Rules" version="1.0.0" est_tokens="900" depth="essential">
  <summary>Five testable rules that gate any application of the multi-agent-systems methodology. Sources: Anthropic — Building Effective Agents; Park et al. — Generative Agents (arXiv:2304.03442); LangGraph state-machine docs.</summary>

  <section title="Rule set">
    <rule id="r1-select-framework-decision" testable="true" depth="essential">
      <statement>Select framework by decision tree: need explicit state + complex branching → LangGraph; want role-based teams with minimal setup → CrewAI; need conversational collaboration → AutoGen; prototype only → Swarm. Default: LangGraph.</statement>
      <rationale source="Anthropic — Building Effective Agents">LangGraph's explicit state machine model is the most debuggable in production. Other frameworks trade flexibility for ease of setup but limit customization for complex workflows.</rationale>
    </rule>

    <rule id="r2-hierarchical-manager-workers" testable="true" depth="essential">
      <statement>Hierarchical (Manager → Workers): use for complex projects with clear task decomposition. Collaborative/P2P: use for creative work and iterative refinement. Sequential pipeline: use for linear workflows and content processing.</statement>
      <rationale source="Park et al. — Generative Agents (arXiv:2304.03442)">Architecture pattern determines coordination overhead, parallelism, and failure isolation. Hierarchical provides the best failure containment — a worker failure doesn't cascade to other workers.</rationale>
    </rule>

    <rule id="r3-separate-model-tiers" testable="true" depth="essential">
      <statement>Use separate model tiers: orchestrator → Claude Opus; workers → Sonnet; formatters/classifiers → Haiku. Never use Opus for worker tasks.</statement>
      <rationale source="LangGraph state-machine docs">Orchestrators make routing decisions requiring broad knowledge; workers execute specific, well-defined tasks. Using Opus for formatting wastes $15/M tokens on a task Haiku handles at $0.80/M.</rationale>
    </rule>

    <rule id="r4-sequential-pipelines-chain" testable="true" depth="essential">
      <statement>For sequential pipelines, chain outputs as explicit inputs to the next stage. Never rely on shared memory or conversation history between agents — always serialize structured state.</statement>
      <rationale source="OpenAI Swarm / Agents SDK">Shared memory introduces race conditions and context window fragmentation. Serialized structured state is explicit, auditable, and restartable from any checkpoint.</rationale>
    </rule>

    <rule id="r5-immutable-state-updates" testable="true" depth="essential">
      <statement>Use immutable state updates: agents append to state, never overwrite previous stages. Use PostgresSaver for LangGraph in production — never InMemorySaver (lost on restart).</statement>
      <rationale source="Datadog — harness-first agents">Overwriting state prevents replay and debugging. In-memory state is lost on any crash or restart, which will happen in production.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
