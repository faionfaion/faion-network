<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="prompt-engineering-fundamentals Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for prompt-engineering-fundamentals. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every prompt MUST include at minimum: a task description (what to do) and an output format specification (what structure is required). Context, system role, examples, and constraints are added only when they measurably improve reliability.</statement>
      <rationale source="v1-source">Minimal prompts are easier to debug. Adding components one at a time lets you isolate which change caused a quality improvement or regression.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Start with zero-shot. Add few-shot examples only when zero-shot produces wrong patterns or formatting. Add chain-of-thought only when reasoning is demonstrably failing. Escalate complexity only after the simpler approach fails.</statement>
      <rationale source="v1-source">Each technique adds tokens and latency. The cheapest technique that achieves the target quality is the right one for production.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Few-shot examples MUST: cover representative diversity, include at least one edge case, follow a consistent format, and number no more than 2–5. Conflicting examples MUST NOT appear in the same prompt.</statement>
      <rationale source="v1-source">Too many examples waste tokens and can confuse the model by emphasizing the pattern over the instruction. Consistent format prevents the model from averaging conflicting signals.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>For Claude, MUST use XML tags to delimit sections (context, task, output) and user input. For GPT-4, MUST use markdown headers and numbered lists. NEVER use Claude-style XML tags in a GPT-4 prompt without testing — the model may treat them as literal text.</statement>
      <rationale source="v1-source">Instruction-following behavior is trained differently per model family. Structural conventions that boost Claude reliability may be neutral or negative for GPT-4.</rationale>
    </rule>
  </section>
</text>
