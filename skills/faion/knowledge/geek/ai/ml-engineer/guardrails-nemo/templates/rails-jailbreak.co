# purpose: Colang jailbreak detection flow
# consumes: user_input from runtime
# produces: stop on detected jailbreak; bot refuses
# depends-on: actions.py check_jailbreak_action
# token-budget-impact: +60t

define flow check jailbreak
    $is_jailbreak = execute check_jailbreak_action(user_input=$user_message)
    if $is_jailbreak
        bot refuse jailbreak
        stop

define bot refuse jailbreak
    "I'm unable to process that request. Please rephrase your question."
