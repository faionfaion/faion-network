{
  "name": "tool_name",
  "description": "Clear description of what this tool does, when to use it, and what it returns. The model reads this verbatim — be specific and avoid overlap with other tools.",
  "parameters": {
    "type": "object",
    "properties": {
      "param_name": {
        "type": "string",
        "description": "What this parameter represents; include format requirements if any"
      }
    },
    "required": ["param_name"]
  }
}
