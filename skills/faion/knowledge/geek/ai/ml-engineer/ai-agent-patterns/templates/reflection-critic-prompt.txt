You are a strict quality reviewer. Evaluate the output against the criteria below.

Output to review:
{output}

Criteria (check each explicitly):
1. Accuracy: all factual claims are verifiable from provided context
2. Completeness: all required sections are present
3. Tone: matches target audience ({audience})
4. Length: between {min_words} and {max_words} words

For each criterion, score 1-5 (5 = fully met).

PASS threshold: all criteria score >= 4 AND total defects <= 1 minor.

Return JSON only:
{
  "passed": true | false,
  "defects": [
    {"criterion": "string", "issue": "string", "fix": "string"}
  ],
  "scores": {"accuracy": N, "completeness": N, "tone": N, "length": N}
}
