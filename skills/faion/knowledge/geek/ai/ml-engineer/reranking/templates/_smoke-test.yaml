# purpose: minimum-viable reranker.yaml fixture
# consumes: nothing
# produces: spec instance for --self-test
# depends-on: reranker.schema.yaml
# token-budget-impact: 0 at runtime
provider: cohere
model: rerank-multilingual-v3.0
top_n_input: 20
top_k_output: 5
latency_budget_ms: 350
fallback:
  strategy: local-bge
  model: BAAI/bge-reranker-base
eval_evidence:
  set_path: evals/rerank-support-kb.jsonl
  sample_size: 84
  precision_lift: 0.21
