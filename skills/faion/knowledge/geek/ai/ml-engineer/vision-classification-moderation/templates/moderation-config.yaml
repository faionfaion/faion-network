# purpose: Cascade thresholds + category map skeleton
# consumes: domain knowledge inputs (see Prerequisites)
# produces: filled-in config matching 02-output-contract schema
# depends-on: 01-core-rules.xml constraints
# token-budget-impact: ~150 tokens loaded as context

# vision-classification-moderation — config skeleton
version: 1.0.0
slug: vision-classification-moderation
fields:
  - name: example-field
    type: string
    required: true
defaults: {}
