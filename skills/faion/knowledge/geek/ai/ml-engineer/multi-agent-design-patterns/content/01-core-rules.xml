<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="multi-agent-design-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for multi-agent-design-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use Supervisor when tasks map to distinct worker domains and centralized control is acceptable.</statement>
      <rationale source="v1-source">A single coordinator routes requests to specialized workers via LLM reasoning or rule-based matching. Predictable, debuggable, but creates a single point of failure.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use Hierarchical when the problem requires recursive decomposition across organizational units; limit to 2–3 levels to avoid coordination overhead.</statement>
      <rationale source="v1-source">Multi-level tree where higher agents decompose goals into sub-tasks for lower agents. Scales well but each coordination layer adds 500ms+ of LLM call overhead.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Use Sequential for linear workflows where stage N depends on stage N-1; use output_key per stage to pass typed state.</statement>
      <rationale source="v1-source">Assembly-line pipeline: each agent reads the previous stage output from shared state and writes its result under a named key. Deterministic, easy to debug, but no fault isolation — failure at any stage blocks all downstream stages.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use Peer-to-Peer only when resilience and fault tolerance outweigh debugging complexity; enforce DAG structure to prevent circular routing.</statement>
      <rationale source="v1-source">Decentralized architecture where agents communicate directly without a central orchestrator. High fault tolerance but requires distributed tracing to understand message flow.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Define clear agent role boundaries and tool access permissions before writing any prompts or code.</statement>
      <rationale source="v1-source">Tool access leakage in frameworks like AutoGen lets agents see tool results intended for others; scoping prevents this.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Block deployment if any of these gates fails: role boundaries undefined, no distributed tracing, no max_iterations guard, no fallback path for primary agent failure.</statement>
      <rationale source="v1-source">Multi-agent failures without traces are nearly impossible to debug; loops without guards drain budgets silently.</rationale>
    </rule>
  </section>
</text>
