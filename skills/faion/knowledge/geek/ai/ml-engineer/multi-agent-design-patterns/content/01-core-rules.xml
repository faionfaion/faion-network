<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Multi-Agent Design Patterns — Core Rules" version="1.0.0" est_tokens="900" depth="essential">
  <summary>Five testable rules that gate any application of the multi-agent-design-patterns methodology. Sources: Anthropic — Building Effective Agents (engineering blog 2024); OpenAI Agents SDK documentation; LangGraph multi-agent patterns.</summary>

  <section title="Rule set">
    <rule id="r1-supervisor-when-tasks" testable="true" depth="essential">
      <statement>Use Supervisor when tasks map to distinct worker domains and centralized control is acceptable.</statement>
      <rationale source="Anthropic — Building Effective Agents (engineering blog 2024)">A single coordinator routes requests to specialized workers via LLM reasoning or rule-based matching. Predictable, debuggable, but creates a single point of failure.</rationale>
    </rule>

    <rule id="r2-hierarchical-when-problem" testable="true" depth="essential">
      <statement>Use Hierarchical when the problem requires recursive decomposition across organizational units; limit to 2–3 levels to avoid coordination overhead.</statement>
      <rationale source="OpenAI Agents SDK documentation">Multi-level tree where higher agents decompose goals into sub-tasks for lower agents. Scales well but each coordination layer adds 500ms+ of LLM call overhead.</rationale>
    </rule>

    <rule id="r3-sequential-linear-workflows" testable="true" depth="essential">
      <statement>Use Sequential for linear workflows where stage N depends on stage N-1; use output_key per stage to pass typed state.</statement>
      <rationale source="LangGraph multi-agent patterns">Assembly-line pipeline: each agent reads the previous stage output from shared state and writes its result under a named key. Deterministic, easy to debug, but no fault isolation — failure at any stage blocks all downstream stages.</rationale>
    </rule>

    <rule id="r4-peer-peer-only" testable="true" depth="essential">
      <statement>Use Peer-to-Peer only when resilience and fault tolerance outweigh debugging complexity; enforce DAG structure to prevent circular routing.</statement>
      <rationale source="AutoGen multi-agent conversation framework">Decentralized architecture where agents communicate directly without a central orchestrator. High fault tolerance but requires distributed tracing to understand message flow.</rationale>
    </rule>

    <rule id="r5-define-clear-agent" testable="true" depth="essential">
      <statement>Define clear agent role boundaries and tool access permissions before writing any prompts or code.</statement>
      <rationale source="CrewAI hierarchical-process docs">Tool access leakage in frameworks like AutoGen lets agents see tool results intended for others; scoping prevents this.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
