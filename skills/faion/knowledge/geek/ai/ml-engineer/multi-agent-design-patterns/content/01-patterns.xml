<?xml version="1.0" encoding="UTF-8"?>
<text id="01-patterns" title="Multi-Agent Design Patterns — Core Four">
  <summary>Supervisor, Hierarchical, Sequential, and Peer-to-Peer patterns with use cases, tradeoffs, and selection criteria.</summary>

  <section title="Supervisor Pattern">
    <rule>
      <statement>Use Supervisor when tasks map to distinct worker domains and centralized control is acceptable.</statement>
      <rationale>A single coordinator routes requests to specialized workers via LLM reasoning or rule-based matching. Predictable, debuggable, but creates a single point of failure.</rationale>
    </rule>
    <p>Supported by: LangGraph (StateGraph + supervisor node), CrewAI (manager agent), AutoGen (GroupChat), Google ADK (transfer_to_agent).</p>
    <list>
      <item>Good for: multi-domain help desks, intelligent routing, centralized monitoring</item>
      <item>Risk: supervisor bottleneck; LLM routing is probabilistic — ambiguous inputs can route incorrectly</item>
    </list>
  </section>

  <section title="Hierarchical Pattern">
    <rule>
      <statement>Use Hierarchical when the problem requires recursive decomposition across organizational units; limit to 2–3 levels to avoid coordination overhead.</statement>
      <rationale>Multi-level tree where higher agents decompose goals into sub-tasks for lower agents. Scales well but each coordination layer adds 500ms+ of LLM call overhead.</rationale>
    </rule>
    <list>
      <item>Good for: complex enterprise workflows with cross-functional teams, large report generation</item>
      <item>Risk: "middle management" latency; difficult to debug across levels without distributed tracing</item>
    </list>
  </section>

  <section title="Sequential Pattern">
    <rule>
      <statement>Use Sequential for linear workflows where stage N depends on stage N-1; use output_key per stage to pass typed state.</statement>
      <rationale>Assembly-line pipeline: each agent reads the previous stage output from shared state and writes its result under a named key. Deterministic, easy to debug, but no fault isolation — failure at any stage blocks all downstream stages.</rationale>
    </rule>
    <p>Supported by: Google ADK (SequentialAgent), LangGraph (linear graph edges), CrewAI (sequential process).</p>
    <list>
      <item>Good for: ETL pipelines, document transformation chains (parse → extract → summarize → format)</item>
      <item>Risk: no parallelism; single failure stops the pipeline</item>
    </list>
  </section>

  <section title="Peer-to-Peer Pattern">
    <rule>
      <statement>Use Peer-to-Peer only when resilience and fault tolerance outweigh debugging complexity; enforce DAG structure to prevent circular routing.</statement>
      <rationale>Decentralized architecture where agents communicate directly without a central orchestrator. High fault tolerance but requires distributed tracing to understand message flow.</rationale>
    </rule>
    <list>
      <item>Good for: cross-organizational collaboration, privacy-preserving systems, adaptive agent networks</item>
      <item>Risk: circular dependency potential; global state is hard to reason about</item>
    </list>
  </section>

  <section title="Pattern Comparison">
    <p>Control: Supervisor=centralized, Hierarchical=multi-level, Sequential=linear, P2P=distributed. Fault tolerance: Supervisor=low, Hierarchical=medium, Sequential=low, P2P=high. Debugging: Sequential=easiest, P2P=hardest.</p>
  </section>

  <section title="Additional Patterns">
    <list>
      <item>Parallel Fan-Out/Gather — spawn concurrent agents, synthesize results</item>
      <item>Generator-Critic — one agent creates, another validates; run at least 2 critique passes</item>
      <item>Loop (Iterative Refinement) — repeated improvement until criteria met; always set max_iterations</item>
      <item>Human-in-the-Loop — explicit approval checkpoints in graph; must be designed in, not retrofitted</item>
      <item>Router — dynamic tool/agent selection based on input classification</item>
    </list>
  </section>

  <section title="Critical Gotchas">
    <antipattern>
      <description>Infinite loop: supervisor routes to agent A, which routes back to supervisor, which routes to A again.</description>
      <reason>Without a max_steps guard the loop never terminates. Set MAX_ITER and return a sentinel message to the user when reached.</reason>
    </antipattern>
    <antipattern>
      <description>Token explosion: each agent forwards its full conversation history to the next agent.</description>
      <reason>History accumulates multiplicatively across multi-agent calls. Summarize or trim conversation history between agent steps.</reason>
    </antipattern>
    <antipattern>
      <description>Parallel fan-out agents writing to shared state simultaneously.</description>
      <reason>Race conditions corrupt state. Use merge nodes with explicit conflict resolution.</reason>
    </antipattern>
  </section>

  <reference path="templates/langgraph-supervisor.py">Minimal LangGraph supervisor implementation</reference>
  <reference path="templates/pipeline-state.py">Typed state models for all four patterns</reference>
</text>
