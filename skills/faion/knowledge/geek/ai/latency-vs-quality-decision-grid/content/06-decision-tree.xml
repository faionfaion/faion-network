<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Latency vs Quality Decision Grid Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Are there ≥3 distinct LLM call sites with different user-facing latency / quality expectations?</root-question>
    <branch when="call_sites.distinct &gt;= 3">
      <question>Is per-site latency telemetry AND a quality eval score available?</question>
      <branch when="latency.measurable == true AND quality.measurable == true">
        <conclusion ref="run-the-checklist"/>
      </branch>
      <branch when="latency.measurable == false OR quality.measurable == false">
        <conclusion ref="r2"/>
      </branch>
    </branch>
    <branch when="call_sites.distinct &lt; 3">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="3 sites (autocomplete, chat, audit), telemetry + eval present → run-the-checklist"/>
  </decision-tree>
</text>
