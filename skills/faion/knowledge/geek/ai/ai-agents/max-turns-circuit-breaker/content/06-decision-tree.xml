<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="max-turns-circuit-breaker Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: agent_kind, per_turn_cost_usd)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'agent_kind' meets the upper threshold?">
          <branch when="agent_kind == 'high' or agent_kind &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'per_turn_cost_usd' present and matches advanced branch?">
              <branch when="per_turn_cost_usd == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="per_turn_cost_usd != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="agent_kind == 'low' or agent_kind &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, agent_kind=high, per_turn_cost_usd=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
