<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="llamaindex-agents-eval Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: reasoning_style, tools_count, eval_budget_usd, latency_target_ms)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'reasoning_style' meets the upper threshold?">
          <branch when="reasoning_style == 'high' or reasoning_style &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'tools_count' present and matches advanced branch?">
              <branch when="tools_count == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="tools_count != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="reasoning_style == 'low' or reasoning_style &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, reasoning_style=high, tools_count=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
