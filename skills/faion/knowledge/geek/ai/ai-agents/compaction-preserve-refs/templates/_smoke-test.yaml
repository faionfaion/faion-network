# purpose: smallest valid compacted state for self-test
# consumes: nothing
# produces: example compacted state (yaml)
# depends-on: schema in content/02-output-contract.xml
# token-budget-impact: ~120 tokens
goal: smoke test the compaction validator
files_touched:
  - scripts/validate-compaction-preserve-refs.py:1
decisions:
  - emit minimum viable state with every required key populated
errors_open: []
refs:
  - {kind: log, id: ".agent/results/smoke.log"}
next_action: run validator with --self-test
