<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="compaction-preserve-refs Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for compaction-preserve-refs. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Compacted output MUST preserve verbatim: absolute or workspace-relative file paths, fully-qualified function/class names, error message strings, URLs, IDs (PR numbers, ticket IDs, execution IDs), explicit decisions ("chose X over Y because Z").</statement>
      <rationale source="v1-source">These are pointers the agent and its downstream tools dereference. Paraphrasing breaks the dereference (a wrong path is worse than no path).</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Compacted output MUST drop: intermediate reasoning chains, tool-call argument repetition, verbatim tool output bodies, and conversational filler ("let me think", "ok", "as we discussed").</statement>
      <rationale source="v1-source">Reasoning is regenerable from inputs; tool output is restorable from disk via path. Keeping them inflates the compacted blob and squeezes the surviving references.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>The compacted state MUST be valid YAML with these top-level keys: goal, files_touched (list), decisions (list), errors_open (list), refs (list of {kind, id}), next_action (one sentence). NO free-text "summary" field.</statement>
      <rationale source="v1-source">Fixed keys make the output machine-checkable; missing one key is a hard failure the orchestrator catches before the agent acts on bad state.</rationale>
    </rule>
  </section>
</text>
