<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="map-reduce-send-fanout Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: item_count, mutates_shared_state, idempotent, framework)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'item_count' meets the upper threshold?">
          <branch when="item_count == 'high' or item_count &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'mutates_shared_state' present and matches advanced branch?">
              <branch when="mutates_shared_state == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="mutates_shared_state != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="item_count == 'low' or item_count &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, item_count=high, mutates_shared_state=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
