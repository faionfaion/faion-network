# purpose: minimum-viable filled-in conversation profile
# consumes: nothing — runnable as-is through the methodology
# produces: deterministic decision-record (buffer + redis)
# depends-on: conversation-profile.yaml template
# token-budget-impact: ~80 tokens
expected_turns: 6
recall_pattern: recency
entity_focus: false
ttl_seconds: 3600
store_backend: redis
session_id_strategy: uuid
target_env: production
