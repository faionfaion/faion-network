# purpose: input contract for mcp-transport-stdio-vs-http methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

multi_tenant: TODO          # fill with concrete value per the driver definition
auth_required: TODO          # fill with concrete value per the driver definition
client_targets: TODO          # fill with concrete value per the driver definition
