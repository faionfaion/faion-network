<!--
purpose: human-readable wrapper around the schema-field-order JSON spec
consumes: validated spec.json from the output contract
produces: rendered markdown for code review
depends-on: 02-output-contract.xml schema
token-budget-impact: ~250 tokens
-->

# Schema Field Order Spec

## Drivers
- `answer_quality_floor`: `<value>`
- `latency_budget_ms`: `<value>`
- `schema_modifiable`: `<value>`

## Decision
- `field_order`: `<value>`
- `reasoning_field_name`: `<value>`
- `schema_strict`: `<value>`

## Audit
Rules consulted: `<r-list>` — see content/01-core-rules.xml.
