<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="llamaindex-hybrid-retrieval Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: corpus_heterogeneity, latency_target_ms, rerank_budget_usd)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'corpus_heterogeneity' meets the upper threshold?">
          <branch when="corpus_heterogeneity == 'high' or corpus_heterogeneity &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'latency_target_ms' present and matches advanced branch?">
              <branch when="latency_target_ms == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="latency_target_ms != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="corpus_heterogeneity == 'low' or corpus_heterogeneity &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, corpus_heterogeneity=high, latency_target_ms=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
