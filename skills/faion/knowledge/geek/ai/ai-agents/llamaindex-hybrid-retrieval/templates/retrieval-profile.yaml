# purpose: input contract for llamaindex-hybrid-retrieval methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

corpus_heterogeneity: TODO          # fill with concrete value per the driver definition
latency_target_ms: TODO          # fill with concrete value per the driver definition
rerank_budget_usd: TODO          # fill with concrete value per the driver definition
