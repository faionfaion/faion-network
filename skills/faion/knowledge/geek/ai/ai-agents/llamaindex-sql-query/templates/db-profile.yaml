# purpose: input contract for llamaindex-sql-query methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

table_count: TODO          # fill with concrete value per the driver definition
schema_size_chars: TODO          # fill with concrete value per the driver definition
read_only_required: TODO          # fill with concrete value per the driver definition
