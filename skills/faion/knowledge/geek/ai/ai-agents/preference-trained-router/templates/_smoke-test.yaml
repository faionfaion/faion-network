# purpose: minimum-viable router spec for preference-trained-router
# consumes: nothing
# produces: deterministic deploy-router decision-record
# depends-on: nothing
# token-budget-impact: ~70 tokens

drivers:
  prompt_volume_per_day: 5000
  weak_strong_cost_ratio: 15
  preference_data_size: 4000
  latency_budget_ms: 1500

weak_model_id: haiku
strong_model_id: opus
target_strong_accuracy: 0.9
