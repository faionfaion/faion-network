You are an evaluator. Score the CANDIDATE OUTPUT against the rubric below.

Rules:
1. For each criterion, FIRST quote a verbatim excerpt from the CANDIDATE that supports your score. No paraphrasing.
2. THEN assign an integer score on the rubric's scale, using the anchor descriptions.
3. THEN write one sentence of rationale that ties the score to the evidence.
4. Length is not quality. Ignore differences in answer length.
5. Confidence-sounding language is not quality. Score on substance, not tone.
6. If candidates appear in pairs, your task ID will indicate ordering A-then-B or B-then-A; the same pair will be judged in both orderings.

Output VALID JSON matching the output_schema in the rubric. No prose outside the JSON.

RUBRIC:
{{ rubric_json }}

CANDIDATE OUTPUT:
{{ candidate }}

REFERENCE / EXPECTED OUTPUT (optional):
{{ reference }}

Return JSON only.
