<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="llamaindex-basics Output Contract" version="2.0.0" est_tokens="800" depth="essential">
  <summary>Decision-record schema for llamaindex-basics. Every emitted record must validate against this JSON schema; both valid and invalid examples below.</summary>

  <output-contract produces="decision-record">
    <schema format="json-schema"><![CDATA[
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["slug", "version", "decision", "drivers", "audit"],
  "properties": {
    "slug": {"const": "llamaindex-basics"},
    "version": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "decision": {
      "type": "object",
      "required": ["loader", "node_parser", "chunk_size", "embedding_model", "vectorstore", "persistence_path"],
      "properties": {
        "loader": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "node_parser": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "chunk_size": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "embedding_model": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "vectorstore": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "persistence_path": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]}
      }
    },
    "drivers": {
      "type": "object",
      "required": ["doc_count", "avg_doc_chars", "query_pattern", "persistence_target"],
      "additionalProperties": true
    },
    "audit": {
      "type": "object",
      "required": ["rule_refs"],
      "properties": {"rule_refs": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^r[0-9]+$"}}}
    }
  }
}
]]></schema>
    <example valid="true" title="Canonical happy-path"><![CDATA[
{
  "slug": "llamaindex-basics",
  "version": "2.0.0",
  "decision": {"loader": "TODO", "node_parser": "TODO", "chunk_size": "TODO", "embedding_model": "TODO", "vectorstore": "TODO", "persistence_path": "TODO"},
  "drivers": {"doc_count": "val", "avg_doc_chars": "val", "query_pattern": "val", "persistence_target": "val"},
  "audit": {"rule_refs": ["r1"]}
}
]]></example>
    <example valid="false" title="Empty decision + empty rule_refs"><![CDATA[
{
  "slug": "llamaindex-basics",
  "version": "2.0.0",
  "decision": {},
  "drivers": {},
  "audit": {"rule_refs": []}
}
]]></example>
    <example-violations>
      <violation>decision missing required keys: "loader", "node_parser", "chunk_size", "embedding_model", "vectorstore", "persistence_path".</violation>
      <violation>drivers missing required keys.</violation>
      <violation>audit.rule_refs empty.</violation>
    </example-violations>
  </output-contract>

  <section title="Self-check checklist">
    <list>
      <item>JSON validates against the schema above.</item>
      <item>decision contains all required keys.</item>
      <item>drivers contains all listed driver fields.</item>
      <item>audit.rule_refs cites ≥1 rule from 01-core-rules.xml.</item>
    </list>
  </section>
</text>
