<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="llamaindex-ingestion-pipeline Output Contract" version="2.0.0" est_tokens="800" depth="essential">
  <summary>Decision-record schema for llamaindex-ingestion-pipeline. Every emitted record must validate against this JSON schema; both valid and invalid examples below.</summary>

  <output-contract produces="decision-record">
    <schema format="json-schema"><![CDATA[
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["slug", "version", "decision", "drivers", "audit"],
  "properties": {
    "slug": {"const": "llamaindex-ingestion-pipeline"},
    "version": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "decision": {
      "type": "object",
      "required": ["loader", "metadata_extractors", "node_parser", "vector_store", "docstore"],
      "properties": {
        "loader": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "metadata_extractors": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "node_parser": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "vector_store": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]},
        "docstore": {"type": ["string", "integer", "number", "boolean", "array", "object", "null"]}
      }
    },
    "drivers": {
      "type": "object",
      "required": ["doc_count", "update_frequency", "embedding_budget_usd"],
      "additionalProperties": true
    },
    "audit": {
      "type": "object",
      "required": ["rule_refs"],
      "properties": {"rule_refs": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^r[0-9]+$"}}}
    }
  }
}
]]></schema>
    <example valid="true" title="Canonical happy-path"><![CDATA[
{
  "slug": "llamaindex-ingestion-pipeline",
  "version": "2.0.0",
  "decision": {"loader": "TODO", "metadata_extractors": "TODO", "node_parser": "TODO", "vector_store": "TODO", "docstore": "TODO"},
  "drivers": {"doc_count": "val", "update_frequency": "val", "embedding_budget_usd": "val"},
  "audit": {"rule_refs": ["r1"]}
}
]]></example>
    <example valid="false" title="Empty decision + empty rule_refs"><![CDATA[
{
  "slug": "llamaindex-ingestion-pipeline",
  "version": "2.0.0",
  "decision": {},
  "drivers": {},
  "audit": {"rule_refs": []}
}
]]></example>
    <example-violations>
      <violation>decision missing required keys: "loader", "metadata_extractors", "node_parser", "vector_store", "docstore".</violation>
      <violation>drivers missing required keys.</violation>
      <violation>audit.rule_refs empty.</violation>
    </example-violations>
  </output-contract>

  <section title="Self-check checklist">
    <list>
      <item>JSON validates against the schema above.</item>
      <item>decision contains all required keys.</item>
      <item>drivers contains all listed driver fields.</item>
      <item>audit.rule_refs cites ≥1 rule from 01-core-rules.xml.</item>
    </list>
  </section>
</text>
