# purpose: minimum-viable profile that drives default branch
# consumes: nothing
# produces: langsmith + streaming + sample_rate=1.0
# depends-on: runtime-profile.yaml shape
# token-budget-impact: ~60 tokens
latency_target_ms: 400
cost_cap_per_turn_usd: 0.02
pii_posture: internal
tracing_backend: langsmith
project_name: smoke
sample_hint: 1.0
