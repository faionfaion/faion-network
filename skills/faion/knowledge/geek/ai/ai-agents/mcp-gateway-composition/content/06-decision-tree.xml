<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="mcp-gateway-composition Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: servers_count, tools_total, agent_clients_count, shared_concerns)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'servers_count' meets the upper threshold?">
          <branch when="servers_count == 'high' or servers_count &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'tools_total' present and matches advanced branch?">
              <branch when="tools_total == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="tools_total != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="servers_count == 'low' or servers_count &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, servers_count=high, tools_total=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
