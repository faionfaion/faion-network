<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="gateway-fallback-chain Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for gateway-fallback-chain. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Configure every production LLM call with a primary model AND at least one fallback model from a different provider; same-vendor fallbacks do not protect against vendor-wide incidents.</statement>
      <rationale source="v1-source">Anthropic, OpenAI, and Google have all had multi-hour outages affecting all of their models simultaneously; cross-provider fallbacks are the only path to availability above the single-vendor floor.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Surface 4xx client errors (400 bad request, 401 unauthenticated, 422 validation) without fallback; these are agent bugs, not provider incidents, and silently retrying masks them.</statement>
      <rationale source="v1-source">Falling back on a 400 means you ship a broken request to every provider in the chain, multiplying the failure surface and hiding the bug from observability.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>If the workload is subject to data-residency or compliance constraints, either self-host the gateway inside the same boundary or call the provider SDK directly and implement the fallback chain in application code.</statement>
      <rationale source="v1-source">Hosted gateways such as OpenRouter route data through their own infrastructure; this can break data-residency contracts and add a third-party processor to the audit scope.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>For any workload that depends on a vendor-specific feature (prompt caching, batch API, fine-tunes), call the vendor SDK directly and implement the fallback chain at the application layer; do not route those calls through a gateway.</statement>
      <rationale source="v1-source">The cost win from prompt caching (~90% on cached input) is far larger than the cost win from gateway routing for those workloads; routing through the gateway typically loses the cache.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Emit OTel GenAI spans for every gateway call with attributes for primary_model, used_model, fallback_count, and cost_usd; alert when fallback_count rises above a baseline.</statement>
      <rationale source="v1-source">Rising fallback rate is the leading indicator of vendor incidents and quota exhaustion; without per-call telemetry the first signal is angry users.</rationale>
    </rule>
  </section>
</text>
