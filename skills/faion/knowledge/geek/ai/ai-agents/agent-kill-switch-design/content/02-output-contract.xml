<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Agent Kill-Switch Design Output Contract" version="1.0.0" est_tokens="700" depth="essential">
  <summary>JSON-schema output contract the subagent applying agent-kill-switch-design must produce. Required fields, forbidden patterns, allowed transformations, plus valid + invalid worked examples.</summary>

  <section title="Output schema (JSON Schema draft-07)">
    <code lang="json"><![CDATA[{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "agent-kill-switch-design-output",
  "required": [
    "scopes",
    "e2e_kill_latency_p95_sec",
    "tested_at",
    "recovery_path",
    "structured_log_emitted",
    "forbidden_seen"
  ],
  "properties": {
    "scopes": {
      "description": "\u22653: global, per-tenant, per-tool."
    },
    "e2e_kill_latency_p95_sec": {
      "description": "\u22645."
    },
    "tested_at": {
      "description": "\u22651 staging test before GA."
    },
    "recovery_path": {
      "description": "Documented recovery procedure."
    },
    "structured_log_emitted": {
      "description": "True iff event log live."
    },
    "forbidden_seen": {
      "description": "Forbidden patterns matched (must be empty)."
    }
  }
}]]></code>
  </section>

  <output-contract>
    <required>
      <field name="scopes" format="array&lt;string&gt;" required="true">≥3: global, per-tenant, per-tool.</field>
      <field name="e2e_kill_latency_p95_sec" format="number" required="true">≤5.</field>
      <field name="tested_at" format="array&lt;ISO date&gt;" required="true">≥1 staging test before GA.</field>
      <field name="recovery_path" format="string" required="true">Documented recovery procedure.</field>
      <field name="structured_log_emitted" format="boolean" required="true">True iff event log live.</field>
      <field name="forbidden_seen" format="array&lt;string&gt;" required="true">Forbidden patterns matched (must be empty).</field>
    </required>
    <forbidden>
      <pattern id="f1">No single-scope kill switch.</pattern>
      <pattern id="f2">No untested kill switch in production.</pattern>
      <pattern id="f3">No recovery path missing from runbook.</pattern>
      <pattern id="f4">No silent kill (no structured event).</pattern>
    </forbidden>
    <allowed-transformations>
      <transformation>Order arrays deterministically by their natural key (slug, id, name) before serialising.</transformation>
      <transformation>Round numeric values to 2 decimal places when serialising.</transformation>
      <transformation>Normalize ISO dates to UTC (`YYYY-MM-DD`).</transformation>
    </allowed-transformations>
  </output-contract>

  <example type="good">
    <code lang="json"><![CDATA[{
  "scopes": [],
  "e2e_kill_latency_p95_sec": 1,
  "tested_at": [],
  "recovery_path": "example-value",
  "structured_log_emitted": true,
  "forbidden_seen": []
}]]></code>
    <note>Required fields populated; `forbidden_seen` empty.</note>
  </example>

  <example type="bad">
    <code lang="json"><![CDATA[{
  "e2e_kill_latency_p95_sec": 1,
  "tested_at": [],
  "recovery_path": "example-value",
  "structured_log_emitted": true,
  "forbidden_seen": [
    "f1"
  ]
}]]></code>
    <note>Missing the first required field and `forbidden_seen` is non-empty — schema rejects both.</note>
  </example>

  <section title="Self-check checklist">
    <list>
      <item>All required fields present and non-null.</item>
      <item>`forbidden_seen` is an empty array.</item>
      <item>Output validates against `scripts/validate-agent-kill-switch-design.py` exit 0.</item>
    </list>
  </section>
</text>
