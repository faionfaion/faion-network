# purpose: minimum-viable input for refusal-field-strict-schema
# consumes: nothing
# produces: deterministic default-branch decision-record
# depends-on: input yaml shape
# token-budget-impact: ~70 tokens

drivers:
  api_flavor: TODO
  production_refusal_rate: TODO
  downstream_consumer_count: TODO
