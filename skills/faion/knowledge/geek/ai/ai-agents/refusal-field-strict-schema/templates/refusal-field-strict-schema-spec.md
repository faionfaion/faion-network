<!--
purpose: human-readable wrapper around the refusal-field-strict-schema JSON spec
consumes: validated spec.json from the output contract
produces: rendered markdown for code review
depends-on: 02-output-contract.xml schema
token-budget-impact: ~250 tokens
-->

# Refusal Field Strict Schema Spec

## Drivers
- `api_flavor`: `<value>`
- `production_refusal_rate`: `<value>`
- `downstream_consumer_count`: `<value>`

## Decision
- `schema_path`: `<value>`
- `strict_mode`: `<value>`
- `refusal_branch_code`: `<value>`

## Audit
Rules consulted: `<r-list>` — see content/01-core-rules.xml.
