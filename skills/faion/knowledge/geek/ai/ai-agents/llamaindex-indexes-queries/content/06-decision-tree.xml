<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="llamaindex-indexes-queries Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: data_shape, hop_depth_max, query_types)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'data_shape' meets the upper threshold?">
          <branch when="data_shape == 'high' or data_shape &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'hop_depth_max' present and matches advanced branch?">
              <branch when="hop_depth_max == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="hop_depth_max != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="data_shape == 'low' or data_shape &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, data_shape=high, hop_depth_max=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
