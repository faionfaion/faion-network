# purpose: input contract for llamaindex-production-gotchas methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

async: TODO          # fill with concrete value per the driver definition
components_used: TODO          # fill with concrete value per the driver definition
cost_cap_per_turn_usd: TODO          # fill with concrete value per the driver definition
