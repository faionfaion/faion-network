<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="manifest-then-fetch Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: tool_outputs_avg_bytes, store_backend, sensitivity)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'tool_outputs_avg_bytes' meets the upper threshold?">
          <branch when="tool_outputs_avg_bytes == 'high' or tool_outputs_avg_bytes &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'store_backend' present and matches advanced branch?">
              <branch when="store_backend == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="store_backend != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="tool_outputs_avg_bytes == 'low' or tool_outputs_avg_bytes &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, tool_outputs_avg_bytes=high, store_backend=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
