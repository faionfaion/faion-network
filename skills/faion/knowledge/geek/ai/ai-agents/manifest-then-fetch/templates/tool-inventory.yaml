# purpose: input contract for manifest-then-fetch methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

tool_outputs_avg_bytes: TODO          # fill with concrete value per the driver definition
store_backend: TODO          # fill with concrete value per the driver definition
sensitivity: TODO          # fill with concrete value per the driver definition
