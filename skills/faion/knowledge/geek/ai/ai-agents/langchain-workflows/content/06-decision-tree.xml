<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="langchain-workflows Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: steps, branches, hitl_points, parallel_branches, persistence_target)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'steps' meets the upper threshold?">
          <branch when="steps == 'high' or steps &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'branches' present and matches advanced branch?">
              <branch when="branches == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="branches != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="steps == 'low' or steps &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, steps=high, branches=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
