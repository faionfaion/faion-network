<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="mcp-resource-vs-tool-vs-prompt Decision Tree" version="2.0.0" est_tokens="700">
  <decision-tree>
    <root-question id="q-root" text="Does the input profile satisfy preconditions (required drivers: side_effect, user_facing, llm_reasons_on_result)?">
      <branch when="profile.valid == false">
        <conclusion ref="skip-this-methodology">Profile incomplete — return structured error, do not guess defaults.</conclusion>
      </branch>
      <branch when="profile.valid == true">
        <question id="q-primary" text="Primary driver 'side_effect' meets the upper threshold?">
          <branch when="side_effect == 'high' or side_effect &gt;= upper_threshold">
            <question id="q-secondary" text="Secondary driver 'user_facing' present and matches advanced branch?">
              <branch when="user_facing == 'advanced'">
                <conclusion ref="r-advanced">Pick the advanced/strict variant — see core rules for the precise wiring.</conclusion>
              </branch>
              <branch when="user_facing != 'advanced'">
                <conclusion ref="r-default-high">Pick the high-tier default variant.</conclusion>
              </branch>
            </question>
          </branch>
          <branch when="side_effect == 'low' or side_effect &lt; upper_threshold">
            <conclusion ref="r-default-low">Pick the low-tier default variant — cheaper, simpler, fewer guarantees.</conclusion>
          </branch>
        </question>
      </branch>
    </root-question>
    <example trace="profile.valid=true, side_effect=high, user_facing=advanced">
      q-root yes → q-primary yes → q-secondary yes → r-advanced
    </example>
  </decision-tree>
</text>
