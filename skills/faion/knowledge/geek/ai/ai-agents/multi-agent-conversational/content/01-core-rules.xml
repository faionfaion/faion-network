<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="multi-agent-conversational Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for multi-agent-conversational. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every ConversationalAgents instance MUST set both a termination_phrase and a max_turns cap. Neither alone is sufficient: the phrase may never appear (causing infinite loops without max_turns), and max_turns alone produces no semantic exit signal.</statement>
      <rationale source="v1-source">Without a termination phrase, the agent has no way to signal completion — the conversation always runs to max_turns. Without max_turns, a bug or confused agent that never says the phrase causes an infinite loop.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The sliding context window MUST be capped (default: last 3 turns). Never pass the full conversation history to each turn prompt.</statement>
      <rationale source="v1-source">Conversational patterns accumulate context linearly. Passing full history to every turn causes quadratic token growth: turn N sees N-1 prior messages. A fixed sliding window (3-5 entries) keeps per-turn cost constant.</rationale>
    </rule>
  </section>
</text>
