{
  "__faion_header__": {
    "purpose": "embedding cache-config artefact",
    "consumes": "backend choice + model name+version + observability",
    "produces": "config validated against 02-output-contract.xml",
    "depends_on": "content/02-output-contract.xml schema",
    "token_budget_impact": "0 LLM tokens; declarative"
  },
  "backend": "valkey",
  "hash_algo": "sha256",
  "key_components": ["model_name", "model_version", "text"],
  "ttl_days": 90,
  "emit_hit_metric": true
}
