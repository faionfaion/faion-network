<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="rag-eval-strategy Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for rag-eval-strategy. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>LLM-judge metrics (faithfulness, answer relevance, context relevance) MUST run on a sampled subset (10-20%) of production queries, not on every query. Retrieval metrics (precision, recall, MRR, hit rate, NDCG) MUST run on the full labeled test set.</statement>
      <rationale source="v1-source">LLM-judge calls cost money and add latency. Running faithfulness on 15% of queries captures statistically meaningful trends while keeping eval costs 5-7x lower. Retrieval metrics have zero API cost and should cover the full test set to avoid sampling noise in rank-quality measurements.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Quality gates MUST be numeric and explicit before any production deployment: for example "faithfulness &gt;= 0.80 AND precision@5 &gt;= 0.70 AND hit_rate &gt;= 0.90". "Looks good" or "metrics are acceptable" are not quality gates.</statement>
      <rationale source="v1-source">Vague quality gates cannot be enforced in CI and create reviewer subjectivity. Numeric gates are runnable as assertions, blockable in deployment pipelines, and comparable across time. Track metric trends over 2+ weeks — a metric trending downward is actionable even if the current value is above threshold.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Quality gates MUST include at least one retrieval metric AND at least one generation metric. Never gate on a single metric.</statement>
      <rationale source="v1-source">A model can improve faithfulness while degrading retrieval recall. A single-metric gate misses this cross-dimension regression. Minimum viable gate: faithfulness (generation) + precision@5 (retrieval).</rationale>
    </rule>
  </section>
</text>
