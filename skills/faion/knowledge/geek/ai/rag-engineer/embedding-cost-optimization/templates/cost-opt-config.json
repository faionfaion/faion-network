{
  "__faion_header__": {
    "purpose": "cost-opt-config artefact",
    "consumes": "cost report + bench + cache backend + corpus size",
    "produces": "config validated against 02-output-contract.xml",
    "depends_on": "content/02-output-contract.xml schema",
    "token_budget_impact": "0 LLM tokens; declarative"
  },
  "dedup_on_ingest": true,
  "batch_size": 512,
  "cache_enabled": true,
  "matryoshka_dim": 512,
  "two_stage_retrieval": true,
  "recall_tolerance_pp": 1.0
}
