<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="graph-rag-indexing Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for graph-rag-indexing. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Run entity extraction with temperature=0 and response_format=json_object. Deduplicate entities by normalized name (lowercase, strip punctuation) before inserting into the graph. Cap at 15 entities and 20 relationships per chunk to prevent noise from dense passages.</statement>
      <rationale source="v1-source">LLM extraction at nonzero temperature introduces variant spellings ("OpenAI" vs "openai") that create duplicate nodes. Without a cap, dense academic or legal text generates hundreds of low-signal entities per chunk that degrade community quality.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Only summarize communities with 5 or more nodes. Do not use community IDs as persistent identifiers across re-index runs — use a content hash of the sorted node set instead, because Louvain output order is non-deterministic.</statement>
      <rationale source="v1-source">Singleton and 2-3 node communities produce summaries with no semantic breadth ("this community contains entity X and entity Y"). Non-deterministic community IDs break any system that caches summaries by ID after a re-index.</rationale>
    </rule>
  </section>
</text>
