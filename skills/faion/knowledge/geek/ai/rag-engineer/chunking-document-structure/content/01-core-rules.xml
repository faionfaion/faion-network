<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="chunking-document-structure Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for chunking-document-structure. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>MarkdownChunker MUST set include_header_in_chunk=True by default. Every produced chunk MUST carry a header_path field that is the concatenation of all ancestor header titles joined by " &gt; ". When a content block exceeds max_chunk_size, MarkdownChunker MUST split it and carry the same header_path into each sub-chunk.</statement>
      <rationale source="v1-source">Embedding the header path inside the chunk text improves retrieval recall for queries that reference section context. Without header_path propagation, sub-chunks of large sections lose their navigational anchor and become indistinguishable from each other in embedding space.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>HTMLChunker MUST use BeautifulSoup with find_all(['section', 'article', 'div']) as the primary splitting surface. For each element, it MUST extract the nearest heading (h1-h6) as the chunk header. Elements exceeding max_chunk_size MUST be word-split into numbered parts carrying the same header. The caller MUST pass well-formed HTML; malformed input behavior is undefined.</statement>
      <rationale source="v1-source">Flat text splitting of HTML includes tag noise and misses structural boundaries. BeautifulSoup's DOM walk respects nesting and script/style tags. Numbered parts preserve source traceability when a large section splits into multiple chunks.</rationale>
    </rule>
  </section>
</text>
