<?xml version="1.0" encoding="UTF-8"?>
<text id="02-fusion-rules" title="Hybrid Search Fusion Rules">
  <summary>RRF vs linear fusion rules, alpha selection heuristics, and pitfalls for hybrid search result merging.</summary>

  <section title="Fusion Method Selection">
    <rule>
      <statement>Prefer RRF over linear combination when the two search methods return scores on different scales (e.g., BM25 vs cosine similarity).</statement>
      <rationale>RRF uses rank position, not raw scores, so it is immune to score scale mismatches. Linear combination requires explicit per-corpus normalization to avoid one signal dominating the other.</rationale>
    </rule>
    <rule>
      <statement>Measure alpha on a labeled eval set before deploying; wrong alpha degrades results worse than using a single search method alone.</statement>
      <rationale>Alpha is data-dependent. A value tuned for one corpus can invert the quality ordering on a different corpus.</rationale>
    </rule>
  </section>

  <section title="Dynamic Alpha Selection">
    <example type="good">
      <code lang="python"><![CDATA[
def determine_alpha(query: str) -> float:
    """Heuristic alpha selection based on query characteristics."""
    has_quotes = '"' in query
    has_codes = any(c.isdigit() for c in query)
    is_short = len(query.split()) <= 3
    if has_quotes:
        return 0.3   # favor keyword
    if has_codes:
        return 0.4   # lean keyword
    if is_short:
        return 0.5   # balanced
    return 0.7       # favor semantic for longer queries
]]></code>
      <note>This is a heuristic; always validate against a labeled eval set before shipping.</note>
    </example>
  </section>

  <section title="Pitfalls">
    <antipattern>
      <description>BM25 tokenizer uses English whitespace splitting for CJK, Arabic, or other non-whitespace-delimited languages.</description>
      <reason>Whitespace tokenization produces single-character or meaningless tokens for CJK text, leading to near-zero BM25 scores. Use a language-appropriate tokenizer.</reason>
    </antipattern>
    <antipattern>
      <description>RRF discards absolute scores, so low-confidence results from both methods can surface at the same rank as high-confidence ones.</description>
      <reason>RRF normalizes all documents to their rank position. A document ranked 5th in both methods gets the same RRF score as one that is clearly top-1 in both. Apply a minimum per-method score filter before fusion.</reason>
    </antipattern>
  </section>

  <section title="Template Reference">
    <reference path="templates/hybrid_search_service.py">HybridSearchService with RRF + linear fusion and dynamic alpha</reference>
  </section>
</text>
