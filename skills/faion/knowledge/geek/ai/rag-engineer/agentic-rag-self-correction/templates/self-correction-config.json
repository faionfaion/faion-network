{
  "__faion_header__": {
    "purpose": "self-correction-config artefact",
    "consumes": "generator + distinct verifier + audit log writer",
    "produces": "config validated against 02-output-contract.xml",
    "depends_on": "content/02-output-contract.xml schema",
    "token_budget_impact": "0 LLM tokens; declarative"
  },
  "max_corrections": 2,
  "generator_model": "opus",
  "verifier_model": "sonnet",
  "max_ungrounded_claims": 2,
  "audit_trace": true
}
