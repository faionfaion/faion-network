    <?xml version="1.0" encoding="UTF-8"?>
    <text id="06-decision-tree" title="Methodology Corpus Licence Bundle — Decision Tree" version="1.1.0" est_tokens="400">
      <decision-tree>
        <root-question id="q-root">Is the customer on a paid tier AND requesting corpus embedding?</root-question>
        <branch when="paid_tier == true and embedding_requested == true">
  <conclusion ref="r1-scope-explicit">Issue a full licence bundle following all 5 rules.</conclusion>
</branch>
<branch when="paid_tier == true and embedding_requested == false">
  <conclusion ref="r4-audit-hook">Skip the bundle; record corpus access in the standard plan record.</conclusion>
</branch>
<branch when="paid_tier == false">
  <conclusion ref="r1-scope-explicit">Reject — bundle requires a paid plan.</conclusion>
</branch>
        <example trace="paid_tier == true and embedding_requested == true">
          <p>Issue a full licence bundle following all 5 rules.</p>
        </example>
      </decision-tree>
    </text>
