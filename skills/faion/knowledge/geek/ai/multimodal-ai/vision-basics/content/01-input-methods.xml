<?xml version="1.0" encoding="UTF-8"?>
<text id="01-input-methods" title="Vision Input Methods">
  <summary>URL vs. base64 image inputs, media type detection, resize before sending, and provider size limits.</summary>

  <section title="Input methods comparison">
    <rule>
      <statement>Send images as base64 for sensitive content. URL-based images are fetched by the provider's servers and may be logged or cached externally.</statement>
      <rationale>Providers that accept URLs retrieve the image from an external server that they control. Base64 encodes the image inline in the request, keeping it within the API call itself.</rationale>
    </rule>
    <rule>
      <statement>Resize images to 1024px on the long edge before sending. This reduces token cost by 50-70% with minimal quality loss for text, diagram, and screenshot tasks.</statement>
      <rationale>VLMs charge by image tokens, which scale with resolution. At 1024px, most documents and screenshots remain fully legible. Going higher rarely improves extraction accuracy.</rationale>
    </rule>
  </section>

  <section title="Provider size limits">
    <rule>
      <statement>Check image file size before making API calls: Anthropic limit is 5MB per image, OpenAI is 20MB. Exceeding these limits causes silent or unhelpful errors if not pre-checked.</statement>
      <rationale>Provider size caps are per-image, not per-request. A 10-image request with 4.9MB images each can still fail if the provider enforces a total payload cap.</rationale>
    </rule>
    <example type="good">
      <note>Resize and encode helper — prevents oversized requests.</note>
      <code lang="python"><![CDATA[
from PIL import Image
import base64, io

def prepare_image(path: str, max_px: int = 1024) -> tuple[str, str]:
    """Resize to max_px on long edge, return (base64_data, media_type)."""
    img = Image.open(path)
    img.thumbnail((max_px, max_px), Image.LANCZOS)
    buf = io.BytesIO()
    fmt = img.format or "PNG"
    img.save(buf, format=fmt)
    data = base64.standard_b64encode(buf.getvalue()).decode()
    mt = {"JPEG": "image/jpeg", "PNG": "image/png",
          "WEBP": "image/webp", "GIF": "image/gif"}.get(fmt, "image/png")
    return data, mt
]]></code>
    </example>
  </section>

  <section title="Multi-image labeling">
    <rule>
      <statement>Label each image explicitly in multi-image prompts: "Image 1:", "Image 2:". VLMs lose track of which image they are describing without explicit labels.</statement>
      <rationale>Without labels, VLMs conflate images or describe them out of order in comparisons. Explicit numbering anchors each reference in the model's response.</rationale>
    </rule>
    <reference path="templates/prepare_image.py">Standalone resize and encode helper</reference>
    <reference path="templates/vision_extract.py">Full image analysis implementations</reference>
  </section>
</text>
