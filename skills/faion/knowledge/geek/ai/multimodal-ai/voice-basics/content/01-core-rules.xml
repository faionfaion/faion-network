<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="voice-basics Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for voice-basics. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Budget each pipeline component: STT under 300ms, LLM under 500ms, TTS under 200ms targeting under 1000ms total end-to-end.</statement>
      <rationale source="v1-source">The typical range is 650-1900ms. VAD adds 50-100ms. Total exceeding 2s feels unresponsive for interactive voice.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use VAD-based recording (webrtcvad or Silero VAD) instead of fixed-duration recording in any production deployment.</statement>
      <rationale source="v1-source">Fixed-duration recording truncates variable-length speech and records silence for short utterances. VAD-based recording stops when the speaker stops.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Keep TTS responses under 50 words per turn. Use tts-1 (not tts-1-hd) for real-time conversation.</statement>
      <rationale source="v1-source">Longer responses feel unnatural and cannot be interrupted. tts-1-hd latency is not suitable for real-time - use it for pre-generated audio only.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Truncate conversation_history to the last 10 turns to prevent context overflow. Summarize older context into the system prompt.</statement>
      <rationale source="v1-source">Conversation history grows unboundedly - will hit context limits after 20-50 turns on GPT-4o, causing silent truncation or API errors.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Always specify the language parameter explicitly in Whisper calls (e.g., language="uk", language="en").</statement>
      <rationale source="v1-source">Auto-detection adds approximately 200ms latency and occasionally misidentifies similar languages (Ukrainian/Russian, Serbian/Croatian).</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Use OpenAI Realtime API (WebSocket) for lowest-latency deployments - it eliminates the STT + LLM + TTS round-trip chain.</statement>
      <rationale source="v1-source">The Realtime API delivers approximately 200ms end-to-end vs 650-1900ms for the chained approach.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use client.audio.speech.with_streaming_response to stream TTS audio to the speaker as chunks arrive rather than buffering the full response.</statement>
      <rationale source="v1-source">Streaming reduces perceived TTS latency from full-response time to first-byte time - typically 100-200ms faster.</rationale>
    </rule>
  </section>
</text>
