<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Tool Card Template — Worked Example" version="1.1.0" est_tokens="600" depth="recommended">
  <summary>One end-to-end worked example illustrating the full procedure for `tool-card-template` against a representative task.</summary>

  <section title="Scenario">
    <p>A geek-tier team applies `tool-card-template` to a fresh instance. Owner: ml-eng:alice; reviewer: tl:bob; cadence: quarterly.</p>
  </section>

  <section title="Inputs">
    <list>
      <item>Each prerequisite supplied per the AGENTS.md table; sources linked.</item>
      <item>Existing artefact catalogue checked for duplicates (none found).</item>
      <item>Named approver: tl:bob.</item>
    </list>
  </section>

  <section title="Walkthrough">
    <p>The team runs the five-step procedure in `content/04-procedure.xml`, recording one paragraph per step. At step 4 the self-check triggers a fix on the rationale (no input cited); re-check passes. The artefact emits with status=&apos;ready-for-owner-review&apos;.</p>
  </section>

  <section title="Output (excerpt)">
    <example type="good">
      <code lang="json"><![CDATA[
{
  "artefact_id": "toolc-acme-pilot-001",
  "owner": "alice@acme.com",
  "decision": "first concrete decision shape per templates/tool-card-template.md",
  "rationale": "Pulls input A (engagement scope doc) and input B (risk register row #3); identifies the load-bearing constraint and the residual risk class. Cites both inputs by name and pins the source URL for follow-up.",
  "inputs_used": [
    {"name": "scope_doc", "source": "repo:engagements/acme-pilot/scope.md"},
    {"name": "risk_register_row3", "source": "repo:engagements/acme-pilot/risk-register.csv#L3"}
  ],
  "version": "1.0.0",
  "last_reviewed": "2026-05-22"
}
]]></code>
    </example>
  </section>

  <section title="Lessons">
    <list>
      <item>The decision tree in `content/06-decision-tree.xml` saved one round at step 1 by ruling out a duplicate artefact.</item>
      <item>Without the self-check at step 4 the rationale would have shipped without input citations.</item>
    </list>
  </section>
</text>
