<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Gemini API Integration Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Is Gemini the chosen vendor for this call site (cost, context, multimodal, or compliance reason)?</root-question>
    <branch when="vendor.chosen == 'gemini'">
      <question>Is any input media &gt;10 MB OR video/audio?</question>
      <branch when="media.heavy == true">
        <conclusion ref="r2"/>
      </branch>
      <branch when="media.heavy == false">
        <conclusion ref="run-the-checklist"/>
      </branch>
    </branch>
    <branch when="vendor.chosen != 'gemini'">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="Gemini chosen for 200-page PDF summarisation → run-the-checklist"/>
  </decision-tree>
</text>
