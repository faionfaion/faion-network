<?xml version="1.0" encoding="UTF-8"?>
<text id="03-multimodal" title="DALL-E, Whisper, and TTS Rules">
  <summary>Critical rules for DALL-E 3 URL expiry, Whisper file size limits and chunking, and TTS model stability in agent pipelines.</summary>

  <section title="DALL-E 3">
    <rule>
      <statement>Download and persist DALL-E 3 generated images immediately — URLs expire after 1 hour and cannot be regenerated.</statement>
      <rationale>Agents that store the URL instead of the image content will have broken links in storage. This is the most common DALL-E 3 failure point in production pipelines.</rationale>
    </rule>

    <rule>
      <statement>Log both the original prompt and revised_prompt for every DALL-E 3 generation; the model rewrites prompts and may introduce content not in the original.</statement>
      <rationale>DALL-E 3 automatically rewrites prompts via revised_prompt. For brand content or compliance, both versions must be reviewable. Agents generating images for brand content must log both.</rationale>
    </rule>

    <example type="good">
      <code lang="python"><![CDATA[
import requests
from pathlib import Path
from openai import OpenAI

client = OpenAI()

def generate_and_save(prompt: str, save_path: str) -> str:
    resp = client.images.generate(
        model="dall-e-3", prompt=prompt, size="1024x1024",
        quality="hd", response_format="url")
    data = resp.data[0]
    # Download immediately — URL expires in 1 hour
    img_bytes = requests.get(data.url).content
    Path(save_path).write_bytes(img_bytes)
    return data.revised_prompt  # log for audit
]]></code>
    </example>
  </section>

  <section title="Whisper">
    <rule>
      <statement>Split audio files larger than 20MB into chunks with ffmpeg before sending to Whisper; repeat the prompt hint on each chunk to maintain proper noun consistency.</statement>
      <rationale>Whisper has a 25MB hard limit. Agents processing long audio must chunk with overlap. The prompt parameter (proper noun hints) only affects the first segment unless repeated per chunk.</rationale>
    </rule>

    <reference path="templates/whisper-chunked.py">Chunked transcription for large audio files</reference>
  </section>

  <section title="TTS">
    <rule>
      <statement>Pin TTS model to either tts-1 or tts-1-hd consistently within a conversation session — switching models changes voice characteristics noticeably and breaks user experience.</statement>
      <rationale>tts-1 and tts-1-hd produce different voice quality profiles. Mid-conversation model switching is audibly jarring.</rationale>
    </rule>
  </section>
</text>
