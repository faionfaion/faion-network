<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="prompt-techniques">
  <metadata>
    <tier>geek</tier>
    <group>ai</group>
    <domain>llm-integration</domain>
    <summary>Advanced prompting patterns, testing, and management strategies: delimiters, meta-prompting, prompt chaining, PromptLibrary, prompt testing, versioning, and A/B testing.</summary>
    <tags>
      <tag>prompt-optimization</tag>
      <tag>meta-prompting</tag>
      <tag>prompt-chaining</tag>
      <tag>prompt-testing</tag>
      <tag>a-b-testing</tag>
    </tags>
    <difficulty>advanced</difficulty>
    <created>2026-04-28</created>
  </metadata>
  <content>
    <title>Advanced Prompt Techniques</title>
    <summary>Advanced prompting patterns, testing, and management strategies.</summary>
    <why>
      <p>A prompt is underperforming when it produces wrong format, misses edge cases, or hallucinates. Systematic improvement via meta-prompting (using an LLM to generate better prompts), A/B testing, and PromptLibrary-based versioning ensures production reliability. Advanced delimiters (XML tags, backticks) prevent prompt injection and reduce ambiguity in multi-step chains. PromptChain decouples discrete LLM steps into maintainable units. Testing frameworks validate accuracy before release.</p>
    </why>
    <when-to-use>
      <list>
        <item>A prompt is underperforming (wrong format, missing edge cases, hallucinating) and needs systematic improvement via meta-prompting or A/B testing</item>
        <item>A pipeline has multiple discrete LLM steps that should be decoupled into a PromptChain for maintainability</item>
        <item>The project needs a versioned PromptLibrary so prompts can be updated without code deployments</item>
        <item>You need to validate prompt accuracy against a known test set before releasing a new version</item>
        <item>Prompt injection or delimiter confusion is causing failures in production</item>
      </list>
    </when-to-use>
    <when-not-to-use>
      <list>
        <item>The prompt is simple (&lt;50 words) and passes manual inspection — over-engineering with libraries adds maintenance cost</item>
        <item>The task changes frequently; a rigid PromptLibrary with versioning becomes a burden if prompts are iterated daily</item>
        <item>You need model-specific optimizations (e.g., Anthropic XML tags vs. OpenAI system messages) — a cross-provider library hides these differences and can regress quality</item>
        <item>Budget is tight and meta-prompting (using GPT-4o to generate prompts) doubles token cost on every iteration</item>
      </list>
    </when-not-to-use>
    <section name="Delimiting and Formatting">
      <p>Clear delimiters separate instructions from content and prevent prompt injection. XML-style tags provide the strongest boundary the model recognizes.</p>
      <example type="good">
        <code lang="python"><![CDATA[
def format_with_delimiters(
    instruction: str,
    content: str,
    delimiter: str = "```"
) -> str:
    """Format prompt with clear delimiters."""
    return f"""{instruction}

{delimiter}
{content}
{delimiter}"""

def format_with_xml_tags(
    instruction: str,
    **sections
) -> str:
    """Format prompt with XML-style tags."""
    prompt = instruction + "\n\n"

    for tag, content in sections.items():
        prompt += f"<{tag}>\n{content}\n</{tag}>\n\n"

    return prompt

# Example
prompt = format_with_xml_tags(
    "Summarize the document and answer the question.",
    document="This is a long document about AI...",
    question="What are the main benefits of AI?",
    format="Provide a 2-3 sentence summary followed by a direct answer."
)
]]></code>
      </example>
    </section>
    <section name="Meta-Prompting">
      <p>Use an LLM to generate or improve prompts. Meta-prompting is effective for optimization but produces model-specific prompts that may not transfer.</p>
      <example type="good">
        <code lang="python"><![CDATA[
def generate_prompt(
    task_description: str,
    client,  # OpenAI client
    model: str = "gpt-4o"
) -> str:
    """Use LLM to generate an effective prompt."""
    meta_prompt = """You are a prompt engineering expert. Create an effective prompt for the following task.

The prompt should:
1. Be clear and specific
2. Include relevant context
3. Specify output format
4. Include examples if helpful
5. Handle edge cases

Task: {task}

Generate only the prompt, no explanation."""

    response = client.chat.completions.create(
        model=model,
        messages=[
            {"role": "user", "content": meta_prompt.format(task=task_description)}
        ]
    )

    return response.choices[0].message.content

def optimize_prompt(
    original_prompt: str,
    failure_cases: List[Dict],
    client,
    model: str = "gpt-4o"
) -> str:
    """Improve prompt based on failure cases."""
    meta_prompt = f"""Analyze this prompt and its failure cases, then create an improved version.

Original prompt:
{original_prompt}

Failure cases:
{json.dumps(failure_cases, indent=2)}

Create an improved prompt that addresses these failures while maintaining the original intent."""

    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": meta_prompt}]
    )

    return response.choices[0].message.content
]]></code>
      </example>
    </section>
    <section name="Prompt Chaining">
      <p>Chain multiple prompts together to break a complex task into discrete, independent steps. Each step receives the output of the previous step.</p>
      <example type="good">
        <code lang="python"><![CDATA[
from typing import List, Callable

class PromptChain:
    """Chain multiple prompts together."""

    def __init__(self, client, model: str = "gpt-4o"):
        self.client = client
        self.model = model
        self.steps: List[Callable] = []

    def add_step(self, prompt_func: Callable):
        """Add a step to the chain."""
        self.steps.append(prompt_func)
        return self

    def run(self, initial_input: str) -> Dict:
        """Execute the chain."""
        current_input = initial_input
        results = {"steps": []}

        for i, step_func in enumerate(self.steps):
            prompt = step_func(current_input)

            response = self.client.chat.completions.create(
                model=self.model,
                messages=[{"role": "user", "content": prompt}]
            )

            output = response.choices[0].message.content

            results["steps"].append({
                "step": i + 1,
                "input": current_input[:100],
                "output": output
            })

            current_input = output

        results["final_output"] = current_input
        return results

# Usage
def step1_extract(text):
    return f"Extract the main entities from this text: {text}"

def step2_categorize(entities):
    return f"Categorize these entities by type: {entities}"

def step3_summarize(categories):
    return f"Create a brief summary of these categorized entities: {categories}"

chain = PromptChain(client, "gpt-4o")
chain.add_step(step1_extract)
chain.add_step(step2_categorize)
chain.add_step(step3_summarize)

result = chain.run("Apple announced a new iPhone with advanced AI features...")
]]></code>
      </example>
    </section>
    <section name="Prompt Library">
      <p>Centralized management of prompts with versioning, categorization, and formatting. PromptLibrary enables updates without code deployments.</p>
      <example type="good">
        <code lang="python"><![CDATA[
from enum import Enum
from dataclasses import dataclass
from typing import Dict, List, Optional

class PromptCategory(Enum):
    CLASSIFICATION = "classification"
    GENERATION = "generation"
    EXTRACTION = "extraction"
    TRANSFORMATION = "transformation"
    ANALYSIS = "analysis"

@dataclass
class ManagedPrompt:
    name: str
    category: PromptCategory
    system: str
    template: str
    examples: Optional[List[Dict]] = None
    version: str = "1.0"
    description: str = ""

class PromptLibrary:
    """Centralized prompt management."""

    def __init__(self):
        self.prompts: Dict[str, ManagedPrompt] = {}

    def register(self, prompt: ManagedPrompt):
        """Register a prompt."""
        self.prompts[prompt.name] = prompt

    def get(self, name: str) -> ManagedPrompt:
        """Retrieve a prompt."""
        if name not in self.prompts:
            raise KeyError(f"Prompt '{name}' not found")
        return self.prompts[name]

    def format(self, name: str, **kwargs) -> List[Dict]:
        """Format a prompt with variables."""
        prompt = self.get(name)
        messages = [{"role": "system", "content": prompt.system}]

        if prompt.examples:
            for ex in prompt.examples:
                messages.append({"role": "user", "content": ex["input"]})
                messages.append({"role": "assistant", "content": ex["output"]})

        messages.append({
            "role": "user",
            "content": prompt.template.format(**kwargs)
        })

        return messages

# Initialize library
library = PromptLibrary()

# Register prompts
library.register(ManagedPrompt(
    name="sentiment_analysis",
    category=PromptCategory.CLASSIFICATION,
    system="You are a sentiment analysis expert. Respond with only: positive, negative, or neutral.",
    template="Analyze the sentiment: {text}",
    examples=[
        {"input": "Analyze the sentiment: I love it!", "output": "positive"},
        {"input": "Analyze the sentiment: This is awful.", "output": "negative"}
    ],
    version="1.2",
    description="Classify text sentiment into three categories"
))

# Usage
messages = library.format("sentiment_analysis", text="The product works as expected.")
]]></code>
      </example>
    </section>
    <section name="Prompt Testing">
      <p>Test prompts against expected outputs before release. Evaluate with custom logic or LLM-as-evaluator. Always pair LLM evaluation with at least one deterministic check.</p>
      <example type="good">
        <code lang="python"><![CDATA[
from typing import List, Dict, Callable
import json

class PromptTester:
    """Test prompts against expected outputs."""

    def __init__(self, client, model: str = "gpt-4o"):
        self.client = client
        self.model = model

    def test_prompt(
        self,
        prompt_messages: List[Dict],
        test_cases: List[Dict],
        evaluator: Callable[[str, str], bool] = None
    ) -> Dict:
        """Test a prompt against multiple cases."""
        results = {
            "passed": 0,
            "failed": 0,
            "cases": []
        }

        for case in test_cases:
            # Build messages with test input
            messages = prompt_messages.copy()
            messages[-1]["content"] = messages[-1]["content"].format(**case["input"])

            # Get response
            response = self.client.chat.completions.create(
                model=self.model,
                messages=messages
            )
            actual = response.choices[0].message.content

            # Evaluate
            expected = case["expected"]
            if evaluator:
                passed = evaluator(actual, expected)
            else:
                passed = expected.lower() in actual.lower()

            if passed:
                results["passed"] += 1
            else:
                results["failed"] += 1

            results["cases"].append({
                "input": case["input"],
                "expected": expected,
                "actual": actual,
                "passed": passed
            })

        results["accuracy"] = results["passed"] / len(test_cases)
        return results

# Usage
tester = PromptTester(client)
messages = library.format("sentiment_analysis", text="{text}")

test_cases = [
    {"input": {"text": "I absolutely love this!"}, "expected": "positive"},
    {"input": {"text": "This is the worst."}, "expected": "negative"},
    {"input": {"text": "It arrived on Tuesday."}, "expected": "neutral"},
]

results = tester.test_prompt(messages, test_cases)
print(f"Accuracy: {results['accuracy']:.2%}")
]]></code>
      </example>
    </section>
    <section name="Advanced Techniques">
      <p>Prompt versioning and A/B testing for systematic improvement and comparison.</p>
      <example type="good">
        <code lang="python"><![CDATA[
@dataclass
class VersionedPrompt:
    """Prompt with version control."""
    name: str
    version: str
    prompt: str
    changelog: List[str]
    created_at: datetime
    tested_accuracy: float = 0.0

    def bump_version(self, change_description: str):
        """Increment version and log change."""
        major, minor, patch = map(int, self.version.split('.'))
        self.version = f"{major}.{minor}.{patch + 1}"
        self.changelog.append(f"v{self.version}: {change_description}")

class PromptABTest:
    """Compare two prompt variants."""

    def __init__(self, client, model: str = "gpt-4o"):
        self.client = client
        self.model = model

    def compare(
        self,
        prompt_a: List[Dict],
        prompt_b: List[Dict],
        test_cases: List[Dict],
        evaluator: Callable
    ) -> Dict:
        """Compare two prompts."""
        results_a = self.test_prompt(prompt_a, test_cases, evaluator)
        results_b = self.test_prompt(prompt_b, test_cases, evaluator)

        return {
            "prompt_a": {
                "accuracy": results_a["accuracy"],
                "passed": results_a["passed"]
            },
            "prompt_b": {
                "accuracy": results_b["accuracy"],
                "passed": results_b["passed"]
            },
            "winner": "a" if results_a["accuracy"] > results_b["accuracy"] else "b"
        }
]]></code>
      </example>
    </section>
    <checklist>
      <check>Implement role-based prompting</check>
      <check>Test expert prompting</check>
      <check>Use meta prompts</check>
      <check>Implement style transfer prompts</check>
      <check>Test on various tasks</check>
      <check>Evaluate technique effectiveness</check>
      <check>Implement prompt chaining</check>
      <check>Build multi-step pipelines</check>
      <check>Handle context limitations</check>
      <check>Implement dynamic prompts</check>
      <check>Test prompt composition</check>
      <check>Validate end-to-end results</check>
      <check>Analyze prompt effectiveness</check>
      <check>A/B test variations</check>
      <check>Optimize for cost</check>
      <check>Optimize for quality</check>
      <check>Test trade-offs</check>
      <check>Document results</check>
      <check>Implement prompt injection defense</check>
      <check>Add input validation</check>
      <check>Implement output filtering</check>
      <check>Test edge cases</check>
      <check>Monitor for issues</check>
      <check>Document patterns</check>
      <check>Create reusable templates</check>
      <check>Version templates</check>
      <check>Document template usage</check>
      <check>Implement template selection</check>
      <check>Test template variations</check>
      <check>Monitor template performance</check>
      <check>Test techniques on real data</check>
      <check>Compare effectiveness</check>
      <check>Benchmark quality metrics</check>
      <check>Test edge cases</check>
      <check>Document best practices</check>
      <check>Build technique library</check>
    </checklist>
    <section name="Agentic workflow">
      <p>A subagent performing prompt optimization should: read the current prompt, run it against 5-10 representative test cases, identify the failure pattern, invoke meta-prompting to generate a candidate improvement, then A/B test the candidate against the original before replacing. The cycle runs as a loop until accuracy meets a defined threshold or a maximum iteration count is reached. For PromptChain, each step should be a named function — this makes the chain readable in code review and debuggable by logging step.input and step.output at each stage.</p>
      <p>Recommended subagents: General-purpose subagent for meta-prompt generation and A/B evaluation cycles; faion-sdd-executor-agent for implementing PromptLibrary and PromptTester as SDD tasks with test coverage.</p>
    </section>
    <section name="Limitations and failure modes">
      <antipattern>
        <description>Meta-prompting produces prompts that work on the generating model but underperform on a smaller/different target model</description>
        <reason>Prompts optimized for GPT-4o degrade on Haiku or other providers due to model-specific quirks.</reason>
      </antipattern>
      <antipattern>
        <description>PromptChain error propagation: an error in step N produces garbage that step N+1 silently accepts, making root cause analysis hard</description>
        <reason>Each step operates independently without validation of the previous step's output.</reason>
      </antipattern>
      <antipattern>
        <description>A/B testing requires a stable evaluator; LLM-as-evaluator is itself non-deterministic and can flip results between runs</description>
        <reason>Model outputs vary with temperature, context, and updates.</reason>
      </antipattern>
      <antipattern>
        <description>PromptLibrary.format() with {variable} in templates breaks if the input contains literal curly braces — must escape {{ / }}</description>
        <reason>Python string formatting treats { } specially.</reason>
      </antipattern>
      <antipattern>
        <description>Prompt versioning without a test suite is theater; bumping versions without accuracy tracking gives false confidence</description>
        <reason>Versions without validation metrics provide no proof of improvement.</reason>
      </antipattern>
      <antipattern>
        <description>Variable substitution: PromptLibrary templates use Python .format(); if user input contains {text} literally, it raises KeyError</description>
        <reason>Input validation required before formatting.</reason>
      </antipattern>
      <antipattern>
        <description>Prompt injection via user data: XML tag injection (&lt;/document&gt;&lt;injection&gt;) in untrusted input can break structured prompts</description>
        <reason>Wrap user content in a neutral delimiter and strip close-tags before insertion.</reason>
      </antipattern>
      <antipattern>
        <description>Chained prompts accumulate latency; a 5-step chain with 2s average latency per step = 10s total</description>
        <reason>Subagents should parallelize independent chain branches when possible.</reason>
      </antipattern>
      <antipattern>
        <description>PromptABTest winner selection is sensitive to test set size; with &lt;20 test cases, random variance can declare a losing prompt as "winner"</description>
        <reason>Use at least 50 diverse cases for reliable comparison.</reason>
      </antipattern>
    </section>
    <section name="Best practices">
      <rule>
        <statement>Use XML tags (&lt;document&gt;, &lt;question&gt;) rather than triple-backtick delimiters for Anthropic models; backticks work better for OpenAI.</statement>
        <rationale>Model training data differs by provider; XML is a universal boundary.</rationale>
      </rule>
      <rule>
        <statement>Version every prompt that reaches production; store name + version + accuracy score together, not just the prompt text.</statement>
        <rationale>Version tracking enables rollback and comparison.</rationale>
      </rule>
      <rule>
        <statement>Never use LLM-as-sole-evaluator in A/B tests; pair it with at least one deterministic check (regex, JSON schema validation).</statement>
        <rationale>LLM evaluation is non-deterministic; deterministic checks provide ground truth.</rationale>
      </rule>
      <rule>
        <statement>Prompt chaining: keep each step's output to the minimum needed for the next step — passing full verbatim outputs bloats context and costs.</statement>
        <rationale>Token budget is constrained; extract only relevant parts between steps.</rationale>
      </rule>
      <rule>
        <statement>When using meta-prompting to generate prompts, specify the target model in the meta-prompt; prompts optimized for GPT-4o degrade on Haiku.</statement>
        <rationale>Model-specific optimizations are required for reliable transfer.</rationale>
      </rule>
      <rule>
        <statement>Human checkpoint needed before deploying a new prompt version to production without a passing test suite; automated meta-prompt improvements can introduce regressions that only appear on edge-case inputs.</statement>
        <rationale>Manual review catches edge-case regressions automated tests miss.</rationale>
      </rule>
    </section>
    <see-also>
      <link href="https://platform.openai.com/docs/guides/prompt-engineering"/>
      <link href="https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering"/>
      <link href="https://promptfoo.dev/docs/intro"/>
      <link href="https://www.promptingguide.ai/"/>
      <link href="https://langfuse.com/docs/prompts/get-started"/>
    </see-also>
  </content>
</methodology>
