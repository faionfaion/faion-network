<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="OpenAI Assistants API — Decision Tree" version="1.1.0" est_tokens="400">
  <decision-tree>
    <root-question id="q-root">Does the session need state across turns AND built-in File Search or Code Interpreter?</root-question>
    <branch when="needs_state == true and uses_file_search_or_code_interpreter == true">
      <conclusion ref="r1-assistant-as-resource">Use Assistants API; create assistant once, manage threads + vector stores.</conclusion>
    </branch>
    <branch when="needs_state == true and uses_file_search_or_code_interpreter == false">
      <conclusion ref="r2-thread-lifecycle">Consider chat.completions with your own message store before paying Assistants overhead.</conclusion>
    </branch>
    <branch when="needs_state == false">
      <conclusion ref="r1-assistant-as-resource">Skip Assistants — use chat.completions stateless.</conclusion>
    </branch>
    <example trace="needs_state == true and uses_file_search_or_code_interpreter == true">
      <p>Use Assistants API; create assistant once, manage threads + vector stores.</p>
    </example>
  </decision-tree>
</text>
