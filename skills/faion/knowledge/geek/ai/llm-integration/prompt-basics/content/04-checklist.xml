<?xml version="1.0" encoding="UTF-8"?>
<text id="04-checklist" title="Prompt Engineering Checklist">
  <summary>Step-by-step checklist for designing, testing, and versioning prompts in production pipelines.</summary>

  <section title="System prompt design">
    <list>
      <item>Define model role clearly: "You are a [specific role]"</item>
      <item>Specify behavioral constraints (be concise, ask clarifying questions)</item>
      <item>Document output format expectations explicitly</item>
      <item>Keep system prompt under 2K tokens</item>
      <item>Use delimiters (""", XML tags, ---) to separate instructions from content</item>
      <item>Test that prompt injection via user content does not override system constraints</item>
    </list>
  </section>

  <section title="Template construction">
    <list>
      <item>Define template as a code constant with named {variables}</item>
      <item>Assign a semantic version ID (e.g., classify-v2) and log it with every call</item>
      <item>Add output format instruction as the last line of the user message</item>
      <item>Include 1 concrete output example in zero-shot prompts for novel schemas</item>
      <item>For few-shot: use 3–5 high-quality pairs; cover edge cases; put the best example last</item>
    </list>
  </section>

  <section title="Testing and iteration">
    <list>
      <item>Test with at least 10 diverse real inputs before shipping</item>
      <item>Test adversarial inputs: empty, malformed, injection attempts</item>
      <item>Set temperature=0 for extraction/classification; test that output is deterministic</item>
      <item>Compare few-shot vs zero-shot on a held-out test set</item>
      <item>Run regression tests after any model version change</item>
      <item>Always wrap json.loads in try/except and retry with a corrective message on failure</item>
    </list>
  </section>

  <section title="Production">
    <list>
      <item>Cache stable system prompts (Anthropic cache_control, OpenAI prompt cache)</item>
      <item>Log prompt version ID alongside every LLM call for debugging</item>
      <item>Alert on output parse failure rate exceeding 1% — indicates prompt regression</item>
    </list>
  </section>
</text>
