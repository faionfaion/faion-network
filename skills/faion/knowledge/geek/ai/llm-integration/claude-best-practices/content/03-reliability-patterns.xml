<?xml version="1.0" encoding="UTF-8"?>
<text id="03-reliability-patterns" title="Reliability Patterns for Claude API Calls">
  <summary>Rules for retry with backoff, rate-limit header parsing, fallback model selection, and async batch processing in production Claude pipelines.</summary>

  <section title="Retry and Rate Limits">
    <rule>
      <statement>Parse the retry-after header from RateLimitError instead of guessing the backoff interval.</statement>
      <rationale>The Anthropic API includes a retry-after header with the exact wait time. Guessing with exponential backoff may wait too long or too short, degrading throughput unnecessarily.</rationale>
    </rule>
    <rule>
      <statement>Implement exponential backoff with jitter for 5xx server errors; retry up to 3 times before raising.</statement>
      <rationale>5xx errors are transient. A single retry captures most transient failures. More than 3 retries risks cascading delays in multi-step pipelines.</rationale>
    </rule>
    <rule>
      <statement>In multi-worker orchestrators, use a shared rate-limit token bucket rather than per-process backoff.</statement>
      <rationale>Per-process backoff does not account for other workers consuming quota simultaneously. A shared bucket prevents all workers from hammering the API at the same time after a backoff period.</rationale>
    </rule>
  </section>

  <section title="Model Fallback">
    <rule>
      <statement>When falling back from Sonnet to Haiku on rate limiting, log the actual model used in the response.</statement>
      <rationale>The orchestrator needs to know which model produced a given output. Silent fallback produces unexpected quality variations that are impossible to audit without this log.</rationale>
    </rule>
  </section>

  <section title="Async Processing">
    <rule>
      <statement>Use AsyncAnthropic with asyncio.gather for concurrent batch calls. Do not spawn threads; the SDK is async-native.</statement>
      <rationale>The Anthropic Python SDK is built on httpx with native async support. Using threads adds overhead and can introduce race conditions. asyncio.gather is the correct concurrency primitive.</rationale>
    </rule>
  </section>

  <example type="good">
    <code lang="python"><![CDATA[
import anthropic, logging, time

log = logging.getLogger(__name__)
client = anthropic.Anthropic()

def call(model: str, system: list, messages: list, max_tokens: int = 2048):
    t0 = time.monotonic()
    r = client.messages.create(
        model=model, max_tokens=max_tokens,
        system=system, messages=messages
    )
    elapsed = time.monotonic() - t0
    total_tok = r.usage.input_tokens + r.usage.output_tokens
    log.info("model=%s tokens=%d elapsed=%.2fs", model, total_tok, elapsed)
    return r
    ]]></code>
    <note>Actual model ID is logged (not the requested alias), enabling post-hoc audit of fallback behavior.</note>
  </example>

  <reference path="templates/monitored-client.py">Minimal Claude client with token usage and latency logging</reference>
</text>
