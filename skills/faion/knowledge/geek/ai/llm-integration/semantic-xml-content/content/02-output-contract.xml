<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Semantic XML Content — Output Contract" version="1.1.0" est_tokens="700" depth="essential">
  <summary>The shape of a single content/*.xml file: root + attributes + first-child summary, body containing only glossary-listed tags.</summary>

  <section title="Output shape">
    <p>Output is a UTF-8 XML file whose root is `<text>` with required attributes `id`, `title`, `version`, `est_tokens`, and optional `depth`. The first child element is `<summary>`; subsequent children are `<section>`, `<rule>`, `<antipattern>`, `<example>`, `<reference>`, `<see-also>`, or other glossary tags. The file parses with a standard XML parser (well-formed). Tag count outside the glossary == 0.</p>
  </section>

  <section title="Required attributes on root">
    <list>
      <item>id — matches the filename without extension (e.g., `01-core-rules`).</item>
      <item>title — short human label.</item>
      <item>version — semver string.</item>
      <item>est_tokens — integer estimate of token budget.</item>
    </list>
  </section>

  <section title="Forbidden patterns">
    <antipattern>
      <description>Tag not present in `tag-glossary.xml`.</description>
      <reason>Open vocabulary breaks the indexer and corrupts diff hygiene.</reason>
    </antipattern>
    <antipattern>
      <description>Formatting tag (`<bold>`, `<heading>`, `<i>`, `<br>`).</description>
      <reason>Carries no role; an agent cannot act on appearance.</reason>
    </antipattern>
    <antipattern>
      <description>Code outside `<code lang>` and not CDATA-wrapped.</description>
      <reason>Inline `<` and `&` break parsers.</reason>
    </antipattern>
  </section>

  <section title="Valid example">
    <example type="good">
      <code lang="xml"><![CDATA[
<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Demo Core Rules" version="1.0.0" est_tokens="200" depth="essential">
  <summary>One sentence describing this file.</summary>
  <section title="Rules">
    <rule id="r1" testable="true" depth="essential">
      <statement>Do X when Y.</statement>
      <rationale source="...">Because Z.</rationale>
    </rule>
  </section>
</text>
]]></code>
    </example>
  </section>

  <section title="Invalid example">
    <example type="bad">
      <code lang="xml"><![CDATA[
<text>
  <heading>Demo</heading>
  <bold>important</bold>
  <p>code: if a > b then ...</p>
</text>
]]></code>
      <note>Invalid: no root attrs, formatting tags, no summary, unescaped `>`.</note>
    </example>
  </section>

  <section title="Self-check checklist">
    <list>
      <item>Root attributes present (id, title, version, est_tokens).</item>
      <item>First child is `<summary>`.</item>
      <item>Every tag exists in the glossary.</item>
      <item>Code blocks wrapped in CDATA.</item>
    </list>
  </section>
</text>
