<?xml version="1.0" encoding="UTF-8"?>
<text id="01-principles" title="Principles of semantic XML for agents">
  <summary>Why XML beats Markdown for agent-readable content, and what makes a tag semantic.</summary>

  <section title="Semantic vs formatting tags">
    <p>A tag is semantic when its name describes what the content IS, not how it should look. Markdown only encodes structure (heading, list, paragraph). Semantic XML encodes role: this is a rule, that is an antipattern, this is a reference to an external file.</p>
    <rule testable="true">
      <statement>Every tag name must answer the question "what kind of content does this hold?" — never "how should this be displayed?".</statement>
      <rationale>Anthropic fine-tuned Claude to recognize role-bearing tags (instructions, context, example, thinking). Formatting tags carry no policy signal and waste tokens.</rationale>
    </rule>
    <example type="good">
      <code lang="xml"><![CDATA[
<rule><statement>Always validate inputs at boundaries.</statement></rule>
<antipattern><description>Skipping validation in trusted callers.</description></antipattern>
      ]]></code>
      <note>Tag names match content roles. Agent can apply different policies per role.</note>
    </example>
    <example type="bad">
      <code lang="xml"><![CDATA[
<bold>Always validate inputs at boundaries.</bold>
<paragraph>Skipping validation is risky.</paragraph>
      ]]></code>
      <note>Formatting tags. Agent learns nothing about the role of each block.</note>
    </example>
  </section>

  <section title="What semantic XML gives the agent">
    <list>
      <item>Unambiguous separation of instructions, data, examples, and references.</item>
      <item>Closed grammar — easier to validate with a schema or parser.</item>
      <item>Per-tag policies: agent treats `&lt;rule&gt;` as binding, `&lt;example&gt;` as illustrative, `&lt;antipattern&gt;` as forbidden.</item>
      <item>Cheaper future migrations — content is already structured if the schema evolves.</item>
    </list>
  </section>

  <section title="The four guarantees">
    <rule testable="true">
      <statement>A semantic XML document satisfies four guarantees: role-tagged content, closed vocabulary, machine parseability, no presentation logic.</statement>
      <rationale>If any of the four breaks, the document degrades into either Markdown-with-angle-brackets or to free-form prose, losing the policy signal that justified XML in the first place.</rationale>
    </rule>
    <reference path="templates/tag-glossary.xml">Closed list of tags allowed in faion-network methodology content.</reference>
  </section>

  <see-also>
    <link path="../02-tag-design.xml">How to choose tag names and structure attributes.</link>
    <link path="../03-anti-patterns.xml">Concrete anti-patterns to avoid.</link>
  </see-also>
</text>
