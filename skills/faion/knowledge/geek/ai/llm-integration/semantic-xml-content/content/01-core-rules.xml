<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="semantic-xml-content Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for semantic-xml-content. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every tag name must answer the question "what kind of content does this hold?" — never "how should this be displayed?".</statement>
      <rationale source="v1-source">Anthropic fine-tuned Claude to recognize role-bearing tags (instructions, context, example, thinking). Formatting tags carry no policy signal and waste tokens.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>A semantic XML document satisfies four guarantees: role-tagged content, closed vocabulary, machine parseability, no presentation logic.</statement>
      <rationale source="v1-source">If any of the four breaks, the document degrades into either Markdown-with-angle-brackets or to free-form prose, losing the policy signal that justified XML in the first place.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Tag names use lowercase ASCII, hyphenated for multi-word concepts, and describe content role.</statement>
      <rationale source="v1-source">Consistent casing avoids parser ambiguity. Descriptive names let the agent route policy by tag without reading content.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use attributes for fixed metadata (id, type, lang, path); use child elements for content the agent will read or quote.</statement>
      <rationale source="v1-source">Attributes are cheap to query and rare to misparse; child text is what the agent renders or quotes back.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Nest only when the relationship is naturally hierarchical: container/items, parent-rule/sub-clauses, example/code+note.</statement>
      <rationale source="v1-source">Anthropic's own pattern: documents → document → document_content + source. Flat lists with descriptive tags beat deep trees.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Wrap source code in `&lt;code lang="..."&gt;` with a CDATA section. Never let `&lt;`, `&gt;`, or `&amp;` leak into element content.</statement>
      <rationale source="v1-source">CDATA preserves code verbatim and prevents the parser from misreading code as XML. The lang attribute lets the agent pick the right interpretation (syntax, formatter, runner).</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Link to other texts and templates with `&lt;reference path="..."&gt;` (one-line purpose) or `&lt;see-also&gt;&lt;link path="..."&gt;...&lt;/link&gt;&lt;/see-also&gt;` (related reading).</statement>
      <rationale source="v1-source">Two distinct relations: a reference is consumed (template, script, schema); a see-also is optional context. Different tags let the agent prioritize.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>If a tag's purpose is visual emphasis or layout, delete it.</statement>
      <rationale source="v1-source">TBD</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Pick a name from the tag glossary. If you need a new tag, propose it as a glossary update first.</statement>
      <rationale source="v1-source">Closed grammars are validatable; ad-hoc grammars drift.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Indent for parser hygiene only (2 spaces per level). Do not pad with blank lines or decorative comments.</statement>
      <rationale source="v1-source">TBD</rationale>
    </rule>
  </section>
</text>
