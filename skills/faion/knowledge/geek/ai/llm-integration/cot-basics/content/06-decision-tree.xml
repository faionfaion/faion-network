<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="CoT Basics Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Does the task require ≥2 reasoning steps before the final answer?</root-question>
    <branch when="reasoning_steps &gt;= 2">
      <question>Is the model a reasoning model (Claude Extended Thinking, o-series)?</question>
      <branch when="model.reasoning == true">
        <conclusion ref="r4"/>
      </branch>
      <branch when="model.reasoning == false">
        <question>Does cost budget allow 2-3x output tokens AND latency budget tolerate 30% slower responses?</question>
        <branch when="budget.tokens_ok == true AND budget.latency_ok == true">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="budget.tokens_ok == false OR budget.latency_ok == false">
          <conclusion ref="skip-this-methodology"/>
        </branch>
      </branch>
    </branch>
    <branch when="reasoning_steps &lt; 2">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="multi-step word problem, sonnet-non-thinking, budget OK → run-the-checklist"/>
  </decision-tree>
</text>
