<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="AI Failure Mode Taxonomy Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Do ≥2 teams operate LLM-backed features AND a central owner can publish + version a shared taxonomy?</root-question>
    <branch when="teams.count &gt;= 2 AND owner.exists == true">
      <question>Are ≥30 incidents or eval categories available to cluster?</question>
      <branch when="data.count &gt;= 30">
        <conclusion ref="run-the-checklist"/>
      </branch>
      <branch when="data.count &lt; 30">
        <conclusion ref="r6"/>
      </branch>
    </branch>
    <branch when="teams.count &lt; 2 OR owner.exists == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="2 product teams + ml-platform owner + 60 incidents → run-the-checklist"/>
  </decision-tree>
</text>
