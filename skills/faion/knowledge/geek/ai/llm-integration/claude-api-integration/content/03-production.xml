<?xml version="1.0" encoding="UTF-8"?>
<text id="03-production" title="Production Service Wrapper and Error Handling">
  <summary>ClaudeService class with ClaudeConfig dataclass, tenacity retry on transient errors, stop_reason validation, and token tracking.</summary>

  <section title="ClaudeService and ClaudeConfig">
    <reference path="templates/claude-service.py">ClaudeService class with ClaudeConfig and retry decorator</reference>
    <example type="good">
      <code lang="python"><![CDATA[
from dataclasses import dataclass
from typing import Optional, List, Dict, Any
import logging
from anthropic import Anthropic

@dataclass
class ClaudeConfig:
    model: str = "claude-sonnet-4-20250514"
    max_tokens: int = 1024
    system: str = ""

class ClaudeService:
    def __init__(self, api_key: Optional[str] = None):
        self.client = Anthropic(api_key=api_key)
        self.logger = logging.getLogger(__name__)

    def complete(self, messages: List[Dict[str, Any]], config: Optional[ClaudeConfig] = None) -> Dict:
        config = config or ClaudeConfig()
        response = self.client.messages.create(
            model=config.model,
            max_tokens=config.max_tokens,
            system=config.system,
            messages=messages
        )
        if response.stop_reason == "max_tokens":
            raise RuntimeError("Response truncated — increase max_tokens or split task")
        return {
            "content": response.content[0].text,
            "model": response.model,
            "usage": {"input": response.usage.input_tokens, "output": response.usage.output_tokens},
            "stop_reason": response.stop_reason
        }
]]></code>
    </example>
  </section>

  <section title="Tenacity retry">
    <example type="good">
      <code lang="python"><![CDATA[
from anthropic import RateLimitError, APIConnectionError, AuthenticationError, APIError
from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type

@retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=4, max=60),
    retry=retry_if_exception_type((RateLimitError, APIConnectionError))
)
def robust_claude(messages: list, system: str = "") -> str:
    try:
        response = client.messages.create(
            model="claude-sonnet-4-20250514",
            max_tokens=1024,
            system=system,
            messages=messages,
            timeout=60.0
        )
        return response.content[0].text
    except AuthenticationError:
        raise ValueError("Invalid ANTHROPIC_API_KEY")
    except APIError as e:
        if e.status_code >= 500:
            raise APIConnectionError("Server error")
        raise
]]></code>
    </example>
  </section>
</text>
