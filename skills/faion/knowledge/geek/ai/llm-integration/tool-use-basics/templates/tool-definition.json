{
  "type": "function",
  "function": {
    "name": "tool_name",
    "description": "One sentence: what this tool does and when to call it. Be specific — the model uses this to decide when to call the tool.",
    "parameters": {
      "type": "object",
      "properties": {
        "required_param": {
          "type": "string",
          "description": "What this param represents. Include valid values or format."
        },
        "optional_param": {
          "type": "string",
          "enum": ["option_a", "option_b"],
          "description": "One of: option_a (meaning A), option_b (meaning B). Default: option_a."
        }
      },
      "required": ["required_param"]
    }
  }
}
