<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="openai-embeddings">
  <metadata>
    <tier>geek</tier>
    <group>ai</group>
    <domain>llm-integration</domain>
    <summary>OpenAI embedding models with cosine similarity search. Ingestion phase (chunk, embed, upsert) and query phase (embed, search, return top-k). Use Batch API for 50% cost reduction.</summary>
    <tags>
      <tag>embeddings</tag>
      <tag>openai</tag>
      <tag>rag</tag>
      <tag>vector-search</tag>
      <tag>batch-api</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-25</created>
  </metadata>

  <content>
    <title>OpenAI Embeddings</title>

    <summary>Generate dense vector representations of text using OpenAI embedding models and search them with cosine similarity. Two-phase workflow: (1) ingestion — chunk documents, embed in batch, upsert into vector store; (2) query — embed user query on demand, nearest-neighbor search, return top-k chunks. Use the Batch API for nightly re-indexing (50% cost reduction).</summary>

    <why>
      <p>Semantic search finds intent-matching documents that keyword search misses. Embeddings enable RAG retrieval, deduplication, clustering, and zero-shot classification without fine-tuning. The Batch API makes offline enrichment of large datasets economically viable.</p>
    </why>

    <when-to-use>
      <list>
        <item>Semantic search over a corpus (docs, tickets, FAQs) where keyword search misses intent</item>
        <item>Building or extending a RAG pipeline requiring dense vector retrieval</item>
        <item>Deduplication or clustering of text records at scale</item>
        <item>Classifying user input without a fine-tuned model</item>
        <item>Offline/async enrichment of large datasets (Batch API for 50% cost reduction)</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Real-time latency-sensitive paths where BM25 keyword index is fast enough</item>
        <item>Corpus fits in context — send documents directly to the LLM instead</item>
        <item>Exact string match is the requirement — embeddings are approximate</item>
        <item>Very short strings (1–3 words) — cosine similarity degrades; use BM25 or hybrid search</item>
      </list>
    </when-not-to-use>

    <section name="embedding-models-chunking-and-similarity">
      <p>Model selection guide, chunking rules, cosine similarity, and dimension reduction trade-offs for OpenAI embeddings.</p>

      <section name="model-selection">
        <rule>
          <statement>Use text-embedding-3-small for most tasks (20x cheaper than ada-002 with better quality); only switch to text-embedding-3-large when retrieval recall is measurably insufficient after benchmarking.</statement>
          <rationale>text-embedding-3-small at $0.02/M tokens vs. text-embedding-3-large at $0.13/M tokens represents a 6.5x cost difference. Quality gap is negligible for most semantic search tasks.</rationale>
        </rule>

        <rule>
          <statement>Never mix embedding model or dimensions between ingestion and query — incompatible vectors produce random similarity scores with no error.</statement>
          <rationale>Embedding a query with text-embedding-3-small at 1536 dims against a corpus embedded with text-embedding-3-large at 3072 dims produces meaningless cosine similarity values.</rationale>
        </rule>
      </section>

      <section name="chunking-rules">
        <rule>
          <statement>Chunk documents at semantic boundaries (paragraphs, sections) with ~10% overlap; always chunk before embedding — input silently truncated at 8191 tokens produces poor-quality embeddings for the truncated portion.</statement>
          <rationale>Fixed-token chunking at sentence boundaries loses semantic coherence. Overlap prevents missing information at chunk boundaries. Silent truncation produces embeddings that represent only part of the content.</rationale>
        </rule>

        <rule>
          <statement>Store both the embedding vector and the original chunk text; cache embeddings for static content — never re-embed unchanged text.</statement>
          <rationale>Embeddings cannot be reversed to recover text. Agents that store only vectors have no way to return the source text in search results. Re-embedding static content wastes money and time.</rationale>
        </rule>
      </section>

      <section name="similarity-computation">
        <example type="good">
          <code lang="python"><![CDATA[
import numpy as np
from openai import OpenAI

client = OpenAI()

def embed(texts: list[str], model="text-embedding-3-small",
          dims=1536) -> list[list[float]]:
    resp = client.embeddings.create(model=model, input=texts, dimensions=dims)
    return [d.embedding for d in resp.data]

def cosine(a, b) -> float:
    a, b = np.array(a), np.array(b)
    return float(np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b)))

def top_k(query_emb, corpus_embs, corpus_texts, k=5):
    scores = [(cosine(query_emb, e), t)
              for e, t in zip(corpus_embs, corpus_texts)]
    return sorted(scores, reverse=True)[:k]
]]></code>
          <note>Normalize embeddings before storing — cosine similarity on normalized vectors equals dot product, which most vector DBs optimize for.</note>
        </example>

        <rule>
          <statement>For RAG retrieval, embed a rewritten/expanded version of the user query, not the raw message; expanded queries improve recall for short or ambiguous inputs.</statement>
          <rationale>Short queries (2–5 words) embed into sparse regions of the vector space that miss relevant documents. Query expansion via LLM rewrites them into semantically richer representations.</rationale>
        </rule>

        <reference path="templates/embed-and-search.py">Reusable embed + cosine + top_k utility</reference>
      </section>
    </section>

    <section name="openai-batch-api-for-bulk-embeddings-and-completions">
      <p>50% cost reduction batch workflow: JSONL file creation, submission, status polling, and result ordering via custom_id.</p>

      <section name="batch-api-rules">
        <rule>
          <statement>Assign meaningful custom_id values (e.g., doc_{id}_chunk_{n}) to every batch request; always use custom_id to reconstruct result order — never assume FIFO output ordering.</statement>
          <rationale>The Batch API returns results in arbitrary order. Agents that rely on positional ordering will silently misattribute results. custom_id is the only reliable correlation key.</rationale>
        </rule>

        <rule>
          <statement>Use the Batch API for nightly re-indexing and offline enrichment only; synchronous embedding is correct for interactive query paths — the 24-hour completion window makes Batch unsuitable for real-time.</statement>
          <rationale>Batch API has a 24-hour completion window and a 50K requests/batch ceiling. Interactive queries cannot wait 24 hours; nightly jobs benefit from the 50% cost reduction.</rationale>
        </rule>

        <example type="good">
          <code lang="python"><![CDATA[
import json
from openai import OpenAI

client = OpenAI()

# Build JSONL batch file
requests = [
    {
        "custom_id": f"doc_{doc_id}_chunk_{chunk_n}",
        "method": "POST",
        "url": "/v1/embeddings",
        "body": {
            "model": "text-embedding-3-small",
            "input": chunk_text,
        },
    }
    for doc_id, chunk_n, chunk_text in chunks
]

with open("batch.jsonl", "w") as f:
    for req in requests:
        f.write(json.dumps(req) + "\n")

# Submit
batch_file = client.files.create(file=open("batch.jsonl", "rb"),
                                  purpose="batch")
batch = client.batches.create(
    input_file_id=batch_file.id,
    endpoint="/v1/embeddings",
    completion_window="24h",
)

# Poll until complete, then parse by custom_id
batch = client.batches.retrieve(batch.id)
if batch.status == "completed":
    lines = client.files.content(batch.output_file_id).text.strip().split("\n")
    results = {json.loads(l)["custom_id"]: json.loads(l) for l in lines}
]]></code>
        </example>

        <antipattern>
          <description>Embedding sensitive PII (names, emails, IDs) into a shared vector store.</description>
          <reason>Data leaks via nearest-neighbor queries are a real attack surface — an adversary can extract PII by querying with known PII fragments and retrieving neighbors.</reason>
        </antipattern>
      </section>
    </section>

    <section name="agentic-workflow">
      <p>An agent embedding pipeline runs in two phases: (1) ingestion — chunk documents, embed in batch, upsert into a vector store; (2) query — embed the user query on demand, nearest-neighbor search, return top-k chunks. Agents should embed queries with the same model and dimension settings used during ingestion. Batch embedding via the OpenAI Batch API is the right choice for nightly re-indexing jobs; synchronous embedding is for interactive query paths only.</p>

      <section name="recommended-subagents">
        <list>
          <item>faion-sdd-executor-agent — coordinates embed-and-store pipeline as a multi-step SDD task</item>
        </list>
      </section>

      <section name="prompt-pattern">
        <p>For RAG retrieval, embed the rewritten/expanded query, not the raw user message:</p>
        <example type="good">
          <code lang="python"><![CDATA[
# Expand query before embedding for better recall
expanded = llm.complete(f"Rewrite for semantic search: {user_query}")
embedding = client.embeddings.create(model="text-embedding-3-small", input=expanded).data[0].embedding
]]></code>
        </example>
      </section>
    </section>

    <section name="best-practices">
      <list>
        <item>Use `text-embedding-3-small` for most tasks — 20x cheaper than `ada-002` with better quality; `text-embedding-3-large` only when retrieval recall is measurably insufficient</item>
        <item>Normalize embeddings before storing — cosine similarity on normalized vectors equals dot product, which most vector DBs optimize for</item>
        <item>Chunk documents at semantic boundaries (paragraphs, sections), not fixed token counts; overlap chunks by ~10% to avoid boundary misses</item>
        <item>Store both the embedding and the chunk text; never try to reverse-engineer text from an embedding</item>
        <item>Use `dimensions` reduction only after benchmarking recall — start at full dimensions, reduce if cost is prohibitive</item>
        <item>For Batch API jobs, assign meaningful `custom_id` values (e.g., `doc_{id}_chunk_{n}`) to correlate results to source records</item>
        <item>Cache embeddings for static content — re-embedding the same text wastes money and time</item>
      </list>
    </section>

    <section name="ai-agent-gotchas">
      <list>
        <item>Agents must chunk long documents before embedding — silently truncated inputs produce poor embeddings</item>
        <item>The Batch API returns results out of order; always use `custom_id` to reconstruct order, never assume FIFO</item>
        <item>Embedding a query and a document with different `dimensions` settings produces incomparable vectors — validate config parity at startup</item>
        <item>Rate limits on the embeddings endpoint are separate from chat completions — an agent that embeds in a loop can exhaust embeddings limits while chat limits are untouched</item>
        <item>Do not embed sensitive PII into a shared vector store — data leaks via nearest-neighbor queries are a real attack surface</item>
      </list>
    </section>

    <section name="limitations">
      <list>
        <item>`text-embedding-3-large` max input is 8191 tokens — longer docs must be chunked before embedding</item>
        <item>Cosine similarity degrades for very short strings (1-3 words) — use BM25 or hybrid search instead</item>
        <item>Embedding model output is not comparable across different models or dimension settings — reindex if you change either</item>
        <item>Batch API has a 24-hour completion window and 50K requests/batch ceiling — not suitable for real-time</item>
        <item>Reducing dimensions (`dimensions=256`) saves cost but may hurt recall for nuanced queries; benchmark before using in production</item>
      </list>
    </section>

    <templates>
      <template path="templates/embed-and-search.py" purpose="embed() + cosine() + top_k() utility functions"/>
    </templates>

    <see-also>
      <link href="https://platform.openai.com/docs/guides/embeddings"/>
      <link href="https://platform.openai.com/docs/api-reference/embeddings"/>
      <link href="https://platform.openai.com/docs/guides/batch"/>
      <link href="https://github.com/openai/openai-python"/>
      <link href="https://arxiv.org/abs/2201.10005"/>
    </see-also>
  </content>
</methodology>
