<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Model Eval Control Bands Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Do all Prerequisites resolve to typed input artefacts AND is there a named owner?</root-question>
    <branch when="inputs.all_typed == true AND owner.is_named_person_or_role == true">
      <question>Is there a downstream consumer (human or agent) that will read this artefact?</question>
      <branch when="downstream.consumer_exists == true">
        <conclusion ref="run-the-checklist"/>
      </branch>
      <branch when="downstream.consumer_exists == false">
        <conclusion ref="skip-this-methodology"/>
      </branch>
    </branch>
    <branch when="inputs.all_typed == false OR owner.is_named_person_or_role == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="3 metric series + named SRE owner + dashboard consumer → run-the-checklist"/>
  </decision-tree>
</text>
