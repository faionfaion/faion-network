<?xml version="1.0" encoding="UTF-8"?>
<text id="03-anti-patterns" title="Anti-patterns in semantic XML">
  <summary>Concrete failure modes when authoring agent-readable XML, with reasons and fixes.</summary>

  <section title="Formatting masquerading as semantics">
    <antipattern>
      <description>Tags named after presentation: `&lt;bold&gt;`, `&lt;italic&gt;`, `&lt;heading&gt;`, `&lt;br&gt;`.</description>
      <reason>Carries no role information. Agent gains nothing over Markdown.</reason>
    </antipattern>
    <rule testable="true">
      <statement>If a tag's purpose is visual emphasis or layout, delete it.</statement>
    </rule>
  </section>

  <section title="Inconsistent vocabulary">
    <antipattern>
      <description>Mixing `&lt;rule&gt;` in one file and `&lt;policy&gt;` or `&lt;requirement&gt;` in another for the same concept.</description>
      <reason>Agents can apply policy by tag only when the vocabulary is closed and consistent.</reason>
    </antipattern>
    <rule testable="true">
      <statement>Pick a name from the tag glossary. If you need a new tag, propose it as a glossary update first.</statement>
      <rationale>Closed grammars are validatable; ad-hoc grammars drift.</rationale>
    </rule>
    <reference path="templates/tag-glossary.xml">The closed list.</reference>
  </section>

  <section title="Generic over-wrapping">
    <antipattern>
      <description>Wrapping every paragraph in `&lt;text&gt;`, `&lt;content&gt;`, `&lt;block&gt;`, or `&lt;item&gt;` when no role distinction exists.</description>
      <reason>Adds depth without information; hurts both human readers and agent token economy.</reason>
    </antipattern>
  </section>

  <section title="CDATA leaks and entity bugs">
    <antipattern>
      <description>Embedding angle brackets or ampersands in element text without CDATA or entity escaping.</description>
      <reason>Parser breaks. Agent sees corrupted content or refuses to load the file.</reason>
    </antipattern>
    <example type="bad">
      <code lang="xml"><![CDATA[
<note>Use a < b for less-than checks.</note>
      ]]></code>
      <note>Bare `&lt;` inside element text is a parse error.</note>
    </example>
    <example type="good">
      <code lang="xml"><![CDATA[
<note>Use &lt; for less-than checks, or wrap code in <code lang="..."> with CDATA.</note>
      ]]></code>
    </example>
  </section>

  <section title="Mixing prose with structured rules">
    <antipattern>
      <description>One giant `&lt;p&gt;` containing a rule, a rationale, an example, and three see-also links.</description>
      <reason>Defeats the point of XML. Split into typed siblings: `&lt;rule&gt;`, `&lt;example&gt;`, `&lt;see-also&gt;`.</reason>
    </antipattern>
  </section>

  <section title="Treating XML as a document format">
    <antipattern>
      <description>Indenting for aesthetics, adding empty lines between every tag, embedding ASCII-art borders.</description>
      <reason>Agent does not care about layout. Whitespace is noise; ASCII art is forbidden by repo rules anyway.</reason>
    </antipattern>
    <rule testable="true">
      <statement>Indent for parser hygiene only (2 spaces per level). Do not pad with blank lines or decorative comments.</statement>
    </rule>
  </section>
</text>
