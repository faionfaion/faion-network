<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Claude Best Practices Core Rules" version="1.0.1" est_tokens="2650" depth="essential">
  <summary>21 testable rules forming the production policy pack: tier-aware model selection, full-date pinning, explicit max_tokens, cache layout, retry-after parsing + shared bucket, batch eligibility. Rules for selecting the right Claude model tier (Haiku, Sonnet, Opus) by task type, setting max_tokens correctly, and avoiding silent truncation of structured output. Rules for prompt caching, Batch API usage, token counting pre-flight, and measuring actual savings in production Claude pipelines. Rules for retry with backoff, rate-limit header parsing, fallback model selection, and async batch processing in production Claude pipelines.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use Haiku for routing/triage/classification, Sonnet for generation/analysis, Opus only for complex reasoning/architecture; never use Opus in a high-throughput inner loop.</statement>
      <rationale source="docs.anthropic.com/en/docs/about-claude/models">Opus rate limits are tighter than Sonnet; a pipeline that works with Sonnet will 429 immediately with Opus.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Pin model ids with full-date suffixes in every production config AND log `response.model` (the model actually used) on every call, not just the model requested.</statement>
      <rationale source="claude-best-practices migration notes">Aliases can resolve to a different snapshot than requested; without `response.model` logging, fallback behaviour is invisible.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Always set `max_tokens` explicitly on every call; treat `stop_reason=="max_tokens"` on a structured-output call as INVALID and retry with higher max_tokens (do not parse a truncated payload).</statement>
      <rationale source="docs.anthropic.com/en/api/messages">Default `max_tokens` is low; silent truncation produces invalid JSON that downstream parsers handle wrong.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Place stable content at the top of the system prompt with `cache_control:{"type":"ephemeral"}`; put dynamic instructions last; target `cache_read_input_tokens / input_tokens > 0.70`.</statement>
      <rationale source="platform.claude.com/docs/en/build-with-claude/prompt-caching">Cache uses prefix matching; dynamic content before the cached block busts the cache; below ~70% hit ratio the 25% write surcharge erodes savings.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Parse the `retry-after` header from `RateLimitError` instead of guessing the backoff; in multi-worker orchestrators use a SHARED token bucket, not per-process backoff.</statement>
      <rationale source="docs.anthropic.com/en/api/errors">Per-process backoff does not account for other workers consuming quota; the API supplies the exact wait time.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Use Batch API for any workload that tolerates 24h SLA — 50% discount on input + output with no other code changes.</statement>
      <rationale source="docs.anthropic.com/en/api/messages-batches">Batch API halves cost for offline workloads; no reason to skip when latency allows.</rationale>
    </rule>
    <rule id="tier-by-task-type" testable="true" depth="essential">
      <statement>Use Haiku for formatting, routing, classification, and simple extraction tasks. Use Sonnet for generation, code, and analysis. Use Opus for complex reasoning, architecture, and strategy tasks.</statement>
      <rationale source="01-model-selection">Model capability and cost scale together. Using Opus for a simple routing decision wastes ~10x the budget. Using Haiku for complex reasoning produces lower-quality output that may require re-work.</rationale>
    </rule>
    <rule id="pin-exact-model-ids" testable="true" depth="essential">
      <statement>Pin exact model IDs in production configs (e.g., "claude-sonnet-4-20250514") rather than aliases ("claude-sonnet-4").</statement>
      <rationale source="01-model-selection">Aliases can update to a newer model version without notice. A silent model upgrade can change output format or behavior, breaking downstream parsing.</rationale>
    </rule>
    <rule id="log-model-actually-used" testable="true" depth="essential">
      <statement>Log the model actually used in every response, not just the model requested. Log this when implementing fallback logic.</statement>
      <rationale source="01-model-selection">Fallback to a cheaper model is invisible to the orchestrator unless explicitly logged. Downstream agents may produce worse outputs without knowing the model was downgraded.</rationale>
    </rule>
    <rule id="max-tokens-explicit-every-call" testable="true" depth="essential">
      <statement>Always set max_tokens explicitly on every Messages API call.</statement>
      <rationale source="01-model-selection">The default max_tokens is low and silently truncates structured output mid-JSON, producing invalid payloads that are hard to debug because the API returns HTTP 200.</rationale>
    </rule>
    <rule id="truncated-structured-output-retry" testable="true" depth="essential">
      <statement>When stop_reason == "max_tokens" on a structured output call, the JSON is truncated and invalid. Retry with higher max_tokens; do not parse the response.</statement>
      <rationale source="01-model-selection">A truncated JSON response from a structured output call will raise a parse error downstream. The correct action is to retry with more headroom, not to handle the parse error.</rationale>
    </rule>
    <rule id="stable-prefix-first-with-cache-control" testable="true" depth="essential">
      <statement>Place the most stable content at the top of the system prompt with cache_control: ephemeral. Put dynamic instructions last.</statement>
      <rationale source="02-cost-optimization">Cache uses prefix matching. Dynamic content at the top busts the cache on every call. Stable content at the top maximizes the proportion of input tokens that are cached.</rationale>
    </rule>
    <rule id="no-dynamic-values-before-cached-block" testable="true" depth="essential">
      <statement>Never inject timestamps, request IDs, or user-specific values before the cached block.</statement>
      <rationale source="02-cost-optimization">Even a single character difference in the prefix causes a cache miss. You pay the write cost (25% above regular) with no read benefit.</rationale>
    </rule>
    <rule id="measure-cache-hit-ratio" testable="true" depth="essential">
      <statement>Measure cache_read_input_tokens / input_tokens ratio per pipeline run. Target above 0.70.</statement>
      <rationale source="02-cost-optimization">Below 70% cache hit rate, write overhead starts eroding the 90% read savings. Low hit rate indicates the cached prefix is being modified per call.</rationale>
    </rule>
    <rule id="batch-api-for-24h-tolerant-workloads" testable="true" depth="essential">
      <statement>Use Batch API for workloads that tolerate up to 24-hour latency to receive 50% cost reduction.</statement>
      <rationale source="02-cost-optimization">Batch API cuts cost in half with no other code changes. The only requirement is that the workload is not real-time: offline enrichment, nightly analysis, evaluation suites.</rationale>
    </rule>
    <rule id="count-tokens-preflight" testable="true" depth="essential">
      <statement>Use client.messages.count_tokens() for pre-flight checks before paying for large-input calls.</statement>
      <rationale source="02-cost-optimization">Token counting is cheap (no generation cost) and prevents paying full generation cost for inputs that exceed the context limit, which fail with an API error.</rationale>
    </rule>
    <rule id="parse-retry-after-header" testable="true" depth="essential">
      <statement>Parse the retry-after header from RateLimitError instead of guessing the backoff interval.</statement>
      <rationale source="03-reliability-patterns">The Anthropic API includes a retry-after header with the exact wait time. Guessing with exponential backoff may wait too long or too short, degrading throughput unnecessarily.</rationale>
    </rule>
    <rule id="backoff-with-jitter-on-5xx" testable="true" depth="essential">
      <statement>Implement exponential backoff with jitter for 5xx server errors; retry up to 3 times before raising.</statement>
      <rationale source="03-reliability-patterns">5xx errors are transient. A single retry captures most transient failures. More than 3 retries risks cascading delays in multi-step pipelines.</rationale>
    </rule>
    <rule id="shared-rate-bucket-multi-worker" testable="true" depth="essential">
      <statement>In multi-worker orchestrators, use a shared rate-limit token bucket rather than per-process backoff.</statement>
      <rationale source="03-reliability-patterns">Per-process backoff does not account for other workers consuming quota simultaneously. A shared bucket prevents all workers from hammering the API at the same time after a backoff period.</rationale>
    </rule>
    <rule id="log-model-on-fallback" testable="true" depth="essential">
      <statement>When falling back from Sonnet to Haiku on rate limiting, log the actual model used in the response.</statement>
      <rationale source="03-reliability-patterns">The orchestrator needs to know which model produced a given output. Silent fallback produces unexpected quality variations that are impossible to audit without this log.</rationale>
    </rule>
    <rule id="async-anthropic-with-gather" testable="true" depth="essential">
      <statement>Use AsyncAnthropic with asyncio.gather for concurrent batch calls. Do not spawn threads; the SDK is async-native.</statement>
      <rationale source="03-reliability-patterns">The Anthropic Python SDK is built on httpx with native async support. Using threads adds overhead and can introduce race conditions. asyncio.gather is the correct concurrency primitive.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
