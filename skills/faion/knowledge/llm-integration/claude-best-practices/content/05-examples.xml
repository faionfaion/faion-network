<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Claude Best Practices Examples" version="1.0.0" est_tokens="850" depth="essential">
  <summary>Three worked snippets for the policy pack: tiered model table with explicit max_tokens and stop_reason check, cached system prompt with hit-rate monitoring, and a monitored client logging the model actually used.</summary>

  <section title="Model tier table and truncation check">
  <example type="good">
    <code lang="python"><![CDATA[
import anthropic

MODELS = {
    "router": "claude-3-5-haiku-20241022",
    "generator": "claude-sonnet-4-20250514",
    "reasoner": "claude-opus-4-5-20251101",
}

client = anthropic.Anthropic()

def call(role: str, messages: list, max_tokens: int = 2048) -> str:
    model = MODELS[role]
    r = client.messages.create(
        model=model,
        max_tokens=max_tokens,
        messages=messages
    )
    if r.stop_reason == "max_tokens":
        raise ValueError(f"Response truncated for role={role}. Retry with higher max_tokens.")
    return r.content[0].text
    ]]></code>
    <note>max_tokens is explicit and stop_reason is checked before returning the response.</note>
  </example>
  </section>

  <section title="Cached system prompt structure">
  <example type="good">
    <code lang="python"><![CDATA[
import anthropic

client = anthropic.Anthropic()

# Cached system prompt structure
STATIC_DOCS = "... large stable documentation (10k+ tokens) ..."

system = [
    {
        "type": "text",
        "text": STATIC_DOCS,
        "cache_control": {"type": "ephemeral"}  # cached prefix
    },
    {
        "type": "text",
        "text": dynamic_instructions  # not cached; appended per-run
    }
]

response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=4096,
    system=system,
    messages=[{"role": "user", "content": user_task}]
)

# Monitor cache effectiveness
u = response.usage
hit_rate = u.cache_read_input_tokens / max(u.input_tokens, 1)
    ]]></code>
    <note>Static docs are marked cached; dynamic instructions appended without cache_control.</note>
  </example>
  <reference path="templates/cached-system-prompt.py">System prompt with static cached prefix and dynamic tail</reference>
  </section>

  <section title="Monitored client with model and latency logging">
  <example type="good">
    <code lang="python"><![CDATA[
import anthropic, logging, time

log = logging.getLogger(__name__)
client = anthropic.Anthropic()

def call(model: str, system: list, messages: list, max_tokens: int = 2048):
    t0 = time.monotonic()
    r = client.messages.create(
        model=model, max_tokens=max_tokens,
        system=system, messages=messages
    )
    elapsed = time.monotonic() - t0
    total_tok = r.usage.input_tokens + r.usage.output_tokens
    log.info("model=%s tokens=%d elapsed=%.2fs", model, total_tok, elapsed)
    return r
    ]]></code>
    <note>Actual model ID is logged (not the requested alias), enabling post-hoc audit of fallback behavior.</note>
  </example>
  <reference path="templates/monitored-client.py">Minimal Claude client with token usage and latency logging</reference>
  </section>

  <see-also>
    <link path="content/01-core-rules.xml"/>
    <link path="content/04-procedure.xml"/>
  </see-also>
</text>
