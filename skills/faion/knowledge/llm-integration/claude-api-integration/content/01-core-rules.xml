<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Claude API Integration Core Rules" version="1.0.1" est_tokens="1750" depth="essential">
  <summary>11 testable rules covering the full Claude integration surface: sync/async/streaming, tool use, vision, Extended Thinking, Prompt Caching, Batch API. Each has a statement, rationale, source. Background lists the model tiers and the cost levers of caching and batch.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Validate `response.stop_reason` in the service layer (NOT in each subagent): raise on `max_tokens`, branch on `tool_use`, return on `end_turn`.</statement>
      <rationale source="docs.anthropic.com/en/api/messages">Truncation is silent. Treating a truncated response as complete corrupts downstream parsing. Tool use loop continuation must be uniform across call sites.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>When streaming, prefer `messages.stream` with `text_stream` for plain text and explicit event-level iteration only when tool use or thinking deltas are expected; never parse partial JSON inline — accumulate `input_json_delta` and parse after `content_block_stop`.</statement>
      <rationale source="docs.anthropic.com/en/api/messages-streaming">Partial JSON is not valid; inline parsing raises `JSONDecodeError`. Event format differs from OpenAI SSE so blind cross-provider parsing fails.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>For tool use, always submit `{"role":"assistant","content": response.content}` (the full block list) before the `tool_result`, and supply `tool_use_id` matching the model's `id` on each tool result.</statement>
      <rationale source="claude-tool-use migration notes">Omitting `tool_use` blocks from history breaks role-alternation and triggers 400 on the next turn; missing `tool_use_id` makes results unmappable.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>For vision: encode local files to base64 and place image content BEFORE text content in the user `content` array; pre-fetch URL images for production reliability.</statement>
      <rationale source="docs.anthropic.com/en/docs/build-with-claude/vision">Image-first ordering empirically improves comprehension; URL-based images can return content-error blocks at inference time.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Never combine Extended Thinking (`thinking.type="enabled"`) with `temperature`; set `max_tokens &gt;= budget_tokens + 4096`.</statement>
      <rationale source="docs.anthropic.com/en/docs/build-with-claude/extended-thinking">The API rejects the combination; insufficient `max_tokens` truncates the answer block silently.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>For Prompt Caching, mark a ≥1024-token stable prefix with `cache_control:{"type":"ephemeral"}` and keep ALL dynamic content (timestamps, user ids) AFTER the cached block; monitor `usage.cache_read_input_tokens / usage.input_tokens` and assert &gt;0.70.</statement>
      <rationale source="platform.claude.com/docs/en/build-with-claude/prompt-caching">Prefix is byte-identical match. Drift before the cached block invalidates the cache; below ~70% hit ratio the 25% write surcharge erodes the 90% read savings.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>For Batch API: poll at intervals ≥60s using `client.messages.batches.retrieve` until `processing_status == "ended"`; iterate results and resubmit `result.type == "errored"` items; use `custom_id` equal to your DB primary key.</statement>
      <rationale source="docs.anthropic.com/en/api/messages-batches">Tight polling adds zero speedup; errored items are not auto-retried; `custom_id` is the only join key against out-of-order results.</rationale>
    </rule>
    <rule id="model-tier-by-task" testable="true" depth="essential">
      <statement>Use Haiku for triage/routing steps, Sonnet for main tasks, Opus only for complex reasoning or architecture — rate limits for Opus are significantly lower than Sonnet.</statement>
      <rationale source="01-client-setup">A pipeline that works with Sonnet may 429 immediately with Opus; Opus rate limits are much tighter, making it unsuitable for high-throughput inner loops.</rationale>
    </rule>
    <rule id="thinking-disables-temperature" testable="true" depth="essential">
      <statement>Extended Thinking is incompatible with temperature — setting both raises an API error; disable temperature when enabling thinking.</statement>
      <rationale source="02-tool-use; short form of r5">Extended Thinking has its own sampling parameters; temperature is redundant and causes a validation error.</rationale>
    </rule>
    <rule id="prompt-caching-byte-identical-prefix" testable="true" depth="essential">
      <statement>Enable Prompt Caching on any system prompt over 1024 tokens that stays constant across calls — the cached prefix must be byte-identical; a single extra space invalidates the cache.</statement>
      <rationale source="04-advanced">Prompt Caching charges cached tokens at ~10% of normal input cost; for pipelines calling the same assistant hundreds of times per session, this is the single largest cost reduction available.</rationale>
    </rule>
    <rule id="batch-api-offline-only" testable="true" depth="essential">
      <statement>Use the Batch API only for offline enrichment tasks — it has up to 24-hour processing time and is not suitable for real-time workflows.</statement>
      <rationale source="04-advanced">Batch API provides 50% cost reduction by processing requests when compute is available; latency is unbounded and unsuitable for user-facing responses.</rationale>
    </rule>
  </section>

  <section title="Background">
    <p>Prompt Caching cuts costs for repetitive pipelines (1024-token minimum, byte-identical prefix); Batch API provides 50% cost reduction for offline enrichment jobs with up to 24-hour processing time.</p>
    <list>
      <item>claude-sonnet-4-20250514: best balance of speed/quality — default choice for most tasks</item>
      <item>claude-opus-*: most capable, complex reasoning and architecture decisions — significantly lower rate limits</item>
      <item>claude-haiku-*: fastest, most cost-effective — use for triage, classification, high-volume inner loops</item>
    </list>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
