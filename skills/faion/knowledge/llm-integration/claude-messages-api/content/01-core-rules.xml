<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="claude-messages-api Core Rules" version="1.0.1" est_tokens="2100" depth="essential">
  <summary>15 testable rules for claude-messages-api. Each rule has statement + rationale + source. Background lists the request parameters.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Validate `response.stop_reason` BEFORE accessing `response.content[0].text`; type-check each block since `content` is `list[TextBlock|ToolUseBlock|ThinkingBlock]`.</statement>
      <rationale source="docs.anthropic.com/en/api/messages">Indexing blindly into `content[0].text` raises AttributeError when the first block is a tool_use or thinking block.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Append the FULL `response.content` list (not just text) as the assistant turn in multi-turn history; consecutive same-role messages raise 400.</statement>
      <rationale source="claude-tool-use migration notes">Missing tool_use or thinking blocks corrupt conversation state and trigger 400 on the next turn.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Always set `max_tokens` explicitly; treat `stop_reason=="max_tokens"` as silent truncation: branch on it (do not parse), retry with larger budget OR decompose the task.</statement>
      <rationale source="docs.anthropic.com/en/api/messages">Default max_tokens is low; truncation is not signalled by HTTP status; downstream parsers handle it as a parse error not a truncation.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>For multimodal user content: place image / PDF parts BEFORE the text part in the `content` array; base64-encode local files; pre-fetch URL images before production calls.</statement>
      <rationale source="docs.anthropic.com/en/docs/build-with-claude/vision">Image-first ordering empirically improves comprehension; URL-based images can fail with content-error blocks at inference time.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>PDFs: validate page count ≤100 and size ≤32MB BEFORE base64-encoding; JPEG/PNG/WebP/GIF: validate ≤20MB.</statement>
      <rationale source="docs.anthropic.com/en/docs/build-with-claude/pdf-support">Over-limit assets fail at inference time after encoding+transmission, wasting bandwidth; the failure is an API error, not a truncation.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>For streaming + tool use: accumulate `input_json_delta.partial_json` per content block, parse only after `content_block_stop`; never inline-parse partial JSON.</statement>
      <rationale source="docs.anthropic.com/en/api/messages-streaming">Partial JSON is not valid; inline parsing raises JSONDecodeError; Claude SSE differs from OpenAI SSE so blind cross-provider parsing fails.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Tag every call with `metadata={"user_id":"..."}` for abuse detection and per-user cost attribution.</statement>
      <rationale source="docs.anthropic.com/en/api/messages">Without metadata, Anthropic's abuse signals and your own cost dashboard cannot map calls back to a user.</rationale>
    </rule>
    <rule id="max-tokens-headroom-for-output" testable="true" depth="essential">
      <statement>Always set max_tokens high enough for expected output — silent truncation (stop_reason="max_tokens") is the most common cause of corrupted downstream data.</statement>
      <rationale source="01-messages-api; short form of r3 / r1 / r2">max_tokens is required and easy to set too low; truncation produces parseable but incomplete output that passes format checks and fails semantic ones.</rationale>
    </rule>
    <rule id="check-stop-reason-before-content" testable="true" depth="essential">
      <statement>Check stop_reason before accessing content[0].text — if stop_reason is "tool_use", content[0] may be a tool_use block, not text.</statement>
      <rationale source="01-messages-api; short form of r3 / r1 / r2">Content blocks are polymorphic; indexing blindly into content[0].text raises AttributeError when the first block is a tool_use or thinking block.</rationale>
    </rule>
    <rule id="append-full-content-in-multi-turn" testable="true" depth="essential">
      <statement>Append the full resp.content list (not just text) as the assistant turn in multi-turn history — tool_use blocks must be preserved or the next turn gets a 400 error.</statement>
      <rationale source="01-messages-api; short form of r3 / r1 / r2">The API reconstructs conversation state from message history; missing tool_use blocks make the history inconsistent and cause an API error.</rationale>
    </rule>
    <rule id="image-before-text-in-content" testable="true" depth="essential">
      <statement>Place image content before text content in multimodal messages — model processes images better when they come first in the content array.</statement>
      <rationale source="02-vision">Empirically, image comprehension quality improves when the image precedes the question about it; this is consistent across Claude model versions.</rationale>
    </rule>
    <rule id="url-images-fetched-at-inference" testable="true" depth="essential">
      <statement>Image URLs are fetched at inference time — if the URL returns a 403 at that moment, the call fails silently with a content error block rather than an API error.</statement>
      <rationale source="02-vision">URL-based images depend on the URL being accessible at inference time; pre-fetch and base64-encode for reliability in production pipelines.</rationale>
    </rule>
    <rule id="validate-pdf-page-count-before-sending" testable="true" depth="essential">
      <statement>Validate PDF page count before sending — PDFs exceeding 100 pages produce an API error, not a truncation; validate before sending to prevent pipeline failures.</statement>
      <rationale source="02-vision">The error surfaces at inference time after the full PDF has been base64-encoded and transmitted; validating first avoids wasting the encoding and transmission.</rationale>
    </rule>
    <rule id="accumulate-input-json-delta" testable="true" depth="essential">
      <statement>For streaming + tool use, accumulate input_json_delta events and parse after content_block_stop — never parse partial JSON inline.</statement>
      <rationale source="03-streaming">input_json_delta events carry fragments of JSON that are not individually parseable; parsing before the block stops raises JSONDecodeError.</rationale>
    </rule>
    <rule id="claude-sse-differs-from-openai" testable="true" depth="essential">
      <statement>Claude SSE format differs from OpenAI SSE — they are not interchangeable without an adapter layer (e.g., LiteLLM).</statement>
      <rationale source="03-streaming">OpenAI uses data: [DONE] to end a stream; Claude uses message_stop event. The delta object structure also differs; treating them as equivalent causes silent parse failures.</rationale>
    </rule>
  </section>
  <section title="Background">
    <p>Messages API parameters:</p>
    <list>
      <item>model: required — model ID string</item>
      <item>max_tokens: required — max response tokens; cannot be omitted; truncates if too low</item>
      <item>messages: required — alternating user/assistant turns; consecutive same-role messages → 400 error</item>
      <item>system: optional — set behavior; kept stable across calls for Prompt Caching benefit</item>
      <item>temperature: optional — 0–1 (not 0–2 like OpenAI); 0=deterministic, 1=creative</item>
      <item>stop_sequences: optional — matches on next token boundary; mid-sentence stop is cut silently</item>
      <item>metadata: optional — add user_id for abuse detection and cost attribution</item>
    </list>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
