<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Creating or Updating Claude Code Commands Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema for the config artefact this methodology produces. Validator enforces required keys + per-field constraints.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://faion.net/schemas/commands.json",
  "type": "object",
  "required": ["name", "description", "tools"],
  "properties": {
    "name": {"type": "string", "pattern": "^[a-z][a-z0-9-]+$"},
    "description": {"type": "string", "minLength": 10, "maxLength": 300},
    "arguments": {"type": "array", "items": {"type": "object", "required": ["name", "type"], "properties": {"name": {"type": "string"}, "type": {"enum": ["string", "integer", "boolean", "path"]}, "required": {"type": "boolean"}}}},
    "tools": {"type": "array", "items": {"type": "string"}, "minItems": 1, "maxItems": 8},
    "line_count": {"type": "integer", "maximum": 250}
  }
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "name": "deploy",
  "description": "Deploys the current branch to the named environment after running pre-deploy checks.",
  "arguments": [
    {
      "name": "env",
      "type": "string",
      "required": true
    }
  ],
  "tools": [
    "Bash",
    "Read"
  ],
  "line_count": 87
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "name": "DeployStuff",
  "tools": []
}
]]></code>
    <note>Name not kebab-case; tools empty; missing description.</note>
  </example>

  <reference path="scripts/validate-commands.py">Validator script</reference>
</text>
