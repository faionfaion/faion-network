<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="KPI Drift Alarm Template Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for kpi-drift-alarm-template.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="baseline_rolling_window_signal == true">
      <conclusion ref="baseline-rolling-window"/>
    </branch>
    <branch when="threshold_explicit_direction_signal == true">
      <conclusion ref="threshold-explicit-direction"/>
    </branch>
    <branch when="two_class_alarm_signal == true">
      <conclusion ref="two-class-alarm"/>
    </branch>
    <branch when="suppression_after_fire_signal == true">
      <conclusion ref="suppression-after-fire"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
