<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Process Mining Tool Shortlist Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for process-mining-tool-shortlist.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="weighted_criteria_signal == true">
      <conclusion ref="weighted-criteria"/>
    </branch>
    <branch when="evidence_anchor_per_score_signal == true">
      <conclusion ref="evidence-anchor-per-score"/>
    </branch>
    <branch when="at_least_three_tools_signal == true">
      <conclusion ref="at-least-three-tools"/>
    </branch>
    <branch when="ownership_named_signal == true">
      <conclusion ref="ownership-named"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
