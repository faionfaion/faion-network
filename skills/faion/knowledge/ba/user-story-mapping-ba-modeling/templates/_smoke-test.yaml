# purpose: minimum viable filled story-map snapshot for smoke test
# consumes: n/a (self-contained)
# produces: schema-valid map for validator self-test parity
# depends-on: templates/story-map.yaml
# token-budget-impact: ~150 tokens

map_id: smoke
version_tag: v0.1.0
personas:
  - {id: u, name: "Smoke user"}
activities:
  - id: act-1
    name: Pick
    tasks:
      - id: t-1
        stories:
          - {id: s-1, title: "pick", persona_id: u, release: R1}
  - id: act-2
    name: Pay
    tasks:
      - id: t-2
        stories:
          - {id: s-2, title: "pay", persona_id: u, release: R1}
