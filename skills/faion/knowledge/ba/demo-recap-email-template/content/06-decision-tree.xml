<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Demo Recap Email Template Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule id from content/01-core-rules.xml for demo-recap-email-template.</summary>
  <decision-tree>
    <root-question id="q-root">Do the applies_if conditions hold AND no skip_if matches for this engagement?</root-question>
    <branch when="applies_if_all_satisfied == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="any_skip_if_matched == true">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <branch when="six_block_shape_signal == true">
      <conclusion ref="six-block-shape"/>
    </branch>
    <branch when="named_decider_signal == true">
      <conclusion ref="named-decider"/>
    </branch>
    <branch when="delta_evidence_signal == true">
      <conclusion ref="delta-evidence"/>
    </branch>
    <branch when="owner_and_date_signal == true">
      <conclusion ref="owner-and-date"/>
    </branch>
  </decision-tree>
  <example trace="Engagement passes applies_if, none of skip_if match. Observable check shows the first non-skip rule's signal is true → conclusion routes to that rule id → agent draft enforces the rule and attaches matching evidence."/>
</text>
