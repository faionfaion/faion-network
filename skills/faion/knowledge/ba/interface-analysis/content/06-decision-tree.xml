<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Interface Analysis Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml or to the universal skip-this-methodology conclusion.</summary>

  <decision-tree>
    <root-question id="q-root">Interfaces present AND ≥5 OR security-sensitive?</root-question>
    <branch when="interface_count &gt;= 5 OR security_sensitive == true">
      <conclusion ref="r1" verdict="Run full interface-analysis: catalog + per-interface spec + landscape merge."/>
    </branch>
    <branch when="interface_count &gt;= 1 AND security_sensitive == false">
      <conclusion ref="r2" verdict="Lightweight catalog + spec; skip the per-interface security depth."/>
    </branch>
    <branch when="interface_count == 0">
      <conclusion ref="skip-this-methodology" verdict="No boundaries to analyse; check scope."/>
    </branch>
    <branch when="default">
      <conclusion ref="r3" verdict="Always classify interface_type before spec drafting."/>
    </branch>
    <example trace="default path">
      <description>When the situation matches the leading branch, apply the referenced rule; otherwise traverse to the next condition until the default branch is reached.</description>
    </example>
  </decision-tree>

  <see-also>
    <link path="content/01-core-rules.xml"/>
  </see-also>
</text>
