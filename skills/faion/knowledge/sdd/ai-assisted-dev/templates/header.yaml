# purpose: Frontmatter schema for the ai-assisted-dev workflow record.
# consumes: Header block of templates/skeleton.md.
# produces: Validator-friendly frontmatter for the record file.
# depends-on: scripts/validate-ai-assisted-dev.py.
# token-budget-impact: zero — pure schema.

version: "1.0.0"
team: "<team name>"
owner: "role:<handle>"
