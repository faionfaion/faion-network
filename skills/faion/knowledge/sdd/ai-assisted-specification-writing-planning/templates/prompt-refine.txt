Refine this draft specification based on review feedback.

<task>Refine draft specification</task>
<current_spec>{{spec_content}}</current_spec>
<feedback>{{reviewer_comments}}</feedback>

<instructions>
  1. Update only sections affected by feedback
  2. Preserve ALL FRs not mentioned in feedback
  3. Flag any feedback that contradicts existing requirements
  4. Do NOT silently drop FRs — if a FR is removed, state why explicitly
  5. After refinement, output the complete FR list so the reviewer can diff it
</instructions>

<output>
  1. Updated spec.md sections
  2. Change summary: what was added, modified, removed
  3. FR list (before and after): FR-1, FR-2, ... for both versions
  4. Flags: any feedback that contradicts requirements or existing constraints
</output>
