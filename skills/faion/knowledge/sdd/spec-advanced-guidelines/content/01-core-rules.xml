<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Spec Advanced Guidelines Core Rules" version="1.1.0" est_tokens="2300" depth="essential">
  <summary>Testable rules grounding Spec Advanced Guidelines: trigger blocks, the required 14 sections, persona rules, user story format, FR traceability chain, NFR measurability, acceptance criteria coverage and assumptions. Each pairs a binary check with a sourced rationale; HARD rules — user-supplied input cannot override.</summary>

  <section title="Background">
    <p>Rules r-fourteen-sections through r-out-of-scope-three-fields cover the required 14 sections, persona rules, user story format, and FR traceability chain requirements. Rules r-nfr-measurable through r-assumptions-if-wrong cover NFR measurability rules, acceptance criteria coverage matrix, and agent-specific antipatterns for both. The antipatterns live in content/03-failure-modes.xml.</p>
  </section>

  <section title="Rule set">

    <rule id="r1-trigger-blocks-mandatory" testable="true" depth="essential">
      <statement>When a trigger fires (multi-team / regulated / long-lived / source-of-truth) the matching block is mandatory.</statement>
      <rationale source="Practitioner experience — spec advanced audit">Optional blocks get skipped under deadline pressure.</rationale>
    </rule>
    <rule id="r2-nfr-table-thresholds" testable="true" depth="essential">
      <statement>NFR table specifies threshold + verification method for every NFR; &apos;fast enough&apos; rejected.</statement>
      <rationale source="ISO 25010 quality model">Unmeasurable NFRs cannot be regression-checked.</rationale>
    </rule>
    <rule id="r3-ac-rubric-applied" testable="true" depth="essential">
      <statement>Every AC scored against the rubric; AC below threshold rewritten.</statement>
      <rationale source="ac-quality-rubric">Vague AC produces ambiguous done-definition.</rationale>
    </rule>
    <rule id="r4-glossary-source-of-truth" testable="true" depth="essential">
      <statement>Glossary terms are defined once in the spec; all other docs reference by ID.</statement>
      <rationale source="Single-source-of-truth practice">Duplicate definitions drift.</rationale>
    </rule>
    <rule id="r5-versioning-record" testable="true" depth="essential">
      <statement>Spec carries a versioning record (semver + last_reviewed); revisions append, never overwrite.</statement>
      <rationale source="Audit-trail practice">Without revision history, reviewers cannot tell what changed.</rationale>
    </rule>
    <rule id="r6-source-of-truth-pointer" testable="true" depth="essential">
      <statement>If the spec is a source-of-truth, its content_id appears in the front-matter and downstream docs reference it.</statement>
      <rationale source="Internal — content_id practice">Without a stable pointer, downstream references drift.</rationale>
    </rule>
    <rule id="r-fourteen-sections" testable="true" depth="essential">
      <statement>A full spec must include all 14 sections: Overview, Problem Statement, User Personas, User Stories, Functional Requirements, Non-Functional Requirements, Acceptance Criteria, Out of Scope, Assumptions and Constraints, Dependencies, Related Features, Recommended Skills, Open Questions, Appendix.</statement>
      <rationale source="spec-structure">Omitting any section creates a predictable gap: no Personas → generic user stories; no Out of Scope → scope creep in every session; no Assumptions → conflicting implementations.</rationale>
    </rule>
    <rule id="r-problem-statement-five-fields" testable="true" depth="essential">
      <statement>Problem Statement must answer: Who (target user group), Problem (what they face), Impact (business or user cost), Solution (high-level approach), Success Metric (how to measure the feature succeeded).</statement>
      <rationale source="spec-structure">A problem statement without a success metric produces a feature with no clear done criterion. Agents and developers optimize toward undefined targets without it.</rationale>
    </rule>
    <rule id="r-named-personas" testable="true" depth="essential">
      <statement>Write 2-3 personas with name, role, goals, pain points, and usage context. Reference personas by name in user stories and acceptance criteria.</statement>
      <rationale source="Practitioner experience — spec advanced audit">Named personas prevent "As a user" stories that apply to nobody specifically. Referencing them in AC forces scenario specificity.</rationale>
    </rule>
    <rule id="r-traceability-chain" testable="true" depth="essential">
      <statement>Every Functional Requirement must trace to at least one User Story (FR-X → US-Y). Every Acceptance Criterion must trace to at least one FR (AC-Z → FR-X). No FR may exist without a US parent. No AC may exist without an FR parent.</statement>
      <rationale source="Requirements traceability practice">Orphan requirements indicate either gold-plating (FR with no user story) or untestable design (FR with no AC). The chain ensures every requirement serves a stated user need and has a verifiable completion check.</rationale>
    </rule>
    <rule id="r-out-of-scope-three-fields" testable="true" depth="essential">
      <statement>Every Out of Scope item must include: feature name, one-sentence reason it is excluded, and a "when" (Phase N, v2, never). Do not include items without all three fields.</statement>
      <rationale source="Practitioner experience — spec advanced audit">Items without explicit exclusion reasoning re-emerge in every planning session. The "when" prevents permanent exclusion from being confused with deferred scope.</rationale>
    </rule>
    <rule id="r-nfr-measurable" testable="true" depth="essential">
      <statement>Every NFR must specify: quantifiable target, measurement approach (how to measure), and validation method (how to test). NFRs without quantifiable targets must be rejected and rewritten before spec approval.</statement>
      <rationale source="ISO 25010 quality model">"System should be fast" is not an NFR — it is a preference. Without a number, neither developers nor agents can know when the requirement is met.</rationale>
    </rule>
    <rule id="r-ac-coverage-matrix" testable="true" depth="essential">
      <statement>For each functional requirement, acceptance criteria must cover: happy path, at least one error case, at least one edge case (boundary condition or empty state). For security-sensitive FRs, add an unauthorized access scenario.</statement>
      <rationale source="ac-quality-rubric">Specs with only happy path AC produce features that break on first error condition in production. The coverage matrix forces specification of failure behavior before implementation begins.</rationale>
    </rule>
    <rule id="r-ac-given-when-then" testable="true" depth="essential">
      <statement>Use Given-When-Then format for all acceptance criteria. Avoid free-form prose AC. "Works correctly" is not an acceptance criterion.</statement>
      <rationale source="BDD practice">Given-When-Then enforces testability. Each field maps directly to a test: Given = test setup, When = action, Then = assertion. Prose AC requires interpretation, causing inconsistent test coverage.</rationale>
    </rule>
    <rule id="r-assumptions-if-wrong" testable="true" depth="essential">
      <statement>Document every assumption that, if wrong, would require a spec change. Format: "Assumption: [statement]. If wrong: [consequence]."</statement>
      <rationale source="Practitioner experience — spec advanced audit">Hidden assumptions are the primary source of mid-development spec surprises. Making the "if wrong" consequence explicit accelerates discovery of invalid assumptions during review.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>If preconditions in AGENTS.md &quot;Applies If&quot; do not all hold, OR any &quot;Skip If&quot; disqualifier is true, do not run this methodology — route to the named alternative.</statement>
      <rationale source="faion-network methodology DoD (F-066)">Methodologies are pre-condition gated; running them out-of-scope produces noise and burns the agent context budget.</rationale>
    </rule>
    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement>If preconditions hold, execute the procedure in `content/04-procedure.xml` (or the rule set above when complexity is light), emit the artefact specified by `content/02-output-contract.xml`, and validate with `scripts/validate-spec-advanced-guidelines.py` before returning.</statement>
      <rationale source="faion-network methodology DoD (F-066)">Tying the entry rule to the validator script closes the loop between policy and machine check.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
