<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Design Docs Patterns Core Rules" version="1.2.0" est_tokens="3400" depth="essential">
  <summary>15 testable rules every Design Docs Patterns application must enforce: when to write, which format, the lifecycle gate, SDD placement, required sections, non-goals, alternatives, review process, ADR extraction and LLM-generation checks. These are HARD rules — user-supplied input cannot override them.</summary>

  <section title="Background">
    <p>Rules r-when-to-write through r-design-after-spec-approved cover format selection: when to write a design document, how to choose between lightweight and heavyweight formats, and how the doc maps to the SDD lifecycle phases.</p>
    <p>Rules r-eight-required-sections through r-llm-doc-three-checks cover writing: required sections, the non-goals rule, alternatives quality bar, ADR extraction, and agent-specific gotchas for LLM-assisted design doc writing.</p>
  </section>

  <section title="Rule set">

    <rule id="r1-one-day-trigger" testable="true" depth="essential">
      <statement>Design doc MUST be written when feature work exceeds 1 engineering day.</statement>
      <rationale source="Internal — Faion design-doc audits 2026">Past 1 day, retrospective rationale loses fidelity.</rationale>
    </rule>
    <rule id="r2-required-sections" testable="true" depth="essential">
      <statement>Doc MUST contain all of {context, goals, non-goals, proposed, alternatives, open-questions}; missing sections rejected.</statement>
      <rationale source="Google design-doc template">Missing sections hide the failure modes most likely to bite. The two sources disagree on the section count: this rule and the output contract require these six, while r-eight-required-sections lists eight (adding overview and cross-cutting concerns); this rule governs until reconciled.</rationale>
    </rule>
    <rule id="r3-non-goals-explicit" testable="true" depth="essential">
      <statement>Non-goals section MUST be non-empty; un-stated non-goals invite scope creep.</statement>
      <rationale source="Google / Stripe practice">Un-stated non-goals re-emerge as 'must-haves' mid-review. The two sources disagree on the minimum count: this rule and the output contract require at least one non-goal, while r-non-goals-are-real-boundaries asks for at least two; this rule governs until reconciled.</rationale>
    </rule>
    <rule id="r4-two-genuine-alternatives" testable="true" depth="essential">
      <statement>Alternatives section MUST list ≥2 genuinely considered options; strawmen rejected.</statement>
      <rationale source="Big-tech design-doc culture">Single-option docs are advocacy; strawmen are theatre.</rationale>
    </rule>
    <rule id="r5-review-deadline-set" testable="true" depth="essential">
      <statement>Circulation MUST set a review deadline; open-ended reviews rejected.</statement>
      <rationale source="Async-review practice">Without deadline, reviews never close.</rationale>
    </rule>

    <rule id="r-when-to-write" testable="true" depth="essential">
      <statement>Write a design document for any feature that takes more than one engineering day, spans multiple PRs or modules, or where requirements are unclear. Skip it for bug fixes with an obvious solution, prototypes where output is discarded, and work under a few hours.</statement>
      <rationale source="Google design-doc practice">Design documents catch design flaws when changes are cheap (before code is written). The act of writing forces clarity: gaps that seem obvious in conversation become visible in prose. The overhead is justified only when the feature is complex enough that silent assumptions could cause rework.</rationale>
    </rule>
    <list>
      <item>Write: feature takes &gt; 1 engineering day</item>
      <item>Write: change is cross-cutting or multi-PR</item>
      <item>Write: high security or privacy implications</item>
      <item>Write: API changes affecting other teams</item>
      <item>Write: requirements are unclear or conflicting</item>
      <item>Skip: bug fix with obvious single-line solution (PR description is sufficient)</item>
      <item>Skip: prototype or spike where code will be discarded</item>
      <item>Skip: internal refactor with identical external behavior under a few hours</item>
    </list>

    <rule id="r-format-matches-audience" testable="true" depth="essential">
      <statement>Use the lightweight Google-style format (1-4 pages) for team-scoped and org-scoped work. Use heavyweight formats (Amazon 6-Pager, Uber RFC) only for cross-org or executive-audience decisions requiring formal sign-off. The format should match the audience and stakes — over-engineering the format wastes time without adding value.</statement>
      <rationale source="Big-tech design-doc survey (design-docs-big-tech)">Lightweight docs take hours to write and review; heavyweight docs take days. Applying a 6-pager format to a team-scoped change adds bureaucratic friction without improving the outcome. Conversely, a lightweight doc for a cross-org infrastructure change may fail to reach the right decision-makers or surface all risks.</rationale>
    </rule>
    <list>
      <item>Lightweight (1-4 pages): internal team or org change, rapid iteration context</item>
      <item>Full design doc (Google-style, 5-10 pages): cross-team service or new product feature</item>
      <item>Amazon 6-Pager: strategic initiative, executive review, narrative format required</item>
      <item>Uber RFC: formal approval required, service SLAs, platform change</item>
      <item>ADR: single architectural decision to record permanently in-repo</item>
    </list>
    <reference path="templates/design-doc-lightweight.md">Lightweight design doc template</reference>

    <rule id="r-approved-gates-implementation" testable="true" depth="essential">
      <statement>A design doc moves through states: DRAFT → REVIEW → APPROVED → IMPLEMENTING → IMPLEMENTED → ARCHIVED. The APPROVED state is the gate before any implementation begins — proceeding before approval means implementation may need to be redone after review feedback.</statement>
      <rationale source="Design-doc lifecycle practice">The lifecycle state provides an explicit handshake between design and implementation. Without it, engineers start coding on a draft that is still under review, creating merge conflicts between feedback changes and in-flight code.</rationale>
    </rule>
    <list>
      <item>DRAFT: author writes initial version, may share informally</item>
      <item>REVIEW: circulate to stakeholders, collect feedback, iterate</item>
      <item>APPROVED: required approvers sign off; doc is authoritative</item>
      <item>IMPLEMENTING: referenced during development; update if design changes</item>
      <item>IMPLEMENTED: feature shipped; doc becomes historical record</item>
      <item>ARCHIVED: doc superseded or system deprecated</item>
    </list>

    <rule id="r-design-after-spec-approved" testable="true" depth="essential">
      <statement>In the SDD lifecycle, the design section is generated after spec.md is approved (90%+ confidence). The design section lives inside plan.md (`## Design` H2 per plan-md-structure) and must reach APPROVED status before user-flows.md / task expansion proceeds. The design answers HOW to build; it must not answer WHAT (that belongs in spec.md).</statement>
      <rationale source="SDD lifecycle (plan-md-structure)">Keeping spec and design as separate artifacts prevents conflation of requirements with implementation. If a reviewer changes the implementation approach, only design.md is updated — the spec (the contract) remains stable. This separation also allows the spec to survive a design rewrite when a better approach is discovered.</rationale>
    </rule>

    <rule id="r-eight-required-sections" testable="true" depth="essential">
      <statement>Every design doc must contain: overview (1-2 paragraphs), context/background, goals (specific and measurable), non-goals (explicit), proposed solution/design, alternatives considered (minimum 2 genuine alternatives), cross-cutting concerns, and open questions. Missing any of these signals an incomplete doc.</statement>
      <rationale source="Google design-doc template">Each required section serves a distinct purpose: overview provides entry-point clarity, non-goals prevent scope creep, alternatives document the reasoning behind the chosen approach, and open questions track unresolved risks. Omitting any of them means future readers must reconstruct reasoning from context, and reviewers cannot give informed sign-off.</rationale>
    </rule>
    <list>
      <item>Overview: 1-2 paragraphs; readable by someone unfamiliar with the project</item>
      <item>Context and background: current system state, constraints, what led to this</item>
      <item>Goals: specific and measurable success criteria</item>
      <item>Non-goals: explicit list of what is out of scope</item>
      <item>Proposed design: architecture, data model, API contracts, key decisions</item>
      <item>Alternatives considered: at least 2 with pros, cons, rejection rationale</item>
      <item>Cross-cutting concerns: security, privacy, scalability, reliability, observability</item>
      <item>Open questions: unresolved items with owners</item>
    </list>

    <rule id="r-non-goals-are-real-boundaries" testable="true" depth="essential">
      <statement>Include a non-goals section in every design doc. List at least two explicit non-goals. Non-goals must be real scope boundaries — not obvious statements that nobody would expect. When reviewers raise "have you considered X?", the answer is either in the design or in the non-goals.</statement>
      <rationale source="Google / Stripe practice">Non-goals prevent scope creep discussions during review and implementation. When a non-goal is not written down, every reviewer asks the same question independently. Explicitly stating non-goals sets boundaries that allow the team to focus the review on the actual proposed scope.</rationale>
    </rule>

    <rule id="r-alternatives-with-why-not-chosen" testable="true" depth="essential">
      <statement>Include at least two genuine alternatives — not strawmen. Each alternative must have: a brief description, honest pros, honest cons, and a clear "why not chosen" rationale. Always include "do nothing" as an alternative when applicable.</statement>
      <rationale source="Big-tech design-doc culture">Strawman alternatives (alternatives that are obviously worse) signal that the author avoided the hard thinking. Genuine alternatives show that tradeoffs were seriously considered. The "why not chosen" rationale is the most important part: it documents the reasoning that future engineers can use to revisit the decision if constraints change.</rationale>
    </rule>

    <rule id="r-review-cap-and-silent-reading" testable="true" depth="essential">
      <statement>Set a review deadline when circulating a design doc. Cap required reviewers at 5-7. For async review, specify comment format expectations in the distribution message. For synchronous review, start with 10-20 minutes of silent reading before discussion.</statement>
      <rationale source="Async-review practice">Design docs without a deadline stay in "in review" indefinitely. Too many required reviewers causes diffusion of responsibility — everyone assumes someone else will catch the issue. Silent reading at the start of a review meeting eliminates the presentation overhead and ensures all reviewers have actually read the doc before discussion.</rationale>
    </rule>

    <rule id="r-extract-adrs-after-approval" testable="true" depth="essential">
      <statement>After a design doc is approved, extract each significant architectural decision into a separate Architecture Decision Record (ADR) committed to the repository. Link the ADR from the design doc. ADRs are immutable once accepted; the design doc can evolve but the ADR records the decision as made.</statement>
      <rationale source="architecture-decision-records">Design docs evolve and may be archived or updated over time. ADRs are permanent records that explain why the codebase is structured the way it is. Without ADRs, engineers inherit code with no context for the decisions behind it, leading to repeated debates about settled questions.</rationale>
    </rule>

    <rule id="r-llm-doc-three-checks" testable="true" depth="essential">
      <statement>When generating a design doc with an LLM, always require: (1) a human review pass before marking APPROVED; (2) at least two genuine alternatives, not placeholder text; (3) cross-cutting concerns filled with project-specific content, not generic boilerplate. Reject docs where these sections contain only template placeholders.</statement>
      <rationale source="Internal — Faion design-doc audits 2026">LLMs pattern-match to design doc templates and generate authoritative-sounding but generic content. The most dangerous failure mode is a doc that looks complete but contains no actual domain-specific constraints. The three checks above catch the most common LLM failure patterns: missing review, strawman alternatives, and uncustomized boilerplate.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
