Review the following spec.md for traceability completeness:
<spec>
{SPEC_CONTENT}
</spec>

For each FR-X item, check:
1. Does it trace to a user story (US-X reference present)?
2. Is the requirement testable with a pass/fail outcome?
3. Is there at least one corresponding AC-X?
4. Is the "System SHALL" imperative used (no "should", "may", "can")?
5. Are ambiguous words absent (fast, good, easy, support, handle)?
6. Is a MoSCoW priority assigned?

Output a traceability matrix in this format:

| FR-X | Traces to US-X | Has AC-X | Testable | Issues |
|------|----------------|----------|----------|--------|
| FR-001 | US-001 | AC-001 | Yes | — |
| FR-002 | — | — | No | Missing US trace, no AC, not testable |

Then list:
- Must FRs as percentage of total (flag if > 60%)
- Any Must FR without at least one AC
- Any AC that uses placeholder values ("valid data", "responds correctly", etc.)
- Out of Scope items missing a reason or timeline

Do not modify the spec — output review findings only.
