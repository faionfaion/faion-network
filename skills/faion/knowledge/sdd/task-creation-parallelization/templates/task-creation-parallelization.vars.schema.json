{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/sdd/task-creation-parallelization/task-creation-parallelization.vars.schema.json",
  "title": "task-creation-parallelization template variables",
  "description": "Values for templates/task-creation-parallelization.md.j2 and templates/task-creation-parallelization.html.j2. Generated by tpl-jinja from task-creation-parallelization.md; `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artefact_slug": {
      "allOf": [
        {
          "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/artefact_slug"
        }
      ],
      "description": "Artefact id. From section 'Task Creation and Parallelization'."
    },
    "decision": {
      "type": "string",
      "title": "Decision",
      "description": "The decision used under 'Task Creation and Parallelization'.",
      "enum": [
        "go",
        "hold",
        "no-op"
      ]
    },
    "input_name": {
      "type": "string",
      "title": "Input name",
      "description": "The input name used under 'Task Creation and Parallelization'."
    },
    "owner_handle": {
      "allOf": [
        {
          "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/owner_handle"
        }
      ],
      "description": "Owner. From section 'Task Creation and Parallelization'."
    }
  },
  "required": [
    "artefact_slug",
    "decision",
    "input_name",
    "owner_handle"
  ],
  "x-faion-dictionary-refs": 2
}
