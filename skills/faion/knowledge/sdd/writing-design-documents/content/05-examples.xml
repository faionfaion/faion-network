<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Writing Design Documents Worked Example" version="1.1.0" est_tokens="1100" depth="essential">
  <summary>End-to-end worked example showing inputs → procedure → output for a typical instance of Writing Design Documents, plus a full AD-X block and the four LLM code-generation patterns.</summary>

  <example>
    <scenario>Feature &apos;PDF digest export&apos;. AD-1 = &apos;use wkhtmltopdf vs weasyprint&apos; (chose weasyprint for license + perf), traced to FR-2. API contract POST /exports with example bodies + 400/422 error matrix.</scenario>
    <trace>r1-ad-numbered → r2-fr-coverage → r3-api-contract-examples → validated design.md</trace>
    <artefact>
      <code lang="json"><![CDATA[
{
  "artefact_id": "writing-design-documents-example",
  "version": "1.0.0",
  "last_reviewed": "2026-05-23",
  "owner": "@solo-founder",
  "feature_id": "F-example",
  "summary": "One-paragraph problem + outcome statement.",
  "items": [
    "item-1",
    "item-2",
    "item-3"
  ],
  "out_of_scope": [
    "item-x",
    "item-y"
  ]
}
]]></code>
    </artefact>
  </example>

  <example type="good" title="AD-X decision block with all six fields">
    <code lang="markdown"><![CDATA[
### AD-001: Use Pydantic for Validation

**Status:** Accepted
**Context:** Need request/response validation with automatic error messages.
**Decision:** Use Pydantic v2 models for all request/response validation.
**Rationale:**
- Native FastAPI integration
- Automatic error message generation with field-level detail
**Alternatives Considered:**
| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|--------------|
| marshmallow | Mature, flexible | Manual integration | More boilerplate |
| Do nothing | No new dep | No validation | Blocks FR-001 |
**Consequences:**
- Positive: type safety, automatic docs generation
- Negative: Pydantic v2 migration required for existing models
**Traces to:** FR-001, FR-002
      ]]></code>
    <note>Satisfies r-ad-x-six-fields and r1-ad-numbered.</note>
  </example>

  <example type="good" title="LLM pattern 1: explicit file patterns">
    <code lang="markdown"><![CDATA[
## File Patterns

Follow existing pattern from `src/auth/handlers/login.ts`:
- Export single async handler function
- Use Zod for validation
- Return standardized response envelope
- Log with context object (not console.log)
        ]]></code>
    <note>References the actual file. Agent reads it, then applies the same pattern. No prose ambiguity.</note>
  </example>

  <example type="good" title="LLM pattern 2: type definitions">
    <code lang="typescript"><![CDATA[
// Include directly in design.md
interface CreateUserRequest {
  email: string;      // RFC 5322 format
  password: string;   // Min 8 chars, 1 uppercase, 1 number
}

interface CreateUserResponse {
  user: {
    id: string;       // UUID v4
    email: string;
    createdAt: Date;
  };
}
        ]]></code>
    <note>Explicit types eliminate the ambiguity that produces incorrect field types and missing fields in generated code.</note>
  </example>

  <example type="good" title="LLM pattern 3: API contracts">
    <code lang="markdown"><![CDATA[
### POST /api/users

**Request:**
{"email": "user@example.com", "password": "SecurePass1"}

**Response (201):**
{"user": {"id": "uuid-v4", "email": "user@example.com", "createdAt": "ISO8601"}}

**Errors:**
- 400: validation failure {"error": {"code": "VALIDATION_ERROR", "details": [...]}}
- 409: email exists {"error": {"code": "DUPLICATE_EMAIL"}}
- 500: server error {"error": {"code": "INTERNAL_ERROR", "request_id": "..."}}
        ]]></code>
    <note>All error codes explicit. Parallel agents implementing frontend and backend from the same contract produce compatible code without coordination.</note>
  </example>

  <example type="good" title="LLM pattern 4: implementation reference table">
    <code lang="markdown"><![CDATA[
| Component | Pattern Source | What to Copy |
|-----------|----------------|--------------|
| Handler | `src/auth/handlers/login.ts` | Error handling, response format |
| Service | `src/auth/services/user.ts` | Dependency injection pattern |
| Tests | `tests/auth/login.test.ts` | Setup, mocking strategy |
        ]]></code>
    <note>Satisfies r-explicit-types-contracts-refs: the executor copies a proven pattern instead of inventing one.</note>
  </example>
</text>
