# purpose: Per-feature user-flows.md skeleton — actor, preconditions, happy and negative paths per flow
# consumes: the feature spec plus its acceptance criteria
# produces: test-spec mapping 1:1 to Playwright specs, matching content/02-output-contract.xml
# depends-on: content/01-core-rules.xml + content/04-procedure.xml
# token-budget-impact: zero at runtime; authoring-time doc only

# user-flows: <feature name>

## Flow F-01: <one-line flow title>

- Actor: <who>
- Preconditions: <what state>
- Happy path:
  1. <action> → <expected result>
  2. <action> → <expected result>
  3. <action> → <expected result>
- Negative paths:
  - <negative case 1>:
    - Trigger: <what causes the error>
    - Expected UX: <what the user sees>
  - <negative case 2 — optional>:
    - Trigger:
    - Expected UX:
- Playwright spec: `<path/to/spec.ts>`

## Flow F-02: <next flow>

(Repeat the shape above per flow. Every flow needs at least one positive AND one negative case.)
