<?xml version="1.0" encoding="UTF-8"?>
<text id="02-shape" title="Per-flow shape">
  <summary>Each flow in user-flows.md has five required fields. Together they map 1:1 to a Playwright spec file.</summary>

  <section title="Required fields per flow">
    <list>
      <item>Actor — who is doing the flow (anonymous user, signed-in solo-tier user, admin).</item>
      <item>Preconditions — what state the system must be in (account exists, payment method on file).</item>
      <item>Happy path — numbered steps + expected result per step. The success criterion is in the final step.</item>
      <item>Negative paths — at least one. Each: trigger (what causes the error), expected error UX (what the user sees, where).</item>
      <item>Playwright spec path — relative path to the spec file that automates this flow.</item>
    </list>
    <rule testable="true">
      <statement>Every flow MUST list all five fields. Missing fields fail the readiness gate.</statement>
      <rationale>The fields jointly describe the contract between feature and test suite. Dropping any one breaks the 1:1 mapping.</rationale>
    </rule>
  </section>

  <section title="Worked example">
    <example type="good">
      <code lang="markdown"><![CDATA[
## Flow F-01: Signed-in user upgrades to solo tier

- Actor: signed-in free-tier user with verified email.
- Preconditions: account exists; Stripe customer record exists; no active subscription.
- Happy path:
  1. Click "Upgrade" in nav → routed to /upgrade.
  2. Select "Solo $19/mo" → Stripe Elements form appears.
  3. Enter test card 4242 4242 4242 4242, submit.
  4. Expect: 200 redirect to /account?upgraded=solo; success toast; tier=solo in /api/account.
- Negative paths:
  - Card declined (test card 4000 0000 0000 0002):
    - Trigger: submit declined card.
    - Expected UX: inline error under card field "Your card was declined.", form stays editable, no charge created.
  - Network drop mid-submit:
    - Trigger: throttle network to offline after click submit.
    - Expected UX: spinner times out at 30s, toast "Connection lost. Try again." NO partial subscription.
- Playwright spec: `faion-net-e2e/tests/upgrade.spec.ts`
      ]]></code>
    </example>
  </section>

  <see-also>
    <link path="content/03-positive-negative.xml"/>
  </see-also>
</text>
