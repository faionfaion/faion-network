<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Design Docs at Big Tech Companies Decision Tree" version="1.1.1" est_tokens="550">
  <summary>Run-or-skip gate based on preconditions, then a second-level question routing to a rule-id conclusion. Minimum-form tree for a methodology of this shape.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do the Applies-If preconditions hold AND none of the Skip-If disqualifiers fire?</root-question>

    <branch when="preconditions == true">
      <question id="q-inputs-complete">Are the Prerequisites artefacts present AND cited with evidence?</question>
        <branch when="inputs_complete == true AND rfc_and_adr_in_one_doc == true">
          <conclusion ref="r8-rfc-adr-separate">Split the consensus RFC from the decision ADR before circulating.</conclusion>
        </branch>
        <branch when="inputs_complete == true AND rfc_and_adr_in_one_doc == false AND llm_alternatives_count &gt; 3">
          <conclusion ref="r10-llm-alternatives-cap">Cut the LLM brainstorm to 2-3 concrete options; human picks the section.</conclusion>
        </branch>
        <branch when="inputs_complete == true AND rfc_and_adr_in_one_doc == false AND llm_alternatives_count &lt;= 3">
          <conclusion ref="r1-write-before-code">Run the methodology — produce the artefact per output contract.</conclusion>
        </branch>
        <branch when="inputs_complete == false">
          <conclusion ref="r5-llm-assist-limit">Pause; gather missing Prerequisites with cited evidence before run.</conclusion>
        </branch>
    </branch>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology">Skip this methodology. Name the failing Applies-If condition or the firing Skip-If disqualifier.</conclusion>
    </branch>
  </decision-tree>

  <example trace="happy-path"><![CDATA[
preconditions=true inputs_complete=true rfc_and_adr_in_one_doc=false llm_alternatives_count=2 → run per r1-write-before-code; artefact passes validate-design-docs-big-tech.py exit 0.
]]></example>
</text>
