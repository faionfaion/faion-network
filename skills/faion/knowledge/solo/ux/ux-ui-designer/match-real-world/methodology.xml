<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="match-real-world">
  <metadata>
    <tier>solo</tier>
    <group>ux</group>
    <domain>ux-ui-designer</domain>
    <summary>Use words, icons, and organizational patterns that match users' mental models — not internal system terminology. Replace jargon with user vocabulary and follow domain conventions.</summary>
    <tags>
      <tag>heuristics</tag>
      <tag>ux-copy</tag>
      <tag>language</tag>
      <tag>mental-models</tag>
      <tag>localization</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-28</created>
  </metadata>

  <content>
    <title>Match Between System and Real World</title>

    <summary>Nielsen Heuristic #2: the interface must use words, icons, and organizational patterns that match users' mental models — not internal system or developer terminology. Replace jargon ("terminate session") with user vocabulary ("log out"). Follow domain conventions (shopping cart, inbox, trash) even when the underlying metaphor is dated. Information must appear in the order users expect, not the order the system processes it.</summary>

    <why>
      <p>When interfaces use system-centric language, users must translate between what they see and what they know — this translation cost increases errors, learning curve, and abandonment. Users already have mental models from real-world experience and other products; matching those models makes the product feel intuitive without any learning. Mismatches (technical abbreviations, wrong date formats, system-centric navigation labels) are invisible to developers but immediately felt by users.</p>
    </why>

    <when-to-use>
      <list>
        <item>Auditing UI copy for jargon, abbreviations, or technical terms before a release</item>
        <item>Reviewing API-generated UI strings (error messages, status labels, field names) for plain language</item>
        <item>Localization: verifying translated content matches mental models in the target locale</item>
        <item>When user research reveals confusion about specific labels or navigation terms</item>
        <item>Code review: checking that developer-written placeholder text and labels are user-friendly</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Technical admin dashboards used exclusively by engineers — technical language is correct there</item>
        <item>B2B products where users are domain experts who expect precise technical vocabulary</item>
        <item>When user research or A/B tests have already validated the existing language</item>
        <item>Early prototype stages where content is placeholder — audit when real content is set</item>
      </list>
    </when-not-to-use>

    <section name="language-rules">
      <rule>
        <statement>Use the words users actually say when describing their tasks. Audit all UI strings by asking: would a non-technical user understand this without explanation?</statement>
        <rationale>Users have zero obligation to learn system terminology. When the interface speaks their language, the learning curve disappears and cognitive load drops. Nielsen's heuristic #2 is empirically tied to task completion rates.</rationale>
      </rule>

      <rule>
        <statement>Replace jargon and system-centric terms with plain equivalents. Apply substitutions consistently across the entire product — inconsistency between screens is itself a heuristic violation.</statement>
        <rationale>Inconsistent terminology ("sign in" on one page, "log in" on another) forces users to question whether the actions are the same, adding cognitive friction where none should exist.</rationale>
      </rule>

      <example type="good" title="Jargon substitution table">
        <code lang="text"><![CDATA[
Terminate session  →  Log out
Execute query      →  Search
Invalid input      →  Please enter a valid email
Repository         →  Folder / Collection
Authenticate       →  Sign in
Synchronize        →  Update / Refresh
DOB                →  Date of birth
Configure SMTP     →  Set up email sending
Record created     →  Your booking is confirmed
        ]]></code>
      </example>
    </section>

    <section name="domain-conventions">
      <rule>
        <statement>Follow established domain conventions for navigation labels, interaction patterns, and terminology even if the underlying metaphor is technically outdated (e.g., floppy disk = save).</statement>
        <rationale>Users have built strong mental models from prior product experience. Deviating from domain conventions for novelty forces unlearning and increases errors. The save icon is universally understood regardless of whether floppy disks exist.</rationale>
      </rule>

      <list>
        <item>E-commerce: shopping cart, checkout, wishlist, order history</item>
        <item>Documents: files, folders, save, open, trash (recoverable)</item>
        <item>Email: inbox, sent, draft, archive, snooze</item>
        <item>Social: friends, followers, like, share, feed</item>
        <item>Navigation: home, back, menu, search</item>
      </list>
    </section>

    <section name="information-order">
      <rule>
        <statement>Order information the way users think about it, not the way the system processes it. Match locale conventions for date, number, and address formats.</statement>
        <rationale>Address forms ordered Country first (system-centric) require users to mentally reverse their natural address recall order. Locale-mismatched date formats cause data entry errors and may have legal implications in financial or medical contexts.</rationale>
      </rule>

      <example type="good" title="Address form — user-natural order">
        <code lang="text"><![CDATA[
Good: Street → City → State → ZIP → Country
Bad:  Country → State → City → Street → ZIP
        ]]></code>
      </example>
    </section>

    <section name="icon-metaphors">
      <rule>
        <statement>Always pair icons with text labels in primary navigation. Never rely on icon-only navigation except for universally recognized symbols with confirmed user recognition.</statement>
        <rationale>Icon comprehension varies significantly by culture and age group. Labels eliminate ambiguity at no usability cost. Icon-only navigation consistently scores lower on task success in usability studies.</rationale>
      </rule>
    </section>

    <section name="good-product-examples">
      <example type="good" title="Airbnb — domain language matching hotel mental models">
        <code lang="text"><![CDATA[
"Check in" / "Check out"  (not "Start date" / "End date")
"Guests"                  (not "Users")
"Listing"                 (not "Property record")
"Your trips"              (not "Booking history")
        ]]></code>
      </example>

      <example type="good" title="Gmail — physical mailbox metaphors users recognize">
        <code lang="text"><![CDATA[
"Inbox"    — physical mailbox concept
"Archive"  — storing for later, not deleting
"Snooze"   — sleep metaphor for timed reminders
"Labels"   — physical label stickers on folders
        ]]></code>
      </example>
    </section>

    <section name="antipatterns">
      <antipattern>
        <description>Technical jargon as primary UI copy: "Configure SMTP parameters for outbound mail relay"</description>
        <reason>Requires technical knowledge the user does not have. Rewrite: "Set up email sending". The action is the same; the user does not need to know what SMTP is.</reason>
      </antipattern>

      <antipattern>
        <description>System-centric confirmation messages: "Record created successfully"</description>
        <reason>Confirms the system's action, not the user's goal. Rewrite: "Your booking is confirmed" — confirms what the user was trying to achieve.</reason>
      </antipattern>

      <antipattern>
        <description>Inconsistent terminology across pages: "Sign in" on homepage, "Login" on mobile, "Log in" in email</description>
        <reason>Forces users to question whether these are the same action. Pick one term and apply it everywhere. Inconsistency is a heuristic #2 violation regardless of individual clarity.</reason>
      </antipattern>

      <antipattern>
        <description>Locale-blind date format: displaying "03/04/2025" without locale detection</description>
        <reason>March 4 in US format is April 3 in European format. For financial, travel, and medical products, this is not just confusing — it can cause errors with real consequences. Use unambiguous formats or locale detection.</reason>
      </antipattern>

      <antipattern>
        <description>Forced simplification of technical terms that users expect: changing "invoice" to "bill" in an accounting tool</description>
        <reason>Domain-expert users have precise vocabulary expectations. Over-simplifying removes signal. For expert-user products, "plain language" means the domain's language, not generic consumer language.</reason>
      </antipattern>
    </section>

    <section name="research-methods">
      <rule>
        <statement>Extract actual user vocabulary from support tickets, user interview recordings, and search query logs before writing UI copy. Use the words users use, not words the team prefers.</statement>
        <rationale>The most common source of heuristic #2 violations is team vocabulary leaking into UI copy. Users reveal their language in support requests and search queries — those are the ground truth.</rationale>
      </rule>
    </section>

    <section name="where-it-fails">
      <p>Agents lack cultural and regional knowledge — "plain English" rewrites may not match the mental models of non-English-native user bases.</p>
      <p>Some technical terms have no plain-language equivalent (e.g., "two-factor authentication") — forced simplification can be less clear.</p>
      <p>Industry conventions shift over time; agents trained on older data may suggest outdated metaphors.</p>
      <p>Language audit alone cannot surface whether users understand icons — requires usability testing with real users.</p>
      <p>Agents cannot detect inconsistency across multi-product environments without being given all string sources simultaneously.</p>
    </section>

    <section name="agentic-workflow">
      <p>An agent can perform a full language audit by extracting all UI strings from a codebase or design file, classifying each as jargon/technical/abbreviation/unclear, and proposing plain-language alternatives using the substitution table pattern. The agent needs the product's target user persona as context — "plain language for a 65-year-old first-time smartphone user" differs from "plain language for a freelance accountant." Human review is required for any string that relates to legal, financial, medical, or security contexts.</p>

      <p>Prompt pattern — Audit UI strings for real-world language match:</p>
      <prompt purpose="Audit UI strings for real-world language match"><![CDATA[You are auditing UI strings for real-world language match (Nielsen Heuristic #2).
Target user: [persona description, e.g., "small business owner, non-technical, age 30-55"].
For each string below, classify it as: OK | JARGON | TECHNICAL | ABBREVIATION | UNCLEAR
Then provide a plain-language alternative for non-OK items.
Strings to audit:
[list of strings]]]></prompt>

      <p>Prompt pattern — Navigation rewrite:</p>
      <prompt purpose="Rewrite navigation labels for plain language"><![CDATA[Rewrite the following navigation labels so they use the words users would naturally say when describing the task they want to do. Avoid verbs from the system perspective.
Example: "Repository" → "Your files". Do not use more than 3 words per label.
Labels: [list]]]></prompt>
    </section>

    <section name="best-practices">
      <p>Audit user-visible strings separately from developer-visible strings — do not apply plain language rules to log messages or API error codes.</p>
      <p>Use real user vocabulary: search support tickets and user interview transcripts for the exact words users use to describe features, then adopt those words in the UI.</p>
      <p>Icon + label is always safer than icon alone — even universally recognized icons (save = floppy disk) should have a visible text label in primary navigation.</p>
      <p>Date and number format localization is mandatory, not optional — a US-format date shown to a European user is a real-world mismatch with legal implications in some contexts.</p>
      <p>Consistency across the product is as important as individual clarity — "sign in" and "log in" on different screens of the same product is a heuristic #2 violation.</p>
      <p>When a technical term is unavoidable, introduce it with a plain-language explanation on first use and use it consistently thereafter.</p>
      <p>A/B test terminology changes before rolling out product-wide — users build habits around existing labels; sudden changes cause confusion even if the new label is objectively clearer.</p>
    </section>

    <section name="ai-agent-gotchas">
      <p>Agents default to US English conventions even when locale is explicitly set — review all rewrites for locale-specific idioms and formatting.</p>
      <p>Agents may over-simplify industry-standard terms that expert users expect (e.g., changing "invoice" to "bill" in an accounting tool).</p>
      <p>Plain-language rewrites often increase string length, which can break fixed-width UI containers — agents must be told character limits.</p>
      <p>Consistency checking across a large codebase requires the agent to hold the full string inventory in context — chunk the audit by feature area and reconcile at the end.</p>
      <p>Agents cannot detect if an icon metaphor is culturally appropriate — visual element review requires human judgment or structured user research.</p>
    </section>

    <templates>
      <template path="templates/language-audit.md" purpose="Audit table: current term, user-friendly assessment, suggested alternative; sections for terminology, error messages, labels"/>
      <template path="templates/extract-strings.py" purpose="Python script: flatten i18n JSON to key:value list for batch language audit"/>
    </templates>

    <see-also>
      <link href="https://www.nngroup.com/articles/match-system-real-world/"/>
      <link href="https://www.plainlanguage.gov/guidelines/"/>
      <link href="https://m3.material.io/foundations/content-design/writing"/>
      <link href="https://learn.microsoft.com/en-us/style-guide/brand-voice-above-all-simple-human"/>
    </see-also>
  </content>
</methodology>
