<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="spec-structure Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for spec-structure. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Reject any HOW language from a spec document. If an agent inserts architecture choices or file paths into a spec, move them to design.md.</statement>
      <rationale source="v1-source">HOW language in specs conflates requirements with solutions, blocking the design phase's ability to choose between approaches freely.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>A full spec v2.0 must contain all twelve sections in order: Reference Documents, Overview, Problem Statement, User Personas, User Stories, Functional Requirements, Non-Functional Requirements, Acceptance Criteria, Out of Scope, Assumptions and Constraints, Dependencies, Related Features.</statement>
      <rationale source="v1-source">Partial specs cause design-phase debates about unstated requirements. Every missing section represents a gap that will surface during implementation at higher cost.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Every NFR must have a numeric target. "Fast response" is rejected; "&lt; 500ms p95" is accepted. "Secure password storage" is rejected; "bcrypt 12 rounds" is accepted.</statement>
      <rationale source="v1-source">Qualitative NFRs cannot be tested and cannot be cited in design decisions. Numeric targets produce testable quality gates.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Status must be "Approved" before the design phase begins. Agents must not auto-proceed from Draft to Approved — this is a human-in-loop gate.</statement>
      <rationale source="v1-source">Auto-proceeding skips stakeholder alignment. A design built on a Draft spec produces waste if the spec changes during review.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Run Pass 1 and Pass 2 as separate agent invocations. Do not combine them.</statement>
      <rationale source="v1-source">Combined passes cause agents to conflate requirements discovery with requirements writing, producing FRs that are too specific (implementation details) or too vague (missing validation rules).</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Traceability must be bidirectional: FR traces to US (forward), AC traces to FR (forward), and AC is validated by FR (back). Orphaned FRs and orphaned ACs are quality gate failures.</statement>
      <rationale source="v1-source">Bidirectional traceability enables mechanical coverage verification: for every Must-priority FR, at least one AC exists and passes, therefore all must-have requirements are satisfied.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>All required sections must be present and non-empty. Problem Statement must have all five fields (Who, Problem, Impact, Solution, Success Metric). FR table must have ID, Requirement, Traces To, and Priority for every row. AC must have Given, When, Then for every scenario.</statement>
      <rationale source="v1-source">Missing sections are not optional — each carries information the design phase requires. An empty "Open Questions" section is worse than no section: it signals the questions were asked but not answered.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Reject any requirement containing these ambiguous words: good, fast, easy, appropriate, reasonable, support, handle, manage, modern, user-friendly. Replace with measurable criteria.</statement>
      <rationale source="v1-source">Ambiguous requirements produce ambiguous ACs, which produce untestable code. Each ambiguous word is a decision deferred to the developer or agent, who may choose incorrectly.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Each requirement must have exactly one interpretation. If two team members would implement the same FR differently, the FR is too ambiguous and must be rewritten.</statement>
      <rationale source="v1-source">Multiple interpretations mean the spec is not a contract. A spec that is not a contract provides no value over informal discussion.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Every FR-ID must be unique within the spec. Every AC must reference a real FR-ID. Every US must have at least one Must-priority FR. MoSCoW priorities must be assigned consistently — not all Must.</statement>
      <rationale source="v1-source">Duplicate IDs and broken references cause executor agents to silently skip or mis-map requirements. All-Must priority is a sign of prioritization debt that will cause scope overrun.</rationale>
    </rule>
    <rule id="r11" testable="true" depth="essential">
      <statement>The spec must reference constitution.md in the Reference Documents table. Related features in done/ must be identified and linked. Out of Scope must have at least 3 explicit exclusions with a timeline (Phase 2, Never, Future).</statement>
      <rationale source="v1-source">Without constitution reference, agents may choose patterns that contradict project standards. Without related feature links, agents may duplicate existing implementations.</rationale>
    </rule>
  </section>
</text>
