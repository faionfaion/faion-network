<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="design-doc-examples Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for design-doc-examples. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>When using this example as a reference, verify the bcrypt cost factor against your project's constitution — cost 12 is not a universal default.</statement>
      <rationale source="v1-source">Agents copy bcrypt cost 12 from this example into every project regardless of performance constraints or constitution decisions.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Inject the relevant example domain (auth for user-identity features, payment for transactional features) as a structural reference in the design-generation prompt. Never mix domains.</statement>
      <rationale source="v1-source">TypeScript interface examples cause agents to generate TypeScript even in Python projects if example is injected without domain filtering.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Annotate example ADs with why each alternative was rejected, not just what was rejected.</statement>
      <rationale source="v1-source">The rejection reason is where agents learn decision-making. An alternatives table without rejection reasons produces agents that list straw-man options.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Review the generated design against the FR coverage table — every FR-X from spec must appear in at least one AD and one file entry.</statement>
      <rationale source="v1-source">The coverage table is the reconciliation step that catches FRs that slipped through the AD drafting pass.</rationale>
    </rule>
  </section>
</text>
