<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Email Preflight Checklist Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable inputs to a rule from `01-core-rules.xml`. MANDATORY for every methodology — even flat ones — per F-066 DoD.</summary>

  <decision-tree>
    <root-question id="q-root">Is a marketing or lifecycle send queued with a defined segment and named owner?</root-question>
    <branch when="send_queued == true AND owner_named == true">
      <conclusion ref="r1-12-items-mandatory"/>
    </branch>
    <branch when="send_queued == false OR owner_named == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
  </decision-tree>

  <example trace="apply-path">
    All preconditions satisfied → conclusion r1-12-items-mandatory.
  </example>
</text>
