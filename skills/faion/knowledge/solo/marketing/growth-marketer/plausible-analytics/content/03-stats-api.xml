<?xml version="1.0" encoding="UTF-8"?>
<text id="03-stats-api" title="Plausible Stats API">
  <summary>API endpoints, parameter formats, Python client reference, and common agent gotchas.</summary>

  <section title="API Endpoints">
    <list>
      <item>Aggregate: /api/v1/stats/aggregate — overall metrics for a period</item>
      <item>Timeseries: /api/v1/stats/timeseries — daily breakdown for a period</item>
      <item>Breakdown: /api/v1/stats/breakdown — grouped by property (page, source, device)</item>
      <item>Realtime: /api/v1/stats/realtime/visitors — current visitors (last 5 min)</item>
    </list>
    <example type="good">
      <code lang="bash"><![CDATA[# Aggregate stats — last 30 days
curl "https://plausible.io/api/v1/stats/aggregate?site_id=yourdomain.com&period=30d&metrics=visitors,pageviews,bounce_rate" \
  -H "Authorization: Bearer $PLAUSIBLE_API_KEY"

# Top pages breakdown
curl "https://plausible.io/api/v1/stats/breakdown?site_id=yourdomain.com&period=30d&property=event:page&metrics=visitors,pageviews" \
  -H "Authorization: Bearer $PLAUSIBLE_API_KEY"]]></code>
    </example>
  </section>

  <section title="Parameter Format Rules">
    <rule>
      <statement>Use exact period parameter values: day, 7d, 30d, month, 6mo, 12mo, or YYYY-MM-DD,YYYY-MM-DD. Non-standard formats return errors.</statement>
      <rationale>Agents frequently use incorrect period formats (e.g., "last_30_days", "30_days", "1m") that are not valid for the Plausible API and produce 400 errors.</rationale>
    </rule>
    <rule>
      <statement>Use exact property values in breakdown queries. Valid examples: event:page, visit:source, visit:device, visit:country.</statement>
      <rationale>Agents hallucinate property names based on what "seems right" (e.g., "page:url", "traffic:source"). Only properties in the official API documentation work.</rationale>
    </rule>
  </section>

  <section title="Python Client">
    <reference path="templates/plausible-client.py">Python client with aggregate(), timeseries(), breakdown(), realtime_visitors()</reference>
    <rule>
      <statement>The Stats API is fully headless and agent-compatible — no browser interaction required. Use it from server-side scripts and agent pipelines directly.</statement>
      <rationale>Unlike most analytics tools, Plausible's Stats API does not require OAuth or browser sessions. A simple Bearer token is sufficient for all read operations.</rationale>
    </rule>
  </section>

  <section title="API Limitations">
    <list>
      <item>No user-level or session-level queries — aggregate only by design</item>
      <item>Custom properties limited on free/self-hosted plan</item>
      <item>Revenue tracking requires hosted Business plan</item>
      <item>Raw event export (Data Export API) requires Business/Enterprise plan</item>
      <item>Rate limits apply — paginate for large breakdowns</item>
    </list>
  </section>
</text>
