<?xml version="1.0" encoding="UTF-8"?>
<text id="01-setup" title="Plausible Analytics Setup">
  <summary>Script variants, Nginx proxy configuration for adblocker bypass, and framework integrations.</summary>

  <section title="Script Variants">
    <rule>
      <statement>Choose the script variant based on the features required. Do not use the all-extensions variant by default — include only what is needed to keep the script minimal.</statement>
      <rationale>Plausible's script is &lt;1KB in its base form. The all-extensions variant adds file-downloads, outbound-links, and tagged-events tracking, increasing size and complexity.</rationale>
    </rule>
    <list>
      <item>Basic: script.js — pageview tracking only</item>
      <item>With custom events: script.tagged-events.js</item>
      <item>With file downloads: script.file-downloads.js</item>
      <item>All extensions: script.tagged-events.file-downloads.outbound-links.js</item>
    </list>
    <example type="good">
      <code lang="html"><![CDATA[<!-- Basic pageview tracking -->
<script defer data-domain="yourdomain.com" src="https://plausible.io/js/script.js"></script>

<!-- With custom events (use this for conversion tracking) -->
<script defer data-domain="yourdomain.com" src="https://plausible.io/js/script.tagged-events.js"></script>]]></code>
    </example>
  </section>

  <section title="Nginx Proxy (Required for Technical Audiences)">
    <rule>
      <statement>Deploy the Nginx proxy before launch on any site targeting a technical audience. Without it, 20-40% of users running adblockers are invisible.</statement>
      <rationale>Plausible's default script URL (plausible.io) is blocked by most adblockers. A proxied script via the site's own domain bypasses these blocks, restoring data accuracy for privacy-conscious technical users.</rationale>
    </rule>
    <reference path="templates/nginx-proxy.conf">Nginx proxy configuration for adblocker bypass</reference>
    <example type="good">
      <code lang="html"><![CDATA[<!-- Use proxied script after Nginx proxy setup -->
<script defer data-domain="yourdomain.com" data-api="/api/event" src="/js/script.js"></script>]]></code>
    </example>
  </section>

  <section title="Framework Integrations">
    <reference path="templates/gatsby-config.js">gatsby-plugin-plausible configuration with custom domain</reference>
    <rule>
      <statement>In Next.js and Gatsby, guard plausible() calls with typeof window !== 'undefined' to prevent SSR errors.</statement>
      <rationale>Plausible's JavaScript function runs in the browser only. SSR environments do not have a window object. Calling plausible() during server-side rendering throws a ReferenceError.</rationale>
    </rule>
  </section>

  <section title="Development Exclusion">
    <rule>
      <statement>Exclude own visits during development by setting localStorage.setItem('plausible_ignore', 'true') in the browser console.</statement>
      <rationale>Development visits that appear in analytics data distort traffic numbers. The localStorage flag is persistent across sessions and survives page reloads.</rationale>
    </rule>
  </section>
</text>
