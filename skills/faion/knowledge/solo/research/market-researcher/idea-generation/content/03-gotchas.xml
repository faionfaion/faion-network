<?xml version="1.0" encoding="UTF-8"?>
<text id="03-gotchas" title="Idea Generation Agent Gotchas">
  <summary>Mode collapse, TAM/SAM fabrication, personal-fit blindness, and human-in-loop checkpoint rules for idea generation agents.</summary>

  <section title="Mode collapse">
    <antipattern>
      <description>Agent produces "AI-powered X", "smart Y for developers" across all 7 frameworks under default settings.</description>
      <reason>These labels are the statistical mode of solopreneur startup content in training data. Every framework converges to the same outputs without diversity forcing.</reason>
    </antipattern>
    <rule>
      <statement>Forbid "AI", "smart", "intelligent", and "GPT" as prefixes or suffixes in the diverge prompt. Require each framework to produce at least one non-software idea.</statement>
      <rationale>Explicit prohibition forces the model to access alternative vocabulary. The non-software constraint surfaces service, content, and physical product opportunities that are often undervalued.</rationale>
    </rule>
  </section>

  <section title="TAM/SAM fabrication">
    <antipattern>
      <description>Agent assigns confident market size numbers ($500M, $1B) without any citation in the scoring step.</description>
      <reason>LLMs produce round-number TAM figures anchored on frequently seen startup deck numbers in training data. These are confabulation, not research.</reason>
    </antipattern>
    <rule>
      <statement>Extract market size numbers as unverified_claim fields and require a citable source before they enter the scorecard. "Unknown" is preferable to fabricated confidence.</statement>
      <rationale>A fabricated $1B TAM that causes the founder to pursue a wrong direction is worse than acknowledging unknown market size and doing targeted research.</rationale>
    </rule>
  </section>

  <section title="Personal fit blindness">
    <antipattern>
      <description>Agent generates ideas without knowing actual skill depth; output skews toward lexically common categories (SaaS) and undercounts service and info-product opportunities.</description>
      <reason>LLMs fill missing personal context with training-data defaults. Without explicit skills input, every founder looks like a developer building B2B SaaS.</reason>
    </antipattern>
    <rule>
      <statement>Seed the diverge agent with concrete constraints: "10h/week, $0 budget, Python + B2B SaaS experience" beats "find me a profitable idea". Vague goals produce generic output.</statement>
      <rationale>Constraints are idea generators, not idea limiters. Specific constraints surface opportunities the generic prompt never reaches.</rationale>
    </rule>
  </section>

  <section title="Human-in-loop checkpoints">
    <rule>
      <statement>Never let an agent autonomously commit to building an idea. The decision after scoring is a founder decision. Agent output ends at a ranked list with evidence; the commit is human.</statement>
      <rationale>Agent confidence is meaningless on novelty — there is no training-data signal for "is this actually new". The founder must cross-check top ideas against Indie Hackers, Product Hunt, and Google manually before committing resources.</rationale>
    </rule>
  </section>
</text>
