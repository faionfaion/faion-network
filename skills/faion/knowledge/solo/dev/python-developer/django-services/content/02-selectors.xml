<?xml version="1.0" encoding="UTF-8"?>
<text id="02-selectors" title="Django Selectors: Optimised Read Operations">
  <summary>The selector pattern isolates complex read logic into dedicated functions, preventing N+1 queries and keeping views and services thin.</summary>

  <section title="When to Use a Selector">
    <rule>
      <statement>Create a selector function (not a service) when: the query spans multiple relations, includes permission-based filtering, involves aggregations, or is reused across multiple views.</statement>
      <rationale>Selectors return QuerySets for further chaining. Placing read logic in a selector makes it testable in isolation and reusable from any entry point.</rationale>
    </rule>
  </section>

  <section title="N+1 Prevention">
    <rule>
      <statement>Every selector must document its expected query count in the docstring. Use select_related() for FK/OneToOne and prefetch_related() for reverse FK/M2M. Add only() or defer() to limit loaded columns when serialising large sets.</statement>
      <rationale>Unoptimised querysets serialised in DRF loop over relations, issuing one query per row (N+1). Prefetch collapses this to 2 queries.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
from django.db.models import Count, QuerySet


def order_list_for_user(*, user: User) -> QuerySet[Order]:
    """
    Get visible orders for a user with items prefetched.

    Query count: 3 (orders, items, products)
    """
    return (
        Order.objects
        .filter(user=user, is_visible=True)
        .select_related("user", "payment")
        .prefetch_related("items__product")
        .annotate(item_count=Count("items"))
        .order_by("-created_at")
    )
]]></code>
    </example>
    <example type="bad">
      <code lang="python"><![CDATA[
def order_list_for_user(*, user: User):
    return Order.objects.filter(user=user)  # N+1 when serialised
]]></code>
      <note>Missing prefetch causes one extra query per order when serialiser accesses items.</note>
    </example>
  </section>

  <section title="Aggregation Selectors">
    <rule>
      <statement>Aggregation selectors must return a QuerySet, not a plain dict, so callers can chain additional filters. Document the output shape in the docstring.</statement>
      <rationale>Returning a QuerySet keeps the selector composable; a plain dict forces a new selector every time a caller needs slightly different filters.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
from datetime import date
from django.db.models import Avg, Count, Sum, QuerySet
from django.db.models.functions import TruncMonth


def sales_monthly_summary(*, start_date: date, end_date: date) -> QuerySet:
    """
    Monthly sales summary.

    Query count: 1
    Output columns: month, total_sales, order_count, avg_order_value
    """
    from apps.orders.models import Order

    return (
        Order.objects
        .filter(
            status="completed",
            created_at__date__gte=start_date,
            created_at__date__lte=end_date,
        )
        .annotate(month=TruncMonth("created_at"))
        .values("month")
        .annotate(
            total_sales=Sum("total"),
            order_count=Count("id"),
            avg_order_value=Avg("total"),
        )
        .order_by("month")
    )
]]></code>
    </example>
  </section>

  <reference path="templates/selector.py">Selector skeleton with prefetch and query-count docstring.</reference>
</text>
