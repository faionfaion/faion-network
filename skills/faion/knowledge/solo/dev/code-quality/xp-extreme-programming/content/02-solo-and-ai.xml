<?xml version="1.0" encoding="UTF-8"?>
<text id="02-solo-and-ai" title="Solo XP and AI-Assisted Development">
  <summary>XP adaptations for solo developers with Claude as navigator, agentic TDD loop, and AI pairing gotchas.</summary>

  <section title="Solo XP Practices">
    <p>Essential solo practices: TDD, Simple Design, Small Releases, Continuous Integration, Refactoring.</p>
    <p>Adapted practices: Pair Programming becomes AI navigator + human driver (or rubber duck). On-site Customer becomes async product feedback. Collective Ownership is not applicable.</p>
    <rule>
      <statement>Track velocity in completed stories per iteration, not lines of code. LLMs inflate LOC — velocity in LOC is meaningless when an agent writes boilerplate.</statement>
      <rationale>Story completion maps to actual business value delivery. LOC is a proxy that breaks down entirely with AI assistance.</rationale>
    </rule>
  </section>

  <section title="AI Navigator Pattern (Red-Green-Refactor)">
    <reference path="templates/xp-navigator-prompt.txt">Claude-as-navigator prompt for TDD pairing session</reference>
    <rule>
      <statement>Human writes the failing test first, then shows it to Claude. Claude proposes the minimal green implementation. Human approves before Claude writes anything.</statement>
      <rationale>"AI as pair" hype skips the disagreement function of pairing. LLMs default to compliance. The human-writes-test-first discipline forces specification before implementation and prevents Claude from generating both test and source in one shot.</rationale>
    </rule>
    <rule>
      <statement>Forbid the agent from editing both the test file and the source file in the same tool call. Test edit must be a separate commit before the implementation edit.</statement>
      <rationale>When agent edits both simultaneously, TDD discipline collapses — the test is written to match the implementation rather than specify it.</rationale>
    </rule>
  </section>

  <section title="AI Pairing Gotchas">
    <antipattern>
      <description>Accepting LLM-proposed "future-proofing" additions.</description>
      <reason>When agent proposes extra features or abstractions not required by the failing test, reject and cite YAGNI. Over-engineering is the LLM's default failure mode.</reason>
    </antipattern>
    <antipattern>
      <description>Claude as the only code reviewer (no human review scheduled).</description>
      <reason>LLMs don't catch architectural drift. Schedule a weekly human review; agents miss design-level problems that accumulate over sessions.</reason>
    </antipattern>
    <antipattern>
      <description>Running long sessions without compaction on high-context work.</description>
      <reason>Burning context on sessions over 100k tokens degrades quality. Reset/compact every ~100k tokens; sustainable pace applies to LLM tokens too.</reason>
    </antipattern>
  </section>

  <section title="Remote Team Adaptations">
    <p>Pair programming: VS Code Live Share, Tuple, Pop. Async stand-ups: Slack/Teams. Virtual planning: Miro, FigJam. More documentation to compensate for async communication.</p>
    <p>Measured cost: screen-share pairing has ~30% lower defect-catch rate than co-located pairing (factor into team expectations).</p>
  </section>

  <section title="Pre-Push Hook">
    <reference path="templates/pre-push-hook.sh">Pre-push hook: never break the build (pytest + ruff)</reference>
  </section>
</text>
