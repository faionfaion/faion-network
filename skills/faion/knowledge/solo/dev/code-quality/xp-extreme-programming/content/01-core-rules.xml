<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="xp-extreme-programming Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for xp-extreme-programming. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Evaluate design proposals in this priority order: (1) passes all tests, (2) reveals intention, (3) no duplication, (4) fewest elements. When in conflict, earlier takes precedence.</statement>
      <rationale source="v1-source">LLMs over-engineer by defaulting to "future-proof" designs. The priority order is an explicit rejection function: any design that adds elements without satisfying a failing test violates YAGNI.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Commit at least once per day. All tests must pass locally before commit. Never leave the build broken. Fix failures immediately — drop everything else.</statement>
      <rationale source="v1-source">A broken build blocks every team member. The XP contract is: integration is everyone's responsibility, not the last person to push.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Track velocity in completed stories per iteration, not lines of code. LLMs inflate LOC — velocity in LOC is meaningless when an agent writes boilerplate.</statement>
      <rationale source="v1-source">Story completion maps to actual business value delivery. LOC is a proxy that breaks down entirely with AI assistance.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Human writes the failing test first, then shows it to Claude. Claude proposes the minimal green implementation. Human approves before Claude writes anything.</statement>
      <rationale source="v1-source">"AI as pair" hype skips the disagreement function of pairing. LLMs default to compliance. The human-writes-test-first discipline forces specification before implementation and prevents Claude from generating both test and source in one shot.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Forbid the agent from editing both the test file and the source file in the same tool call. Test edit must be a separate commit before the implementation edit.</statement>
      <rationale source="v1-source">When agent edits both simultaneously, TDD discipline collapses — the test is written to match the implementation rather than specify it.</rationale>
    </rule>
  </section>
</text>
