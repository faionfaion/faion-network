<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="tailwind-architecture Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for tailwind-architecture. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>All design values (colors, spacing, font families, border radii, shadows) must be defined in tailwind.config.ts (v3) or an @theme block (v4). No arbitrary Tailwind values (w-[347px], bg-[#3b82f6]) are allowed outside of one-off overrides explicitly safelist-annotated.</statement>
      <rationale source="v1-source">Arbitrary values bypass the token pipeline entirely. When the design changes, arbitrary values must be hunted down with grep. Config-defined utilities are found by IDE autocomplete and Tailwind's purge.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>All conditional class composition must go through a project-tuned cn() function that wraps clsx + tailwind-merge. Ban raw clsx-only joins in lint. The cn() must be configured with extendTailwindMerge for any custom token utilities so conflicts are resolved correctly.</statement>
      <rationale source="v1-source">Without tailwind-merge, conflicting classes (bg-red-500 overridden by bg-blue-500) both apply — the last class in the string wins based on CSS specificity, not code order. tailwind-merge resolves conflicts by Tailwind utility group.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Extract a duplicated utility chain into a primitive component when it meets ALL of: 5 or more utilities, appears in 3 or more call sites, and represents a named UI concept (Card, Stack, Button). Do not extract for 2 duplicates — the cost of indirection exceeds the benefit.</statement>
      <rationale source="v1-source">Premature extraction adds abstraction cost (imports, props, docs) without simplifying code. The 5/3 threshold is a concrete, auditable rule agents can apply without judgment.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>@apply is allowed only inside @layer components in CSS files. It is forbidden inside component .tsx/.vue/.svelte files and inside @layer utilities. Any @apply in component code must be replaced with a React/Vue component abstraction.</statement>
      <rationale source="v1-source">@apply in component files defeats Tailwind's JIT tree-shaking, inflates CSS bundle size, and forces CSS file management alongside component files. Component abstraction serves the same purpose with better colocation.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use prettier-plugin-tailwindcss to enforce canonical class order automatically. Never manually reorder classes in PR review — the formatter defines the order. Run the formatter before any diff review to eliminate noise.</statement>
      <rationale source="v1-source">Inconsistent class order makes diffs unreadable and causes merge conflicts on unrelated lines. Automated formatting makes class order a non-discussion.</rationale>
    </rule>
  </section>
</text>
