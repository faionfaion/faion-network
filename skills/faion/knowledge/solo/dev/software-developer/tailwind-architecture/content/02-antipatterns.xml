<?xml version="1.0" encoding="UTF-8"?>
<text id="02-antipatterns" title="Tailwind Architecture Antipatterns">
  <summary>Dynamic class purge failure, tailwind-merge config drift, v3/v4 mixing, deprecated opacity syntax, and over-extraction.</summary>

  <section title="Dynamic Class Purge Failure">
    <antipattern>
      <description>Building class names dynamically: bg-${color}-500, text-${size}.</description>
      <reason>JIT cannot statically analyze template string concatenation. The generated class names are not included in the CSS bundle. Production styles are missing. Use a static lookup map: const colorMap = { red: 'bg-red-500', blue: 'bg-blue-500' } and safelist entries for any truly dynamic cases.</reason>
    </antipattern>
  </section>

  <section title="tailwind-merge Config Drift">
    <antipattern>
      <description>Adding custom token utilities to tailwind.config.ts theme.extend without calling extendTailwindMerge to register them.</description>
      <reason>tailwind-merge does not know about custom utilities. When two custom utilities from the same group conflict, both apply silently instead of the second overriding the first.</reason>
    </antipattern>
  </section>

  <section title="v3 vs v4 Config Mixing">
    <antipattern>
      <description>Emitting a tailwind.config.js in a project using Tailwind v4's @theme CSS block, or writing @theme in a v3 project.</description>
      <reason>v4 removed tailwind.config.js as the primary config mechanism. v3 does not support @theme. Mixing produces broken builds with no error message about the version mismatch.</reason>
    </antipattern>
  </section>

  <section title="Deprecated Opacity Syntax">
    <antipattern>
      <description>Using bg-opacity-50 or text-opacity-75 instead of bg-black/50 or text-gray-900/75.</description>
      <reason>bg-opacity-* was deprecated in Tailwind v3. The slash syntax is canonical and supported by all modern Tailwind versions. bg-opacity-* may be removed in v4.</reason>
    </antipattern>
  </section>

  <section title="Over-Extraction">
    <antipattern>
      <description>Creating a component (ButtonRow, TextWrapper, PaddedBox) for 2-utility duplicates.</description>
      <reason>The cost of an additional import, props interface, and documentation exceeds the benefit of removing 2 classes. Apply the 5-utility / 3-site threshold before extracting.</reason>
    </antipattern>
  </section>

  <section title="@apply Everywhere">
    <antipattern>
      <description>Using @apply to create utility aliases: .text-big { @apply text-2xl; } or embedding @apply inside .tsx component files.</description>
      <reason>Utility aliases hide what classes are being used from IDE tools and the Tailwind scan. Embedded @apply in component files increases CSS bundle size and breaks JIT tree-shaking.</reason>
    </antipattern>
  </section>
</text>
