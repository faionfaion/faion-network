<?xml version="1.0" encoding="UTF-8"?>
<text id="03-antipatterns" title="Python Async Anti-patterns">
  <summary>Blocking calls in async, unawaited coroutines, and fire-and-forget misuse.</summary>

  <antipattern>
    <description>Calling sync I/O (requests.get, time.sleep, psycopg2.connect) inside async def.</description>
    <reason>Blocks the entire event loop for all concurrent tasks for the full duration of the call.</reason>
  </antipattern>

  <example type="bad">
    <code lang="python"><![CDATA[
# BAD: blocks event loop
async def fetch_data():
    response = requests.get(URL)  # sync HTTP call
    return response.json()
]]></code>
  </example>

  <example type="good">
    <code lang="python"><![CDATA[
# GOOD: async client
async def fetch_data():
    async with httpx.AsyncClient() as client:
        response = await client.get(URL)
        return response.json()
]]></code>
  </example>

  <antipattern>
    <description>Calling a coroutine without await: result = fetch_data() instead of result = await fetch_data().</description>
    <reason>The coroutine is created but never executed; the call silently does nothing and returns a coroutine object.</reason>
  </antipattern>

  <antipattern>
    <description>Fire-and-forget with asyncio.create_task() without storing the reference.</description>
    <reason>Tasks without strong references may be garbage-collected mid-execution; exceptions are silently swallowed.</reason>
  </antipattern>

  <antipattern>
    <description>Catching Exception (or bare except) in async code, which includes CancelledError on Python 3.7; swallowing it prevents graceful shutdown.</description>
    <reason>CancelledError must propagate for TaskGroup and timeout() to cancel correctly. On 3.8+ CancelledError no longer inherits Exception, but agents use old patterns.</reason>
  </antipattern>

  <antipattern>
    <description>Creating httpx.AsyncClient() inside a request handler or loop iteration.</description>
    <reason>Each instantiation opens a new connection pool; leaks sockets and breaks HTTP/2 keep-alive. App-scoped singleton is required.</reason>
  </antipattern>

  <rule>
    <statement>Treat CancelledError as control flow: catch only to perform cleanup, then re-raise unconditionally.</statement>
    <rationale>Swallowing CancelledError prevents TaskGroup and timeout() from completing cancellation, leaving zombie tasks.</rationale>
  </rule>

  <reference path="templates/detect_sync_leaks.py">AST scanner for blocking calls inside async def</reference>
</text>
