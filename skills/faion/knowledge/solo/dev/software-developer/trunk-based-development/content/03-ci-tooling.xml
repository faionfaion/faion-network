<?xml version="1.0" encoding="UTF-8"?>
<text id="03-ci-tooling" title="Pre-commit Hooks, GitHub Actions Pipeline, DORA Metrics">
  <summary>Pre-commit hook config for local quality gates, GitHub Actions CI pipeline for trunk pushes, and DORA metrics that TBD should improve.</summary>

  <section title="Pre-commit Hooks">
    <rule>
      <statement>Gate every commit with tests, lint, and type-check locally to catch issues before they hit CI; this is the first line of defense for "trunk always green".</statement>
      <rationale>A CI failure that takes 8 minutes to detect means developers have already started the next task. Local pre-commit hooks give feedback in seconds.</rationale>
    </rule>
    <example type="good">
      <code lang="yaml"><![CDATA[
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: tests
        name: Run tests
        entry: pytest --tb=short -q
        language: system
        pass_filenames: false
      - id: lint
        name: Lint
        entry: ruff check .
        language: system
        pass_filenames: false
      - id: type-check
        name: Type check
        entry: mypy src/
        language: system
        pass_filenames: false
]]></code>
    </example>
    <reference path="templates/trunk-ci.yml">GitHub Actions workflow with test, lint, type-check, staging deploy</reference>
  </section>

  <section title="DORA Metrics Target">
    <p>TBD correlates with elite DORA performance. Targets: deploy frequency = on demand (multiple times/day); lead time for changes &lt;1 h; change failure rate &lt;15%; time to restore &lt;1 h. Track with Sleuth, LinearB, or a lightweight custom script.</p>
    <rule>
      <statement>Measure deployment frequency and lead time weekly; if neither improves after 4 weeks of TBD, revisit CI speed and PR size SLAs first.</statement>
      <rationale>TBD is only justified by measurable DORA improvement. If metrics don't improve, the bottleneck is usually slow CI or PRs that are too large, not the branching model itself.</rationale>
    </rule>
  </section>
</text>
