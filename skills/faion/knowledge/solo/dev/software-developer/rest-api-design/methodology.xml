<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="rest-api-design">
  <metadata>
    <tier>solo</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>Resource-oriented HTTP API design conventions: plural nouns, correct HTTP method semantics, standard status codes, consistent filter/pagination parameters.</summary>
    <tags>
      <tag>rest</tag>
      <tag>http-api</tag>
      <tag>api-design</tag>
      <tag>openapi</tag>
      <tag>conventions</tag>
    </tags>
    <category>design</category>
    <difficulty>intermediate</difficulty>
    <created>2026-04-29</created>
  </metadata>
  <content>
    <title>REST API Design</title>
    <summary>Resource-oriented HTTP API design conventions: plural noun paths, correct HTTP method semantics, standard status codes, and consistent filter/pagination parameters. Core rule: paths use plural nouns in kebab-case with no verbs (/users, not /getUsers); POST returns 201 + Location header; DELETE returns 204; errors use RFC 7807 application/problem+json.</summary>
    <why>
      <p>APIs without consistent shape cause integration failures, client SDK mismatches, and maintenance nightmares. LLM agents misuse PUT vs PATCH and pick 200 everywhere; a Spectral ruleset committed to the repo enforces conventions in CI rather than relying on reviewers.</p>
    </why>
    <when-to-use>
      <list>
        <item>Designing a new HTTP API for a web/mobile client or third-party consumer</item>
        <item>Refactoring RPC-shaped endpoints (/getUsers, /doThing) into resource-oriented routes</item>
        <item>Reviewing PR diffs for status-code, method-semantic, or naming drift</item>
        <item>Generating client SDKs or OpenAPI specs that depend on consistent route shapes</item>
        <item>Onboarding LLM agents that will call the API — predictable verbs/nouns reduce tool-use errors</item>
      </list>
    </when-to-use>
    <when-not-to-use>
      <list>
        <item>Internal RPC between trusted services with strict latency budgets — gRPC fits better</item>
        <item>Highly relational graph queries with many shapes per page — GraphQL avoids round-trips</item>
        <item>Server-streamed events (logs, ticks, model output) — SSE/WebSockets/HTTP streaming</item>
        <item>Pure file transfer pipelines — S3-style presigned URLs beat REST envelopes</item>
        <item>One-off webhook receivers where shape is dictated by the sender</item>
      </list>
    </when-not-to-use>
    <section name="Path Naming">
      <rule>
        <statement>Paths use plural nouns in kebab-case; no verbs; singletons nested under their collection: /users/{id}/orders/{order-id}.</statement>
        <rationale>Verb paths (/getUsers, /doThing) couple the URL to implementation, break caching semantics, and confuse SDK generators. Plural nouns make collection vs singleton unambiguous.</rationale>
      </rule>
      <example type="good">
        <code lang="text"><![CDATA[GET    /users                    # list
GET    /users/{id}               # get one
POST   /users                    # create
PATCH  /users/{id}               # partial update
PUT    /users/{id}               # full replace
DELETE /users/{id}               # delete
GET    /users/{id}/orders        # sub-collection]]></code>
      </example>
      <antipattern>
        <description>Using verbs in paths: /getUsers, /createOrder, /api/getUserById/{id}.</description>
        <reason>Verbs in paths make REST APIs RPC-shaped, breaking HTTP caching, client SDK generation, and gateway routing rules.</reason>
      </antipattern>
    </section>
    <section name="HTTP Method Semantics">
      <rule>
        <statement>Use PATCH for partial updates and PUT for full replacement; most CRUD operations want PATCH. GET and DELETE must be idempotent.</statement>
        <rationale>LLMs default to PUT for every update; this silently NULLs fields the client didn't send. PATCH with a partial body is safe for incremental edits.</rationale>
      </rule>
      <p>Method table: GET = read, safe + idempotent. POST = create, not safe/idempotent. PUT = replace, idempotent. PATCH = partial update. DELETE = remove, idempotent.</p>
    </section>
    <section name="Status Codes">
      <rule>
        <statement>POST that creates a resource must return 201 + Location header. Successful DELETE returns 204. Never use 200 for creation or deletion.</statement>
        <rationale>200 on creation hides whether the resource is new or existing; clients can't distinguish. Location on 201 gives the new resource URI without a second request.</rationale>
      </rule>
      <p>Core codes: 200 GET/PUT/PATCH success. 201 POST success. 204 DELETE success. 400 validation. 401 missing/invalid auth. 403 forbidden. 404 not found. 409 conflict. 422 semantic validation. 429 rate limit. 500 server error.</p>
    </section>
    <section name="Error Shape">
      <rule>
        <statement>Return errors as RFC 7807 application/problem+json so clients and LLM agents parse a stable structure.</statement>
        <rationale>Custom error shapes per endpoint require endpoint-specific parsing. RFC 7807 gives a known shape (type, title, status, detail) that clients handle generically.</rationale>
      </rule>
    </section>
    <section name="HATEOAS">
      <rule>
        <statement>Include _links only when clients actually follow them; don't invest in full hypermedia if clients ignore links and hardcode paths anyway.</statement>
        <rationale>HATEOAS is the ideal but rarely justified in practice — most client teams hardcode paths regardless. YAGNI applies; add links only when a client will use them.</rationale>
      </rule>
      <example type="good">
        <code lang="json"><![CDATA[{
  "id": "123",
  "name": "John Doe",
  "_links": {
    "self":   { "href": "/users/123" },
    "orders": { "href": "/users/123/orders" },
    "update": { "href": "/users/123", "method": "PATCH" }
  }
}]]></code>
      </example>
    </section>
    <section name="Pagination">
      <rule>
        <statement>Pick one pagination strategy globally — cursor for streams, offset for finite listings — and use consistent parameter names (?limit, ?cursor or ?offset).</statement>
        <rationale>Per-endpoint pagination parameters make API clients maintain per-endpoint pagination logic. One strategy, one parameter set, applied everywhere.</rationale>
      </rule>
      <example type="good">
        <code lang="text"><![CDATA[# Offset (finite, sortable):
GET /users?limit=20&offset=0

# Cursor (stream, stable):
GET /events?limit=20&cursor=eyJpZCI6MTIzfQ]]></code>
      </example>
    </section>
    <section name="Filtering and Sorting">
      <rule>
        <statement>Use query params for filtering (?status=active) and sorting (?sort=-created_at for descending). Use ?fields=a,b,c for sparse fieldsets.</statement>
        <rationale>Consistent filter and sort conventions mean SDK generators and LLM agents can infer the pattern from one example and apply it to all endpoints.</rationale>
      </rule>
    </section>
    <section name="Idempotency Keys">
      <rule>
        <statement>Document and enforce Idempotency-Key headers on POST endpoints that charge, create orders, or send messages; without them retries cause double-creates.</statement>
        <rationale>Network failures make retries necessary. Without idempotency keys, a client that retried a timed-out order creation will create two identical orders.</rationale>
      </rule>
    </section>
    <section name="Sub-resource Depth">
      <rule>
        <statement>Limit sub-resource nesting to two levels (/a/{id}/b/{id}); anything deeper is a code smell — flatten with query params.</statement>
        <rationale>Deep nesting (/a/{id}/b/{id}/c/{id}) creates excessively long, hard-to-maintain URLs. LLM agents readily produce three+ levels of nesting.</rationale>
      </rule>
    </section>
    <section name="Where it fails / limitations">
      <p>HATEOAS links rarely get used by real clients; over-investing wastes design budget. "Resource-oriented" forces awkward modeling of actions ("approve order") — accept POST /orders/{id}/approvals instead of bending verbs. Filter/sort/pagination semantics are not standardized; teams reinvent them per endpoint. Bulk operations don't fit cleanly; POST /batch or RFC 7396 patches end up bespoke. LLM agents misuse PUT vs PATCH and pick 200 where 201/204 is correct — needs explicit linting.</p>
    </section>
    <section name="Agentic workflow">
      <p>Run a design pass with a planning subagent that produces an OpenAPI sketch from the user's resource list, then a reviewer subagent that lints route shapes (plural nouns, kebab-case, no verbs, correct method-status pairing). Implementation agents consume the sketch to scaffold routers, serializers, and tests. Wire the linter into CI so PRs that drift from the design are blocked, not just commented on.</p>
    </section>
    <section name="Recommended subagents">
      <list>
        <item>faion-sdd-executor-agent — feature-level design → spec → impl loop with quality gates</item>
        <item>Custom api-design-reviewer (sonnet) — checks routes against the rule table; reads OpenAPI YAML and PR diffs</item>
        <item>Custom openapi-scaffolder (haiku) — generates router boilerplate from a vetted spec</item>
      </list>
    </section>
    <section name="Prompt pattern">
      <code lang="text"><![CDATA[Design REST routes for resource <X> with operations <create/list/get/update/delete/<custom>>.
Rules: plural nouns, kebab-case, no verbs in path, sub-resources for ownership.
Return: route table | method | status code | request shape | response shape.
Then output an OpenAPI 3.1 fragment.]]></code>
    </section>
    <section name="CLI tools">
      <p>spectral: Lint OpenAPI/AsyncAPI specs against rulesets (Stoplight). npm i -g @stoplight/spectral-cli · https://stoplight.io/open-source/spectral</p>
      <p>redocly lint: Strict OpenAPI validation + style guide. npm i -g @redocly/cli</p>
      <p>openapi-generator-cli: Generate clients/servers from spec. npm i -g @openapitools/openapi-generator-cli</p>
      <p>httpie / curl: Manual smoke tests during agent loops. https://httpie.io</p>
      <p>hurl: Repeatable HTTP test files; LLM-readable. https://hurl.dev</p>
      <p>schemathesis: Property-based tests driven by OpenAPI. pip install schemathesis</p>
      <p>dredd: Contract-test API against spec. npm i -g dredd</p>
      <p>bruno / posting: Local-first API clients with Git-versioned collections. https://www.usebruno.com · https://posting.sh</p>
    </section>
    <section name="Services and apps">
      <p>Stoplight Studio: SaaS+OSS, visual OpenAPI editor; agents drive via Spectral CLI instead.</p>
      <p>Postman / Bruno: SaaS / OSS, Bruno stores collections as files — agents can edit/run via CLI.</p>
      <p>Insomnia: OSS, has CLI (inso) for spec lint and run.</p>
      <p>ReadMe / Mintlify / Redocly: SaaS, render OpenAPI to docs sites; webhook-driven.</p>
      <p>Apidog / SwaggerHub: SaaS, UI-first; agents prefer Spectral+Git workflow.</p>
      <p>Cloudflare API Gateway / Kong: SaaS / OSS, enforce schemas at edge; agents push specs via API.</p>
    </section>
    <section name="Best practices">
      <list>
        <item>Lock the route-shape rules in a Spectral ruleset committed to the repo so CI, not reviewers, enforces them</item>
        <item>Use Location header on 201; never invent a custom field for the new resource ID</item>
        <item>Pagination: pick one — cursor for streams, offset for finite listings — and apply globally (?limit, ?cursor/?offset)</item>
        <item>Use ?fields=a,b,c and ?expand=... consistently rather than per-endpoint shape flags</item>
        <item>Reserve PUT for full replacement; PATCH for partial. Most CRUD wants PATCH — agents default to PUT incorrectly</item>
        <item>Return errors as RFC 7807 application/problem+json so clients/LLMs parse a stable shape</item>
        <item>Document idempotency keys on POST for charge/order endpoints; without them retries cause double-creates</item>
      </list>
    </section>
    <section name="AI-agent gotchas">
      <list>
        <item>LLMs invent /api/getUserById/{id} patterns from training data; lint must catch verb prefixes</item>
        <item>Agents pick 200 OK everywhere; require a per-route status-code annotation in the spec stage</item>
        <item>When generating SDKs from spec, agents will hallucinate query params not in the schema — pin generation to the committed spec digest</item>
        <item>Human-in-loop checkpoint: any breaking change (removed field, narrowed type, status-code shift) must trigger explicit reviewer approval, not auto-merge</item>
        <item>Agents forget to set Content-Type: application/json on examples, then build wrong test fixtures</item>
        <item>Sub-resource depth >2 (/a/{id}/b/{id}/c/{id}) is a code smell agents happily produce; flag and refactor</item>
      </list>
    </section>
    <templates>
      <template path="templates/spectral-ruleset.yaml" purpose="Spectral ruleset enforcing plural nouns, kebab-case, POST→201 in CI"/>
    </templates>
    <see-also>
      <link href="https://www.rfc-editor.org/rfc/rfc7231"/>
      <link href="https://www.rfc-editor.org/rfc/rfc7807"/>
      <link href="https://www.rfc-editor.org/rfc/rfc8594"/>
      <link href="https://github.com/microsoft/api-guidelines"/>
      <link href="https://opensource.zalando.com/restful-api-guidelines/"/>
      <link href="https://cloud.google.com/apis/design"/>
      <link href="https://stoplight.io/open-source/spectral"/>
    </see-also>
  </content>
</methodology>
