<?xml version="1.0" encoding="UTF-8"?>
<text id="03-migrations" title="Migration Strategy">
  <summary>Alembic migrations, expand-contract pattern for zero-downtime changes, partitioning for large tables.</summary>

  <section title="Migration Rules">
    <rule>
      <statement>Use the expand-contract pattern for adding NOT NULL columns to large tables: add as nullable, backfill in batches, then add the NOT NULL constraint once 100% populated.</statement>
      <rationale>A single `ALTER TABLE ADD COLUMN NOT NULL DEFAULT` on a large Postgres table rewrites the entire table and holds an exclusive lock for the duration. This causes production downtime proportional to table size.</rationale>
    </rule>
    <rule>
      <statement>Every migration must have a reversible `downgrade()` function or be explicitly marked as irreversible with a comment explaining why.</statement>
      <rationale>Without a down migration, rolling back a deployment requires manual intervention. Even irreversible data migrations (e.g., dropping a column) should have a stub that raises an explicit error rather than silently failing.</rationale>
    </rule>
  </section>

  <section title="Alembic Migration Example">
    <example type="good">
      <code lang="python"><![CDATA[
"""Add user_preferences table — reversible migration

Revision ID: a1b2c3d4e5f6
Revises: 9z8y7x6w5v4u
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

def upgrade():
    op.create_table(
        'user_preferences',
        sa.Column('id', postgresql.UUID(), primary_key=True),
        sa.Column('user_id', postgresql.UUID(), nullable=False),
        sa.Column('theme', sa.String(20), default='light'),
        sa.Column('language', sa.String(5), default='en'),
        sa.ForeignKeyConstraint(['user_id'], ['users.id'], ondelete='CASCADE'),
        sa.UniqueConstraint('user_id')
    )
    op.create_index('idx_user_prefs_user', 'user_preferences', ['user_id'])

def downgrade():
    op.drop_table('user_preferences')
]]></code>
    </example>
    <reference path="templates/migration.py">Alembic migration with expand-contract pattern</reference>
  </section>
</text>
