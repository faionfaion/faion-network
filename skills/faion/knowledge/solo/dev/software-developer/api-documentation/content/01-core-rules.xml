<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="api-documentation Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for api-documentation. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every API reference must include: Overview, Authentication, Quick Start (with copy-paste curl), Endpoints, Error Codes table, and Changelog.</statement>
      <rationale source="v1-source">Developers evaluate APIs in under 5 minutes. Missing any of these six sections causes abandonment before integration begins.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Quick Start must include a working curl command that produces a visible result with no setup beyond an API key.</statement>
      <rationale source="v1-source">A runnable first call is the fastest proof of value. SDKs and full docs can wait; a curl example cannot.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Error Codes table must include: code, meaning, and a resolution hint for every non-2xx response the API can return.</statement>
      <rationale source="v1-source">Clients debug by error code. An error code with no resolution hint requires reading source code or filing a support ticket.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Changelog must record breaking changes, new endpoints, and bug fixes per version with a date.</statement>
      <rationale source="v1-source">Consumers must know when to update their integrations. Undocumented breaking changes cause production outages.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Provide code examples in at least two languages (curl + one SDK); keep them synchronized with tests so they do not go stale.</statement>
      <rationale source="v1-source">Most consumers copy-paste the example closest to their stack. Stale examples that do not work destroy trust faster than missing docs.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Include rate limit information (requests per window, headers returned, behavior on breach) in the Authentication or Overview section.</statement>
      <rationale source="v1-source">Clients that hit rate limits without prior knowledge implement incorrect retry logic, causing cascading failures.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Set title, description (with auth instructions and rate limits), and version in the FastAPI app constructor to populate Swagger UI and Redoc automatically.</statement>
      <rationale source="v1-source">FastAPI generates OpenAPI from app metadata. Setting it once populates both /docs and /redoc with no extra work.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Add named examples under requestBody and responses for every endpoint that has non-trivial input or output variants.</statement>
      <rationale source="v1-source">Named examples appear in Swagger UI's dropdown, letting developers try each scenario without constructing the body manually.</rationale>
    </rule>
  </section>
</text>
