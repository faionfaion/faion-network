<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="shadcn-ui-architecture">
  <metadata>
    <tier>solo</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>Copy-into-codebase component architecture using shadcn/ui: Radix UI primitives + Tailwind + CVA variants. The team owns the component source (not an npm dependency).</summary>
    <tags>
      <tag>shadcn</tag>
      <tag>ui-components</tag>
      <tag>radix</tag>
      <tag>tailwind</tag>
      <tag>design-tokens</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-26</created>
    <tools>
      <tool>claude-code</tool>
      <tool>cursor</tool>
    </tools>
  </metadata>

  <content>
    <title>shadcn/ui Component Architecture</title>

    <summary>Copy-into-codebase component architecture using shadcn/ui: Radix UI primitives + Tailwind + CVA variants. The team owns the component source (not an npm dependency). Primitives live in `components/ui/` (protected); feature compositions reference them from `components/&lt;feature&gt;/`. Design tokens live in CSS variables in `globals.css`.</summary>

    <why>
      <p>shadcn gives accessible Radix primitives with CVA-driven variant management without the semver coupling of a library. Teams can fork and modify any component. The MCP server lets agents scaffold via `npx shadcn@latest add` without hand-writing boilerplate. Tailwind CSS variable tokens provide theme-ability and dark mode without hardcoded color values.</p>
    </why>

    <when-to-use>
      <list>
        <item>Bootstrapping a Tailwind + Radix design system on Next.js, Remix, Vite+React, or Astro</item>
        <item>Solo/small teams that need to fork and tweak primitives without upstream coupling</item>
        <item>Projects where an agent can scaffold via the official CLI and layer compositions on top</item>
        <item>Products that need dark mode theming via CSS custom properties</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Vue, Svelte, Solid — use the framework-specific port (shadcn-vue, shadcn-svelte) instead</item>
        <item>Regulated environments requiring versioned, audited UI dependencies with full supply-chain provenance</item>
        <item>Teams that want semver-guaranteed components — shadcn is a starter, not a maintained dependency</item>
        <item>Purely static layouts with no interactive behavior — a CSS-only kit is lighter</item>
      </list>
    </when-not-to-use>

    <section name="directory-layout">
      <rule testable="true">
        <statement>Keep `components/ui/` as vendored primitives only: no business logic, no feature-specific state. Feature compositions live in `components/&lt;feature&gt;/` and reference `ui/` primitives.</statement>
        <rationale>Mixing business logic into primitives makes them reusable in name only; every feature requirement bleeds in and the component becomes untestable independently.</rationale>
      </rule>
      <rule testable="true">
        <statement>Treat `components/ui/*` as protected: gate via CODEOWNERS or a lint rule; require a separate explicit PR to modify them, never silent edits during feature work.</statement>
        <rationale>UI primitive changes are blast-radius high — they affect every consumer. Silent agent edits during feature work are a top source of regressions.</rationale>
      </rule>
    </section>

    <section name="design-tokens">
      <rule testable="true">
        <statement>Define all colors, radii, and spacing tokens in ONE place — `globals.css` `:root` and `.dark` blocks. Ban inline hex values (`#fff`, `bg-[#abc123]`) via an ESLint rule.</statement>
        <rationale>Scattered hardcoded values make theme changes require a global search-replace; a single token source makes dark mode and brand updates a one-line change.</rationale>
      </rule>
      <rule testable="true">
        <statement>In dark mode, always use semantic token names (`--background`, `--foreground`, `--destructive`), never concrete Tailwind shades (`dark:bg-gray-800`).</statement>
        <rationale>Concrete shades require per-component dark-mode overrides and drift across components; semantic tokens propagate theme changes automatically.</rationale>
      </rule>
    </section>

    <section name="cva-variant-management">
      <rule testable="true">
        <statement>Use `cva` for variants; use `compoundVariants` for state combinations (`variant="outline" + size="sm"`) instead of adding new flat variants. Stop adding variants when count exceeds ~6 per prop.</statement>
        <rationale>Flat variant explosion creates a junk-drawer component with 12+ variants that are impossible to maintain or visually verify.</rationale>
      </rule>
      <rule testable="true">
        <statement>Always export both the component and its `*Variants` CVA function (e.g., `buttonVariants`). Downstream code may need `cn(buttonVariants({ variant }))` for non-button elements.</statement>
        <rationale>Exporting only the component hides the variant contract from compositions that style non-semantic elements to match.</rationale>
      </rule>
    </section>

    <section name="forwardref-and-displayname">
      <rule testable="true">
        <statement>All primitive components must use `React.forwardRef`. Add `ComponentName.displayName = 'ComponentName'` immediately after the `forwardRef` call.</statement>
        <rationale>Without `forwardRef`, ref wiring breaks for Radix `asChild` slots. Without `displayName`, React DevTools shows `_c2` and agent debugging is impossible.</rationale>
      </rule>
      <antipattern>
        <description>Re-running `npx shadcn@latest add button` to "upgrade" overwrites local edits silently.</description>
        <reason>shadcn has no automatic upgrade path; running `add` again replaces your local modifications. Always diff after `add`.</reason>
      </antipattern>
      <antipattern>
        <description>Wrapping `&lt;Slot&gt;` children in extra divs when using `asChild`.</description>
        <reason>Radix `asChild` requires exactly one React child. Extra wrapper divs break slot semantics and lose focus management.</reason>
      </antipattern>
    </section>

    <section name="primitive-with-cva">
      <example type="good" title="Primitive component with CVA and forwardRef">
        <code lang="tsx"><![CDATA[
// components/ui/button.tsx — primitive, never edited during feature work
import { cva, type VariantProps } from 'class-variance-authority';
import React from 'react';
import { cn } from '@/lib/utils';

const buttonVariants = cva(
  'inline-flex items-center justify-center rounded-md font-medium transition-colors',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        destructive: 'bg-destructive text-destructive-foreground',
        outline: 'border border-input bg-background hover:bg-accent',
        ghost: 'hover:bg-accent hover:text-accent-foreground',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm: 'h-9 px-3',
        lg: 'h-11 px-8',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: { variant: 'default', size: 'default' },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, ...props }, ref) => (
    <button
      className={cn(buttonVariants({ variant, size, className }))}
      ref={ref}
      {...props}
    />
  )
);
Button.displayName = 'Button';

export { Button, buttonVariants };
        ]]></code>
        <note>Exports both component and variants function. Uses semantic token names only. displayName set after forwardRef.</note>
      </example>
    </section>

    <section name="feature-composition">
      <example type="good" title="Feature-level composition outside ui/">
        <code lang="tsx"><![CDATA[
// components/forms/submit-button.tsx — composition, lives outside ui/
import { Button } from '@/components/ui/button';
import { Spinner } from '@/components/ui/spinner';

interface SubmitButtonProps {
  loading?: boolean;
  children: React.ReactNode;
}

export function SubmitButton({ loading, children }: SubmitButtonProps) {
  return (
    <Button type="submit" disabled={loading}>
      {loading ? <Spinner aria-hidden="true" /> : null}
      {children}
    </Button>
  );
}
        ]]></code>
        <note>Compositions extend, never modify, primitives. Feature-specific logic stays outside `components/ui/`.</note>
      </example>
    </section>

    <section name="theming">
      <example type="good" title="Design tokens in globals.css">
        <code lang="css"><![CDATA[
/* globals.css — single source of truth for all design tokens */
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --radius: 0.5rem;
  }
  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
  }
}
        ]]></code>
        <note>All colors as HSL triplets in CSS vars. Components reference `var(--background)` via Tailwind utilities — never hardcoded hex.</note>
      </example>
    </section>

    <section name="agentic-workflow">
      <p>Drive shadcn from a Claude subagent that owns three phases: (1) install primitives via `npx shadcn@latest add` based on a feature spec, (2) compose feature-level components in `components/&lt;feature&gt;/` referencing only `components/ui/*` primitives, (3) verify a11y + variant coverage against the checklist. Use the official `shadcn` MCP server so the agent can read the registry, list available components, and resolve dependencies before writing code. Keep `components/ui/*` in a protected glob — agents propose patches there as separate PRs, never silent edits.</p>
    </section>

    <section name="recommended-subagents">
      <p>`frontend-implementer` (Sonnet) — runs `shadcn add`, scaffolds compositions, wires CVA variants from a spec.</p>
      <p>`ui-reviewer` (Sonnet) — diffs new compositions against patterns, flags variant explosion and missing `forwardRef`/`displayName`.</p>
      <p>`a11y-auditor` (Haiku) — runs the accessibility checklist (semantic HTML, ARIA, keyboard, focus traps) on every new primitive composition.</p>
      <p>`tailwind-token-curator` (Haiku) — keeps `globals.css` CSS variables and `tailwind.config.*` tokens in sync; rejects hardcoded colors.</p>
    </section>

    <section name="prompt-pattern">
      <p>Task: Add a confirmation dialog to &lt;feature&gt;.</p>
      <p>Constraints:</p>
      <list>
        <item>Use existing components/ui/dialog.tsx (do NOT modify).</item>
        <item>New file: components/&lt;feature&gt;/confirm-action-dialog.tsx.</item>
        <item>Compose Dialog + Button (variant="destructive") + Form.</item>
        <item>forwardRef + displayName required.</item>
        <item>All colors via design tokens (--destructive, --background); zero hex values.</item>
      </list>
      <p>Output: file content + 3-line variant-coverage note.</p>
    </section>

    <section name="best-practices">
      <list>
        <item>Pin shadcn registry version in `components.json` (`"$schema"` + lockfile commit) so an agent re-running `add` is deterministic.</item>
        <item>Treat `components/ui/*` as vendored code: tag the directory with a CODEOWNERS entry, gate via PR review.</item>
        <item>Co-locate Storybook stories with each ui primitive — agents validate variants visually before merging.</item>
        <item>Define design tokens in ONE place (`globals.css` `:root` + `.dark`); ban inline hex/`bg-[#...]` via an ESLint rule.</item>
        <item>Use `cva` `compoundVariants` for state combinations (`variant: "outline" + size: "sm"`) instead of new flat variants.</item>
        <item>Always export both the component and its `*Variants` CVA function — downstream code may want `cn(buttonVariants({ variant }))` for non-button elements.</item>
        <item>Add `displayName` immediately after `forwardRef`; otherwise React DevTools shows `_c2` and agent debugging breaks.</item>
      </list>
    </section>

    <section name="ai-agent-gotchas">
      <list>
        <item>Agents love to "improve" `components/ui/*` files — protect via lint rule (`eslint-plugin-boundaries`) or a glob-restricted hook; require a separate explicit task to edit them.</item>
        <item>Copy-paste drift: when the agent re-runs `shadcn add button`, it overwrites your local edits silently. Force diff review (`git diff --check` after add).</item>
        <item>Class-name explosion: agents generate huge `className={cn(...20-classes)}` strings; require them to factor into CVA when length > 6 utilities.</item>
        <item>`asChild` confusion: agents wrap `&lt;Slot&gt;` children in extra divs, which breaks Radix asChild semantics. Lint rule: `&lt;Slot&gt;` must have exactly one React child.</item>
        <item>Tailwind v4 vs v3: agent fetches outdated examples and writes v3 `hsl(var(--x))` while config is v4. Keep version in `AGENTS.md` for the dir.</item>
        <item>Dark-mode mishandling: agents add `dark:bg-gray-800` instead of using semantic tokens. Enforce: only `--background`, `--foreground`, etc.</item>
        <item>Human-in-loop checkpoint: any change touching `components/ui/*` or `globals.css` token block requires a human review — these are blast-radius high.</item>
      </list>
    </section>

    <templates>
      <template path="templates/scaffold.sh" purpose="Bulk-scaffold shadcn primitives and generate barrel export"/>
    </templates>

    <see-also>
      <link href="https://ui.shadcn.com/docs"/>
      <link href="https://ui.shadcn.com/docs/cli"/>
      <link href="https://ui.shadcn.com/docs/mcp"/>
      <link href="https://cva.style/docs"/>
      <link href="https://www.radix-ui.com/primitives"/>
      <link href="https://manupa.dev/blog/anatomy-of-shadcn-ui"/>
      <link href="https://tailwindcss.com/docs/upgrade-guide"/>
    </see-also>
  </content>
</methodology>
