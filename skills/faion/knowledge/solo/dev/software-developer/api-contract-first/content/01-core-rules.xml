<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="api-contract-first Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for api-contract-first. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Each endpoint life-cycle requires human approval at the spec stage (design review gate) before code generation begins.</statement>
      <rationale source="v1-source">Agents can produce technically valid specs that are semantically poor. Human-in-loop ensures the contract aligns with product intent before consuming teams depend on stubs.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>The OpenAPI spec is the source of truth. Implementation must conform to the spec; the spec is never updated to match a drifted implementation.</statement>
      <rationale source="v1-source">Allowing spec-to-implementation drift invalidates generated clients and documentation. Once consumers rely on the spec, breaking it silently causes integration failures.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Treat spec changes that break existing responses (remove fields, change types) as breaking changes requiring a version bump.</statement>
      <rationale source="v1-source">Additive changes (new optional fields) are backwards-compatible. Removals and type changes break existing clients silently.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Run Spectral lint on every PR that touches the spec file; fail the build on errors.</statement>
      <rationale source="v1-source">Spectral catches missing operationIds, undocumented error responses, and naming violations before review — issues that are cheap to fix early and expensive later.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Run contract tests (openapi-core response validation) on every commit; do not merge if responses diverge from the spec.</statement>
      <rationale source="v1-source">Contract tests are the only automated check that the running implementation still matches the published spec.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Generate server stubs and client SDKs from the spec using openapi-generator; regenerate on every spec change.</statement>
      <rationale source="v1-source">Manual synchronization between spec and generated code always drifts. Automated regeneration is the only reliable approach.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Enforce at minimum: operation-operationId (error), oas3-schema (error), operation-description (warn).</statement>
      <rationale source="v1-source">Missing operationIds break SDK generation. Schema errors break client validation. Missing descriptions degrade documentation quality.</rationale>
    </rule>
  </section>
</text>
