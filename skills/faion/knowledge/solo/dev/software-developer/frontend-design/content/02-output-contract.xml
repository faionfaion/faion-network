<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Frontend Design Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema for the artefact this methodology produces. The validator script enforces this contract before the artefact is accepted.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/frontend-design.json",
  "type": "object",
  "required": ["spec_id", "variant_count", "chosen_variant", "tokens_defined", "storybook_version_pinned", "components_planned"],
  "properties": {
    "spec_id": {"type": "string", "minLength": 3},
    "variant_count": {"type": "integer", "minimum": 3, "maximum": 5},
    "chosen_variant": {"type": "integer", "minimum": 1, "maximum": 5},
    "tokens_defined": {"type": "boolean"},
    "storybook_version_pinned": {"type": "boolean"},
    "components_planned": {"type": "integer", "minimum": 1},
    "validated_at": {"type": "string", "format": "date-time"}
  }
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "spec_id": "acme-dashboard-2026",
  "variant_count": 4,
  "chosen_variant": 2,
  "tokens_defined": true,
  "storybook_version_pinned": true,
  "components_planned": 32,
  "validated_at": "2026-05-23T10:00:00Z"
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "spec_id": "x",
  "variant_count": 2,
  "tokens_defined": false
}
]]></code>
    <note>spec_id too short, variant_count below minimum, missing chosen_variant + storybook flag + components_planned.</note>
  </example>

  <output-contract>
    <forbidden>
      <pattern id="f1">Variant set with &lt;3 or &gt;5 entries</pattern>
      <pattern id="f2">Variant selection without screenshot artefacts</pattern>
      <pattern id="f3">Components defined before tokens (token system empty)</pattern>
      <pattern id="f4">Storybook deps with semver ranges instead of pins</pattern>
    </forbidden>
    <allowed-transformations>
      <transformation>Sort variants deterministically by id.</transformation>
      <transformation>Default tokens_defined to false when omitted (forces explicit confirmation).</transformation>
    </allowed-transformations>
  </output-contract>

  <reference path="scripts/validate-frontend-design.py">Validator script</reference>
</text>
