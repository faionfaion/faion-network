<?xml version="1.0" encoding="UTF-8"?>
<text id="01-workflow" title="Frontend Design Workflow">
  <summary>Five-phase workflow for solopreneur UI projects: requirements, variant brainstorm, selection, Storybook scaffold, component delivery.</summary>

  <section title="Phase 1 — Requirements">
    <rule>
      <statement>Fix type (Landing/Dashboard/Form/Components), style (Minimalist/Bold/Corporate/Playful), and tech stack before any design work begins.</statement>
      <rationale>Unconstrained brainstorming produces trendy noise. Three locked parameters cut the variant space to a tractable size.</rationale>
    </rule>
  </section>

  <section title="Phase 2 — Brainstorm (3-5 Variants)">
    <rule>
      <statement>Each variant must differ in (a) primary navigation pattern, (b) hero composition, and (c) information density. Variants that differ only in color or typography are rejected.</statement>
      <rationale>Without a distinctness contract, agents produce five minor variations of the same layout. The three-axis rule forces genuine divergence.</rationale>
    </rule>
    <rule>
      <statement>Each variant ships as a runnable HTML/JSX file in designs/variant-N-name/ with inline design tokens and a rationale.md explaining its unique thesis.</statement>
      <rationale>Rationale files kill lookalikes by forcing justification; runnable files enable screenshot rendering rather than vibes-based review.</rationale>
    </rule>
    <rule>
      <statement>Cap variants at 5. Run Playwright screenshots at 1440x900 and 375x812 after each. Run an a11y audit (pa11y / axe-core) on every variant; reject any failing WCAG AA (contrast ratio &lt; 4.5:1, missing focus rings).</statement>
      <rationale>More than 5 options causes decision paralysis. A11y gate prevents shipping inaccessible variants to the selection step.</rationale>
    </rule>
    <example type="good">
      <note>Prompt that enforces distinctness</note>
      <code lang="text"><![CDATA[
Brief: <copy from .product/brief.md>. Tech: React+TS+Tailwind. Style: minimalist.
Generate 5 DISTINCT layout variants. Each must differ in:
(a) primary navigation pattern, (b) hero composition, (c) information density.
Output to designs/variant-{1..5}-<name>/index.html with inline tokens.
After each, run Playwright screenshot at 1440x900 + 375x812.
Show me screenshot grid. Do not iterate yet.
      ]]></code>
    </example>
  </section>

  <section title="Phase 3 — Selection and Refinement">
    <rule>
      <statement>Variant selection is a human decision, not an agent decision. The agent ranks variants by a11y and brand-voice match, but the human picks.</statement>
      <rationale>Design choice is taste, not logic. Agent rankings serve as inputs, not verdicts.</rationale>
    </rule>
  </section>

  <section title="Phase 4 — Storybook and Tokens">
    <rule>
      <statement>Define design tokens (colors, typography, spacing) before generating any component. Tokens must live in a single source file (tokens.json or tokens/ folder in W3C design-tokens format) compiled by style-dictionary to CSS vars, JS, and any other target.</statement>
      <rationale>Token-first prevents components from hardcoding hex values. Post-hoc token extraction is painful and produces drift between Figma, CSS, and Storybook.</rationale>
    </rule>
    <rule>
      <statement>Pin Storybook to a specific major version and use CSF3 format for all stories. Do not upgrade Storybook mid-project without a dedicated migration task.</statement>
      <rationale>Storybook 6/7/8 differ substantially; agents pull stale tutorials causing silent CSS isolation breakage.</rationale>
    </rule>
  </section>

  <section title="Phase 5 — Components">
    <rule>
      <statement>Colocate every component with its test, story, styles, and index: Component/, Component/Component.tsx, Component/Component.test.tsx, Component/Component.stories.tsx, Component/Component.module.css (if CSS Modules), Component/index.ts.</statement>
      <rationale>Colocation makes renames mechanical and keeps context local. Scattered files are the #1 cause of stale imports after refactors.</rationale>
    </rule>
    <rule>
      <statement>Maintain an explicit components-needed.md list. Refuse any component the agent proposes that is not on the list.</statement>
      <rationale>Agents implement 30 components when 8 are needed. Scope creep from agents is fast and silent.</rationale>
    </rule>
    <reference path="scripts/render-variants.sh">Render all design variants to PNG grid for review</reference>
  </section>
</text>
