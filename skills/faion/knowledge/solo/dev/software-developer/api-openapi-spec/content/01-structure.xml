<?xml version="1.0" encoding="UTF-8"?>
<text id="01-structure" title="OpenAPI 3.1 Document Structure">
  <summary>Top-level OpenAPI 3.1 elements: info, servers, paths, security, and tags with required field rules.</summary>

  <section title="Required Fields">
    <rule>
      <statement>Every path operation must have operationId, tags, a request body schema (for POST/PUT/PATCH), and all likely response codes including at least one 4xx via $ref to components/responses.</statement>
      <rationale>Missing operationId breaks codegen method names; missing 4xx responses cause generated SDK clients to have no error types.</rationale>
    </rule>
    <example type="good">
      <code lang="yaml"><![CDATA[
openapi: 3.1.0
info:
  title: User Management API
  version: 1.0.0
  contact:
    name: API Support
    email: api@example.com

servers:
  - url: https://api.example.com/v1
    description: Production
  - url: https://staging-api.example.com/v1
    description: Staging

paths:
  /users:
    post:
      summary: Create a new user
      operationId: createUser
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

security:
  - bearerAuth: []

tags:
  - name: Users
    description: User management operations
]]></code>
    </example>
    <reference path="templates/openapi-user-api.yaml">Full OpenAPI 3.1 example with users CRUD, pagination, errors, bearer auth</reference>
  </section>

  <section title="Security Schemes">
    <rule>
      <statement>Define security schemes in components/securitySchemes and apply them at the root level with per-operation overrides; never repeat auth logic inline in each operation.</statement>
      <rationale>Root-level security provides a safe default; operations that are genuinely public can override with security: [] explicitly, making public endpoints visible.</rationale>
    </rule>
    <example type="good">
      <code lang="yaml"><![CDATA[
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
]]></code>
    </example>
  </section>
</text>
