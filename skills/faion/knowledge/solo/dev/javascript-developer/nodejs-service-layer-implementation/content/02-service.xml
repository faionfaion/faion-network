<?xml version="1.0" encoding="UTF-8"?>
<text id="02-service" title="Service Layer: Business Rules and Error Handling">
  <summary>Defines the UserService pattern: interface, constructor injection, business rule implementation, and domain error usage.</summary>

  <section title="Service Rules">
    <rule>
      <statement>Hash passwords in the service layer, never in the controller or repository. The controller does not know about hashing; the repository does not know about passwords as a concept.</statement>
      <rationale>Password hashing is a business rule ("before storing a user, hash their password"). Business rules belong in services. Controllers handle HTTP; repositories handle data storage. Hashing in either layer is a layer violation.</rationale>
    </rule>
    <rule>
      <statement>Throw domain errors from service methods: NotFoundError when a resource is not found, ConflictError on uniqueness violations. Never return null or undefined from a service method that expects a result — throw instead.</statement>
      <rationale>Domain errors carry HTTP status codes and error codes. Controllers catch them and delegate to the error handler. Returning null forces every caller to check for null, distributing the error-handling logic across all callers.</rationale>
    </rule>
    <rule>
      <statement>Verify entity existence before update or delete. Call this.getUser(id) at the top of updateUser and deleteUser. If the entity does not exist, throw NotFoundError immediately.</statement>
      <rationale>Prisma's update and delete throw a Prisma-specific error on missing records, not a domain NotFoundError. Normalizing to domain errors in the service layer keeps the error contract consistent regardless of ORM.</rationale>
    </rule>
  </section>

  <example type="good">
    <code lang="typescript"><![CDATA[
export class UserService implements IUserService {
  constructor(private userRepository: IUserRepository) {}

  async createUser(data: CreateUserDto): Promise<User> {
    const existing = await this.userRepository.findByEmail(data.email);
    if (existing) throw new ConflictError('User with this email already exists');

    const hashedPassword = await hashPassword(data.password);
    return this.userRepository.create({
      email: data.email,
      name: data.name,
      hashedPassword,
    });
  }

  async updateUser(id: string, data: UpdateUserDto): Promise<User> {
    await this.getUser(id); // throws NotFoundError if missing
    if (data.email) {
      const existing = await this.userRepository.findByEmail(data.email);
      if (existing && existing.id !== id) throw new ConflictError('Email already in use');
    }
    return this.userRepository.update(id, data);
  }
}
    ]]></code>
  </example>

  <reference path="templates/user-service.ts">Full IUserService interface + UserService implementation</reference>
  <reference path="templates/errors.ts">Domain error hierarchy: AppError, NotFoundError, ConflictError, UnauthorizedError</reference>
</text>
