<?xml version="1.0" encoding="UTF-8"?>
<text id="03-owasp-and-api-security" title="OWASP Top 10 2025 and API Security Top 10 2023">
  <summary>Current OWASP Web and API risk rankings with test approaches for auth and authorization vulnerabilities.</summary>

  <section title="OWASP Top 10 2025 Key Entries">
    <list>
      <item>A01 Broken Access Control — #1 risk; includes SSRF consolidated from 2021. Test authorization at every function level.</item>
      <item>A02 Security Misconfiguration — moved up from #5; debug modes, default credentials, unnecessary features enabled.</item>
      <item>A03 Software Supply Chain Failures — NEW; extends beyond dependencies to build systems and distribution infrastructure.</item>
      <item>A05 Injection — SQL, NoSQL, OS command, LDAP, XSS. Use parameterized queries; never concatenate user input.</item>
      <item>A07 Authentication Failures — weak passwords, session management, credential stuffing.</item>
      <item>A10 Mishandling Exceptional Conditions — NEW; improper error handling, failing open, logical errors.</item>
    </list>
  </section>

  <section title="OWASP API Security Top 10 2023">
    <list>
      <item>API1 Broken Object Level Authorization (BOLA) — access other users' objects by changing IDs in requests.</item>
      <item>API2 Broken Authentication — token manipulation, session attacks.</item>
      <item>API3 Broken Object Property Level Authorization (BOPLA) — access restricted properties via mass assignment.</item>
      <item>API4 Unrestricted Resource Consumption — rate limiting, payload size limits.</item>
      <item>API5 Broken Function Level Authorization — admin endpoint access, privilege escalation.</item>
      <item>API7 Server Side Request Forgery — internal network access via API parameters.</item>
      <item>API9 Improper Inventory Management — shadow APIs, undocumented endpoints.</item>
    </list>
    <rule>
      <statement>BOLA (API1) and BOPLA (API3) require human review — scanners cannot detect them because the vulnerability is in business logic, not code patterns.</statement>
      <rationale>Automated tools look for code signatures. BOLA exploits are valid HTTP requests using different IDs — indistinguishable from legitimate traffic to a scanner.</rationale>
    </rule>
  </section>

  <section title="Auth and Authorization Test Cases">
    <rule>
      <statement>For every authenticated endpoint, test: correct credentials (200), missing Authorization header (401), expired token (401), insufficient role (403), and another user's resource ID (403 or 404 — must not be 200).</statement>
      <rationale>Horizontal privilege escalation (user A accessing user B's data) is the #1 API bug class and requires explicit per-endpoint testing.</rationale>
    </rule>
    <example type="good">
      <note>Injection test matrix — minimum payloads to probe per input field.</note>
      <code lang="text"><![CDATA[
SQL:     ' OR '1'='1    ;DROP TABLE users--
NoSQL:   {"$gt": ""}    {"$ne": null}
Command: ; ls           | cat /etc/passwd
XSS:     <script>alert(1)</script>
Path:    ../../../etc/passwd
XXE:     <!DOCTYPE x [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>
]]></code>
    </example>
  </section>
</text>
