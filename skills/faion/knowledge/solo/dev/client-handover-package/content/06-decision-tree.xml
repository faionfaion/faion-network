<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Client Handover Package Decision Tree" version="1.0.0" est_tokens="500" depth="essential">
  <summary>Routes handover by section completeness → secrets transferred → successor named → dual sign-off. Branches name observable artefact features.</summary>

  <decision-tree>
    <root-question id="q-prereqs">Are prerequisites satisfied: engagement closing, runbook draft + open-items list available?</root-question>

    <branch when="prereqs_met == false">
      <conclusion ref="skip-this-methodology" verdict="abort"/>
    </branch>

    <branch when="prereqs_met == true">
      <question id="q-sections">Are all 7 sections present and each above minLength?</question>

      <branch when="sections_incomplete == true">
        <conclusion ref="r1-seven-sections" verdict="block-missing-sections"/>
      </branch>

      <branch when="sections_incomplete == false">
        <question id="q-secrets">For each credential, are rotation_date AND acknowledged_by populated?</question>

        <branch when="secrets_incomplete == true">
          <conclusion ref="r2-credential-transfer" verdict="block-secrets-not-transferred"/>
        </branch>

        <branch when="secrets_incomplete == false">
          <question id="q-successor">Is successor_email a named human (not team alias)?</question>

          <branch when="successor_team_alias == true">
            <conclusion ref="r4-named-successor" verdict="block-no-successor"/>
          </branch>

          <branch when="successor_team_alias == false">
            <question id="q-signoff">Are both consultant + client signed?</question>

            <branch when="signoff_partial == true">
              <conclusion ref="r5-dual-signoff" verdict="block-no-signoff"/>
            </branch>

            <branch when="signoff_partial == false">
              <conclusion ref="run-the-checklist" verdict="archive-and-close"/>
            </branch>
          </branch>
        </branch>
      </branch>
    </branch>
  </decision-tree>

  <example trace="happy-path">
    prereqs_met=true → 7 sections complete → 1 credential rotated+ack → successor named → dual signoff → verdict=archive-and-close (ref run-the-checklist).
  </example>
</text>
