<?xml version="1.0" encoding="UTF-8"?>
<text id="01-rules" title="Design Tokens: Implementation Rules">
  <summary>Mandatory rules for a reliable token pipeline: layer hierarchy, mode files, DTCG vs Tokens Studio, CI validation.</summary>

  <section title="Three-Layer Hierarchy">
    <rule>
      <statement>Organise tokens in three layers: primitives (raw hex/rem values), semantic (intent-named aliases like bg.surface.default), and component (component-scoped like button.primary.bg). App code must consume only semantic and component layers.</statement>
      <rationale>Consuming primitives directly couples components to raw values. When a brand color changes, only the primitive file and semantic aliases need updating — not every component.</rationale>
    </rule>
  </section>

  <section title="Mode Files">
    <rule>
      <statement>Store each mode (light, dark, high-contrast, brand) in a separate file (tokens/modes/light.json, dark.json). Never inline multi-mode values as {light: ..., dark: ...} inside a single token entry.</statement>
      <rationale>Inlined modes drift: updating one side silently breaks the other's contrast pair. Separate files make mode changes auditable in PR diffs.</rationale>
    </rule>
  </section>

  <section title="Generated Artifacts">
    <rule>
      <statement>Generated files (CSS, JS, XML, Swift) must be in dist/ and listed in .gitignore unless this package IS the published artifact. Rebuild in CI; never hand-edit generated outputs.</statement>
      <rationale>Agents that commit generated CSS alongside source JSON create dirty repo state on the next build, breaking CI.</rationale>
    </rule>
  </section>

  <section title="Validation">
    <rule>
      <statement>Validate token JSON against a schema in CI before running the build. Catch broken alias references ({color.brand.500} cycles) before they produce cryptic build errors.</statement>
      <rationale>Style Dictionary silently drops or errors on unresolvable references. A JSON schema check fails fast with a readable message.</rationale>
    </rule>
  </section>

  <section title="DTCG vs Tokens Studio">
    <rule>
      <statement>For new projects, use the W3C DTCG format ($value, $type). For Tokens Studio exports, add @tokens-studio/sd-transforms to the Style Dictionary pipeline; without it, the build silently drops styles.</statement>
      <rationale>Tokens Studio uses value and type (no $ prefix). Missing the transform adapter produces empty CSS with no error.</rationale>
    </rule>
  </section>

  <section title="Style Dictionary Version">
    <rule>
      <statement>Pin the Style Dictionary major version explicitly. v3 uses formatter: function({dictionary, file}); v4 uses format: ({dictionary, options}) =&gt; string. Do not mix APIs.</statement>
      <rationale>Agents trained on v3 examples emit broken v4 configs. The version mismatch produces silent build failures.</rationale>
    </rule>
  </section>

  <reference path="templates/style-dictionary.config.js">Style Dictionary v3 config emitting CSS, SCSS, JS, TS outputs.</reference>
  <reference path="templates/tokens-semantic.json">Semantic token layer example with alias references.</reference>
</text>
