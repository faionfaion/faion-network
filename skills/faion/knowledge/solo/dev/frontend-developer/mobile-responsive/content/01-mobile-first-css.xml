<?xml version="1.0" encoding="UTF-8"?>
<text id="01-mobile-first-css" title="Mobile-First CSS and Fluid Sizing">
  <summary>Base styles are unconditional (mobile); min-width queries layer up for larger screens. clamp() replaces stepwise media queries for typography and spacing. srcset + sizes for responsive images.</summary>

  <section title="Mobile-First Order">
    <rule>
      <statement>Write base (mobile) styles without any media query. Add min-width queries to layer desktop enhancements. Never use max-width first.</statement>
      <rationale>Desktop-first CSS leaks desktop assumptions into the base; every mobile breakpoint becomes an override that accumulates complexity.</rationale>
    </rule>
    <example type="good">
      <code lang="css"><![CDATA[
/* Base: mobile, no query */
.grid { display: grid; gap: 1rem; grid-template-columns: 1fr; }

/* Tablet enhancement */
@media (min-width: 768px) { .grid { grid-template-columns: repeat(2, 1fr); } }

/* Desktop enhancement */
@media (min-width: 1024px) { .grid { grid-template-columns: repeat(3, 1fr); } }
      ]]></code>
    </example>
  </section>

  <section title="Fluid Typography with clamp()">
    <rule>
      <statement>Use CSS clamp(min, fluid, max) for typography and spacing instead of breakpoint steps. This eliminates 30-50% of media queries.</statement>
      <rationale>clamp() produces a smooth scale between the min and max without discrete jumps; it works on any viewport without additional breakpoints.</rationale>
    </rule>
    <example type="good">
      <code lang="css"><![CDATA[
:root {
  --font-size-base: clamp(1rem, 0.9rem + 0.5vw, 1.125rem);
  --font-size-xl:   clamp(1.25rem, 1rem + 1.25vw, 1.5rem);
  --space-md:       clamp(1rem, 0.8rem + 1vw, 1.5rem);
  --space-lg:       clamp(1.5rem, 1rem + 2.5vw, 2.5rem);
}
      ]]></code>
    </example>
  </section>

  <section title="Viewport Meta">
    <rule>
      <statement>Always set &lt;meta name="viewport" content="width=device-width, initial-scale=1"&gt;. Never add maximum-scale=1 or user-scalable=no — this violates WCAG 1.4.4.</statement>
      <rationale>user-scalable=no prevents users with low vision from zooming the page; it silently breaks a11y compliance.</rationale>
    </rule>
  </section>

  <section title="Responsive Images">
    <rule>
      <statement>Provide srcset with multiple widths and a sizes attribute that matches the actual CSS layout. Never set sizes="100vw" for multi-column layouts.</statement>
      <rationale>Wrong sizes="100vw" on a 3-column layout causes the browser to download the largest image at every viewport, wasting mobile bandwidth.</rationale>
    </rule>
  </section>
</text>
