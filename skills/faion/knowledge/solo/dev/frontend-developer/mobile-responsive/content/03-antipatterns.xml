<?xml version="1.0" encoding="UTF-8"?>
<text id="03-antipatterns" title="Mobile Responsive Anti-patterns">
  <summary>Failures agents introduce when making a layout responsive: desktop-first overrides, Bootstrap breakpoints, missing container-type, flex-wrap everywhere.</summary>

  <antipattern>
    <description>Desktop-first CSS with max-width media queries as the primary pattern.</description>
    <reason>max-width-first requires complex overrides for every mobile breakpoint; each override adds specificity that defeats future fixes.</reason>
  </antipattern>

  <antipattern>
    <description>Lifting breakpoint values from Bootstrap (576/768/992/1200px) without checking the project's design tokens.</description>
    <reason>Bootstrap widths are device-sized; content-driven breakpoints (when the layout actually breaks) produce simpler CSS and fewer edge cases.</reason>
  </antipattern>

  <antipattern>
    <description>Adding container-query rules without container-type: inline-size on the parent element.</description>
    <reason>Without container-type, @container rules are parsed but never match; agents check "rendered without error" and consider the task complete.</reason>
  </antipattern>

  <antipattern>
    <description>Adding flex-wrap: wrap to every flex container when asked to "make it responsive".</description>
    <reason>flex-wrap on every container looks correct on phones and produces broken desktop layouts where items wrap prematurely.</reason>
  </antipattern>

  <antipattern>
    <description>Setting meta name="viewport" content="user-scalable=no" or maximum-scale=1.</description>
    <reason>These attributes prevent zoom for users with low vision and fail WCAG 1.4.4. They are copied from legacy templates and are a silent a11y violation.</reason>
  </antipattern>

  <antipattern>
    <description>Converting vh to dvh without keeping the vh fallback line.</description>
    <reason>Older browsers (Safari &lt;15.4, Chrome &lt;108) do not support dvh; removing the vh fallback breaks full-height layouts on those browsers.</reason>
  </antipattern>
</text>
