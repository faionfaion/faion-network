<?xml version="1.0" encoding="UTF-8"?>
<text id="02-container-queries" title="Container Queries, Touch Targets, and iOS Fixes">
  <summary>Container queries decouple component layout from viewport size. Touch targets must be 44x44 CSS px. iOS Safari needs 100dvh instead of 100vh.</summary>

  <section title="Container Queries">
    <rule>
      <statement>Set container-type: inline-size on the parent element before writing @container rules. Without it, queries silently never match.</statement>
      <rationale>Agents regularly write @container rules without the container-type declaration, mark the task done, and the styles never apply.</rationale>
    </rule>
    <example type="good">
      <code lang="css"><![CDATA[
.card-wrapper {
  container-type: inline-size;
  container-name: card;
}

/* Respond to container width, not viewport */
@container card (min-width: 400px) {
  .card { grid-template-columns: 150px 1fr; }
}
@container card (min-width: 600px) {
  .card { grid-template-columns: 200px 1fr auto; }
}
      ]]></code>
    </example>
  </section>

  <section title="Touch Targets">
    <rule>
      <statement>Every interactive element (button, link, icon button) must have a minimum touch target of 44x44 CSS px per WCAG 2.5.8.</statement>
      <rationale>Icon buttons with padding: 4px are visually compact but inaccessible on touch devices; this is the most common a11y regression on mobile.</rationale>
    </rule>
    <example type="good">
      <code lang="css"><![CDATA[
button, a, [role="button"], input[type="button"] {
  min-height: 44px;
  min-width: 44px;
}
      ]]></code>
    </example>
  </section>

  <section title="iOS Safari dvh Fix">
    <rule>
      <statement>Use 100dvh for full-height layouts with a 100vh fallback. iOS Safari's 100vh includes the URL bar, causing layout jumps when it hides.</statement>
      <rationale>dvh accounts for the dynamic viewport height; older browsers fall back to vh without any styling breakage.</rationale>
    </rule>
    <example type="good">
      <code lang="css"><![CDATA[
.full-screen {
  min-height: 100vh;   /* fallback */
  min-height: 100dvh;  /* iOS-safe */
}
      ]]></code>
    </example>
    <reference path="templates/playwright-devices.ts">Playwright device config for mobile/tablet/desktop testing</reference>
  </section>
</text>
