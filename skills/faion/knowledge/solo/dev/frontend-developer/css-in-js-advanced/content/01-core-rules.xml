<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="css-in-js-advanced Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for css-in-js-advanced. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Always set defaultVariants in a recipe() definition. When a consumer calls button({ color: undefined }), vanilla-extract must have a fallback variant.</statement>
      <rationale source="v1-source">Omitting defaultVariants causes runtime errors on undefined variant slots. The recipe type cannot be narrowed at call time if variants are optional.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Keep Sprinkles properties narrowly scoped. Every added property expands CSS by multiplying its value count by the condition count. Over-broad arrays explode CSS by 5-10x.</statement>
      <rationale source="v1-source">The resulting stylesheet causes measurable parse-time LCP regressions on mobile. A 500kb stylesheet can delay first paint by 200-500ms on 3G networks.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>vanilla-extract runs .css.ts files in a restricted Node sandbox at build time. Do not import app-level modules that reference browser globals or environment checks.</statement>
      <rationale source="v1-source">Importing app code that uses window or process.env crashes the build with cryptic "cannot find module" errors. The sandbox has no browser APIs and cannot evaluate async imports.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>styled-components v6 without the registry produces hydration mismatches because CSS is injected client-side after the server-rendered HTML is painted.</statement>
      <rationale source="v1-source">The browser paints unstyled HTML (Flash of Unstyled Content) before JS runs and applies styles. The registry injects style tags into the server response, preventing FOUC.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>FOUC is only visible on slow networks; it cannot be caught in development but fails user perception on first paint in production. Always write a Playwright test that confirms styles are in the initial HTML.</statement>
      <rationale source="v1-source">Developers with fast networks and cached assets never see FOUC locally. It only appears on first visit over 3G. Tests catch this before production.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Emotion runtime requires a client context; without the CacheProvider wrapper in App Router, the css prop silently degrades to a plain style attribute and FOUC occurs.</statement>
      <rationale source="v1-source">Emotion's CSS-in-JS machinery requires a React context to collect styles. App Router's default server behavior cannot use that context; the wrapper bridges it.</rationale>
    </rule>
  </section>
</text>
