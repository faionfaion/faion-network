<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="DX Friction Log Format Worked Example" version="1.1.0" est_tokens="700" depth="essential">
  <summary>One worked end-to-end example showing a DX Friction Log Format artefact filled from a realistic input bundle. Trace runs from input brief → evidence gathering → schema fill → validator pass → review surfacing.</summary>

  <section title="Scenario">
    <p>Solo founder logs 18 friction rows in 2 weeks. Triage shows 'vitest config' surfaces 6 papercuts; one 90-minute config-cleanup PR removes them all, surfacing ~30min/week savings.</p>
  </section>

  <section title="Trace">
    <list>
      <item>Step 1 — prerequisites_ok=true; all input refs reachable.</item>
      <item>Step 2 — evidence array populated with ≥1 citation per non-trivial field.</item>
      <item>Step 3 — draft artefact written; status=draft.</item>
      <item>Step 4 — validator script exits 0; no violations.</item>
      <item>Step 5 — status=ready_for_review; named reviewer notified.</item>
    </list>
  </section>

  <section title="Outcome">
    <p>The artefact is auditable by a downstream reviewer without re-deriving the reasoning. Every non-trivial field traces back to an evidence row; rejected fields surface as structured skip-with-reason instead of polluting the library with degraded output.</p>
  </section>
</text>
