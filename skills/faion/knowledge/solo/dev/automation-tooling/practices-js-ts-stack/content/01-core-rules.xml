<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="practices-js-ts-stack Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for practices-js-ts-stack. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>React components MUST define an explicit TypeScript interface for props (`interface ButtonProps { ... }`), use `React.FC&lt;ButtonProps&gt;` or direct destructuring, and be placed in `src/components/{Name}/{Name}.tsx`. The component file MUST NOT contain inline styles longer than a class reference.</statement>
      <rationale source="v1-source">Explicit prop interfaces enable TypeScript strict-mode checking, autocomplete in IDEs, and documentation in Storybook. Consistent file placement makes agents find existing components before creating duplicates.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Shared async data-fetching logic MUST be extracted into a custom hook in `hooks/use{Name}.ts`. The hook MUST return `{ data, loading, error }`. Components MUST NOT contain raw `useEffect` + `useState` for fetching — use the hook abstraction.</statement>
      <rationale source="v1-source">Duplicated useEffect fetch patterns across components are the most common React code smell. Custom hooks allow centralized caching, error handling, and retry logic. TanStack Query replaces this pattern for server-state caching.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>All TypeScript projects MUST enable `"strict": true` in tsconfig.json, which includes `noImplicitAny`, `strictNullChecks`, `noUnusedLocals`, and `noUnusedParameters`. Type-only imports MUST use `import type { X }` syntax to keep bundle slim. No `any` type in production code.</statement>
      <rationale source="v1-source">TypeScript strict mode catches null pointer errors, implicit any, and unused variables at compile time. `import type` eliminates type-only symbols from the bundle and satisfies `verbatimModuleSyntax`. Agents default to non-strict configs and then add `any` casts as a workaround.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Node.js/TypeScript projects MUST follow this layout: `package.json` + `tsconfig.json` at root, `src/` for source (index.ts, routes/, services/, models/), `tests/` for test files (`*.test.ts`), `.env.example` committed but never `.env`.</statement>
      <rationale source="v1-source">Consistent layout prevents agents from scattering source files at the project root or placing tests alongside source without a clear boundary.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Next.js 13+ projects MUST use the App Router (`app/` directory). Route groups use `(name)/` syntax for layout isolation. API routes use `app/api/{resource}/route.ts`. Root layout in `app/layout.tsx` wraps all pages. Dashboard-level layout is `app/dashboard/layout.tsx`.</statement>
      <rationale source="v1-source">Next.js 15 is App Router-only. Pages Router (`pages/`) is deprecated. Agents that default to Pages Router patterns produce code that conflicts with the App Router's server component model and causes hydration errors.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Select state management by scope: `useState` for local component state; `useContext` for shared UI state (theme, auth); Zustand for medium-complexity cross-component state; Redux Toolkit only for large apps requiring time-travel debugging; TanStack Query for all server-state caching. Do NOT use Redux for server state — use TanStack Query.</statement>
      <rationale source="v1-source">Overengineering state is the most common frontend architecture mistake. Adding Redux to a project that needs two shared values doubles its complexity. TanStack Query handles cache invalidation, background refetching, and loading states that would require 200 lines of Redux boilerplate.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Select CSS approach by project context: CSS Modules for component isolation in React; Tailwind CSS for rapid prototyping and utility-first projects; styled-components for CSS-in-JS with dynamic styles; SCSS + BEM for large traditional projects. Do NOT mix approaches within one project.</statement>
      <rationale source="v1-source">Mixed CSS approaches cause naming collisions and specificity wars. Agents produce Tailwind utility classes inside CSS Modules files or vice versa when the project convention is not explicit.</rationale>
    </rule>
  </section>
</text>
