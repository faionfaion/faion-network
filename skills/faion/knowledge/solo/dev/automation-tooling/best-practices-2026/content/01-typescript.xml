<?xml version="1.0" encoding="UTF-8"?>
<text id="01-typescript" title="TypeScript 5 Strict Configuration — 2026 Baseline">
  <summary>Recommended TS 5 strict flags, module resolution settings, and advanced type patterns for 2026.</summary>

  <section title="Strict Flags">
    <rule>
      <statement>Enable strict, noUncheckedIndexedAccess, noImplicitOverride, exactOptionalPropertyTypes, and verbatimModuleSyntax in new projects; migrate legacy code under per-file // @ts-strict-check before flipping the global flag.</statement>
      <rationale>Flipping global strict in an existing codebase without budget creates hundreds of errors at once. Per-file gradual adoption lets the team absorb changes incrementally.</rationale>
    </rule>
    <reference path="templates/tsconfig-strict-2026.json">Full TS 5 strict baseline with all 2026 recommended flags.</reference>
    <example type="good">
      <code lang="json"><![CDATA[
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "noPropertyAccessFromIndexSignature": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "skipLibCheck": true
  }
}
      ]]></code>
    </example>
  </section>

  <section title="Module Resolution">
    <rule>
      <statement>Use moduleResolution: "bundler" for projects targeting Vite, Next.js, or tsup; use "node16" or "nodenext" for pure Node projects without a bundler.</statement>
      <rationale>"bundler" allows extensionless imports that bundlers resolve; "node16" requires .js extensions in import paths for native ESM compliance.</rationale>
    </rule>
  </section>

  <section title="verbatimModuleSyntax">
    <rule>
      <statement>With verbatimModuleSyntax enabled, always use import type for type-only imports; the compiler will error on value imports that are erased at runtime.</statement>
      <rationale>Prevents accidental inclusion of types-only modules in runtime bundles and makes the module graph explicit.</rationale>
    </rule>
    <example type="good">
      <code lang="typescript"><![CDATA[
import type { User } from './types';
import { createUser } from './factory'; // value import — fine
      ]]></code>
    </example>
  </section>
</text>
