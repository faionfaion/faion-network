<?xml version="1.0" encoding="UTF-8"?>
<text id="03-antipatterns" title="A/B Testing Anti-patterns">
  <summary>Peeking, multiple testing without correction, mid-run changes, and agent-specific failure modes.</summary>

  <antipattern>
    <description>Peeking: checking statistical significance before the pre-registered sample size is reached.</description>
    <reason>Each look at results inflates the false-positive rate. At alpha=0.05, 5 peeks give ~20% Type I error instead of 5%. Use a sequential testing library or fix a single analysis date.</reason>
  </antipattern>

  <antipattern>
    <description>Multiple testing: running 5+ secondary metrics each at alpha=0.05 and reporting any p&lt;0.05 as a win.</description>
    <reason>With 5 independent tests the familywise error rate is 1-(0.95^5) ≈ 23%. Apply Bonferroni correction or report only confidence intervals for secondary metrics.</reason>
  </antipattern>

  <antipattern>
    <description>Changing experiment mid-run: modifying allocation, variants, or primary metric after users are already bucketed.</description>
    <reason>Users bucketed under the old allocation are not comparable to users bucketed under the new one. Results are confounded and uninterpretable.</reason>
  </antipattern>

  <antipattern>
    <description>Ignoring sample-ratio mismatch (SRM): the observed variant split deviates from the configured allocation.</description>
    <reason>SRM is evidence of a bucketing bug or logging failure. Analysis on an SRM dataset produces biased lift estimates. Always run chi-square SRM check before analysis.</reason>
  </antipattern>

  <antipattern>
    <description>Agent confuses relative vs absolute MDE: "+10% lift on 6% baseline" is +0.6pp absolute, not +10pp.</description>
    <reason>Forces units explicitly in every prompt and formula call. Wrong MDE input produces a wildly incorrect sample-size estimate.</reason>
  </antipattern>
</text>
