<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="web-scraping-pagination Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for web-scraping-pagination. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>For next-button pagination, loop until the next button is absent or disabled. For infinite scroll, break when document.body.scrollHeight stops growing. For load-more buttons, break when the button is absent or not in the viewport. Always deduplicate collected items by index or hash.</statement>
      <rationale source="v1-source">Each termination condition maps to a different DOM state. Using the wrong check (e.g., "next button exists" when the button is disabled but still in DOM) produces infinite loops. Deduplication guards against the off-by-one overlap in infinite scroll.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Always close pages in a finally block after use. For concurrent URL lists, use a semaphore or p-limit to cap open pages. Implement a BrowserPool when launching multiple browser instances to avoid unbounded memory growth.</statement>
      <rationale source="v1-source">Each open Chromium page consumes 50–200 MB of RAM. A 100-URL batch without page management exhausts memory on any standard VPS within minutes.</rationale>
    </rule>
  </section>
</text>
