Generate a Playwright test in TypeScript for: <user flow description>.

Rules:
- Use page.getByRole / getByLabel / getByText. NO CSS selectors unless no semantic role exists.
- No page.waitForTimeout. Use auto-wait built into locator actions or expect(locator).toBeVisible().
- Save auth once via storageState in tests/global-setup.ts; reuse across all tests.
- Headless: true. Trace: 'on-first-retry'. Screenshot: 'only-on-failure'.
- One assertion per distinct behavior.
- Output file: tests/<flow-name>.spec.ts

If a role locator returns strict mode violation (multiple elements), narrow with { name: 'label' }.
Never broaden the selector as a fix — that silently tests the wrong element.
