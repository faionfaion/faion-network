<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="Logging Patterns Worked Example" version="1.0.0" est_tokens="700" depth="reference">
  <summary>End-to-end worked example applying the methodology to a representative input. Output matches the schema in content/02-output-contract.xml.</summary>
  <section title="Trace">
    <paragraph>Audit finds 90 printf logs + no request_id + tight-loop log spew → install JsonFormatter → middleware injects request_id → redaction patterns → sampling on the loop → log volume drops 80%, p95 query latency dashboards work.</paragraph>
  </section>
  <section title="Resulting artefact">
    <code lang="json"><![CDATA[
{
  "format": "json",
  "request_id": true,
  "redaction": {
    "enabled": true,
    "patterns": [
      "email",
      "phone",
      "card_like"
    ]
  },
  "perf_fields": [
    "duration_ms",
    "db_query_count",
    "cache_hit_count"
  ],
  "sink": "loki"
}
]]></code>
  </section>
  <section title="Decision path applied">
    <paragraph>The decision tree in content/06-decision-tree.xml routed this input to the recommended action. Each rule referenced by the conclusion is listed in content/01-core-rules.xml.</paragraph>
  </section>
</text>
