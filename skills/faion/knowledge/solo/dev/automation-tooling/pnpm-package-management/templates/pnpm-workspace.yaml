# pnpm-workspace.yaml — monorepo workspace declaration
# Place at repo root alongside root package.json and pnpm-lock.yaml

packages:
  - 'apps/*'       # application packages (web, api, mobile)
  - 'packages/*'   # shared packages (ui, types, config, utils)
  - 'tools/*'      # build tools and scripts
