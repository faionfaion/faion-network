<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="practices-frontend-components Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for practices-frontend-components. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Storybook stories MUST use CSF3 format: `Meta&lt;typeof Component&gt;` as the default export type, `StoryObj&lt;typeof Component&gt;` for each story, and be co-located as `{Name}.stories.tsx` in the component folder. Stories MUST define `args` for every required prop to be usable in the Storybook controls panel.</statement>
      <rationale source="v1-source">CSF3 provides TypeScript inference for controls and actions. CSF2 (using `Template.bind({})`) is deprecated in Storybook v7+. Co-located stories make it impossible to forget to update stories when the component's props change.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Design values MUST be defined as TypeScript constants in a `tokens/` directory, never as magic hex strings inline. Colors use a palette object with numeric scales (50-900) and a semantic layer (success, error, warning). Spacing uses a numeric key with rem values. Export from a single `index.ts`.</statement>
      <rationale source="v1-source">Magic hex strings scattered across component files cause color drift — small variants of the same color appear across the codebase. TypeScript token objects provide autocomplete, make global color changes O(1), and are consumable by CSS-in-JS, Tailwind config, and design tools via style-dictionary.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Each component in a shared library MUST be in its own folder `src/components/{Name}/` containing exactly: `{Name}.tsx` (component), `{Name}.stories.tsx` (Storybook), `{Name}.test.tsx` (tests), `{Name}.module.css` (scoped styles, if CSS Modules is the project convention), and `index.ts` (barrel export). No other files.</statement>
      <rationale source="v1-source">Co-located test and story files ensure they are updated when the component changes. The barrel `index.ts` keeps import paths clean (`from '@/components/Button'` instead of `from '@/components/Button/Button'`). Scattered files lead to orphaned stories with no corresponding component.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Every project folder that contains source code MUST have a CLAUDE.md (or AGENTS.md) following the 100-150 line skeleton: H1 folder name, one-sentence description, Overview (2-3 sentences), Structure table (path → purpose), Key Concepts list, Entry Points list, Common Operations with bash examples, Dependencies list. Hard cap: 200 lines. Never exceed this — agents consistently over-generate.</statement>
      <rationale source="v1-source">CLAUDE.md is auto-loaded by Claude Code when entering a directory. If it is too short, agents lack context. If it is too long, agents waste tokens on non-essential content. The 100-150 line sweet spot gives enough routing context without consuming the working context window.</rationale>
    </rule>
  </section>
</text>
