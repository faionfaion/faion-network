<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="trunk-based-dev-principles Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for trunk-based-dev-principles. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Single Source of Truth: One main branch ("trunk" / "main"). All developers commit to trunk. Trunk is always releasable. No long-lived feature branches (max 1-2 days).</statement>
      <rationale source="v1-source">Everyone sees the latest code immediately. No merge hell. Simpler mental model.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Small, Frequent Commits: At least once per day. Smaller changes are easier to review. Each commit builds and tests pass. Ideally less than 200 lines changed per commit.</statement>
      <rationale source="v1-source">Small diffs are faster to review and merge. Easier to bisect when bugs appear. Smaller blast radius.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Trunk is Always Releasable: Enforced by automated tests (must pass to merge), feature flags (hide incomplete work), code review (short turnaround), and CI pipeline (fast feedback). If trunk breaks, stop everything, fix immediately, do root cause analysis, improve tests/gates.</statement>
      <rationale source="v1-source">A broken trunk blocks the entire team. Fast fixes and prevention are critical.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Continuous Integration: Every commit integrates with trunk within hours. CI pipeline runs tests, linting, type-checking, and deployment to staging. Feedback completes in under 10 minutes. Broken trunk auto-reverts and alerts the author.</statement>
      <rationale source="v1-source">Fast feedback sustains trust. Slow CI kills TBD adoption.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Trunk Breaks Too Often: Solution — strengthen CI gates. Require tests, linting, type-checking, and integration tests before merge. Auto-revert red trunk.</statement>
      <rationale source="v1-source">A broken trunk kills adoption. Fast, automated gates are non-negotiable.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Merges Cause Conflicts: Solution — keep branches shorter. Rebase frequently. Coordinate with teammates on overlapping files. CI should enforce linear history.</statement>
      <rationale source="v1-source">Long branches accumulate divergence. Short branches = fewer conflicts.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Code Review Takes Too Long: Solution — keep PRs small (under 200 lines). Enforce SLA (review within 4 hours). Rotate reviewers. Use pair programming for critical code.</statement>
      <rationale source="v1-source">Small PRs are reviewed in minutes. Large PRs block for days.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Developers Ignore Test Failures: Solution — fail the build immediately. Block merges on failing tests. Make failure visible (Slack notification, red build indicator). Enforce "trunk is always releasable".</statement>
      <rationale source="v1-source">Visible failure accountability prevents "I'll fix it later" behavior.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Feature Takes Too Long to Implement: Solution — use feature flags to hide incomplete work. Break the feature into smaller daily increments. Each increment = one commit = one merge.</statement>
      <rationale source="v1-source">Feature flags decouple deployment from release.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Fast Reviews Required: Review within 4 hours (ideally 1 hour). Small PRs enable fast reviews. Async review or pair on complex changes.</statement>
      <rationale source="v1-source">Slow reviews block merges and defeat TBD's speed advantage.</rationale>
    </rule>
    <rule id="r11" testable="true" depth="essential">
      <statement>Review Checklist: Tests pass, feature flag if needed, backward compatible, no breaking changes to trunk.</statement>
      <rationale source="v1-source">These checks prevent integration issues.</rationale>
    </rule>
    <rule id="r12" testable="true" depth="essential">
      <statement>Pair Programming Alternative: Write code together = no review needed. Pair = instant review. Especially good for complex, risky changes.</statement>
      <rationale source="v1-source">Synchronous code review via pairing is sometimes faster and higher-quality than async PR review.</rationale>
    </rule>
  </section>
</text>
