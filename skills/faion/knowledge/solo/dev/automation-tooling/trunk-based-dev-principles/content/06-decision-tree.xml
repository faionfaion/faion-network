<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Trunk-Based Dev Principles Decision Tree" version="1.0.0" est_tokens="500" depth="essential">
  <summary>Routes a TBD-readiness assessment by per-principle pass/fail to verdict adopt / partial-adopt / not-ready. Branches name observable scores; leaves reference rules.</summary>

  <decision-tree>
    <root-question id="q-prereqs">Are the prerequisites satisfied: repo handle, CI config, branching policy artefact accessible?</root-question>

    <branch when="prereqs_met == false">
      <conclusion ref="skip-this-methodology" verdict="abort"/>
    </branch>

    <branch when="prereqs_met == true">
      <question id="q-single-trunk">Is principle single_trunk score ≥ 15 / 25 (no &gt;2-day branches AND BRANCHING.md present)?</question>

      <branch when="single_trunk_score &lt; 15">
        <conclusion ref="r1" verdict="not-ready"/>
      </branch>

      <branch when="single_trunk_score &gt;= 15">
        <question id="q-small-commits">Is principle small_commits score ≥ 15 / 25 (median PR &lt; 200 LoC AND median branch age &lt; 24h)?</question>

        <branch when="small_commits_score &lt; 15">
          <conclusion ref="r2" verdict="not-ready"/>
        </branch>

        <branch when="small_commits_score &gt;= 15">
          <question id="q-releasable">Is principle releasable_trunk score ≥ 15 / 25 (auto-revert AND flag inventory non-empty AND lint+typecheck+test gates)?</question>

          <branch when="releasable_trunk_score &lt; 15">
            <conclusion ref="r3" verdict="partial-adopt"/>
          </branch>

          <branch when="releasable_trunk_score &gt;= 15">
            <question id="q-fast-ci">Is principle fast_ci score ≥ 15 / 25 (p50 &lt; 10 min AND p95 &lt; 15 min)?</question>

            <branch when="fast_ci_score &lt; 15">
              <conclusion ref="r4" verdict="partial-adopt"/>
            </branch>

            <branch when="fast_ci_score &gt;= 15">
              <conclusion ref="run-the-checklist" verdict="adopt"/>
            </branch>
          </branch>
        </branch>
      </branch>
    </branch>
  </decision-tree>

  <example trace="weak-ci">
    prereqs_met=true → single_trunk_score=24 → small_commits_score=22 → releasable_trunk_score=20 → fast_ci_score=11 (p50 13min) → verdict=partial-adopt (ref r4); fix CI before declaring adopted.
  </example>
</text>
