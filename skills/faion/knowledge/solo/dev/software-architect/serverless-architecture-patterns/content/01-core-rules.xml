<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="serverless-architecture-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for serverless-architecture-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Each Lambda function MUST have a single responsibility scoped to one business capability or bounded context, NOT to one HTTP verb. A products function owns all product operations; separate functions own cart and orders domains.</statement>
      <rationale source="v1-source">Functions scoped per HTTP verb cannot scale paths independently, share permissions inappropriately, and create deployment coupling. Domain scoping allows independent scaling, tailored IAM policies, and isolated failure domains.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Every Lambda handler invoked from SQS, EventBridge, or async Lambda invocations MUST be idempotent. Implement with a deterministic idempotency key stored in DynamoDB with TTL, or use the aws-lambda-powertools idempotency decorator.</statement>
      <rationale source="v1-source">Lambda retries are at-least-once across SQS, EventBridge, and async invokes. Non-idempotent handlers produce duplicate charges, duplicate emails, and corrupted state on retry.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>AWS clients (DynamoDB, S3, Secrets Manager, etc.) MUST be initialized at module scope outside the handler function, NOT inside the handler. Imports and expensive initializations also belong at module scope.</statement>
      <rationale source="v1-source">Module-scope initialization runs once per container (warm invocation reuses the instance). Handler-scope initialization runs on every invocation, adding TLS handshake time, connection setup, and import time to every request.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Synchronous Lambda-to-Lambda chains MUST NOT span more than one hop without Step Functions or an async messaging layer (SQS, EventBridge) in between.</statement>
      <rationale source="v1-source">Synchronous chains compound latency additively, cascade timeouts (Lambda A times out waiting for Lambda B), and make error attribution impossible. Each hop also incurs invocation cost while waiting.</rationale>
    </rule>
  </section>
</text>
