<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="serverless-cost-optimization Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for serverless-cost-optimization. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>All Lambda functions MUST use ARM/Graviton (Architectures: [arm64]) by default. Functions using x86 MUST document the specific reason (e.g., x86-only native extension) in a code comment.</statement>
      <rationale source="v1-source">ARM is 20% cheaper per GB-second than x86 with no behavioral difference for Python, Node.js, Go, and Rust. The savings compound at scale — at 10M invocations/month at 200ms/512MB, the difference is ~$6/month per function.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Lambda functions processing multiple items (from SQS, Kinesis, DynamoDB Streams) MUST use batching: set BatchSize to the maximum safe value for the workload and MaximumBatchingWindowInSeconds to 5 or higher unless real-time latency is required.</statement>
      <rationale source="v1-source">Batching reduces invocation count proportionally. At BatchSize=10, invocation cost drops 10x. The $0.20/million invocation charge becomes the dominant cost factor at high throughput; batching eliminates it.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Production Lambda functions MUST have per-function cost tracking enabled via tagging (Service, Environment, Function name tags on the Lambda resource) and Cost Explorer tag filters. A monthly cost anomaly alert MUST be configured for each function exceeding $50/month.</statement>
      <rationale source="v1-source">Without per-function cost visibility, runaway functions (stuck retry loops, fan-out storms, incorrect memory allocation) go undetected until the monthly bill. Tag-based cost attribution enables per-function chargebacks and anomaly detection.</rationale>
    </rule>
  </section>
</text>
