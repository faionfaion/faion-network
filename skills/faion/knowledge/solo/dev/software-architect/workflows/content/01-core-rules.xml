<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="workflows Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for workflows. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Always decompose into exactly six phases: CLARIFY → ESTIMATE → DESIGN → DEEP-DIVE → TRADE-OFFS → DOCUMENT. Never merge TRADE-OFFS into DESIGN — the validation step requires a separate adversarial pass.</statement>
      <rationale source="v1-source">Merged design+validation phases produce rubber-stamp output; the same persona that designed cannot adversarially attack it.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Treat ADRs like code: versioned, reviewed via PR, lifecycle-tracked (proposed → accepted → deprecated → superseded). One ADR per decision.</statement>
      <rationale source="v1-source">ADRs stored in prose wikis drift and become orphaned; Git history gives authorship, review comments, and diff trails.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Force the evaluator agent to cite at least one source per claim (release notes, benchmarks, official docs). Uncited claims are hallucination candidates.</statement>
      <rationale source="v1-source">LLM-generated technology comparisons frequently invent performance numbers; citations make claims falsifiable.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>ATAM utility trees from LLMs are usually too uniform — every leaf marked "high importance, high difficulty." Require an explicit ranking distribution (no more than 20% of leaves at the highest tier).</statement>
      <rationale source="v1-source">Uniform trees provide no prioritization signal; the point of ATAM is to surface the top-3 risk themes, not to enumerate everything.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Require the agent to draft the rollback plan before the cutover plan. Migration plans without rollback are incomplete.</statement>
      <rationale source="v1-source">Agents skip rollback, dual-write, and feature-flag cutover unless they are required headings in the template.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Never combine Roadmap Review and Design Review in the same session. They serve different stakeholder audiences and different questions.</statement>
      <rationale source="v1-source">Mixed sessions produce shallow coverage of both: stakeholders disengage during design details; implementers tune out during strategic framing.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Always run the Validate phase as a separate adversarial agent call, never as a closing paragraph appended to the Design call. Different prompt, different system context, explicit failure-finding instructions.</statement>
      <rationale source="v1-source">The same persona that designed the system cannot adversarially attack it; the critic role requires a context switch enforced by prompt separation.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Treat each workflow as a pipeline of role-specialized subagents: clarifier, designer, critic, documenter. Materialize state to disk between steps (spec.md, options.md, adr-NNN.md) so agents reload from files, not transcript memory.</statement>
      <rationale source="v1-source">Transcript memory is lost on context reset; disk state survives and allows parallel or sequential agent handoffs without information loss.</rationale>
    </rule>
  </section>
</text>
