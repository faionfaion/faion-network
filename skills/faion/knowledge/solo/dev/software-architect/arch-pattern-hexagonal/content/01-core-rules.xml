<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="arch-pattern-hexagonal Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for arch-pattern-hexagonal. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>The application core MUST depend only on port interfaces, never on adapter implementations. All inbound ports (use-case interfaces) and outbound ports (repository, service interfaces) MUST live inside core/ports/, NOT inside the adapters directory.</statement>
      <rationale source="v1-source">If the core imports an adapter directly, swapping the adapter requires modifying the core — this breaks the pattern's fundamental guarantee. Port interfaces decouple the hexagon from its surroundings.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Core services MUST be tested with in-memory adapter implementations of all outbound ports, never against real databases or HTTP services. This keeps the core test suite fast (&lt;1s) and environment-independent.</statement>
      <rationale source="v1-source">If core service tests require a database, every developer needs a local DB, CI needs it, and tests become flaky on network timeouts. In-memory adapters eliminate the dependency entirely.</rationale>
    </rule>
  </section>
</text>
