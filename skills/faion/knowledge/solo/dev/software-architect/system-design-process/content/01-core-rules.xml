<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="system-design-process Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for system-design-process. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Execute design in five phases in order: Understand (requirements list), Scope (in/out of scope), Design (architecture + diagrams), Validate (adversarial review), Document (ADRs + C4). The Validate phase must be a separate agent call — never a closing paragraph of the Design phase.</statement>
      <rationale source="v1-source">Agents that skip the Validate phase produce designs without identified single points of failure, hot partitions, or data-loss windows. Adversarial review requires a different prompt persona, not an afterthought.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Force NFR numerics before any diagram: an LLM with vague NFRs will always over-engineer. Every NFR must have a measurable target and a source (user-impact analysis or competitive benchmark).</statement>
      <rationale source="v1-source">Vague NFRs ("it should be fast") produce architectures sized for 100x the actual load because the LLM defaults to the largest example in its training data.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Write RPS/storage/bandwidth calculations in the design prompt; do not let the agent skip to architecture without these numbers. LLM scale estimates without anchors are off by an order of magnitude.</statement>
      <rationale source="v1-source">Architecture choices are load-sensitive: a service at 10 RPS can be a single-instance monolith; at 100k RPS it needs sharding. Getting this wrong at design time is expensive to reverse.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Always produce three architecture options with explicit NFR scores before committing to one. Single-option output is the failure signature of an under-constrained prompt.</statement>
      <rationale source="v1-source">LLMs default to microservices and event-driven architectures regardless of scale; requiring three options with NFR trade-offs surfaces the simpler alternatives.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Start at Context (L1), zoom to Container (L2) for all designs. Add Component (L3) only for containers that are architecturally significant. Code (L4) is rarely needed.</statement>
      <rationale source="v1-source">Most projects only need L1-L2 to communicate architecture to all stakeholders; L3 and L4 add maintenance burden for marginal clarity gains.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Pin the agent to actual repo structure (paste tree -L 2 output) before asking for component diagrams — LLM-generated C4 diagrams frequently invent components not present in the codebase.</statement>
      <rationale source="v1-source">Fabricated services in architecture diagrams create false expectations and mislead new engineers about what actually exists.</rationale>
    </rule>
  </section>
</text>
