<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="structural-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for structural-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use the Adapter pattern to make an incompatible existing interface work with a new interface, without modifying the existing code. Adapter is a retrofit pattern — it is applied after the incompatibility is discovered, not during initial design.</statement>
      <rationale source="v1-source">Third-party libraries and legacy systems have fixed interfaces. Modifying them is impossible or risky. The Adapter wraps them and translates the interface, preserving the existing code's behavior while enabling it to participate in new code contracts.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use Bridge when two independent dimensions of variation exist and both must be extensible independently. Bridge is a design-time pattern — define it before building the class hierarchy, not as a retrofit to an existing hierarchy.</statement>
      <rationale source="v1-source">Without Bridge, adding a new dimension multiplies the class count: N notification types × M urgency levels = N×M classes. Bridge splits the abstraction (notification type) from the implementation (urgency rendering) into independent hierarchies that can grow independently.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Use Composite when clients must treat individual objects (leaves) and compositions (branches) uniformly. The key constraint: both leaf and branch must implement the same interface so client code needs no type-checking.</statement>
      <rationale source="v1-source">Without Composite, client code must distinguish leaves from branches via isinstance checks, violating OCP. The Composite interface lets clients call the same method on a single file or an entire directory tree — the traversal logic is encapsulated in the composite node.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use Decorator to add cross-cutting behavior (logging, caching, auth, retry) without subclassing. Decorators can be stacked: each wraps the previous and adds one responsibility. All decorators must implement the same interface as the wrapped object.</statement>
      <rationale source="v1-source">Subclassing to add behavior produces a class explosion: LoggedService, CachedService, AuthedService, LoggedCachedService, etc. Decorators compose at runtime: stack LoggingDecorator(CachingDecorator(RealService())) and any combination is possible without additional classes.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use Facade to provide a single, simplified entry point to a complex subsystem. The Facade does not prevent direct access to subsystem classes — it provides a convenience layer for the common use cases. Never let a Facade become a God Object.</statement>
      <rationale source="v1-source">Subsystems with many interacting classes create high coupling in client code: clients must know the correct order of operations, error states, and side effects. A Facade encapsulates the most common workflows, reducing client coupling without hiding the subsystem from clients who need more control.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Select the proxy type by the problem: Virtual (lazy initialization), Protection (access control), Cache (memoization), Remote (transparent local interface to a remote object), Logging (audit trail). All types implement the same interface as the real object.</statement>
      <rationale source="v1-source">All proxy types share the same structural pattern but solve different problems. Choosing the wrong proxy type results in unnecessary complexity: a Logging Proxy should not make access control decisions.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use Flyweight only when the application creates thousands (or millions) of similar objects and memory profiling shows that object count is a measurable bottleneck. Flyweight adds extrinsic state management complexity — only justified by demonstrated memory savings.</statement>
      <rationale source="v1-source">Flyweight trades memory for complexity: the caller must now provide extrinsic state on every operation instead of storing it on the object. Without measured memory pressure, this complexity has no benefit.</rationale>
    </rule>
  </section>
</text>
