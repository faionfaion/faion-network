<?xml version="1.0" encoding="UTF-8"?>
<text id="02-decorator-facade-proxy" title="Decorator, Facade, and Proxy Patterns">
  <summary>Decorator adds behavior dynamically by wrapping an object that implements the same interface. Facade simplifies a complex subsystem behind a single entry point. Proxy controls access to an object — four proxy types (Virtual, Protection, Cache, Logging).</summary>

  <section title="Decorator">
    <rule>
      <statement>Use Decorator to add cross-cutting behavior (logging, caching, auth, retry) without subclassing. Decorators can be stacked: each wraps the previous and adds one responsibility. All decorators must implement the same interface as the wrapped object.</statement>
      <rationale>Subclassing to add behavior produces a class explosion: LoggedService, CachedService, AuthedService, LoggedCachedService, etc. Decorators compose at runtime: stack LoggingDecorator(CachingDecorator(RealService())) and any combination is possible without additional classes.</rationale>
    </rule>
    <list>
      <item>Component: the interface both the real object and all decorators implement</item>
      <item>ConcreteComponent: the real object being decorated</item>
      <item>Decorator: wraps a Component, implements the same interface, delegates + adds behavior</item>
      <item>Stacking: decorators are applied in a chain — outermost executes first, innermost last</item>
      <item>Use cases: middleware chains (WSGI/ASGI), logging decorators, retry wrappers, caching layers</item>
    </list>
  </section>

  <section title="Facade">
    <rule>
      <statement>Use Facade to provide a single, simplified entry point to a complex subsystem. The Facade does not prevent direct access to subsystem classes — it provides a convenience layer for the common use cases. Never let a Facade become a God Object.</statement>
      <rationale>Subsystems with many interacting classes create high coupling in client code: clients must know the correct order of operations, error states, and side effects. A Facade encapsulates the most common workflows, reducing client coupling without hiding the subsystem from clients who need more control.</rationale>
    </rule>
    <list>
      <item>Facade: provides simple methods for common operations on the subsystem</item>
      <item>Subsystem classes: remain accessible for direct use when Facade is insufficient</item>
      <item>Do not add business logic to the Facade — it should only orchestrate subsystem calls</item>
      <item>One Facade per subsystem — multiple facades for different client types is acceptable</item>
    </list>
    <antipattern>
      <description>Facade that grows into a God Object (50+ methods, 1000+ lines)</description>
      <reason>A large Facade is just a different form of coupling — all clients depend on one giant class. Split into multiple focused facades or redesign the subsystem.</reason>
    </antipattern>
  </section>

  <section title="Proxy">
    <rule>
      <statement>Select the proxy type by the problem: Virtual (lazy initialization), Protection (access control), Cache (memoization), Remote (transparent local interface to a remote object), Logging (audit trail). All types implement the same interface as the real object.</statement>
      <rationale>All proxy types share the same structural pattern but solve different problems. Choosing the wrong proxy type results in unnecessary complexity: a Logging Proxy should not make access control decisions.</rationale>
    </rule>
    <list>
      <item>Virtual Proxy: delays expensive object creation until first use — image loading, large document parsing</item>
      <item>Protection Proxy: checks permissions before delegating — RBAC enforcement, API key validation</item>
      <item>Cache Proxy: memoizes results to avoid repeated expensive computation — database query caching</item>
      <item>Remote Proxy: represents a remote object locally — gRPC stubs, service proxies, HTTP client wrappers</item>
      <item>Logging Proxy: records all calls to the real object for audit or debugging</item>
    </list>
  </section>
</text>
