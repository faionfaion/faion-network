<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="api-gateway-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for api-gateway-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use simple routing when the architecture has a direct mapping between client requests and backend services with minimal transformation needed and a single client type or uniform client needs.</statement>
      <rationale source="v1-source">Simple routing avoids operational overhead of BFF or aggregation patterns. Adding patterns prematurely creates maintenance burden without benefit.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use BFF when different clients have vastly different data needs, mobile needs optimized payloads, third-party integrations require different contracts, or different security requirements exist per client type.</statement>
      <rationale source="v1-source">A single gateway serving all clients tends to accumulate client-specific logic and becomes a bottleneck; BFF pushes that complexity to per-client surfaces owned by the teams that know those clients.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Use aggregation when reducing client-side complexity is a primary goal, when minimizing network round trips matters (mobile, low-bandwidth), or when creating composite API views from independently deployed services.</statement>
      <rationale source="v1-source">Without aggregation, clients make N parallel requests; mobile clients on slow links feel latency multiplied by the number of services. Aggregation at the gateway trades gateway complexity for client simplicity.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use edge+mesh when services require mutual TLS between them, when fine-grained traffic management between services is needed, or when a zero-trust security model is required. Do NOT use a service mesh for a simple two-tier app — the operational overhead is substantial.</statement>
      <rationale source="v1-source">The mesh handles east-west (service-to-service) while the gateway handles north-south (client-to-gateway). Combining them avoids duplicating concerns at both layers.</rationale>
    </rule>
  </section>
</text>
