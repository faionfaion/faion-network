<?xml version="1.0" encoding="UTF-8"?>
<text id="02-process-and-tooling" title="ADR Process, Git Workflow, and Tooling">
  <summary>Git-based ADR workflow, team governance (who proposes and who accepts), CI validation, and tool comparison (adr-tools, log4brains, pyadr).</summary>

  <section title="Git Workflow">
    <rule>
      <statement>Commit each ADR in the same pull request as the code change it describes. A PR implementing a new database selection should include both the migration scripts and the ADR. This ties the decision record to the code that implements it in git history.</statement>
      <rationale>ADRs committed separately from their code lose the associative link in git history. When bisecting a regression, the code change and its decision context should appear together in `git log`.</rationale>
    </rule>
    <list>
      <item>New decision: branch from main, add ADR as Draft, implement code, change status to Proposed in PR description, merge when approved</item>
      <item>Superseding decision: new ADR references the old one; old ADR updated to Superseded status in the same commit</item>
      <item>CI validation: script checks all ADR files have required sections and valid status values (see adr-lint.sh template)</item>
      <item>PR template: include checkbox "ADR added/updated if this is an architecturally significant change"</item>
    </list>
  </section>

  <section title="Governance">
    <rule>
      <statement>Define who can propose and who can accept ADRs before the first ADR is written. The minimum governance: any engineer proposes; tech lead or designated architect approves; approval requires documented objection handling, not just silence.</statement>
      <rationale>Without governance, ADRs become either bottlenecks (nothing gets written because approval is unclear) or rubber stamps (everything is accepted without review). Document the governance in ADR-0001.</rationale>
    </rule>
    <list>
      <item>Proposer: any engineer who identifies an architecturally significant decision</item>
      <item>Reviewer: 2+ engineers including at least one tech lead or senior engineer</item>
      <item>Approver: tech lead or designated architect for the affected area</item>
      <item>Review period: 1 week for non-urgent decisions; 24h for urgent hotfixes (document the urgency)</item>
      <item>Objection handling: objections must be addressed in the ADR's Considered Options section, not silently ignored</item>
    </list>
  </section>

  <section title="Tooling">
    <list>
      <item>adr-tools (bash, npryce): `adr new "Use PostgreSQL"` → scaffolds numbered file; `adr list` → lists all ADRs</item>
      <item>log4brains (Node.js, jhollingsworth): web UI for browsing ADRs; generates static site; MADR-native; CI/CD GitHub Action available</item>
      <item>pyadr (Python): CLI for Nygard and MADR; supports status transitions; integrates with pre-commit hooks</item>
      <item>ADR Manager (VS Code extension): creates/views ADRs from editor; compatible with multiple formats</item>
    </list>
  </section>
</text>
