<?xml version="1.0" encoding="UTF-8"?>
<text id="03-agent-workflow" title="Agent Workflow for ADR Generation">
  <summary>Four-pass agentic pipeline for AI-assisted ADR writing; critical gotchas for AI fabrication of non-existent tools, versions, and case studies.</summary>

  <section title="Four-Pass Pipeline">
    <rule>
      <statement>Use a four-pass pipeline when asking an AI agent to help write an ADR: (1) trigger detection — identify whether the decision is architecturally significant; (2) context gathering — collect constraints, quality attribute requirements, and existing ADRs; (3) alternatives generation — enumerate 3–5 realistic options with trade-offs; (4) link-back — connect the ADR to related ADRs and the affected code.</statement>
      <rationale>Single-prompt ADR generation produces shallow records that skip alternatives and fail to document the constraints that made this decision necessary. The four-pass structure enforces completeness and forces the agent to reason about trade-offs rather than anchor on the most popular option.</rationale>
    </rule>
    <list>
      <item>Pass 1 — Trigger detection: is this decision reversible? does it affect more than one module? does it have a significant performance or security impact? if yes to any → ADR warranted</item>
      <item>Pass 2 — Context: what quality attributes are at stake? what are the budget, team skill, and operational constraints? what prior ADRs does this build on or conflict with?</item>
      <item>Pass 3 — Alternatives: generate ≥3 realistic options; for each: implementation effort, trade-offs, known failure modes</item>
      <item>Pass 4 — Link-back: reference the related ADRs; link to the PR implementing the decision; note which quality attribute scenarios (from the QA methodology) this decision addresses</item>
    </list>
  </section>

  <section title="AI-Agent Gotchas">
    <rule>
      <statement>Always verify AI-generated ADR content for: fabricated benchmark numbers, non-existent tool versions, outdated ecosystem comparisons, and trendy recommendations not grounded in the project's constraints.</statement>
      <rationale>LLMs generate plausible-sounding but fabricated performance figures and cite outdated ecosystem comparisons. An ADR with fabricated data is worse than no ADR — it provides false justification for the wrong decision.</rationale>
    </rule>
    <list>
      <item>Fabricated benchmarks: AI may cite "X is 3x faster than Y" with no source — require a linked benchmark or remove the claim</item>
      <item>Version staleness: AI training cutoff may be 6-24 months old; verify current version numbers for all tools mentioned</item>
      <item>Trendy recommendations: AI anchors on popular patterns (microservices, event sourcing) regardless of project context; explicitly provide your constraints in the prompt</item>
      <item>Survivorship bias: AI examples skew toward success stories; require the agent to enumerate failure modes and known limitations for each option</item>
      <item>Context injection: always provide existing ADRs, current tech stack, team size, budget, and SLO requirements as input — AI cannot infer your project context from generic knowledge</item>
    </list>
  </section>
</text>
