<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="behavioral-patterns Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for behavioral-patterns. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Apply Strategy when you have three or more algorithm variants that swap at runtime AND the switching logic is a growing if/else or switch-on-type. With two variants, a simple conditional is cleaner.</statement>
      <rationale source="v1-source">Premature abstraction with one or two implementations produces an interface with no real polymorphism; the Strategy indirection adds ceremony without extensibility benefit.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use Observer for fan-out notification to N consumers where the subject does not need to know who is listening. Use direct callbacks or dependency injection when the relationship is one-to-one.</statement>
      <rationale source="v1-source">Observer decouples the subject from consumers at the cost of making notification order and error propagation implicit. One-to-one relationships don't need that indirection.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Apply Command when you need undo/redo, operation queuing, or audit logging — not as a general wrapper for every method call. Command adds a class per operation; reserve it for operations where that overhead is justified.</statement>
      <rationale source="v1-source">Wrapping every service method in a Command object creates an explosion of classes with no real benefit if the operations never need to be undone, queued, or replayed.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Apply State when an object's behavior changes significantly across multiple states AND the state-transition logic is growing into a complex conditional. With 2–3 states and simple transitions, an enum + switch is cleaner.</statement>
      <rationale source="v1-source">State pattern promotes each state to a class; this is only worth the overhead when per-state behavior has real complexity. Simple enums with minimal branching don't justify the class explosion.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use Chain of Responsibility for middleware pipelines where each handler decides to process the request or pass it on. Prefer a function-composition approach (list of handler functions iterated in order) over class-based chains for simple pipelines.</statement>
      <rationale source="v1-source">Class-based chains add boilerplate for simple middleware cases; a list of handler functions achieves the same decoupling with less ceremony in languages with first-class functions.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Apply Template Method when multiple classes share the same algorithm structure but differ in steps. If algorithm structure itself varies, use Strategy instead.</statement>
      <rationale source="v1-source">Template Method uses inheritance to fix the skeleton; Strategy uses composition to swap the whole algorithm. Inheritance is harder to test and refactor; prefer Strategy when both could work.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use Mediator to reduce many-to-many coupling between components. Stop when the Mediator becomes a god object that knows too much — split into multiple mediators by domain.</statement>
      <rationale source="v1-source">A Mediator that coordinates all components becomes a single point of failure and a maintainability bottleneck. The pattern solves coupling; one Mediator per domain keeps it bounded.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Apply Visitor when you need to add multiple operations to a stable class hierarchy without modifying those classes. If the hierarchy changes frequently, Visitor is the wrong choice — every new class requires updating all visitors.</statement>
      <rationale source="v1-source">Visitor trades hierarchy extensibility for operation extensibility. It is the inverse of the Open-Closed trade-off: Visitor makes adding operations easy, but adding new element types hard.</rationale>
    </rule>
  </section>
</text>
