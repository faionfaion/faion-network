<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="decision-tree-architecture-style Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for decision-tree-architecture-style. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Teams with fewer than 10 developers MUST start with a Monolith. Teams of 10-50 MUST use a Modular Monolith unless bounded contexts are already well-defined and DevOps maturity is high. Teams of 50+ with high DevOps maturity MAY adopt Microservices only when all three preconditions hold: bounded contexts are clear, teams can own services end-to-end, and a platform team exists.</statement>
      <rationale source="v1-source">Microservices impose distributed-systems overhead (network latency, distributed tracing, deployment pipelines per service) that consumes engineering capacity. Small and medium teams cannot afford this overhead while also building product. The thresholds come from CNCF 2025 and Gartner 2024 data.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use the following threshold table to score each input before walking the tree. If multiple inputs disagree on the recommended style, prefer the more conservative (simpler) style and document the disagreement in the ADR.</statement>
      <rationale source="v1-source">Single-axis trees miss interaction effects. Scoring inputs first and comparing them surfaces disagreements before the walk, forcing explicit acknowledgment in the ADR.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>The agent MUST output the runner-up leaf and its key trade alongside the primary recommendation. Outputting only the winning leaf is a rubber-stamp failure mode.</statement>
      <rationale source="v1-source">Forcing the runner-up eliminates the rubber-stamp behavior where agents present a single option as unconditional. The runner-up reveals which input is most fragile and what would change the decision.</rationale>
    </rule>
  </section>
</text>
