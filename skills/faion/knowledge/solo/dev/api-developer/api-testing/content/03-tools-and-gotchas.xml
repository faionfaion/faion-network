<?xml version="1.0" encoding="UTF-8"?>
<text id="03-tools-and-gotchas" title="Tools, Services, and AI-Agent Gotchas">
  <summary>CLI tools for API testing, recommended services, and critical gotchas when AI agents write or run tests.</summary>

  <section title="CLI Tools">
    <list>
      <item>schemathesis — property-based testing of OpenAPI/GraphQL APIs; pip install schemathesis</item>
      <item>dredd — OpenAPI conformance runner; npm i -g dredd</item>
      <item>pact-cli — contract broker verifier; docs.pact.io</item>
      <item>newman — Postman collection runner for CI; npm i -g newman</item>
      <item>hurl — plain-text HTTP test files, fast in CI; brew install hurl</item>
      <item>k6 — load + functional checks; k6.io</item>
      <item>prism — OpenAPI mock + validating proxy; npm i -g @stoplight/prism-cli</item>
      <item>vcrpy / pytest-recording — record/replay HTTP cassettes; pip install pytest-recording</item>
    </list>
    <reference path="templates/schemathesis-gate.sh">CI fuzz gate using Schemathesis against a running OpenAPI app</reference>
  </section>

  <section title="Key Services">
    <list>
      <item>PactFlow (SaaS) — hosted Pact broker + can-i-deploy; REST API, agent-friendly</item>
      <item>Checkly (SaaS) — synthetic monitoring + Playwright; checkly CLI + IaC</item>
      <item>Stoplight Prism (OSS) — OpenAPI mock + validating proxy; agent-drivable</item>
      <item>WireMock Cloud (SaaS + OSS) — mock external APIs; JSON config</item>
      <item>42Crunch (SaaS) — OpenAPI security audit + conformance; CI plugin</item>
    </list>
  </section>

  <section title="AI-Agent Gotchas">
    <rule>
      <statement>Force agents to assert against openapi.yaml, not against current implementation. Prompt: "assert against the spec, not the code."</statement>
      <rationale>Agents default to writing tests that mirror current behavior. Tests pass; regressions slip because the spec was never consulted.</rationale>
    </rule>
    <antipattern>
      <description>Agent-generated tests pile up time.sleep(1) to wait for async operations</description>
      <reason>Creates slow, flaky tests. Replace with explicit polling helpers or pytest-asyncio fixtures.</reason>
    </antipattern>
    <antipattern>
      <description>Agent fixing a flaky test by adding --reruns without a root-cause fix</description>
      <reason>Hides the real problem. Require any --reruns &gt; 0 to have an issue link explaining the flake.</reason>
    </antipattern>
    <antipattern>
      <description>Agent commits production secrets into test config files</description>
      <reason>Test files often skip linting. Run secret-scan on tests/ too.</reason>
    </antipattern>
    <antipattern>
      <description>E2E agent suite against shared staging mutates data for other tests</description>
      <reason>Use per-run namespacing (org_id derived from PR number) or ephemeral environments.</reason>
    </antipattern>
  </section>
</text>
