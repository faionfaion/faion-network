<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="api-openapi-spec">
  <metadata>
    <tier>solo</tier>
    <group>dev</group>
    <domain>api-developer</domain>
    <summary>Describe REST APIs as OpenAPI 3.1 contracts. Shard sources into paths/components, $ref everything reusable, bundle with redocly, gate breaking changes with oasdiff in CI.</summary>
    <tags>
      <tag>openapi</tag>
      <tag>contract-first</tag>
      <tag>redocly</tag>
      <tag>spectral</tag>
      <tag>oasdiff</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-27</created>
    <related>
      <ref slug="api-rest-design"/>
      <ref slug="api-contract-first"/>
      <ref slug="api-documentation"/>
      <ref slug="api-versioning"/>
    </related>
  </metadata>
  <content>
    <title>OpenAPI Specification</title>

    <summary>Describe REST APIs as machine-readable YAML/JSON contracts (OpenAPI 3.1) that drive codegen, mocking, contract testing, and reference docs. Shard sources into `paths/`, `components/schemas/`, `components/responses/`, `components/parameters/` and bundle to a single `openapi.yaml` via `redocly bundle`. Every operation must have `operationId`, `summary`, `tags`, and examples; every reusable shape must live in `components/` as a `$ref`.</summary>

    <why>
      <p>A spec that is the single source of truth lets generators produce TypeScript/Python/Go clients, server stubs, mock servers, and rendered docs from one file. `oasdiff` in CI catches breaking changes before they reach consumers. Without the spec as contract, client teams diverge silently and breaking changes ship undetected.</p>
    </why>

    <when-to-use>
      <list>
        <item>HTTP / REST APIs that need a machine-readable contract for codegen, mocking, contract testing, and reference docs.</item>
        <item>Multi-language SDK distribution where TS / Python / Go / Java clients regenerate from a single source on each release.</item>
        <item>Public APIs where developer adoption depends on Swagger UI / Redoc / Mintlify-rendered reference docs.</item>
        <item>Internal API platforms standardizing on contract-first development with `oasdiff`-enforced compatibility.</item>
        <item>Agentic backend authoring — a strict OpenAPI spec is the most reliable scaffolding for LLMs to extend without hallucinating endpoints / fields.</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>gRPC / Protobuf services — use `.proto` + `buf` instead; OpenAPI shoehorned around RPC fights tooling.</item>
        <item>AsyncAPI / event-driven systems — use AsyncAPI 2.x/3.x for Kafka, MQTT, WebSocket message envelopes.</item>
        <item>GraphQL — use the GraphQL SDL and tools like `graphql-codegen`; a partial OpenAPI overlay adds confusion.</item>
        <item>Highly volatile experimental endpoints — generating clients from each iteration is overhead; freeze the contract before publishing.</item>
      </list>
    </when-not-to-use>

    <section name="limitations">
      <p>Where it fails / limitations:</p>
      <list>
        <item>Spec rot. Code outpaces spec; clients break silently. Mitigation: contract tests (`schemathesis`/`dredd`) and `oasdiff` in CI.</item>
        <item>OpenAPI 3.0 vs 3.1 mismatch. Generators / linters trail; some tools only support 3.0. Pin a version per service.</item>
        <item>Polymorphism quirks. `oneOf` + `discriminator` is the only reliable polymorphism pattern; `anyOf` / `allOf` mixes confuse generators.</item>
        <item>Nullable inconsistency. OpenAPI 3.1 follows JSON Schema (`type: [string, "null"]`); 3.0 uses `nullable: true`. Mixing breaks codegen.</item>
        <item>Inline schema explosion. Operations carry duplicated inline shapes; refactors miss one. Always `$ref` shared schemas.</item>
        <item>Generator quality varies wildly by language and target framework. `python-fastapi` generator output is rough; `typescript-axios` is solid; `go` is OK. Audit before commit.</item>
        <item>Spec-as-docs only. Teams ship spec but never use mocks / codegen / contract tests; spec becomes write-once.</item>
        <item>`additionalProperties: true` default allows unknown fields; later strict validation breaks clients.</item>
        <item>Examples drift from schema. Hand-edited examples violate the schema. Linters catch with `oas3-valid-media-example`.</item>
        <item>Security scheme misuse. Bearer / OAuth2 / API key all defined but only one used; spec confuses doc readers.</item>
        <item>No multi-file structure. A 5,000-line `openapi.yaml` is unmaintainable. Bundle via `redocly bundle` from `paths/` and `components/` shards.</item>
      </list>
    </section>

    <section name="core-structure-rules">
      <p>Core structure rules:</p>
      <rule>
        <statement>Set openapi: 3.1.0 and define info.title, info.version, servers (prod + staging), and a global security scheme.</statement>
        <rationale>3.1 aligns with JSON Schema 2020-12, making schema validation and tooling consistent.</rationale>
      </rule>
      <rule>
        <statement>Every operation must have operationId, summary, tags, at least one request/response example, and refs to common error responses.</statement>
        <rationale>operationId drives SDK method names; missing it produces ugly generated names. Examples are the client's first learning surface.</rationale>
      </rule>
      <rule>
        <statement>All reusable shapes go in components/schemas, components/responses, or components/parameters and are referenced via $ref. No inline schemas in operations.</statement>
        <rationale>Inline schemas duplicate on every refactor; a single $ref is the single source of truth.</rationale>
      </rule>
    </section>

    <section name="component-sharding">
      <p>For any spec over ~200 lines, shard sources into a directory structure and bundle with redocly.</p>
      <example type="good">
        <code lang="text"><![CDATA[
openapi/
├── _root.yaml              # openapi, info, servers, security, tags
├── paths/
│   ├── users.yaml          # /users GET+POST
│   └── users-{id}.yaml     # /users/{userId} GET+PATCH+DELETE
└── components/
    ├── schemas/
    │   ├── User.yaml
    │   ├── CreateUserRequest.yaml
    │   └── Error.yaml
    ├── responses/
    │   ├── BadRequest.yaml
    │   ├── Unauthorized.yaml
    │   └── NotFound.yaml
    └── parameters/
        ├── UserId.yaml
        ├── PageLimit.yaml
        └── PageOffset.yaml
        ]]></code>
        <note>Bundle with: redocly bundle openapi/_root.yaml -o openapi.yaml</note>
      </example>
      <example type="bad">
        <code lang="text"><![CDATA[
# Single 3000-line openapi.yaml with all schemas inline
paths:
  /users:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  ...  # duplicated everywhere
        ]]></code>
        <note>Inline schemas drift on each edit; $ref discipline prevents this.</note>
      </example>
    </section>

    <section name="security-schemes">
      <p>Security schemes:</p>
      <rule>
        <statement>Define security schemes once in components/securitySchemes and apply at root level or per-operation. Remove declared schemes that are never applied.</statement>
        <rationale>Multiple declared but unused schemes confuse doc readers and SDK generators.</rationale>
      </rule>
      <example type="good">
        <code lang="yaml"><![CDATA[
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
        ]]></code>
      </example>
    </section>

    <section name="per-operation-requirements">
      <p>Per-operation requirements:</p>
      <rule>
        <statement>Each operation must have: operationId (camelCase), summary (imperative phrase), description (optional but useful), tags (1+), requestBody examples, and refs to all possible error responses.</statement>
        <rationale>Codegen uses operationId; missing causes ugly method names. Error refs teach SDK consumers what to expect.</rationale>
      </rule>
      <rule>
        <statement>Common error responses (400 BadRequest, 401 Unauthorized, 403 Forbidden, 404 NotFound, 429 TooManyRequests, 422 ValidationError) must live in components/responses and be referenced, never inlined.</statement>
        <rationale>One canonical error shape per code prevents clients handling two different error bodies for the same status.</rationale>
      </rule>
    </section>

    <section name="schema-discipline">
      <p>Schema discipline:</p>
      <rule>
        <statement>Set additionalProperties: false on all request schemas and additionalProperties: true only where intentionally extensible on response schemas.</statement>
        <rationale>additionalProperties defaults to true, silently allowing unknown fields that later strict validation breaks.</rationale>
      </rule>
      <rule>
        <statement>For polymorphism, use oneOf with discriminator.propertyName mapping all variants. Never use anyOf for discriminated unions.</statement>
        <rationale>anyOf without discriminator causes generators to emit unsafe union types; runtime parsing breaks.</rationale>
      </rule>
      <rule>
        <statement>In OpenAPI 3.1, use type: [string, "null"] for nullable fields, not nullable: true.</statement>
        <rationale>nullable: true is a 3.0 extension; 3.1 follows JSON Schema 2020-12 where null is a type.</rationale>
      </rule>
    </section>

    <section name="ci-gates">
      <p>CI gates:</p>
      <rule>
        <statement>Run spectral lint, redocly lint, and oasdiff breaking check on every PR against the main branch spec.</statement>
        <rationale>Without oasdiff, breaking changes ship to SDK consumers undetected.</rationale>
      </rule>
      <rule>
        <statement>Run schemathesis against a running impl on every PR to detect spec-vs-impl drift.</statement>
        <rationale>A spec that validates but whose server emits a different shape is worse than no spec — clients trust the spec.</rationale>
      </rule>
    </section>

    <section name="antipatterns">
      <antipattern>
        <description>Spec exists as docs only — no mocks, no codegen, no contract tests.</description>
        <reason>Spec becomes stale; code diverges; clients break silently on next release.</reason>
      </antipattern>
      <antipattern>
        <description>Hand-editing the bundled openapi.yaml instead of source shards.</description>
        <reason>Next bundle run overwrites edits; agents lose changes.</reason>
      </antipattern>
      <antipattern>
        <description>Operations without examples.</description>
        <reason>SDK users and LLMs learn the API shape from examples; missing examples = more support questions + hallucinated payloads.</reason>
      </antipattern>
    </section>

    <section name="agentic-workflow">
      <p>Drive OpenAPI work top-down: (1) a structure-author subagent shards spec into `paths/`, `components/schemas/`, `components/responses/`, `components/parameters/` and bundles via `redocly bundle`; (2) a schema-author subagent maintains `$ref`-only operations — every shape lives in `components/schemas`; (3) a lint-and-diff subagent runs `spectral` with project ruleset plus `oasdiff` against `main`; (4) a codegen-orchestrator regenerates clients/server stubs, mocks, docs; (5) a contract-tester runs `schemathesis` against the running impl. `faion-sdd-executor-agent` runs lint/build/test gates. Forbid hand-edited inline schemas via lint; treat spec as the source of truth.</p>
      <p>Recommended subagents:</p>
      <list>
        <item>`faion-sdd-executor-agent` (`agents/faion-sdd-executor-agent.md`) — quality gate: lint, bundle, codegen, contract tests.</item>
        <item>A purpose-built spec-bundler — runs `redocly bundle paths/_root.yaml -o openapi.yaml` and commits a single deployable file alongside the source shards.</item>
        <item>A spec-linter — runs `spectral` with custom rules (no inline schemas, kebab-case paths, every operation has `operationId`/`summary`/`tags`/examples, error responses reference Problem Details).</item>
        <item>A breaking-change-bot — `oasdiff breaking` between PR head and `main`; posts changelog comment.</item>
        <item>A discriminator-validator — checks every `oneOf` has `discriminator.propertyName` mapping all variants.</item>
        <item>An example-validator — runs `examples.value` against schema; rejects mismatches.</item>
        <item>A client-codegen — re-runs `openapi-generator` for ts / python / go targets; commits to `generated/` dirs.</item>
        <item>`password-scrubber-agent` (`agents/password-scrubber-agent.md`) — scans examples for tokens / PII.</item>
      </list>
      <prompt purpose="schema-authoring"><![CDATA[
Add component schema CreateInvoiceRequest under components/schemas/.
Required: customerId (uuid), amount (integer, min 1), currency
(enum USD/EUR/GBP), lineItems (array, min 1, max 100).
Optional: dueDate (date), memo (string, max 500), metadata
(additionalProperties: string, max 20 keys).
Provide one realistic example.
Reuse Money component if amount + currency repeat.
Output a unified diff against components/schemas/ only.
      ]]></prompt>
      <prompt purpose="operation-wiring"><![CDATA[
Add POST /invoices that accepts CreateInvoiceRequest, returns
201 + Invoice + Location header. Errors:
- 400 ValidationError (Problem Details, code VALIDATION_ERROR)
- 402 PaymentRequired (code TIER_LIMIT_REACHED)
- 409 IdempotencyConflict (code IDEMPOTENCY_KEY_REUSED)
Require Idempotency-Key header (component parameter ref).
Tag: Invoices. operationId: createInvoice.
Output diffs only to paths/invoices.yaml.
      ]]></prompt>
    </section>

    <section name="cli-tools">
      <p>CLI tools:</p>
      <list>
        <item>`redocly` — Lint, bundle, build static docs. https://redocly.com/docs/cli</item>
        <item>`spectral` — Lint with built-in + custom rules. https://stoplight.io/open-source/spectral</item>
        <item>`swagger-cli` — Validate + bundle. https://github.com/APIDevTools/swagger-cli</item>
        <item>`openapi-generator-cli` — Multi-language codegen. https://openapi-generator.tech</item>
        <item>`openapi-typescript` — TypeScript types from spec. https://github.com/drwpow/openapi-typescript</item>
        <item>`openapi-fetch` — Typed fetch client from `openapi-typescript` types. https://github.com/drwpow/openapi-typescript</item>
        <item>`oasdiff` — Breaking-change detection. https://github.com/Tufin/oasdiff</item>
        <item>`schemathesis` — Property-based contract tests. https://schemathesis.readthedocs.io</item>
        <item>`prism` — Mock server + proxy validation. https://stoplight.io/open-source/prism</item>
        <item>`mockoon` — Local mock. https://mockoon.com</item>
        <item>`vacuum` — Fast OpenAPI linter (Go). https://quobix.com/vacuum/</item>
        <item>`optic` — API change tracking + diff CI. https://www.useoptic.com</item>
      </list>
    </section>

    <section name="services-and-apps">
      <p>Services &amp; apps:</p>
      <list>
        <item>Stoplight Studio — SaaS / Desktop, partial agent support. Visual editing; export YAML.</item>
        <item>SwaggerHub — SaaS, agent-friendly. Spec hosting + codegen + collab.</item>
        <item>Bump.sh — SaaS, agent-friendly. Spec hosting + diff-driven changelog.</item>
        <item>Redocly App — SaaS, agent-friendly. Hosted Redoc + workflow CI.</item>
        <item>Apicurio Registry — OSS, agent-friendly. Schema registry for OpenAPI / Avro / Protobuf.</item>
        <item>Speakeasy — SaaS, agent-friendly. High-quality SDK generation.</item>
        <item>Fern — SaaS, agent-friendly. Spec-driven SDK + docs platform.</item>
        <item>Mintlify — SaaS, agent-friendly. Doc framework with OpenAPI ingest.</item>
        <item>Postman — SaaS, agent-friendly. Import OpenAPI; generate collections.</item>
      </list>
    </section>

    <section name="bundle-check-script">
      <p>Use this gate to keep `openapi.yaml` and source shards in sync:</p>
      <example type="good" title="openapi-bundle-check.sh">
        <code lang="bash"><![CDATA[#!/usr/bin/env bash
# openapi-bundle-check.sh — bundled output must equal sources.
set -euo pipefail
SRC="${1:-openapi/_root.yaml}"
OUT="${2:-openapi.yaml}"
TMP=$(mktemp)
npx --yes @redocly/cli@latest bundle "$SRC" -o "$TMP" >/dev/null
if ! diff -q "$TMP" "$OUT" >/dev/null; then
  echo "FAIL — $OUT is stale; run: redocly bundle $SRC -o $OUT"
  diff -u "$OUT" "$TMP" | head -200
  exit 1
fi
npx --yes @stoplight/spectral-cli lint "$OUT" --fail-severity=warn
npx --yes @redocly/cli@latest lint "$OUT"
echo "OK"]]></code>
      </example>
    </section>

    <section name="best-practices">
      <p>Best practices:</p>
      <list>
        <item>Shard sources under `paths/`, `components/schemas/`, `components/responses/`, `components/parameters/`; bundle to a single `openapi.yaml` for tooling.</item>
        <item>`$ref` everything reusable. No inline schemas in operations beyond trivial enums.</item>
        <item>Every operation: `operationId`, `summary`, `description`, `tags`, request/response examples, error refs.</item>
        <item>Common error responses (`Unauthorized`, `Forbidden`, `NotFound`, `TooManyRequests`, `ValidationError`) live in `components/responses/` and are referenced from each operation.</item>
        <item>Pin OpenAPI version (3.1 if your toolchain supports JSON Schema 2020-12, otherwise 3.0).</item>
        <item>Discriminator on `oneOf` for tagged unions; ensure mapping covers every variant.</item>
        <item>Examples per response. Even 4xx — clients learn error shapes from realistic examples.</item>
        <item>Server URLs for prod / staging / sandbox; mark sandbox in description.</item>
        <item>Security schemes declared once and applied at operation or root level; document scopes for OAuth2.</item>
        <item>Document deprecation via `deprecated: true` and a `x-deprecation-date` extension; remove only on major version bump.</item>
        <item>`oasdiff` in CI plus a labeled `breaking-change` flag required to merge incompatible changes.</item>
        <item>Contract tests (`schemathesis run --checks=all`) against a running impl on every PR.</item>
        <item>Codegen output committed with `linguist-generated` attribute so reviewers can skip generated diffs.</item>
      </list>
    </section>

    <section name="ai-agent-gotchas">
      <p>AI-agent gotchas:</p>
      <list>
        <item>Inline schema duplication. Agent inlines a `User`-shaped object instead of `$ref`. Lint blocks via custom rule.</item>
        <item>3.0 vs 3.1 confusion. Agent uses `nullable: true` in a 3.1 doc; tools accept it but it's deprecated. Pin and lint version.</item>
        <item>`oneOf` without discriminator. Generators emit unsafe types; runtime parsing breaks.</item>
        <item>Examples that fail validation. Agent writes `example: { ... }` violating the schema. `oas3-valid-media-example` rule catches.</item>
        <item>Operations missing `operationId`. Codegen produces `path_method` ugly names. Always require `operationId`.</item>
        <item>`additionalProperties` default true. Agent unknowingly allows unknown fields; later strictification breaks clients. Set explicitly.</item>
        <item>Forgetting `Location` on 201. Spec sample missing the header; client SDKs can't follow new resources.</item>
        <item>Bundling from wrong root. Agent bundles a `paths/users.yaml` rather than `_root.yaml`; output is partial.</item>
        <item>Hand-editing bundled `openapi.yaml`. Bundle script overwrites; agents lose edits. Edit only sources.</item>
        <item>Multiple security schemes declared but only one applied. Doc readers confused. Remove unused schemes.</item>
        <item>Generator output drift. Agent regenerates with a different generator version; massive diff. Lock version per service.</item>
        <item>Polyglot anyOf. Generators handle inconsistently; pick `oneOf` with discriminator or simplify.</item>
        <item>Renaming fields with no deprecation. `oasdiff` must run; without it, agents ship breaking changes.</item>
        <item>Spec validates but server emits different shape. Run `schemathesis` to catch impl drift.</item>
      </list>
    </section>

    <templates>
      <template path="templates/openapi-skeleton.yaml" purpose="Minimal OpenAPI 3.1 spec with User resource, pagination, JWT security, and error responses"/>
      <template path="templates/openapi-bundle-check.sh" purpose="CI script: bundle-check + Spectral lint + Redocly lint"/>
    </templates>

    <see-also>
      <link href="https://spec.openapis.org/oas/v3.1.0"/>
      <link href="https://spec.openapis.org/oas/v3.0.4"/>
      <link href="https://openapi-generator.tech"/>
      <link href="https://stoplight.io/open-source/spectral"/>
      <link href="https://redocly.com/docs/cli"/>
      <link href="https://github.com/Tufin/oasdiff"/>
      <link href="https://schemathesis.readthedocs.io"/>
      <link href="https://github.com/stripe/openapi"/>
      <link href="https://github.com/github/rest-api-description"/>
      <ref slug="api-rest-design"/>
      <ref slug="api-contract-first"/>
      <ref slug="api-documentation"/>
      <ref slug="api-versioning"/>
    </see-also>
  </content>
</methodology>
