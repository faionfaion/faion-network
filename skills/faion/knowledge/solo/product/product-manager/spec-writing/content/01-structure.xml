<?xml version="1.0" encoding="UTF-8"?>
<text id="01-structure" title="Spec Structure: Required Sections, Rules, and Antipatterns">
  <summary>The required sections of a product specification, the rule each section must satisfy, and the antipatterns that make specs unusable.</summary>

  <section title="Required Sections">
    <rule>
      <statement>A complete spec must contain: Overview, Problem Statement, Goals (with current/target metric table), Non-Goals, User Stories, Functional Requirements (FR-N IDs), Non-Functional Requirements, Acceptance Criteria, Out of Scope, Open Questions. Missing any section is a reject signal.</statement>
      <rationale>Each section prevents a specific class of delivery failure: Non-Goals prevent scope creep, Open Questions prevent false confidence, Acceptance Criteria prevent untestable scope.</rationale>
    </rule>
  </section>

  <section title="Goals">
    <rule>
      <statement>Every goal must have a measurable current baseline and a numeric target. "Improve UX" is not a goal. "Reduce time-to-find-product from 3 min to 30 sec" is a goal.</statement>
      <rationale>Unmeasurable goals cannot be verified at launch and cannot drive prioritization decisions during delivery.</rationale>
    </rule>
  </section>

  <section title="Non-Goals">
    <rule>
      <statement>List everything explicitly excluded from this spec. Non-Goals section must be non-empty for any feature touching more than one surface.</statement>
      <rationale>The absence of Non-Goals is the leading cause of scope creep. Every unspecified behaviour becomes "obviously included" during implementation.</rationale>
    </rule>
    <example type="good">
      <note>Non-Goals: Advanced filters (separate feature), AI-powered recommendations, Voice search.</note>
    </example>
  </section>

  <section title="User Stories">
    <rule>
      <statement>Every story uses the format: "As a [named persona], I want to [action] so that [measurable benefit]." Use persona IDs (not "the user") in every story. Include the "so that" clause — it encodes motivation and validates the story is worth building.</statement>
      <rationale>Stories without persona IDs cannot be traced to a validated user segment. Stories without "so that" cannot be evaluated for user value.</rationale>
    </rule>
  </section>

  <section title="Requirements">
    <rule>
      <statement>Functional requirements use "FR-N: The system shall [do X]" format. Cap at ~15 FRs per spec; larger features must be split. Keep implementation details out: no library names, DB schemas, or UI component names.</statement>
      <rationale>Specs mixing what and how box in engineers and are ignored. Implementation decisions belong in the design document, linked from the spec.</rationale>
    </rule>
  </section>

  <section title="Open Questions">
    <rule>
      <statement>Open Questions section must be non-empty. A spec with zero open questions signals false confidence. List technical unknowns, pending business decisions, and user research gaps with an owner per question.</statement>
      <rationale>LLMs in particular will "answer" every question rather than acknowledge uncertainty. Explicitly requiring a non-empty Open Questions section forces intellectual honesty.</rationale>
    </rule>
  </section>

  <section title="Length and Versioning">
    <rule>
      <statement>Target 1-3 pages (or ~15 FRs). Use Mini-Spec for features under one sprint. Track decisions made post-approval as a versioned addendum — never silently rewrite the original spec.</statement>
      <rationale>Beyond 5 pages, nobody reads the spec. Silent rewrites destroy the audit trail that later resolves scope disputes.</rationale>
    </rule>
  </section>

  <antipattern>
    <description>Spec reviewed and approved without Non-Goals or Open Questions sections.</description>
    <reason>Without Non-Goals, every engineer draws their own scope boundary. Without Open Questions, unstated assumptions become untracked risks.</reason>
  </antipattern>

  <see-also>
    <link path="content/02-requirements.xml"/>
    <link path="templates/prd.md"/>
    <link path="templates/mini-spec.md"/>
    <link path="templates/validate-spec.py"/>
  </see-also>
</text>
