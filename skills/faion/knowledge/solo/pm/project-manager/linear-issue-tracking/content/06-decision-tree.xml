<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Linear Issue Tracking (Project Manager) Decision Tree" version="1.0.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable inputs to a rule from `01-core-rules.xml`. MANDATORY for every methodology — even flat ones — per F-066 DoD.</summary>

  <decision-tree>
    <root-question id="q-root">Linear workspace exists AND PM has admin/triage rights?</root-question>
    <branch when="linear_workspace == true AND pm_admin_access == true">
      <question id="q-sub">Cycle cadence agreed AND status comms expected?</question>
      <branch when="cycle_cadence_set == true AND status_comms_expected == true">
        <conclusion ref="r1-daily-triage"/>
      </branch>
      <branch when="NOT (cycle_cadence_set == true AND status_comms_expected == true)">
        <conclusion ref="skip-this-methodology"/>
      </branch>
    </branch>
    <branch when="linear_workspace == false OR pm_admin_access == false">
      <conclusion ref="skip-this-methodology"/>
    </branch>
  </decision-tree>

  <example trace="apply-path">
    All preconditions satisfied → conclusion r1-daily-triage.
  </example>
</text>
