<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="clickup-setup Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for clickup-setup. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Limit hierarchy depth to 4 levels: Workspace (organization) → Space (department/team) → Folder (project/initiative) → List (phase/workflow stage). Never create Folders inside Folders — it creates navigation complexity that cannot be displayed in most views.</statement>
      <rationale source="v1-source">Every hierarchy level adds a navigation click. Deeper hierarchies increase onboarding friction and create permission management complexity.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use Spaces to separate teams with different workflows and permission requirements. Spaces have independent status sets and ClickApp configurations — never share a Space between teams with incompatible workflows.</statement>
      <rationale source="v1-source">Mixing teams in one Space forces a single status workflow on all of them. Separate Spaces allow Engineering to use Sprints while Marketing uses a content calendar without conflict.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Enable these ClickApps at workspace level: Custom Fields, Priorities (Urgent/High/Normal/Low), Multiple Assignees. Enable Sprints and Time Tracking at Space level where applicable. Enabling Sprints workspace-wide forces sprint discipline on spaces that do not practice it (e.g., Support).</statement>
      <rationale source="v1-source">Workspace-level ClickApps apply to all Spaces. Space-level ClickApps are opt-in — enabling sprint management only where needed reduces noise elsewhere.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Create custom fields at the highest applicable level: Workspace if used everywhere, Space if team-specific, Folder if project-specific. Never create the same field twice at different levels — use the inherited version.</statement>
      <rationale source="v1-source">Duplicate fields at multiple hierarchy levels create data inconsistency when filters aggregate across levels.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Use dropdown type for Sprint and Component fields — not free text. This prevents typos, enables filtering, and allows automation rules to trigger on specific values.</statement>
      <rationale source="v1-source">Free-text sprint names like "Sprint1", "sprint 1", "Sprint 01" break filters and automation. Dropdowns enforce consistent values.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Keep the total number of visible custom fields per view to 8 or fewer. Archive custom fields that are not actively used.</statement>
      <rationale source="v1-source">Too many fields create cognitive load and slow down task entry. Fields not used in automation or reporting have no value in the main view.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Define the status workflow at Space level, not List level. A consistent status set across all Lists in a Space enables cross-list filtering and automation. Typical development workflow: Backlog → Ready for Dev → In Development → Code Review → QA Testing → Complete / Won't Do.</statement>
      <rationale source="v1-source">Per-List status sets make cross-list "show all In Development tasks" queries impossible. Space-level statuses are queryable across all Folders and Lists.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Start with 3-5 automation rules and add more only as specific manual friction points are identified. Document every automation rule in a team handbook — undocumented automations confuse new team members who observe unexpected behavior.</statement>
      <rationale source="v1-source">Automation bloat creates circular triggers and unexpected state changes. Each rule should solve a confirmed pain point, not a hypothetical one.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Test every automation rule on a test task before enabling it in production. Automation loops (rule A triggers rule B which triggers rule A) cause infinite task state changes. Check for circular dependencies before enabling.</statement>
      <rationale source="v1-source">ClickUp automation loops are not automatically detected. They cause task status thrashing and notification spam that is difficult to diagnose after the fact.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Create purpose-specific views rather than general-purpose catch-all views: List view for detailed task management (all fields visible), Board for kanban execution (grouped by status), Gantt for timeline planning (with dependencies), Workload for capacity planning, Dashboard for sprint metrics. Each view should answer a specific question.</statement>
      <rationale source="v1-source">General-purpose views become cluttered. Purpose-specific views load faster (fewer fields) and train team members on which view to use for each workflow step.</rationale>
    </rule>
    <rule id="r11" testable="true" depth="essential">
      <statement>Add Workload view whenever Sprint ClickApp is enabled. Workload requires Time Estimates on tasks to show capacity — set time estimates as a required field when Sprints are active.</statement>
      <rationale source="v1-source">Capacity planning without time estimates is guesswork. Workload view is only useful when estimates are populated consistently.</rationale>
    </rule>
    <rule id="r12" testable="true" depth="essential">
      <statement>For GitHub integration: enable branch naming convention "clickup-{task_id}-{task_name}" to create automatic task-to-PR links. Set auto status to "Complete" on PR merge. Store the integration webhook URL in .aidocs/memory/ — it changes when re-authenticated.</statement>
      <rationale source="v1-source">Branch name conventions enable ClickUp to detect linked PRs automatically without manual linking by developers.</rationale>
    </rule>
    <rule id="r13" testable="true" depth="essential">
      <statement>For Slack integration: notify on status changes and comment additions, not on every field change. Over-notification causes recipients to mute the channel within days.</statement>
      <rationale source="v1-source">Signal-to-noise ratio is the primary failure mode of PM tool Slack integrations. Scope notifications to meaningful state changes only.</rationale>
    </rule>
  </section>
</text>
