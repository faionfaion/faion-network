<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Nomad Low-Energy Mode Decision Tree" version="1.0.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable inputs to a rule from `01-core-rules.xml`. MANDATORY for every methodology — even flat ones — per F-066 DoD.</summary>

  <decision-tree>
    <root-question id="q-root">Travel / illness / family ≥3 consecutive low-capacity days planned?</root-question>
    <branch when="low_capacity_days_planned &gt;= 3">
      <question id="q-sub">Normal-mode rhythm defined AND escalation contact available?</question>
      <branch when="normal_mode_defined == true AND escalation_contact_available == true">
        <conclusion ref="r1-two-hour-cap"/>
      </branch>
      <branch when="NOT (normal_mode_defined == true AND escalation_contact_available == true)">
        <conclusion ref="skip-this-methodology"/>
      </branch>
    </branch>
    <branch when="low_capacity_days_planned &lt; 3">
      <conclusion ref="skip-this-methodology"/>
    </branch>
  </decision-tree>

  <example trace="apply-path">
    All preconditions satisfied → conclusion r1-two-hour-cap.
  </example>
</text>
