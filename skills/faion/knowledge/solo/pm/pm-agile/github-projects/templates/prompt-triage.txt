Using the GitHub GraphQL API, query project <PROJECT_NUMBER> in org <ORG>
for all items where the Status field is "Backlog" and the Estimate field is null.
For each item return: {number, title, repository, labels, assignees}.
Do not modify any items.

After fetching, for each unestimated item, suggest a Priority (P0/P1/P2/P3) based on:
- label "critical" or "urgent" -> P0
- label "bug" with no workaround mentioned -> P1
- default -> P2

Return JSON array:
[
  {
    "item_id": "node-id",
    "number": 123,
    "title": "...",
    "suggested_priority": "P1",
    "reason": "..."
  }
]

Await human approval before updating any Priority fields.
