Query Notion database <TASKS_DB_ID> for tasks where:
- Sprint relation is empty (not yet assigned to any sprint)
- Status not in [Done, Cancelled]
Include: page_id, task title, assignee, status, estimate (number), priority (select).
Return JSON array. Read-only.

Given the backlog JSON and team capacity: <capacity_points>
Select tasks for the sprint:
1. Sort by Priority (Urgent > High > Medium > Low)
2. Include tasks until total Estimate >= capacity * 0.85
3. Do not split a task — include fully or skip
4. Skip tasks with missing Estimate (flag them separately)

Return:
{
  "selected": [{"page_id": "...", "title": "...", "estimate": N}],
  "total_estimate": N,
  "missing_estimates": ["page_id_1", "page_id_2"]
}

Await human approval before calling assign_to_sprint() with the selected page IDs.
