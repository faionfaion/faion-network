Given sprint metrics JSON: <metrics>
{
  "total": <N>,
  "done": <N>,
  "blocked": <N>,
  "in_progress": <N>
}
and stakeholder list: <stakeholders>

Render a Weekly Status Report per the weekly-status-report.md template.
Compute overall status:
- On Track: >= 85% complete
- At Risk: 70-84% complete
- Off Track: < 70% complete

If blocked > 3, add a "Blockers Require Attention" section listing blocked items.
Keep executive summary under 2 sentences.
Do NOT render the report if total == 0 — return a data quality error instead:
{"error": "Sprint query returned 0 items — check data quality before proceeding."}
