<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="health-checks-autoheal Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for health-checks-autoheal. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Always return 503 when any critical dependency is unhealthy, not 200 with an error body.</statement>
      <rationale source="v1-source">Load balancers and health check scripts check HTTP status codes, not body content. A 200 with {"status":"error"} will be treated as healthy.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Send watchdog notification at WatchdogSec/2 interval, not WatchdogSec. This provides a safety margin before systemd triggers the kill.</statement>
      <rationale source="v1-source">If the notification thread hangs right after sending at exactly WatchdogSec, the next cycle may be too late. Sending at half the interval provides one missed cycle before restart.</rationale>
    </rule>
  </section>
</text>
