<?xml version="1.0" encoding="UTF-8"?>
<text>
  <section name="modern-cli-tools">
    <summary>Tool-by-tool overview of the modern CLI replacement stack, Ubuntu naming conflicts, and installation commands for Ubuntu 24.04.</summary>

    <section name="tool-reference">
      <list>
        <item>bat — replaces cat: syntax highlighting, git change markers, line numbers, automatic paging</item>
        <item>eza — replaces ls: color-coded permissions, git status column, icons, tree view</item>
        <item>fd — replaces find: faster, respects .gitignore, simpler syntax, Unicode-aware</item>
        <item>ripgrep (rg) — replaces grep: multi-threaded, respects .gitignore, better output formatting</item>
        <item>delta — replaces git diff output: side-by-side view, syntax highlighting, line numbers</item>
        <item>starship — replaces PS1 prompt: cross-shell, git-aware, language version display, fast</item>
        <item>zoxide — replaces cd: frecency-based jump, interactive mode with fzf</item>
        <item>fzf — fuzzy finder: Ctrl+R history, Ctrl+T file picker, Alt+C directory jump</item>
        <item>btop — replaces top/htop: full UI with process tree, network, disk, beautiful layout</item>
        <item>duf — replaces df: color-coded table, filters, human-readable by default</item>
        <item>dust — replaces du: visual tree sorted by size, respects .gitignore</item>
      </list>
    </section>

    <section name="ubuntu-naming-conflicts">
      <rule>
        <statement>On Ubuntu/Debian, bat is installed as batcat and fd is installed as fdfind; create /usr/local/bin symlinks to avoid breaking scripts and fzf integrations.</statement>
        <rationale>The name conflicts exist because bat conflicts with the Ubuntu bat (Bytes Around Text) command and fd conflicts with an older package. Without symlinks, all scripts expecting bat/fd by name fail silently.</rationale>
      </rule>

      <example type="good">
        <code lang="bash"><![CDATA[
# Install from apt (Ubuntu 24.04)
sudo apt install -y bat fd-find fzf ripgrep btop duf

# Fix Ubuntu naming conflicts
sudo ln -sf /usr/bin/batcat /usr/local/bin/bat
sudo ln -sf /usr/bin/fdfind /usr/local/bin/fd

# Verify
bat --version
fd --version
        ]]></code>
      </example>
    </section>

    <section name="github-release-installs">
      <summary>Tools not in Ubuntu 24.04 apt repos or where the apt version is too old: eza, delta, starship, zoxide, dust.</summary>

      <example type="good">
        <code lang="bash"><![CDATA[
# eza (latest via PPA)
sudo mkdir -p /etc/apt/keyrings
wget -qO- https://raw.githubusercontent.com/eza-community/eza/main/deb.asc \
    | sudo gpg --dearmor -o /etc/apt/keyrings/gierens.gpg
echo "deb [signed-by=/etc/apt/keyrings/gierens.gpg] http://deb.gierens.de stable main" \
    | sudo tee /etc/apt/sources.list.d/gierens.list
sudo apt update && sudo apt install -y eza

# delta (git diff)
DELTA_VER="0.18.2"
wget -q "https://github.com/dandavison/delta/releases/download/${DELTA_VER}/git-delta_${DELTA_VER}_amd64.deb" -O /tmp/delta.deb
sudo dpkg -i /tmp/delta.deb && rm /tmp/delta.deb

# starship (prompt)
curl -sS https://starship.rs/install.sh | sh -s -- -y

# zoxide (smart cd)
curl -sSfL https://raw.githubusercontent.com/ajeetdsouza/zoxide/main/install.sh | sh
        ]]></code>
      </example>
    </section>

    <reference path="templates/install-cli-tools.sh">Idempotent install script for all tools on Ubuntu 24.04 with existence checks</reference>
  </section>
</text>
