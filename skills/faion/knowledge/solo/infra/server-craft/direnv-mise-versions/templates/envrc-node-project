# .envrc — Node.js project environment
# Run `direnv allow` after creating or modifying this file.

# Activate mise-managed Node.js (reads .tool-versions or mise.toml)
use mise

# Load secrets
dotenv_if_exists .env

# Reload on dependency file changes
watch_file package.json

# Local scripts on PATH
PATH_add node_modules/.bin
