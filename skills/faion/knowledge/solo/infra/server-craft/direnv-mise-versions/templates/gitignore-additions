# .gitignore additions for direnv + mise projects

# direnv — generated cache (not config, .envrc is committed)
.direnv/
.envrc.local

# mise — local overrides (not the shared mise.toml)
.mise.local.toml

# Python virtualenv created by `layout python-venv`
.venv/
venv/
