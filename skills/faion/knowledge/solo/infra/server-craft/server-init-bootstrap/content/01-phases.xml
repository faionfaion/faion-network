<text id="bootstrap-phases" title="Server Init Bootstrap: 5-Phase Sequence">

  <summary>
    5 phases in strict order: (1) access+users, (2) system identity, (3) packages+tools, (4) security hardening, (5) services foundation. Each phase has a verification step. Never skip to phase 4 without completing phase 1 test — SSH lockout is unrecoverable without console access.
  </summary>

  <section title="Phase 1: Access and Users">
    <p>First 5 minutes. SSH as root, create non-root user, deploy SSH key, TEST login as new user before any other action.</p>
    <rule>
      <statement>Always test SSH as the new user in a separate terminal BEFORE disabling root login.</statement>
      <rationale>A misconfigured sshd_config (wrong AllowUsers, wrong key) will lock you out permanently unless you have the server provider's console. Testing first guarantees a recovery path.</rationale>
    </rule>
    <list>
      <item>SSH as root → create non-root user with sudo</item>
      <item>Deploy SSH public key to new user's authorized_keys</item>
      <item>Open a new terminal, test: `ssh newuser@server-ip`</item>
      <item>Verify sudo works: `sudo whoami` → should output "root"</item>
      <item>Only now: disable root SSH login and password auth</item>
    </list>
  </section>

  <section title="Phase 2: System Identity">
    <list>
      <item>Set hostname: `hostnamectl set-hostname server-name`</item>
      <item>Update /etc/hosts: add `127.0.1.1 server-name`</item>
      <item>Set timezone: `timedatectl set-timezone Europe/Lisbon`</item>
      <item>Set locale: `locale-gen en_US.UTF-8 &amp;&amp; update-locale LANG=en_US.UTF-8`</item>
      <item>Enable NTP: `timedatectl set-ntp true`</item>
    </list>
  </section>

  <section title="Phase 3: Packages and Tools">
    <list>
      <item>`apt update &amp;&amp; apt upgrade -y` — apply all pending updates first</item>
      <item>Install essentials: build-essential, curl, git, tmux, htop, jq, rsync, ncdu</item>
      <item>Install Python dev: python3-dev, python3-venv, libpq-dev</item>
      <item>Install mise: `curl https://mise.run | sh`</item>
      <item>Install direnv: `apt install -y direnv`</item>
      <item>Add hooks to .bashrc: mise before direnv</item>
    </list>
  </section>

  <section title="Phase 4: Security Hardening">
    <list>
      <item>Configure UFW: deny incoming, allow SSH port, allow 80/443, enable</item>
      <item>Install fail2ban, configure jail.local (3 retries, 1h ban)</item>
      <item>Install unattended-upgrades, enable auto-reboot at 4 AM</item>
      <item>Apply sshd_config hardening: no root, no password, MaxAuthTries 3</item>
      <item>Test SSH config: `sshd -t` before reloading</item>
    </list>
  </section>

  <section title="Phase 5: Services Foundation">
    <rule>
      <statement>Enable loginctl linger for the service user before creating any systemd user services.</statement>
      <rationale>Without linger, user services (--user) only run during an active login session. They stop when you log out and do not start at boot. Linger makes them persistent.</rationale>
    </rule>
    <list>
      <item>`loginctl enable-linger username` — services survive logout and start at boot</item>
      <item>`mkdir -p ~/.config/systemd/user` — create user service directory</item>
      <item>Install Docker if needed (bind all ports to 127.0.0.1)</item>
      <item>Configure journal size: `SystemMaxUse=500M` in journald.conf</item>
    </list>
  </section>

  <see-also>
    <link path="templates/bootstrap.sh">Interactive full bootstrap script</link>
    <link path="templates/cloud-init.yml">cloud-init user-data for automated provisioning</link>
    <link path="templates/verify-bootstrap.sh">Post-bootstrap verification checklist</link>
  </see-also>

</text>
