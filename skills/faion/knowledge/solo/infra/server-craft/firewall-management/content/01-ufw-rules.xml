<text id="ufw-rules" title="Firewall Management: UFW Rules">

  <summary>
    UFW on Ubuntu 24.04 uses nftables as its backend. Default policy is deny-all incoming, allow outgoing. Add SSH rule before enabling UFW — forgetting it locks you out immediately. Rate limit SSH with `ufw limit`; use fail2ban for finer control.
  </summary>

  <section title="Default Policies">
    <rule>
      <statement>Set deny incoming, allow outgoing, and deny routed before adding any allow rules.</statement>
      <rationale>Routed traffic must be explicitly denied or Docker VPN forwarding can pass through unfiltered. The order matters — defaults apply after user rules.</rationale>
    </rule>
    <example type="good">
      <code lang="bash"><![CDATA[
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw default deny routed   # blocks forwarded packets (Docker/VPN)
      ]]></code>
    </example>
  </section>

  <section title="Common Rules">
    <example type="good">
      <code lang="bash"><![CDATA[
# SSH — always first, before enable
sudo ufw limit 22022/tcp comment 'SSH'

# Web traffic
sudo ufw allow 80/tcp  comment 'HTTP'
sudo ufw allow 443/tcp comment 'HTTPS'

# WireGuard
sudo ufw allow 51820/udp comment 'WireGuard'

# Show rules
sudo ufw status numbered
sudo ufw status verbose
      ]]></code>
    </example>
    <rule>
      <statement>Always add the SSH rule before running `ufw enable`.</statement>
      <rationale>UFW applies rules immediately on enable. Missing SSH rule = instant lockout from the server.</rationale>
    </rule>
  </section>

  <section title="Rate Limiting SSH">
    <p>`ufw limit` allows max 6 new connections per 30 seconds from a single IP. It cannot be tuned — for finer control use fail2ban.</p>
    <example type="good">
      <code lang="bash"><![CDATA[
sudo ufw limit 22022/tcp comment 'SSH (rate limited)'
      ]]></code>
    </example>
  </section>

  <section title="UFW Logging">
    <p>Use `low` for production — logs blocked packets without flooding disk. Logs go to /var/log/ufw.log via rsyslog.</p>
    <example type="good">
      <code lang="bash"><![CDATA[
sudo ufw logging low
sudo tail -f /var/log/ufw.log
sudo journalctl -k | grep "UFW BLOCK"
      ]]></code>
    </example>
  </section>

  <see-also>
    <link path="content/02-docker-security.xml">Docker UFW bypass and solutions</link>
    <link path="templates/ufw-setup-webserver.sh">Standard web server UFW setup</link>
  </see-also>

</text>
