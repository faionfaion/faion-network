<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="firewall-management Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for firewall-management. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Set deny incoming, allow outgoing, and deny routed before adding any allow rules.</statement>
      <rationale source="v1-source">Routed traffic must be explicitly denied or Docker VPN forwarding can pass through unfiltered. The order matters — defaults apply after user rules.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Always add the SSH rule before running ufw enable.</statement>
      <rationale source="v1-source">UFW applies rules immediately on enable. Missing SSH rule = instant lockout from the server.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Never publish Docker ports to 0.0.0.0 on a production server. Always bind to 127.0.0.1.</statement>
      <rationale source="v1-source">Binding to 0.0.0.0 publishes the port to all interfaces, bypassing UFW entirely. 127.0.0.1 restricts access to the host loopback — only processes on the same machine can connect.</rationale>
    </rule>
  </section>
</text>
