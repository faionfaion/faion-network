<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="fail2ban-setup Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for fail2ban-setup. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use backend=systemd for services that log to journald (sshd on Ubuntu 24.04). Use backend=auto for services with explicit log files (nginx). Never set logpath when using backend=systemd — it is silently ignored.</statement>
      <rationale source="v1-source">The systemd backend reads the journal directly; there is no log file path. Including logpath with backend=systemd triggers a warning and may prevent the jail from starting correctly.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Never edit jail.conf or fail2ban.conf. Create all customizations in jail.local (global defaults) and jail.d/*.conf (per-jail drop-ins). These survive package upgrades.</statement>
      <rationale source="v1-source">jail.conf is overwritten on package upgrade. jail.local and jail.d/ files are preserved. This mirrors the sshd_config.d/ pattern.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Set banaction=nftables and banaction_allports=nftables[type=allports] in [DEFAULT]. Ubuntu 24.04 uses nftables as the default firewall backend; mixing iptables and nftables rules causes conflicts.</statement>
      <rationale source="v1-source">fail2ban defaults to iptables on older systems. Ubuntu 24.04 uses nftables via UFW. Setting the nftables banaction explicitly ensures fail2ban's bans integrate correctly with the existing ruleset.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use fail2ban-client reload (not restart) after config changes. reload preserves existing ban state; restart clears all bans.</statement>
      <rationale source="v1-source">Restarting fail2ban unbans all currently banned IPs, giving active attackers an immediate window to reconnect.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Always whitelist localhost, Docker networks (172.16.0.0/12, 10.0.0.0/8), your static IP, VPN subnet, and all Cloudflare IP ranges BEFORE enabling any jail. A misconfigured whitelist bans you immediately on first nginx scan.</statement>
      <rationale source="v1-source">fail2ban bans based on log patterns without geographic awareness. Your own monitoring tools, Docker containers, and Cloudflare proxy IPs all generate traffic that matches scanner patterns.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Configure SSH jail with maxretry=3, findtime=600, bantime=3600. Use filter=sshd[mode=aggressive] to catch invalid user attempts in addition to failed passwords.</statement>
      <rationale source="v1-source">3 failures in 10 minutes is aggressive enough to block brute-force (which tries hundreds per minute) while allowing legitimate multi-key authentication failures without permanent lockout.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Configure the recidive jail: if an IP is banned 3 times in 24 hours, ban it for 1 week on ALL ports using banaction=nftables[type=allports]. recidive requires /var/log/fail2ban.log to exist — logtarget must not be set to journal-only.</statement>
      <rationale source="v1-source">Short bans (1h) do not deter persistent scanners. Recidive escalates to a week-long all-ports block for IPs that return after being banned, effectively removing persistent attackers.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Enable bantime.increment=true in [DEFAULT] with bantime.factor=2 and bantime.maxtime=604800 to automatically double ban time for repeat offenders within the same jail, up to 1 week maximum.</statement>
      <rationale source="v1-source">Linear ban times do not deter automated scanners. Exponential escalation per IP makes repeated attempts progressively more costly without manual intervention.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>Place custom filters in /etc/fail2ban/filter.d/&lt;name&gt;.conf with a [Definition] section containing failregex patterns. Use &lt;HOST&gt; as the placeholder for the IP address. Test every filter with fail2ban-regex before enabling the jail.</statement>
      <rationale source="v1-source">An unmatched filter silently produces zero bans — there is no error. fail2ban-regex validates the pattern against real logs before deployment, showing match count and sample matched lines.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Run fail2ban-regex against the actual log file before enabling any jail. Check that the match count is non-zero and that the IP capture group works (shows actual IPs, not empty). A zero exit code does not mean matches were found — check stdout.</statement>
      <rationale source="v1-source">fail2ban-regex returns exit 0 even if no matches are found. Agents and operators must read the match count line explicitly.</rationale>
    </rule>
    <rule id="r11" testable="true" depth="essential">
      <statement>When nginx is behind Cloudflare, configure nginx real_ip module with Cloudflare's IP ranges and real_ip_header CF-Connecting-IP so nginx logs the actual client IP. Without this, fail2ban bans Cloudflare proxy IPs, blocking all traffic through that proxy.</statement>
      <rationale source="v1-source">Cloudflare forwards traffic from a small set of well-known IP ranges. If nginx logs the Cloudflare proxy IP instead of the real client IP, fail2ban will ban Cloudflare itself, disrupting all CDN-proxied traffic.</rationale>
    </rule>
  </section>
</text>
