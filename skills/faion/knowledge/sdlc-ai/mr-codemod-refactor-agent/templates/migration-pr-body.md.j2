<!--
purpose: PR body documenting an automated codemod migration — from/to symbol, scope, tool, stats, risk, reviewer notes.
consumes: nothing beyond the codemod run's own stats
produces: migration PR body artefact
depends-on: content/02-output-contract.xml
token-budget-impact: ~150 tokens when filled
-->

<!-- AUTO-MIGRATION-BODY — managed by codemod runner; edit only below HUMAN line -->

## Migration
- From: `{from_symbol_or_version}`
- To: `{to_symbol_or_version}`
- Scope: `{logical_group}`

## Codemod
- Tool: `{tool}`            <!-- jscodeshift | ts-morph | libcst | semgrep | codemod.com -->
- Codemod ID: `{codemod_path}`
- Run: `{cli_invocation}`

## Stats
| Metric | Value |
|--------|-------|
| Files touched | {files_count} |
| Call-sites rewritten | {call_sites} |
| LLM tail-pass files | {tail_files} |
| Idempotence check | passed |

## Risk
{risk_level} — {risk_one_liner}

## Out of scope
- {out_of_scope_1}
- {out_of_scope_2}

<!-- HUMAN — edits below survive re-runs -->

## Reviewer notes
