<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Pre-Commit Hooks as the Non-Negotiable Merge Floor Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">repo_has_code?</root-question>

    <branch when="has_code == false">
      <conclusion ref="skip-this-methodology" verdict="skip — docs-only"/>
    </branch>
    <branch when="has_code == true">
      <question id="q-2">framework_installable?</question>
        <branch when="installable == false">
          <conclusion ref="skip-this-methodology" verdict="skip — env blocked"/>
        </branch>
        <branch when="installable == true">
          <conclusion ref="framework-committed" verdict="wire pre-commit + 4 hook categories"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="has_code=true → installable=true → framework-committed → .pre-commit-config.yaml + make hooks + CI mirror">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
