<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Go → TypeScript DTO Contract via tygo Decision Tree" version="1.1.0" est_tokens="500" depth="essential">
  <summary>Routing tree mapping observable preconditions to a rule from 01-core-rules.xml. Every leaf is a conclusion ref bound to a rule id.</summary>

  <decision-tree>
    <root-question id="q-root">frontend_language?</root-question>

    <branch when="frontend_lang != typescript">
      <conclusion ref="skip-this-methodology" verdict="skip — tygo TS-only"/>
    </branch>
    <branch when="frontend_lang == typescript">
      <question id="q-2">grpc_protoc_already_covers_dto?</question>
        <branch when="grpc_covers == true">
          <conclusion ref="skip-this-methodology" verdict="skip — protoc already does this"/>
        </branch>
        <branch when="grpc_covers == false">
          <conclusion ref="go-is-source-of-truth" verdict="adopt tygo + CI diff gate"/>
        </branch>
    </branch>

  </decision-tree>

  <example trace="frontend=TS → grpc=false → go-is-source-of-truth → tygo.yaml + CI gate">
    Canonical traversal from root question through branches to a single conclusion.
  </example>
</text>
