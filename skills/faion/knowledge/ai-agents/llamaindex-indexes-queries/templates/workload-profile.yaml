# purpose: input contract for llamaindex-indexes-queries methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

data_shape: TODO          # fill with concrete value per the driver definition
hop_depth_max: TODO          # fill with concrete value per the driver definition
query_types: TODO          # fill with concrete value per the driver definition
