# purpose: minimum-viable task spec for plan-execute-vs-react methodology
# consumes: nothing — runnable as-is
# produces: deterministic plan-execute decision-record
# depends-on: task-spec.yaml shape
# token-budget-impact: ~70 tokens

goal: "Deploy a new feature to staging"
drivers:
  known_substeps: 5
  horizon: 5
  adaptability_needed: false
  audit_required: true
  mixed_predictability: false

max_turns_budget: 1
