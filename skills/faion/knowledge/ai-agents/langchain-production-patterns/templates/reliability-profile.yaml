# purpose: input contract for hardening
# consumes: human author
# produces: typed profile for the decision tree
# depends-on: none
# token-budget-impact: ~150 tokens
target_uptime: 0.999       # 0..1
latency_budget_ms: 1500
providers:                 # ordered: first is primary
  - anthropic
  - openai
dep_policy: locked         # locked | floating
notes: |
  Tier-1 prod chain; lockfile committed; fallback to OpenAI on 5xx.
