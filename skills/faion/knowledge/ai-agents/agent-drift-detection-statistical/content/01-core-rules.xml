<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="Agent Drift Detection (Statistical) Core Rules" version="1.1.0" est_tokens="550" depth="essential">
  <summary>6 testable rules every application of agent-drift-detection-statistical enforces. Each rule has a statement, a rationale anchored to industry practice, and a source citation.</summary>
  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Pass-rate control chart uses CUSUM with k=0.5 and h=4 (standard EWMA defaults); alert when CUSUM exceeds h.</statement>
      <rationale source="Statistical process control / Montgomery 2020">Single-day pass-rate drops are noisy; CUSUM accumulates small drifts that single-threshold detectors miss.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Output-distribution drift is measured by KL divergence over a categorical bucketing of the output (intent, schema-key set, length bucket). Alert at KL ≥ 0.1 vs baseline.</statement>
      <rationale source="ML monitoring practice 2026">KL surfaces distribution shifts before any single metric crosses a threshold.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Judge-score regression uses a paired t-test against baseline; significance threshold p&lt;0.05 with Bonferroni correction across metrics.</statement>
      <rationale source="Statistics 101">Without correction, the 'false alarm' rate compounds across N metrics; Bonferroni keeps family-wise error bounded.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Drift alert severity: yellow (KL ≥ 0.05 OR CUSUM ≥ 2), red (KL ≥ 0.1 OR CUSUM ≥ 4 OR p&lt;0.01 regression).</statement>
      <rationale source="Industry practice 2026">Severity bands route to different responses; binary alerts produce alarm fatigue.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Every alert triggers a retest with a 2x sample size; if retest is also red, freeze deploys; if yellow, course-correct without freeze.</statement>
      <rationale source="Industry practice 2026">Single-run alerts are noisy; double-sample retest cuts false positives ~50%.</rationale>
    </rule>
    <rule id="skip-methodology" testable="true" depth="essential">
      <statement>When the product is pre-MVP and no eval suite exists — fewer than 30 ground-truth examples and no deploy / rollback / freeze decision keyed to a score — the team MUST NOT configure the CUSUM, KL-divergence or paired-t-test detectors. Instead it captures every run as a trace with a manual-review verdict (pass / fail / unsure) and re-enters the tree only when the labelled set reaches 30 examples and one production decision is gated on the score.</statement>
      <rationale source="Montgomery, Introduction to Statistical Quality Control">A control chart needs a baseline estimated from an in-control sample; with n below 30 the pass-rate estimate is so wide that CUSUM with h=4 alarms on ordinary variation, and a paired t-test on a handful of judge scores has almost no power, so the detectors emit red and yellow bands that nobody can act on. The manual-review log is what becomes the baseline once the suite exists.</rationale>
    </rule>
  </section>
  <see-also>
    <link path="content/02-output-contract.xml" />
    <link path="content/03-failure-modes.xml" />
    <link path="content/06-decision-tree.xml" />
  </see-also>
</text>