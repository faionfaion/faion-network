{
  "__faion_header__": {
    "purpose": "TBD-template-header",
    "consumes": "input from methodology",
    "produces": "output artefact",
    "depends-on": "01-core-rules.xml",
    "token-budget-impact": "small"
  },
  "name": "get_full_result",
  "description": "Load the full body of a previously executed tool call. Call ONLY when the manifest preview is insufficient to make the next decision. The body is loaded once and persists in context — fetching is irreversible.",
  "input_schema": {
    "type": "object",
    "required": ["execution_id"],
    "properties": {
      "execution_id": {
        "type": "string",
        "description": "The execution_id from a prior tool-result manifest."
      },
      "range": {
        "type": "object",
        "description": "Optional byte/line slice to load only part of the body.",
        "properties": {
          "lines_from": {"type": "integer", "minimum": 1},
          "lines_to": {"type": "integer", "minimum": 1}
        }
      }
    }
  }
}
