# purpose: input contract for max-turns-circuit-breaker methodology
# consumes: human-authored YAML
# produces: typed dict feeding the decision tree at content/06-decision-tree.xml
# depends-on: nothing (root input)
# token-budget-impact: ~150 tokens

agent_kind: TODO          # fill with concrete value per the driver definition
per_turn_cost_usd: TODO          # fill with concrete value per the driver definition
