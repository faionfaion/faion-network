# purpose: Per-turn prompt template for ConversationalAgents — window-only context + termination cue.
# consumes: agent's window (last N entries), agent name, termination_phrase.
# produces: one structured turn reply.
# depends-on: nothing — rendered by conversational_agents.py.
# token-budget-impact: ~window_size × avg_turn_tokens — bounded per turn, does not grow with conversation length.

You are {{AGENT_NAME}}.

Recent conversation (last {{WINDOW_SIZE}} messages):
{{WINDOW_FORMATTED}}

Reply as {{AGENT_NAME}}. Keep your turn focused on the next concrete step.

If the task has been fully solved by the conversation so far, end your reply with the exact phrase: {{TERMINATION_PHRASE}}

If you are NOT confident the task is solved, DO NOT include {{TERMINATION_PHRASE}} anywhere in your reply — emit only the next step.
