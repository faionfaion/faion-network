<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="User Research at Scale Procedure" version="1.1.1" est_tokens="1700" depth="essential">
  <summary>Step-by-step procedure to apply the User Research at Scale methodology end-to-end. Each step lists input, action, output, and an optional decision gate. Folds in two former files: Nine-stage pipeline, ten-subagent table with model assignments, and the frozen-codebook pattern for preventing theme drift. CLI tools for transcription and analysis, platform service landscape, and operational best practices for large-scale research pipelines. The frozen-codebook pattern and best practices are rules in `content/01-core-rules.xml`.</summary>
  <section title="Procedure">
    <step n="1" name="intake">
      <input>Research requests</input>
      <action>Classify by study type + segment + tier; queue.</action>
      <output>Intake queue.</output>
    </step>
    <step n="2" name="sampling">
      <input>Intake + segment plan</input>
      <action>Stratified sampling design + recruit batch sizes.</action>
      <output>Recruit plan.</output>
    </step>
    <step n="3" name="instrument-and-collect">
      <input>Recruit plan + tools</input>
      <action>Run sessions / unmoderated tests; record consent + transcripts.</action>
      <output>Raw session data.</output>
      <decision-gate>Refuse to proceed without consent records.</decision-gate>
    </step>
    <step n="4" name="transcribe">
      <input>Raw audio/video</input>
      <action>Run vendor transcription; strip interviewer turns.</action>
      <output>Speaker-attributed transcripts.</output>
    </step>
    <step n="5" name="code-frozen-book">
      <input>Transcripts + codebook.yaml</input>
      <action>Apply frozen codes; route novel themes to proposed_codes.</action>
      <output>Coded transcripts + proposed_codes queue.</output>
      <decision-gate>Reject auto-extension of codebook.</decision-gate>
    </step>
    <step n="6" name="synthesize">
      <input>Coded corpus</input>
      <action>Opus aggregates across studies; emits candidate findings with evidence counts.</action>
      <output>Candidate findings.</output>
    </step>
    <step n="7" name="hitl-review">
      <input>Candidate findings</input>
      <action>Human review; pass/reject per finding; merge accepted codes into book.</action>
      <output>HITL-passed findings + codebook delta.</output>
      <decision-gate>Refuse publish if evidence&lt;50 / sessions&lt;10 / segments&lt;2.</decision-gate>
    </step>
    <step n="8" name="publish-and-distribute">
      <input>HITL-passed findings</input>
      <action>Push to Dovetail/Notion with idempotency keys; emit weekly report.</action>
      <output>research-ops-report.md.</output>
    </step>
  </section>

  <section title="Pipeline at a glance">
    <p>Nine-stage view of the same procedure: stages 3-4 here are step 3 above, stage 8 is step 7, stage 9 is step 8.</p>
    <list>
      <item>1. intake: convert PM brief to study spec, screener, hypotheses</item>
      <item>2. sampling: build quotas, randomize, dedupe panel respondents</item>
      <item>3. instrumentation: configure unmoderated test scripts in Maze/Userlytics</item>
      <item>4. collection: execute tests and gather session recordings</item>
      <item>5. transcription: forced-aligned transcription with diarization, PII redaction</item>
      <item>6. coding: apply frozen codebook, tag quotes with codes and valence</item>
      <item>7. synthesis: cross-segment pattern mining, confidence-tiered findings</item>
      <item>8. review: human-in-the-loop checkpoint before publication</item>
      <item>9. publish: push themes/sentiment to Looker/Notion/Dovetail</item>
    </list>
  </section>

  <section title="Subagent table">
    <list>
      <item>research-intake (Sonnet): PM brief → study-spec.md + screener JSON</item>
      <item>panel-sampler (Haiku): quotas, randomize, dedupe → participant CSV</item>
      <item>protocol-runner (Haiku): execute unmoderated tests in Maze/Userlytics → session IDs</item>
      <item>transcript-cleaner (Haiku): strip filler, redact PII, normalize timestamps → clean VTT/JSON</item>
      <item>theme-coder (Sonnet): apply codebook, tag quotes with codes + valence → coded JSONL</item>
      <item>pattern-analyst (Opus): cross-segment clustering, confidence-tiered findings → findings draft</item>
      <item>quote-curator (Sonnet): representative + outlier quotes per finding → quote bank</item>
      <item>report-writer (Opus): executive synthesis, recommendations, confidence tiers → final report MD</item>
      <item>bias-auditor (Sonnet): leading phrasing, sample bias detection → audit notes</item>
      <item>dashboard-publisher (Haiku): push themes/sentiment to Looker/Notion/Dovetail → URLs</item>
    </list>
  </section>

  <section title="Tooling">
    <list>
      <item>whisperx: forced-aligned transcription with diarization, faster than vanilla Whisper</item>
      <item>pyannote-audio: speaker diarization when WhisperX is insufficient</item>
      <item>ffmpeg: slice session recordings into per-task clips for AI scoring</item>
      <item>yt-dlp: pull session videos from CDN URLs given by panel platforms</item>
      <item>jq / duckdb: query coded JSONL transcripts and platform exports</item>
      <item>pandas + sentence-transformers: embed and cluster open-ended survey responses</item>
      <item>bertopic: topic modeling on large response corpora</item>
      <item>dvc / lakefs: version control for transcripts + codebooks (audit trail)</item>
      <item>presidio: PII redaction before sending to external LLMs</item>
    </list>
  </section>

  <section title="Platform services">
    <list>
      <item>UserTesting (enterprise): REST API + webhook; largest panel, AI Insights add-on</item>
      <item>Userlytics (mid+): REST API; 2M+ panel, AI analytics, EU-friendly</item>
      <item>Maze (self-serve+): API + Zapier; bias detection, dynamic follow-ups</item>
      <item>dscout (enterprise): API; mobile ethnography and longitudinal diaries</item>
      <item>Dovetail (all tiers): REST + GraphQL; repository + AI tagging, codebook host</item>
      <item>Sprig (mid): API + JS SDK; in-product micro-surveys at session scale</item>
      <item>Hotjar / FullStory (mid): API; session replay + heatmaps for behavior layer</item>
      <item>Rally / User Interviews (mid): API; recruitment and scheduling automation</item>
    </list>
  </section>

  <reference path="templates/codebook.yaml">Frozen taxonomy with codes, valence defaults, examples, and segment axes.</reference>
  <reference path="templates/code-batch.sh">Batch-coding shell script with dedup and parquet output.</reference>
</text>
