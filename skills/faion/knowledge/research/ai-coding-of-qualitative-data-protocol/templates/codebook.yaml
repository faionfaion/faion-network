# purpose: Codebook skeleton
# consumes: inputs declared in AGENTS.md Prerequisites table
# produces: artefact conforming to content/02-output-contract.xml (ai-coding-of-qualitative-data-protocol)
# depends-on: content/01-core-rules.xml
# token-budget-impact: ~150-400 tokens when loaded as context
# codebook.yaml — qualitative coding codebook.
version: v1.2.0
codes:
  - id: pain.auth
    definition: User expresses frustration with authentication friction.
    examples:
      - "I gave up after the third login redirect"
      - "The auth boilerplate ate two days"
  - id: goal.shipfast
    definition: User wants to ship a working product quickly.
    examples:
      - "I need to ship in a week"
      - "Speed matters more than polish"
