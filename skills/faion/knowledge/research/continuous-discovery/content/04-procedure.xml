<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Continuous Discovery Procedure" version="1.1.1" est_tokens="1350" depth="essential">
  <summary>Step-by-step procedure to apply the Continuous Discovery methodology end-to-end. Each step lists input, action, output, and an optional decision gate. Folds in the former tools file: CLI tools, service landscape, best practices for OST hygiene, and agent failure modes specific to continuous discovery pipelines. Best practices are rules in `content/01-core-rules.xml`; failure modes are in `content/03-failure-modes.xml`.</summary>
  <section title="Procedure">
    <step n="1" name="daily-collect">
      <input>Analytics + tickets last 24h</input>
      <action>Haiku watchers append to insight-log.md and ticket-themes.md (atomic append + lock).</action>
      <output>Daily insight log entries.</output>
    </step>
    <step n="2" name="weekly-interview">
      <input>Recruit pool + interview guide</input>
      <action>Conduct 2-3 interviews; pipe transcripts to interview-synthesizer (sonnet) with interviewer-turn stripping.</action>
      <output>interview-notes/YYYY-MM-DD.md.</output>
    </step>
    <step n="3" name="weekly-assumption-test">
      <input>Open assumptions register + OST leaves</input>
      <action>assumption-tester (sonnet) designs smoke/fake-door/prototype/Mom-Test test; runs; records result rubric.</action>
      <output>assumption-results.md.</output>
      <decision-gate>Reject 'ship and measure' as a test type.</decision-gate>
    </step>
    <step n="4" name="bi-weekly-synth">
      <input>All weekly outputs</input>
      <action>discovery-synthesizer (opus) updates OST; scores opportunities freq x sev x addressability.</action>
      <output>Updated opportunity-solution-tree.md + delta log.</output>
      <decision-gate>Refuse to publish a 'finding' that fails N&gt;=5 / 2-source floor.</decision-gate>
    </step>
    <step n="5" name="monthly-review">
      <input>All synthesis outputs</input>
      <action>research-reviewer (opus) writes strategic memo + kill list + doubled-down list.</action>
      <output>research-report.md + archived-opportunities.md.</output>
      <decision-gate>Kill list mandatory; refuse if empty after &gt;=4 weeks.</decision-gate>
    </step>
  </section>

  <section title="Subagent pipeline">
    <list>
      <item>analytics-watcher (Haiku, daily): PostHog/Amplitude exports, GA4 anomalies → insight-log.md daily entry</item>
      <item>support-triage (Haiku, daily): Intercom/Zendesk/Linear tickets last 24h → theme cluster + count + severity</item>
      <item>competitor-monitor (Haiku, weekly): RSS, changelog scrapes, ProductHunt, G2 reviews → competitor-changes.md diff</item>
      <item>interview-scheduler (Sonnet, weekly): user segment criteria, recruit pool → recruit list + outreach drafts</item>
      <item>interview-synthesizer (Sonnet, per call): Otter/Fireflies/Looppanel transcript → tagged notes, JTBD pulls, quotes</item>
      <item>assumption-tester (Sonnet, weekly): open assumptions list, OST leaves → test design + result rubric</item>
      <item>discovery-synthesizer (Opus, bi-weekly): all weekly outputs → updated OST, opportunity scores</item>
      <item>research-reviewer (Opus, monthly): all synthesis outputs → strategic memo, kill list, doubled-down list</item>
    </list>
    <p>All agents write to .aidocs/product_docs/discovery/. The bi-weekly synthesizer reads the full week's outputs and updates the OST in place.</p>
  </section>

  <section title="Tooling">
    <list>
      <item>claude (Claude Code): orchestrate subagents via Task tool; slash commands per cadence (/discovery-daily, /discovery-weekly, /discovery-synth)</item>
      <item>gh: pull GitHub issue feedback as signal source; gh issue list --label feedback --json feeds support-triage</item>
      <item>posthog CLI / amplitude-cli: pull funnel + retention deltas; pipe into analytics-watcher</item>
      <item>linear CLI: query bug+feature requests; tag against OST opportunity nodes</item>
      <item>otter / fireflies / tldv API: fetch transcripts of completed interviews; auto-feed interview-synthesizer</item>
      <item>dovetail API (/v1/notes, /v1/tags): push tagged quotes from synthesizer</item>
      <item>userinterviews.com API: automate weekly recruit batches</item>
      <item>slack CLI / webhook: post weekly synthesis digest to product channel</item>
    </list>
  </section>

  <section title="Services">
    <list>
      <item>Dovetail: REST + webhooks; repository + AI tagging; Magic AI summaries 2025+</item>
      <item>Looppanel: REST; AI interview analysis, auto-tagging, JTBD extraction</item>
      <item>Userinterviews.com: REST; panel access, scheduling, incentives</item>
      <item>Maze: REST; fast assumption tests (quant + qual) for weekly cadence</item>
      <item>PostHog: REST + SQL; self-hostable; feeds analytics-watcher with daily funnel deltas</item>
      <item>Otter / Fireflies / tl;dv: REST; transcription for async interview synthesis</item>
    </list>
  </section>

  <reference path="templates/crontab.txt">Cron schedule for four discovery cadences.</reference>
  <reference path="templates/analytics-watcher.py">Agent SDK daily watcher skeleton.</reference>
  <reference path="templates/ost-schema.json">OST node schema for git-tracked tree.</reference>
</text>
