<?xml version="1.0" encoding="UTF-8"?>
<text id="02-prompt-pattern" title="Continuous Discovery Prompt Pattern and Key Rules">
  <summary>Canonical XML prompt pattern for all cadence agents, key rules (N>=5, opportunity vs solution, curse-of-knowledge), and the Torres framework vocabulary constraints.</summary>

  <section title="Canonical Prompt Pattern">
    <rule>
      <statement>Use this exact XML prompt structure for every cadence agent — roles, inputs, rules, and task — with the Torres framework constraints hardcoded in the rules block.</statement>
      <rationale>Agents trained on generic UX content drift into "validation theater" and solution mining without explicit Torres-vocabulary constraints. Hardcoding the rules block prevents terminology drift across agent runs.</rationale>
    </rule>
    <example type="good">
      <note>Standard cadence agent prompt with Torres rules:</note>
      <code lang="xml"><![CDATA[
<role>You are the {agent}. Continuous Discovery (Torres) practitioner.</role>

<inputs>
  <since>{ISO8601 last-run timestamp}</since>
  <signals>{paths to analytics/tickets/transcripts}</signals>
  <ost>{path to opportunity-solution-tree.md}</ost>
  <assumptions>{path to open-assumptions.md}</assumptions>
</inputs>

<rules>
  - Tag every observation with: source, user_segment, frequency, severity (1-5).
  - Distinguish OPPORTUNITY (unmet need) from SOLUTION (proposed feature). Reject solution-shaped input.
  - Never declare "validated" from a single signal. Require N>=5 across >=2 sources.
  - Surface curse-of-knowledge risk: flag claims that assume internal jargon.
  - Output JSON matching schema {schema_path}, plus a markdown digest <= 60 lines.
</rules>

<task>{cadence-specific instruction}</task>
      ]]></code>
    </example>
  </section>

  <section title="Key Rules for All Agents">
    <rule>
      <statement>Ban the word "validated" from all agent prompts and outputs — use "assumption tested" or "evidence found" instead.</statement>
      <rationale>Torres explicitly rejects "validation theater". Agents trained on generic UX content conflate validation (proving a hypothesis) with testing (gathering evidence). The word ban forces the right framing.</rationale>
    </rule>
    <rule>
      <statement>Require N >= 5 observations across at least 2 independent sources before any finding is published — reject completions that violate this rule.</statement>
      <rationale>LLMs love to declare "insights" from thin signal. A single viral Slack thread or one angry review is not a pattern. Five observations across two source types is the minimum for a credible signal.</rationale>
    </rule>
    <rule>
      <statement>Force "opportunity vs solution" classification at intake; reject any item phrased as a feature ("add filter X") and re-prompt for the underlying need.</statement>
      <rationale>Teams revert to solution mode under time pressure. The explicit classification at intake, backed by rejection, maintains discovery discipline even when stakeholders deliver solution-shaped requests.</rationale>
    </rule>
    <rule>
      <statement>Score opportunities on (frequency * severity * addressability), not gut feel; emit numeric scores and surface deltas week-over-week.</statement>
      <rationale>Gut-feel prioritization in OSTs is unauditable. Numeric scores with week-over-week deltas reveal which opportunities are gaining or losing urgency and enable evidence-based ranking.</rationale>
    </rule>
  </section>

  <section title="Curse-of-Knowledge Guard">
    <rule>
      <statement>Add an explicit lint step that flags any term in agent output not present in the last 20 interview transcripts — agents trained on internal docs use jargon that users do not.</statement>
      <rationale>Internal jargon in opportunities ("improve the onboarding wizard UX") signals that the agent is reasoning from product assumptions, not from the user's mental model. The transcript lint catches this before it enters the OST.</rationale>
    </rule>
  </section>
</text>
