<?xml version="1.0" encoding="UTF-8"?>
<text id="04-custom-context" title="Custom Hooks and Context Provider Pattern">
  <summary>Custom hook shape with typed result interface (useFetch, useDebounce), and the Context+null+runtime-error pattern for shared state.</summary>

  <section title="Custom Hook Shape">
    <rule>
      <statement>Name custom hooks with the use prefix; return a typed interface (not a tuple unless it mirrors useState); declare the return interface explicitly above the hook.</statement>
      <rationale>A named interface makes the hook's contract visible at the call site and prevents callers from relying on positional tuple indices that break on refactoring.</rationale>
    </rule>
    <example type="good">
      <code lang="typescript"><![CDATA[
interface UseFetchResult<T> {
  data: T | null;
  isLoading: boolean;
  error: Error | null;
  refetch: () => Promise<void>;
}

function useFetch<T>(url: string): UseFetchResult<T> {
  const [data, setData] = useState<T | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetchData = useCallback(async () => {
    setIsLoading(true);
    setError(null);
    try {
      const response = await fetch(url);
      if (!response.ok) throw new Error(`HTTP ${response.status}`);
      setData(await response.json());
    } catch (err) {
      setError(err instanceof Error ? err : new Error('Unknown error'));
    } finally {
      setIsLoading(false);
    }
  }, [url]);

  useEffect(() => { fetchData(); }, [fetchData]);

  return { data, isLoading, error, refetch: fetchData };
}

// Usage
const { data: user, isLoading, error } = useFetch<User>('/api/user/1');
]]></code>
    </example>
    <example type="good">
      <code lang="typescript"><![CDATA[
function useDebounce<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState(value);

  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);

  return debouncedValue;
}

// Usage — search input debounced 300 ms
function SearchInput() {
  const [query, setQuery] = useState('');
  const debouncedQuery = useDebounce(query, 300);

  useEffect(() => {
    if (debouncedQuery) performSearch(debouncedQuery);
  }, [debouncedQuery]);

  return <input value={query} onChange={e => setQuery(e.target.value)} />;
}
]]></code>
    </example>
  </section>

  <section title="Context Provider with Hook">
    <rule>
      <statement>Initialize Context with null and throw a descriptive error in the consumer hook when the value is null; wrap the mutable value in useMemo so consumers do not re-render on every provider render.</statement>
      <rationale>A null sentinel makes it impossible to consume context outside its provider without a silent bug. Memoizing the value object prevents all consumers from re-rendering when an unrelated piece of provider state changes.</rationale>
    </rule>
    <example type="good">
      <code lang="typescript"><![CDATA[
interface AuthContextValue {
  user: User | null;
  isAuthenticated: boolean;
  login: (credentials: Credentials) => Promise<void>;
  logout: () => void;
}

const AuthContext = createContext<AuthContextValue | null>(null);

export function AuthProvider({ children }: { children: ReactNode }) {
  const [user, setUser] = useState<User | null>(null);

  const login = useCallback(async (credentials: Credentials) => {
    const user = await authApi.login(credentials);
    setUser(user);
  }, []);

  const logout = useCallback(() => {
    authApi.logout();
    setUser(null);
  }, []);

  const value = useMemo(
    () => ({ user, isAuthenticated: user !== null, login, logout }),
    [user, login, logout],
  );

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
}

export function useAuth(): AuthContextValue {
  const context = useContext(AuthContext);
  if (!context) throw new Error('useAuth must be used within AuthProvider');
  return context;
}
]]></code>
    </example>
    <antipattern>
      <description>Calling hooks conditionally based on a prop or runtime flag.</description>
      <reason>React relies on a stable hook call order across renders. Conditional hooks violate the Rules of Hooks and cause runtime errors.</reason>
    </antipattern>
    <example type="bad">
      <code lang="typescript"><![CDATA[
function Component({ enabled }: { enabled: boolean }) {
  if (enabled) {
    const [value, setValue] = useState(''); // Error!
  }
}
]]></code>
    </example>
    <example type="good">
      <code lang="typescript"><![CDATA[
function Component({ enabled }: { enabled: boolean }) {
  const [value, setValue] = useState('');
  if (!enabled) return null;
  return <input value={value} onChange={e => setValue(e.target.value)} />;
}
]]></code>
    </example>
  </section>
</text>
