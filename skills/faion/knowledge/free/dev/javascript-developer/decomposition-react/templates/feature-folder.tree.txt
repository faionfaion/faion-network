# purpose: canonical feature-folder layout for a React app
# consumes: component decomposition plan (decomposition-react)
# produces: a deterministic file tree the team agrees on
# depends-on: ESLint with import/order; TypeScript path aliases (@/features/*)
# token-budget-impact: ~120 tokens when loaded as context

src/
├── features/
│   ├── billing/
│   │   ├── BillingPage.tsx         # ≤80 lines JSX
│   │   ├── useInvoiceFilter.ts     # hook
│   │   ├── useInvoiceList.ts       # hook
│   │   ├── invoiceApi.ts           # service (I/O)
│   │   ├── invoice.types.ts        # types
│   │   ├── __tests__/
│   │   │   └── BillingPage.test.tsx
│   │   └── index.ts                # public re-exports
│   └── auth/
│       └── ...
├── shared/                          # cross-feature reusables
│   ├── ui/
│   └── utils/
├── app/                             # router, providers, layout
└── main.tsx
