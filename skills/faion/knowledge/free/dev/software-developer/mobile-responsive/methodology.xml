<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="mobile-responsive"><metadata><tier>free</tier><group>dev</group><domain>software-developer</domain><summary>Mobile-responsive web design: viewport configuration, CSS media queries, flexible layouts, touch-friendly interactions, and mobile-first development for devices from 320px to 2560px.</summary><difficulty>intermediate</difficulty><created>2026-04-26</created><tags><tag>responsive</tag><tag>mobile</tag><tag>css</tag><tag>web-design</tag><tag>ux</tag></tags></metadata><content><title>Mobile-Responsive Web Design</title><summary><p>Mobile-responsive design adapts web pages to any device width using flexible layouts, CSS media queries, and touch-friendly interactions. Modern approaches use mobile-first CSS (start at 320px) and viewport meta tags. Responsive design is no longer optional—it is a baseline requirement for web accessibility.</p></summary><why><p># Mobile Responsive Design

## Summary

Mobile-first CSS design: start with styles for the smallest viewport, enhance progressively for larger screens. Use relative units (rem, %, clamp()), CSS Grid/Flexbox for layout, viewport meta tag, and touch targets of at least 44x44px. No fixed pixel widths above 1px borders.

## Why

Over 60% of web traffic comes from mobile devices. Desktop-first CSS generates complex mobile overrides that accumulate technical debt. Mobile-first produces simpler, more maintainable code and ensures the smallest-resource experience works correctly.

## When To Use

- Any web UI: mobile-first is the default, not an option.
- Auditing an existing site for mobile regressions after redesign.
- Generating Tailwind configs, Storybook stories, or component libraries.
- Any UI that must render correctly from 320px to 1920px.

## When NOT To Use

- Internal admin tools targeting fixed-resolution kiosks or desktop-only dashboards.
- Embedded WebViews where the host app fixes the viewport (e.g., Telegram WebApp pinned to mobile).
- Print stylesheets, PDF generators, static reports — different rendering model.
- Native mobile (React Native, Flutter, SwiftUI) — different layout primitives.

## Content

| File | What's inside |
|------|---------------|
| `content/01-css-rules.xml` | Viewport meta, mobile-first media queries, fluid typography with clamp(), responsive images, touch targets. |
| `content/02-components.xml` | Responsive navigation pattern (React + CSS), responsive tables, container queries. |
| `content/03-antipatterns.xml` | Fixed widths, desktop-first, device-specific breakpoints, tiny touch targets, horizontal scroll. |

## Templates

| File | Purpose |
|------|---------|
| `templates/responsive-check.ts` | Playwright script: screenshot at 3 viewports, detect horizontal scroll overflow. |
</p><p># Agent Integration — Mobile Responsive

## When to use
- Building any web UI agent-side: the default starting point is mobile-first CSS, viewport meta, fluid type via `clamp()`.
- Auditing an existing site for mobile parity (e.g., regression after redesign), where an agent runs Lighthouse + Playwright in multiple viewports.
- Generating Tailwind configs, Storybook stories, or component libraries that must render correctly from 320px to 1920px.
- Producing email/embed snippets where viewport meta and fluid units are non-negotiable.

## When NOT to use
- Internal admin tooling targeting fixed-resolution kiosks or desktop-only ops dashboards — adding breakpoints adds drag without payoff.
- Embedded WebViews where the host app forces a known viewport (e.g., a Telegram WebApp pinned to mobile only) — design for one width.
- Print stylesheets, PDF generators, or static reports — different rendering model entirely.
- Native mobile (React Native, Flutter, SwiftUI) — different layout primitives; this methodology is web-CSS-specific.

## Where it fails / limitations
- Pure breakpoint design ignores container queries; modern UIs need `@container` for self-aware components.
- `100vh` on iOS Safari includes the URL bar height — leads to hidden bottom UI; use `100dvh` (dynamic viewport) or `svh`/`lvh`.
- Touch targets &lt;44px violate Apple HIG and WCAG 2.5.5 — easy to miss when an agent is just translating Figma px to CSS.
- Hover-based UI breaks on touch; agents that translate `:hover` patterns from desktop demos produce broken mobile UX.
- Performance regressions hide on dev machines: a `&lt;picture&gt;` with 4 sources can still ship a 2 MB image to a 4G phone if the agent forgot `loading="lazy"` and `srcset` widths.

## Agentic workflow
A frontend subagent generates the markup + Tailwind classes (or vanilla CSS), then a verification subagent runs headless Chromium at 360×640, 768×1024, 1280×800, 1920×1080 and screenshots each. Layout-shift, scroll-overflow, and tap-target size are checked programmatically. A11y checks run in the same pass. The agent never accepts a UI as "responsive" without three viewport screenshots and a Lighthouse mobile score above an agreed threshold (e.g., perf &gt; 85, a11y &gt; 95).

### Recommended subagents
- `faion-sdd-executor-agent` — owns the UI task and reads acceptance criteria including viewport list.
- A purpose-built `responsive-auditor` subagent (Playwright + axe-core + Lighthouse) — runs the verification pass.
- For component libraries, drive Storybook viewport addon via the same agent so each story is checked in `mobile1`/`tablet`/`desktop`.

### Prompt pattern
```
Implement &lt;component&gt; mobile-first. No fixed widths. Use rem/em/%, clamp()
for type, and CSS Grid for layout. Tap targets &gt;= 44x44px. Test at
360, 768, 1280 widths. Output the full CSS plus the Playwright assertions.
```
```
Audit &lt;file&gt;. Find: (1) px values that should be rem, (2) hover-only
interactions, (3) missing viewport meta, (4) 100vh usages, (5) tap
targets under 44px. Return a JSON list of findings with line numbers.
```

## CLI tools
| Tool | Purpose | Install / docs |
|------|---------|----------------|
| `playwright` | Headless multi-viewport screenshots &amp; assertions | `npm i -D @playwright/test` · https://playwright.dev/ |
| `lighthouse` | Mobile perf/a11y audits | `npm i -g lighthouse` · https://developer.chrome.com/docs/lighthouse/ |
| `axe-core` / `@axe-core/cli` | Accessibility checks (touch target, contrast) | `npm i -g @axe-core/cli` |
| `pa11y` | Single-URL a11y CI runner | `npm i -g pa11y` |
| `puppeteer` + `puppeteer-cluster` | Bulk screenshot diffing | `npm i puppeteer puppeteer-cluster` |
| `responsively-app` | Multi-device live preview during dev | https://responsively.app/ |
| `tailwindcss` | Mobile-first utility classes (sm/md/lg/xl/2xl) | `npm i -D tailwindcss` |
| `polypane` | Multi-device preview + a11y; CLI exists for screenshots | https://polypane.app/ |
| `unlighthouse` | Site-wide Lighthouse crawl | `npx unlighthouse --site &lt;url&gt;` |

## Services &amp; apps
| Service | Type (SaaS/OSS) | Agent-friendly? | Notes |
|---------|-----------------|-----------------|-------|
| BrowserStack | SaaS | Yes (REST API) | Real-device testing across iOS/Android matrix. |
| Sauce Labs | SaaS | Yes (REST API) | Same niche; integrates with Playwright/WebDriver. |
| LambdaTest | SaaS | Yes | API + Playwright-cloud runners. |
| Percy / Chromatic | SaaS | Yes | Visual diffing across breakpoints; agent-driven baselines. |
| Web.dev / PageSpeed Insights | SaaS | Yes | Programmatic via `psi` API for CI. |
| Storybook + Viewport addon | OSS | Yes | Per-story breakpoint tests. |

## Templates &amp; scripts
See `templates.md` for full mobile-first CSS scaffolds. Inline Playwright assertion helper:

```ts
// scripts/responsive-check.ts
import { chromium, devices } from "@playwright/test";

const viewports = [
  { name: "iphone-se", ...devices["iPhone SE"] },
  { name: "ipad",      ...devices["iPad Pro 11"] },
  { name: "desktop",   viewport: { width: 1280, height: 800 } },
];

const url = process.argv[2] ?? "http://localhost:3000";
const browser = await chromium.launch();
for (const v of viewports) {
  const ctx = await browser.newContext(v);
  const page = await ctx.newPage();
  await page.goto(url);
  await page.screenshot({ path: `out/${v.name}.png`, fullPage: true });
  const overflow = await page.evaluate(
    () =&gt; document.body.scrollWidth &gt; window.innerWidth,
  );
  if (overflow) console.error(`HORIZONTAL SCROLL at ${v.name}`);
  await ctx.close();
}
await browser.close();
```

## Best practices
- Always start with the viewport meta tag and `box-sizing: border-box` global rule; agent UI templates that omit these regress on day one.
- Use `clamp(min, fluid, max)` for both type and spacing — eliminates most breakpoint-specific font rules.
- Prefer CSS Grid for top-level layout (auto responsive via `grid-template-columns: repeat(auto-fit, minmax(...))`), Flexbox inside cells.
- Use container queries (`@container`) for components reused at different widths inside the same page.
- Replace `100vh` with `100dvh` (or `min(100vh, 100dvh)` for older browsers) to fix iOS bottom-bar bugs.
- Touch target rule: every interactive element ≥44×44 CSS px, with non-zero padding around small icons.
- Use `&lt;picture&gt;` + `srcset` + `sizes` for hero images; never ship a desktop-resolution PNG to a 360px viewport.
- Add a `prefers-reduced-motion` media query and gate all animations.
- Lock-in via Tailwind: enable `screens` config, ditch arbitrary breakpoints, use `min-h-dvh` once available.

## AI-agent gotchas
- LLMs default to `px` units when generating CSS because training data is full of legacy px examples. Prompt explicitly: "no fixed pixel widths above 1px borders".
- Agents copying Figma exports often invert mobile-first into desktop-first (`@media (max-width: ...)`); add a lint rule rejecting `max-width` media queries except for print/dark-mode overrides.
- LLMs frequently emit `&lt;meta name="viewport" content="width=device-width"&gt;` without `initial-scale=1.0`, which breaks iOS Safari rotation. Verify both attributes are present.
- When asked to "make this responsive," LLMs often only add breakpoints to width and forget grid columns, font sizes, and touch targets. Provide a checklist explicitly.
- Auto-screenshot diffs need locked fonts/clock. Without `await page.evaluate(() =&gt; document.fonts.ready)` and freezing time, agents will report false-positive diffs every run.
- iOS-specific: agents miss `-webkit-tap-highlight-color: transparent`, momentum scrolling (`-webkit-overflow-scrolling: touch`) — flag these in PR review.
- A "passing" Lighthouse mobile run on localhost lies (no throttling). Always force `--throttling-method=devtools --preset=mobile` so the agent sees real perf budgets.
- Container queries land at different times in Safari/Firefox — agent must check support matrix before generating `@container` rules; provide a fallback or polyfill.

## References
- https://web.dev/articles/responsive-web-design-basics — Google's canonical guide
- https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_container_queries — container queries reference
- https://www.w3.org/WAI/WCAG21/Understanding/target-size.html — WCAG target size rule
- https://css-tricks.com/the-large-small-and-dynamic-viewport-units/ — dvh/svh/lvh explainer
- https://utopia.fyi/ — fluid type/space calculator producing clamp() values
- https://every-layout.dev/ — patterns for resilient component layouts
- https://playwright.dev/docs/emulation — device emulation reference
</p><p>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;text id="03-antipatterns" title="Mobile Responsive Antipatterns"&gt;
  &lt;summary&gt;Common failures in agent-generated responsive CSS: fixed widths, desktop-first, tiny touch targets, viewport unit bugs.&lt;/summary&gt;

  &lt;section title="Layout Antipatterns"&gt;
    &lt;antipattern&gt;
      &lt;description&gt;Using fixed px widths for layout elements (width: 960px, width: 400px).&lt;/description&gt;
      &lt;reason&gt;Fixed widths overflow on smaller viewports. Use max-width with percentage or fluid units instead. Exception: 1px borders and icon sizes.&lt;/reason&gt;
    &lt;/antipattern&gt;

    &lt;antipattern&gt;
      &lt;description&gt;Writing desktop-first CSS using max-width media queries for mobile overrides.&lt;/description&gt;
      &lt;reason&gt;Desktop-first produces cascading overrides that become unmaintainable. Every mobile style must undo a desktop style. Mobile-first is additive and produces less CSS.&lt;/reason&gt;
    &lt;/antipattern&gt;

    &lt;antipattern&gt;
      &lt;description&gt;Using device-specific breakpoints (480px for iPhone, 1024px for iPad) instead of content-based breakpoints.&lt;/description&gt;
      &lt;reason&gt;Device dimensions change every year. Break when the content breaks, not at specific device sizes.&lt;/reason&gt;
    &lt;/antipattern&gt;
  &lt;/section&gt;

  &lt;section title="Touch and Interaction Antipatterns"&gt;
    &lt;antipattern&gt;
      &lt;description&gt;Interactive elements smaller than 44x44px — common when translating Figma designs pixel-for-pixel.&lt;/description&gt;
      &lt;reason&gt;Touch targets below WCAG 2.5.5 minimum cause mis-taps. Figma designs often show visual size, not tap area. Add padding to meet the minimum.&lt;/reason&gt;
    &lt;/antipattern&gt;

    &lt;antipattern&gt;
      &lt;description&gt;Hover-only interactions without active/focus equivalents for touch.&lt;/description&gt;
      &lt;reason&gt;Touch devices have no hover state. Tooltips, dropdowns, and effects triggered only on :hover are invisible to mobile users.&lt;/reason&gt;
    &lt;/antipattern&gt;
  &lt;/section&gt;

  &lt;section title="Viewport Unit Antipatterns"&gt;
    &lt;antipattern&gt;
      &lt;description&gt;Using 100vh for full-height layouts on mobile.&lt;/description&gt;
      &lt;reason&gt;On iOS Safari, 100vh includes the URL bar height, cutting off bottom UI. Use 100dvh (dynamic viewport height) or min(100vh, 100dvh) for older browser fallback.&lt;/reason&gt;
    &lt;/antipattern&gt;

    &lt;antipattern&gt;
      &lt;description&gt;Missing initial-scale=1.0 in the viewport meta tag.&lt;/description&gt;
      &lt;reason&gt;Without initial-scale, iOS Safari does not respect the viewport on rotation. Both width=device-width and initial-scale=1.0 are required.&lt;/reason&gt;
    &lt;/antipattern&gt;
  &lt;/section&gt;
&lt;/text&gt;

&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;text id="02-components" title="Responsive Component Patterns"&gt;
  &lt;summary&gt;Responsive navigation (React + CSS slide-in), responsive tables, and container queries for self-aware components.&lt;/summary&gt;

  &lt;section title="Responsive Navigation"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Mobile navigation must use aria-expanded, aria-controls, and aria-label on the toggle button. Screen readers and keyboard users must be able to open and close the menu.&lt;/statement&gt;
      &lt;rationale&gt;Navigation is one of the most common accessibility failures. Aria attributes cost nothing and are required for WCAG 2.1 compliance.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;example type="good"&gt;
      &lt;code lang="tsx"&gt;&lt;![CDATA[
export function Navigation() {
  const [isOpen, setIsOpen] = useState(false);
  return (
    &lt;header className="header"&gt;
      &lt;button
        className="menu-toggle"
        aria-expanded={isOpen}
        aria-controls="main-nav"
        aria-label={isOpen ? 'Close menu' : 'Open menu'}
        onClick={() =&gt; setIsOpen(!isOpen)}
      &gt;
        &lt;span className="menu-icon" aria-hidden="true" /&gt;
      &lt;/button&gt;
      &lt;nav id="main-nav" className={`nav ${isOpen ? 'nav--open' : ''}`}&gt;
        &lt;ul className="nav-list"&gt;
          &lt;li&gt;&lt;a href="/products"&gt;Products&lt;/a&gt;&lt;/li&gt;
          &lt;li&gt;&lt;a href="/about"&gt;About&lt;/a&gt;&lt;/li&gt;
        &lt;/ul&gt;
      &lt;/nav&gt;
      {isOpen &amp;&amp; (
        &lt;div className="nav-overlay" onClick={() =&gt; setIsOpen(false)} aria-hidden="true" /&gt;
      )}
    &lt;/header&gt;
  );
}
      ]]&gt;&lt;/code&gt;
    &lt;/example&gt;
  &lt;/section&gt;

  &lt;section title="Responsive Tables"&gt;
    &lt;rule&gt;
      &lt;statement&gt;For data tables on mobile, use either horizontal scroll with overflow-x: auto (simple) or the card-pattern that converts each row to a block with data-label (accessible). Never hide columns on mobile.&lt;/statement&gt;
      &lt;rationale&gt;Hiding columns removes data from mobile users. Both scroll and card patterns keep all data accessible.&lt;/rationale&gt;
    &lt;/rule&gt;
  &lt;/section&gt;

  &lt;section title="Container Queries"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Use @container queries for components that are reused at different widths on the same page (e.g., a card in a sidebar vs in a main grid). Viewport queries cannot handle this case.&lt;/statement&gt;
      &lt;rationale&gt;A component in a 300px sidebar and a 1200px main area cannot be responsive with viewport queries alone. Container queries respond to the component's own container width.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;example type="good"&gt;
      &lt;code lang="css"&gt;&lt;![CDATA[
.card-container {
  container-type: inline-size;
  container-name: card;
}

/* Default: stacked */
.card { display: grid; gap: 1rem; }

/* Wide container: side-by-side */
@container card (min-width: 400px) {
  .card { grid-template-columns: 150px 1fr; }
}
      ]]&gt;&lt;/code&gt;
    &lt;/example&gt;
  &lt;/section&gt;
&lt;/text&gt;

&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;text id="01-css-rules" title="Mobile-First CSS Rules"&gt;
  &lt;summary&gt;Viewport meta, mobile-first media queries, fluid typography with clamp(), responsive images, and touch targets.&lt;/summary&gt;

  &lt;section title="Viewport and Box Model"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Every HTML page must include &amp;lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&amp;gt; with both attributes. Missing initial-scale=1.0 breaks iOS Safari rotation.&lt;/statement&gt;
      &lt;rationale&gt;Without the viewport meta, mobile browsers render the page at desktop width and scale it down, making text unreadable without zooming.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;rule&gt;
      &lt;statement&gt;Set box-sizing: border-box globally before any other styles. This makes width calculations predictable — padding and border are included in the specified width.&lt;/statement&gt;
      &lt;rationale&gt;The default content-box model causes layout breaks when borders or padding are added to percentage-width elements.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;rule&gt;
      &lt;statement&gt;Use 100dvh (dynamic viewport height) instead of 100vh. On iOS Safari, 100vh includes the URL bar height, hiding bottom UI.&lt;/statement&gt;
      &lt;rationale&gt;The browser-chrome issue on iOS is the most common mobile layout bug. dvh units use the actual visible viewport.&lt;/rationale&gt;
    &lt;/rule&gt;
  &lt;/section&gt;

  &lt;section title="Mobile-First Media Queries"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Write base styles for the smallest viewport without a media query. Add min-width media queries for larger breakpoints. Never use max-width queries except for print or dark-mode overrides.&lt;/statement&gt;
      &lt;rationale&gt;Mobile-first produces additive CSS — smaller files for mobile and progressive enhancement for desktop. max-width queries produce subtractive overrides that accumulate complexity.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;example type="good"&gt;
      &lt;code lang="css"&gt;&lt;![CDATA[
/* Base: mobile (no query) */
.grid { grid-template-columns: 1fr; gap: 1rem; }

/* Tablet and up */
@media (min-width: 768px) {
  .grid { grid-template-columns: repeat(2, 1fr); }
}

/* Desktop and up */
@media (min-width: 1024px) {
  .grid { grid-template-columns: repeat(3, 1fr); }
}
      ]]&gt;&lt;/code&gt;
    &lt;/example&gt;
  &lt;/section&gt;

  &lt;section title="Fluid Typography"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Use clamp(min, fluid, max) for font sizes and spacing. This eliminates most breakpoint-specific font rules and produces smooth scaling between viewport sizes.&lt;/statement&gt;
      &lt;rationale&gt;Stepped font sizes at breakpoints produce jarring jumps. clamp() produces continuous scaling with defined minimum and maximum bounds.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;example type="good"&gt;
      &lt;code lang="css"&gt;&lt;![CDATA[
:root {
  --font-size-base: clamp(1rem, 0.9rem + 0.5vw, 1.125rem);
  --font-size-2xl:  clamp(1.5rem, 1rem + 2.5vw, 2rem);
  --space-md:       clamp(1rem, 0.8rem + 1vw, 1.5rem);
}
body { font-size: var(--font-size-base); }
      ]]&gt;&lt;/code&gt;
    &lt;/example&gt;
  &lt;/section&gt;

  &lt;section title="Touch Targets"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Every interactive element (button, link, input) must have a minimum touch target of 44x44 CSS px. This is WCAG 2.5.5 and Apple HIG minimum.&lt;/statement&gt;
      &lt;rationale&gt;Touch targets below 44px cause mis-taps on mobile. Agents translating Figma designs pixel-for-pixel routinely produce targets of 20-32px.&lt;/rationale&gt;
    &lt;/rule&gt;

    &lt;example type="good"&gt;
      &lt;code lang="css"&gt;&lt;![CDATA[
.button, a, input {
  min-height: 44px;
  min-width: 44px;
  padding: 0.75rem 1rem;
}

/* Remove hover effects on touch devices */
@media (hover: none) {
  .button:hover { background: var(--button-bg); }
}
      ]]&gt;&lt;/code&gt;
    &lt;/example&gt;
  &lt;/section&gt;

  &lt;section title="Responsive Images"&gt;
    &lt;rule&gt;
      &lt;statement&gt;Set max-width: 100%; height: auto on all images. Use srcset + sizes for resolution switching. Use the picture element for art direction (different crops per viewport).&lt;/statement&gt;
      &lt;rationale&gt;Images without max-width overflow their container on small viewports. Without srcset, desktop-resolution images load on mobile, wasting bandwidth.&lt;/rationale&gt;
    &lt;/rule&gt;
  &lt;/section&gt;
&lt;/text&gt;

</p><p>/**
 * responsive-check.ts — Playwright script for multi-viewport screenshot and overflow detection.
 * Usage: npx ts-node scripts/responsive-check.ts http://localhost:3000
 * Output: screenshots in out/&lt;viewport-name&gt;.png, console errors on horizontal overflow.
 */
import { chromium, devices } from "@playwright/test";
import * as fs from "fs";

const viewports = [
  { name: "iphone-se", ...devices["iPhone SE"] },
  { name: "ipad", ...devices["iPad Pro 11"] },
  { name: "desktop", viewport: { width: 1280, height: 800 } },
];

const url = process.argv[2] ?? "http://localhost:3000";

(async () =&gt; {
  if (!fs.existsSync("out")) fs.mkdirSync("out");
  const browser = await chromium.launch();

  for (const v of viewports) {
    const ctx = await browser.newContext(v as Parameters&lt;typeof browser.newContext&gt;[0]);
    const page = await ctx.newPage();
    await page.goto(url, { waitUntil: "networkidle" });
    await page.evaluate(() =&gt; document.fonts.ready);

    await page.screenshot({ path: `out/${v.name}.png`, fullPage: true });

    const overflow = await page.evaluate(
      () =&gt; document.body.scrollWidth &gt; window.innerWidth,
    );
    if (overflow) {
      console.error(`FAIL: horizontal scroll detected at ${v.name}`);
    } else {
      console.log(`OK: ${v.name}`);
    }
    await ctx.close();
  }

  await browser.close();
})();

</p></why><when-to-use><list><item>Any public-facing web application or website (desktop, tablet, mobile).</item><item>Progressive web apps (PWA) targeting mobile-first users.</item><item>Content-driven sites (blogs, news, documentation) where mobile traffic is significant.</item><item>APIs with web UI dashboards accessed from phones and tablets.</item><item>E-commerce sites where mobile conversion is critical.</item></list></when-to-use><when-not-to-use><list><item>Intranet-only tools for desktop users only (pure desktop apps, VPN-gated software).</item><item>Native mobile apps (iOS/Android) don't need responsive web CSS.</item><item>Hardware-restricted embedded interfaces where screen size is fixed.</item><item>Temporary prototypes or throwaway tools with no long-term maintenance.</item></list></when-not-to-use><section name="External Resources"><see-also><link href="https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design" /><link href="https://www.w3.org/TR/mobile-bp/" /><ref slug="tailwind" /><ref slug="react-hooks" /></see-also></section></content></methodology>