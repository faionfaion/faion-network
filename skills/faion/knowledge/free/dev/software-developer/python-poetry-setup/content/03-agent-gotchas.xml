<?xml version="1.0" encoding="UTF-8"?>
<text id="03-agent-gotchas" title="Agent Gotchas for Poetry">
  <summary>Known failure modes when agents manage Python projects with Poetry.</summary>

  <section title="Critical Agent Failures">
    <antipattern>
      <description>Agent calls poetry shell in headless CI or Docker.</description>
      <reason>poetry shell hangs forever waiting for a TTY. Poetry 2.0 removed it entirely. Always use poetry run &lt;cmd&gt; from agents, never poetry shell.</reason>
    </antipattern>

    <antipattern>
      <description>Agent uses pip install to add a package to a Poetry-managed project.</description>
      <reason>pip install bypasses Poetry's solver and does not update pyproject.toml or poetry.lock. The next poetry install will remove the package.</reason>
    </antipattern>

    <antipattern>
      <description>Agent writes "^0.1" expecting it to mean &lt;1.0.0.</description>
      <reason>In Poetry, ^0.1 means >=0.1.0,&lt;0.2.0 (not &lt;1.0.0). The caret rule applies to the leftmost non-zero digit. Always run poetry add --dry-run and read back the resolved range before committing.</reason>
    </antipattern>

    <antipattern>
      <description>Agent calls poetry add X when the lockfile is dirty (uncommitted local changes).</description>
      <reason>Poetry silently merges the dirty state into the new lock. Run poetry check --lock before any add/remove.</reason>
    </antipattern>

    <antipattern>
      <description>Agent uses poetry self add instead of pip install to install a Poetry plugin.</description>
      <reason>Actually reversed: pip install into the Poetry interpreter corrupts Poetry's own deps. Use poetry self add for plugins. The antipattern is using pip for Poetry's own environment.</reason>
    </antipattern>
  </section>
</text>
