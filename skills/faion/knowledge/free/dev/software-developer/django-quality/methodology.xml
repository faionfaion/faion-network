<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="django-quality">
  <metadata>
    <tier>free</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>Quality toolchain for Django: ruff (format + lint), mypy + django-stubs (typing), bandit (security), pre-commit hooks.</summary>
    <tags>
      <tag>django</tag>
      <tag>ruff</tag>
      <tag>mypy</tag>
      <tag>quality</tag>
      <tag>pre-commit</tag>
    </tags>
    <difficulty>beginner</difficulty>
    <created>2026-04-27</created>
  </metadata>
  <content>
    <title>Django Code Quality Tools</title>
    <summary>Quality toolchain for Django projects: ruff (format + lint), mypy + django-stubs (typing), bandit (security), and pre-commit hooks orchestrating all of them. The canonical setup is pyproject.toml-only — no separate .flake8, no setup.cfg, no black/isort alongside ruff.</summary>
    <why>
      <p>A deterministic format/lint baseline removes whitespace noise from diffs so code review focuses on logic. Pre-commit hooks enforce the baseline at commit time; CI enforces it at merge time. mypy + django-stubs catches type errors before runtime. Without this, each agent writes code in a different style and introduces subtle type errors that only surface in production.</p>
    </why>
    <when-to-use>
      <list>
        <item>Bootstrapping a new Django project before first feature commit.</item>
        <item>Migrating a legacy Django repo from black+isort+flake8 to ruff.</item>
        <item>Onboarding agents to a Django codebase that needs a deterministic baseline.</item>
        <item>Adding type-checking incrementally module by module via mypy overrides.</item>
      </list>
    </when-to-use>
    <when-not-to-use>
      <list>
        <item>Non-Django Python projects — drop mypy_django_plugin and DJ rules; rest still applies.</item>
        <item>Single-script utilities where pre-commit setup exceeds the code itself.</item>
        <item>Legacy migration files — gate with --diff first or you'll create a 5k-line format PR.</item>
      </list>
    </when-not-to-use>
    <section name="agentic-workflow">
      <p>A bootstrap subagent writes pyproject.toml, .pre-commit-config.yaml, mypy.ini in one pass and commits before any feature work. A pre-commit fix-loop subagent runs ruff check --fix &amp;&amp; ruff format and re-stages on every diff. A type-debt subagent works module-by-module to enable strict = True mypy in [[tool.mypy.overrides]] blocks; never set repo-wide strict at once. CI runs ruff (fast), mypy (slower, can be on a schedule), and pip-audit weekly. Reviewer subagent rejects PRs with # type: ignore without an explanation comment.</p>
    </section>
    <section name="where-it-fails-limitations">
      <p>The README still pins black + isort + flake8; modern equivalent is ruff format + ruff check (one tool, faster, fewer config files). Treat the README as historical and steer agents to ruff.</p>
      <p>mypy + Django is fragile: querysets are typed loosely, Manager.get_or_create() returns tuple[Model, bool] only with stubs, custom managers need from_queryset + explicit annotation.</p>
      <p>Pre-commit doesn't run in CI by default — must be wired with pre-commit run --all-files job.</p>
      <p>flake8 extensions (bugbear, comprehensions) overlap with ruff rules; running both wastes time.</p>
      <p>bandit/pip-audit produce false positives in Django (e.g., assert in tests flagged as B101) — agents will silence with # noqa blanket instead of fixing.</p>
    </section>
    <section name="ai-agent-gotchas">
      <p>Agents will leave the README's outdated black + isort + flake8 setup intact when generating new projects. Anchor prompts to "Use ruff (format + lint), no black/isort/flake8".</p>
      <p>Agents add # type: ignore to silence mypy on first error rather than fix it. Reviewer subagent must enforce explanatory comment.</p>
      <p>mypy_django_plugin requires DJANGO_SETTINGS_MODULE set in [tool.django-stubs]; agents set it as env var only and CI fails.</p>
      <p>Agents add noqa: E501 everywhere instead of letting the formatter wrap lines. Use line-length consistently and remove blanket noqa.</p>
      <p>pre-commit hook revisions go stale — agents won't bump unless prompted. Use pre-commit autoupdate quarterly.</p>
      <p>Bandit "high severity" hits on subprocess.call(..., shell=True) are usually real; don't let agents suppress without code review.</p>
      <p>Human-in-loop: dependency upgrades (mypy major, ruff version) need human triage because rule changes can mass-flag working code.</p>
      <p>When agent edits settings/base.py, force python manage.py check --deploy in CI — easy to introduce config regressions silently.</p>
    </section>
    <section name="modern-django-quality-stack">
      <p>One source of style truth: ruff config in pyproject.toml. Don't keep a .flake8 lying around. Run pre-commit run --all-files once at adoption, commit the format pass alone, then start feature work — keeps history readable. mypy strictness ratchets up per module via overrides; never flip global strict = true in a legacy repo. Bandit results: triage and either fix or add a per-line # nosec B&lt;id&gt; reason: ... comment. python manage.py check --deploy in production-facing CI catches misconfigured DEBUG, ALLOWED_HOSTS, SECURE_* flags. Coverage threshold: start at current measured value, raise in increments. Setting 100% from day one drives gaming. Run pip-audit weekly via scheduled GHA — daily is noisy, monthly is too late.</p>
    </section>
    <section name="tools-and-services">
      <p>CLI tools: ruff (Format + lint, replaces black/isort/flake8), mypy (Static typing), django-stubs (Type stubs for Django), djangorestframework-stubs (Type stubs for DRF), bandit (Security linter), pip-audit (CVE scanner for installed packages), safety (Alternative CVE scanner), pre-commit (Hook orchestrator), python manage.py check --deploy (Django's built-in production audit), coverage.py / pytest-cov (Coverage with branch tracking), django-upgrade (Auto-rewrite to current Django idioms), pyupgrade (Auto-rewrite to current Python idioms, now part of ruff UP), djhtml (Format Django templates), djade (Modernize template syntax).</p>
      <p>Services: GitHub Actions (Run pre-commit run --all-files + pytest matrix per Python version), Codecov / Coveralls (Coverage delta on PR), Snyk / GitHub Dependabot (Continuous CVE alerts; complement pip-audit), Sentry (Pairs with quality gates: lint/type errors caught locally, runtime errors in Sentry), Sourcery / CodeRabbit / GitHub Copilot review (AI review augments human reviewer), Posit Connect / Cloudflare Pages (If publishing internal type-coverage dashboards).</p>
    </section>
    <section name="recommended-subagents">
      <p>faion-sdd-execution — quality gates already include format/lint/type checks. password-scrubber-agent — pairs with bandit for secret detection. A quality-bootstrap subagent (custom) — input: empty repo or repo with old toolchain; output: modern pyproject.toml + .pre-commit-config.yaml + initial pre-commit run --all-files commit. A mypy-incremental subagent — adds one module to strict mypy at a time, fixing errors as they surface.</p>
    </section>
    <section name="prompt-patterns">
      <p>Bootstrap quality tooling for this Django project. Output: 1. pyproject.toml with [tool.ruff], [tool.ruff.lint] (select E/F/I/B/UP/SIM/DJ/T20/PT/RUF), [tool.ruff.format], [tool.mypy] (with django-stubs). 2. .pre-commit-config.yaml with ruff, ruff-format, end-of-file-fixer, trailing-whitespace, check-yaml hooks. NO black, NO isort, NO flake8. 3. CI step: pre-commit run --all-files. Run pre-commit on the whole repo and fix any errors that auto-fix can resolve.</p>
      <p>Audit # type: ignore and # noqa comments. Each must have: The specific rule code (e.g. # type: ignore[arg-type], # noqa: B902). A trailing comment explaining why. Block PR for blanket suppressions.</p>
    </section>
    <section name="ruff-replaces-black-isort-flake8">
      <rule>
        <statement>Use ruff for both formatting and linting. Do not install black, isort, or flake8 alongside ruff — they overlap and add CI time.</statement>
        <rationale>ruff is 10-100x faster than the three-tool stack, covers all the same rules plus more (DJ, UP, SIM, T20), and is configured in one place (pyproject.toml).</rationale>
      </rule>
      <rule>
        <statement>Enable rule groups: E, F, I, B, UP, SIM, DJ, T20, PT, RUF, S. Exclude migrations via extend-exclude. Ignore E501 (let formatter wrap) and S101 (allow assert in tests).</statement>
        <rationale>These groups cover formatting, pyflakes errors, import order, bugbear, upgrade hints, Django-specific, no-print, pytest, and ruff-native rules.</rationale>
      </rule>
      <reference path="templates/pyproject-ruff.toml">Full ruff + mypy config block for Django</reference>
    </section>
    <section name="mypy-and-django-stubs">
      <rule>
        <statement>Install mypy and django-stubs. Set django_settings_module in [tool.django-stubs], not only as an environment variable. Without it, CI fails when DJANGO_SETTINGS_MODULE is not in the shell.</statement>
        <rationale>Agents commonly set DJANGO_SETTINGS_MODULE as env var only. The [tool.django-stubs] setting is required for mypy to find the Django config.</rationale>
      </rule>
      <rule>
        <statement>Enable mypy strictness per module via [[tool.mypy.overrides]], not repo-wide. Never flip strict = true for an entire legacy repo in one commit.</statement>
        <rationale>Repo-wide strict mypy on a legacy codebase produces hundreds of errors. Module-by-module ratcheting is the only sustainable path.</rationale>
      </rule>
      <rule>
        <statement>Every # type: ignore must include the specific error code and a comment explaining why. Block PRs with blanket # type: ignore suppressions.</statement>
        <rationale>Blanket suppressions hide real type errors and accumulate silently. Specific codes are self-documenting and reviewable.</rationale>
      </rule>
    </section>
    <section name="pre-commit-hooks">
      <rule>
        <statement>Wire pre-commit with ruff, ruff-format, bandit, end-of-file-fixer, trailing-whitespace, and check-yaml hooks. Run pre-commit run --all-files once at adoption and commit the format pass alone before feature work.</statement>
        <rationale>A standalone format commit keeps history readable — subsequent feature commits contain only logic changes.</rationale>
      </rule>
      <rule>
        <statement>Run python manage.py check --deploy in production-facing CI. It catches DEBUG, ALLOWED_HOSTS, and SECURE_* misconfigurations that linters cannot see.</statement>
        <rationale>Django's built-in deployment check covers settings that are valid in development but dangerous in production.</rationale>
      </rule>
      <reference path="templates/pre-commit-config.yaml">Modern pre-commit config with ruff and bandit</reference>
    </section>
    <section name="setup-checklist">
      <list>
        <item>Install ruff, mypy, django-stubs, bandit, pre-commit</item>
        <item>Write pyproject.toml with [tool.ruff], [tool.ruff.lint], [tool.mypy], [tool.django-stubs]</item>
        <item>Write .pre-commit-config.yaml with ruff, ruff-format, bandit, pre-commit-hooks</item>
        <item>Run pre-commit install and pre-commit run --all-files</item>
        <item>Commit format-only pass before first feature commit</item>
        <item>Add pre-commit run --all-files step to CI</item>
        <item>Run pip-audit weekly via scheduled GHA job</item>
      </list>
    </section>
    <templates>
      <template path="templates/pyproject-ruff.toml" purpose="Modern ruff + mypy config block for Django (replaces black/isort/flake8)."/>
      <template path="templates/pre-commit-config.yaml" purpose="pre-commit hooks: ruff, ruff-format, bandit, end-of-file-fixer, trailing-whitespace."/>
    </templates>
    <see-also>
      <link href="https://docs.astral.sh/ruff"/>
      <link href="https://mypy.readthedocs.io"/>
      <link href="https://github.com/typeddjango/django-stubs"/>
      <link href="https://pre-commit.com"/>
    </see-also>
  </content>
</methodology>
