<?xml version="1.0" encoding="UTF-8"?>
<text id="02-patterns" title="Tailwind Component Patterns">
  <summary>cn() helper, CVA variant pattern, responsive design, dark mode, and common layout snippets.</summary>

  <section title="cn() Helper">
    <rule>
      <statement>Use cn() (clsx + tailwind-merge) for all conditional class joining. Forbid template-literal class joins via lint rule.</statement>
      <rationale>tailwind-merge resolves conflicting utilities (e.g. p-4 and p-6 → p-6). Raw template literals can produce invalid merged strings.</rationale>
    </rule>
    <example type="good">
      <code lang="tsx"><![CDATA[
<div className={cn(
  "base-styles",
  isActive && "active-styles",
  className
)} />
]]></code>
    </example>
    <example type="bad">
      <code lang="tsx"><![CDATA[
<div className={`base-styles ${isActive ? "active-styles" : ""} ${className}`} />
]]></code>
      <note>Template literals do not resolve conflicting utilities and cannot be lint-validated.</note>
    </example>
    <reference path="templates/cn-helper.ts">cn() utility implementation.</reference>
  </section>

  <section title="Variant Pattern">
    <rule>
      <statement>Use CVA (class-variance-authority) or tailwind-variants for component variant props. Do not scatter variant class strings across usage sites.</statement>
      <rationale>Centralizing variant class strings makes theme changes a single-file edit and enables agents to extend variants safely.</rationale>
    </rule>
    <example type="good">
      <code lang="tsx"><![CDATA[
const variants = {
  primary: "bg-blue-600 text-white hover:bg-blue-700",
  secondary: "bg-gray-200 text-gray-900 hover:bg-gray-300",
  ghost: "bg-transparent hover:bg-gray-100",
}

<button className={cn("px-4 py-2 rounded", variants[variant])} />
]]></code>
    </example>
    <reference path="templates/cva-variant.tsx">CVA-based variant component example.</reference>
  </section>

  <section title="Responsive Design">
    <rule>
      <statement>Apply mobile-first breakpoints: base class is mobile, md: is tablet, lg: is desktop. Never write desktop-first media queries.</statement>
      <rationale>Mobile-first aligns with Tailwind's breakpoint model and reduces override complexity.</rationale>
    </rule>
    <example type="good">
      <code lang="tsx"><![CDATA[
<div className="flex flex-col md:flex-row lg:justify-between" />
]]></code>
    </example>
  </section>

  <section title="Dark Mode">
    <rule>
      <statement>Use class strategy dark mode with next-themes (or framework equivalent). Pair every color utility with a dark: variant or use semantic CSS variable tokens.</statement>
      <rationale>Without a script-in-head theme detection, light-to-dark FOUC occurs. Semantic tokens eliminate the need to add dark: to every class.</rationale>
    </rule>
    <example type="good">
      <code lang="tsx"><![CDATA[
<div className="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100">
]]></code>
    </example>
  </section>

  <section title="Common Layout Snippets">
    <p>Standard patterns agents should reuse verbatim rather than reinvent:</p>
    <list>
      <item>Container: mx-auto max-w-7xl px-4 sm:px-6 lg:px-8</item>
      <item>Card: rounded-lg border bg-card p-6 shadow-sm</item>
      <item>Stack: flex flex-col gap-4</item>
      <item>Center: flex items-center justify-center</item>
      <item>Truncate single line: truncate</item>
      <item>Truncate multi-line: line-clamp-2</item>
    </list>
  </section>
</text>
