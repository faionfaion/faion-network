<?xml version="1.0" encoding="UTF-8"?>
<text id="01-coverage-types" title="Coverage Types">
  <summary>Line, branch, path, and condition coverage — why branch is the practical minimum for meaningful test gating.</summary>

  <section title="Coverage taxonomy">
    <rule>
      <statement>Use branch coverage (--cov-branch in pytest, --branches in nyc) as the minimum CI gate, not line coverage. Line coverage misses uncovered else branches on covered if lines.</statement>
      <rationale>A test that exercises the if-True path covers all lines containing the if statement and the if-body, but the else branch is untested. Branch coverage requires both paths to execute.</rationale>
    </rule>
    <list>
      <item>LINE COVERAGE — which lines executed; misses untaken else branches</item>
      <item>BRANCH COVERAGE — both sides of each conditional tested; practical floor</item>
      <item>PATH COVERAGE — all execution paths; exponential complexity, no mainstream Python tool</item>
      <item>CONDITION COVERAGE — each boolean sub-expression in compound conditions</item>
    </list>
  </section>

  <section title="Coverage thresholds">
    <rule>
      <statement>Gate diff coverage (new code) at 90% via diff-cover, not repo total. Set repo total as a soft trend target (80%), not a hard gate.</statement>
      <rationale>Hard gates on repo total block PRs on codebases with legacy untested code. Diff gating locks new code without requiring full uplift in one go.</rationale>
    </rule>
    <list>
      <item>New code diff gate: 90% (fail-under in diff-cover)</item>
      <item>Overall project target: 80% (soft warning, not hard gate)</item>
      <item>Critical business logic: 90%+</item>
      <item>Generated / migration / boilerplate: exclude explicitly</item>
    </list>
  </section>
</text>
