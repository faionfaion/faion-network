<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="code-coverage Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for code-coverage. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use branch coverage (--cov-branch in pytest, --branches in nyc) as the minimum CI gate, not line coverage. Line coverage misses uncovered else branches on covered if lines.</statement>
      <rationale source="v1-source">A test that exercises the if-True path covers all lines containing the if statement and the if-body, but the else branch is untested. Branch coverage requires both paths to execute.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Gate diff coverage (new code) at 90% via diff-cover, not repo total. Set repo total as a soft trend target (80%), not a hard gate.</statement>
      <rationale source="v1-source">Hard gates on repo total block PRs on codebases with legacy untested code. Diff gating locks new code without requiring full uplift in one go.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Stop chasing 100% coverage. Set 80–90 as the practical ceiling; remaining 10–20% is defensive guards and error paths better validated by integration tests.</statement>
      <rationale source="v1-source">Chasing 100% leads to testing trivial getters, mocking everything, and writing assertions that mirror implementation rather than verify behavior.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Run diff-cover --fail-under=90 --compare-branch=origin/main after generating coverage.xml. This gates only changed lines, not legacy code.</statement>
      <rationale source="v1-source">diff-cover compares the coverage report against the git diff, reporting coverage only for lines added or changed in the PR. Legacy untested code does not trigger failures.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Prompt pattern for per-module uplift: "Read coverage report for src/&lt;module&gt;. Identify the largest uncovered branch (use --cov-report=term-missing). Write 2-4 pytest tests that: (1) Cover the missed branches, (2) Assert observable behavior (return value, side effect), not implementation detail, (3) Reuse existing fixtures from tests/conftest.py. Run: pytest tests/test_&lt;module&gt;.py --cov=src.&lt;module&gt; --cov-branch --cov-report=term-missing --cov-fail-under=90."</statement>
      <rationale source="v1-source">TBD</rationale>
    </rule>
  </section>
</text>
