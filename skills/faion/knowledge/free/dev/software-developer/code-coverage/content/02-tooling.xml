<?xml version="1.0" encoding="UTF-8"?>
<text id="02-tooling" title="Coverage Tooling">
  <summary>pytest-cov configuration, jest/vitest thresholds, CI workflow, and diff-cover for new-code gating.</summary>

  <section title="Python: pytest-cov configuration">
    <example type="good">
      <code lang="toml"><![CDATA[
[tool.coverage.run]
source = ["src"]
branch = true
omit = [
    "*/tests/*",
    "*/__init__.py",
    "*/migrations/*",
    "*/conftest.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
    "@abstractmethod",
]
fail_under = 80
show_missing = true

[tool.pytest.ini_options]
addopts = "--cov=src --cov-branch --cov-report=term-missing --cov-report=html --cov-fail-under=80"
]]></code>
      <note>Always set omit = ["*/tests/*"] — otherwise test files inflate coverage numbers.</note>
    </example>
    <reference path="templates/coverage.toml">Ready-to-paste pyproject.toml coverage block</reference>
  </section>

  <section title="TypeScript: Jest coverage thresholds">
    <example type="good">
      <code lang="javascript"><![CDATA[
// jest.config.js
module.exports = {
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/index.ts',
    '!src/**/*.test.{ts,tsx}',
  ],
  coverageThreshold: {
    global: { branches: 80, functions: 80, lines: 80, statements: 80 },
    './src/services/': { branches: 90, functions: 90, lines: 90 },
  },
};
]]></code>
    </example>
    <reference path="templates/jest.coverage.js">Jest coverage config with per-directory thresholds</reference>
  </section>

  <section title="CI: diff-cover gating">
    <rule>
      <statement>Run diff-cover --fail-under=90 --compare-branch=origin/main after generating coverage.xml. This gates only changed lines, not legacy code.</statement>
      <rationale>diff-cover compares the coverage report against the git diff, reporting coverage only for lines added or changed in the PR. Legacy untested code does not trigger failures.</rationale>
    </rule>
    <reference path="templates/diff-cover-ci.sh">CI script combining pytest + diff-cover</reference>
  </section>
</text>
