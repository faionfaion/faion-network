<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="methodologies Decision Tree" version="1.0.0" est_tokens="400">
  <decision-tree>
    <root-question>Does the task's verb+noun match exactly one keyword row in this dispatcher?</root-question>
    <branch when="keyword_match == 1">
      <question>Does Glob resolve the matched slug's AGENTS.md on disk?</question>
      <branch when="resolved == true">
        <conclusion ref="run-the-checklist"/>
      </branch>
      <branch when="resolved == false">
        <conclusion ref="r2"/>
      </branch>
    </branch>
    <branch when="keyword_match == 0 OR keyword_match > 1">
      <conclusion ref="skip-this-methodology"/>
    </branch>
    <example trace="task=scaffold pnpm config -> match=pnpm -> resolved -> run-the-checklist"/>
  </decision-tree>
</text>
