<?xml version="1.0" encoding="UTF-8"?>
<text id="01-routing-index" title="Software Developer Methodology Routing Index">
  <summary>Slug table mapping task domains to sibling folder names; dispatch rules for agents.</summary>

  <section title="Dispatch Rules">
    <rule>
      <statement>Always hop to the sibling folder. This index is a map, not a manual. Load the sibling AGENTS.md before implementing anything.</statement>
      <rationale>The stub code in this index is 5-line skeletons. Agents that quote them produce shallow output. The full authoritative content lives one folder away.</rationale>
    </rule>
    <rule>
      <statement>Validate agent names against the actual agents/ directory before delegating. Do not use names from historical rows in this index.</statement>
      <rationale>Historical agent names (faion-code-agent, faion-test-agent, faion-devops-agent) do not exist. Only faion-sdd-executor-agent, nero-sdd-executor-agent, and password-scrubber-agent are present.</rationale>
    </rule>
  </section>

  <section title="Routing Table">
    <list>
      <item>Python / Django patterns → django-coding-standards, django-testing</item>
      <item>Python modern features (3.12/3.13), async, type hints → best-practices-2026</item>
      <item>Go concurrency, goroutines, worker pools → go-concurrency</item>
      <item>Test-first development (any language) → tdd-workflow</item>
      <item>Storybook, component documentation, visual testing → storybook-setup</item>
      <item>CLAUDE.md / AGENTS.md creation or audit → documentation</item>
      <item>2026 best practices across TS/React/Python/AI → best-practices-2026</item>
      <item>DevOps, Docker, K8s, Terraform, CI/CD → pro/infra/ siblings (out of scope here)</item>
    </list>
  </section>

  <section title="Canonical Ordering for Cross-Cutting Tasks">
    <p>When a task spans multiple domains, apply siblings in this order: Python ecosystem → JS/TS → other backends → DevOps → docs. This is a sequencing guide, not a priority ranking.</p>
  </section>

  <section title="Deprecation Markers">
    <p>When a pattern here is superseded, edit this file to point to the replacement sibling. Do not expand this index back into a textbook — fragmentation into siblings was the correct architectural decision.</p>
  </section>
</text>
