<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="mocking-strategies Decision Tree" version="1.0.0" est_tokens="500">
  <decision-tree>
    <root-question>Is the existence/signature of the call part of the contract under test?</root-question>
    <branch when="call_is_contract == true">
      <conclusion ref="r1"/>
    </branch>
    <branch when="call_is_contract == false">
      <question>Is the collaborator a repository-shaped interface?</question>
      <branch when="repo_shaped == true">
        <conclusion ref="r5"/>
      </branch>
      <branch when="repo_shaped == false">
        <question>Does the test need a return value from the collaborator?</question>
        <branch when="needs_return == true">
          <conclusion ref="run-the-checklist"/>
        </branch>
        <branch when="needs_return == false">
          <conclusion ref="skip-this-methodology"/>
        </branch>
      </branch>
    </branch>
    <example trace="stripe.PaymentIntent.create needs state, not call contract -> stub via r1 (small) -> run-the-checklist"/>
  </decision-tree>
</text>
