<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="unit-testing">
  <metadata>
    <tier>free</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>Isolated, deterministic tests with one behavior per test using AAA. Names encode behavior, fixtures provide setup, mocks only at process boundaries (network, disk, clock).</summary>
    <tags>
      <tag>unit-testing</tag>
      <tag>pytest</tag>
      <tag>aaa-pattern</tag>
      <tag>test-doubles</tag>
      <tag>tdd</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-27</created>
    <languages>
      <language>python</language>
      <language>typescript</language>
    </languages>
  </metadata>

  <content>
    <title>Unit Testing</title>

    <summary>A methodology for writing isolated, deterministic tests that verify a single behavior per test using the Arrange-Act-Assert pattern. Each test names its scenario explicitly (`test_&lt;behavior&gt;_when_&lt;context&gt;`), uses fixtures or fakes for setup, and mocks only at process boundaries (network, disk, clock).</summary>

    <why>
      <p>Tests written without this discipline rot quickly: naming collisions, shared mutable state, and deep mocking make suites brittle — tests pass even when production behavior breaks. The AAA pattern separates concerns, the naming convention makes failures self-documenting, and the mock-at-boundary rule ensures fakes exercise real contracts rather than verify their own setup.</p>
    </why>

    <when-to-use>
      <list>
        <item>New code with branching logic, math, parsing, validation, or state transitions.</item>
        <item>Refactoring legacy code — safety net before changing internals.</item>
        <item>Bug fixes — write the failing test first, then fix.</item>
        <item>Boundary cases: empty inputs, min/max, off-by-one, unicode, time zones.</item>
        <item>TDD workflow: spec → failing test → impl → green → refactor.</item>
        <item>New code with branching logic, math, parsing, validation, or state transitions — pure functions and small classes.</item>
        <item>Refactor of legacy code where you need a safety net before changing internals.</item>
        <item>Bug fixes — write a failing test reproducing the bug FIRST, then fix.</item>
        <item>Boundary cases (empty inputs, max/min, off-by-one, time-zones, locale, unicode edge cases).</item>
        <item>Self-documenting executable specs alongside docs.</item>
      </list>
    </when-to-use>

    <when-not-to-use>
      <list>
        <item>Pure plumbing (controller → service → repo) — integration test is more honest.</item>
        <item>UI rendering — visual regression or snapshot tests catch more; unit tests on JSX are brittle.</item>
        <item>Network protocols, DB-specific queries, cache eviction — needs real dependency.</item>
        <item>Code ≤5 lines that is trivially correct (`return a + b`).</item>
        <item>High-churn experimental / spike code — tests rot faster than the code; defer.</item>
        <item>Pure plumbing (controller calls service calls repo) — integration test is more honest, fewer mocks.</item>
        <item>UI rendering — visual regression / snapshot tests catch more, unit tests on JSX/HTML brittle.</item>
        <item>Network protocols, DB-specific queries, cache eviction — needs a real dependency, integration territory.</item>
        <item>Code that's ≤5 lines and trivially correct (`def add(a, b): return a + b`) — coverage noise.</item>
        <item>High-churn experimental code (spike, prototype) — tests rot faster than the code; defer.</item>
      </list>
    </when-not-to-use>

    <section name="aaa-pattern">
      <rule>
        <statement>Structure every test as Arrange / Act / Assert with blank lines between phases.</statement>
        <rationale>The three-phase structure makes the intent of each test scannable and prevents interleaved setup-and-assertion patterns that obscure failures.</rationale>
      </rule>
    </section>

    <section name="naming">
      <rule>
        <statement>Name tests as test_&lt;behavior&gt;_when_&lt;context&gt; or test_should_&lt;expected&gt;_when_&lt;condition&gt;.</statement>
        <rationale>The name must be readable as a failure message. test_calculate_total_returns_zero_for_zero_quantity conveys more than test_calculate_total.</rationale>
      </rule>
      <rule>
        <statement>Use one test per behavior, not one test per method.</statement>
        <rationale>Methods have multiple behaviors (normal path, error path, edge case). Grouping by method makes suites non-exhaustive and failure messages ambiguous.</rationale>
      </rule>
    </section>

    <section name="isolation">
      <rule>
        <statement>Each test must be independent: no shared mutable state, no execution-order dependencies.</statement>
        <rationale>Shared mutable state causes intermittent failures that depend on test execution order — the hardest category of bug to diagnose.</rationale>
      </rule>
      <rule>
        <statement>Mock only at the process boundary (network, disk, external clock). Use fakes or in-memory implementations for internal collaborators.</statement>
        <rationale>MagicMock accepts any method call, including ones that do not exist on the real object. Fakes exercise real contracts. Mocking too deep tests the mock, not the behavior.</rationale>
      </rule>
      <rule>
        <statement>Use spec= or autospec=True when a unittest.mock.Mock is unavoidable.</statement>
        <rationale>Without spec, mock.foo.bar.baz() returns a Mock silently. spec= constrains the mock to the interface of the real object.</rationale>
      </rule>
    </section>

    <section name="fixtures">
      <rule>
        <statement>Prefer pytest fixtures over setUp methods: fixtures are composable, scope-aware (function / module / session), and parametrizable.</statement>
        <rationale>setUp runs before every test in the class regardless of need. Fixtures compose, can be shared across modules, and support teardown via yield.</rationale>
      </rule>
      <rule>
        <statement>Pin time and randomness in tests: use freezegun or time-machine for datetime.now(), and random.seed(0) for pseudo-random values.</statement>
        <rationale>Tests that fail twice a year at midnight UTC represent the worst class of flake: rare, environment-dependent, and hardest to reproduce locally.</rationale>
      </rule>
    </section>

    <section name="speed">
      <rule>
        <statement>Keep the unit test suite under 30 seconds total. If slower, the suite includes integration tests; mark and separate them.</statement>
        <rationale>A suite that takes minutes stops being run on every save. Speed is the primary property that makes unit tests useful as a feedback loop.</rationale>
      </rule>
    </section>

    <section name="aaa-python-example">
      <example type="good">
        <code lang="python"><![CDATA[
class TestPriceCalculator:
    def test_calculate_total_with_tax(self):
        # Arrange
        calculator = PriceCalculator(tax_rate=Decimal("0.10"))
        # Act
        result = calculator.calculate_total(price=Decimal("100.00"), quantity=1)
        # Assert
        assert result == Decimal("110.00")

    def test_calculate_total_with_zero_quantity_returns_zero(self):
        calculator = PriceCalculator(tax_rate=Decimal("0.10"))
        result = calculator.calculate_total(price=Decimal("50.00"), quantity=0)
        assert result == Decimal("0.00")
]]></code>
        <note>Blank lines between AAA phases. Each test covers one behavior.</note>
      </example>
    </section>

    <section name="parametrized-tests">
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.mark.parametrize("email,is_valid", [
    ("user@example.com", True),
    ("user.name@domain.co.uk", True),
    ("invalid", False),
    ("@no-local.com", False),
    ("user@", False),
])
def test_email_validation(email, is_valid):
    assert validate_email(email) == is_valid

@pytest.mark.parametrize("a,b,expected", [
    pytest.param(1, 2, 3, id="positive"),
    pytest.param(-1, 1, 0, id="mixed"),
    pytest.param(0, 0, 0, id="zeros"),
])
def test_addition(a, b, expected):
    assert add(a, b) == expected
]]></code>
        <note>pytest.param with id= produces readable failure messages. Named IDs beat positional indexes.</note>
      </example>
    </section>

    <section name="exception-testing">
      <example type="good">
        <code lang="python"><![CDATA[
def test_withdraw_more_than_balance_raises_error():
    account = BankAccount(balance=Decimal("100.00"))

    with pytest.raises(InsufficientFundsError) as exc_info:
        account.withdraw(Decimal("150.00"))

    assert exc_info.value.balance == Decimal("100.00")
    assert exc_info.value.amount == Decimal("150.00")
]]></code>
        <note>Use specific exception class, never bare pytest.raises(Exception). Assert exception attributes when they carry information.</note>
      </example>
    </section>

    <section name="fixtures-example">
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.fixture
def sample_user():
    return User(id="user-123", email="test@example.com", name="Test User")

@pytest.fixture
def user_service(sample_user):
    repo = InMemoryUserRepository()
    repo.add(sample_user)
    return UserService(repo)

class TestUserService:
    def test_find_user_by_id(self, user_service, sample_user):
        result = user_service.find_by_id(sample_user.id)
        assert result == sample_user

    def test_find_nonexistent_user_returns_none(self, user_service):
        assert user_service.find_by_id("nonexistent") is None
]]></code>
        <note>Fixtures compose: user_service depends on sample_user. InMemoryUserRepository is a fake, not a MagicMock.</note>
      </example>
    </section>

    <section name="test-doubles">
      <example type="good">
        <code lang="python"><![CDATA[
# Fake: working in-memory implementation
class FakeUserRepository:
    def __init__(self):
        self.users = {}

    def save(self, user):
        self.users[user.id] = user
        return user

    def find_by_id(self, user_id):
        return self.users.get(user_id)

# Mock: verify interactions at a process boundary
def test_sends_notification_after_order_complete():
    notifier = Mock()
    service = OrderService(notifier=notifier)

    service.complete_order("order-123")

    notifier.send_notification.assert_called_once_with("order-123", "Order completed")
]]></code>
        <note>Fakes for repositories; mocks for process boundaries (email, HTTP, clock). Never mock collaborators that have testable contracts.</note>
      </example>
    </section>

    <section name="typescript-jest-example">
      <example type="good">
        <code lang="typescript"><![CDATA[
describe('OrderService', () => {
  let service: OrderService;
  let mockRepo: jest.Mocked<OrderRepository>;
  let mockPayment: jest.Mocked<PaymentGateway>;

  beforeEach(() => {
    mockRepo = { save: jest.fn(), findById: jest.fn() };
    mockPayment = { charge: jest.fn() };
    service = new OrderService(mockRepo, mockPayment);
  });

  it('should save order and process payment', async () => {
    mockRepo.save.mockResolvedValue({ id: 'order-1' });
    mockPayment.charge.mockResolvedValue({ id: 'payment-1' });

    const result = await service.placeOrder({ items: [], total: 100 });

    expect(result.orderId).toBe('order-1');
    expect(mockPayment.charge).toHaveBeenCalledWith(100);
  });
});
]]></code>
        <note>beforeEach recreates mocks; no shared mutable state between tests.</note>
      </example>
    </section>

    <section name="antipatterns">
      <antipattern>
        <description>Testing implementation details: asserting that a private method was called or that internal state changed.</description>
        <reason>Tests break when the implementation is refactored even though the behavior is unchanged. Tests should assert observable outputs and side effects, not internal mechanics.</reason>
      </antipattern>
      <antipattern>
        <description>Over-mocking: replacing every collaborator with a MagicMock, then asserting on mock calls.</description>
        <reason>The test verifies the mock's configuration, not the behavior. MagicMock with no spec accepts calls to nonexistent methods, so tests pass even when the interface drifts.</reason>
      </antipattern>
      <antipattern>
        <description>Shared mutable state: class-level lists or dicts that tests append to without reset.</description>
        <reason>Execution-order dependency makes tests fail intermittently and only in certain run permutations. pytest-randomly surfaces these on day one.</reason>
      </antipattern>
      <antipattern>
        <description>Giant test methods: one test function with 10 asserts checking different behaviors.</description>
        <reason>When the first assertion fails, the test runner skips the rest. The failure message does not indicate which behavior broke. Split into one test per behavior.</reason>
      </antipattern>
      <antipattern>
        <description>No assertions: a test that runs code and passes regardless of output.</description>
        <reason>The test provides false coverage confidence. Static analysis cannot detect this; require at least one explicit assert per test in code review.</reason>
      </antipattern>
      <antipattern>
        <description>pytest.raises(Exception) catching too broadly.</description>
        <reason>Exception catches everything including KeyboardInterrupt and SystemExit. Always assert the specific exception class expected.</reason>
      </antipattern>
      <antipattern>
        <description>time.sleep() in tests for timing-dependent behavior.</description>
        <reason>Sleep-based tests are slow and still flaky if the machine is under load. Use freezegun.freeze_time, asyncio.sleep(0), or event-based waiting instead.</reason>
      </antipattern>
      <antipattern>
        <description>@patch decorator order reversed from parameter order.</description>
        <reason>@patch decorators apply bottom-up to parameters. @patch("a") @patch("b") def test(self, mock_b, mock_a). Reversing the mental model produces off-by-one mock assignments that are hard to debug.</reason>
      </antipattern>
    </section>

    <section name="where-it-fails-limitations">
      <list>
        <item>README's AAA example (`PriceCalculator`) is decent but never shows mocking — agents need patterns for collaborator isolation.</item>
        <item>No section on test doubles (dummy / stub / spy / mock / fake distinction). Agents reach for `MagicMock` even when a fake would be clearer.</item>
        <item>No guidance on **flaky test triage**: detection, quarantine, root-cause hierarchy (timing → I/O → state → env).</item>
        <item>No mention of property-based testing (hypothesis / fast-check) — entire classes of bugs missed.</item>
        <item>Naming conventions implied (`test_&lt;behavior&gt;_when_&lt;context&gt;`) but not enforced; agents will mix `test_create_user`, `test_user_creation_works`, `test_should_create_user`.</item>
        <item>No anti-pattern catalog (testing private methods, asserting log messages, global state mutation, time.sleep).</item>
        <item>Doesn't address parametrized tests — given snippet uses one assert per test, missing the leverage.</item>
      </list>
    </section>

    <section name="agentic-workflow">
      <p>For each unit under test: (1) read the unit; identify inputs (args, config, env), outputs (return, exceptions, side effects), and collaborators, (2) draft test names from the behavior matrix (one row per behavior, one column per relevant context), (3) AAA scaffold per row, (4) collaborator strategy: prefer fakes/stubs over mocks; only mock at process boundary (network/disk/clock), (5) run `pytest -x --tb=short`, (6) on red, fix the test or the code — never disable. CI: ban `pytest.mark.skip` without an issue link.</p>
      <list>
        <item>`faion-test-agent` — Default for writing unit tests + fixtures.</item>
        <item>`faion-code-agent` — Refactors untestable code into testable seams (DI, pure functions, command/query split).</item>
        <item>`faion-software-architect` — Decides where the unit/integration boundary sits per layer.</item>
        <item>`faion-sdd-execution` — TDD loop: spec → failing test → impl → green → refactor.</item>
        <item>`faion-devtools-developer` — Owns test infra (parallelization, sharding, flake detection).</item>
      </list>
      <prompt purpose="behavior-first-scaffold"><![CDATA[
Unit: src/billing/price_calculator.py:calculate_total.
Read it. List behaviors it must satisfy (one bullet per behavior with
inputs and expected output/exception). Then write pytest tests in
tests/billing/test_price_calculator.py:
  - One test class per behavior cluster.
  - AAA layout, blank lines between phases.
  - Names: test_<behavior>_when_<context>.
  - Use fixtures for shared setup. No mocks unless calling network/clock.
  - Cover edges: empty list, negative quantity, fractional tax rate
    (Decimal precision), zero subtotal.
Run: pytest tests/billing/test_price_calculator.py -v.
]]></prompt>
      <prompt purpose="bug-repro"><![CDATA[
Bug report: Issue #123 — calculate_total returns 0 for quantity=0.
1) Add a failing test in tests/billing/test_price_calculator.py named
   test_calculate_total_returns_zero_for_zero_quantity.
2) Run pytest, confirm it fails for the bug reason.
3) Fix code minimally to pass.
4) Re-run; commit test + fix together. Reference issue in commit body.
]]></prompt>
    </section>

    <section name="cli-tools">
      <list>
        <item>`pytest` — Python test runner. https://docs.pytest.org</item>
        <item>`pytest-xdist` — Parallel test execution. https://pytest-xdist.readthedocs.io</item>
        <item>`pytest-randomly` — Randomize test order to flush state leaks. https://github.com/pytest-dev/pytest-randomly</item>
        <item>`pytest-rerunfailures` — Quarantine flake detection (last resort). https://github.com/pytest-dev/pytest-rerunfailures</item>
        <item>`freezegun` — Pin `datetime.now()` in tests. https://github.com/spulec/freezegun</item>
        <item>`time-machine` — Faster freezegun alt. https://github.com/adamchainz/time-machine</item>
        <item>`hypothesis` — Property-based testing. https://hypothesis.readthedocs.io</item>
        <item>`vitest` / `jest` — JS/TS unit test runners. https://vitest.dev, https://jestjs.io</item>
        <item>`ts-jest` — Jest + TypeScript. https://kulshekhar.github.io/ts-jest/</item>
        <item>`go test ./... -race -count=1` — Go runner with race detector, no cache. (bundled)</item>
        <item>`cargo test` / `cargo nextest` — Rust runners (nextest faster). https://nexte.st</item>
        <item>`mocha` + `chai` — Node alt. https://mochajs.org</item>
      </list>
    </section>

    <section name="services-and-apps">
      <list>
        <item>GitHub Actions / GitLab CI (CI, agent-friendly) — Matrix on Python/Node versions; cache deps.</item>
        <item>CircleCI (CI, agent-friendly — orbs simplify pytest) — Test-splitting by timing.</item>
        <item>Buildkite (CI, agent self-hosted) — Good for mono-repos.</item>
        <item>Sauce Labs / BrowserStack (SaaS) — for E2E, not unit; skip for unit.</item>
        <item>pytest-html (Local, agent-friendly) — HTML report; useful for sharing failures.</item>
        <item>ReportPortal (OSS / SaaS, agent-friendly) — JUnit XML ingest; aggregate flake stats across runs.</item>
      </list>
    </section>

    <section name="templates-and-scripts-extras">
      <p>README has AAA. Add a behavior table generator + smoke runner (≤45 lines):</p>
      <example type="good" title="test-unit.sh fast feedback runner">
        <code lang="bash"><![CDATA[
#!/usr/bin/env bash
# scripts/test-unit.sh — fast feedback loop for unit tests.
set -euo pipefail
SCOPE="${1:-tests/}"
pytest "$SCOPE" \
  -x \
  --tb=short \
  -p no:cacheprovider \
  -m "not integration and not e2e and not slow" \
  --maxfail=1 \
  --randomly-seed=last \
  --durations=10
]]></code>
      </example>
      <example type="good" title="pyproject.toml markers">
        <code lang="toml"><![CDATA[
[tool.pytest.ini_options]
markers = [
  "slow: > 1s per test, excluded from -m 'not slow'",
  "integration: spans multiple units / hits real deps",
  "e2e: full stack",
]
addopts = "--strict-markers"
]]></code>
        <note>pyproject.toml markers (matches the script).</note>
      </example>
    </section>

    <section name="best-practices">
      <list>
        <item>**Behavior names, not function names.** `test_charges_zero_when_quantity_is_zero` beats `test_calculate_total_zero`.</item>
        <item>**One reason to fail per test.** Multiple asserts OK if checking aspects of one behavior; if asserting two different behaviors, split.</item>
        <item>**Fixtures over `setup_method`.** Composable, scope-aware (`function`, `module`, `session`), parametrizable.</item>
        <item>**Inject collaborators**, don't construct them inside the unit. Hard to test == bad design signal, refactor instead of mocking deep.</item>
        <item>**Mock at process boundary, fake elsewhere.** `requests.get` mock OK; `OrderRepository` should be a fake, not `MagicMock(spec=OrderRepository)` lying about the contract.</item>
        <item>**Pin time and randomness.** `freezegun.freeze_time(...)`, `random.seed(0)`. A test that fails twice a year at midnight UTC is the worst kind.</item>
        <item>**Ban `time.sleep` in tests.** Use `await asyncio.sleep(0)` in async, `freezegun.tick`, or refactor to await an event.</item>
        <item>**Run `pytest-randomly` from day 1.** Flushes order dependencies before they entrench.</item>
        <item>**Parametrize boundary cases.** README's password test is a model — error message asserted per row.</item>
        <item>**Fast unit suite &lt; 30s.** If slower, you're including integration; mark and split.</item>
        <item>**Tests live next to code.** `apps/users/tests/test_services.py`, not a top-level `tests/` mirror — easier to keep in sync on refactor.</item>
        <item>**No production logic in test helpers.** A bug in `make_user_with_address` masks bugs in `User.create`.</item>
        <item>**Assertion messages**: `assert x == 5, f"got {x}, expected 5 because ..."` — saves debugging on rare failures.</item>
      </list>
    </section>

    <section name="ai-agent-gotchas">
      <list>
        <item>**`MagicMock` accepts everything**: `mock.foo.bar.baz()` returns a Mock. Tests pass while production calls a method that doesn't exist. Use `spec=` or `autospec=True`.</item>
        <item>**`@patch` decorator order is reversed** vs argument order: bottom-up. `@patch("a") @patch("b") def test(self, mock_b, mock_a)`. Off-by-one bugs galore.</item>
        <item>**Patching the wrong path**: `@patch("foo.bar")` works only if `bar` was imported as `from foo import bar`. If the SUT does `import foo; foo.bar()`, you need `@patch("foo.bar")` AND it works; if SUT did `from foo import bar`, you must patch `sut_module.bar`.</item>
        <item>**Async tests need `pytest-asyncio`** + `@pytest.mark.asyncio` (or `asyncio_mode = "auto"` in config). Forgetting it makes the coroutine return without running.</item>
        <item>**`assert mock.called`** is True even after one call; use `mock.assert_called_once_with(...)` for stricter contracts.</item>
        <item>**Frozen time + DB**: `freezegun` doesn't freeze the DB server's clock. `created_at` set by `auto_now_add` may use server time, not client.</item>
        <item>**`pytest.raises(Exception)`** catches everything including `KeyboardInterrupt`. Use specific exception class.</item>
        <item>**Test order coupling**: a test that mutates `os.environ` without restoring poisons later tests. `monkeypatch` fixture restores automatically.</item>
        <item>**Class-level state** (`class Foo: items = []`) shared across tests if SUT uses class attributes. `setUp` doesn't reset it; explicit `Foo.items = []` per test.</item>
        <item>**Reading from `__pycache__`** can mask file-edit changes when running `pytest` repeatedly without `-p no:cacheprovider` — agents debug "why didn't my fix take effect".</item>
        <item>**Flaky test detection**: `pytest --count=10 -p pytest-repeat` runs same test 10x; cheap way to surface ordering / timing flakes.</item>
        <item>**Async + freezegun**: `time-machine` works, freezegun has known issues. Pick one.</item>
        <item>**Parametrize with mutable defaults**: `@pytest.mark.parametrize("x", [[]])` — if test mutates `x`, next run reuses same list. Use factories.</item>
      </list>
    </section>

    <templates>
      <template path="templates/pytest-runner.sh" purpose="Fast feedback loop script: pytest with markers, deduplication, duration report."/>
      <template path="templates/pyproject-markers.toml" purpose="pytest markers config: slow, integration, e2e, strict-markers."/>
    </templates>

    <see-also>
      <link href="https://docs.pytest.org"/>
      <link href="https://pytest-mock.readthedocs.io"/>
      <link href="https://hypothesis.readthedocs.io"/>
      <link href="https://github.com/spulec/freezegun"/>
      <link href="https://github.com/adamchainz/time-machine"/>
      <link href="http://xunitpatterns.com"/>
      <link href="https://realpython.com/python-mock-library/"/>
    </see-also>
  </content>
</methodology>
