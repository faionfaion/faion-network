<?xml version="1.0" encoding="UTF-8"?>
<text id="01-result-option-basics" title="Result and Option Fundamentals">
  <summary>Result and Option types, the ? propagation operator, and conversions between them.</summary>

  <section title="Core Types">
    <rule>
      <statement>Use Result&lt;T, E&gt; for every function that can fail for expected reasons. Use Option&lt;T&gt; for values that may legitimately be absent. Never use a sentinel value (0, -1, empty string) to represent absence.</statement>
      <rationale>Sentinel values are untyped contracts that callers can forget to check. Result and Option make the possibility of absence part of the type signature, enforced at compile time.</rationale>
    </rule>
  </section>

  <section title="The ? Operator">
    <rule>
      <statement>Use the ? operator to propagate errors up the call stack. It is equivalent to an early return with Err(e.into()). Use ? instead of match err { Ok(v) =&gt; v, Err(e) =&gt; return Err(e.into()) }.</statement>
      <rationale>Explicit match for every fallible call produces deeply nested code that obscures the happy path. ? makes fallible code as readable as exception-throwing code while remaining explicit.</rationale>
    </rule>

    <example type="good">
      <code lang="rust"><![CDATA[
use std::fs;
use std::io;

fn read_username() -> Result<String, io::Error> {
    let mut s = String::new();
    fs::File::open("username.txt")?.read_to_string(&mut s)?;
    Ok(s)
}
      ]]></code>
      <note>Two fallible operations, both propagated with ?. The happy path is linear and readable.</note>
    </example>
  </section>

  <section title="Converting Between Result and Option">
    <list>
      <item>option.ok_or(err) — Option to Result, providing an error value</item>
      <item>option.ok_or_else(|| err) — Option to Result, lazy error value</item>
      <item>result.ok() — Result to Option, discards the error</item>
      <item>result.err() — Result to Option of the error type</item>
    </list>
  </section>
</text>
