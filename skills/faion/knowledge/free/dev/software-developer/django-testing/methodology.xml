<?xml version="1.0" encoding="UTF-8"?>
<methodology slug="django-testing">
  <metadata>
    <tier>free</tier>
    <group>dev</group>
    <domain>software-developer</domain>
    <summary>pytest-based testing patterns for Django using model_bakery (or factory_boy) for fixtures, @pytest.mark.django_db for DB access, APIClient for DRF.</summary>
    <tags>
      <tag>django</tag>
      <tag>pytest</tag>
      <tag>testing</tag>
      <tag>fixtures</tag>
      <tag>model-bakery</tag>
    </tags>
    <difficulty>intermediate</difficulty>
    <created>2026-04-27</created>
  </metadata>
  <content>
    <title>Django Testing Reference</title>
    <summary>pytest-based testing patterns for Django projects using model_bakery (or factory_boy) for fixtures, @pytest.mark.django_db for DB access, and APIClient for DRF endpoint tests. The core rule: service-layer tests must call real code, not mocks; mock only at process boundaries (outbound HTTP, S3, third-party SDKs).</summary>
    <why>
      <p>Agents default to mocking every dependency (Java/.NET style), producing tests that pass while integration is broken. Django's baker.make(...) randomises fixture data, which hides value-specific bugs. Prescribing explicit fixture pinning and forbidding in-app mocks prevents both failure modes while keeping the test suite fast and readable.</p>
    </why>
    <when-to-use>
      <list>
        <item>Adding test coverage to a new Django app: pytest-django + model_bakery is the canonical default.</item>
        <item>Generating or reviewing service-layer tests where the "no mocks for in-app code" rule must be enforced.</item>
        <item>Migrating from unittest.TestCase to pytest style.</item>
        <item>Setting up parametrized tests for validation/calculation services.</item>
        <item>Adding DRF API tests without writing the full request stack manually.</item>
      </list>
    </when-to-use>
    <when-not-to-use>
      <list>
        <item>Non-Django Python projects — patterns assume @pytest.mark.django_db, app fixtures, Django settings.</item>
        <item>Pure unit tests of pure functions with no DB access — skip model_bakery overhead.</item>
        <item>Browser / E2E tests — use playwright (pytest-playwright).</item>
        <item>Performance / load testing — use locust or k6.</item>
        <item>Codebases standardized on unittest.TestCase where migration is out of scope.</item>
      </list>
    </when-not-to-use>
    <section name="fixture-library-selection">
      <rule>
        <statement>Use model_bakery.baker.make() as the default fixture strategy. Switch to factory_boy only when sequence-dependent or related-object chains are needed.</statement>
        <rationale>baker.make() requires no upfront factory definition and auto-fills required fields. factory_boy adds value for complex relationships and deterministic sequences.</rationale>
      </rule>
      <rule>
        <statement>Pin every field that the test assertion reads. Never assert on baker's random defaults.</statement>
        <rationale>Random data hides value-specific bugs. A test that asserts user.email == user.email (random vs random) always passes but catches nothing.</rationale>
      </rule>
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.mark.django_db
def test_activate_item_success(db):
    user = baker.make('users.User', is_active=True)
    item = baker.make('inventory.Item', code='ITEM-001', is_active=False, user=None)

    result = services.activate_user_item(user=user, item_code='ITEM-001')

    assert result.is_active is True   # concrete value assertion
    assert result.user == user        # concrete value assertion
        ]]></code>
      </example>
      <example type="bad">
        <code lang="python"><![CDATA[
def test_user_exists():
    user = baker.make('users.User')
    assert user  # truthy — always passes, catches nothing
        ]]></code>
        <note>Truthy assertion on baker output passes regardless of the object's state. Always assert specific field values.</note>
      </example>
      <rule>
        <statement>Use @pytest.mark.parametrize for value/expected pairs instead of duplicating test functions. A 10-line parametrized test beats 10 copy-paste functions.</statement>
      </rule>
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.mark.django_db
@pytest.mark.parametrize('amount,expected_status', [
    pytest.param(Decimal('100.00'), 'success', id='valid'),
    pytest.param(Decimal('0.00'), 'error', id='zero'),
    pytest.param(Decimal('-10.00'), 'error', id='negative'),
])
def test_order_validation(user, amount, expected_status):
    result = validate_order(user=user, amount=amount)
    assert result.status == expected_status
        ]]></code>
      </example>
      <reference path="templates/conftest.py">Reusable Django + DRF + baker fixtures</reference>
      <reference path="templates/pyproject-pytest.toml">pytest.ini_options with reuse-db, coverage 80%, random order</reference>
    </section>
    <section name="service-layer-tests">
      <rule>
        <statement>Test the service, not the model. Services contain branching logic; testing model field assignments is type-checking, not behavior verification.</statement>
      </rule>
      <rule>
        <statement>Forbid unittest.mock for in-process service calls. Mock only at the process boundary: outbound HTTP (responses/pytest-httpx), S3, third-party SDKs.</statement>
        <rationale>Mock-everything produces tests that pass while integration is broken. Service calls must execute the real code path.</rationale>
      </rule>
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.mark.django_db
def test_activate_item_not_found(user):
    with pytest.raises(inventory_models.Item.DoesNotExist):
        inventory_services.activate_user_item(
            user=user,
            item_code='invalid-code',
        )
        ]]></code>
      </example>
    </section>
    <section name="api-tests">
      <rule>
        <statement>Use reverse() for URLs in API tests, never hardcoded strings. Assert status code, response shape, and absence of leaked fields.</statement>
      </rule>
      <example type="good">
        <code lang="python"><![CDATA[
@pytest.mark.django_db
def test_user_detail_api(authed_client, user):
    url = reverse('user-detail', args=[user.id])
    response = authed_client.get(url)

    assert response.status_code == 200
    assert response.data['email'] == user.email
    assert 'password' not in response.data
        ]]></code>
      </example>
      <rule>
        <statement>Have at least one happy-path test that exercises the actual auth flow (token endpoint or client.login()), not just force_authenticate().</statement>
        <rationale>force_authenticate skips the real auth path. Auth bugs slip through when every test bypasses authentication entirely.</rationale>
      </rule>
    </section>
    <section name="quality-gate-checklist">
      <checklist>
        <check>Test calls real code, not just mocks</check>
        <check>Assertions check specific values, not truthiness</check>
        <check>Edge cases covered: None, empty list, negative, boundary values</check>
        <check>Test would fail if the code broke</check>
        <check>Time-dependent behavior uses freezegun or monkeypatch on timezone.now</check>
      </checklist>
    </section>
    <section name="mock-and-assertion-antipatterns">
      <antipattern>
        <description>Mocking in-app services with mocker.patch('apps.orders.services.create_order').</description>
        <reason>Mocking in-process code means the test passes regardless of what the service actually does. Only mock at process boundaries.</reason>
      </antipattern>
      <antipattern>
        <description>Writing assert result (truthy assertion) instead of assert result.status == 'success'.</description>
        <reason>Truthy assertions pass against any non-None return value. They provide no coverage of actual behavior.</reason>
      </antipattern>
      <antipattern>
        <description>Hardcoding URLs: api_client.get('/api/users/1/').</description>
        <reason>Hardcoded URLs break on URL refactoring. Use reverse() to decouple tests from URL structure.</reason>
      </antipattern>
    </section>
    <section name="fixture-antipatterns">
      <antipattern>
        <description>Pulling the user fixture into tests that don't use it.</description>
        <reason>Unused fixtures create unnecessary DB rows and slow the suite without benefit.</reason>
      </antipattern>
      <antipattern>
        <description>Using baker.make_recipe without registering a recipe first.</description>
        <reason>Runtime error. Use baker.make() unless a recipe is explicitly defined and registered.</reason>
      </antipattern>
    </section>
    <section name="async-and-time-antipatterns">
      <antipattern>
        <description>Time-dependent assertions like assert order.created_at > yesterday without time-freezing.</description>
        <reason>Flakes around midnight, across CI regions, and in DST transitions. Freeze time with freezegun or monkeypatch.</reason>
      </antipattern>
      <antipattern>
        <description>Using sync Client or APIClient for async Django views.</description>
        <reason>Async views require AsyncClient (Django 4.1+). Sync client misses async-only code branches.</reason>
      </antipattern>
    </section>
    <section name="coverage-antipatterns">
      <antipattern>
        <description>Adding tests that import code (raising line coverage) without asserting behavior.</description>
        <reason>Coverage percentage is signal, not a goal. Tests that don't assert catch no regressions.</reason>
      </antipattern>
    </section>
    <section name="agentic-workflow">
      <p>Use Claude subagents in three phases. (1) Test planner reads the service/view source and emits a list of test cases with names, inputs, and expected outcomes (no code yet). (2) Test author generates pytest files using the README templates: fixtures via baker for default cases, factory_boy for sequence-dependent ones, parametrize for branchy validation. (3) Test reviewer runs the suite, verifies coverage gain, applies the quality checklist, and rejects mock-only tests. The reviewer also enforces the assert on specific values (not just truthiness).</p>
    </section>
    <section name="where-it-fails-limitations">
      <p>model_bakery magic surprises. Random data hides bugs that only manifest on specific values; tests pass on baker.make(...) defaults but fail in prod. Pin critical fields explicitly.</p>
      <p>@pytest.mark.django_db reset cost. Default rollback strategy slows large suites; without --reuse-db or transactional fixtures, suites cross 5 minutes quickly.</p>
      <p>Fixture coupling. README's user and item fixtures are global; growing test files leak state across tests via db reuse.</p>
      <p>No coverage of factories vs. baker trade-off. README shows both; agents alternate per file, fragmenting the codebase.</p>
      <p>API tests bypass middleware. APIClient.get skips some middlewares depending on settings; tests pass while the real request fails. Use Client (Django) for full pipeline checks where it matters.</p>
      <p>Authentication tested superficially. The example api_client.get('/api/users/{id}/') doesn't exercise login flows; agents copy this and never test the auth boundary.</p>
      <p>No async/Celery test guidance. Modern Django uses async views and Celery for background work; the README is silent on pytest-asyncio and CELERY_TASK_ALWAYS_EAGER.</p>
      <p>assert response.data['email'] == user.email — silent serializer drift. Agents lock tests to internal serializer keys; renaming a field breaks tests for the wrong reason.</p>
      <p>Quality checklist is generic. "Test calls real code" is good but unenforced; without a custom plugin, mock-heavy tests still pass.</p>
    </section>
    <section name="best-practices">
      <p>Test the service, not the model. Services contain branching; testing model fields is type-checking. Pin fixture values that assertions read. baker.make("Item", price=Decimal("9.99")) — never assert on a random default. Parametrize value tables. A 10-line parametrized test beats 10 copy-paste test functions. Use --reuse-db locally, drop it in CI when migrations change. Add --create-db to the migration-changing PRs. Run pytest-randomly. Order-dependent tests are state leaks; surface them early. Seed factories deterministically per test (factory.fuzzy.reseed_random(42)) when reproducibility matters. Forbid unittest.mock in service tests. Mock only at the process boundary (HTTP, S3, third-party SDK) via responses / pytest-httpx. API test the contract. Assert status, response shape (use jsonschema if you have one), and absence of leaked fields ('password' not in response.data). Use freezegun or monkeypatch on timezone.now when behavior depends on time. Real clocks make tests flaky. pytest --collect-only after generation to confirm no broken imports before running the full suite. Keep DB tests &lt;5s each. Anything slower goes behind @pytest.mark.slow and runs only in nightly CI. Coverage is signal, not goal. 80% on services, 50% on views, 0% on migrations is healthier than 95% blanket.</p>
    </section>
    <section name="ai-agent-gotchas">
      <p>Mock-everything by default. Agents trained on Java / .NET examples mock every dependency; the README explicitly forbids this. Reject service tests with mocker.patch(...) on in-app modules. Random data assertions. Agents do assert user.email (truthy) — the test always passes. Force value assertions: assert user.email == "u1@example.com". Forgetting @pytest.mark.django_db. Test errors with "Database access not allowed"; agents then add @pytest.fixture(autouse=True) hacks. Just mark the function. Fixture overuse. Agents pull the user fixture into tests that don't use it, creating unused DB rows and slowing the suite. Hardcoded URLs in API tests. Agents write /api/users/1/; refactor breaks. Use reverse("user-detail", args=[user.id]). Asserting on serializer internals. response.data['profile']['bio'] couples to the serializer shape; rename → 100 broken tests. Assert via jsonschema or a stable subset. force_authenticate everywhere. Agents skip the actual auth flow; auth bugs slip through. Have at least one happy-path test that exercises client.login(...) or token endpoint. Time-dependent tests without freezing. assert order.created_at &gt; yesterday flakes around midnight, in DST transitions, and across CI regions. baker.make_recipe confusion. Agents call make_recipe without registering a recipe; runtime error. Stick to baker.make unless the recipe exists. factory_boy lazy attributes evaluated at import. Agents define email = factory.Faker(...) outside class Meta; faker fires once per test session. Use factory.LazyAttribute for per-instance. Skipping --reuse-db lock. Schema changes require --create-db; agents forget after a migration PR. Add a CI step that detects migration changes and toggles flags. Treating APIClient and Client as interchangeable. They handle auth and middleware differently; agents use the wrong one and tests pass while real requests fail. Async view tests with sync client. Async Django views need AsyncClient (Django 4.1+); agents use sync Client and miss async-only branches. Cassette drift. VCR/responses cassettes go stale; agents rerun tests, regenerate cassettes, lose the assertion of "we still call this endpoint." Lock cassettes to specific routes/headers. Coverage gaming. Agents add tests that import code (raising coverage) without asserting behavior. Reviewer must check assert density per file.</p>
    </section>
    <section name="recommended-subagents">
      <p>faion-sdd-executor-agent — gates Django PRs against the SDD test-plan.md; rejects merges that drop coverage on changed lines. faion-feature-executor (skill, not an agent file) — sequential SDD task execution; the test phase consumes this methodology. A purpose-built django-test-reviewer agent (worth creating): runs pytest --cov, parses the report, applies the quality checklist programmatically (zero mocks for service tests, value assertions, edge cases present). password-scrubber-agent — when sample fixtures contain plausible-looking emails/tokens that resemble real ones, scrub before sharing.</p>
    </section>
    <templates>
      <template path="templates/conftest.py" purpose="Reusable Django + DRF + baker fixtures (api_client, user, staff_user, authed_client, freeze_now)."/>
      <template path="templates/pyproject-pytest.toml" purpose="pytest.ini_options with --reuse-db, coverage floor 80%, randomised order."/>
    </templates>
    <see-also>
      <link href="https://docs.pytest.org"/>
      <link href="https://pytest-django.readthedocs.io"/>
      <link href="https://model-bakery.readthedocs.io"/>
      <link href="https://factoryboy.readthedocs.io"/>
      <link href="https://docs.djangoproject.com/en/stable/topics/testing/"/>
      <link href="https://www.django-rest-framework.org/api-guide/testing/"/>
    </see-also>
  </content>
</methodology>
