<?xml version="1.0" encoding="UTF-8"?>
<text id="03-antipatterns" title="Django Testing Antipatterns">
  <summary>Common agent failure modes in Django test generation and how to prevent them.</summary>

  <section title="Mock and Assertion Antipatterns">
    <antipattern>
      <description>Mocking in-app services with mocker.patch('apps.orders.services.create_order').</description>
      <reason>Mocking in-process code means the test passes regardless of what the service actually does. Only mock at process boundaries.</reason>
    </antipattern>
    <antipattern>
      <description>Writing assert result (truthy assertion) instead of assert result.status == 'success'.</description>
      <reason>Truthy assertions pass against any non-None return value. They provide no coverage of actual behavior.</reason>
    </antipattern>
    <antipattern>
      <description>Hardcoding URLs: api_client.get('/api/users/1/').</description>
      <reason>Hardcoded URLs break on URL refactoring. Use reverse() to decouple tests from URL structure.</reason>
    </antipattern>
  </section>

  <section title="Fixture Antipatterns">
    <antipattern>
      <description>Pulling the user fixture into tests that don't use it.</description>
      <reason>Unused fixtures create unnecessary DB rows and slow the suite without benefit.</reason>
    </antipattern>
    <antipattern>
      <description>Using baker.make_recipe without registering a recipe first.</description>
      <reason>Runtime error. Use baker.make() unless a recipe is explicitly defined and registered.</reason>
    </antipattern>
  </section>

  <section title="Async and Time Antipatterns">
    <antipattern>
      <description>Time-dependent assertions like assert order.created_at > yesterday without time-freezing.</description>
      <reason>Flakes around midnight, across CI regions, and in DST transitions. Freeze time with freezegun or monkeypatch.</reason>
    </antipattern>
    <antipattern>
      <description>Using sync Client or APIClient for async Django views.</description>
      <reason>Async views require AsyncClient (Django 4.1+). Sync client misses async-only code branches.</reason>
    </antipattern>
  </section>

  <section title="Coverage Antipatterns">
    <antipattern>
      <description>Adding tests that import code (raising line coverage) without asserting behavior.</description>
      <reason>Coverage percentage is signal, not a goal. Tests that don't assert catch no regressions.</reason>
    </antipattern>
  </section>
</text>
