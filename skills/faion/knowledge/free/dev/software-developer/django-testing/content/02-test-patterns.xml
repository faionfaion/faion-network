<?xml version="1.0" encoding="UTF-8"?>
<text id="02-test-patterns" title="Django Test Patterns and Quality Checklist">
  <summary>Service-layer test rules, API test patterns with APIClient, and the quality gate checklist every test must pass.</summary>

  <section title="Service Layer Tests">
    <rule>
      <statement>Test the service, not the model. Services contain branching logic; testing model field assignments is type-checking, not behavior verification.</statement>
    </rule>
    <rule>
      <statement>Forbid unittest.mock for in-process service calls. Mock only at the process boundary: outbound HTTP (responses/pytest-httpx), S3, third-party SDKs.</statement>
      <rationale>Mock-everything produces tests that pass while integration is broken. Service calls must execute the real code path.</rationale>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
@pytest.mark.django_db
def test_activate_item_not_found(user):
    with pytest.raises(inventory_models.Item.DoesNotExist):
        inventory_services.activate_user_item(
            user=user,
            item_code='invalid-code',
        )
]]></code>
    </example>
  </section>

  <section title="API Tests">
    <rule>
      <statement>Use reverse() for URLs in API tests, never hardcoded strings. Assert status code, response shape, and absence of leaked fields.</statement>
    </rule>
    <example type="good">
      <code lang="python"><![CDATA[
@pytest.mark.django_db
def test_user_detail_api(authed_client, user):
    url = reverse('user-detail', args=[user.id])
    response = authed_client.get(url)

    assert response.status_code == 200
    assert response.data['email'] == user.email
    assert 'password' not in response.data
]]></code>
    </example>
    <rule>
      <statement>Have at least one happy-path test that exercises the actual auth flow (token endpoint or client.login()), not just force_authenticate().</statement>
      <rationale>force_authenticate skips the real auth path. Auth bugs slip through when every test bypasses authentication entirely.</rationale>
    </rule>
  </section>

  <section title="Quality Gate Checklist">
    <list>
      <item>Test calls real code, not just mocks</item>
      <item>Assertions check specific values, not truthiness</item>
      <item>Edge cases covered: None, empty list, negative, boundary values</item>
      <item>Test would fail if the code broke</item>
      <item>Time-dependent behavior uses freezegun or monkeypatch on timezone.now</item>
    </list>
  </section>
</text>
