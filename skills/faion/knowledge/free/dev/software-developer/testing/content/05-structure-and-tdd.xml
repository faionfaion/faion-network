<?xml version="1.0" encoding="UTF-8"?>
<text id="05-structure-and-tdd" title="Test Structure and TDD Workflow">
  <summary>AAA and Given-When-Then patterns, test isolation rules, coverage practices, and red-green-refactor cycle.</summary>

  <section title="Test Structure Patterns">
    <rule>
      <statement>Every test must follow Arrange-Act-Assert: set up state, perform one action, assert one outcome. A test that asserts multiple outcomes should be split into multiple tests.</statement>
      <rationale>Single-outcome tests produce precise failure messages. Multi-outcome tests mask which assertion failed and why.</rationale>
    </rule>

    <example type="good">
      <code lang="python"><![CDATA[
def test_user_creation():
    # Arrange
    service = UserService()
    user_data = {"name": "John", "email": "john@example.com"}

    # Act
    user = service.create(user_data)

    # Assert
    assert user.id is not None
    assert user.name == "John"
      ]]></code>
    </example>
  </section>

  <section title="Test Isolation">
    <rule>
      <statement>Tests must not share state. Each test must be able to run in any order and produce the same result. Use autouse fixtures to reset state rather than relying on test ordering.</statement>
      <rationale>Order-dependent tests hide bugs that only surface in CI when test discovery order differs from local run order.</rationale>
    </rule>
  </section>

  <section title="Coverage Practices">
    <rule>
      <statement>Set coverage thresholds at the current measured value, then raise incrementally. Never set 100% from day one — it drives test-gaming (adding assertions that execute code without verifying behavior).</statement>
      <rationale>100% coverage targets produce tests that assert constants, mock everything, and exercise code paths without validating outcomes.</rationale>
    </rule>

    <rule>
      <statement>Exclude generated code, __init__.py files, and TYPE_CHECKING blocks from coverage. Track branch coverage, not just line coverage.</statement>
      <rationale>Line coverage misses conditional branches. A function can be 100% line-covered with one test that never hits the else branch.</rationale>
    </rule>
  </section>

  <section title="TDD Red-Green-Refactor">
    <rule>
      <statement>Write a failing test (RED) before writing any implementation. Write the minimum code to make it pass (GREEN). Refactor both code and test with the safety net in place (REFACTOR). Never skip the red step.</statement>
      <rationale>Skipping RED means the test may never have been failing, which means it may not actually test what it claims. A test that never fails is a test that provides no safety.</rationale>
    </rule>
  </section>
</text>
