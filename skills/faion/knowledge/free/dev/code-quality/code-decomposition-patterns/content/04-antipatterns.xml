<?xml version="1.0" encoding="UTF-8"?>
<text id="04-antipatterns" title="Decomposition Antipatterns">
  <summary>Common failures when agents apply decomposition patterns: leaky abstractions, over-extraction, missed import updates, and barrel re-exports.</summary>

  <antipattern>
    <description>Leaky service: extracted service method accepts request/response/HttpContext parameters.</description>
    <reason>Framework types in service signatures tie the service to the HTTP layer, defeating testability. Services must accept only plain domain types.</reason>
  </antipattern>

  <antipattern>
    <description>Over-extraction: agent creates a micro-service for a 5-line helper with a single caller.</description>
    <reason>Unnecessary files increase cognitive overhead without adding testability or reusability. Apply a minimum threshold: a service/component must be &gt;=40 lines or have &gt;=3 callers to justify extraction.</reason>
  </antipattern>

  <antipattern>
    <description>Import update misses: agent moves a file but only updates the moved file, not its importers.</description>
    <reason>Broken imports cause runtime errors that only appear when the code path is exercised. Always run a grep pass on importers + `tsc --noEmit` or `python -c 'import app'` after each move.</reason>
  </antipattern>

  <antipattern>
    <description>Barrel re-export sprawl: agent adds `export *` in index.ts to make imports convenient after Extract Module.</description>
    <reason>`export *` re-exports everything including internals, breaks tree-shaking, creates implicit public APIs, and causes circular dependency risks. Forbid `export *`; export named symbols only.</reason>
  </antipattern>

  <antipattern>
    <description>Settings circular import: env file (production.py) imports from base.py which imports from production.py.</description>
    <reason>Circular imports in config fail at import time with an inscrutable error. Enforce one-way load: __init__.py imports env file; env file imports base; base imports nothing from the settings package.</reason>
  </antipattern>

  <antipattern>
    <description>Doc drift: pattern application without updating the directory's AGENTS.md in the same PR.</description>
    <reason>AGENTS.md file tables go stale immediately after a structural change. Always update AGENTS.md in the same commit as the decomposition.</reason>
  </antipattern>
</text>
