# Refactoring Patterns

## Summary

A closed catalog of eight structure-preserving transforms — extract method/class, replace conditional with polymorphism, introduce parameter object, replace magic numbers, decompose conditional, rename for clarity, move method — applied one at a time behind a green test suite. Each refactor is a single commit. External behavior must remain identical after every step.

## Why

Behavior-preserving transforms in small steps make code easier to understand and extend without introducing regressions. Martin Fowler's research and broad industry practice confirm that incremental, test-guarded refactoring lowers bug rates and reduces feature-delivery friction. Mixing refactoring with feature work in the same commit removes the ability to bisect regressions.

## When To Use

- Code works but is hard to understand or touch without fear.
- Adding a feature would require changes in many unrelated files.
- Duplicate logic exists across the codebase.
- A class or function has grown beyond a single clear responsibility.
- Before adding new functionality ("make the change easy, then make the easy change").
- Spreading a rename or API change across many call sites in a monorepo.

## When NOT To Use

- No test coverage on the target code — refactoring without a safety net silently changes behavior.
- Cross-cutting architectural rewrite (monolith → services, CQRS introduction) — use software-architect patterns instead.
- Refactors require domain knowledge the agent lacks (regulatory rules, financial rounding, license-encoded behavior).
- During incidents or release-freeze windows — risk outweighs benefit.
- Code generated by another agent in the same session — re-editing for taste, not improvement.

## Content

| File | What's inside |
|------|---------------|
| `content/01-catalog.xml` | Eight patterns with before/after code examples and when-to-apply rules. |
| `content/02-workflow.xml` | Safe refactoring loop: test gate → one pattern → run tests → commit. Anti-patterns and gotchas. |

## Templates

| File | Purpose |
|------|---------|
| `templates/refactor-loop.sh` | One-pattern-per-commit bash loop: plan → apply diff → test → commit or revert. |
| `templates/planner-prompt.txt` | Agent prompt: pick ONE catalog pattern, justify, output unified diff + test command. |
