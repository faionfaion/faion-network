<?xml version="1.0" encoding="UTF-8"?>
<text id="01-stack-checklist" title="2026 Stack Quality Checklist">
  <summary>Per-stack quality gates for TypeScript, Python, and React — used to generate configs and audit existing repos.</summary>

  <section title="TypeScript Projects">
    <rule>
      <statement>Enable strict mode in tsconfig.json; ban the any type (use unknown instead); add explicit return types on all public functions.</statement>
      <rationale>TypeScript strict mode catches a class of runtime errors at compile time; any defeats the type system entirely.</rationale>
    </rule>
    <list>
      <item>strict: true in tsconfig.json</item>
      <item>No any types — use unknown with type guards</item>
      <item>Explicit return types on public functions</item>
      <item>ESLint flat config + Prettier configured</item>
      <item>tsc --noEmit in CI</item>
    </list>
  </section>

  <section title="Python Projects">
    <rule>
      <statement>Require type hints on all functions, mypy --strict enabled, ruff replacing black/isort/flake8, pytest with branch coverage &gt;= 80%.</statement>
      <rationale>ruff is 10-100x faster than the tools it replaces and enforces the same rule set; mypy strict catches annotation gaps that lead to runtime AttributeErrors.</rationale>
    </rule>
    <list>
      <item>Type hints on all functions</item>
      <item>mypy --strict in CI</item>
      <item>ruff for lint + format (replaces black, isort, flake8)</item>
      <item>pytest --cov with branch=true, fail_under=80</item>
    </list>
  </section>

  <section title="React Projects">
    <rule>
      <statement>Use Server Components by default; minimize client boundaries; verify accessibility with axe-core; enforce Lighthouse score &gt; 90 in CI.</statement>
      <rationale>React 19 Server Components reduce JS bundle size by default; early a11y checks are cheaper than retrofits.</rationale>
    </rule>
    <list>
      <item>TypeScript strict mode</item>
      <item>Server Components where possible; minimize use client</item>
      <item>axe-core a11y check in CI</item>
      <item>Lighthouse CI score &gt; 90</item>
      <item>Performance budget: FCP &lt; 1.8s, TTI &lt; 3.8s</item>
    </list>
  </section>

  <section title="Universal Security Items">
    <list>
      <item>Input validation on all external inputs</item>
      <item>No hardcoded secrets (gitleaks / trufflehog in CI)</item>
      <item>SQL injection prevention (parameterized queries only)</item>
      <item>Automated dependency security scans (pip-audit, npm audit)</item>
      <item>CSRF tokens on state-mutating endpoints</item>
    </list>
    <antipattern>
      <description>Treating security checklist items as binary tickboxes without threat modeling.</description>
      <reason>Real security requires understanding attack vectors, not just checking boxes; pair with security-review skill for sensitive changes.</reason>
    </antipattern>
  </section>

  <section title="CI Required Checks">
    <list>
      <item>Lint (ESLint flat config / ruff)</item>
      <item>Type check (tsc --noEmit / mypy --strict)</item>
      <item>Unit tests with branch coverage gate</item>
      <item>Security scan</item>
      <item>Build verification</item>
    </list>
  </section>
</text>
