# Python Basics

## Summary

Foundational Python patterns for 3.12+ projects: data structures, control flow, functions, OOP, error handling, file I/O, and tooling (uv, ruff, mypy). Core rule: use built-in generic syntax (`list[int]`, `dict[str, int]`, `X | None`), prefer `uv` for all environment and dependency management, and use `ruff` for both lint and format.

## Why

Legacy patterns (`from typing import List`, `os.path`, bare `except:`, mutable default args) are the most common source of bugs in Python code reviewed by LLMs and generated by agents. Establishing idiomatic 3.12+ patterns at project bootstrap prevents an entire class of regressions and drift.

## When To Use

- Bootstrapping a fresh Python 3.12+ project (`pyproject.toml`, `uv`, `ruff`, `mypy`, `pytest`).
- Code review where the issue is fundamentals: mutable defaults, EAFP misuse, legacy typing imports.
- Refactoring legacy 3.7/3.8 code to 3.12+ idioms.
- Generating idiomatic examples or onboarding content.
- Style/idiom gate in CI (ruff `UP`, `B`, `E`, `F` rule sets).

## When NOT To Use

- Inside a domain-specific feature (Django model, FastAPI route) — load the domain methodology; this overview is too generic.
- Performance optimization — route to `python-modern-2026/` or specialized profiling tooling.
- Type-system depth (generics, ParamSpec, Protocol) — route to `python-typing/`.
- Async patterns — route to `python-async/`.
- Testing strategies — route to a pytest methodology.

## Content

| File | What's inside |
|------|---------------|
| `content/01-data-structures.xml` | List/dict/set/tuple idioms, comprehensions, `collections` module usage. |
| `content/02-functions-classes.xml` | Type hints, keyword-only args, decorators with `functools.wraps`, dataclasses, Protocols, context managers. |
| `content/03-error-handling.xml` | EAFP pattern, narrow try blocks, exception hierarchy, `dict.get()` vs try/except KeyError. |
| `content/04-tooling.xml` | uv commands, pyproject.toml structure, ruff config, mypy strict setup. |

## Templates

| File | Purpose |
|------|---------|
| `templates/pyproject.toml` | Modern pyproject.toml with ruff (line-length 100), mypy strict, pytest config. |
| `templates/bootstrap.sh` | One-shot project bootstrap: uv init, dev deps, ruff/mypy/pytest config. |
