<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="django-imports Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for django-imports. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Organize imports in this order: (1) __future__, (2) standard library, (3) third-party (Django, DRF, Celery, etc.), (4) cross-app absolute imports, (5) local relative imports. Separate each group with a blank line.</statement>
      <rationale source="v1-source">PEP 8 defines this order. Django adds the cross-app vs local-relative distinction. Ruff I (isort) with known-first-party enforcement automates this — developers should not sort manually.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use absolute imports for Django core (from django.db import models), third-party packages, and cross-app imports. Use single-dot relative imports (from .models import User) within the same app only.</statement>
      <rationale source="v1-source">Absolute imports make the module's location unambiguous when reading the file in isolation. Single-dot relative imports are idiomatic within an app and survive app renaming. Multi-dot relative imports (from ...users.models) are fragile and break on any restructuring.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Never use multi-dot relative imports (from .., from ...). Replace them with absolute cross-app imports using the full apps.X.Y path.</statement>
      <rationale source="v1-source">Multi-dot relative imports are fragile — any reorganization of the directory tree breaks them. They also confuse readers about where the module lives. Absolute cross-app imports are always unambiguous regardless of nesting depth.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>All cross-app imports MUST use module-level aliases: from apps.users import models as user_models. Even when no current naming collision exists, future apps will introduce one.</statement>
      <rationale source="v1-source">Unaliased cross-app imports (from apps.users.models import User) are the primary cause of ImportError: cannot import name 'User' circular-dependency crashes. An alias makes the origin explicit (user_models.User vs order_models.User) and prevents silent shadowing when two apps export the same name.</rationale>
    </rule>
    <rule id="r5" testable="true" depth="essential">
      <statement>Apply circular dependency strategies in this order: (1) string FK references for model fields, (2) apps.get_model() for runtime model access in services, (3) TYPE_CHECKING guard for type-only imports, (4) function-scoped imports as a last resort, (5) extract shared types to a core/ app if multiple strategies are needed in the same module.</statement>
      <rationale source="v1-source">Each strategy has a different scope and cost. String FK references are zero-cost and the standard Django model pattern. apps.get_model() is safe but verbose. TYPE_CHECKING guards work for types but break runtime isinstance checks. Function-scoped imports add call overhead. core/ extraction is a structural fix for systemic circular dependency clusters.</rationale>
    </rule>
    <rule id="r6" testable="true" depth="essential">
      <statement>Always use string references in ForeignKey and ManyToManyField definitions: ForeignKey('users.User', ...) instead of ForeignKey(User, ...). This is the Django standard and makes the model layer circular-import-proof.</statement>
      <rationale source="v1-source">String references ('app_label.ModelName') are resolved by Django's app registry after all apps have loaded. Direct import references create load-order dependencies between apps that can cause circular import failures depending on INSTALLED_APPS order.</rationale>
    </rule>
    <rule id="r7" testable="true" depth="essential">
      <statement>Use apps.get_model('app_label', 'ModelName') in service/selector functions that need model classes at runtime but cannot import them at module level due to circular deps.</statement>
      <rationale source="v1-source">apps.get_model() returns the model class from Django's app registry, which is fully populated after django.setup(). It moves the import from module load time to function call time, breaking the circular import chain without coupling the module to a specific import path.</rationale>
    </rule>
    <rule id="r8" testable="true" depth="essential">
      <statement>Move imports needed only for type hints into if TYPE_CHECKING: blocks. Combine with from __future__ import annotations to avoid evaluating the annotation strings at runtime.</statement>
      <rationale source="v1-source">TYPE_CHECKING is False at runtime — imports inside the block are never executed. Type checkers (mypy, pyright) treat TYPE_CHECKING as True and follow the imports for analysis. This eliminates circular imports caused by cross-app type annotations.</rationale>
    </rule>
    <rule id="r9" testable="true" depth="essential">
      <statement>When three or more apps have circular import relationships, extract the shared types, constants, or base classes into a core/ (or common/) app. The core/ app imports from no other app.</statement>
      <rationale source="v1-source">Circular import clusters indicate shared concepts that belong in a common layer. Moving them to core/ establishes a dependency direction: all apps can import from core/, but core/ never imports from other apps. This pattern resolves the entire cluster in one structural change.</rationale>
    </rule>
    <rule id="r10" testable="true" depth="essential">
      <statement>Configure ruff with known-first-party = ["apps"] and section-order to place cross-app imports in their own section. Run ruff check --select I --fix on staged files in pre-commit.</statement>
      <rationale source="v1-source">Without known-first-party configured, ruff treats apps.users as a third-party package and merges it with Django imports. The separate section makes the cross-app dependency graph visible at a glance.</rationale>
    </rule>
  <rule id="r4-alias-convention" testable="true" depth="essential"><statement>Stub rule for conclusion 'r4-alias-convention' referenced from 06-decision-tree.xml. Replace with the real testable rule that resolves this branch of the tree.</statement><rationale source="phase-d-autofix">Auto-added by fix-methodology-phase-d.py to keep tree refs resolvable.</rationale></rule><rule id="r6-string-fk" testable="true" depth="essential"><statement>Stub rule for conclusion 'r6-string-fk' referenced from 06-decision-tree.xml. Replace with the real testable rule that resolves this branch of the tree.</statement><rationale source="phase-d-autofix">Auto-added by fix-methodology-phase-d.py to keep tree refs resolvable.</rationale></rule><rule id="r7-apps-get-model" testable="true" depth="essential"><statement>Stub rule for conclusion 'r7-apps-get-model' referenced from 06-decision-tree.xml. Replace with the real testable rule that resolves this branch of the tree.</statement><rationale source="phase-d-autofix">Auto-added by fix-methodology-phase-d.py to keep tree refs resolvable.</rationale></rule><rule id="r8-type-checking" testable="true" depth="essential"><statement>Stub rule for conclusion 'r8-type-checking' referenced from 06-decision-tree.xml. Replace with the real testable rule that resolves this branch of the tree.</statement><rationale source="phase-d-autofix">Auto-added by fix-methodology-phase-d.py to keep tree refs resolvable.</rationale></rule><rule id="r9-core-extraction" testable="true" depth="essential"><statement>Stub rule for conclusion 'r9-core-extraction' referenced from 06-decision-tree.xml. Replace with the real testable rule that resolves this branch of the tree.</statement><rationale source="phase-d-autofix">Auto-added by fix-methodology-phase-d.py to keep tree refs resolvable.</rationale></rule></section>
</text>