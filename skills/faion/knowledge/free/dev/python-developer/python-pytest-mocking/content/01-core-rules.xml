<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="python-pytest-mocking Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for python-pytest-mocking. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Always patch the name where it is USED (imported into), not where it is DEFINED. If module_a imports helper_function from module_b and you want to intercept the call in module_a, patch "module_a.helper_function", not "module_b.helper_function".</statement>
      <rationale source="v1-source">When Python executes "from module_b import helper_function", it binds the name helper_function in module_a's namespace. Patching module_b.helper_function replaces the original but module_a.helper_function still points to the old binding. Patching where it is used replaces the binding that the code under test actually calls.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>Use mocker.patch.object(cls, "method", autospec=True) or mocker.create_autospec(RealClass) when mocking non-trivial collaborators. Never use a plain mocker.Mock() for objects whose interface you do not own or that evolve frequently.</statement>
      <rationale source="v1-source">A plain Mock accepts any call signature. If the real method's signature changes, the mock silently accepts the old call and the test keeps passing while production code breaks. autospec catches the mismatch at test time.</rationale>
    </rule>
  </section>
</text>
