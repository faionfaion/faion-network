<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="python-pytest-parametrize Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Migrated testable rules for python-pytest-parametrize. Verify and refine before relying on for production.</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Every @pytest.mark.parametrize call that produces more than three test cases MUST supply ids= (or use pytest.param(..., id=...)) so failures print as test_x[descriptive-id] rather than test_x[arg0-arg1].</statement>
      <rationale source="v1-source">Opaque IDs like "arg0" force developers to count positions in the params list to understand which case failed. Descriptive IDs make CI output actionable without opening the source file.</rationale>
    </rule>
  </section>
</text>
