<text id="unit-principles" title="Unit Testing Principles">

  <summary>
    FIRST properties, Arrange-Act-Assert structure, test naming conventions, and what constitutes a single unit.
  </summary>

  <section title="FIRST Properties">
    <list>
      <item>Fast — unit tests must run in milliseconds. No I/O, no network, no sleep.</item>
      <item>Isolated — each test sets up its own state and does not depend on other tests or global state.</item>
      <item>Repeatable — produces identical results on any machine, any time, any order.</item>
      <item>Self-validating — has a binary pass/fail outcome. No manual inspection of logs.</item>
      <item>Timely — written at the same time as (or before) the production code they test.</item>
    </list>
    <rule>If a test violates any FIRST property, it is not a unit test — it is an integration or system test.</rule>
  </section>

  <section title="Arrange-Act-Assert">
    <rule>Structure every test with a blank line separating Arrange, Act, and Assert sections.</rule>
    <rule>One test = one Act. Multiple assertions on the result of a single Act are acceptable.</rule>
    <example type="good">
      <code lang="python"><![CDATA[
def test_cart_applies_percentage_discount():
    # Arrange
    cart = ShoppingCart()
    cart.add_item(price=100.0)
    cart.add_item(price=50.0)

    # Act
    total = cart.total(discount_percent=20)

    # Assert
    assert total == 120.0
      ]]></code>
    </example>
    <example type="bad">
      <description>Multiple acts in one test — splits into two separate tests</description>
      <code lang="python"><![CDATA[
def test_cart():
    cart = ShoppingCart()
    cart.add_item(price=100.0)
    assert len(cart.items) == 1  # act + assert 1
    cart.add_item(price=50.0)
    assert len(cart.items) == 2  # act + assert 2 — should be a separate test
      ]]></code>
    </example>
  </section>

  <section title="Naming Conventions">
    <rule>Name tests to describe what they test, not how they implement it.</rule>
    <rule>Preferred formats:</rule>
    <list>
      <item>method_scenario_expected: add_item_when_duplicate_increments_quantity</item>
      <item>should_behavior_when_condition: should_raise_when_price_is_negative</item>
      <item>given_when_then: given_premium_user_when_checkout_then_discount_applied</item>
    </list>
    <rule>Test class names: TestClassName (groups tests for the same class).</rule>
    <rule>Test function names must start with test_ for pytest discovery.</rule>
  </section>

  <section title="What Is a Unit">
    <rule>A unit is the smallest piece of behavior that can be tested in isolation.</rule>
    <rule>A unit is NOT always a single function — it can be a cluster of collaborating classes if they form a coherent behavior.</rule>
    <rule>Replace external dependencies (I/O, time, randomness) with test doubles. Keep internal collaborators real.</rule>
  </section>

</text>
