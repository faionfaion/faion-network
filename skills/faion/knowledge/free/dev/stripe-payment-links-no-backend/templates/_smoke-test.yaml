# purpose: minimum-viable filled-in catalog for stripe-payment-links-no-backend
# consumes: nothing — runnable as-is through the methodology
# produces: deterministic default-branch decision-record (r-payment-link)
# depends-on: catalog.yaml shape
# token-budget-impact: ~80 tokens

catalog:
  - sku: smoke-29
    price_cents: 2900
    currency: usd
    name: "Smoke Test Lifetime Deal"
    fulfill_action: email-license-key

drivers:
  billing_model: one-off
  custom_amount_per_buyer: false
  connect_required: false

fulfillment_channels:
  - {channel: email, address: smoke@faion.net}

webhook_events_needed:
  - checkout.session.completed

idempotency_store: "https://docs.google.com/spreadsheets/d/SMOKE/edit"
owner: smoke-test@faion.net
