<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="WCAG 2.2 Compliance Procedure" version="1.1.0" est_tokens="700" depth="essential">
  <summary>6-step procedure to apply the methodology end-to-end, from the 2.1 baseline through the delta scan to the CI gate.</summary>

  <section title="Procedure">

    <step n="1" name="import-2-1-baseline">
      <input>Existing WCAG 2.1 AA audit</input>
      <action>Load the 2.1 AA findings as the baseline and subtract the criteria 2.2 removed (4.1.1 Parsing). What remains is the floor this audit assumes; the 9 new SC are the delta on top of it.</action>
      <output>Baseline the delta is measured against</output>
      <decision-gate>No 2.1 AA baseline means no delta — stop and run the full a11y audit first.</decision-gate>
    </step>

    <step n="2" name="scope-audit">
      <input>Page + component inventory</input>
      <action>Define scope (which templates + components are in/out).</action>
      <output>scope string</output>
    </step>

    <step n="3" name="run-automated-checks">
      <input>Scope + axe-core</input>
      <action>Run axe-core / Lighthouse over each page; capture violations per SC.</action>
      <output>Automated findings</output>
      
    </step>

    <step n="4" name="manual-sc-checks">
      <input>Automated findings + 9 SC checklist</input>
      <action>Manually verify SC that automated tools can't fully cover (3.2.6, 3.3.8).</action>
      <output>sc_results JSON</output>
      <decision-gate>If any SC blocked by missing context, mark not-applicable with rationale.</decision-gate>
    </step>

    <step n="5" name="compute-overall">
      <input>sc_results JSON</input>
      <action>Overall = AA-conformant only if all 9 SC are pass or not-applicable.</action>
      <output>overall_conformance</output>
      <decision-gate>An auth flow with no non-cognitive alternative is a hard stop: 3.3.8 fails, and no combination of the other eight can produce an AA claim.</decision-gate>
    </step>

    <step n="6" name="validate-and-gate-ci">
      <input>Report + validator</input>
      <action>Run `validate-wcag-22-compliance.py`. Add CI gate that fails build on any new 'fail' status.</action>
      <output>Validated report + CI gate</output>
      
    </step>

  </section>
</text>
