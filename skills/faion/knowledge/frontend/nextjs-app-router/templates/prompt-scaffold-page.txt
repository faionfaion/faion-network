Generate a Next.js App Router page for route /<route-path>.
Requirements:
- Server Component (no 'use client' unless the page itself is interactive)
- Auth check using getServerSession, redirect('/login') if no session
- Suspense boundaries with skeleton loading states for async data
- generateMetadata export with page title: "<Title>"
- Data fetching: read from <data source> directly (no API route needed)
- Next.js 15 style: await params before accessing fields
Output files:
- app/<route>/page.tsx
- app/<route>/loading.tsx (Skeleton component matching the page layout)
- app/<route>/error.tsx ('use client', with reset prop)
- app/<route>/actions.ts (if form present, with 'use server', Zod, revalidatePath, redirect)
