Convert this form from client-side fetch to a Next.js Server Action.
Current: POST /<endpoint> with JSON body from a 'use client' component.
Target: app/<route>/actions.ts with:
- 'use server' at file top
- Zod schema validation of FormData fields
- Return { error: parsed.error.flatten() } on validation failure
- revalidatePath('<affected-path>') after successful write
- redirect('<destination>') after revalidate — NOT inside try/catch
Update the form component to use action={serverActionFn} and remove the fetch call.
Output: actions.ts + updated form component.
