# purpose: OpenAPI 3 skeleton matching the spec
# consumes: see AGENTS.md Prerequisites
# produces: Audit-Grade API Design spec
# depends-on: content/02-output-contract.xml schema
# token-budget-impact: ~400 tokens when filled

openapi: 3.0.3
info:
  title: Sample API
  version: 1.0.0
paths:
  /v1/ping:
    get:
      summary: Health probe
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
