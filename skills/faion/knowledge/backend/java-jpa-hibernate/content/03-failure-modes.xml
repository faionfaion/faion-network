<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="JPA / Hibernate Failure Modes" version="1.1.0" est_tokens="900" depth="essential">
  <summary>6 antipatterns observed in real Spring Data JPA + Hibernate deployments. Each pairs symptom + root cause + concrete fix.</summary>

  <section title="Antipatterns">

    <antipattern id="lombok-data-on-entities" severity="high">
      <symptom>Adding an entity to a `Set` triggers infinite recursion and a `StackOverflowError`; or `Set` semantics break (duplicates allowed).</symptom>
      <root-cause source="Lombok docs">`@Data` generates `equals`/`hashCode` over all fields, including lazy collections; touching a collection loads it from the DB and recurses.</root-cause>
      <fix>Replace `@Data` with `@Getter @Setter`. Implement `equals`/`hashCode` manually over a business key per `business-key-equality` rule.</fix>
    </antipattern>

    <antipattern id="eager-fetch-default" severity="high">
      <symptom>Query returns 5MB JSON or throws `MultipleBagFetchException`; profile shows multi-million-row Cartesian join.</symptom>
      <root-cause source="Hibernate fetch strategy docs">Author set `FetchType.EAGER` on an association "to be safe"; combined with another EAGER collection it explodes.</root-cause>
      <fix>Switch all associations to LAZY; do per-query `JOIN FETCH` or `@EntityGraph` when callers actually need the data.</fix>
    </antipattern>

    <antipattern id="cascade-all-on-manytoone" severity="high">
      <symptom>Deleting an `OrderLine` cascades to the parent `Order` and wipes the entire order plus all sibling lines.</symptom>
      <root-cause source="JPA docs — cascade semantics">`CascadeType.ALL` on a `@ManyToOne` propagates persist/merge/remove up the ownership chain — semantically backwards.</root-cause>
      <fix>Remove cascade from `@ManyToOne`. Cascade is only appropriate from parent to children on `@OneToMany`.</fix>
    </antipattern>

    <antipattern id="missing-migration" severity="high">
      <symptom>Deploy aborts with `SchemaManagementException: missing column ...` or — worse — `ddl-auto=update` silently alters the prod schema.</symptom>
      <root-cause source="Internal — migration retro 2026-Q1">Entity diff merged without a paired Flyway migration.</root-cause>
      <fix>Enforce `ddl-auto=validate` in prod profile; gate PRs on a `validate-paired-migration.py` script that fails if an entity diff lands without a `db/migration/V*.sql` change.</fix>
    </antipattern>

    <antipattern id="modifying-without-clear" severity="medium">
      <symptom>Bulk update reports `n` rows updated; subsequent `findById` returns the pre-update entity; logic that depends on the new state breaks silently.</symptom>
      <root-cause source="Spring Data JPA docs — @Modifying">`@Modifying` JPQL bypasses Hibernate's first-level cache; without `clearAutomatically`, stale entities linger in the persistence context.</root-cause>
      <fix>Annotate `@Modifying(clearAutomatically = true, flushAutomatically = true)` on every bulk update/delete repository method.</fix>
    </antipattern>

    <antipattern id="springboottest-for-slice" severity="medium">
      <symptom>Repository test suite takes 90 s to boot; CI flakes when the full context cache exceeds the JVM heap.</symptom>
      <root-cause source="Spring Boot test slice docs">Author used `@SpringBootTest` for what should be a repository-only test, loading the entire web + security context.</root-cause>
      <fix>Switch to `@DataJpaTest` + Testcontainers; reserve `@SpringBootTest` for cross-layer integration tests where the full stack is the SUT.</fix>
    </antipattern>

  </section>
</text>
