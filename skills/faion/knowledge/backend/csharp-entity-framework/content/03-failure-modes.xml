<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="EF Core Data Layer Failure Modes" version="1.2.0" est_tokens="1200" depth="essential">
  <summary>8 antipatterns observed in production EF Core deployments. Each pairs symptom + root cause + concrete fix.</summary>

  <section title="Antipatterns">

    <antipattern id="multiple-cascade-paths" severity="high">
      <symptom>Migration apply fails with `Introducing FOREIGN KEY constraint ... may cause cycles or multiple cascade paths` — only at apply time on SqlServer.</symptom>
      <root-cause source="SqlServer FK semantics">Two relations on the same entity both use `DeleteBehavior.Cascade`; SqlServer rejects the combination.</root-cause>
      <fix>Set `DeleteBehavior.Restrict` or `SetNull` on the secondary FK; review the migration SQL before apply per `migration-sql-review` rule.</fix>
    </antipattern>

    <antipattern id="missing-concurrency-token" severity="high">
      <symptom>Concurrent edits silently overwrite each other; audit log shows "lost update" pattern with no exception.</symptom>
      <root-cause source="EF Core concurrency docs">Aggregate root has no `RowVersion`/`[ConcurrencyCheck]` field; EF falls back to last-write-wins.</root-cause>
      <fix>Add `[Timestamp] byte[] RowVersion { get; set; }` (SqlServer) or `xmin` mapping (Postgres) on every aggregate root. Catch `DbUpdateConcurrencyException` in the service layer and surface a 409 Conflict.</fix>
    </antipattern>

    <antipattern id="include-chain-cartesian-explosion" severity="high">
      <symptom>Endpoint returns 5MB JSON; SQL profile shows a single query returning 10^6 rows for a 1000-row table.</symptom>
      <root-cause source="EF Core query docs">Multiple `Include().ThenInclude()` chains multiply rows via Cartesian join.</root-cause>
      <fix>Add `.AsSplitQuery()` for collection includes, or project to a DTO with explicit sub-queries.</fix>
    </antipattern>

    <antipattern id="case-sensitive-equality-bug" severity="medium">
      <symptom>Login works on SqlServer dev environment, fails on Postgres prod for the same email "Jane@example.com".</symptom>
      <root-cause source="Postgres docs — text equality">SqlServer is case-insensitive by default; Postgres is case-sensitive. `FirstOrDefault(u =&gt; u.Email == email)` differs per provider.</root-cause>
      <fix>Store normalised email (`.ToLowerInvariant()`) and compare normalised on both sides; or use `EF.Functions.ILike` for explicit case-insensitive matching.</fix>
    </antipattern>

    <antipattern id="task-run-around-savechanges" severity="medium">
      <symptom>Random `InvalidOperationException: A second operation started on this context` under load.</symptom>
      <root-cause source="EF Core threading docs">Author wrapped `await _context.SaveChangesAsync()` in `Task.Run` "to avoid blocking", introducing concurrent DbContext access.</root-cause>
      <fix>Remove `Task.Run` — `SaveChangesAsync` is already async IO. DbContext is not thread-safe.</fix>
    </antipattern>

    <antipattern id="audit-fields-per-entity" severity="medium">
      <symptom>Each `Configuration` sets `CreatedAt` defaults; three entities forgot it; rows ship with `0001-01-01` timestamps.</symptom>
      <root-cause source="EF Core interceptors docs">Cross-cutting audit fields configured per-entity instead of via a single interceptor.</root-cause>
      <fix>Add a `SaveChangesInterceptor` that sets `CreatedAt`/`UpdatedAt`/`UpdatedBy` for any entity implementing `IAuditable`. Register once via `optionsBuilder.AddInterceptors(...)`.</fix>
    </antipattern>

    <antipattern id="lazy-loading-proxies" severity="high">
      <symptom>`UseLazyLoadingProxies()` is enabled and a controller returns entities; the serializer touches a navigation, EF fires N+1 SQL, and once the request-scoped DbContext is gone the response dies mid-write with `ObjectDisposedException`.</symptom>
      <root-cause source="EF Core docs — Lazy loading">Lazy loading plus entities-in-responses creates a hidden runtime contract that only holds while the DbContext is alive, which a request-scoped context guarantees it will not be.</root-cause>
      <fix>Disable `UseLazyLoadingProxies`. Load related data with explicit `Include`/`ThenInclude` or a projection, so every navigation has a visible eager-load path.</fix>
    </antipattern>

    <antipattern id="edited-migration" severity="high">
      <symptom>An engineer renames a migration file or edits its `Up()` after merge to fix a typo. Environments diverge: prod applied the old version, staging the new, and neither can be rerun cleanly.</symptom>
      <root-cause source="EF Core docs — Migrations workflow">The migration was treated as code rather than as an immutable schema-history record keyed in `__EFMigrationsHistory`.</root-cause>
      <fix>Revert the edit and add a new migration (`FixTypoInOrdersTable`) that applies the correction. Pin the append-only policy in CONTRIBUTING.</fix>
    </antipattern>

  </section>
</text>
