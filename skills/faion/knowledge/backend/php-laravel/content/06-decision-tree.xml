<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Laravel Umbrella Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping feature shape to a sub-methodology + a rule from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-feature-shape">Feature shape?</root-question>

    <branch when="shape == http_endpoint">
      <conclusion ref="route-to-sub-methodology" verdict="apply php-laravel-patterns + php-eloquent + php-phpunit-testing"/>
    </branch>

    <branch when="shape == background_job">
      <conclusion ref="queue-driver-not-sync-in-prod" verdict="apply php-laravel-queues + php-phpunit-testing; confirm Horizon + Supervisor"/>
    </branch>

    <branch when="shape == scheduled_task">
      <conclusion ref="scheduler-runs-via-supervisor" verdict="add to app/Console/Kernel.php schedule; confirm single cron entry; heartbeat ping"/>
    </branch>

    <branch when="shape == model_only_change">
      <conclusion ref="route-to-sub-methodology" verdict="apply php-eloquent; pair migration + factory + Resource"/>
    </branch>

    <branch when="shape == framework_wiring_smell">
      <question id="q-smell">Which wiring smell?</question>
      <branch when="smell == facade_in_service">
        <conclusion ref="constructor-injection-over-facades" verdict="refactor the service to constructor-inject the contract"/>
      </branch>
      <branch when="smell == inline_bind">
        <conclusion ref="contract-bind-in-provider" verdict="move the binding into a Service Provider register()"/>
      </branch>
      <branch when="smell == inline_route_closure">
        <conclusion ref="routes-in-route-files-only" verdict="convert the closure to a controller method reference so route:cache works"/>
      </branch>
      <branch when="smell == repeated_cross_cut">
        <conclusion ref="middleware-for-crosscutting" verdict="extract the check into middleware on the route group"/>
      </branch>
      <branch when="smell == env_outside_config">
        <conclusion ref="env-secrets-not-config-cache-leak" verdict="move env() into config/*.php and read it via config()"/>
      </branch>
    </branch>

    <branch when="shape == hot_path_sub_10ms">
      <conclusion ref="php-version-floor" verdict="skip-this-methodology — reach for Go/Rust or Octane only after benchmarking"/>
    </branch>
  </decision-tree>

  <example trace="charge-invoice-feature">
    shape=http_endpoint → route-to-sub-methodology → apply php-laravel-patterns (FormRequest + Action) + php-eloquent (Invoice model) + php-laravel-queues (ChargeInvoiceJob) + php-phpunit-testing (Pest feature test).
  </example>
</text>
