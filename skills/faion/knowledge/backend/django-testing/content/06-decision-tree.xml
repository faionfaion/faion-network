<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Django Testing with pytest — Decision Tree" version="1.0.0" est_tokens="400" depth="essential">
  <decision-tree>
    <root-question id="q-applies">Do the Applies-If preconditions ALL hold for this task?</root-question>
    <branch when="applies_if_all_hold == true">
      <question id="q-primary">Is the primary trigger (first Applies-If item) present in the input artefact?</question>
      <branch when="primary_trigger_present == true">
        <question id="q-secondary">Is the secondary constraint (second Applies-If item) also met?</question>
        <branch when="secondary_constraint_met == true">
          <conclusion ref="r1-pyproject-pytest-config">Apply the full methodology starting from r1-pyproject-pytest-config; load content/01-core-rules.xml and follow content/04-procedure.xml (if present).</conclusion>
        </branch>
        <branch when="secondary_constraint_met == false">
          <conclusion ref="r2-savepoint-default">Partial application: apply r2-savepoint-default only; do not enforce later-stage rules.</conclusion>
        </branch>
      </branch>
      <branch when="primary_trigger_present == false">
        <conclusion ref="r3-conftest-shared-fixtures">Pre-conditions not met; revisit Applies-If list before applying any rule. Pause and clarify.</conclusion>
      </branch>
    </branch>
    <branch when="applies_if_all_hold == false">
      <conclusion ref="r7-parametrize-permission-matrix">Skip this methodology; route to the sibling methodology indicated by the Skip-If reason.</conclusion>
    </branch>
    <example trace="canonical-path">
      <p>Typical happy path: applies-if holds, primary trigger present, secondary met → run the full methodology against r1-pyproject-pytest-config.</p>
    </example>
  </decision-tree>
</text>
