<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="RSpec Testing for Rails Applications Decision Tree" version="1.1.0" est_tokens="400">
  <summary>Two-question gate: preconditions present? duplicate artefact exists? Routes to run / update / skip. Every conclusion references a rule id from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Are ALL preconditions satisfied? (prerequisite artefacts present AND named owner identifiable AND artefact store writable)</root-question>

    <branch when="q-preconditions == true">
      <question id="q-duplicate">Does an existing working artefact already cover this gap?</question>

      <branch when="q-duplicate == true">
        <conclusion ref="update-existing-artefact">Skip: do not duplicate. Update the existing artefact in place (bump version, refresh last_reviewed).</conclusion>
      </branch>

      <branch when="q-duplicate == false">
        <question id="q-spec-smell">Which spec smell dominates the existing suite? (flat `it` list / `create` overuse / `rails_helper` in unit specs / duplicated invariant checks / `let!` overuse / none)</question>

        <branch when="q-spec-smell == 'none'">
          <conclusion ref="run-the-checklist">Run the procedure. Emit a new artefact per the output contract.</conclusion>
        </branch>

        <branch when="q-spec-smell == 'flat-it-list'">
          <conclusion ref="r-describe-context-it-discipline">Wrap the variants in `context` blocks first — until then no reviewer can tell what the suite actually covers.</conclusion>
        </branch>

        <branch when="q-spec-smell == 'create-overuse'">
          <conclusion ref="r-build-stubbed-default">Switch to `build_stubbed` everywhere persistence is not asserted; run `--profile 10` to pick the order of work.</conclusion>
        </branch>

        <branch when="q-spec-smell == 'rails-helper-in-unit'">
          <conclusion ref="r-isolated-service-specs">Move pure-logic specs to `spec_helper` and inject `instance_double` collaborators.</conclusion>
        </branch>

        <branch when="q-spec-smell == 'duplicate-invariant-check'">
          <conclusion ref="r-shared-examples-for-invariants">Extract to `shared_examples_for` under spec/support/shared/ and apply with `it_behaves_like`.</conclusion>
        </branch>

        <branch when="q-spec-smell == 'let-bang-overuse'">
          <conclusion ref="r-lazy-let-default">Downgrade each `let!` to lazy `let` unless the side effect is required regardless of what is tested.</conclusion>
        </branch>
      </branch>
    </branch>

    <branch when="q-preconditions == false">
      <conclusion ref="skip-this-methodology">Skip this methodology. Surface the missing precondition. Defer until the gap is closed.</conclusion>
    </branch>
  </decision-tree>

  <example trace="all-preconditions-and-no-duplicate"><![CDATA[
inputs: {owner=alex@acme.com, prerequisites_present=true, existing_artefact=false}
q-preconditions -> true
q-duplicate -> false
=> conclusion run-the-checklist: emit new artefact.
]]></example>
</text>