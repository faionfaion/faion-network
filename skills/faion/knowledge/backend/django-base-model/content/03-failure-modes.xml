<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="django-base-model Failure Modes" version="1.2.0" est_tokens="1200" depth="essential">
  <summary>Eight high-impact base-model antipatterns.</summary>

  <section title="Failure modes">

    <antipattern id="fm-1-pk-swap">
      <symptom>Migration drops integer id columns and recreates as UUID. Every ForeignKey on the table breaks. Squashed migrations no longer apply. Backup restore fails.</symptom>
      <root-cause>Developer believed "switching to UUID is just changing the field type". The PK swap requires renaming the column, rewiring every FK in the schema, and rewriting every cached query plan.</root-cause>
      <fix>Reject the PK swap. Apply rule r1-separate-uid: keep integer id as PK; add a separate `uid` UUIDField for external IDs; expose `uid` in API responses; internal joins continue to use id.</fix>
    </antipattern>

    <antipattern id="fm-2-missing-partial-unique">
      <symptom>Restoring a soft-deleted row fails with IntegrityError: duplicate key violates unique constraint. The deleted row still occupies the unique index.</symptom>
      <root-cause>Field declared as `email = models.EmailField(unique=True)` on a soft-deletable model. Partial-index requirement was missed in code review.</root-cause>
      <fix>Replace `unique=True` with a `Meta.constraints = [UniqueConstraint(fields=['email'], condition=Q(deleted_at__isnull=True), name='unique_active_email')]`. The validator rule r3-partial-unique-under-soft-delete catches this.</fix>
    </antipattern>

    <antipattern id="fm-3-manager-shadowing">
      <symptom>Django admin shows zero deleted users even when querying by deleted_at. loaddata fails to overwrite soft-deleted rows. ORM-level signals fire on hard delete only.</symptom>
      <root-cause>Developer set `objects = SoftDeleteManager()` and removed the default Manager. Tools that expect Model.objects to return ALL rows (admin, loaddata, some migration ops) silently lose access.</root-cause>
      <fix>Preserve both managers: `objects = SoftDeleteManager()` (default — excludes deleted) AND `all_objects = models.Manager()` (every row). Configure admin's `get_queryset` to use all_objects with a "Include deleted" filter.</fix>
    </antipattern>

    <antipattern id="fm-4-cascade-soft-delete">
      <symptom>Soft-deleting a Customer hard-deletes their Orders. Compliance review fails: "deleted" customer's order history is gone.</symptom>
      <root-cause>Order.customer = ForeignKey(Customer, on_delete=models.CASCADE). Customer.delete() (soft) triggers QuerySet.delete() on children via cascade — which is the ORM's hard-delete path.</root-cause>
      <fix>Pick on_delete=PROTECT (cannot soft-delete a Customer with active Orders) or SET_NULL (orphan the Order, retain history). The spec's foreign_keys[].on_delete and reason fields force the explicit choice.</fix>
    </antipattern>

    <antipattern id="fm-5-uuid-as-pk">
      <symptom>Greenfield model declares `id = models.UUIDField(primary_key=True, default=uuid.uuid4)`. At a few million rows, joins are roughly twice as slow and index size is up ~25% against the same schema on an integer PK.</symptom>
      <root-cause>"Opaque external ids" was solved by changing the primary key rather than by adding a second column. A random 16-byte key also destroys B-tree insert locality, so every insert dirties a different page.</root-cause>
      <fix>Keep the auto-increment integer PK and add `uid` alongside it (rule r1-separate-uid). If a UUID PK is genuinely required, use UUIDv7 so the key is time-ordered — never random v4.</fix>
    </antipattern>

    <antipattern id="fm-6-int-pk-in-urls">
      <symptom>API routes read `/api/v1/orders/12345/`. Support tickets arrive quoting neighbouring order numbers that belong to other customers.</symptom>
      <root-cause>The view routes on the PK because that is the DRF default lookup field, and nobody set `lookup_field = "uid"`.</root-cause>
      <fix>Set `lookup_field = "uid"` / `lookup_url_kwarg = "uid"` on the ViewSet and use `&lt;uuid:uid&gt;` in URLconf. Rule r8-uid-in-urls; the serializer exposes `uid` and never `id`.</fix>
    </antipattern>

    <antipattern id="fm-7-concrete-table-inheritance">
      <symptom>Every read of a concrete model emits a JOIN against a `basemodel` table that nobody wrote a query for. `EXPLAIN` shows the extra node on every single query.</symptom>
      <root-cause>BaseModel was declared without `Meta.abstract = True`, so Django created a real table and used multi-table inheritance.</root-cause>
      <fix>Set `abstract = True` on the base and re-generate migrations. Rule r2-abstract-base. Abstract fields are inlined into each child table — zero runtime cost.</fix>
    </antipattern>

    <antipattern id="fm-8-missing-composite-index">
      <symptom>A dashboard query filtering on (user, status, created_at) is fast in dev and takes seconds in production. `EXPLAIN` shows a sequential scan despite an index on the FK.</symptom>
      <root-cause>Only the FK column is indexed — automatically, by Django. The status and date predicates have nothing to use, so the planner scans.</root-cause>
      <fix>Declare `Meta.indexes = [models.Index(fields=["user", "status", "-created_at"])]` matching the actual predicate order. Rule r9-composite-indexes.</fix>
    </antipattern>

  </section>
</text>
