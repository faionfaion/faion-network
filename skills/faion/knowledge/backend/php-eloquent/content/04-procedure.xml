<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Eloquent ORM Procedure" version="1.1.0" est_tokens="800" depth="essential">
  <summary>6-step procedure to apply the Eloquent ORM methodology end-to-end.</summary>

  <section title="Procedure">

    <step n="1" name="scaffold-model-migration-factory">
      <input>Entity name + column list</input>
      <action>Run `php artisan make:model -mfr &lt;Name&gt;`. In the migration declare every column with an explicit type + index for FK / filter / sort columns. In the model declare `protected $fillable`.</action>
      <output>Model + migration + factory + Resource</output>
      <decision-gate>If `$guarded = []` is left, BLOCK per `fillable-whitelist` rule.</decision-gate>
    </step>

    <step n="2" name="declare-relations-and-accessors">
      <input>Domain relations + computed attributes</input>
      <action>Add `belongsTo` / `hasMany` / `belongsToMany` methods. Use `Attribute::make(get: ..., set: ...)` for computed attributes. Cast columns with `protected $casts`.</action>
      <output>Relations + accessors via canonical API</output>
      <decision-gate>If both `Attribute::make` and `getXAttribute` exist, BLOCK per `attribute-make-only` rule.</decision-gate>
    </step>

    <step n="3" name="add-scopes-and-repository-eager-load">
      <input>Common queries</input>
      <action>Add named scopes (`scopeActive`, `scopeForTenant`) on the model. Repositories / services call `Model::with('...')-&gt;active()-&gt;...` at the boundary. Resources receive already-loaded relations.</action>
      <output>Scopes + repository methods</output>
      <decision-gate>If Resource triggers a lazy relation, BLOCK per `eager-load-at-boundary` rule.</decision-gate>
    </step>

    <step n="4" name="wrap-multistep-writes">
      <input>Action / Service implementation</input>
      <action>Wrap create + attach sequences in `DB::transaction(fn() =&gt; ...)`. Dispatch downstream events via `DB::afterCommit`.</action>
      <output>Atomic write path</output>
    </step>

    <step n="5" name="enable-strict-mode-in-dev">
      <input>`AppServiceProvider::boot`</input>
      <action>Call `Model::preventLazyLoading(! $this-&gt;app-&gt;isProduction())`, `Model::preventSilentlyDiscardingAttributes(! $this-&gt;app-&gt;isProduction())`. Add a Pest `query-budget.sh` assertion.</action>
      <output>Strict mode active + query-budget gate</output>
      <decision-gate>If strict mode is off, BLOCK per `prevent-lazy-loading-in-dev` rule.</decision-gate>
    </step>

    <step n="6" name="test-the-invariants">
      <input>Pest / PHPUnit suite</input>
      <action>Write three tests per hardened model: a mass-assignment test posting an extra protected key and asserting it is dropped; an `assertQueryCount` test on the list endpoint proving the N+1 is gone; and a rollback test that throws mid-`DB::transaction` and asserts the database is unchanged. Run `scripts/eloquent-n-plus-one-audit.sh` over the project root to catch new lazy-in-loop sites.</action>
      <output>Invariants pinned by tests + a clean N+1 audit</output>
      <decision-gate>If a WHERE clause appears at ≥2 call sites, BLOCK per `scope-for-reused-where`; if a typed column is missing from `$casts`, BLOCK per `casts-for-typed-attrs`.</decision-gate>
    </step>

  </section>
</text>
