<?xml version="1.0" encoding="UTF-8"?>
<index domain="backend" count="134" version="3.0" generated="2026-08-16">
  <description>L2 index of methodologies in the backend domain. The retriever reads this after picking backend from L1 domains.xml. Entries list slug + tier + summary, plus any &lt;assumes&gt; from the leaf's meta.json — a methodology to load first, and why. Open the leaf meta.json / content/*.xml for full payload.</description>
  <methodology slug="agent-dev-tuning" tier="solo" path="skills/faion/knowledge/backend/agent-dev-tuning">
    <summary>Server-side tuning for AI agent workloads on a Linux VPS: inotify ceilings, PAM nofile, 4-8GB swap with swappiness=10, per-agent MemoryMax via systemd-run, tmux linger, git worktrees for parallelism.</summary>
  </methodology>
  <methodology slug="api-contract-pattern-selection" tier="solo" path="skills/faion/knowledge/backend/api-contract-pattern-selection">
    <summary>Per-API decision record routing the contract style to one of {OpenAPI 3.1, GraphQL SDL, tRPC, JSON-RPC, gRPC, none-internal-only} based on consumer count, tooling needs, schema-evolution rate, and ...</summary>
    <assumes slug="api-rest-design">REST design baseline — what the OpenAPI 3.1 path produces</assumes>
    <assumes slug="api-graphql">GraphQL baseline — what the SDL path produces</assumes>
  </methodology>
  <methodology slug="api-monitoring" tier="solo" path="skills/faion/knowledge/backend/api-monitoring">
    <summary>Production monitoring config: Prometheus RED-method metrics, structured JSON logs with request_id, split liveness/readiness probes, and SLO definitions that drive alerts.</summary>
    <assumes slug="api-rest-design">Endpoint definitions the metrics attach to</assumes>
  </methodology>
  <methodology slug="api-versioning" tier="solo" path="skills/faion/knowledge/backend/api-versioning">
    <summary>Versions a REST API only on breaking changes via URL path (/api/vN), runs N and N-1 simultaneously, emits Deprecation/Sunset headers, and enforces sunset with 410 Gone.</summary>
    <assumes slug="api-rest-design">REST contract conventions this versioning sits on top of</assumes>
  </methodology>
  <methodology slug="audit-grade-api-design" tier="pro" path="skills/faion/knowledge/backend/audit-grade-api-design">
    <summary>Produces an audit-grade API spec: contract, error taxonomy, auth model, pagination, rate-limit, versioning, deprecation policy.</summary>
    <assumes slug="audit-grade-code-review-checklist">Reviewers will check the API code against the spec at review time</assumes>
  </methodology>
  <methodology slug="backup-recovery" tier="solo" path="skills/faion/knowledge/backend/backup-recovery">
    <summary>3-2-1 backup strategy for solo VPS: PostgreSQL pg_dump -Fc with verify, Redis RDB snapshot, config capture, restic encrypted offsite to B2/S3 with --keep-daily 7 --keep-weekly 4 --keep-monthly 6 retention.</summary>
  </methodology>
  <methodology slug="bash-aliases" tier="solo" path="skills/faion/knowledge/backend/bash-aliases">
    <summary>Organized ~/.bash_aliases methodology: simple aliases vs functions, safety flags on destructive commands, modern-tool gating with `command -v`, tab-completion wiring, categorized sections for system / git / docker / systemd / network.</summary>
  </methodology>
  <methodology slug="caching-http-headers" tier="pro" path="skills/faion/knowledge/backend/caching-http-headers">
    <summary>Produces a per-endpoint HTTP-caching spec: `Cache-Control` directives, `ETag`/`Vary` rules, `s-maxage`, `stale-while-revalidate`, private vs public, and CDN surrogate-key plan.</summary>
  </methodology>
  <methodology slug="caching-in-memory" tier="pro" path="skills/faion/knowledge/backend/caching-in-memory">
    <summary>Produces an L1 in-process cache spec: `lru_cache` for pure deterministic functions, `cachetools.TTLCache` with TTL + thread-safety, `WarmableCache` for preload-on-startup; explicit when to fall back to Redis.</summary>
  </methodology>
  <methodology slug="caching-invalidation" tier="pro" path="skills/faion/knowledge/backend/caching-invalidation">
    <summary>Produces a per-entity invalidation strategy: TTL floor, event-based purges on write, Redis SET-backed tag groups, and version-prefixed namespaces; selection by freshness SLO.</summary>
  </methodology>
  <methodology slug="caching-stampede-prevention" tier="pro" path="skills/faion/knowledge/backend/caching-stampede-prevention">
    <summary>Produces a stampede-safe cache spec for hot keys: Redis SETNX distributed lock, probabilistic early refresh (XFetch), and request coalescing wrapper; with budgets per pattern.</summary>
    <assumes slug="caching-invalidation">invalidation patterns</assumes>
  </methodology>
  <methodology slug="caching-write-patterns" tier="pro" path="skills/faion/knowledge/backend/caching-write-patterns">
    <summary>Produces a per-entity write-side cache spec: cache-aside (lazy), write-through (sync DB+cache), write-behind (async DB flush); selection by consistency budget + write frequency.</summary>
    <assumes slug="caching-invalidation">purge semantics</assumes>
  </methodology>
  <methodology slug="cloudflare-domain-dns" tier="solo" path="skills/faion/knowledge/backend/cloudflare-domain-dns">
    <summary>End-to-end procedure to buy a domain on Cloudflare Registrar, point it at your own server (A/AAAA + CNAMEs), choose proxy per record, lock SSL to Full(Strict), and verify propagation from multiple resolvers.</summary>
  </methodology>
  <methodology slug="cloudflare-pages-github" tier="solo" path="skills/faion/knowledge/backend/cloudflare-pages-github">
    <summary>Connect a GitHub repo to Cloudflare Pages so production-branch pushes deploy to the global CDN and every PR gets a preview URL. Includes build env vars, wrangler local preview, custom domain, branch deploy rules.</summary>
  </methodology>
  <methodology slug="cron-automation" tier="solo" path="skills/faion/knowledge/backend/cron-automation">
    <summary>Linux cron patterns for VPS: script structure (lock + log + trap + env), flock-based overlap prevention, env handling for non-login shells, FLOW-style hourly silent / daily report cadence, robust error reporting via Telegram.</summary>
  </methodology>
  <methodology slug="csharp-aspnet-core" tier="pro" path="skills/faion/knowledge/backend/csharp-aspnet-core">
    <summary>Produces a layered ASP.NET Core 8/9 service: feature folders, IXService interfaces, scoped DbContext, CancellationToken threaded to EF Core, ProblemDetails errors, record DTOs, TimeProvider, AsNoTracking + eager Include, keyset pagination, explicit transactions, Testcontainers integration tests.</summary>
    <assumes slug="audit-grade-api-design">API contract defines the controller surface</assumes>
  </methodology>
  <methodology slug="csharp-background-services" tier="pro" path="skills/faion/knowledge/backend/csharp-background-services">
    <summary>In-process .NET workers that shut down cleanly: a bounded Channel&lt;T&gt; with an explicit FullMode, scoped services resolved per item inside ExecuteAsync, per-item try/catch rethrowing OperationCanceledException only on the stopping token, PeriodicTimer instead of a Task.Delay loop, and ILogger scopes per item.</summary>
    <assumes slug="csharp-aspnet-core">Hosted service runs inside the same Generic Host as the API</assumes>
    <assumes slug="csharp-dotnet">Base .NET wiring, DI, hosting model</assumes>
    <assumes slug="csharp-entity-framework">Scoped DbContext lifecycle the worker depends on</assumes>
  </methodology>
  <methodology slug="csharp-dotnet" tier="pro" path="skills/faion/knowledge/backend/csharp-dotnet">
    <summary>Greenfield ASP.NET Core 8/9 service umbrella — controllers, services, EF Core, xUnit, BackgroundService — with DI lifetimes, interface-only injection, CancellationToken plumbing, feature folders, record DTOs, options validated on start, and secrets kept out of appsettings.</summary>
    <assumes slug="csharp-aspnet-core">Sub-module for controller/middleware patterns.</assumes>
    <assumes slug="csharp-entity-framework">Sub-module for EF Core data layer.</assumes>
    <assumes slug="csharp-xunit-testing">Sub-module for testing layering.</assumes>
  </methodology>
  <methodology slug="csharp-dotnet-patterns" tier="pro" path="skills/faion/knowledge/backend/csharp-dotnet-patterns">
    <summary>Clean Architecture + CQRS for .NET 8/9 — Domain/Application/Infrastructure/API layering, record commands with one handler each, rich aggregates without public setters, DTOs never entities, an explicitly ordered MediatR pipeline, outbox for cross-aggregate events, and NetArchTest fitness.</summary>
    <assumes slug="csharp-dotnet">Umbrella covering CancellationToken plumbing + DI lifetimes.</assumes>
    <assumes slug="csharp-entity-framework">Infrastructure layer EF Core patterns.</assumes>
  </methodology>
  <methodology slug="csharp-entity-framework" tier="pro" path="skills/faion/knowledge/backend/csharp-entity-framework">
    <summary>EF Core 8/9 data layer: one IEntityTypeConfiguration per entity, entities with private setters, AsNoTracking + projection on reads, AsSplitQuery on multi-collection includes, repositories returning PagedResult never IQueryable, concurrency tokens, bulk ExecuteUpdate/Delete, audit interceptor, append-only migrations reviewed as SQL.</summary>
    <assumes slug="csharp-dotnet">Umbrella covering DI lifetimes that govern DbContext scope.</assumes>
  </methodology>
  <methodology slug="csharp-xunit-testing" tier="pro" path="skills/faion/knowledge/backend/csharp-xunit-testing">
    <summary>xUnit + Moq + WebApplicationFactory test suite: one Fact or Theory row per branch, async tests returning Task, IClassFixture for shared hosts, precise Moq matchers with Verify, FluentAssertions on non-primitives, Testcontainers instead of InMemory, and a coverage gate in CI.</summary>
    <assumes slug="csharp-dotnet">Umbrella for service layout that drives test layout.</assumes>
  </methodology>
  <methodology slug="database-design" tier="pro" path="skills/faion/knowledge/backend/database-design">
    <summary>Produces a PostgreSQL schema spec: 3NF tables, access-pattern indexes, DB-layer constraints (FK / CHECK / NOT NULL / UNIQUE), forward + backward migrations, TIMESTAMPTZ + PK type policy.</summary>
  </methodology>
  <methodology slug="decomposition-django" tier="solo" path="skills/faion/knowledge/backend/decomposition-django">
    <summary>Splits a Django monolith into bounded apps by ownership boundaries, locks the dependency graph with import-linter, and migrates models with explicit boundary contracts.</summary>
    <assumes slug="django-services">Service layer pattern this builds boundaries on top of.</assumes>
    <assumes slug="django-celery">Background-task isolation; cross-app calls often go via Celery.</assumes>
  </methodology>
  <methodology slug="decomposition-laravel" tier="pro" path="skills/faion/knowledge/backend/decomposition-laravel">
    <summary>LLM-friendly Laravel decomposition — Action + DTO + FormRequest + Resource + Policy, one verb per Action, controllers ≤150 lines, enforced by a structural lint.</summary>
    <assumes slug="php-laravel">Sub-module covering controller / service / queue patterns.</assumes>
    <assumes slug="laravel-patterns">Sibling on Service-class layer when needed.</assumes>
  </methodology>
  <methodology slug="decomposition-rails" tier="pro" path="skills/faion/knowledge/backend/decomposition-rails">
    <summary>LLM-friendly Rails 7+ decomposition — Service Objects, Query Objects, Form Objects, Policies, Serializers — narrow concerns, canonical return shape, enforced by a structural lint.</summary>
    <assumes slug="ruby-rails">Sub-module covering controller / model patterns.</assumes>
    <assumes slug="ruby-activerecord">Scope discipline that Query Objects build on.</assumes>
  </methodology>
  <methodology slug="deploy-scripts" tier="solo" path="skills/faion/knowledge/backend/deploy-scripts">
    <summary>Bash deploy orchestrator: rsync source to runtime, virtualenv via `pip install -e .`, systemd reload, pre/post hooks, atomic switch, rollback path, exit-code propagation.</summary>
  </methodology>
  <methodology slug="direnv-mise-versions" tier="solo" path="skills/faion/knowledge/backend/direnv-mise-versions">
    <summary>Per-project environment management on Ubuntu 24: direnv for auto-load on cd, mise (formerly rtx) for Python/Node/Go/Ruby versions, .envrc + .mise.toml committed to repo, no global pollution.</summary>
  </methodology>
  <methodology slug="django-api" tier="free" path="skills/faion/knowledge/backend/django-api">
    <summary>Produces a Django REST API spec (framework choice DRF/Ninja, ViewSet vs APIView, serializer pairs, JWT cfg, throttle scopes, pagination) for a Django 5.x service.</summary>
    <assumes slug="django-models">Field types + Meta consumed by serializers.</assumes>
    <assumes slug="django-base-model">`uid` exposure: API returns uid, never id.</assumes>
    <assumes slug="django-imports">Conventional import order in viewsets/serializers.</assumes>
  </methodology>
  <methodology slug="django-base-model" tier="free" path="skills/faion/knowledge/backend/django-base-model">
    <summary>Produces a Django abstract base-model spec (BaseModel + TimestampMixin + SoftDeleteMixin + UUID uid + managers) for a Django 5.2 LTS project.</summary>
    <assumes slug="django-models">Concrete model conventions (Meta options, indexes, constraints) consumed downstream.</assumes>
    <assumes slug="django-project-structure">Apps + base/ module placement.</assumes>
  </methodology>
  <methodology slug="django-celery" tier="solo" path="skills/faion/knowledge/backend/django-celery">
    <summary>Configures Celery with Django: explicitly named idempotent tasks keyed by business id, transaction.on_commit dispatch, retry policy with exponential backoff and jitter, mandatory soft/hard time limits, named queues, and a dead-letter strategy.</summary>
    <assumes slug="django-services">Tasks call services; tasks are not the place for business logic.</assumes>
    <assumes slug="logging-patterns">Structured logging with task id + business id correlation.</assumes>
  </methodology>
  <methodology slug="django-constants" tier="free" path="skills/faion/knowledge/backend/django-constants">
    <summary>Produces a constants.py spec for each Django app (TextChoices/IntegerChoices enums + UPPER_SNAKE numeric limits) eliminating magic strings and numbers.</summary>
    <assumes slug="django-models">Field definitions consuming the choices=.</assumes>
    <assumes slug="django-imports">Import order for constants in models.py / services.py.</assumes>
  </methodology>
  <methodology slug="django-decision-tree" tier="free" path="skills/faion/knowledge/backend/django-decision-tree">
    <summary>Produces a Django architecture decision-record (framework, API stack, layering, DB, deployment, dep audit) at bootstrap or major-refactor checkpoint, plus the code-placement rules that layering choice implies.</summary>
    <assumes slug="django-project-structure">Layering choices materialise into folder structure.</assumes>
    <assumes slug="django-api">DRF vs Ninja decision lands here.</assumes>
    <assumes slug="django-base-model">Base model decisions consumed by the architecture record.</assumes>
  </methodology>
  <methodology slug="django-imports" tier="free" path="skills/faion/knowledge/backend/django-imports">
    <summary>Produces a Django imports spec (PEP 8 section order, mandatory cross-app aliases, TYPE_CHECKING guards, string FK refs, ruff config) for a multi-app project.</summary>
    <assumes slug="django-project-structure">apps/ layout assumed here.</assumes>
    <assumes slug="django-quality-linting">ruff rule set this spec extends.</assumes>
  </methodology>
  <methodology slug="django-models" tier="free" path="skills/faion/knowledge/backend/django-models">
    <summary>Produces a Django models spec (BaseModel inheritance, TextChoices, on_delete choice with rationale, indexes, constraints, full_clean policy) for a Django 5.x app.</summary>
    <assumes slug="django-base-model">Abstract bases this models spec extends.</assumes>
    <assumes slug="django-constants">TextChoices/IntegerChoices classes consumed here.</assumes>
    <assumes slug="django-imports">String FK refs and import style assumed here.</assumes>
  </methodology>
  <methodology slug="django-project-structure" tier="free" path="skills/faion/knowledge/backend/django-project-structure">
    <summary>Produces a Django project layout spec (apps/, core/, config/, tests/factories) with per-app file inventory and HackSoft-style services/selectors split.</summary>
    <assumes slug="django-imports">Cross-app alias convention that depends on the apps/ namespace.</assumes>
    <assumes slug="django-decision-tree">Layering tier decided upstream.</assumes>
  </methodology>
  <methodology slug="django-pytest-factories" tier="free" path="skills/faion/knowledge/backend/django-pytest-factories">
    <summary>Produces a pytest-factoryboy spec (factory classes, SubFactory graph, register list, mute_signals scope) for Django test data without manual setUp.</summary>
    <assumes slug="django-models">Model fields + FK graph consumed by SubFactory.</assumes>
    <assumes slug="django-pytest-fixtures">Per-test fixture scoping.</assumes>
  </methodology>
  <methodology slug="django-pytest-fixtures" tier="free" path="skills/faion/knowledge/backend/django-pytest-fixtures">
    <summary>Produces a pytest-django fixtures spec (db vs transactional_db, scopes, conftest layout, test settings, MEDIA_ROOT, parallel pytest-xdist config).</summary>
    <assumes slug="django-pytest-factories">Factories produce the data each fixture wraps.</assumes>
    <assumes slug="django-models">Signal handler list informs db vs transactional_db.</assumes>
  </methodology>
  <methodology slug="django-pytest-integration" tier="free" path="skills/faion/knowledge/backend/django-pytest-integration">
    <summary>Produces an integration-tests spec — per-endpoint 401/403/200/400/404 matrix, service-layer test plan, transactional_db cases, CI Postgres config, ≥80% coverage gate.</summary>
    <assumes slug="django-pytest-fixtures">api_client / authenticated_client / admin_client fixtures.</assumes>
    <assumes slug="django-pytest-factories">factories used inside the integration tests.</assumes>
    <assumes slug="django-api">endpoint matrix consumed here.</assumes>
  </methodology>
  <methodology slug="django-pytest-mocking" tier="free" path="skills/faion/knowledge/backend/django-pytest-mocking">
    <summary>Produces a pytest mocking spec (import-site patches, responses for HTTP, freezegun/time-machine for clocks, Celery .delay() mocks) — no ORM mocks, no sleep mocks.</summary>
    <assumes slug="django-pytest-fixtures">Fixtures + CELERY_TASK_ALWAYS_EAGER setting.</assumes>
    <assumes slug="django-pytest-integration">Integration tests consume the mocks declared here.</assumes>
  </methodology>
  <methodology slug="django-pytest-parametrize" tier="free" path="skills/faion/knowledge/backend/django-pytest-parametrize">
    <summary>Produces a parametrize spec (validation matrices, permission grids, error-code tables) with descriptive IDs, valid-payload base, and group-by-category splits.</summary>
    <assumes slug="django-pytest-integration">Endpoint matrix consumed by validation parametrize.</assumes>
    <assumes slug="django-pytest-fixtures">api_client / authenticated_client used inside parametrize.</assumes>
  </methodology>
  <methodology slug="django-quality-linting" tier="free" path="skills/faion/knowledge/backend/django-quality-linting">
    <summary>Produces a Django code-quality stack spec (Ruff config, mypy + django-stubs, pre-commit hooks, CI gates including coverage 80%) for a Python 3.11+/Django 5.x repo.</summary>
    <assumes slug="django-imports">ruff `I` config consumed here.</assumes>
    <assumes slug="django-pytest-integration">coverage gate referenced.</assumes>
    <assumes slug="typescript-strict-mode">analogous strict-flag migration pattern (cross-stack).</assumes>
  </methodology>
  <methodology slug="django-quality-logging" tier="free" path="skills/faion/knowledge/backend/django-quality-logging">
    <summary>Produces a Django production logging + Sentry configuration report (settings diff + structlog wiring + Sentry init + check --deploy verdict).</summary>
  </methodology>
  <methodology slug="django-quality-queries" tier="free" path="skills/faion/knowledge/backend/django-quality-queries">
    <summary>Produces a Django ORM query optimization report (N+1 inventory + select_related/prefetch_related fixes + assertNumQueries locks + index recommendations).</summary>
  </methodology>
  <methodology slug="django-quality-security" tier="free" path="skills/faion/knowledge/backend/django-quality-security">
    <summary>Produces a Django production security audit report (HTTPS/HSTS/CSP/cookies + rate limits + input validation + secrets + check --deploy verdict).</summary>
  </methodology>
  <methodology slug="django-selectors" tier="free" path="skills/faion/knowledge/backend/django-selectors">
    <summary>Produces Django selector functions (read-only, kwarg-only, returning QuerySet with select_related/prefetch_related) centralising query logic for views/tasks/admin.</summary>
  </methodology>
  <methodology slug="django-serializers" tier="free" path="skills/faion/knowledge/backend/django-serializers">
    <summary>Produces DRF Input/Output serializer pairs (Input + Output split, explicit fields, no business logic, validation only) for a Django REST Framework endpoint.</summary>
  </methodology>
  <methodology slug="django-service-layer" tier="free" path="skills/faion/knowledge/backend/django-service-layer">
    <summary>Produces a reviewed service-module diff that moves every write operation out of views and serializers into keyword-only service functions with transaction.atomic + full_clean + domain exceptions.</summary>
    <assumes slug="django-selectors">The read-side counterpart; this methodology must coexist with it.</assumes>
    <assumes slug="django-pytest">The pytest runner that exercises the extracted services.</assumes>
    <assumes slug="django-imports">Settles the import ordering used inside `services.py` modules.</assumes>
  </methodology>
  <methodology slug="django-services" tier="solo" path="skills/faion/knowledge/backend/django-services">
    <summary>Separates Django write logic into entity_action services that accept and return primitives, with keyword-only args, full type hints, explicit @transaction.atomic, on_commit-deferred side effects, and one module per aggregate free of HTTP imports.</summary>
    <assumes slug="decomposition-django">App boundaries this services live inside.</assumes>
    <assumes slug="django-celery">Side effects deferred via on_commit when async.</assumes>
  </methodology>
  <methodology slug="django-testing" tier="free" path="skills/faion/knowledge/backend/django-testing">
    <summary>pytest-django for Django and DRF: savepoint django_db by default with transaction=True only where on_commit runs, shared api_client fixtures in conftest, one Factory Boy factory per model, force_authenticate for auth, permission matrices as parametrize.</summary>
  </methodology>
  <methodology slug="docker-compose-patterns" tier="solo" path="skills/faion/knowledge/backend/docker-compose-patterns">
    <summary>Docker Compose V2 patterns for stateful infra (Postgres, Redis, RabbitMQ) on a single VPS alongside systemd services: bind-to-127.0.0.1 (UFW bypass guard), healthchecks, depends_on with condition, named volumes, restart policy.</summary>
  </methodology>
  <methodology slug="dotfiles-management" tier="solo" path="skills/faion/knowledge/backend/dotfiles-management">
    <summary>Git-based dotfiles using GNU stow for symlinks: per-tool subdir (bash/, vim/, tmux/), .stowrc for ignore patterns, install procedure on fresh machine, secret separation (private repo vs public).</summary>
  </methodology>
  <methodology slug="error-handling" tier="pro" path="skills/faion/knowledge/backend/error-handling">
    <summary>Produces a per-service error-envelope spec: RFC 7807 / 9457 Problem Details (`type`, `title`, `status`, `detail`, `instance`, `traceId`), single exception mapper, and validation error array.</summary>
  </methodology>
  <methodology slug="fail2ban-setup" tier="solo" path="skills/faion/knowledge/backend/fail2ban-setup">
    <summary>Dynamic IP banning with fail2ban + nftables backend on Ubuntu 24: SSH brute-force jail, sshd port 2222 awareness, retention (bantime 1h escalating to 1w), Telegram alert action, whitelist for WireGuard.</summary>
  </methodology>
  <methodology slug="firewall-management" tier="solo" path="skills/faion/knowledge/backend/firewall-management">
    <summary>Configure UFW with nftables backend on Ubuntu 24: deny incoming, allow outgoing, deny routed before any allow rule, SSH rate-limited on actual port, Docker services bound to 127.0.0.1 to prevent UFW bypass.</summary>
  </methodology>
  <methodology slug="git-server-workflow" tier="solo" path="skills/faion/knowledge/backend/git-server-workflow">
    <summary>Workspace/runtime separation pattern: source in ~/workspace/repos/&lt;name&gt;, runtime in /srv/&lt;project&gt;, rsync as the deploy boundary, no live edits in /srv, git-tagged releases, deploy.sh as the single deploy entrypoint.</summary>
  </methodology>
  <methodology slug="go-backend" tier="pro" path="skills/faion/knowledge/backend/go-backend">
    <summary>Produces a Go service scaffold spec: cmd/ + internal/ + pkg/ layout, handler→service→repository layering, request DTOs, context propagation, typed errors, graceful shutdown, framework choice (Gin/Echo/Chi/stdlib).</summary>
    <assumes slug="go-layout-directory-structure">directory skeleton</assumes>
  </methodology>
  <methodology slug="go-channels" tier="pro" path="skills/faion/knowledge/backend/go-channels">
    <summary>Produces a channel topology spec: directional channels, sender-closes rule, select-with-context, buffered-vs-unbuffered choice, pipeline stages with done-channel, race-detector gate.</summary>
    <assumes slug="go-goroutines">goroutine semantics</assumes>
  </methodology>
  <methodology slug="go-concurrency-patterns" tier="pro" path="skills/faion/knowledge/backend/go-concurrency-patterns">
    <summary>Produces a per-task concurrency-pattern spec: worker pool (IO-bound), fan-out/fan-in (CPU-bound), errgroup + semaphore (fail-fast), pipeline (streaming). Context propagation + panic recovery + goleak test mandatory.</summary>
    <assumes slug="go-channels">channel primitives</assumes>
    <assumes slug="go-goroutines">goroutine basics</assumes>
  </methodology>
  <methodology slug="go-error-handling" tier="pro" path="skills/faion/knowledge/backend/go-error-handling">
    <summary>Produces a Go error-taxonomy spec: project-wide `AppError` struct (code, message, HTTP status, wrap chain), per-package sentinels, Gin/Echo middleware mapping `AppError` to RFC 7807 JSON.</summary>
    <assumes slug="error-handling">RFC 7807 envelope</assumes>
    <assumes slug="go-backend">service layout</assumes>
  </methodology>
  <methodology slug="go-error-handling-patterns" tier="pro" path="skills/faion/knowledge/backend/go-error-handling-patterns">
    <summary>Produces a Go error-handling tactics spec: wrap with `%w` once per layer, translate storage sentinels at repository boundary, single `apperror` package with sentinel vars + AppError struct, log exactly once per request.</summary>
    <assumes slug="go-error-handling">AppError taxonomy</assumes>
  </methodology>
  <methodology slug="go-goroutines" tier="pro" path="skills/faion/knowledge/backend/go-goroutines">
    <summary>Produces a goroutine-lifecycle spec: each goroutine has explicit start + exit; context-driven cancellation; bounded worker pools; goleak-tested; no `go func()` without WaitGroup.</summary>
    <assumes slug="go-channels">channel primitives</assumes>
  </methodology>
  <methodology slug="go-http-handlers" tier="pro" path="skills/faion/knowledge/backend/go-http-handlers">
    <summary>Produces a Go HTTP-handler spec: handler methods on dependency-injecting struct, request DTOs with binding/validate tags, typed response structs, RFC 7807 mapping, httptest-driven table tests.</summary>
    <assumes slug="go-backend">service skeleton</assumes>
    <assumes slug="go-error-handling">AppError + middleware</assumes>
  </methodology>
  <methodology slug="go-layout-agentic-workflow" tier="pro" path="skills/faion/knowledge/backend/go-layout-agentic-workflow">
    <summary>Produces a four-stage agentic pipeline for Go service scaffolding: layout-agent (skeleton) → code-gen-agent (resources) → wiring-agent (main.go) → review-agent (import rules). Prompts checked into `.aidocs/`.</summary>
    <assumes slug="go-layout-directory-structure">skeleton conventions</assumes>
  </methodology>
  <methodology slug="go-layout-directory-structure" tier="pro" path="skills/faion/knowledge/backend/go-layout-directory-structure">
    <summary>Produces a canonical Go service tree: `cmd/&lt;bin&gt;`, `internal/&lt;feature&gt;`, `pkg/&lt;lib&gt;`, `migrations/` with file-naming conventions; one agreed layout that scales from single service to multi-binary monorepo.</summary>
  </methodology>
  <methodology slug="go-layout-layer-rules" tier="pro" path="skills/faion/knowledge/backend/go-layout-layer-rules">
    <summary>Produces an enforced layering plan: handler imports service interface, service imports repository interface, repository implements it. Automated via depguard + golangci-lint; PR review gate.</summary>
    <assumes slug="go-layout-directory-structure">directory tree</assumes>
  </methodology>
  <methodology slug="go-layout-toolchain" tier="pro" path="skills/faion/knowledge/backend/go-layout-toolchain">
    <summary>Produces a toolchain map per layout slot: `cmd/` (cobra), `internal/handler` (Gin/Echo/Chi), `internal/repository` (sqlc/sqlx), `migrations/` (Goose/Atlas), tests (testify, goleak), live-reload (air), CI services.</summary>
    <assumes slug="go-layout-directory-structure">layout convention</assumes>
  </methodology>
  <methodology slug="go-project-structure" tier="pro" path="skills/faion/knowledge/backend/go-project-structure">
    <summary>Standard Go project layout using cmd/, internal/, and an optional pkg/ for code intended for external import.</summary>
  </methodology>
  <methodology slug="graphql-api-design" tier="solo" path="skills/faion/knowledge/backend/graphql-api-design">
    <summary>Designs a GraphQL service schema-first: Relay cursor pagination, per-request DataLoaders, mutation payloads with typed error unions, and depth/complexity limits.</summary>
    <assumes slug="api-rest-design">Some clients may also need REST endpoints next to GraphQL</assumes>
  </methodology>
  <methodology slug="health-checks-autoheal" tier="solo" path="skills/faion/knowledge/backend/health-checks-autoheal">
    <summary>Health monitoring and automatic recovery for multi-service platforms on Ubuntu 24: per-service health endpoint, journald-based check loop, systemd OnFailure auto-restart, escalation to Telegram after N retries.</summary>
  </methodology>
  <methodology slug="java-jpa-hibernate" tier="pro" path="skills/faion/knowledge/backend/java-jpa-hibernate">
    <summary>Spring Data JPA + Hibernate methodology — LAZY by default, business-key equality, expand-contract migrations, @DataJpaTest slices, OSIV off, DTO projections in controllers.</summary>
    <assumes slug="java-spring-boot">Sub-module for service / controller layering.</assumes>
    <assumes slug="java-junit-testing">Test layering that drives `@DataJpaTest` vs `@SpringBootTest`.</assumes>
    <assumes slug="ddd-repositories">Narrow-repository pattern the `narrow-repo-interface` rule builds on.</assumes>
  </methodology>
  <methodology slug="java-junit-testing" tier="pro" path="skills/faion/knowledge/backend/java-junit-testing">
    <summary>Layered Spring Boot testing with JUnit 5 + Mockito + AssertJ — @WebMvcTest controllers, plain Mockito services, @DataJpaTest repositories with Testcontainers, Jacoco coverage gate in CI.</summary>
    <assumes slug="java-spring-boot">Umbrella for service layering.</assumes>
    <assumes slug="java-jpa-hibernate">Drives `@DataJpaTest` configuration for repositories.</assumes>
  </methodology>
  <methodology slug="java-spring" tier="pro" path="skills/faion/knowledge/backend/java-spring">
    <summary>Canonical layered Spring Boot 3 architecture — @RestController → @Service → JpaRepository, Bean Validation, MapStruct DTOs, @Transactional writes, Pageable lists, BCrypt password hashing.</summary>
    <assumes slug="java-spring-boot">Sibling for richer enterprise-grade patterns.</assumes>
    <assumes slug="java-jpa-hibernate">Repository layer discipline.</assumes>
  </methodology>
  <methodology slug="java-spring-async" tier="pro" path="skills/faion/knowledge/backend/java-spring-async">
    <summary>Spring @Async methodology — named ThreadPoolTaskExecutors, CallerRunsPolicy backpressure, CompletableFuture return types, TaskDecorator MDC propagation, graceful shutdown.</summary>
    <assumes slug="java-spring-boot">Umbrella for service layering.</assumes>
  </methodology>
  <methodology slug="java-spring-boot" tier="pro" path="skills/faion/knowledge/backend/java-spring-boot">
    <summary>Spring Boot 3 layered architecture — constructor injection, immutable DTOs (records), MapStruct mapping, Bean Validation, RFC 7807 ProblemDetail errors, @Transactional service boundaries.</summary>
    <assumes slug="java-spring">Sibling for the core layered pattern.</assumes>
    <assumes slug="java-jpa-hibernate">Repository discipline.</assumes>
    <assumes slug="java-junit-testing">Test layering.</assumes>
  </methodology>
  <methodology slug="java-spring-boot-patterns" tier="pro" path="skills/faion/knowledge/backend/java-spring-boot-patterns">
    <summary>Enterprise Spring Boot 3 patterns — BaseEntity (UUID PK + audit + @Version), record DTOs, MapStruct, @Transactional(readOnly=true) default, JpaSpecificationExecutor search, ProblemDetail errors, Actuator+Micrometer wired.</summary>
    <assumes slug="java-spring-boot">Sibling for the core layered shape.</assumes>
    <assumes slug="java-jpa-hibernate">Persistence discipline (LAZY, business-key, migrations).</assumes>
    <assumes slug="java-junit-testing">Test layering.</assumes>
  </methodology>
  <methodology slug="kernel-tuning" tier="solo" path="skills/faion/knowledge/backend/kernel-tuning">
    <summary>sysctl parameter tuning for Ubuntu 24 VPS: inotify ceilings, FD max, network buffer + BBR, vm.swappiness, vm.max_map_count for ES-like workloads, kernel.pid_max for parallel agents.</summary>
  </methodology>
  <methodology slug="laravel-patterns" tier="pro" path="skills/faion/knowledge/backend/laravel-patterns">
    <summary>Enterprise Laravel 10/11 patterns — thin controllers, Action/Service classes, Form Request validation, API Resources, DTOs, DB::transaction for writes, ShouldBeUnique jobs, eager-load discipline.</summary>
    <assumes slug="php-laravel">Umbrella for service / queue patterns.</assumes>
    <assumes slug="decomposition-laravel">Action + DTO + FormRequest pattern.</assumes>
    <assumes slug="php-eloquent">ORM discipline that prevents N+1.</assumes>
  </methodology>
  <methodology slug="monitoring-logging" tier="solo" path="skills/faion/knowledge/backend/monitoring-logging">
    <summary>Monitoring + logging for solo VPS without Prometheus/Grafana: journald with log rotation, health-check scripts, Telegram digest, single-line summaries per service per day, alert-only on failure.</summary>
  </methodology>
  <methodology slug="mq-broker-implementations" tier="pro" path="skills/faion/knowledge/backend/mq-broker-implementations">
    <summary>Production-ready Python implementations for the four most common message brokers: RabbitMQ (pika), Redis Streams (redis-py), Celery, and AWS SQS (boto3).</summary>
  </methodology>
  <methodology slug="mq-idempotent-consumers" tier="pro" path="skills/faion/knowledge/backend/mq-idempotent-consumers">
    <summary>At-least-once delivery means messages arrive more than once; idempotent consumers + idempotency_key + transactional outbox guarantee one observable side-effect per business event.</summary>
  </methodology>
  <methodology slug="mq-patterns" tier="pro" path="skills/faion/knowledge/backend/mq-patterns">
    <summary>Produces a queue-topology contract picking among work-queue / fan-out / request-reply / DLQ with idempotency + monitoring spec.</summary>
  </methodology>
  <methodology slug="mq-reliability" tier="pro" path="skills/faion/knowledge/backend/mq-reliability">
    <summary>Reliability config bundle: publisher confirms, DLQ + alert, exponential backoff + jitter, prefetch limits, schema registry — produces a reliability-config YAML.</summary>
  </methodology>
  <methodology slug="multi-project-hosting" tier="solo" path="skills/faion/knowledge/backend/multi-project-hosting">
    <summary>Pattern for hosting 2-5 web projects on a single VPS: port allocation by 100-range convention (8000-8099 project A, 8100-8199 project B), shared nginx reverse proxy, per-project user, /srv/&lt;project&gt; isolation, port registry.</summary>
  </methodology>
  <methodology slug="nginx-reverse-proxy" tier="solo" path="skills/faion/knowledge/backend/nginx-reverse-proxy">
    <summary>Generates an nginx vhost set + snippet pack for a multi-domain VPS — proxy_pass, WebSocket upgrade, security headers, rate limits — gated by a validated DeploymentPlan.</summary>
  </methodology>
  <methodology slug="nodejs-patterns" tier="free" path="skills/faion/knowledge/backend/nodejs-patterns">
    <summary>Produces a layered TS Express scaffold spec (routes → controllers → services → middleware → errors → Pino logger) with correct middleware order.</summary>
    <assumes slug="typescript-strict-mode">Strict tsconfig is the baseline for type-safe layer boundaries.</assumes>
    <assumes slug="typescript-patterns">Discriminated unions for the error class hierarchy + Result types.</assumes>
  </methodology>
  <methodology slug="nodejs-service-layer-architecture" tier="solo" path="skills/faion/knowledge/backend/nodejs-service-layer-architecture">
    <summary>Node.js service layer architecture spec: Controller-Service-Repository layout, DI seams, error-type taxonomy, transaction boundaries, and logging shape.</summary>
    <assumes slug="nodejs-service-layer-implementation">Workflow context: related methodology in the same family</assumes>
  </methodology>
  <methodology slug="nodejs-service-layer-implementation" tier="solo" path="skills/faion/knowledge/backend/nodejs-service-layer-implementation">
    <summary>Node.js service layer implementation spec: TypeScript Controller-Service-Repository code (Express + Prisma), error middleware, and per-layer test pattern.</summary>
    <assumes slug="nodejs-service-layer-architecture">Workflow context: related methodology in the same family</assumes>
  </methodology>
  <methodology slug="nosql-cassandra-patterns" tier="pro" path="skills/faion/knowledge/backend/nosql-cassandra-patterns">
    <summary>Query-driven schema for Cassandra / DynamoDB: high-cardinality partition keys, one table per access pattern, time-bucketed partitions, LWT/counters tuned to traffic.</summary>
  </methodology>
  <methodology slug="nosql-mongodb-patterns" tier="pro" path="skills/faion/knowledge/backend/nosql-mongodb-patterns">
    <summary>Access-pattern-driven MongoDB schema: embedding vs referencing decision, index plan from query filters, schemaVersion field + migration scripts, TTL indexes for ephemeral data.</summary>
  </methodology>
  <methodology slug="nosql-neo4j-patterns" tier="pro" path="skills/faion/knowledge/backend/nosql-neo4j-patterns">
    <summary>Graph modelling for Neo4j: labelled nodes + typed directed relationships with properties, bounded Cypher traversals with EXPLAIN/PROFILE, property indexes on every filtered property.</summary>
  </methodology>
  <methodology slug="nosql-redis-patterns" tier="pro" path="skills/faion/knowledge/backend/nosql-redis-patterns">
    <summary>Pick the right Redis structure per access pattern: strings for caching, hashes for object fields, lists/streams for queues, sorted sets for sliding-window rate limits, with TTLs on every key.</summary>
  </methodology>
  <methodology slug="php-eloquent" tier="pro" path="skills/faion/knowledge/backend/php-eloquent">
    <summary>Eloquent ORM methodology for Laravel — $fillable whitelist, Attribute::make accessors, eager-load discipline, DB::transaction for multi-write, preventLazyLoading in dev, paired migration+factory+resource on every change.</summary>
    <assumes slug="php-laravel">Umbrella for controller / queue patterns.</assumes>
    <assumes slug="laravel-patterns">Enterprise patterns that drive Eloquent discipline.</assumes>
  </methodology>
  <methodology slug="php-laravel" tier="pro" path="skills/faion/knowledge/backend/php-laravel">
    <summary>Umbrella Laravel 10/11 methodology — controller / service / Eloquent / queue / test layering, with sub-methodologies for patterns, Eloquent, queues, PHPUnit.</summary>
    <assumes slug="php-laravel-patterns">Sub-module for layered architecture.</assumes>
    <assumes slug="php-eloquent">Sub-module for ORM discipline.</assumes>
    <assumes slug="php-phpunit-testing">Sub-module for test layering.</assumes>
    <assumes slug="decomposition-laravel">Sub-module for Action + DTO decomposition.</assumes>
  </methodology>
  <methodology slug="php-laravel-patterns" tier="pro" path="skills/faion/knowledge/backend/php-laravel-patterns">
    <summary>Clean-architecture layering for Laravel — thin Controller → FormRequest → Service (DB::transaction) → optional Repository → JsonResource; controllers contain no Eloquent or business logic.</summary>
    <assumes slug="php-laravel">Umbrella for queue / scheduler discipline.</assumes>
    <assumes slug="php-eloquent">ORM rules that controllers / services rely on.</assumes>
    <assumes slug="decomposition-laravel">Action + DTO + FormRequest decomposition.</assumes>
  </methodology>
  <methodology slug="php-phpunit-testing" tier="pro" path="skills/faion/knowledge/backend/php-phpunit-testing">
    <summary>Layered PHPUnit/Pest testing for Laravel — Feature tests drive HTTP via getJson/postJson + assertJsonPath, Unit tests cover services without booting Laravel, RefreshDatabase + factories + fakes wired before the SUT runs.</summary>
    <assumes slug="php-laravel">Umbrella for queue / scheduler setup.</assumes>
    <assumes slug="php-laravel-patterns">Layering that drives Feature vs Unit boundary.</assumes>
  </methodology>
  <methodology slug="python-async" tier="free" path="skills/faion/knowledge/backend/python-async">
    <summary>Structured concurrency for I/O-bound Python: asyncio.TaskGroup for all-or-nothing fan-out, Semaphore acquired inside the task coroutine, asyncio.timeout instead of wait_for, no blocking call under async def, and create_task results kept referenced.</summary>
  </methodology>
  <methodology slug="python-basics" tier="free" path="skills/faion/knowledge/backend/python-basics">
    <summary>The Python 3.12+ defaults a new file should already follow: built-in generics and X | None over typing imports, EAFP over pre-checks, no mutable default arguments, pathlib rather than os.path, and frozen slotted dataclasses for plain data.</summary>
  </methodology>
  <methodology slug="python-code-quality" tier="free" path="skills/faion/knowledge/backend/python-code-quality">
    <summary>Python quality gate: Ruff as the single lint-and-format tool with black and isort removed, strict mypy or pyright, a hard pytest-cov threshold, bandit on every commit, and pre-commit re-run in CI so --no-verify cannot bypass it.</summary>
  </methodology>
  <methodology slug="python-fastapi" tier="free" path="skills/faion/knowledge/backend/python-fastapi">
    <summary>FastAPI production patterns: asynccontextmanager lifespan, Pydantic v2 Base/Create/Update/Response quartet, Annotated dependency aliases, thin routes over async services returning ORM, per-task AsyncSession with flush+refresh, and an openapi snapshot gate.</summary>
  </methodology>
  <methodology slug="python-modern-2026" tier="free" path="skills/faion/knowledge/backend/python-modern-2026">
    <summary>Adopting Python 3.12-3.14: one pinned interpreter per repo with requires-python and ruff/mypy target versions in lock-step, PEP 695 generics replacing TypeVar, uv as the only environment manager, and free-threaded builds only for pure-Python CPU work.</summary>
  </methodology>
  <methodology slug="python-overview" tier="free" path="skills/faion/knowledge/backend/python-overview">
    <summary>Classify the Python project as web, data, ML, automation or embedded before any framework methodology loads, pin one interpreter version for that domain, default to uv, and never run two primary frameworks in one project.</summary>
  </methodology>
  <methodology slug="python-poetry-setup" tier="free" path="skills/faion/knowledge/backend/python-poetry-setup">
    <summary>Poetry 2.x setup keeping the PEP 621 [project] table as the source of truth, poetry.lock committed and installed with --sync in CI, deps split into main/dev/test/docs groups, lower-bound constraints for publishable libraries, an in-project .venv, and a multi-stage Docker build that never ships the Poetry runtime.</summary>
  </methodology>
  <methodology slug="python-pytest-async" tier="free" path="skills/faion/knowledge/backend/python-pytest-async">
    <summary>Async test suites on pytest-asyncio: asyncio_mode=auto, @pytest_asyncio.fixture for async fixtures, AsyncMock rather than MagicMock, FastAPI exercised through httpx ASGITransport instead of a booted uvicorn, and no sync Django ORM call inside an async test.</summary>
  </methodology>
  <methodology slug="python-pytest-fixtures" tier="free" path="skills/faion/knowledge/backend/python-pytest-fixtures">
    <summary>pytest fixtures as dependency injection: pick the narrowest scope that works, yield for teardown that survives a failing test, factory fixtures when tests need variants, autouse reserved for transparent cheap setup, and no mutable state shared without reset.</summary>
  </methodology>
  <methodology slug="python-pytest-mocking" tier="free" path="skills/faion/knowledge/backend/python-pytest-mocking">
    <summary>Mocking in pytest through the mocker fixture instead of bare unittest.mock: patch at the use site rather than the definition, autospec so wrong-argument calls fail, spy when the real implementation must still run, and never patch the ORM.</summary>
  </methodology>
  <methodology slug="python-pytest-parametrize" tier="free" path="skills/faion/knowledge/backend/python-pytest-parametrize">
    <summary>Folds three or more near-identical tests into one @pytest.mark.parametrize: explicit ids so failures name themselves, pytest.param for per-case xfail or skip marks, stacked decorators for cartesian products, and indirect= when values must flow through a fixture.</summary>
  </methodology>
  <methodology slug="python-pytest-setup" tier="free" path="skills/faion/knowledge/backend/python-pytest-setup">
    <summary>pytest config + plugin set + coverage gate + parallel CI + agentic TDD loop.</summary>
  </methodology>
  <methodology slug="ruby-activerecord" tier="pro" path="skills/faion/knowledge/backend/ruby-activerecord">
    <summary>Rails ActiveRecord methodology — composable Query Objects, named scopes (no default_scope), eager-load at the controller boundary, find_each for bulk, Bullet + Prosopite N+1 gates in CI.</summary>
    <assumes slug="ruby-rails">Umbrella for controller / service layering.</assumes>
    <assumes slug="decomposition-rails">Service / Query / Form decomposition.</assumes>
  </methodology>
  <methodology slug="ruby-rails" tier="pro" path="skills/faion/knowledge/backend/ruby-rails">
    <summary>Production-grade Rails 7+ — Service Objects with shared ServiceResult, transactional writes with side effects via after_commit, Sidekiq jobs taking IDs only, RSpec + FactoryBot per-branch tests.</summary>
    <assumes slug="ruby-activerecord">ORM discipline that Service Objects rely on.</assumes>
    <assumes slug="ruby-rails-patterns">ServiceResult shape + transaction patterns.</assumes>
    <assumes slug="decomposition-rails">Service / Query / Form decomposition.</assumes>
  </methodology>
  <methodology slug="ruby-rails-patterns" tier="pro" path="skills/faion/knowledge/backend/ruby-rails-patterns">
    <summary>Rails 7+ Service Object + ServiceResult patterns — single canonical ServiceResult class, services accept params:+current_user:, side effects via after_commit, Pundit verify_authorized, Bullet+Brakeman+bundler-audit gates in CI.</summary>
    <assumes slug="ruby-rails">Umbrella for the broader Rails patterns.</assumes>
    <assumes slug="ruby-activerecord">ORM discipline.</assumes>
    <assumes slug="decomposition-rails">Service / Query / Form decomposition.</assumes>
  </methodology>
  <methodology slug="ruby-rspec-testing" tier="pro" path="skills/faion/knowledge/backend/ruby-rspec-testing">
    <summary>Produces a layered RSpec test plan + skeleton (model / service / request / system specs) with factory_bot, shoulda-matchers, SimpleCov branch coverage gates and one-behaviour-per-it discipline.</summary>
    <assumes slug="ruby-rails">host framework conventions</assumes>
  </methodology>
  <methodology slug="ruby-sidekiq-jobs" tier="pro" path="skills/faion/knowledge/backend/ruby-sidekiq-jobs">
    <summary>Produces a Sidekiq job spec: idempotent `perform`, IDs not records, explicit retry/dead settings, custom backoff for transient errors, RecordNotFound handling, thin job class delegating to a service.</summary>
    <assumes slug="ruby-rails">host framework conventions</assumes>
  </methodology>
  <methodology slug="rust-backend" tier="pro" path="skills/faion/knowledge/backend/rust-backend">
    <summary>Opinionated Axum service layout: AppError + IntoResponse mapping to RFC 7807, layered modules, Arc-shared AppState, spawn_blocking for CPU-bound work, vertical-slice delivery.</summary>
  </methodology>
  <methodology slug="rust-http-handlers" tier="pro" path="skills/faion/knowledge/backend/rust-http-handlers">
    <summary>Handler shape for Axum and Actix: typed DTOs behind a validator, shared state as Arc&lt;AppState&gt;, one AppError implementing IntoResponse, tracing instrumentation, and public messages scrubbed of internals.</summary>
  </methodology>
  <methodology slug="rust-project-structure" tier="pro" path="skills/faion/knowledge/backend/rust-project-structure">
    <summary>src/{config,error,routes,handlers,services,models,db,middleware}/ layout with AppState behind Arc; main.rs ≤80 lines; new modules added via scaffold script.</summary>
  </methodology>
  <methodology slug="rust-testing-antipatterns" tier="pro" path="skills/faion/knowledge/backend/rust-testing-antipatterns">
    <summary>Review checklist catching Utc::now() in assertions, sleep-driven async waits, shared global state, hardcoded ports, hallucinated mockall predicates, and flake quarantine misuse.</summary>
  </methodology>
  <methodology slug="rust-testing-ci-toolchain" tier="pro" path="skills/faion/knowledge/backend/rust-testing-ci-toolchain">
    <summary>Produces a pinned CI config (nextest.toml + GitHub Actions workflow) that runs cargo nextest + llvm-cov + nightly miri with cached tool installs.</summary>
    <assumes slug="rust-testing-unit">this CI gate executes the unit suite the rust-testing-unit methodology authored</assumes>
  </methodology>
  <methodology slug="rust-testing-integration" tier="pro" path="skills/faion/knowledge/backend/rust-testing-integration">
    <summary>Produces Rust integration tests that drive the real Axum/Actix router via tower::ServiceExt::oneshot, isolate DB state via testcontainers + per-test schema, and snapshot output with insta.</summary>
    <assumes slug="rust-testing-unit">Defines colocated unit-test placement; integration tests live in tests/ and must not collide</assumes>
  </methodology>
  <methodology slug="rust-testing-property" tier="pro" path="skills/faion/knowledge/backend/rust-testing-property">
    <summary>Produces property-based tests in Rust using proptest with persisted regression seeds, approx assertions for floats, and bounded `cases` to keep CI minutes predictable.</summary>
    <assumes slug="rust-testing-unit">Property tests live in the same #[cfg(test)] mod blocks as unit tests</assumes>
  </methodology>
  <methodology slug="rust-testing-unit" tier="pro" path="skills/faion/knowledge/backend/rust-testing-unit">
    <summary>Unit testing in Rust: colocating tests in #[cfg(test)] modules with black-box tests at the crate root, and mocking trait dependencies rather than structs that have a cheap real implementation.</summary>
    <assumes slug="rust-testing-ci-toolchain">CI must run cargo nextest to actually execute the tests authored here</assumes>
  </methodology>
  <methodology slug="rust-tokio-async" tier="pro" path="skills/faion/knowledge/backend/rust-tokio-async">
    <summary>Produces Tokio async code that picks the lightest correct primitive per workload class (try_join, spawn_blocking, buffer_unordered+Semaphore, JoinSet), wraps all I/O in timeouts, and holds no MutexGuard across .await.</summary>
    <assumes slug="rust-testing-unit">Tokio tests use the flavors authored by rust-testing-unit</assumes>
  </methodology>
  <methodology slug="secrets-management" tier="solo" path="skills/faion/knowledge/backend/secrets-management">
    <summary>Generates an env-file + systemd EnvironmentFile + 1Password integration plan for a solo VPS — defense-in-depth across file perms, .gitignore, and pre-commit hooks.</summary>
  </methodology>
  <methodology slug="server-init-bootstrap" tier="solo" path="skills/faion/knowledge/backend/server-init-bootstrap">
    <summary>Generates a 5-phase first-login plan for a fresh Ubuntu 24.04 VPS — access, identity, packages, hardening, services foundation — gated by SSH-key-verified non-root login.</summary>
  </methodology>
  <methodology slug="shell-productivity" tier="solo" path="skills/faion/knowledge/backend/shell-productivity">
    <summary>Generates a per-host shell-productivity bundle (fzf + ripgrep + bat + starship + zoxide) with shell config + alias file, gated by an idempotent installer.</summary>
  </methodology>
  <methodology slug="sql-optimization" tier="pro" path="skills/faion/knowledge/backend/sql-optimization">
    <summary>Produces a SQL optimization report (EXPLAIN ANALYZE evidence, index recommendations, query rewrites, before/after timings) for the top resource-consuming queries.</summary>
    <assumes slug="database-design">Schema shape (PK, FK, normalization) is the precondition for index choices</assumes>
  </methodology>
  <methodology slug="ssh-hardening" tier="solo" path="skills/faion/knowledge/backend/ssh-hardening">
    <summary>Generates a sshd_config drop-in + client config + AllowUsers list — key-only ed25519, modern crypto, second-terminal safety rule — for an Ubuntu/Debian VPS.</summary>
  </methodology>
  <methodology slug="ssl-tls-management" tier="solo" path="skills/faion/knowledge/backend/ssl-tls-management">
    <summary>Generates a per-domain TLS plan — Cloudflare-origin OR Let's Encrypt — with cert paths, renewal cron, and ssl_params snippet — gated by an expiry-monitor cron.</summary>
  </methodology>
  <methodology slug="swap-memory-management" tier="solo" path="skills/faion/knowledge/backend/swap-memory-management">
    <summary>Generates a per-host swap + cgroup memory plan — swappiness, swap size, MemoryHigh/Max per unit, low-memory alert — gated by total-RAM and workload class.</summary>
  </methodology>
  <methodology slug="systemd-user-services" tier="solo" path="skills/faion/knowledge/backend/systemd-user-services">
    <summary>Generates a systemd --user unit + drop-in plan (Restart, MemoryMax, EnvironmentFile, journal) for a long-running solo service — gated by `loginctl enable-linger`.</summary>
  </methodology>
  <methodology slug="tmux-power-user" tier="solo" path="skills/faion/knowledge/backend/tmux-power-user">
    <summary>Generates a tmux config + named-session script + plugin set (resurrect, continuum, yank) for a multi-host operator — gated by versioned dotfiles.</summary>
  </methodology>
  <methodology slug="unattended-upgrades" tier="solo" path="skills/faion/knowledge/backend/unattended-upgrades">
    <summary>Generates an unattended-upgrades policy — security only, reboot window, mail-on-failure — gated by an upgrade-status report + reboot-required check.</summary>
  </methodology>
  <methodology slug="vercel-deployment-discipline" tier="solo" path="skills/faion/knowledge/backend/vercel-deployment-discipline">
    <summary>Generates a DeploymentPlan + per-env audit for Vercel — env scoping, edge-runtime allow-list, ISR tenant discriminator, atomic rollback — gated by go/no-go criteria.</summary>
  </methodology>
  <methodology slug="websocket-design" tier="solo" path="skills/faion/knowledge/backend/websocket-design">
    <summary>Designs a WebSocket service with versioned envelope, heartbeat/ping-pong, exponential reconnect with full jitter, Redis Pub/Sub fan-out, and bounded backpressure queues.</summary>
    <assumes slug="api-rest-design">ticket endpoint and lifecycle webhooks ride on top of REST conventions</assumes>
  </methodology>
  <methodology slug="wireguard-vpn" tier="solo" path="skills/faion/knowledge/backend/wireguard-vpn">
    <summary>Generates a WireGuard server + per-peer config (split-tunnel dev, full-tunnel mobile, site-to-site gateway) — cryptokey routing AllowedIPs per peer — gated by sysctl forwarding.</summary>
  </methodology>
</index>
