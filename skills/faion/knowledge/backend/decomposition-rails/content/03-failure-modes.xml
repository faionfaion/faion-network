<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="Rails Decomposition Failure Modes" version="1.1.0" est_tokens="900" depth="essential">
  <summary>6 antipatterns observed in real Rails codebases applying the decomposition pattern. Each pairs symptom + root cause + concrete fix.</summary>

  <section title="Antipatterns">

    <antipattern id="service-explosion" severity="medium">
      <symptom>`Users::CreateService`, `Users::UpdateService`, `Users::DeleteService` — three nearly-identical service classes copied from a template.</symptom>
      <root-cause source="Internal — service-bloat retro">Agents created one service per HTTP verb instead of consolidating CRUD variants.</root-cause>
      <fix>Consolidate variations into a single `Users::PersistenceService` with conditional logic; reserve multiple services for distinct domain operations, not HTTP verbs.</fix>
    </antipattern>

    <antipattern id="return-shape-drift" severity="high">
      <symptom>Some services return `User`, some return `Result.new(ok:, errors:)`, some raise; controllers handle each pattern differently and miss edge cases.</symptom>
      <root-cause source="Avdi Grimm — Object on Rails">No canonical return shape locked in `CONTRIBUTING.md`.</root-cause>
      <fix>Pick one (Result vs entity-or-raise), document it, and enforce via code review + a static check (`bundle exec rubocop --only ServiceReturnShape`).</fix>
    </antipattern>

    <antipattern id="concern-dumping-ground" severity="medium">
      <symptom>`app/models/concerns/searchable.rb` is 320 LOC and mixes search, slug, soft-delete, audit logic.</symptom>
      <root-cause source="DHH — Concerns blog post">Concerns used as a dumping ground for unrelated cross-cutting logic.</root-cause>
      <fix>Concerns stay narrow mixins (1-20 LOC) for one single cross-cutting behaviour. Complex logic belongs in services or query objects.</fix>
    </antipattern>

    <antipattern id="form-service-overlap" severity="medium">
      <symptom>The same validation appears in both `Users::SignupForm` and `Users::CreateService`; agents update one and miss the other.</symptom>
      <root-cause source="Internal — validation-drift retro">No clear contract between form-shape validation and business-rule validation.</root-cause>
      <fix>Form Objects validate input shape only (presence, format). Services validate business rules. Refactor overlaps into a single shape contract.</fix>
    </antipattern>

    <antipattern id="scope-drift" severity="medium">
      <symptom>`User.where(active: true)` inside a query object AND `scope :active` on the model; one site updates, the other doesn't.</symptom>
      <root-cause source="Internal — query-drift retro 2026-Q1">Query Object re-implemented an existing scope.</root-cause>
      <fix>Query Objects compose existing scopes (`User.active.recent.where(...)`); no inline filter duplication.</fix>
    </antipattern>

    <antipattern id="update-columns-bypass" severity="high">
      <symptom>Counter caches stale; audit log missing entries; agent reached for `update_columns` "for performance".</symptom>
      <root-cause source="Rails docs — update_columns warning">Service bypassed callbacks via `update_columns`, breaking cross-cutting behaviour.</root-cause>
      <fix>Document which callbacks fire on which methods. Flag `update_columns` / `update_all` in code review; require an explicit `# @warning bypasses callbacks` comment.</fix>
    </antipattern>

  </section>
</text>
