<?xml version="1.0" encoding="UTF-8"?>
<text id="05-examples" title="django-serializers Worked Example" version="1.1.0" est_tokens="400" depth="optional">
  <summary>Worked example: user-create endpoint with separate Input and Output serializers, service call, and PII guard test.</summary>

  <section title="Serializers">
    <example type="good" title="Input + Output split">
      <code lang="python"><![CDATA[
# apps/users/serializers.py
import re
from rest_framework import serializers


class UserCreateRequest(serializers.Serializer):
    email = serializers.EmailField(required=True, allow_blank=False, allow_null=False)
    name = serializers.CharField(required=True, allow_blank=False, max_length=100)
    password = serializers.CharField(required=True, min_length=12, write_only=True)

    def validate_password(self, value: str) -> str:
        # SHAPE-ONLY: no DB / service calls
        if not re.search(r"[A-Z]", value) or not re.search(r"\d", value):
            raise serializers.ValidationError("password must contain uppercase letter and digit")
        return value


class UserResponse(serializers.Serializer):
    id = serializers.IntegerField(read_only=True)
    email = serializers.EmailField(read_only=True)
    name = serializers.CharField(read_only=True)
    created_at = serializers.DateTimeField(read_only=True)
    # NOTE: no password, no is_staff, no mfa_secret — explicit allowlist
]]></code>
    </example>
  </section>

  <section title="View">
    <example type="good">
      <code lang="python"><![CDATA[
# apps/users/views.py
from rest_framework.views import APIView
from rest_framework.response import Response
from rest_framework import status
from apps.users.serializers import UserCreateRequest, UserResponse
from apps.users import services


class UserCreateApi(APIView):
    def post(self, request):
        serializer = UserCreateRequest(data=request.data)
        serializer.is_valid(raise_exception=True)
        user = services.user_create(**serializer.validated_data)
        return Response(UserResponse(user).data, status=status.HTTP_201_CREATED)
]]></code>
    </example>
  </section>

  <section title="PII-guard test">
    <example type="good">
      <code lang="python"><![CDATA[
# apps/users/tests/test_user_create_api.py
import pytest


@pytest.mark.django_db
def test_user_create_response_excludes_password(client):
    resp = client.post(
        "/api/users/",
        data={"email": "a@b.test", "name": "A", "password": "Aa1bbbbbbbbb"},
        content_type="application/json",
    )
    assert resp.status_code == 201
    body = resp.json()
    assert "password" not in body
    assert "is_staff" not in body
    assert "mfa_secret" not in body
]]></code>
      <note>Deliberately renaming the Output to include `password` causes this test to fail — the PII boundary is regression-locked.</note>
    </example>
  </section>
</text>
