{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://faion.net/schemas/vars/backend/server-init-bootstrap/server-init-bootstrap.vars.schema.json",
  "title": "server-init-bootstrap template variables",
  "description": "Values for templates/server-init-bootstrap.md.j2 and templates/server-init-bootstrap.html.j2. Generated by tpl-jinja from server-init-bootstrap.md; `description` on each property is the question put to the author.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artefact_slug": {
      "allOf": [
        {
          "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/artefact_slug"
        }
      ],
      "description": "Artefact id. From section 'Server Init Bootstrap'."
    },
    "decision": {
      "type": "string",
      "title": "Decision",
      "description": "The decision used under 'Server Init Bootstrap'.",
      "enum": [
        "go",
        "hold",
        "no-op"
      ]
    },
    "input_name": {
      "type": "string",
      "title": "Input name",
      "description": "The input name used under 'Server Init Bootstrap'."
    },
    "owner_handle": {
      "allOf": [
        {
          "$ref": "../../../../templates/vars-dictionary.schema.json#/$defs/owner_handle"
        }
      ],
      "description": "Owner. From section 'Server Init Bootstrap'."
    }
  },
  "required": [
    "artefact_slug",
    "decision",
    "input_name",
    "owner_handle"
  ],
  "x-faion-dictionary-refs": 2
}
