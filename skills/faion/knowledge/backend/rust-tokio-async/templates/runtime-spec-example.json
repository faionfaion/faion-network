{
  "__faion_header__": {
    "purpose": "Filled-in runtime-level tokio spec: the companion record to the per-pattern artefact.",
    "consumes": "service workload, crate list, producer/consumer rates, per-call SLO, signal handling",
    "produces": "artefact conforming to the runtime-level companion contract in content/02-output-contract.xml",
    "depends-on": "content/01-core-rules.xml (r3, r6, r9, r10, r11) + content/04-procedure.xml step 4b",
    "token-budget-impact": "~200 tokens when loaded as context"
  },
  "runtime_flavour": "multi_thread",
  "worker_threads": 4,
  "blocking_offload": "spawn_blocking",
  "channel_policy": {
    "bounded": true,
    "default_capacity": 512
  },
  "timeout_default_ms": 3000,
  "shutdown_signal": "ctrl_c"
}
