<?xml version='1.0' encoding='utf-8'?>
<text id="01-core-rules" title="django-pytest-parametrize Core Rules" version="1.1.0" est_tokens="1300" depth="essential">
  <summary>6 testable rules for django-pytest-parametrize: parametrize semantics, three-tuple matrix, refactor threshold, fixture switching via getfixturevalue, plus the two spec-level branch rules the decision tree routes to (collapse band, keep-separate or split).</summary>

  <section title="Rule set">
    <rule id="r1" testable="true" depth="essential">
      <statement>Use @pytest.mark.parametrize("argnames", [...values]) to run the same test body with different inputs. For multiple arguments, pass a comma-separated string for argnames and tuples for values. Use ids= parameter for descriptive test IDs when tuple values are not self-explanatory. Each combination must be independent and must not share mutable state.</statement>
      <rationale source="v1-source">Descriptive IDs make test failure messages pinpoint the exact failing case: FAILED test_validation[email-invalid] is clearer than FAILED test_validation[0]. Each combination runs as an independent test, so failures don't cascade. Shared mutable state between combinations creates order-dependent failures.</rationale>
    </rule>
    <rule id="r2" testable="true" depth="essential">
      <statement>For API validation tests, parametrize over (field, value, expected_error) tuples. Start from a valid payload dict and override the field under test. Assert on both status code (400) and the presence of the expected error message in response.data[field]. Use descriptive comments or inline variable names in the parameter tuples.</statement>
      <rationale source="v1-source">The three-tuple pattern (field, value, error) is self-documenting: it reads as "when field has value, expect error". Starting from a valid payload ensures the only variable is the field under test, isolating the validation rule. Asserting on the error message (not just 400) catches cases where validation fails for the wrong reason.</rationale>
    </rule>
    <rule id="r3" testable="true" depth="essential">
      <statement>Refactor a group of tests to parametrize when: (1) the tests have the same body structure and differ only in data, (2) there are 3 or more similar tests, (3) adding a new case requires copying an existing test. Keep each parameter combination's intent clear through comments in the tuple or the ids= parameter. Do NOT parametrize tests that have different setup, teardown, or assertion logic.</statement>
      <rationale source="v1-source">Three identical tests are a maintenance burden: changes must be applied three times. parametrize collapses them to one body, so changes apply to all cases at once. The ids= parameter gives each combination a readable name in pytest output, making failures easy to understand without reading the source.</rationale>
    </rule>
    <rule id="r4" testable="true" depth="essential">
      <statement>Use request.getfixturevalue(fixture_name) inside a parametrized test to select between fixtures by name. Use @pytest.mark.parametrize("fixture", [...], indirect=True) when you need to pass parameters through a fixture to prepare test objects. Avoid using indirect= for simple cases; prefer explicit fixture composition.</statement>
      <rationale source="v1-source">getfixturevalue lets you parametrize over client fixtures (api_client, authenticated_client, admin_client) without duplicating the test body for each role. indirect= is a power-user feature for when fixture preparation depends on the parameter value; it's harder to read and should be used sparingly.</rationale>
    </rule>
    <rule id="r1-parametrize" testable="true" depth="essential">
      <statement>When 3 to 20 near-identical test bodies differ only in data and share one control-flow path, they MUST collapse into one parametrizations[] entry: argnames is a comma-separated string naming every varying value (field, value, expected_error), each case carries an id that names the scenario in words (amount-negative, due-date-past) via pytest.param(..., id=) or ids=, and ids_descriptive is true. A case whose args contain a model instance is rejected: pass the factory name or a primary key and build the object inside the test body. Auto-numeric ids ([0], [1]) or an id that repeats another case's id are rejected by scripts/validate-django-pytest-parametrize.py.</statement>
      <rationale source="pytest documentation, 'How to parametrize fixtures and test functions' (pytest.param, ids)">The value of the collapse is in the failure line: FAILED test_create_invoice_validation[due-date-past] tells the reader which rule broke without opening the file, [2] does not, which is fm-3. Model instances in the parameter list are constructed at collection time, outside the db fixture and outside the test's transaction, so they either error on import or leak rows across cases; ids plus a factory call keep every case independent, which is what makes the 3-to-20 band safe to run under xdist.</rationale>
    </rule>
    <rule id="r3-refactor-threshold" testable="true" depth="essential">
      <statement>When fewer than 3 near-identical bodies exist, or when the cases differ in control flow (a different branch, setup, teardown or assertion shape), the tests MUST stay as separate named functions and no parametrizations[] entry is written; the third copy is the trigger, not the second. When more than 20 cases share one body, the parametrization MUST be split into category-named test functions (test_valid_emails, test_invalid_emails) so that no single parametrizations[].cases list exceeds 20 entries; a spec entry with more than 20 cases is rejected.</statement>
      <rationale source="pytest documentation, parametrize; Martin Fowler, 'Refactoring' (Rule of Three)">Below three copies the parametrize scaffolding costs more lines than it removes and hides the second test's intent behind a tuple; cases with different control flow are different concepts, and forcing them under one body produces if-branches inside the test that the ids cannot explain. Above twenty rows the parameter table stops being readable in review and a single failure is lost in a wall of ids (fm-1); splitting by category restores a name per concept and keeps each table short enough to audit.</rationale>
    </rule>
  </section>
</text>