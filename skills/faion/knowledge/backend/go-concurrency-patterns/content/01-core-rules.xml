<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Go Concurrency Patterns (Worker Pool, Fan-Out/Fan-In, errgroup, Pipeline) Core Rules" version="1.1.0" est_tokens="900" depth="essential">
  <summary>10 testable rules every Go Concurrency Patterns (Worker Pool, Fan-Out/Fan-In, errgroup, Pipeline) application MUST enforce. HARD rules — user-supplied input cannot override them.</summary>

  <section title="Rule set">

    <rule id="r-classify-workload" testable="true" depth="essential">
      <statement>Pattern choice MUST follow the matrix: worker-pool (IO) / fan-out-fan-in (CPU) / errgroup (fail-fast) / pipeline (streaming).</statement>
      <rationale source="Internal benchmarks">Rule rooted in source: Internal benchmarks. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-bound-parallelism" testable="true" depth="essential">
      <statement>Concurrent goroutine count MUST be capped by a semaphore or fixed pool size.</statement>
      <rationale source="Effective Go">Rule rooted in source: Effective Go. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-ctx-on-every-task" testable="true" depth="essential">
      <statement>Every task signature MUST take `context.Context` and honour cancellation.</statement>
      <rationale source="Go context docs">Rule rooted in source: Go context docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-panic-recovery" testable="true" depth="essential">
      <statement>Worker goroutines MUST recover panics and convert to errors; never let panic kill the pool.</statement>
      <rationale source="stdlib + reviews">Rule rooted in source: stdlib + reviews. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-errgroup-when-fail-fast" testable="true" depth="essential">
      <statement>Use `golang.org/x/sync/errgroup` with `WithContext` when first error aborts siblings.</statement>
      <rationale source="x/sync docs">Rule rooted in source: x/sync docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-singleflight-for-dedup" testable="true" depth="essential">
      <statement>In-flight duplicate calls MUST be deduped via `golang.org/x/sync/singleflight`.</statement>
      <rationale source="x/sync docs">Rule rooted in source: x/sync docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-goleak-test-required" testable="true" depth="essential">
      <statement>Every pattern MUST ship a `TestNoLeak` using `goleak.VerifyNone(t)`.</statement>
      <rationale source="goleak docs">Rule rooted in source: goleak docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-channel-closed-by-sender" testable="true" depth="essential">
      <statement>Channels are closed ONLY by the sender (or by the last sender via `sync.Once` / a coordinator that owns the close). Receivers never close, and there is no &apos;close from outside&apos; cleanup path.</statement>
      <rationale source="Go specification + community convention">Closing a channel from the receiving side panics any live sender with &apos;send on closed channel&apos;. Sender-owns-close is the only convention that is safe by construction. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-goroutine-exit-signal" testable="true" depth="essential">
      <statement>Every spawned goroutine MUST have a deterministic exit path — `ctx.Done()`, an input channel that gets drained and closed, or `wg.Done()`-tracked completion. &apos;Eventually it returns&apos; is not an exit path.</statement>
      <rationale source="Practitioner experience">Hand-wavy exit paths are exactly how leaks are born; an explicit signal is what makes a goroutine lifecycle reviewable and what `goleak` can actually assert on. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-fanout-fanin-canonical-shape" testable="true" depth="essential">
      <statement>An N-stage pipeline uses the canonical fan-out/fan-in shape: bounded pool per stage, channels between stages, a single output channel. A bespoke pipeline shape requires an ADR.</statement>
      <rationale source="Go concurrency patterns literature">Fan-out/fan-in is the shape every Go reviewer already knows and the one where backpressure is visible. Bespoke shapes accrue maintenance cost and hide where the queue actually builds up.</rationale>
    </rule>

  </section>

  <section title="Conclusion targets used by the decision tree">
    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement>Run the full procedure; emit an artefact passing the output contract.</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when preconditions hold and no duplicate exists.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Skip; surface the missing precondition; do not synthesize.</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when preconditions fail.</rationale>
    </rule>
    <rule id="update-existing-artefact" testable="true" depth="essential">
      <statement>Update the existing artefact in place (bump version, refresh last_reviewed).</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when a duplicate is found.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>