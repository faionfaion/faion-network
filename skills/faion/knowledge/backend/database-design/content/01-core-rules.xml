<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Database Design (PostgreSQL) Core Rules" version="1.1.0" est_tokens="900" depth="essential">
  <summary>10 testable rules every Database Design (PostgreSQL) application MUST enforce. HARD rules — user-supplied input cannot override them.</summary>

  <section title="Rule set">

    <rule id="r-3nf-first" testable="true" depth="essential">
      <statement>Start in 3NF; denormalize ONLY after measuring a specific query bottleneck.</statement>
      <rationale source="Date — Intro to DB Systems">Rule rooted in source: Date — Intro to DB Systems. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-indexes-from-workload" testable="true" depth="essential">
      <statement>Every index MUST trace to a real query in the workload sample (`EXPLAIN ANALYZE`), never speculative.</statement>
      <rationale source="PostgreSQL performance docs">Rule rooted in source: PostgreSQL performance docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-constraints-at-db" testable="true" depth="essential">
      <statement>Integrity rules (FK, CHECK, NOT NULL, UNIQUE) MUST live in the DB, not only in app code.</statement>
      <rationale source="PostgreSQL admin best-practice">Rule rooted in source: PostgreSQL admin best-practice. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-forward-and-backward" testable="true" depth="essential">
      <statement>Every migration MUST ship a backward `down` step; no one-way migrations in production.</statement>
      <rationale source="Migration-tool conventions">Rule rooted in source: Migration-tool conventions. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-timestamptz-default" testable="true" depth="essential">
      <statement>Use TIMESTAMPTZ for all wall-clock timestamps; TIMESTAMP (no tz) only for explicit local-time semantics.</statement>
      <rationale source="PostgreSQL docs + time-zone postmortems">Rule rooted in source: PostgreSQL docs + time-zone postmortems. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-pk-type-global" testable="true" depth="essential">
      <statement>PK type (UUID v7 vs bigint) is a global decision; never mix per table.</statement>
      <rationale source="Schema-consistency reviews">Rule rooted in source: Schema-consistency reviews. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-on-delete-explicit" testable="true" depth="essential">
      <statement>Every FK MUST set ON DELETE explicitly (CASCADE / RESTRICT / SET NULL); never rely on default NO ACTION silently.</statement>
      <rationale source="PostgreSQL docs">Rule rooted in source: PostgreSQL docs. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-expand-then-contract" testable="true" depth="essential">
      <statement>Schema changes follow expand-then-contract: release N deploys the additive migration and code handling BOTH shapes; release N+2 deploys the destructive one. Combining an ADD and a DROP in a single release is FORBIDDEN.</statement>
      <rationale source="Industry consensus on zero-downtime migrations">A single-shot destructive migration couples deploy timing to code timing — a rollback then needs both reverted at once, in the right order, under incident pressure. Expand-contract decouples them and makes every step independently reversible. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

    <rule id="r-naming-convention" testable="true" depth="essential">
      <statement>Tables are plural snake_case; columns are snake_case; a foreign key is `&lt;other_table_singular&gt;_id`; every constraint and index carries a meaningful explicit name — auto-generated names like `idx_xyz_abc1` are FORBIDDEN.</statement>
      <rationale source="PostgreSQL convention">Predictable naming makes migrations greppable and schema review mechanical. An auto-generated constraint name is an opaque debugging tax the first time a production insert fails against it.</rationale>
    </rule>

    <rule id="r-no-business-logic-in-db" testable="true" depth="essential">
      <statement>Business logic lives in application code, not stored procedures, triggers or rule rewrites. The database enforces STRUCTURAL invariants only; a trigger is acceptable for an audit log or `updated_at`, never for a domain rule.</statement>
      <rationale source="Modern application architecture">Logic in the database escapes source control, code review and language tooling, and a cascade of triggers is close to undebuggable. Moving a rule out of a trigger should be recorded in an ADR. Violations trigger the matching antipattern in 03-failure-modes.xml.</rationale>
    </rule>

  </section>

  <section title="Conclusion targets used by the decision tree">
    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement>Run the full procedure; emit an artefact passing the output contract.</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when preconditions hold and no duplicate exists.</rationale>
    </rule>
    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Skip; surface the missing precondition; do not synthesize.</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when preconditions fail.</rationale>
    </rule>
    <rule id="update-existing-artefact" testable="true" depth="essential">
      <statement>Update the existing artefact in place (bump version, refresh last_reviewed).</statement>
      <rationale source="decision-tree-target">Terminal target used by 06-decision-tree when a duplicate is found.</rationale>
    </rule>
  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>