<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Go Error Handling Patterns (Wrap, Translate, Sentinel, Log-Once) Decision Tree" version="1.1.0" est_tokens="400">
  <summary>Two-question gate: preconditions present? duplicate artefact exists? Routes to run / update / skip. Every conclusion references a rule id from 01-core-rules.xml.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Are ALL preconditions satisfied? (prerequisite artefacts present AND named owner identifiable AND artefact store writable)</root-question>

    <branch when="q-preconditions == true">
      <question id="q-duplicate">Does an existing working artefact already cover this gap?</question>

      <branch when="q-duplicate == true">
        <conclusion ref="update-existing-artefact">Skip: do not duplicate. Update the existing artefact in place (bump version, refresh last_reviewed).</conclusion>
      </branch>

      <branch when="q-duplicate == false">
        <question id="q-error-tactics-gates">Which tactic gate is red? (single-layer script or foreign error model / chain broken by string concat or %v / type assertions on errors / panic used for a domain error / more than one edge translator / all green)</question>

        <branch when="q-error-tactics-gates == 'all-green'">
          <conclusion ref="run-the-checklist">Run the procedure. Emit a new artefact per the output contract.</conclusion>
        </branch>

        <branch when="q-error-tactics-gates == 'chain-broken'">
          <conclusion ref="r-wrap-once-per-layer">BLOCK — restore the chain with `fmt.Errorf(&quot;verb: %w&quot;, err)` before anything else; every downstream check is currently matching against a string.</conclusion>
        </branch>

        <branch when="q-error-tactics-gates == 'type-assertions-on-errors'">
          <conclusion ref="r-errors-is-not-equality">BLOCK — replace assertions and `==` comparisons with `errors.Is` / `errors.As`.</conclusion>
        </branch>

        <branch when="q-error-tactics-gates == 'panic-for-domain-error'">
          <conclusion ref="r-no-panic-for-control-flow">BLOCK — return the sentinel instead; a recovered panic cannot carry a status the edge can map.</conclusion>
        </branch>

        <branch when="q-error-tactics-gates == 'multiple-edge-translators'">
          <conclusion ref="r-translate-at-edge-once">BLOCK — merge the translators into one middleware / interceptor; the API is currently inconsistent per endpoint.</conclusion>
        </branch>

        <branch when="q-error-tactics-gates == 'single-layer-or-foreign-model'">
          <conclusion ref="skip-this-methodology">Skip: single-layer script, library package, or a project on `pkg/errors` / `cockroachdb/errors` whose model these rules would fight.</conclusion>
        </branch>
      </branch>
    </branch>

    <branch when="q-preconditions == false">
      <conclusion ref="skip-this-methodology">Skip this methodology. Surface the missing precondition. Defer until the gap is closed.</conclusion>
    </branch>
  </decision-tree>

  <example trace="all-preconditions-and-no-duplicate"><![CDATA[
inputs: {owner=alex@acme.com, prerequisites_present=true, existing_artefact=false}
q-preconditions -> true
q-duplicate -> false
=> conclusion run-the-checklist: emit new artefact.
]]></example>
</text>