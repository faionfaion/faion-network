<?xml version="1.0" encoding="UTF-8"?>
<text id="03-failure-modes" title="Laravel PHPUnit / Pest Failure Modes" version="1.1.0" est_tokens="900" depth="essential">
  <summary>8 antipatterns observed in real Laravel test suites. Each pairs symptom + root cause + concrete fix.</summary>

  <section title="Antipatterns">

    <antipattern id="unit-tests-booting-laravel" severity="medium">
      <symptom>`tests/Unit/` runs in 18s even though there are 12 tests; profile shows Laravel boot per test.</symptom>
      <root-cause source="Laravel docs — Test structure">Unit test class extends `Tests\TestCase` (which boots Laravel) instead of `PHPUnit\Framework\TestCase`.</root-cause>
      <fix>Switch base class. Reserve `Tests\TestCase` for Feature tests that actually need the framework.</fix>
    </antipattern>

    <antipattern id="fake-after-action" severity="high">
      <symptom>Test asserts no mail was sent, but the user reports receiving the email; CI green, prod broken.</symptom>
      <root-cause source="Laravel docs — Mocking">`Mail::fake()` was called AFTER `$this-&gt;postJson(...)`; the real mail dispatched, then the fake started for the assertion phase.</root-cause>
      <fix>Move every `*::fake()` to the top of the test, before the action. Add a Pest lint rule that fails on `*::fake()` appearing below `$this-&gt;` HTTP calls.</fix>
    </antipattern>

    <antipattern id="assertjsonstructure-misuse" severity="medium">
      <symptom>Response has `"email": null` and the test still passes.</symptom>
      <root-cause source="Laravel docs — JSON assertions">`assertJsonStructure(['data' =&gt; ['email']])` asserts the key exists, not its value.</root-cause>
      <fix>Use `assertJsonPath('data.email', $expected)` for value checks. Reserve `assertJsonStructure` for deep key-presence in dynamic payloads.</fix>
    </antipattern>

    <antipattern id="shared-mutable-fixture" severity="high">
      <symptom>Tests pass in isolation, fail when run in a specific order; root cause hides for weeks.</symptom>
      <root-cause source="Internal — flake retro 2026-Q1">Static factory state persists across tests; or `setUp` mutates a shared singleton without resetting.</root-cause>
      <fix>Always use `RefreshDatabase` on Feature tests; reset singletons via `$this-&gt;app-&gt;instance(...)` in `setUp`; avoid static state in factories. Prove it by running `--order-by=random` three times, per `no-test-order-dependence`.</fix>
    </antipattern>

    <antipattern id="copy-paste-cases" severity="medium">
      <symptom>Twenty near-identical test methods differ only in one input and one expected value; adding a case means another copy.</symptom>
      <root-cause source="PHPUnit reference — data providers">No data provider; the cases were duplicated instead of tabulated.</root-cause>
      <fix>Add `public static function casesProvider(): array` and `#[DataProvider('casesProvider')]` on a single test method, per `data-provider-table-tests`.</fix>
    </antipattern>

    <antipattern id="facade-fake-everywhere" severity="medium">
      <symptom>The unit suite takes minutes; every test boots Laravel even when the class under test has no framework dependency.</symptom>
      <root-cause source="PHPUnit reference — test doubles">Every test uses facade fakes (`Mail::fake()`), which require the container.</root-cause>
      <fix>Convert service tests to constructor-injected mocks (`createMock(MailService::class)`) and reserve facade fakes for Feature tests, per `mocks-via-di`.</fix>
    </antipattern>

    <antipattern id="no-aaa" severity="low">
      <symptom>The test reads as a maze; a reviewer cannot say which line is the behaviour under test.</symptom>
      <root-cause source="TDD by Example (Beck)">Arrange, act and assert are interleaved.</root-cause>
      <fix>Collect the setup at the top, keep a single act in the middle and put every assertion at the bottom, separated by blank lines, per `aaa-structure`.</fix>
    </antipattern>

    <antipattern id="time-based-flakes" severity="medium">
      <symptom>Test asserts `Carbon::now()-&gt;diffInSeconds(...) &lt; 1` and flakes 1 in 50 CI runs.</symptom>
      <root-cause source="Laravel docs — Time helpers">Test uses real wall-clock time without freezing it.</root-cause>
      <fix>Wrap the action in `$this-&gt;travelTo(now())` (Laravel 9+); assert exact equality with the frozen time. Never use `sleep(...)` in tests.</fix>
    </antipattern>

  </section>
</text>
