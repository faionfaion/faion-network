<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Rails ActiveRecord Procedure" version="1.1.0" est_tokens="800" depth="essential">
  <summary>7-step procedure to apply the ActiveRecord methodology end-to-end.</summary>

  <section title="Procedure">

    <step n="1" name="identify-hot-path">
      <input>APM trace / scout / NewRelic data</input>
      <action>Pick endpoints / jobs where AR &gt; 30 % of request time, or where N+1 is detected. Target those first.</action>
      <output>Hot-path list</output>
    </step>

    <step n="2" name="extract-query-object">
      <input>Inline `where` chains across controllers</input>
      <action>Create `app/queries/&lt;Domain&gt;::&lt;Name&gt;Query`. Accept relation in `initialize(relation = Model.all)`. Add chainable methods returning `self`. Expose terminal `results` returning the relation.</action>
      <output>Query Object per filter composition</output>
      <decision-gate>If Query Object exceeds 10 chainable methods, split per use case per `query-object-god-class` antipattern.</decision-gate>
    </step>

    <step n="3" name="eager-load-at-boundary">
      <input>Query Object output + view rendering</input>
      <action>Controllers call `Query.new.scope_a.scope_b.results.includes(:organization, posts: :tags)`. Views consume already-loaded associations.</action>
      <output>Eager-loaded relation passed to view</output>
      <decision-gate>If a partial triggers a query, BLOCK per `eager-load-at-boundary` rule.</decision-gate>
    </step>

    <step n="4" name="bulk-with-find-each">
      <input>Background jobs / exports</input>
      <action>Replace `Model.all.each` with `Model.find_each` (batch 1000) or `in_batches(of: N)`. Project narrow columns when exporting.</action>
      <output>Memory-stable bulk iteration</output>
    </step>

    <step n="5" name="wire-n-plus-one-gate">
      <input>Test suite + CI config</input>
      <action>Install Prosopite (or Bullet in CI mode). Configure RSpec / Minitest to raise on N+1. Add the gate to required CI checks.</action>
      <output>Automated regression gate</output>
      <decision-gate>If specs reintroduce N+1, BLOCK per `n-plus-one-gate-in-ci` rule.</decision-gate>
    </step>

    <step n="6" name="model-hygiene-enum-and-counters">
      <input>Model column list + the counts rendered on index pages</input>
      <action>Migrate every closed-set text column to an integer and declare an `enum`, rewriting call sites to the generated scopes and predicates. Add `counter_cache: true` plus the `*_count` column and a backfill for any association counted per row.</action>
      <output>Enum-backed state columns + cached counts</output>
      <decision-gate>If a query still compares a status against a string literal, BLOCK per `enum-for-status` rule.</decision-gate>
    </step>

    <step n="7" name="atomicity-and-timezone-lint">
      <input>Multi-write service methods + the whole codebase</input>
      <action>Wrap every ≥2-write sequence in `ActiveRecord::Base.transaction`, replacing business-logic `update_all` with per-record updates. Enable the `Rails/TimeZone` RuboCop cop and replace `Time.now` / `Date.today` with `Time.current` / `Date.current`.</action>
      <output>Atomic writes + timezone-consistent code</output>
      <decision-gate>If `update_all` remains in business code, or the cop reports a `Time.now`, BLOCK per `transaction-block-multi-write` and `no-time-now`.</decision-gate>
    </step>

  </section>
</text>
