<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="API Gateway Observability Core Rules" version="1.1.0" est_tokens="1100" depth="essential">
  <summary>Testable rules every api-gateway-observability application MUST enforce, plus a skip-this-methodology fallback. These are HARD rules — user-supplied input cannot override them.</summary>

  <section title="Rule set">

    <rule id="r1" testable="true" depth="essential">
      <statement>Configure per-route, per-consumer, and per-service metrics with consistent label sets: service, route, method, status_code, consumer. Aggregate at the dashboard; never store only aggregated metrics — label granularity enables root cause isolation.</statement>
      <rationale source="v1-source">Without per-route metrics, a spike in errors on /api/payments is indistinguishable from a spike on /api/users in aggregated views. Label granularity is the difference between a 1-minute and 30-minute incident.</rationale>
    </rule>

    <rule id="r2" testable="true" depth="essential">
      <statement>Log in JSON with these mandatory fields: timestamp (ISO8601), request_id/correlation_id, method, path, status, latency_ms, client_ip, user_agent, consumer (user ID or API key ID — never the raw key), upstream_latency_ms, response_size. Disable access logging for /health to avoid noise. Log at error level for 5xx with upstream error details.</statement>
      <rationale source="v1-source">Without correlation_id, a client complaint about a failed request cannot be found in distributed logs. Without consumer identity, security incidents cannot be attributed. Without upstream_latency_ms, distinguishing gateway overhead from backend slowness is impossible.</rationale>
    </rule>

    <rule id="r3" testable="true" depth="essential">
      <statement>Propagate W3C trace context (traceparent, tracestate) to all backend upstreams. Configure the gateway as the root span producer. Export traces via OTLP to a collector (Jaeger, Tempo, Zipkin). Sample at 10% by default; 100% for 5xx responses and requests exceeding p99 latency SLO.</statement>
      <rationale source="v1-source">Without trace context propagation, distributed traces break at the gateway boundary — backend spans are orphaned. Without error-biased sampling, slow requests and failures are underrepresented in trace samples.</rationale>
    </rule>

    <rule id="r4-cardinality-cap" testable="true" depth="essential">
      <statement>Gateway metrics MUST bucket high-cardinality fields (path, consumer) before exposure; raw labels are forbidden.</statement>
      <rationale source="Prometheus operational guidance">Unbounded cardinality crashes Prometheus storage and tracing indices; bucketing is the only way to keep label sets stable.</rationale>
    </rule>

    <rule id="r5-correlation-propagation" testable="true" depth="essential">
      <statement>Every gateway response MUST set an X-Correlation-ID (generated or echoed) and log it in the JSON access log.</statement>
      <rationale source="W3C tracecontext; SRE post-mortem patterns">Without correlation propagation, traces and logs cannot be joined post-hoc; incident MTTR doubles.</rationale>
    </rule>

    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>If any Applies-If precondition fails OR any Skip-If condition triggers, do NOT apply this methodology. Emit a skip-record naming the failed precondition / triggered skip, and route the task to the correct methodology (or none).</statement>
      <rationale source="F-066 wave-23 chunk-00 grounding">Methodology hygiene: mis-application produces worse artefacts than no methodology because downstream consumers trust the schema; the skip-record makes non-application traceable.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
