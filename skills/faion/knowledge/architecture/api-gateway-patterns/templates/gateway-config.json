{
  "__faion_header__": {
    "purpose": "gateway_config skeleton — the operational half of the api-gateway-patterns artefact",
    "consumes": "selected product from the ADR, auth provider, timeout budget, GitOps controller",
    "produces": "a gateway_config block validating against content/02-output-contract.xml (r6-r13)",
    "depends_on": "content/01-core-rules.xml, content/02-output-contract.xml",
    "token_budget_impact": "~200-800 tokens when loaded as context"
  },
  "gateway_config": {
    "stateless": true,
    "rate_limit": {
      "scope": "api-key"
    },
    "request_id_propagation": true,
    "timeouts": {
      "upstream_ms": 2000,
      "gateway_ms": 5000,
      "client_ms": 10000
    },
    "circuit_breaker": {
      "scope": "per-upstream-service"
    },
    "gitops": {
      "managed_by": "deck"
    }
  }
}
