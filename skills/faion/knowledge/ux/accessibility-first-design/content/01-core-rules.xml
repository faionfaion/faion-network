<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Accessibility-First Design Core Rules" version="1.1.1" est_tokens="1800" depth="essential">
  <summary>12 testable rules anchoring the accessibility-first-design methodology, each grounded in a source. Concrete design-phase rules for WCAG 2.2 AA compliance: contrast ratios, touch targets, focus visibility, semantic structure, and motion controls — all enforced at the token and component-spec level. Includes the designer-review gate on automated fixes and the shift-left KPI.</summary>

  <section title="Rule set">

    <rule id="token-encoded-contrast" testable="true" depth="essential">
      <statement>Every text/background token pair MUST carry a `contrast_ratio` field; pairs failing 4.5:1 (body) or 3:1 (large/non-text) MUST be excluded from the system. Enforce a minimum contrast ratio of 4.5:1 for body text and 3:1 for large text (18pt+), UI components, and focus indicators; embed these thresholds in design tokens so non-compliant primitives cannot be exported.</statement>
      <rationale source="WCAG 2.2 SC 1.4.3 + 1.4.11">Token-encoded contrast prevents brand colour from shipping below thresholds; the constraint lives where the design lives. WCAG 2.2 SC 1.4.3 and 1.4.11 define these ratios. Encoding them in tokens prevents a designer from accidentally choosing a brand color that fails at the theme level before any component is built.</rationale>
    </rule>

    <rule id="color-not-sole-indicator" testable="true" depth="essential">
      <statement>Never use color as the sole indicator of meaning; pair every color signal with a shape, label, or pattern.</statement>
      <rationale source="WCAG 2.2 SC 1.4.1">Approximately 8% of males have color vision deficiency. Color-only signals (red = error) are invisible to this population and fail WCAG SC 1.4.1.</rationale>
    </rule>

    <rule id="touch-target-44px-with-spacing" testable="true" depth="essential">
      <statement>Set minimum interactive element size to 44x44 CSS pixels (or platform equivalent); ensure sufficient spacing between adjacent targets so misfire probability is low.</statement>
      <rationale source="WCAG 2.2 SC 2.5.8">WCAG 2.2 SC 2.5.8 (Target Size, Minimum) formalizes this. Spacing matters independently of size — clustered 44px targets still cause misfires for users with motor impairments or tremor.</rationale>
    </rule>

    <rule id="semantic-first-components" testable="true" depth="essential">
      <statement>Components MUST be defined by semantic archetype (button, link, dialog, etc.) before visual styling; styling MUST NOT override role.</statement>
      <rationale source="ARIA APG">Visual-first design routinely ships clickable divs and links-styled-as-buttons; archetype-first prevents the regression at source.</rationale>
    </rule>

    <rule id="focus-visible-always" testable="true" depth="essential">
      <statement>`:focus-visible` MUST be defined on every interactive token; `outline:none` without a replacement is forbidden across the system.</statement>
      <rationale source="WCAG 2.2 SC 2.4.7">Global outline:none with no replacement is the most common SC 2.4.7 violation.</rationale>
    </rule>

    <rule id="focus-indicator-contrast-3-to-1" testable="true" depth="essential">
      <statement>Make focus indicators visible with at least 3:1 contrast against the adjacent background; never rely on browser defaults which often fail this threshold.</statement>
      <rationale source="WCAG 2.2 SC 2.4.11">WCAG 2.2 SC 2.4.11 (Focus Appearance, Minimum) requires a visible focus ring. Browser defaults (thin dotted outline) are often invisible on colored backgrounds.</rationale>
    </rule>

    <rule id="semantic-handoff-annotations" testable="true" depth="essential">
      <statement>Specify heading level (H1-H6), landmark roles (nav, main, header, footer), and ARIA labels in design handoff annotations; do not leave semantic decisions to developer interpretation.</statement>
      <rationale source="folded from 01-rules.xml">Developers recreate heading hierarchies inconsistently without explicit annotation. Screen reader users navigate by headings; a flat or misordered hierarchy breaks the primary navigation pattern.</rationale>
    </rule>

    <rule id="reduced-motion-baked" testable="true" depth="essential">
      <statement>Every motion token MUST have a `prefers-reduced-motion` variant declared; raw motion-only definitions are forbidden.</statement>
      <rationale source="WCAG 2.2 SC 2.3.3">Vestibular safety + SC 2.3.3; baking the variant into the token prevents per-component oversight.</rationale>
    </rule>

    <rule id="pause-controls-for-auto-animation" testable="true" depth="essential">
      <statement>Include pause/stop controls for all auto-playing animations longer than 5 seconds; respect prefers-reduced-motion at the CSS level.</statement>
      <rationale source="WCAG 2.2 SC 2.2.2 + 2.3.3">WCAG 2.2 SC 2.2.2 (Pause, Stop, Hide) and 2.3.3 (Animation from Interactions) protect users with vestibular disorders and cognitive disabilities from motion that prevents reading or causes nausea.</rationale>
    </rule>

    <rule id="locale-aware-direction" testable="true" depth="essential">
      <statement>Spacing/positioning tokens MUST be expressed as logical properties (inline-start/end) when the system targets RTL locales.</statement>
      <rationale source="CSS Logical Properties spec">left/right tokens break under dir=rtl; logical properties prevent RTL regressions.</rationale>
    </rule>

    <rule id="no-automated-fix-without-designer-review" testable="true" depth="essential">
      <statement>Never apply automated contrast or target-size fixes without designer review; fixes often break visual hierarchy (e.g., darkening a brand color to pass AA).</statement>
      <rationale source="folded from 02-agent-patterns.xml">Automated fixes optimize for the accessibility metric, not for design intent. A darkened brand primary may pass contrast but invalidate the brand. Fixes must be flagged for designer decision, not silently applied.</rationale>
    </rule>

    <rule id="shift-left-kpi" testable="true" depth="essential">
      <statement>Track accessibility issues found at design stage versus handoff stage versus production; the ratio of design-stage finds to later-stage finds is the shift-left KPI.</statement>
      <rationale source="folded from 02-agent-patterns.xml">If most finds are in production rather than design, the design-phase process is not working. The KPI makes the shift-left investment visible and improvable.</rationale>
    </rule>

    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>If the Skip-If preconditions hold, the methodology MUST be skipped and the caller routed to the linked alternative.</statement>
      <rationale source="self">Applying this machinery to the wrong workflow burns tokens without producing a usable artefact.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/04-procedure.xml"/>
    <link path="content/05-examples.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
