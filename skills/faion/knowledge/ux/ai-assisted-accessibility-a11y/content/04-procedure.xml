<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="AI-Assisted Accessibility Procedure" version="1.1.1" est_tokens="600" depth="essential">
  <summary>7-step procedure to apply the ai-assisted-accessibility methodology end-to-end. Decision-gates surface stops when inputs do not pass.</summary>

  <section title="Procedure">

    <step n="1" name="scan-axe-pa11y">
      <input>Target pages</input>
      <action>Run axe + pa11y; produce JSON reports per tool.</action>
      <output>Two scan artifacts.</output>
    </step>
    <step n="2" name="haiku-filter-rank">
      <input>Scan artifacts</input>
      <action>Haiku subagent dedupes + ranks by impact + filters known false positives.</action>
      <output>templates/prompt-triage-issues.txt produces ranked list.</output>
    </step>
    <step n="3" name="sonnet-propose-fixes">
      <input>Ranked list</input>
      <action>Sonnet subagent generates code fixes with WCAG citations + file:line refs.</action>
      <output>templates/prompt-generate-fix.txt outputs.</output>
    </step>
    <step n="4" name="expert-validates">
      <input>Sonnet fix proposals</input>
      <action>Human a11y expert reviews each fix; approves / rejects / amends.</action>
      <output>Validated fix list.</output>
      <decision-gate>If approval rate &lt; 50%, retune prompts before continuing.</decision-gate>
    </step>
    <step n="5" name="create-tickets">
      <input>Validated fixes</input>
      <action>Open dev tickets carrying WCAG citation + file:line; throttle bulk alt-text per rule.</action>
      <output>Tickets in dev backlog.</output>
    </step>
    <step n="6" name="user-testing">
      <input>Deployed fixes</input>
      <action>Test with people with disabilities using real assistive technology; validate the assumptions the AI filter and fix generator made.</action>
      <output>User-validated fixes; new findings fed back to step 1.</output>
      <decision-gate>If a fix fails with real AT users, reopen the ticket; do not close on scanner green alone.</decision-gate>
    </step>
    <step n="7" name="continuous-monitoring">
      <input>CI/CD pipeline</input>
      <action>Run the scan on every deploy (templates/ci-a11y-gate.sh); track trends over time; alert on new critical issues.</action>
      <output>Per-deploy scan gate + trend dashboard.</output>
    </step>

  </section>
</text>
