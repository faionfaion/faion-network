<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Flexibility and Efficiency of Use Core Rules" version="1.0.0" est_tokens="800" depth="essential">
  <summary>Testable rules grounding Flexibility and Efficiency of Use. Each pairs a binary check with a sourced rationale; HARD rules — user-supplied input cannot override.</summary>

  <section title="Rule set">

    <rule id="r1-accelerator-per-frequent-task" testable="true" depth="essential">
      <statement>Every workflow performed ≥10× per week per persona has a documented accelerator (shortcut, bulk op, template).</statement>
      <rationale source="Nielsen Heuristic #7">Every workflow performed ≥10× per week per persona has a documented accelerator (shortcut, bulk op, template).</rationale>
    </rule>

    <rule id="r2-multiple-paths" testable="true" depth="essential">
      <statement>Critical goals have ≥2 reachable paths (e.g., mouse + keyboard).</statement>
      <rationale source="Accessibility practice">Critical goals have ≥2 reachable paths (e.g., mouse + keyboard).</rationale>
    </rule>

    <rule id="r3-progressive-disclosure" testable="true" depth="essential">
      <statement>Advanced controls are hidden by default and reachable in ≤2 clicks.</statement>
      <rationale source="Information architecture">Advanced controls are hidden by default and reachable in ≤2 clicks.</rationale>
    </rule>

    <rule id="r4-customisation-persisted" testable="true" depth="essential">
      <statement>User customisations (layout, defaults, shortcut overrides) persist across sessions.</statement>
      <rationale source="Practitioner experience">User customisations (layout, defaults, shortcut overrides) persist across sessions.</rationale>
    </rule>

    <rule id="r5-no-novice-tax" testable="true" depth="essential">
      <statement>Accelerators must not make the novice path slower or harder.</statement>
      <rationale source="Heuristic balance">Accelerators must not make the novice path slower or harder.</rationale>
    </rule>

    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>If preconditions in AGENTS.md &quot;Applies If&quot; do not all hold, OR any &quot;Skip If&quot; disqualifier is true, do not run this methodology — route to the named alternative.</statement>
      <rationale source="faion-network methodology DoD (F-066)">Methodologies are pre-condition gated; running them out-of-scope produces noise and burns the agent context budget.</rationale>
    </rule>

    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement>If preconditions hold, execute the procedure in `content/04-procedure.xml` (or the rule set above when complexity is light), emit the artefact specified by `content/02-output-contract.xml`, and validate with `scripts/validate-flexibility-efficiency.py` before returning.</statement>
      <rationale source="faion-network methodology DoD (F-066)">Tying the entry rule to the validator script closes the loop between policy and machine check.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
