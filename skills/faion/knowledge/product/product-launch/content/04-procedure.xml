<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Product Launch Procedure" version="1.0.0" est_tokens="700" depth="essential">
  <summary>Step-by-step procedure to apply Product Launch end-to-end. Each step names its input, action, and output.</summary>

  <section title="Procedure">

    <step n="1" name="position">
      <input>Brief + candidate segments</input>
      <action>Author the one-page positioning: primary segment, the alternative it displaces, the value claim. One segment, one message.</action>
      <output>Positioning doc</output>
      <decision-gate>If more than one segment is named as primary, stop — pick, and file the rest as derivative angles.</decision-gate>
    </step>
    <step n="2" name="pre-launch">
      <input>Positioning + audience + metrics</input>
      <action>Build assets, brief support, arm monitors, draft the per-channel day-of runbook with timestamps, owners and reversal steps.</action>
      <output>Pre-launch checklist signed + runbook drafted</output>
    </step>
    <step n="3" name="dry-run">
      <input>Asset bundle + runbook</input>
      <action>Walk the day-of timeline through with each named owner and verify the core user flows by hand before any flag is flipped.</action>
      <output>Validated runbook</output>
      <decision-gate>A flow that fails here is fixed quietly; the same failure found after the email blast needs a public correction.</decision-gate>
    </step>
    <step n="4" name="launch-day">
      <input>Pre-launch checklist</input>
      <action>Release, announce per-segment cadence, watch monitors live.</action>
      <output>Launch executed + monitor snapshot</output>
    </step>
    <step n="5" name="first-72h">
      <input>Launch monitors</input>
      <action>Stand-by for incident response; respond to feedback queue.</action>
      <output>Incident log + support volume</output>
    </step>
    <step n="6" name="post-launch-review">
      <input>+14d data</input>
      <action>Compare to success thresholds; emit scale / hold / rollback decision.</action>
      <output>Decision record</output>
    </step>

  </section>
</text>
