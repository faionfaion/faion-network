<?xml version="1.0" encoding="UTF-8"?>
<text id="01-core-rules" title="Terraform Modules — Prompts Core Rules" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5 testable rules + 2 routing conclusions (run-the-checklist, skip-this-methodology) grounding the terraform-modules-prompts methodology. Each rule pairs a binary check + sourced rationale.</summary>

  <section title="Rule set">

    <rule id="r1-bound-scope" testable="true" depth="essential">
      <statement>Apply terraform-modules-prompts to a single instance of the triggering activity. Never batch multiple unrelated decisions through one pass.</statement>
      <rationale source="Anti-conflation pattern (PMBOK 7 Tailoring)">Batching turns a 1-decision artefact into a debate club. Each instance gets its own record so the rationale is traceable.</rationale>
    </rule>

    <rule id="r2-typed-input" testable="true" depth="essential">
      <statement>Every input field has a declared type and source. Free-form prose without a source citation is rejected.</statement>
      <rationale source="Contract-first artefacts (anti-hallucination practice)">Untyped inputs let LLMs hallucinate plausible-but-wrong values. Typed + sourced inputs make the output falsifiable.</rationale>
    </rule>

    <rule id="r3-named-owner" testable="true" depth="essential">
      <statement>The output names a single accountable owner (handle, email, or org role). No 'team' or 'we' as owner.</statement>
      <rationale source="RACI / single-throat-to-choke">Plural ownership = no ownership. Future readers need a named human to chase when the artefact is stale.</rationale>
    </rule>

    <rule id="r4-versioned" testable="true" depth="essential">
      <statement>Output carries semver-style version and last_reviewed date. Stale records (&gt;90 days, no review) must be flagged on read.</statement>
      <rationale source="Anti-decay practice (Schema-evolution from Avro / Protobuf)">Methodology outputs decay; treating them as immutable is a defect. Versioning + review-by-date forces refresh cadence.</rationale>
    </rule>

    <rule id="r5-traceable-decision" testable="true" depth="essential">
      <statement>Every decision in the output cites the input artefact(s) that justified it. Untraceable decisions are rejected.</statement>
      <rationale source="Audit-trail discipline (SOC2 CC8.1)">Without traceability, the artefact becomes opinion. Citations make the output reviewable by a future operator with no context.</rationale>
    </rule>

    <rule id="run-the-checklist" testable="true" depth="essential">
      <statement>Routing conclusion — preconditions are satisfied (Applies If: ALL hold, Skip If: NONE holds). Proceed to procedure / output-contract.</statement>
      <rationale source="Decision-tree routing convention">Every methodology decision tree needs an explicit 'go' conclusion that maps back to a rule id.</rationale>
    </rule>

    <rule id="skip-this-methodology" testable="true" depth="essential">
      <statement>Routing conclusion — at least one Skip-If predicate holds, or Applies-If preconditions are not all satisfied. Hand off to the named upstream / fallback methodology or stop.</statement>
      <rationale source="Decision-tree routing convention">Explicit skip prevents partial application that produces low-confidence artefacts.</rationale>
    </rule>

  </section>

  <see-also>
    <link path="content/02-output-contract.xml"/>
    <link path="content/03-failure-modes.xml"/>
    <link path="content/06-decision-tree.xml"/>
  </see-also>
</text>
