# purpose: Skeleton for the config artefact produced by the gcp-org-policies methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-org-policies.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

constraint: example-constraint
level: example-level
enforcement: example-enforcement
exceptions: []  # array
