<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Helm Basics Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="chart_version_not_semver == true">
      <conclusion ref="semver-chart-version" verdict="rewrite version to MAJOR.MINOR.PATCH"/>
    </branch>

    <branch when="secrets_hardcoded == true">
      <conclusion ref="no-static-secrets" verdict="move to values + external secret manager; rotate"/>
    </branch>

    <branch when="deps_present == true &amp;&amp; lockfile_missing == true">
      <conclusion ref="lockfile-committed" verdict="commit Chart.lock"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="values-schema" verdict="ship + lint + apply"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; chart_version_not_semver == true -&gt; secrets_hardcoded == true -> conclusion ref.</example>
</text>
