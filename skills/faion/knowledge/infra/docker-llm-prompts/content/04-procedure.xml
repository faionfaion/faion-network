<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Docker LLM Prompt Templates Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on LLM adoption.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="list-tasks">
      <input>Backlog</input>
      <action>Identify recurring Docker LLM tasks.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="parameterize">
      <input>Each task</input>
      <action>Convert ad-hoc prompts into templates with slots.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="embed-baselines">
      <input>Safety baselines</input>
      <action>Inject non-root / HEALTHCHECK / no-secrets rules.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="publish-test-validate">
      <input>Library</input>
      <action>Tag model + version; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
