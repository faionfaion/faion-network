<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="IaC Module Design Patterns Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="outputs_untyped == true">
      <conclusion ref="typed-outputs" verdict="type + describe every output"/>
    </branch>

    <branch when="examples_missing == true">
      <conclusion ref="examples-required" verdict="add at least one runnable example"/>
    </branch>

    <branch when="rename_proposed_in_minor == true">
      <conclusion ref="deprecation-cycle" verdict="bump major; alias the old name"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="semver-tags" verdict="tag SemVer + publish"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; outputs_untyped == true -&gt; examples_missing == true -> conclusion ref.</example>
</text>
