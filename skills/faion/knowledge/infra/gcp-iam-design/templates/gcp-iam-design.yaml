# purpose: Skeleton for the config artefact produced by the gcp-iam-design methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-iam-design.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

principal: example-principal
role: example-role
scope: example-scope
condition: example-condition
