Generate a declarative Jenkins pipeline for: [DESCRIBE THE APPLICATION AND DEPLOYMENT TARGET]

Application context:
- Language/runtime: [Node.js / Python / Go / Java]
- Build tool: [npm / maven / gradle / make]
- Docker registry: [registry URL or placeholder]
- Kubernetes namespace(s): [staging, production]
- Shared library name: [library name and version, or "none"]

Pipeline requirements:
1. Use declarative syntax with options block: buildDiscarder (keep 10), timeout (30m), disableConcurrentBuilds, timestamps
2. Kubernetes pod agent with containers for: build runtime, docker:dind, kubectl
3. Parallel test stage: unit tests + security scan (npm audit / trivy / snyk)
4. Build and push Docker image with version = BUILD_NUMBER-GIT_COMMIT[:7]
5. Deploy to staging on merge to main (kubectl set image + rollout status)
6. Deploy to production with manual input gate (submitter: "admin,deployers")
7. post block: always cleanWs(), failure slackSend with danger color

Shared library usage (if library provided):
- buildApp(appName, registry, version) for Docker build/push
- deployToK8s(namespace, deployment, image, timeout) for K8s deploy
- notifySlack(channel, status) for notifications
- Pin library version: @Library('name@vX.Y.Z') _

Security:
- Credentials via withCredentials only — no plaintext in sh steps
- kubeconfig via file credential, not env variable with base64

Output as a single Jenkinsfile code block.
