<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="IaC Pull-Request Review Checklist Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema (draft-07) for the `checklist` artefact produced by iac-pr-review-checklist, plus valid / invalid / forbidden examples.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "slug",
    "items",
    "summary"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]+$"
    },
    "summary": {
      "type": "string",
      "minLength": 20,
      "maxLength": 400
    },
    "items": {
      "type": "array",
      "minItems": 5,
      "items": {
        "type": "object",
        "required": [
          "id",
          "rule",
          "must_or_should",
          "evidence"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "rule": {
            "type": "string",
            "minLength": 10
          },
          "must_or_should": {
            "enum": [
              "MUST",
              "SHOULD"
            ]
          },
          "evidence": {
            "type": "string"
          },
          "owner": {
            "type": "string"
          }
        }
      }
    }
  },
  "additionalProperties": false
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "slug": "iac-pr-review-checklist",
  "summary": "Validated iac-pr-review-checklist checklist applied to a single review.",
  "items": [
    {
      "id": "i1",
      "rule": "First rule verified in scope",
      "must_or_should": "MUST",
      "evidence": "PR comment ID 42"
    },
    {
      "id": "i2",
      "rule": "Second rule verified in scope",
      "must_or_should": "MUST",
      "evidence": "plan output line 7"
    },
    {
      "id": "i3",
      "rule": "Third rule verified in scope",
      "must_or_should": "SHOULD",
      "evidence": "monitoring dashboard"
    },
    {
      "id": "i4",
      "rule": "Fourth rule verified in scope",
      "must_or_should": "MUST",
      "evidence": "policy report"
    },
    {
      "id": "i5",
      "rule": "Fifth rule verified in scope",
      "must_or_should": "SHOULD",
      "evidence": "design doc section 3"
    }
  ]
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "slug": "iac-pr-review-checklist"
}
]]></code>
    <note>Missing all required fields beyond `slug`; schema rejects on the first missing required key.</note>
  </example>

  <section title="Forbidden patterns">
    <list>
      <item>Plain prose / markdown output instead of a JSON object</item>
      <item>Placeholder tokens or empty required fields anywhere inside the artefact</item>
      <item>Unbounded arrays whose item schema is missing</item>
    </list>
  </section>

  <reference path="scripts/validate-iac-pr-review-checklist.py">Validator script — exit 0 on pass, 1 on fail.</reference>
</text>
