<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Backstage Developer Portal Deployment Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on service count + ownership.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="model-catalog">
      <input>Service inventory</input>
      <action>Author catalog-info.yaml schema + onboarding policy.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="wire-techdocs">
      <input>Docs layout</input>
      <action>Move docs alongside code; build via CI.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="add-templates-plugins">
      <input>Needs list</input>
      <action>Author software templates; allowlist plugins.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="schedule-upgrades-validate">
      <input>Cadence</input>
      <action>Set Renovate rules; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
