<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Docker Core Operations Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on shipping shape.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="pin-base-image">
      <input>Base policy</input>
      <action>Pick allowlisted base + pin digest.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="layer-and-stage">
      <input>Build/runtime split</input>
      <action>Multi-stage: builder + runtime; ordered for cache.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="harden-user-healthcheck">
      <input>Security baseline</input>
      <action>Add non-root USER + HEALTHCHECK + exec-form CMD.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="dockerignore-validate">
      <input>Context</input>
      <action>Author `.dockerignore`; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
