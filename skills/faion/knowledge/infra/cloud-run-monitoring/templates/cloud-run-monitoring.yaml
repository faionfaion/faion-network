# purpose: Skeleton for the config artefact produced by the cloud-run-monitoring methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-cloud-run-monitoring.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

service_name: example-service_name
log_format: example-log_format
alert_policies: []  # array
trace_propagation: false  # boolean
notification_channels: []  # array
