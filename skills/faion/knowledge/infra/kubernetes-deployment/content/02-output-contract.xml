<?xml version="1.0" encoding="UTF-8"?>
<text id="02-output-contract" title="Kubernetes Deployment Strategies Output Contract" version="1.1.0" est_tokens="900" depth="essential">
  <summary>JSON Schema (draft-07) for the `decision-record` artefact produced by kubernetes-deployment, plus valid / invalid / forbidden examples.</summary>

  <section title="Schema">
    <code lang="json"><![CDATA[
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "slug",
    "title",
    "context",
    "options",
    "decision",
    "consequences"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]+$"
    },
    "title": {
      "type": "string"
    },
    "context": {
      "type": "string",
      "minLength": 30
    },
    "options": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object"
      }
    },
    "decision": {
      "type": "string",
      "minLength": 10
    },
    "consequences": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false
}
]]></code>
  </section>

  <example valid="true">
    <code lang="json"><![CDATA[
{
  "slug": "kubernetes-deployment",
  "title": "Kubernetes Deployment",
  "context": "Selecting commitment strategy for the next 12 months of compute baseline.",
  "options": [
    {
      "id": "o1",
      "name": "Standard RI"
    },
    {
      "id": "o2",
      "name": "Compute Savings Plan"
    }
  ],
  "decision": "Choose Compute Savings Plan at 55% of 5th percentile floor.",
  "consequences": [
    "Locks $X/yr discount",
    "Re-evaluate quarterly"
  ]
}
]]></code>
  </example>

  <example valid="false">
    <code lang="json"><![CDATA[
{
  "slug": "kubernetes-deployment"
}
]]></code>
    <note>Missing all required fields beyond `slug`; schema rejects on the first missing required key.</note>
  </example>

  <section title="Forbidden patterns">
    <list>
      <item>Plain prose / markdown output instead of a JSON object</item>
      <item>Placeholder tokens or empty required fields anywhere inside the artefact</item>
      <item>Unbounded arrays whose item schema is missing</item>
    </list>
  </section>

  <reference path="scripts/validate-kubernetes-deployment.py">Validator script — exit 0 on pass, 1 on fail.</reference>
</text>
