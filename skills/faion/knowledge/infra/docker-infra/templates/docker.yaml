# purpose: Skeleton for the config artefact produced by the docker methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-docker.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

base_image: example-base_image
multi_stage: false  # boolean
user: example-user
healthcheck: example-healthcheck
pinned_digest: false  # boolean
