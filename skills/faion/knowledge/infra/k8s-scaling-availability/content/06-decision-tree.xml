<?xml version="1.0" encoding="UTF-8"?>
<text id="06-decision-tree" title="Kubernetes Scaling and Availability Decision Tree" version="1.1.0" est_tokens="600" depth="essential">
  <summary>Routing tree mapping observable signals to a rule from 01-core-rules.xml. Use when in doubt about which variant to apply.</summary>

  <decision-tree>
    <root-question id="q-preconditions">Do all Applies-If preconditions hold and no Skip-If trigger fires?</root-question>

    <branch when="preconditions == false">
      <conclusion ref="skip-this-methodology" verdict="skip"/>
    </branch>

    <branch when="min_replicas_under_2 == true">
      <conclusion ref="min-max-bounds" verdict="raise minReplicas to at least 2"/>
    </branch>

    <branch when="topology_spread_missing == true">
      <conclusion ref="topology-spread" verdict="add zone topology spread"/>
    </branch>

    <branch when="stabilization_default == true">
      <conclusion ref="stabilization-window" verdict="set stabilizationWindowSeconds at least 300"/>
    </branch>

    <branch when="all_checks_passed == true">
      <conclusion ref="hpa-v2-only" verdict="wire HPA v2 + PDB + commit"/>
    </branch>

  </decision-tree>

  <example trace="happy-path">preconditions == false -&gt; min_replicas_under_2 == true -&gt; topology_spread_missing == true -> conclusion ref.</example>
</text>
