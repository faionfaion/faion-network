Analyze the following AWS Cost Explorer data and provide optimization recommendations:

```
[PASTE AWS COST EXPLORER CSV OR JSON EXPORT HERE]
```

Context:
- Monthly spend: $[AMOUNT]
- Top services by cost: [list top 3-5 services]
- Current commitments: [Savings Plans / Reserved Instances, or none]
- Usage pattern: [steady 24/7 | business hours only | batch/variable]

Analysis requested:

1. Idle resource identification
   - EC2 instances with avg CPU < 5% over 7 days
   - RDS instances with avg CPU < 5% and connections < 10
   - Unattached EBS volumes and unused Elastic IPs

2. Rightsizing recommendations
   - Over-provisioned instances (CPU < 20% or memory < 30% avg)
   - Recommended instance type changes with estimated savings

3. Commitment analysis
   - Identify baseline steady-state spend eligible for Savings Plans
   - Calculate 1-year Compute Savings Plan break-even and savings
   - Identify workloads suitable for Reserved Instances vs Spot

4. Storage optimization
   - S3 buckets not using lifecycle policies
   - EBS volumes with gp2 that should migrate to gp3 (20% cheaper)
   - Data transfer costs and potential reduction strategies

5. Action plan
   - Prioritize by: (savings amount) / (implementation effort)
   - Provide monthly savings estimate per recommendation
   - Tag any resources missing required tags (Environment, Owner, Project)

Output as a structured report with a summary table of estimated monthly savings.
