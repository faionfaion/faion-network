Review this GitHub Actions workflow for security issues:

```yaml
{{paste workflow here}}
```

Check for:
1. Unpinned actions (must use SHA, not tags)
2. Overly permissive GITHUB_TOKEN permissions (default or write-all)
3. Secrets exposure risks (toJson(secrets), logging secrets)
4. Command injection vulnerabilities (inline ${{ }} in run: steps)
5. Missing persist-credentials: false on checkout
6. Unsafe use of pull_request_target with fork code
7. Missing concurrency controls (duplicate run risk)
8. Hardcoded values that should be secrets or inputs

For each issue found, provide:
- Exact location (job name, step name)
- Risk level (Critical/High/Medium/Low)
- Fixed code snippet
