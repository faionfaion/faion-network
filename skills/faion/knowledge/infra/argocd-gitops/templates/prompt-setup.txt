You are an ArgoCD GitOps architect. Design a complete GitOps setup for the system described below.

Context:
- System description: {{SYSTEM_DESCRIPTION}}
- Number of services/apps: {{APP_COUNT}}
- Environments: {{ENVIRONMENTS}} (e.g., dev, staging, production)
- Number of clusters: {{CLUSTER_COUNT}}
- Current deployment method: {{CURRENT_DEPLOY_METHOD}}
- Templating: {{HELM_OR_KUSTOMIZE}}
- Team size: {{TEAM_SIZE}}

Design the following:

1. GIT REPOSITORY STRUCTURE
   Recommend mono-repo vs poly-repo strategy with justification.
   If mono-repo, show the directory structure for {{APP_COUNT}} apps across {{ENVIRONMENTS}} environments.
   Key rule: use folders for environments, NOT branches.

2. APPLICATION HIERARCHY
   Map the App of Apps pattern for this setup:
   - Root Application (in argocd namespace)
   - AppProject per environment
   - Individual Applications per service
   Show the parent-child relationship.
   If >5 apps: recommend ApplicationSet with generator type (Git directory, List, or Matrix).

3. SYNC STRATEGY
   For each environment recommend:
   - Automated vs manual sync (justify based on environment risk)
   - selfHeal setting
   - prune setting
   - syncWindows if production
   - Branch/tag pinning strategy (HEAD for dev, immutable tags for production)

4. RBAC DESIGN
   Map teams to AppProject roles:
   - Who can sync production?
   - Who can sync staging?
   - Who is read-only?
   - How are break-glass escalations handled?

5. SECRET MANAGEMENT INTEGRATION
   How do secrets reach ArgoCD-managed apps?
   - ESO (External Secrets Operator) — recommended for K8s
   - Sealed Secrets — if no secret manager available
   - Vault Agent Injector — if HashiCorp Vault in use
   Provide the integration pattern for the chosen approach.

6. NOTIFICATION SETUP
   ArgoCD Notifications config for:
   - Sync failure → Slack/PagerDuty
   - Health degraded → alert
   - Successful production sync → audit log
   Show the trigger and template format.

7. MIGRATION PLAN
   From {{CURRENT_DEPLOY_METHOD}} to GitOps:
   - Step 1: Install ArgoCD, configure SSO
   - Step 2: Create AppProjects and seed Applications in dry-run mode
   - Step 3: Parallel run (old + ArgoCD both managing)
   - Step 4: Cut over (disable old deployment method)
   Identify risks at each step.

Output as structured sections with concrete YAML snippets where relevant.
