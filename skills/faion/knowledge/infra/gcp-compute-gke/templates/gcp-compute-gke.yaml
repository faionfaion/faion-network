# purpose: Skeleton for the config artefact produced by the gcp-compute-gke methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-compute-gke.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

name: example-name
mode: example-mode
region: example-region
node_pools: []  # array
workload_identity: false  # boolean
private_nodes: false  # boolean
