<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Terraform (Advanced) Procedure" version="1.1.1" est_tokens="800" depth="essential">
  <summary>7-step procedure to apply the methodology end-to-end. Each step has input, action, output, and an optional decision-gate.</summary>

  <section title="Procedure">

    <step n="1" name="gather-typed-inputs">
      <input>Trigger context (ticket, transcript, dashboard) + team roster</input>
      <action>Populate the Prerequisites table from Section above. Reject any free-form prose without a named source.</action>
      <output>Typed inputs object (name + source per row)</output>
      <decision-gate>If any required input is missing OR has no source, STOP and request from upstream.</decision-gate>
    </step>

    <step n="2" name="check-applicability">
      <input>Typed inputs from step 1</input>
      <action>Walk `content/06-decision-tree.xml` starting from root-question. Record which branch was taken at each node.</action>
      <output>Conclusion id (run-the-checklist OR skip-this-methodology OR ...)</output>
      <decision-gate>If conclusion == skip-this-methodology, STOP and route to the named fallback.</decision-gate>
    </step>

    <step n="3" name="produce-artefact">
      <input>Typed inputs + conclusion = run-the-checklist</input>
      <action>Fill the template at `templates/`. Anchor every non-trivial value to a verbatim citation (trace_refs).</action>
      <output>Draft artefact matching the `produces` shape</output>
    </step>

    <step n="4" name="validate-contract">
      <input>Draft artefact</input>
      <action>Run `scripts/validate-&lt;slug&gt;.py &lt;artefact-path&gt;`. Address every reported violation.</action>
      <output>Valid artefact (exit 0)</output>
      <decision-gate>If the script exits 1, fix the violations and re-run. Do not bypass.</decision-gate>
    </step>

    <step n="5" name="register-and-handoff">
      <input>Valid artefact</input>
      <action>Commit / publish to the named storage location. Notify the named owner. Schedule next review (&lt;=90 days).</action>
      <output>Registered artefact + scheduled review</output>
    </step>

    <step n="6" name="pre-deployment-checklist">
      <input>Terraform project + pipeline definition</input>
      <action>Walk the checklist rules ck-state-architecture, ck-blast-radius-isolation, ck-pipeline-stages, ck-ci-auth-and-audit, ck-security-hardening and ck-pre-prod-apply from `content/01-core-rules.xml`; record each as pass/fail.</action>
      <output>Checklist with every item marked pass</output>
      <decision-gate>Any failed item blocks the prod apply until fixed.</decision-gate>
    </step>

    <step n="7" name="post-apply-verification">
      <input>Completed prod apply</input>
      <action>Apply ck-post-prod-apply from `content/01-core-rules.xml`: health checks, smoke tests, alert verification, and a second clean plan.</action>
      <output>Clean second plan (no drift) + green health checks</output>
      <decision-gate>If the second plan shows drift, investigate the out-of-band change before closing the change ticket.</decision-gate>
    </step>

  </section>
</text>
