# purpose: Skeleton for the config artefact produced by the gcp-cloud-run-serverless methodology.
# consumes: see content/02-output-contract.xml.
# produces: a valid config that passes scripts/validate-gcp-cloud-run-serverless.py.
# depends-on: content/01-core-rules.xml + content/04-procedure.xml.
# token-budget-impact: ~200-600 tokens when loaded as context.

name: example-name
image: example-image
min_instances: 0  # int|number|string
max_instances: 0  # int|number|string
concurrency: 0  # int|number|string
cpu: example-cpu
memory: 0  # int|number|string
