<?xml version="1.0" encoding="UTF-8"?>
<text id="04-procedure" title="Docker Image Optimization Procedure" version="1.1.0" est_tokens="900" depth="essential">
  <summary>5-step procedure to apply the methodology end-to-end with decision gates.</summary>

  <section title="Procedure">

    <step n="1" name="decide-apply-vs-skip">
      <input>Inputs</input>
      <action>Decision tree on SLO + ownership.</action>
      <output>step 1 output</output>
      <decision-gate>If decision != apply, STOP and document `skip-this-methodology` rationale.</decision-gate>
    </step>
    <step n="2" name="measure-baseline">
      <input>`docker history` / Dive</input>
      <action>Capture before-state size + layer breakdown.</action>
      <output>step 2 output</output>
    </step>
    <step n="3" name="pick-base">
      <input>Language</input>
      <action>Choose distroless / slim / alpine with justification.</action>
      <output>step 3 output</output>
    </step>
    <step n="4" name="wire-cache-mounts">
      <input>BuildKit</input>
      <action>Add cache mounts for dep installs.</action>
      <output>step 4 output</output>
    </step>
    <step n="5" name="record-validate">
      <input>Plan</input>
      <action>Document target size + CI budget; run validator; commit.</action>
      <output>step 5 output</output>
      <decision-gate>If validator exits non-zero, return to the previous draft step and fix violations.</decision-gate>
    </step>

  </section>
</text>
