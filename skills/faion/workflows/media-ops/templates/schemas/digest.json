{
  "type": "object",
  "required": ["intro", "items", "outro"],
  "properties": {
    "intro": {"type": "string"},
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["emoji", "title", "slug"],
        "properties": {
          "emoji": {"type": "string"},
          "title": {"type": "string"},
          "slug": {"type": "string"}
        }
      }
    },
    "outro": {"type": "string"}
  }
}
