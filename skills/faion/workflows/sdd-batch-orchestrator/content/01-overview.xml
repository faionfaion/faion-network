<?xml version="1.0" encoding="UTF-8"?>
<text id="01-overview" title="Overview and core principle">
  <summary>The orchestrator coordinates phase-specific subagents by handing each one a path to a versioned prompt file plus a small parameter block, instead of re-typing instructions inline.</summary>

  <section title="Core principle">
    <rule>
      <statement>The orchestrator MUST NOT inline long instructions into the `Agent` tool's `prompt` argument. It dispatches the subagent with a short message that points at a file under `prompts/` and supplies a small key-value parameter block.</statement>
      <rationale>Behaviour drifts when the same instructions are paraphrased every run. Versioned prompt files make each phase a deterministic capability and let prompts be reviewed, diffed in git, and improved over time.</rationale>
    </rule>

    <rule>
      <statement>Each subagent prompt file MUST be self-contained — it briefs the subagent like a colleague who has not seen the parent conversation.</statement>
      <rationale>Subagents spawned via the `Agent` tool do not inherit the parent's context window. A prompt that assumes shared state silently degrades into vague instructions.</rationale>
    </rule>
  </section>

  <section title="Role split">
    <p>Three roles, and they are not interchangeable. The orchestrator composes, the coordinator checks, the executors work.</p>
    <list>
      <item><b>Orchestrator</b> — the main thread. Decides which phase runs next, decides what can run in parallel, confirms sensitive actions (push, deploy, destructive ops) with the user, gates on quota. It writes no code, reviews no diff, resolves no conflict. Its approach is fixed by `templates/orchestrator-approach.md`, read at batch start; it is the one role with no dispatchable prompt file, because it is the thing doing the dispatching.</item>
      <item><b>Coordinator</b> — an independent subagent that runs at every wave boundary: after wave N merges, before wave N+1 dispatches. It answers one question — is the merge point sound enough to build the next wave on — and returns CLEAR / HOLD / ABORT. It is a checker, never a fixer. See `content/11-coordinator.xml`.</item>
      <item><b>Executors</b> — phase subagents, one prompt file each, one verb and one verdict each.</item>
      <item>Reference docs: cross-cutting knowledge (git conventions, screenshot strategy, delivery channel auth).</item>
      <item>Playbooks: per-delivery-surface specializations (faion-net-be, faion-net-fe, knowledge, storybook, mediamanager-fe, ETL).</item>
    </list>
    <rule>
      <statement>The orchestrator MUST NOT perform the coordinator's checks itself, even when only one wave is planned.</statement>
      <rationale>The orchestrator holds the executors' reports in its context and decided the wave grouping being checked. Checking its own grouping against reports it already believes is not a check. The same independence argument that separates REVIEW from EXECUTE separates the coordinator from the orchestrator — and it is the orchestrator, not the executor, whose error the coordinator is most likely to catch, because a wrong wave grouping is an orchestrator decision.</rationale>
    </rule>
  </section>

  <section title="Tracker shape (faion-network)">
    <p>The pattern uses the project's `.aidocs/` SDD lifecycle as the source of truth. Each "ticket" is a feature folder whose status is its location in the lifecycle:</p>
    <list>
      <item>`.aidocs/&lt;project&gt;/backlog/&lt;feature-id&gt;/` — accepted but not started.</item>
      <item>`.aidocs/&lt;project&gt;/todo/&lt;feature-id&gt;/` — ready for planning / execution.</item>
      <item>`.aidocs/&lt;project&gt;/in-progress/&lt;feature-id&gt;/` — currently being worked.</item>
      <item>`.aidocs/&lt;project&gt;/done/&lt;feature-id&gt;/` — merged locally, awaiting push or already pushed.</item>
    </list>
    <p>Each feature folder contains the SDD artifact set: `spec.md`, `plan.md` (merged `## Design` + `## Execution Plan` per plan-md-structure), conditional `user-flows.md` (user-facing) and `ui-ux-design.md` (UI), atomic `TASK_*.md` files, `readiness.md` (gate before move to done/), and a `clarifications.md` produced by the CLARIFY phase. The orchestrator never hard-codes a vendor tracker.</p>
    <reference path="../../../../../docs/directory-structure.md">Repo-wide layout including the SDD lifecycle folders.</reference>
  </section>

  <section title="Language convention">
    <rule>
      <statement>User-facing strings (clarification questions, delivery captions, AskUserQuestion payloads) use Ukrainian. Code, commit messages, prompt files, reference docs, and CHANGELOG entries use English.</statement>
      <rationale>Mixed-language code is hard to grep; mixed-language conversation is impolite. The split is consistent across runs because it is encoded in the prompt files.</rationale>
    </rule>
  </section>
</text>
