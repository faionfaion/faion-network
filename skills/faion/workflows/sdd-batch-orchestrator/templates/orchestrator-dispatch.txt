Read <abs-path-to-prompt-file> and execute it with these inputs:

  feature_folder:  <abs path to .aidocs/<project>/in-progress/<feature-id>/>
  repo_path:       <abs path to the target repo>
  default_branch:  main | master
  worktree_root:   /tmp/wt-<feature-id>
  merge_lock:      /tmp/<repo-slug>-merge.lock
  <other-param>:   <value or omit>

Return the report shape declared in the prompt file's `Output` section.
The last line of your final message MUST match the marker the prompt declares,
e.g. `done=<feature_id> commit=<short-sha>` or `verdict=PASS|FAIL-WITH-NITS|FAIL`.

Constraints (inherited, do not deviate):
  - No `git push` unless this input block contains `push: true`.
  - No `--no-verify`, no `--amend`, no force push, no `git reset --hard`.
  - Commit title ≤ 50 chars, format `<type>: <short>`. No `Co-Authored-By`. No emojis.
  - Append a `CHANGELOG.md` entry under `## [Unreleased]` per commit.
  - User-facing strings in Ukrainian; everything else in English.
