# Pro / Backend Systems Playbooks

PostgreSQL scaling, connection pooling, caching patterns, and data architecture for agency backend teams. Citation scope: `knowledge/free/ + solo/ + pro/`.

## Playbooks

| Slug | Goal |
|------|------|
| `db-scaling-read-replicas` | PgBouncer pooling + read replicas + materialized views + tenant sharding decision tree |

## Authoring

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
