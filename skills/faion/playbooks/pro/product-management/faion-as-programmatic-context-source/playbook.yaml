# =============================================================================
# faion playbook manifest — P7 llm-agent-developer
# Generated for F-064 from playbook-index.json entry #18
# =============================================================================

slug: faion-as-programmatic-context-source
title: "Make faion a programmatic context source for an agent"
version: "0.1.0"
status: draft
last_reviewed: 2026-05-20
maintainers: ["faion"]
content_id: 8f7f1c1e30906cfc

tier_min: pro
complexity: deep
context: [team]
persona_hints: [P7]

intent: |
  Agent builder wires faion CLI as structured methodology lookup → no markdown parsing, emits faion-citation tokens.

scope: |
  Agent builder wires faion CLI as a structured methodology lookup inside their agent (RAG-as-a-service / SDR / code-review bot) without parsing human-readable markdown, and emits faion-citation tokens in agent output.

non_goals:
  - "Building a public faion API wrapper SDK — out of scope"
  - "End-user-facing search UI"

prerequisites:
  - "Faion CLI installed and authenticated"
  - "Agent with tool-call support"

success_criteria:
  - "Faion exposed as a tool the agent can call"
  - "Agent receives JSON, not markdown"
  - "Citation tokens emitted with version + content_id"
  - "Eval row asserting citation contract"

stages:
  - name: "Tool Wrap"
    intent: "Wrap faion CLI as an agent tool with strict schema."
    tasks:
      - "Discriminated-union output for tool result"
      - "Strict required fields"
      - "Structured errors"
    methodologies:
      - slug: discriminated-union-output
        path: geek/ai/ai-agents/discriminated-union-output
        required: true
        tier_gate: geek
      - slug: strict-mode-required-fields
        path: geek/ai/ai-agents/strict-mode-required-fields
        required: true
        tier_gate: geek
      - slug: structured-tool-errors
        path: geek/ai/ai-agents/structured-tool-errors
        required: true
        tier_gate: geek
      - slug: schema-version-pinning
        path: geek/ai/ai-agents/schema-version-pinning
        required: true
        tier_gate: geek
    outputs:
      - "Tool wrapper module"
    decision_gate: |
      Advance only when contract tests pass on the wrapper.
  - name: "Context Hygiene"
    intent: "Manifest-then-fetch + progressive disclosure to keep context lean."
    tasks:
      - "Manifest-then-fetch pattern"
      - "Progressive disclosure of skills"
      - "Auto-evict tool results"
      - "Prompt-cache prefix order"
    methodologies:
      - slug: manifest-then-fetch
        path: geek/ai/ai-agents/manifest-then-fetch
        required: true
        tier_gate: geek
      - slug: progressive-disclosure-skills
        path: geek/ai/ai-agents/progressive-disclosure-skills
        required: true
        tier_gate: geek
      - slug: auto-evict-tool-results
        path: geek/ai/ai-agents/auto-evict-tool-results
        required: true
        tier_gate: geek
      - slug: prompt-cache-prefix-order
        path: geek/ai/ai-agents/prompt-cache-prefix-order
        required: true
        tier_gate: geek
    outputs:
      - "Context-pruning rules"
    decision_gate: |
      Advance only when context budget holds on the longest trace.
  - name: "Citation Contract"
    intent: "Emit faion citation tokens with content_id + version."
    tasks:
      - "Define citation token schema"
      - "Wire emission in agent output"
      - "Eval that asserts citations are present + traceable"
    methodologies:
      []
    outputs:
      - "Citation schema doc"
      - "Eval row"
    decision_gate: |
      Ship only when eval asserts every grounded answer has a citation.

common_pitfalls:
  - "Treating eval scores as ground truth without judge calibration"
  - "Shipping prompt or model changes without a regression gate"

quality_checklist:
  - "Can I roll back this change in one step?"
  - "Is the regression eval committed BEFORE the fix?"

related_playbooks:
  - eval-harness-continuous-benchmark-suite
  - agent-observability-drift-detection-rollout

token_budget_estimate: 18000
cli_invocations:
  human:  "faion get-content faion-as-programmatic-context-source --format md"
  agent:  "faion get-content faion-as-programmatic-context-source --format context"
  json:   "faion get-content faion-as-programmatic-context-source --format json"

changelog:
  - version: "0.1.0"
    date: 2026-05-20
    note: "Initial draft for F-064 P7 llm-agent-developer persona"

gaps:
  - methodology_slug: methodology-as-json-feed
    expected_tier: pro
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: methodology-as-json-feed required by playbook chain"
  - methodology_slug: faion-cli-agent-adapter-pattern
    expected_tier: pro
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: faion-cli-agent-adapter-pattern required by playbook chain"
  - methodology_slug: citation-contract-back-to-source
    expected_tier: pro
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: citation-contract-back-to-source required by playbook chain"
  - methodology_slug: methodology-versioning-and-changelog
    expected_tier: pro
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: methodology-versioning-and-changelog required by playbook chain"
