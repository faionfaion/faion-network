# Pro / HR Ops

Recruiting funnel + structured interview process for growing teams. Citation scope: `knowledge/free/ + solo/ + pro/`.

| Slug | Goal |
|------|------|
| `hiring-funnel` | Full ATS-style funnel from sourced→signed with per-stage conversion |
| `technical-interview-process` | Structured 4-stage interview loop with scorecards + calibration |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
