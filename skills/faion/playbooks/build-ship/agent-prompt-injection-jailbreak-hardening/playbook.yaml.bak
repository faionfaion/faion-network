# =============================================================================
# faion playbook manifest — P7 llm-agent-developer
# Generated for F-064 from playbook-index.json entry #19
# =============================================================================

slug: agent-prompt-injection-jailbreak-hardening
title: "Harden an agent against prompt injection and jailbreak across tool boundaries"
version: "0.1.0"
status: draft
last_reviewed: 2026-05-20
maintainers: ["faion"]
content_id: f5aeb811daeacdc7

tier_min: geek
complexity: deep
context: [team]
persona_hints: [P7]

intent: |
  Agent with file-read / web-fetch / MCP tools → survives indirect-prompt-injection red-team without exfiltrating data, escalating permissions, or breaking refusal policy.

scope: |
  Agent with file-read / web-fetch / MCP tools survives an indirect-prompt-injection red-team suite without exfiltrating data, escalating permissions, or violating refusal policy.

non_goals:
  - "Building a security product — out of scope"
  - "Full red-team consulting engagement"

prerequisites:
  - "Agent with tools that ingest external content"
  - "Eval harness reachable"

success_criteria:
  - "Indirect-injection eval suite committed"
  - "Trust boundary documented per tool"
  - "Refusal policy holds across boundary"
  - "Canary exfiltration tokens deployed"
  - "Adversarial eval gates merges"

stages:
  - name: "Boundary Model"
    intent: "Document trust boundaries per tool."
    tasks:
      - "Bundle-vs-split tools with trust in mind"
      - "Idempotent writes with auth checks"
      - "Cheap guardrail tripwires on ingested content"
    methodologies:
      - slug: bundle-vs-split-tools
        path: geek/ai/ai-agents/bundle-vs-split-tools
        required: true
        tier_gate: geek
      - slug: idempotent-write-tools
        path: geek/ai/ai-agents/idempotent-write-tools
        required: true
        tier_gate: geek
      - slug: cheap-guardrail-tripwire
        path: geek/ai/ai-agents/cheap-guardrail-tripwire
        required: true
        tier_gate: geek
    outputs:
      - "Trust boundary table"
    decision_gate: |
      Advance only when every tool has a documented boundary.
  - name: "Refusal & Output"
    intent: "Strict refusal field + discriminated union."
    tasks:
      - "Strict refusal schema"
      - "Discriminated union output"
    methodologies:
      - slug: refusal-field-strict-schema
        path: geek/ai/ai-agents/refusal-field-strict-schema
        required: true
        tier_gate: geek
      - slug: discriminated-union-output
        path: geek/ai/ai-agents/discriminated-union-output
        required: true
        tier_gate: geek
    outputs:
      - "Hardened output schema"
    decision_gate: |
      Advance only when schema enforces refusal explicitly.
  - name: "Red-Team"
    intent: "Run injection + jailbreak suite via chaos eval."
    tasks:
      - "Chaos-eval with injection payloads"
      - "Wire canary tokens for exfil detection"
    methodologies:
      - slug: chaos-eval-fault-injection
        path: geek/ai/ai-agents/chaos-eval-fault-injection
        required: true
        tier_gate: geek
    outputs:
      - "Red-team eval results"
    decision_gate: |
      Advance only when suite catches at least one planted attack.
  - name: "Gate"
    intent: "Make adversarial eval block merges."
    tasks:
      - "Wire CI to fail on suite regression"
      - "Document rollback for guardrails"
    methodologies:
      []
    outputs:
      - "CI gate active"
    decision_gate: |
      Ship only after a known-bad PR is correctly blocked.

common_pitfalls:
  - "Treating eval scores as ground truth without judge calibration"
  - "Shipping prompt or model changes without a regression gate"

quality_checklist:
  - "Can I roll back this change in one step?"
  - "Is the regression eval committed BEFORE the fix?"

related_playbooks:
  - eval-harness-continuous-benchmark-suite
  - agent-observability-drift-detection-rollout

token_budget_estimate: 18000
cli_invocations:
  human:  "faion get-content agent-prompt-injection-jailbreak-hardening --format md"
  agent:  "faion get-content agent-prompt-injection-jailbreak-hardening --format context"
  json:   "faion get-content agent-prompt-injection-jailbreak-hardening --format json"

changelog:
  - version: "0.1.0"
    date: 2026-05-20
    note: "Initial draft for F-064 P7 llm-agent-developer persona"

gaps:
  - methodology_slug: indirect-prompt-injection-defense
    expected_tier: geek
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: indirect-prompt-injection-defense required by playbook chain"
  - methodology_slug: tool-trust-boundary-model
    expected_tier: geek
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: tool-trust-boundary-model required by playbook chain"
  - methodology_slug: jailbreak-eval-suite-bootstrap
    expected_tier: geek
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: jailbreak-eval-suite-bootstrap required by playbook chain"
  - methodology_slug: data-exfiltration-canary-tokens
    expected_tier: geek
    blocks_stage: 1
    note: "Gap from brainstorm-2026-05-17: data-exfiltration-canary-tokens required by playbook chain"
