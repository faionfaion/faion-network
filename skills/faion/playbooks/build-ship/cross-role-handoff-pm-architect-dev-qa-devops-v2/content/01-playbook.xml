<?xml version="1.0" encoding="UTF-8"?>
<playbook id="01-playbook" slug="cross-role-handoff-pm-architect-dev-qa-devops-v2" goal="build-ship" complexity="deep">
  <intent>A feature has to cross product, architecture, development, QA and operations, and every crossing so far has meant re-explaining the same context or discovering a blocker three roles too late. It is over once the feature has launched, every crossing has been picked apart in a retro, and the handoff template sits at a new locked version carrying whatever those retros exposed.</intent>
  <scope>A feature moves through 5 roles with a single living artifact (spec + design + test-plan + runbook). Each role inherits prior context and emits a delta. Handoffs blocked if Definition-of-Ready not met</scope>
  <success-criteria>
    <criterion>The product write-up names the problem, the audience, the success signal and the non-goals, the architect has signed it, and each ambiguity was resolved in the log instead of in a corridor</criterion>
    <criterion>The design covers API, data, components and trade-offs, and the developer who must build it has confirmed in writing that they can, with every blocking comment answered</criterion>
    <criterion>The QA test plan traces back to each acceptance criterion, and DevOps has reviewed capacity, monitoring and alerts after a rollback drill that ran clean before anything reached production</criterion>
    <criterion>Post-launch retro notes exist for each role-to-role crossing, and the handoff template has been reissued at a new locked version carrying the fixes those notes demanded</criterion>
  </success-criteria>
  <stages>
    <stage n="1" name="PM Handoff">
      <intent>Outcome doc, not a Jira link dump.</intent>
      <tasks>
        <task>PM writes the problem, audience, success signal, non-goals</task>
        <task>Architect reviews and signs off on the doc</task>
        <task>Anything ambiguous goes back to PM until clear</task>
      </tasks>
      <methodologies>
        <ref slug="code-review" />
      </methodologies>
      <outputs>
        <output>PM outcome doc</output>
        <output>architect signoff</output>
        <output>ambiguity-resolution log</output>
      </outputs>
      <decision-gate>Advance only when architect signs the doc.</decision-gate>
    </stage>
    <stage n="2" name="Architect Handoff">
      <intent>Design doc the dev can build from.</intent>
      <tasks>
        <task>Architect writes the design: API, data, components, tradeoffs</task>
        <task>Dev reviews; flags anything they can't build</task>
        <task>Address every blocking comment in writing</task>
      </tasks>
      <methodologies>
        <ref slug="inc-runbook-as-markdown-tagged-steps" />
      </methodologies>
      <outputs>
        <output>design doc</output>
        <output>dev review notes</output>
        <output>blocker resolutions</output>
      </outputs>
      <decision-gate>Advance only when dev confirms they can build it as designed.</decision-gate>
    </stage>
    <stage n="3" name="Dev → QA Handoff">
      <intent>Spec + acceptance + how to test.</intent>
      <tasks>
        <task>Dev hands QA the spec, AC, and test entry-points</task>
        <task>QA writes the test plan from the AC</task>
        <task>Dev unblocks anything that prevents the test plan</task>
      </tasks>
      <methodologies>
        <ref slug="task-spec-kit-three-step" />
      </methodologies>
      <outputs>
        <output>spec + AC delivered</output>
        <output>test plan</output>
        <output>blocker resolutions</output>
      </outputs>
      <decision-gate>Advance only when QA confirms test plan covers every AC.</decision-gate>
    </stage>
    <stage n="4" name="QA → DevOps Handoff">
      <intent>Deploy + observe + rollback agreed.</intent>
      <tasks>
        <task>QA hands DevOps the deploy plan and the rollback drill</task>
        <task>DevOps reviews capacity, monitoring, alerts</task>
        <task>Rollback drill is run before any prod deploy</task>
      </tasks>
      <methodologies>
        <ref slug="test-tdd-red-green-split-agents" />
      </methodologies>
      <outputs>
        <output>deploy + rollback plan</output>
        <output>DevOps review notes</output>
        <output>rollback drill log</output>
      </outputs>
      <decision-gate>Advance only when rollback drill ran clean.</decision-gate>
    </stage>
    <stage n="5" name="Close the Loop">
      <intent>Every role signed at every gate.</intent>
      <tasks>
        <task>After launch, retro the handoff at each gate</task>
        <task>Patch the playbook template with any gap found</task>
        <task>Lock the new template version</task>
      </tasks>
      <methodologies>
        <ref slug="architecture-decision-records" />
        <ref slug="backlog-grooming-roadmapping" />
        <ref slug="writing-design-documents" />
      </methodologies>
      <outputs>
        <output>per-gate retro notes</output>
        <output>playbook diff</output>
        <output>new template version</output>
      </outputs>
      <decision-gate>Required output: a written updated handoff playbook.</decision-gate>
    </stage>
  </stages>
  <gaps>
    <gap slug="definition-of-ready-template" expected-tier="geek" />
    <gap slug="definition-of-done-template" expected-tier="geek" />
    <gap slug="handoff-protocol-template" expected-tier="geek" />
    <gap slug="qa-acceptance-signoff-template" expected-tier="geek" />
    <gap slug="release-train-coordination" expected-tier="geek" />
    <gap slug="on-call-handoff-protocol" expected-tier="geek" />
    <gap slug="deploy-blue-green-canary" expected-tier="geek" />
    <gap slug="prd-template" expected-tier="geek" />
  </gaps>
  <extra key="context" value="[team]" />
  <extra key="token_budget_estimate" value="18000" />
</playbook>
