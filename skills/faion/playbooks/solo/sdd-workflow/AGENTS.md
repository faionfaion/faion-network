# Solo / SDD Workflow

Lightweight Spec-Driven Development for one-person teams. Citation scope: `knowledge/free/ + solo/`.

| Slug | Goal |
|------|------|
| `writing-first-spec` | Turn a one-line idea into a spec.md with F1-Fn + AC1-ACn |
| `sdd-for-solos` | Minimal SDD discipline for solo developers |
| `scope-cutting` | Halve a feature's AC list within a spec |
| `spec-to-code-pipeline` | Hand off design.md → TASK_NN.md → agent → PR |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
