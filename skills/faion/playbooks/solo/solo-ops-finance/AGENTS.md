# Solo / Operations and Finance

The non-product side: runway, taxes, time-tracking. Citation scope: `knowledge/free/ + solo/`.

| Slug | Goal |
|------|------|
| `runway-calc` | Compute runway months and pick a trigger to switch to revenue mode |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
