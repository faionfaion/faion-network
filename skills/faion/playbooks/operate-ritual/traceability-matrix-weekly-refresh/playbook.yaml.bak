# faion playbook manifest — role-ba
# Generated for F-059-063 chunk-03; persona=role-ba, angle=atomic

slug: traceability-matrix-weekly-refresh
title: "Traceability matrix weekly refresh"
version: "1.0.0"
status: draft
last_reviewed: 2026-05-20
maintainers: ["faion-network"]
content_id: 34e5cbd69d674382

tier_min: pro
complexity: light
context: [solo, team]
persona: role-ba
group: business-analyst
angle: atomic
persona_hints: [role-ba]

intent: |
  Once a week the matrix reflects every shipped story, every open AC, and every stranded requirement.
  Stranded items get an owner and a date by end of session.

scope: |
  Once a week the matrix reflects every shipped story, every open AC, and every stranded requirement.
  Stranded items get an owner and a date by end of session.

summary: |
  Once a week the matrix reflects every shipped story, every open AC, and every stranded requirement.
  Stranded items get an owner and a date by end of session.

methodology_refs:
  - "requirements-lifecycle"
  - "requirements-traceability"

success_criteria:
  - "Prep stage complete with written artifact and decision-gate note"
  - "Frame stage complete with written artifact and decision-gate note"
  - "Execute stage complete with written artifact and decision-gate note"
  - "Capture stage complete with written artifact and decision-gate note"
  - "Decide stage complete with written artifact and decision-gate note"

stages:
  - name: "Prep"
    intent: "Get inputs, environment, and prior context lined up before the session starts."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: requirements-lifecycle
        path: pro/ba/ba-core/requirements-lifecycle
        required: true
        tier_gate: pro
    outputs:
      - "Written artifact for stage 1 (Prep) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 1 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Frame"
    intent: "Name the single outcome and the boundaries; lock the inputs in writing."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: requirements-management
        path: pro/ba/ba-core/requirements-management
        required: true
        tier_gate: pro
    outputs:
      - "Written artifact for stage 2 (Frame) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 2 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Execute"
    intent: "Run the core activity end-to-end without context switches."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: requirements-traceability
        path: pro/ba/ba-core/requirements-traceability
        required: true
        tier_gate: pro
    outputs:
      - "Written artifact for stage 3 (Execute) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 3 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Capture"
    intent: "Write the outputs down in a form a teammate can read later."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: requirements-traceability
        path: pro/ba/business-analyst/requirements-traceability
        required: true
        tier_gate: pro
    outputs:
      - "Written artifact for stage 4 (Capture) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 4 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Decide"
    intent: "Apply the decision gate; produce a go/no-go or next-action note."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
    outputs:
      - "Written artifact for stage 5 (Decide) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 5 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."

common_pitfalls:
  - "Skipping decision-gate verification collapses the playbook into a checklist"
  - "Treating outputs as optional kills traceability and future re-use"
  - "Running stages out of order because one feels more urgent absorbs risk downstream"

quality_checklist:
  - "Did every stage produce a written artifact a stranger could read?"
  - "Did I advance only when the decision-gate criterion was met in writing?"
  - "Did I document every methodology gap before it shipped to a user?"

related_playbooks: []

token_budget_estimate: 18000
cli_invocations:
  human:  "faion get-content traceability-matrix-weekly-refresh --format md"
  agent:  "faion get-content traceability-matrix-weekly-refresh --format context"
  json:   "faion get-content traceability-matrix-weekly-refresh --format json"

changelog:
  - version: "1.0.0"
    date: 2026-05-20
    note: "Initial draft from F-059-063 chunk-03; persona=role-ba, angle=atomic"

gaps:
  - methodology_slug: jira-trace-export-script
    expected_tier: pro
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: requirements-management
    expected_tier: pro
    blocks_stage: 2
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: traceability-matrix-template-csv
    expected_tier: pro
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
