# faion playbook manifest — P7
# Generated for F-059-063 chunk-03; persona=P7, angle=global

slug: methodology-corpus-integration-faion-into-our-agent-2-weeks
title: "Methodology corpus integration: faion-into-our-agent (2 weeks)"
version: "1.0.0"
status: draft
last_reviewed: 2026-05-20
maintainers: ["faion-network"]
content_id: df37d079558eddbc

tier_min: geek
complexity: medium
context: [solo]
persona: P7
group: llm-agent
angle: global
persona_hints: [P7]

intent: |
  Embed the faion knowledge base as a reasoning prior for an in-house vertical agent: ingestion,
  chunking, retrieval, reranking, eval, plus governance / licence / drift hygiene.

scope: |
  Embed the faion knowledge base as a reasoning prior for an in-house vertical agent: ingestion,
  chunking, retrieval, reranking, eval, plus governance / licence / drift hygiene.

summary: |
  Embed the faion knowledge base as a reasoning prior for an in-house vertical agent: ingestion,
  chunking, retrieval, reranking, eval, plus governance / licence / drift hygiene.

methodology_refs:
  - "auto-evict-tool-results"
  - "compaction-preserve-refs"
  - "file-reference-passing"
  - "manifest-then-fetch"
  - "progressive-disclosure-skills"
  - "rerank-before-reasoning"
  - "terse-default-tool-output"
  - "embeddings-batch-and-cache"
  - "embeddings-evaluation"
  - "eu-ai-act-compliance"
  - "chunking-document-structure"
  - "chunking-production-service"

success_criteria:
  - "Frame stage complete with written artifact and decision-gate note"
  - "Discover stage complete with written artifact and decision-gate note"
  - "Design stage complete with written artifact and decision-gate note"
  - "Build stage complete with written artifact and decision-gate note"
  - "Validate stage complete with written artifact and decision-gate note"
  - "Close stage complete with written artifact and decision-gate note"

stages:
  - name: "Frame"
    intent: "State the outcome, constraints, and exit-condition in one paragraph."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: auto-evict-tool-results
        path: geek/ai/ai-agents/auto-evict-tool-results
        required: true
        tier_gate: geek
      - slug: terse-default-tool-output
        path: geek/ai/ai-agents/terse-default-tool-output
        required: true
        tier_gate: geek
      - slug: chunking-semantic
        path: geek/ai/rag-engineer/chunking-semantic
        required: true
        tier_gate: geek
      - slug: rag-eval-production-monitoring
        path: geek/ai/rag-engineer/rag-eval-production-monitoring
        required: true
        tier_gate: geek
      - slug: kb-codebase-rag-symbol-chunked
        path: geek/sdlc-ai/kb-codebase-rag-symbol-chunked
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 1 (Frame) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 1 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Discover"
    intent: "Pull evidence and prior art before any design decision."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: compaction-preserve-refs
        path: geek/ai/ai-agents/compaction-preserve-refs
        required: true
        tier_gate: geek
      - slug: embeddings-batch-and-cache
        path: geek/ai/ml-engineer/embeddings-batch-and-cache
        required: true
        tier_gate: geek
      - slug: embedding-caching
        path: geek/ai/rag-engineer/embedding-caching
        required: true
        tier_gate: geek
      - slug: rag-eval-retrieval-metrics
        path: geek/ai/rag-engineer/rag-eval-retrieval-metrics
        required: true
        tier_gate: geek
      - slug: kb-versioned-agent-memory-files
        path: geek/sdlc-ai/kb-versioned-agent-memory-files
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 2 (Discover) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 2 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Design"
    intent: "Sketch target structure with explicit cuts and trade-offs."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: file-reference-passing
        path: geek/ai/ai-agents/file-reference-passing
        required: true
        tier_gate: geek
      - slug: embeddings-evaluation
        path: geek/ai/ml-engineer/embeddings-evaluation
        required: true
        tier_gate: geek
      - slug: embedding-model-selection
        path: geek/ai/rag-engineer/embedding-model-selection
        required: true
        tier_gate: geek
      - slug: reranking-diversity-mmr
        path: geek/ai/rag-engineer/reranking-diversity-mmr
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 3 (Design) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 3 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Build"
    intent: "Implement the smallest version that proves the chain works."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: manifest-then-fetch
        path: geek/ai/ai-agents/manifest-then-fetch
        required: true
        tier_gate: geek
      - slug: eu-ai-act-compliance
        path: geek/ai/ml-engineer/eu-ai-act-compliance
        required: true
        tier_gate: geek
      - slug: hybrid-search-implementation
        path: geek/ai/rag-engineer/hybrid-search-implementation
        required: true
        tier_gate: geek
      - slug: reranking-pipeline-integration
        path: geek/ai/rag-engineer/reranking-pipeline-integration
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 4 (Build) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 4 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Validate"
    intent: "Test against the rubric or real data before broad rollout."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: progressive-disclosure-skills
        path: geek/ai/ai-agents/progressive-disclosure-skills
        required: true
        tier_gate: geek
      - slug: chunking-document-structure
        path: geek/ai/rag-engineer/chunking-document-structure
        required: true
        tier_gate: geek
      - slug: rag-architecture
        path: geek/ai/rag-engineer/rag-architecture
        required: true
        tier_gate: geek
      - slug: reranking-two-stage
        path: geek/ai/rag-engineer/reranking-two-stage
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 5 (Validate) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 5 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Close"
    intent: "Apply decision gate, document outcome, archive for re-use."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: rerank-before-reasoning
        path: geek/ai/ai-agents/rerank-before-reasoning
        required: true
        tier_gate: geek
      - slug: chunking-production-service
        path: geek/ai/rag-engineer/chunking-production-service
        required: true
        tier_gate: geek
      - slug: rag-eval-pipeline
        path: geek/ai/rag-engineer/rag-eval-pipeline
        required: true
        tier_gate: geek
      - slug: gov-license-compliance-scan
        path: geek/sdlc-ai/gov-license-compliance-scan
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 6 (Close) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 6 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."

common_pitfalls:
  - "Skipping decision-gate verification collapses the playbook into a checklist"
  - "Treating outputs as optional kills traceability and future re-use"
  - "Running stages out of order because one feels more urgent absorbs risk downstream"

quality_checklist:
  - "Did every stage produce a written artifact a stranger could read?"
  - "Did I advance only when the decision-gate criterion was met in writing?"
  - "Did I document every methodology gap before it shipped to a user?"

related_playbooks: []

token_budget_estimate: 18000
cli_invocations:
  human:  "faion get-content methodology-corpus-integration-faion-into-our-agent-2-weeks --format md"
  agent:  "faion get-content methodology-corpus-integration-faion-into-our-agent-2-weeks --format context"
  json:   "faion get-content methodology-corpus-integration-faion-into-our-agent-2-weeks --format json"

changelog:
  - version: "1.0.0"
    date: 2026-05-20
    note: "Initial draft from F-059-063 chunk-03; persona=P7, angle=global"

gaps:
  - methodology_slug: faion-cli-as-agent-skill
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: methodology-corpus-licence-bundle
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: retrieval-drift-alerting-recipe
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
