# Geek / Context Engineering

Context engineering playbooks for indie AI product founders and AI-builder power users. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `prompt-caching-anthropic` | Cache large system prompts with Anthropic cache_control breakpoints to cut repeated-query costs by up to 10x |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
