# Geek / Cost Optimization

Cost optimization playbooks for AI builders. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `model-routing-cheap-vs-strong` | Build a two-stage classifier routing prompts to Haiku/Sonnet/Opus for ~6x cost savings |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
