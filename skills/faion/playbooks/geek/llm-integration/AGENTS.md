# Geek / LLM Integration

LLM provider integration playbooks for AI product founders and agent engineers. Citation scope: all four tiers.

| Slug | Goal |
|------|------|
| `llm-fallback-chains` | Ordered provider failover chain with tenacity retries and failover-rate telemetry |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
