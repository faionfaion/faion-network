# Geek / MCP Protocol

Model Context Protocol — building servers and integrating into Claude Code. Citation scope: all four tiers.

| Slug | Goal |
|------|------|
| `mcp-server-build` | Build an MCP server (stdio + HTTP transports, schema-validated tools) |
| `mcp-claude-code-integration` | Register and consume external MCP servers in Claude Code |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
