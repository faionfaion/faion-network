# Geek / AI Agents

AI agent engineering playbooks for agent builders. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `multi-agent-orchestration` | Orchestrate parallel Claude subagents with shared state and fault-tolerant coordination |
| `react-loop-production` | Build a production ReAct loop with tool_use/tool_result blocks, halt conditions, retries, and JSONL trace logging |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
