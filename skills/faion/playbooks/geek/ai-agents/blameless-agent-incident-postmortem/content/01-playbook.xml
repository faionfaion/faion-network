<?xml version="1.0" encoding="UTF-8"?>
<playbook id="01-playbook" slug="blameless-agent-incident-postmortem" goal="fix-incident" complexity="medium">
  <intent>Agent shipped hallucinated answer / mis-called tool / jailbroke itself → blameless postmortem attributes cause to layer, lands regression eval, prevents recurrence.</intent>
  <scope>When the agent ships a hallucinated answer, mis-calls a tool, or jailbreaks itself, the team produces a structured postmortem that attributes cause to model/prompt/tool/context layer, lands a regression eval, and prevents recurrence.</scope>
  <success-criteria>
    <criterion>Postmortem doc written</criterion>
    <criterion>Layer attribution: model / prompt / tool / context</criterion>
    <criterion>Regression eval row landed before fix</criterion>
    <criterion>Action items tracked</criterion>
  </success-criteria>
  <stages>
    <stage n="1" name="Reconstruct">
      <intent>Replay the bad trajectory.</intent>
      <tasks>
        <task>Pull trace</task>
        <task>Record-replay</task>
      </tasks>
      <methodologies>
        <ref slug="record-replay-debugging" />
        <ref slug="trajectory-eval-otel" />
      </methodologies>
      <outputs>
        <output>Reproduced incident</output>
      </outputs>
      <decision-gate>Advance only when failure reproduces.</decision-gate>
    </stage>
    <stage n="2" name="Attribute">
      <intent>Layer-by-layer attribution with chaos cross-check.</intent>
      <tasks>
        <task>Chaos-eval cross-check</task>
        <task>Generator-critic bound check</task>
      </tasks>
      <methodologies>
        <ref slug="chaos-eval-fault-injection" />
        <ref slug="generator-critic-bounded-loop" />
      </methodologies>
      <outputs>
        <output>Attribution note</output>
      </outputs>
      <decision-gate>Advance only when one layer is clearly implicated.</decision-gate>
    </stage>
    <stage n="3" name="Prevent">
      <intent>Land regression eval + auto-draft postmortem.</intent>
      <tasks>
        <task>Add regression eval BEFORE the fix</task>
        <task>Auto-draft postmortem; gate publish</task>
        <task>Track action items</task>
      </tasks>
      <methodologies>
        <ref slug="inc-postmortem-auto-draft-no-publish" />
      </methodologies>
      <outputs>
        <output>Postmortem doc</output>
        <output>Eval row added</output>
      </outputs>
      <decision-gate>Close only when postmortem is signed and eval row landed.</decision-gate>
    </stage>
  </stages>
  <gaps>
    <gap slug="agent-incident-postmortem-template" expected-tier="geek" />
    <gap slug="agent-failure-taxonomy" expected-tier="geek" />
    <gap slug="regression-eval-before-fix-rule" expected-tier="geek" />
  </gaps>
  <extra key="context" value="[team]" />
  <extra key="non_goals" value="[Customer comms response — out of scope, Long-term architecture rewrite]" />
  <extra key="prerequisites" value="[Trace of the incident, Eval harness reachable]" />
  <extra key="common_pitfalls" value="[Treating eval scores as ground truth without judge calibration, Shipping prompt or model changes without a regression gate]" />
  <extra key="quality_checklist" value="['Can I roll back this change in one step?', 'Is the regression eval committed BEFORE the fix?']" />
  <extra key="related_playbooks" value="[eval-harness-continuous-benchmark-suite, agent-observability-drift-detection-rollout]" />
  <extra key="token_budget_estimate" value="9000" />
  <extra key="cli_invocations" value="{human: faion get-content blameless-agent-incident-postmortem --format md, agent: faion get-content blameless-agent-incident-postmortem --format context, json: faion get-content blameless-agent-incident-postmortem --format json}" />
  <extra key="changelog" value="[{version: 0.1.0, date: 2026-05-20, note: Initial draft for F-064 P7 llm-agent-developer persona}]" />
</playbook>
