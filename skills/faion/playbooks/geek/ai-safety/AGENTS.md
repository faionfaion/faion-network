# Geek / AI Safety

AI safety and guardrails playbooks for AI builders. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `pii-redaction-pipeline` | Redact PII before sending to an LLM and restore original values in the response using Microsoft Presidio and the Anthropic SDK |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
