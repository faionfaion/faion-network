# Geek / RAG Pipelines

RAG pipeline engineering playbooks for AI builders. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `rag-chunking-benchmark` | Benchmark fixed, semantic, and recursive chunking on a 50-doc corpus to pick the best strategy by recall@5 and answer F1 |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
