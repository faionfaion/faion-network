# Geek / Multimodal

Multimodal AI pipeline playbooks for AI builders. Citation scope: all four tiers (`knowledge/free/ + solo/ + pro/ + geek/`).

| Slug | Goal |
|------|------|
| `ocr-pipeline-vision-llm` | Extract invoice text and table structure from PDF pages using the Anthropic vision API and validate into a Pydantic schema |

Spec: `../../../../../.aidocs/conventions/playbooks/playbook-spec.md`. Validator: `python3 scripts/validate-tier-playbook.py <path>`.
