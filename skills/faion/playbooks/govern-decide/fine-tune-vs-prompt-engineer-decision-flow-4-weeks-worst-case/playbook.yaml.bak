# faion playbook manifest — P7
# Generated for F-059-063 chunk-03; persona=P7, angle=global

slug: fine-tune-vs-prompt-engineer-decision-flow-4-weeks-worst-case
title: "Fine-tune vs prompt-engineer decision flow (4 weeks worst case)"
version: "1.0.0"
status: draft
last_reviewed: 2026-05-20
maintainers: ["faion-network"]
content_id: 0bcbb758873dd0dc

tier_min: geek
complexity: deep
context: [solo]
persona: P7
group: llm-agent
angle: global
persona_hints: [P7]

intent: |
  Structured decision and execution path: from baseline scorecard to a clear go/no-go on fine-tuning,
  including LoRA SFT/DPO data prep, training, eval, and deployment — or staying with prompt+retrieval
  if economics don't beat the floor.

scope: |
  Structured decision and execution path: from baseline scorecard to a clear go/no-go on fine-tuning,
  including LoRA SFT/DPO data prep, training, eval, and deployment — or staying with prompt+retrieval
  if economics don't beat the floor.

summary: |
  Structured decision and execution path: from baseline scorecard to a clear go/no-go on fine-tuning,
  including LoRA SFT/DPO data prep, training, eval, and deployment — or staying with prompt+retrieval
  if economics don't beat the floor.

methodology_refs:
  - "confidence-thresholded-cascade"
  - "record-replay-debugging"
  - "role-specialized-models"
  - "schema-version-pinning"
  - "cost-optimization"
  - "decision-framework"
  - "fine-tuning-lora"
  - "fine-tuning-openai-data-prep"
  - "fine-tuning-openai-deployment"
  - "fine-tuning-openai-dpo"
  - "fine-tuning-openai-eval"
  - "fine-tuning-openai-sft"

success_criteria:
  - "Frame stage complete with written artifact and decision-gate note"
  - "Discover stage complete with written artifact and decision-gate note"
  - "Design stage complete with written artifact and decision-gate note"
  - "Build stage complete with written artifact and decision-gate note"
  - "Validate stage complete with written artifact and decision-gate note"
  - "Ship stage complete with written artifact and decision-gate note"
  - "Close stage complete with written artifact and decision-gate note"

stages:
  - name: "Frame"
    intent: "State the outcome, constraints, and exit-condition in one paragraph."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: confidence-thresholded-cascade
        path: geek/ai/ai-agents/confidence-thresholded-cascade
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-data-prep
        path: geek/ai/ml-engineer/fine-tuning-openai-data-prep
        required: true
        tier_gate: geek
      - slug: prompt-engineering-evaluation
        path: geek/ai/ml-engineer/prompt-engineering-evaluation
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 1 (Frame) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 1 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Discover"
    intent: "Pull the evidence and prior art needed to make informed choices."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: record-replay-debugging
        path: geek/ai/ai-agents/record-replay-debugging
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-deployment
        path: geek/ai/ml-engineer/fine-tuning-openai-deployment
        required: true
        tier_gate: geek
      - slug: cost-reduction-strategies
        path: geek/ai/ml-ops/cost-reduction-strategies
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 2 (Discover) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 2 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Design"
    intent: "Sketch the target system / process / artifact before touching anything live."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: role-specialized-models
        path: geek/ai/ai-agents/role-specialized-models
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-dpo
        path: geek/ai/ml-engineer/fine-tuning-openai-dpo
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-basics
        path: geek/ai/ml-ops/fine-tuning-openai-basics
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 3 (Design) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 3 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Build"
    intent: "Implement the smallest version that exercises the validated chain end-to-end."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: schema-version-pinning
        path: geek/ai/ai-agents/schema-version-pinning
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-eval
        path: geek/ai/ml-engineer/fine-tuning-openai-eval
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-production
        path: geek/ai/ml-ops/fine-tuning-openai-production
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 4 (Build) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 4 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Validate"
    intent: "Stress-test against the rubric, real users / data, or eval harness."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: cost-optimization
        path: geek/ai/ml-engineer/cost-optimization
        required: true
        tier_gate: geek
      - slug: fine-tuning-openai-sft
        path: geek/ai/ml-engineer/fine-tuning-openai-sft
        required: true
        tier_gate: geek
      - slug: finetuning-basics
        path: geek/ai/ml-ops/finetuning-basics
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 5 (Validate) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 5 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Ship"
    intent: "Roll out to the target audience with rollback path and observability."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: decision-framework
        path: geek/ai/ml-engineer/decision-framework
        required: true
        tier_gate: geek
      - slug: finetuning
        path: geek/ai/ml-engineer/finetuning
        required: true
        tier_gate: geek
      - slug: finetuning-datasets
        path: geek/ai/ml-ops/finetuning-datasets
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 6 (Ship) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 6 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."
  - name: "Close"
    intent: "Decide go/no-go, write the postmortem, and archive artifacts for re-use."
    tasks:
      - "Read the assigned methodology references and reconcile them with this stage's goal."
      - "Produce the stage artifact named in 'outputs' and store it where the next stage can read it."
      - "Re-check the decision gate before advancing; loop within this stage if it fails."
    methodologies:
      - slug: fine-tuning-lora
        path: geek/ai/ml-engineer/fine-tuning-lora
        required: true
        tier_gate: geek
      - slug: llm-decision-framework
        path: geek/ai/ml-engineer/llm-decision-framework
        required: true
        tier_gate: geek
      - slug: lora-qlora
        path: geek/ai/ml-ops/lora-qlora
        required: true
        tier_gate: geek
    outputs:
      - "Written artifact for stage 7 (Close) named per the project's docs convention"
      - "Decision-gate note (advance / iterate / kill) appended to the playbook log"
    decision_gate: "Advance when the stage 7 artifact exists in writing AND the methodology chain assigned to this stage has been applied. Iterate or escalate otherwise."

common_pitfalls:
  - "Skipping decision-gate verification collapses the playbook into a checklist"
  - "Treating outputs as optional kills traceability and future re-use"
  - "Running stages out of order because one feels more urgent absorbs risk downstream"

quality_checklist:
  - "Did every stage produce a written artifact a stranger could read?"
  - "Did I advance only when the decision-gate criterion was met in writing?"
  - "Did I document every methodology gap before it shipped to a user?"

related_playbooks: []

token_budget_estimate: 18000
cli_invocations:
  human:  "faion get-content fine-tune-vs-prompt-engineer-decision-flow-4-weeks-worst-case --format md"
  agent:  "faion get-content fine-tune-vs-prompt-engineer-decision-flow-4-weeks-worst-case --format context"
  json:   "faion get-content fine-tune-vs-prompt-engineer-decision-flow-4-weeks-worst-case --format json"

changelog:
  - version: "1.0.0"
    date: 2026-05-20
    note: "Initial draft from F-059-063 chunk-03; persona=P7, angle=global"

gaps:
  - methodology_slug: fine-tune-vs-prompt-economic-model
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: production-trace-mining-for-training-data
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
  - methodology_slug: tuned-model-shadow-deploy-protocol
    expected_tier: geek
    blocks_stage: 1
    note: "Referenced by playbook chain but not yet authored under KNOWLEDGE_ROOT (or refers to a playbook, not a methodology)."
