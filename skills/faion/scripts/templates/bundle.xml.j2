<faion_knowledge>
  <summary files="{{ files|length }}" total_words="{{ total_words }}" budget="{{ word_budget }}"/>

{% for doc in documents %}
  <document path="{{ doc.path }}"{% if doc.reason %} reason="{{ doc.reason | e }}"{% endif %} words="{{ doc.words }}">
<![CDATA[
{{ doc.content }}
]]>
  </document>

{% endfor %}
</faion_knowledge>
