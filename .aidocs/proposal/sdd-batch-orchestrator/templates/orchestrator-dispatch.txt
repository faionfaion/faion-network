Read <abs-path-to-skill>/prompts/<NN-phase-name>.md and execute it with these inputs:

  <param-1>: <value>
  <param-2>: <value>
  <optional-param>: <value or omit>

Return the report shape declared in the prompt file's `Output` section.
